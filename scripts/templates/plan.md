# {{NN}}. {{TITLE}}

> {{TITLE}} — 한 줄 요약 | 범위: FE|BE|FS | 상태: 기획
> /create-plan이 사용자 인터뷰로 채운다. plan은 *무엇이 참이어야 하나*(WHAT)를 다른 클라이언트로도 재구현할 만큼 적는다.

## 목적
<!-- 왜 이 기능인가. 사용자·제품 관점. -->

## 범위 / 비목표
<!-- 무엇을 포함하고, 무엇은 명시적으로 안 하나(비목표가 중요). -->

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](00.overview.md) §불변 원칙
- tech/policy/concept 근거: <!-- 의존하는 문서 -->

## 설계 요점
<!-- 어떻게 구현하나 — 핵심 결정·계약(proto/RPC)·데이터 모델·상태머신 등. -->

## 수용 기준 (인수 조건)
<!-- 평서문/EARS. 이 기능이 "참"이 되는 판정 기준. job의 "인수 조건" 섹션으로 복사된다. -->
1.

## 영향 파일
<!-- 건드릴 정확한 경로(예상 blast radius). -->

## 정책 영향 (policy / values)
<!-- policy/**에 반영할 규칙 + spec/values.yaml에 둘 **모든 config/튜닝 수치**(임계값·계수·상한·기본값…). config는 코드 하드코딩 금지 — values.yaml 단일 출처 → pnpm gen:values로 FE/BE 상수 생성·import. (공식·배열 콘텐츠·proto/DB 제외, 카운트는 배열 길이 파생.) /create-plan이 해당 policy·ux 문서도 함께 만든다/고친다. -->
