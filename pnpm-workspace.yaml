packages:
  - "apps/web"
  - "apps/mobile"
  - "apps/blog"
  - "packages/*"
  # apps/api is the Go backend, driven by the Go toolchain — not a pnpm member.
  # apps/*-mvp are reference-only and deliberately omitted so a clean install never touches them.
