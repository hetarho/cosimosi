// Cross-import public API (FSD `@x`) — entities/synapse 전용. VizSynapse의 concept(별 종류) 타입만
// 노출한다. synapse는 이 파일에서만 star를 가져온다.
export type { StarObject } from '../model/types'
