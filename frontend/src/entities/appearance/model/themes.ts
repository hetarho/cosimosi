// @deprecated 정명은 ./backgrounds(spec 44). 이 파일은 옛 themes.ts import 경로의 호환 re-export만 남긴다.
// 새 코드는 ./backgrounds(BACKGROUNDS·DEFAULT_BACKGROUND·paletteForBackground)를 직접 쓴다.
export {
  BACKGROUNDS,
  DEFAULT_BACKGROUND,
  backgroundMeta,
  themeAccent,
  themeBg,
  paletteForBackground,
  THEMES,
  DEFAULT_THEME,
} from './backgrounds'
