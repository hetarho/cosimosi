// Cross-import public API (FSD `@x`) — entities/theory 전용. 이론 데모(SilentEngramDemo)가
// 실제 망각 모델의 바닥값·변조 감쇠식으로 시연하도록 노출한다(시연·실제 표류 방지).
export {
  A_MIN,
  modulatedBrightness,
} from '../model/activation'
