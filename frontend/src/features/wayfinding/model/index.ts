// wayfinding/model — frame-all 기하(centroid·반경·margin)만 남는 순수 모델. 강조/프레임 상태는
// entities/memory의 focus 머신으로 이전(spec 39). No three/React/DOM (헌법4).
export { frameTarget, FRAME_MARGIN, FRAME_MIN_DISTANCE, type FrameTarget } from './frame'
