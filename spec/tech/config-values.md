# tech: config values

> As-built rules for `spec/values.yaml`, generated TypeScript constants, and generated Go constants. The architecture
> rule is [ARCHITECTURE.md](../ARCHITECTURE.md) §4: tuning scalars are build-time generated config, not runtime mutable
> state.

## 1. Source and ownership

`spec/values.yaml` is the committed source for product-shaping constants that must stay identical across frontend and
backend builds. Its TypeScript and Go outputs are generated by `pnpm gen:values` and checked by `pnpm check:gen`;
generated files are never edited by hand.

Values are build-time config. They are not remote config, environment config, feature flags, formulas, copy, schema, or
theme content.

## 2. Schema

Groups, generated keys, and scalar-map keys use `snake_case`. The generator accepts:

- finite numeric scalars;
- non-empty numeric arrays;
- string scalars;
- non-empty one-level scalar maps whose values are all finite numbers or all strings.

The generator rejects empty YAML, non-finite numbers, invalid group/key casing, generated-name collisions, empty
arrays/maps, nested arrays, nested maps, and mixed-type scalar maps with clear `values.yaml: ...` errors.

## 3. Generated outputs

`pnpm gen:values` writes:

- `packages/config/src/values.gen.ts`, exported through `@cosimosi/config`; this package is platform-pure and importable
  by web, mobile, and Node tests;
- `apps/api/internal/platform/values/values_gen.go`, an API platform package outside product domain packages.

`pnpm gen` runs values generation before message, token, proto, and sql generation.

## 4. What stays out

Do not put these in `spec/values.yaml`:

- runtime environment, deployment config, URLs, DSNs, API keys, Supabase settings, or secrets;
- feature flags or remote-config values;
- proto contracts, SQL schemas, or DB migration choices;
- product formulas whose behavior belongs in code;
- UI copy, locale data, design tokens, theme CSS, or mood/color content tables;
- array lengths that can be derived from the array content itself.

Feature-flag defaults remain in the observability/flags registry. Design tokens remain in `packages/ui/src/tokens.ts`
and generated theme CSS. Runtime settings remain environment/ops config.
