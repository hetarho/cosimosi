---
review: "03"
status: resolved
title: Web FSD and Workflow Instruction Audit
created: 2026-07-01
scope: code-quality
---

# Code Review 03: Web FSD and Workflow Instruction Audit

> Read-only audit of why `apps/web/src/app` stayed flat while the project claims FSD, and why the
> `/create-plan` → `/create-plan-job` → `/implement-job` workflow never forced the architecture into the resulting
> code. The FSD-structure findings have since been **implemented** (see Resolution); the full original audit body was
> trimmed once resolved — recover it from git history (commit `d440c12`) if the detailed evidence is ever needed. The
> remaining follow-ups are tracked below.

## Resolution (implemented 2026-07-01)

The FSD-structure findings were implemented directly (user chose to fix, not to file a separate refactor job). Status
→ `resolved`. Per-finding:

- **R001 ✅** Web app layer moved from flat files into segments (`app/providers/`, `app/styles/`); mobile loose files
  (`api-client.probe.ts` → `app/providers/`, `ui-showcase.stories.tsx` → `app/diagnostics/`) segmented too.
  `ARCHITECTURE.md` §3.1 now documents the app-layer segment contract explicitly (the `app/` sub-tree + the
  "non-sliced layers are still segmented" rule). New executable guard `scripts/lint-fsd-layout.mjs`
  (`pnpm lint:fsd:layout`) fails on loose app-layer files + generic-type folders, wired into root `lint` + pre-commit.
- **R002 ✅** New placement gate skills `/fe-architecture` · `/be-architecture` · `/mobile-architecture` (thin —
  reference `ARCHITECTURE.md` §3/§2 + a decision procedure + self-audit; in `.claude/skills/` and `.codex/skills/`).
  `/implement-job` (both copies) now reads `ARCHITECTURE.md` in grounding and invokes the placement skill before writing
  + re-audits at review. `/create-plan` + `/create-plan-job` + `scripts/templates/job.md` now require target placement.
- **R004 ✅ (instance)** Job 17 reconciled: `status: done` made consistent with the body, A9/T011 corrected to
  "built + source-gate verified · on-device pending", archived to `spec/jobs/archive/17.*.md`; the on-device gate is
  tracked in plan/14 As-built + `00.overview`.
- **R005 ✅** Plan 14 AC #1/#2/#9 + Scope/Design + supersession note now name the `@cosimosi/3d-renderer` package; the
  eslint `no-restricted-imports` message points at the package (not the superseded `shared/lib`).

## Open follow-ups (tracked, not done here)

- **R003 ⏳** Skill/template grounding refresh — `/create-plan` still cites non-existent example plans (`plan/21`,
  `plan/30`; repo has 01–14, and the `.claude` copy's link depth is wrong) and templates say "8 invariants"/`§불변 원칙`
  while `00.overview` uses `[I1]–[I11]`. Add a link/anchor check. → job `workflow-skill-template-grounding-refresh`.
- **R004 ⏳ (gate)** A spec-workflow consistency lint: fail when a `status: done` job has unchecked acceptance/DoD boxes
  or was not archived; teach `/implement-job` to run it. → job `spec-workflow-state-consistency-gate`.
- **R006 ⏳** Comment-history lint for source roots + clean the `UniverseHomePage.tsx` process-narration comment
  (`Epic A` / "foundation shell" narration). → job `comment-history-lint`.
