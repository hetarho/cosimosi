---
job: "32"
type: change
source: changes/18.radial-degree-drift
plan: plan/38.self-anchored-layout
status: done
title: 연결 가중 차등 표류 + 포화 해소(R_MAX 2배)
---

# Job 32: 연결 가중 차등 표류 + 포화 해소(R_MAX 2배)  (change)

> 구현 작업 문서. 출처 스펙: [changes/18.radial-degree-drift](../changes/18.radial-degree-drift.md).
> `/implement-job 32`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/18.radial-degree-drift에서)
- [x] A1 활성도·시간이 같고 연결성만 다른 두 별 중, **연결성(degree+Σweight)이 큰 별이 중심에 더 가깝다**(작은 반지름).
- [x] A2 연결성이 0에 가까운 약한 별은 같은 Δt에서 연결성 큰 별보다 **유의미하게 더 멀리** 간다.
- [x] A3 연결성 항은 감쇠를 **늦추기만** — 어떤 입력으로도 반지름이 R_MIN 아래거나 무연결 별보다 더 멀어지지 않는다.
- [x] A4 R_MAX=80이고, 가장 오래된 별 반지름이 그 사이를 단조 증가로 채운다.
- [x] A5 한 달·두 달 시점에서 먼 별 반지름이 **계속 증가**(두 시점 차이 ≠ 0).
- [x] A6 R_MAX(80)에 닿은 별은 반지름은 안 늘지만 각도 드리프트로 가장자리에서 회전(완전 정지 아님).
- [x] A7 회상 시 활성도↑ → 반지름↓(중앙 복귀) 그대로 작동(회귀 없음).
- [x] A8 라이브·오버레이·변천사 스크러버 **세 표면 모두** 동일 새 공식으로 배치.
- [x] A9 어떤 별·시냅스도 삭제 안 됨, 각도는 `prevPos` resume으로 보존.

## 구현 체크리스트 (implementation — 구현 관점)
- [x] T001 (gen:values) `spec/values.yaml`: `radialLayout.rMax` 40→80, 연결성 가중 계수 신규(`connDriftAlpha` 등 degree/Σweight 항), 포화 재매핑 계수. `pnpm gen:values`로 FE/BE 상수 생성.
- [x] T002 [weight.ts](../../frontend/src/entities/memory/model/weight.ts) `memoryR`/강도 계산에 **연결성 입력**(degreeNorm + weightedDegreeNorm) 추가 — 연결 강할수록 R 유지(감쇠 지연). 입력은 호출부에서 주입.
- [x] T003 [layout.ts](../../frontend/src/shared/lib/layout.ts) `targetRadius`: R_MAX 80 반영 + 포화 재매핑(먼 구간이 일찍 R_MAX에 안 붙도록). R_MIN→밝음 단조 보존.
- [x] T004 [radial-layout.ts](../../frontend/src/widgets/universe-canvas/model/radial-layout.ts) `radiusOf` 시그니처 확장 — degree/Σweight를 받아 weight.ts에 전달.
- [x] T005 호출부 3곳에서 연결성(degree·Σweight) 산출·전달: 라이브 [layout 컨트롤러](../../frontend/src/widgets/universe-canvas/ui/controllers/layout.tsx), 오버레이 [OverlayUniverse](../../frontend/src/widgets/universe-canvas/ui/overlay/OverlayUniverse.tsx), 변천사 스크러버. (degreeNormById/weightedDegreeById 기존 헬퍼 재사용 — StarField 참고.)
- [x] T006 가장자리 회전: R_MAX 도달 별이 `applyAngularDrift`로 계속 회전하는지 확인(이미 있으면 배선만).
- [x] T007 plan 38 문서의 "Flutter 동등성" 옛 strength 공식 → Bjork R + 연결성 기준으로 정합.
- [x] T008 단위 테스트: `radiusOf`/`targetRadius`/연결성 항(차등·단조·바닥·포화) — `weight.test.ts`·`layout.test.ts` 확장.

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 — 헌법2·3
- 건드리는 tech/policy/values: [policy/domain/universe.md](../policy/domain/universe.md)(R_MIN/R_MAX), `spec/values.yaml`
- 짝 job: 33(밝기=반지름) — 33이 이 반지름을 읽어 밝기로 변환하므로 32 먼저.

## 영향 파일 (blast radius)
- `frontend/src/entities/memory/model/weight.ts`, `frontend/src/shared/lib/layout.ts`, `frontend/src/widgets/universe-canvas/model/radial-layout.ts`
- 호출부: `frontend/src/widgets/universe-canvas/ui/controllers/layout.tsx`, `.../overlay/OverlayUniverse.tsx`, 변천사 스크러버
- 테스트: `weight.test.ts`, `layout.test.ts`; `spec/values.yaml`, `spec/policy/domain/universe.md`, `spec/plan/38.self-anchored-layout.md`

## 검증 / DoD
- [x] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [x] 기존 plan 38 수용 기준 회귀 없음(회상→중앙 복귀, 동심 분포 등)
- [x] `pnpm gen:values` 적용
- [x] FE `--filter ./frontend build`·`lint` 통과
- [x] 헌법 sanity: 행 삭제·records 본문 UPDATE 없음

## 리뷰
- [x] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [x] `plan/38`을 새 현실로 갱신 · `policy/domain/universe.md` · `spec/values.yaml`(+`pnpm gen:values`)
- [x] `changes/18.radial-degree-drift`를 `changes/archive/`로 이동
- [x] 00.overview 진행 현황 ✅ · frontmatter `status: done`
