---
job: "34"
type: change
source: changes/20.nightly-rework
plan: plan/27.nightly-consolidation
status: doing
title: 야간 배치 재작성 (반지름 스코프·링크 재가중·spread·요지화 단계·재-KNN)
---

# Job 34: 야간 배치 재작성 (반지름 스코프·링크 재가중·spread·요지화 단계·재-KNN)  (change)

> 구현 작업 문서. 출처 스펙: [changes/20.nightly-rework](../changes/20.nightly-rework.md).
> `/implement-job 34`이 구현. **선행: job 32·33(망각 토대).** 가장 큰 변경 — DB 마이그레이션 포함.

## 인수 조건 (acceptance — changes/20.nightly-rework에서)
- [x] A1 잡이 04:00 KST(19:00 UTC)에 깨어난다. (`consolidateHourUTC = values.ConsolidationHourUtc=19`)
- [x] A2 재안정화·재분배가 **반지름 임계 안 별에만** 적용(멀리 표류한 별 좌표 안 끌려옴). (`scopeSubgraph` radius≤60; far 별은 캐시 미갱신)
- [x] A3 야간 후 `temporal` 링크 weight↓, `semantic` 링크 weight↑(상한 내). (`ReweightLinks` psql 검증: 0.8→0.776·0.5→0.51·co_recall 불변)
- [x] A4 재분배 후 성단이 한 점에 안 뭉치고 3D 분산(같은 입력→같은 결과, 결정론). (`spreadClusters` 성단 min-id 해시 — 링크 순서 무관 결정론 테스트)
- [x] A5 요지화: 반지름 임계 초과 시 `abstraction_stage` +1(0→…→4 단조, 최대 4). 나이/회상 트리거 폐기. (`AbstractStarsByRadius` psql: m1 0→2·m5 0→3·재실행 무변)
- [x] A6 가지치기 후에도 **모든 별 degree ≥ 1**(완전 고립 없음). 링크 row DELETE 없음. (`PruneWeakLinks` 살아있는 최강 보호 — severed-최강 엣지케이스 psql 검증)
- [x] A7 재-KNN 패스가 고립/끊긴 옛 별에 닮은 기억 생기면 새 링크 생성. (`ListReknnCandidates`+`KnnNearest`+`ReknnUpsertLinks` severed=false 되살림)
- [x] A8 schemaBonus가 코드·values에서 사라짐. (consolidate.go·values.yaml에서 제거)
- [x] A9 원본 record·기존 별 row 무변경·무삭제(잡 재실행 안전). (DELETE 0·records 본문 UPDATE 0; 멱등 + `CompleteConsolidateJob` 가드로 exactly-once)

## 구현 체크리스트 (implementation)
- [x] T001 (migrate) DB: 별 `abstraction_stage smallint default 0`(0~4) 컬럼 + `memory_links.severed boolean`. 00013 + schema.sql + sqlc 재생성.
- [x] T002 (gen:values) `spec/values.yaml`: hour_utc 19·radius_scope·temporal_link_decay·semantic_link_gain·spread_strength·gist_stage_radii[4]·reknn_min_age_days. **schemaBonus/gist_age/recall/form_simplify 제거.**
- [x] T003 [consolidate.go](../../backend/internal/job/consolidate.go) 시각 상수 `consolidateHourUTC` → `values.ConsolidationHourUtc`(19).
- [x] T004 재안정화/재분배에 **반지름 스코프**(`scopeSubgraph`) — 반지름(change 18 공식 서버 포팅 [radius.go](../../backend/internal/job/radius.go)) ≤ radius_scope 별만. far 별 캐시 미갱신.
- [x] T005 재분배에서 **schemaBonus 분기·상수 제거**(`redistribute` 단순 lerp).
- [x] T006 **링크 재가중 패스**(`ReweightLinks`): temporal(intra_entry·temporal) weight×decay, semantic +gain(cap). co_recall·severed 제외.
- [x] T007 **spread 패스**(`spreadClusters`): 성단별 결정론 변위(성단 min-id FNV 해시·Math.random 없음).
- [x] T008 **요지화 재작성**(`AbstractStarsByRadius`): 트리거 → 반지름이 넘긴 임계 수, `abstraction_stage = GREATEST(현재, target)`(target>현재만 승급·version++·변천사 append). form_seed_delta 불변(23 보존).
- [x] T009 **가지치기 + 마지막 1링크 보호**(`PruneWeakLinks`): 약·idle 선 weight 바닥+severed, 단 노드별 **살아있는** 최강 1개 보호(degree≥1). DELETE 없음.
- [x] T010 **재-KNN 패스**(`collectReknnLinks`+`ReknnUpsertLinks`): 옛 고립/끊긴 별 의미 KNN 재실행 → 신규/되살림 링크(severed=false). Go에서 무방향 쌍 dedupe.
- [x] T011 `RunConsolidation` 트랜잭션 통합: cache→reweight→abstract(+history)→prune→re-KNN→guarded complete. 멱등 + exactly-once(`CompleteConsolidateJob`).
- [x] T012 테스트: `consolidate_test.go` — stageForRadius·연결성 반지름·scope·spread 결정론(단일+다중성단)·spreadDirection. SQL 의미는 psql로 검증.

## 참고 (grounding)
- 헌법: [00.overview](../plan/00.overview.md) §불변 원칙 — 헌법1·2(삭제 없음, 끊김도 weight/플래그)·3
- 선행: job 32·33(망각 토대). 소비처: job 35(단계→형태)·36(단계→AI).
- 건드리는: [policy/domain/universe.md](../policy/domain/universe.md) §야간 공고화, `spec/values.yaml`

## 영향 파일 (blast radius)
- `backend/internal/job/consolidate.go`·`repository_pg.go`·`excitability.go`(재-KNN 재사용)
- DB 마이그레이션(`abstraction_stage`, link severed), `backend/internal/db/queries/**`·sqlc gen
- `spec/values.yaml`, `spec/policy/domain/universe.md`, `spec/plan/27.nightly-consolidation.md`

## 검증 / DoD
- [x] 위 **인수 조건** 전 항목 참(A1~A9)
- [x] 기존 plan 27 회귀 없음 — 멱등·재시도 안전 보존(비-멱등 reweight를 `CompleteConsolidateJob` 가드로 exactly-once化; abstract/prune/reknn/cache는 GREATEST/LEAST/overwrite로 멱등)
- [x] `pnpm db:migrate`·`pnpm gen:sql`·`pnpm gen:values` 적용(생성물 커밋 대상)
- [x] BE `go vet`·`go build`·`go test ./internal/job/...` 통과(Docker golang:1.26)
- [x] 헌법 sanity: consolidate 경로 `records`/`memories`/`memory_links` 행 삭제 0·records 본문 UPDATE 0

## 리뷰
- [x] `/code-review`(2 finder 병렬) + `/codex:review --background`(gpt-5.5) 합산 반영:
  - **반영**: ① reweight 비-멱등(lease 재claim 이중 커밋) → `CompleteConsolidateJob` 가드 + 0행이면 롤백(exactly-once). ② reweight가 severed 링크를 매일 재강화 → `AND severed=false`. ③ prune 보호가 *이미 끊긴* 최강을 보호해 마지막 살아있는 링크를 끊을 수 있음 → incident/protected·UPDATE를 `severed=false`로 한정(degree≥1 보장; psql 엣지케이스 검증). ④ spread가 union-find root(순서 의존) 해시 → 성단 **min-id** 해시로 결정론화(+다중성단 테스트, cluster.go 주석 정정).
  - **기각**: re-KNN 후보를 tx 전 읽음(한 밤 지연) — 보호가 신규 고립을 막아 실질 지연 없음(고립은 출생-고립뿐, 즉시 후보). severed 링크가 반지름 연결성에 포함 — 클라도 severed 구분 불가라 일관(서버 반지름은 근사·비권위). 둘 다 의도된 설계로 문서화.

## 완료 후 — SSOT 반영
- [x] `plan/27` 갱신(반지름 스코프·재가중·spread·요지 단계·재-KNN·schemaBonus 제거) · `policy/domain/{universe,synapse,star}.md` · `spec/values.yaml`
- [x] `changes/20.nightly-rework` → `changes/archive/`
- [x] 00.overview ✅ · `status: done`
