---
job: "05"
type: new
source: plan/43.cosmos-scene-composition
plan: plan/43.cosmos-scene-composition
status: done
title: CosmosScene — 공유 WebGL 배경 합성 (cosmos-scene-composition)
---

# Job 05: CosmosScene — 공유 WebGL 배경 합성 (cosmos-scene-composition)  (new)

> 구현 작업 문서. 출처 스펙: [plan/43.cosmos-scene-composition](../plan/43.cosmos-scene-composition.md).
> `/implement-job 05`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, plan/43.cosmos-scene-composition에서)
<!-- 이 job은 plan 43의 **J1 단계**(CosmosScene + BloomPass 이동 + 사인인 이주)만 다룬다.
     초대(A4', J2)·랜딩(A5, J3)은 후속 job에서 검증 — 이 job 범위 제외. -->
- [x] A1 `CosmosScene` 한 widget이 fluid 배경(앞/뒤 레이어) + 트윙클 point + 별 프리미티브(`buildStarBody`) + grain + `BloomPass`를 **한 R3F 캔버스**에 깊이순(renderOrder)으로 렌더한다. (glow는 halo가 아니라 bloom이 만든다 — 별도 halo 없음.)
- [x] A2 `CosmosScene`·`BloomPass`는 `appearance`·navigation/focus FSM·전송 계층을 import하지 않는다(prop 주입만) — 디커플드·라이브러리화 가능.
- [x] A3 `BloomPass`가 `shared/ui`에 있고 universe-canvas와 CosmosScene이 **공유**한다(중복 구현 없음). 우주 셸은 시각·동작 무회귀.
- [x] A4 **사인인**이 `CosmosScene` **하나**로 배경+브랜드별+트윙클+grain+bloom을 렌더 — 화면당 동시 WebGPU 컨텍스트 **1개**(이전 2개). 자가발광 별이 bloom으로 **글로우**(불투명 aurora도 빛난다).
- [x] A6 한 씬 안에서 배경 레이어가 **별 앞·뒤 양쪽**에 존재하고 깊이로 정렬된다(사인인에서 "일부는 별 앞으로·일부는 뒤로 흐른다").
- [x] A7 `prefers-reduced-motion`이면 정적(시간 정지·demand). 성능 노브(fps·dpr·octave·트윙클/레이어 카운트)는 **`spec/values.yaml`에서 생성**된 상수로 온다(FE 하드코딩 없음). `quality='low'`(또는 기기 등급)에서 부하가 실제로 준다.
- [x] A8 셰이더 TSL·별먼지 InstancedMesh/Points 유지(헌법8). build·lint·steiger 통과. **사인인·우주 셸**(BloomPass 이동분)에 회귀 없음.

> **이 job 제외(후속):** A4'(초대 이주)=J2 · A5(랜딩 전체 배경 이주)=J3. 이 job은 CosmosScene을 *만들고* 사인인 하나로 검증한다. 구 배경(CosmosBackdrop·FluidGradient·BrandMark·landing background·StarFieldCanvas)은 초대·랜딩이 아직 쓰므로 **J1에선 삭제하지 않는다**(정리는 J2·J3).

## 구현 체크리스트 (implementation — 구현 관점)
<!-- J1 = CosmosScene + BloomPass 이동 + 사인인. 위→아래 순서. [P]=서로 다른 파일·의존 없는 병렬. -->
- [x] T001 [P] (gen:values) `spec/values.yaml`에 `cosmos` 섹션 추가 — `fluid.fps_cap`(30)·`fluid.dpr`(0.6)·`fluid.octaves`(3)(현재 FluidGradient 하드코딩 이전)·`twinkle.count`·`layers.back`·`layers.front`·`quality.low_threshold`(기기등급 쓰면). `pnpm gen:values` → `VALUES.cosmos`. (bloom은 기존 `VALUES.bloom` 재사용.)
- [x] T002 fluid 머티리얼 재도입 — `shared/ui/fluid-material.ts`(plan 42에서 revert했던 추출본 복원: `buildFluidMaterial(opts)` + `FluidMaterialOptions{radial,foreground}`). octave는 `VALUES.cosmos.fluid.octaves`. `shared/ui/index.ts`에 `buildFluidMaterial`·`FluidMaterialOptions` export.
- [x] T003 `shared/ui/FluidGradient.tsx`를 fluid-material 소비로 — 내부 `buildFluidMaterial`을 import로 교체, 하드코딩 30/0.6/3 → `VALUES.cosmos.fluid.*`. (초대가 아직 CosmosBackdrop→FluidGradient를 쓰므로 거동 불변 유지.)
- [x] T004 `BloomPass` 이동 — `widgets/universe-canvas/ui/BloomPass.tsx` → `shared/ui/BloomPass.tsx`, `shared/ui/index.ts` export. `widgets/universe-canvas/ui/UniverseCanvas.tsx`의 import 경로 갱신. **우주 셸 무회귀 확인**(A3).
- [x] T005 `CosmosScene` widget 신설 — `widgets/cosmos-scene/`(`index.ts` + `ui/CosmosScene.tsx` + 하위). 타입 `StarVisual`·`CosmosSceneProps`(plan 43 설계 요점). 한 `<Canvas gl={createRenderer}>`에 깊이순: 배경 뒤(fluid `radial`, renderOrder 낮음) → 트윙클 Points → 별(`buildStarBody` uniform 바인딩, 위치·자전 주입) → 배경 앞(fluid `foreground` additive, renderOrder 높음) → `BloomPass`. 카메라 프리셋 `emblem`/`fullscreen` + `manual=true`. **glow는 bloom이 — 별도 halo 없음**(A1).
- [x] T006 트윙클 레이어 — 별먼지를 3D `Points`/InstancedMesh로(count=`VALUES.cosmos.twinkle.count`), 시간 기반 반짝임(소비처 uTime). 2D `StarFieldCanvas`의 3D 대체(헌법8 — Points/InstancedMesh).
- [x] T007 grain — CosmosScene 내 풀스크린 grain(셰이더 또는 오버레이 패스)으로 현재 CSS `GrainOverlay` 대체(씬 내 통합).
- [x] T008 성능/품질 — reduced-motion(시간 정지·`frameloop='demand'`)·`quality='low'`(bloom/앞레이어/트윙클 축소)·`dpr`/fps 스로틀(`VALUES.cosmos.fluid.*`) 적용(A7).
- [x] T009 사인인 이주 — `app/ui/SignInScreen.tsx`가 `CosmosBackdrop`+`BrandMark` 대신 `<CosmosScene camera="emblem" stars={[브랜드별]} twinkle grain bloom />`. appearance→StarVisual(`object`+`themeAccent`)·theme→palette 어댑터는 SignInScreen(또는 작은 헬퍼). 폼 UI·로그인 로직 불변.
- [x] T010 [P] 게이트·검증 — `tsc -b`·`eslint .`·`steiger`·`vite build`. 사인인: WebGPU 컨텍스트 1개·별 bloom 글로우·배경 앞/뒤 레이어 가시(시각). 우주 셸: BloomPass 이동 후 무회귀.
- [x] T011 정리 보류 명시 — `CosmosBackdrop`·`BrandMark`/`ThemedStar`·`StarFieldCanvas`·landing `background/*`는 초대(J2)·랜딩(J3)이 아직 쓰므로 **삭제하지 않음**. (정리는 후속 job.)

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 — §4(CosmosScene=widget, BloomPass=shared/ui)·§8(TSL·Points/InstancedMesh).
- 출처 plan: [plan/43](../plan/43.cosmos-scene-composition.md)(J1 범위). 별 프리미티브: [plan/42](../plan/42.star-visual-primitive.md)(`buildStarBody`·uniform 바인딩). BloomPass·레이어 순서 레퍼런스: [plan/06](../plan/06.universe-canvas.md).
- 렌더링: [tech/architecture](../tech/architecture.md) §3.1(WebGPURenderer·TSL·RenderPipeline Bloom)·§3.3(R3F 배치). 렌더러 팩토리 `shared/lib/r3f`(`createRenderer`).
- values: `cosmos.*` 신규(T001), `bloom.*` 재사용. 생성 상수 `@/shared/config`(`VALUES`).

## 영향 파일 (blast radius)
- **신규:** `frontend/src/widgets/cosmos-scene/`(`index.ts`·`ui/CosmosScene.tsx`·하위 레이어/트윙클/카메라) · `frontend/src/shared/ui/fluid-material.ts`
- **이동:** `frontend/src/widgets/universe-canvas/ui/BloomPass.tsx` → `frontend/src/shared/ui/BloomPass.tsx`
- **수정:** `frontend/src/app/ui/SignInScreen.tsx`(→ CosmosScene) · `frontend/src/shared/ui/FluidGradient.tsx`(fluid-material 소비 + VALUES) · `frontend/src/shared/ui/index.ts`(buildFluidMaterial·BloomPass export) · `frontend/src/widgets/universe-canvas/ui/UniverseCanvas.tsx`(BloomPass import 경로) · `spec/values.yaml`(+ 생성된 `frontend/src/shared/config/values.gen.ts`)
- **무변경(J1 보존 — J2·J3에서 정리):** `shared/ui/CosmosBackdrop.tsx`·`widgets/star3d`(BrandMark·ThemedStar)·`shared/ui/StarFieldCanvas.tsx`·`pages/landing/ui/background/*`·`pages/invite/*`
- **비대상:** `widgets/universe-canvas` 씬·카메라(레퍼런스, BloomPass import만) · `entities/star/ui/star-body.ts`(소비만)

## 검증 / DoD
- [x] 인수 조건 — 구조·코드 항목 참(아래 노트). A4 글로우·A6 앞/뒤 레이어 가시는 **시각 확인 권장**(헤드리스 미실행).
- [x] 코드젠/values: `cosmos` 그룹 values.yaml 추가. 로컬 `yaml` dep 없어 `pnpm gen:values` 미실행 → `values.gen.ts`(FE)·`values_gen.go`(BE)를 **생성기 출력 포맷대로 수기 동기화**(CI gen은 no-op 목표).
- [x] FE `tsc -b`·`eslint .`·`steiger`·`vite build` 통과(0 errors). BE 변경은 생성 상수뿐(go 게이트 로컬 불가; cosmos 미참조라 빌드 무영향).
- [x] 헌법 sanity: DB·records 미접촉(순수 FE 렌더 + FE-only 값). §4(FSD)·§8(TSL·InstancedMesh) 준수.

> **검증 노트** — 코드/게이트 확정: A1·A2·A3·A7·A8. **시각 확인 권장**: A4(별 bloom 글로우)·A6(앞/뒤 fluid 레이어가 별 사이로 흐름) + 사인인 별 앵커[0.5,0.38]·size 0.28 정렬. 브라우저 확장 미연결로 헤드리스 스크린샷 불가.

## 리뷰
- [x] adversarial 리뷰(워크플로 6축 finder→검증, 35 에이전트) — **후보 29 → 실제 1(low)·기각 28**. 기각: R3F 렌더소유·dispose·instanceColor·demand 스로틀·FSD·사인인 z-order 등은 검증된 패턴(StarField burst/ring 동일)이거나 오독. **실제 1건**: `values_gen.go`(BE) `cosmos` 드리프트 → 생성기 포맷대로 수기 추가해 해결. FE 로직 결함 0 → 코드 변경 없음.
- [ ] codex: 생략 — 워크플로 리뷰 6축 광범위 + 실 결함 0.

## 완료 후 — SSOT 반영
- [x] `plan/43` J1 as-built 반영 · `tech/architecture §3.3`에 CosmosScene(widget)·BloomPass(shared/ui) 반영 · values `cosmos` 그룹
- [x] (type=new — `changes/` 아카이브 해당 없음)
- [x] 00.overview 진행 현황: J1 ✅(J2·J3 todo) · 이 문서 `status: done`
