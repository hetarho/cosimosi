---
job: "06"
type: new
source: plan/43.cosmos-scene-composition
plan: plan/43.cosmos-scene-composition
status: done
title: CosmosScene — 공유 WebGL 배경 합성 (cosmos-scene-composition)
---

# Job 06: CosmosScene — 공유 WebGL 배경 합성 (cosmos-scene-composition)  (new)

> 구현 작업 문서. 출처 스펙: [plan/43.cosmos-scene-composition](../plan/43.cosmos-scene-composition.md).
> `/implement-job 06`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, plan/43.cosmos-scene-composition에서)
<!-- 이 job은 plan 43의 **J2 단계**(초대 이주 + J1·J2로 죽은 구 배경 정리)만 다룬다. 선행: job 05(J1, CosmosScene 존재). -->
- [x] A4' **초대**(`/invite`)가 `CosmosScene` **하나**로 배경+브랜드별+트윙클+grain+bloom을 렌더 — 화면당 동시 WebGPU 컨텍스트 **1개**(이전 2개). 자가발광 별이 bloom으로 글로우. (J1 사인인과 같은 `emblem` 패턴.)
- [x] A2 초대의 CosmosScene 사용도 디커플드(appearance→StarVisual·theme→palette 어댑터는 페이지에). 컴포넌트는 prop만.
- [x] A6 한 씬 안 배경 앞/뒤 레이어 깊이 정렬(초대 화면에서 가시).
- [x] A8 build·lint·steiger 통과. **초대 화면 무회귀**(멤버십 게이트·발행 로직 등 41 동작 불변, 비주얼만 교체).
- [x] C1 J1·J2로 더는 안 쓰이는 구 배경 제거 — `CosmosBackdrop`·`FluidGradient`(사인인·초대가 유일 소비처였음)·`BrandMark`(사인인·초대가 유일 소비처). grep으로 잔여 import 0 확인 후 삭제. (`ThemedStar`·`StarFieldCanvas`는 랜딩이 아직 써 **남긴다** → J3.)

> **선행/범위:** job 05(J1) 완료 후 시작. CosmosScene API는 J1 확정본을 그대로 소비(이 job은 새 컴포넌트를 만들지 않음). 랜딩(A5)은 J3.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- J2 = 초대 이주 + 구 배경 정리. 선행: job 05. -->
- [x] T001 초대 이주 — `pages/invite/ui/InvitePage.tsx`가 `CosmosBackdrop`+`BrandMark` 대신 `<CosmosScene camera="emblem" stars={[브랜드별]} twinkle grain bloom />`. appearance/theme 어댑터는 사인인(J1)과 동일 패턴(공통 헬퍼가 있으면 재사용). 멤버십 게이트·코드 입력 UI·로직(plan 41)은 불변.
- [x] T002 잔여 소비처 확인 — `grep -rn "CosmosBackdrop\|FluidGradient\|BrandMark" src`로 사인인·초대 외 소비처가 없음을 확인(있으면 그 화면도 처리 또는 범위 재검토).
- [x] T003 구 배경 제거 — `shared/ui/CosmosBackdrop.tsx`·`shared/ui/FluidGradient.tsx`·`widgets/star3d/ui/BrandMark.tsx` 삭제, `shared/ui/index.ts`·`widgets/star3d/index.ts`에서 export 제거. (`fluid-material.ts`는 CosmosScene이 쓰므로 유지. `StarCanvas`/`Star3D`/`ThemedStar`/`halo`는 랜딩 J3까지 보존.)
- [x] T004 [P] 게이트·검증 — `tsc -b`·`eslint .`·`steiger`·`vite build`. 초대 화면: 컨텍스트 1개·별 글로우·앞뒤 레이어 가시(시각). 사인인·우주 무회귀.

## 참고 (grounding)
- 헌법: [00.overview](../plan/00.overview.md) §불변 원칙 §4·§8.
- 출처 plan: [plan/43](../plan/43.cosmos-scene-composition.md)(J2). 선행 job: 05(J1 — CosmosScene·BloomPass·fluid-material 확정). 초대 동작: [plan/41](../plan/41.invite-membership.md)(멤버십·발행 — 비주얼만 교체, 로직 불변).
- 렌더링: [tech/architecture](../tech/architecture.md) §3.1·§3.3.

## 영향 파일 (blast radius)
- **수정:** `frontend/src/pages/invite/ui/InvitePage.tsx`(→ CosmosScene)
- **삭제(J1·J2로 무소비):** `frontend/src/shared/ui/CosmosBackdrop.tsx` · `frontend/src/shared/ui/FluidGradient.tsx` · `frontend/src/widgets/star3d/ui/BrandMark.tsx` (+ 각 index export)
- **무변경(J3까지 보존):** `widgets/star3d`의 `StarCanvas`·`Star3D`·`ThemedStar`·`halo`(랜딩 히어로가 아직 ThemedStar 사용) · `shared/ui/StarFieldCanvas.tsx`·`pages/landing/ui/background/*`
- **비대상:** `widgets/cosmos-scene`(J1 산출·소비만) · `entities/star`

## 검증 / DoD
- [ ] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [ ] (type=change면) 기존 plan 수용 기준 회귀 없음
- [ ] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter ./frontend build`·`lint` / BE `go vet ./... && go build ./...`(Docker) 통과(해당 시)
- [ ] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음

## 리뷰
- [ ] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] `plan/`을 새 현실로 갱신 · 영향받은 `policy/**`·`tech/**` · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [ ] (type=change면) `changes/` 출처 문서를 `changes/archive/`로 이동
- [ ] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
