---
job: "41"
type: change
source: changes/25.demo-preset-diary-flow
plan: plan/47.demo-free-mode-onboarding
status: done
title: demo preset diary flow
---

# Job 41: demo preset diary flow  (change)

> 구현 작업 문서. 출처 스펙: [changes/25.demo-preset-diary-flow](../changes/25.demo-preset-diary-flow.md).
> `/implement-job 41`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/25.demo-preset-diary-flow에서)
- [x] A1 데모 자유모드 `새 별 띄우기` → production `MemoryForm` compose와 동일 UI, 본문은 read-only 프리셋 일기(현재 페르소나·`demoToday()` 날짜).
- [x] A2 `별 나누기` → 프리셋 사전분절(조각 텍스트 + 조각별 mood/intensity/valence)이 production과 동일한 segmenting→review→fan-out 시퀀스로 펼쳐짐(AI만 프리셋 대체).
- [x] A3 펼쳐진 조각 별·연결이 production 규칙·정전 값으로 반영 — 일내 결속·의미 KNN(클라 근사)·감정 유사도 링크·거리=강함·밝기=반경.
- [x] A4 `DEMO_MAX_FRAGMENTS` 상한·데모 mood 키워드 추정·데모 밝기 바닥(`FLOOR`)·간이 감쇠·데모 전용 링크 가중치 제거, production 정전 값 사용(데모 고유는 프리셋 콘텐츠뿐).
- [x] A5 자유모드 즉시 랜덤 별 생성(`demoAddRandomStars` 진입점)이 더는 호출되지 않음.
- [x] A6 실계정 `MemoryForm` 작성(compose→review→submit→fan-out) 회귀 없음; 데모는 서버 미호출.
- [x] A7 별·시냅스·원본 삭제·수정 없음; 데모 데이터 실서버 미기록·demo exit/페르소나 전환 리셋에서 정리.
- [x] A8 백엔드·proto·RPC·sqlc 변경 없이 FE+values만; build·lint·typecheck·steiger 통과; 온보딩·페르소나/시간 버튼·망원경·회상·UI 숨김 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
- [x] T001 [shared/lib/demo/personas.ts 또는 신규 코퍼스] 페르소나별 read-only 프리셋 일기 + 사전분절 데이터(조각 텍스트 + 조각별 mood/intensity/valence) 작성 — 콘텐츠(코드, values 아님). `demoToday()` 날짜에 맞춰 고른다. [A1][A2]
- [x] T002 [features/record-memory/ui/MemoryForm.tsx · model/compose.machine.ts] 데모 모드 분기 추가 — compose 본문 read-only + 프리셋 주입, `별 나누기`(SEGMENT)가 서버 `segmentMemory` 대신 프리셋 사전분절을 반환해 동일한 segmenting→review 전이를 탄다(UI·상태머신·표현·타이밍 production 그대로). 실계정 경로 불변. [A1][A2][A6]
- [x] T003 [pages/home/model/use-demo-flow.ts + 하단 새 별 버튼] `새 별 띄우기` 핸들러를 `addDemoRandomStars`→프리셋 작성 폼 오픈으로 교체. [A1][A5]
- [x] T004 [shared/lib/demo/data.ts · simulate.ts] `demoAddRecord` 생성·연결을 production 규칙으로 정렬 — 일내 결속·의미 KNN(클라 근사)·감정 유사도 링크(job 37)·거리/밝기. `DEMO_MAX_FRAGMENTS`·데모 mood 키워드 추정·`FLOOR`·간이 감쇠·`demoLinking` 가중치 제거(정전 값 사용). [A3][A4]
- [x] T005 [shared/lib/demo/data.ts] `demoAddRandomStars` 진입점 제거(또는 비노출). 관련 테스트(`free-mode.test.ts`) 갱신. [A5]
- [x] T006 (gen:values) 제거되는 데모 전용 노브(`demo_free_mode.random_star_*`·`demoLinking`·데모 요지/바닥 등)를 `spec/values.yaml`에서 정리 후 `pnpm gen:values`. (job 40·43과 겹치는 노브는 충돌 없이 합의 — 한 곳에서 제거). [A4]
- [x] T007 검증 — 실계정 작성 회귀(A6), 데모 미기록·리셋 정리(A7), build·lint·typecheck·steiger + 회귀(A8). 헌법 sanity.

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 (원본 불변·서버 비좌표)
- 건드리는 tech/policy/values: production 작성 흐름 재사용([features/record-memory](../../frontend/src/features/record-memory/)) · 정전 [policy/domain/star.md](../policy/domain/star.md)·[synapse.md](../policy/domain/synapse.md)(KNN 게이트·감정 유사도)·[memory.md](../policy/domain/memory.md)(조각 fan-out). 프리셋 일기/사전분절은 배열 콘텐츠 → values 제외. 구현 완료 시 [policy/ux/interaction.md](../policy/ux/interaction.md) 데모 규칙 갱신.
- 의존: 생성·연결·밝기의 *로직 동치*는 [job 43](43.demo-production-logic-parity.md)와 공유(이 job은 *작성 UI 흐름*, 43은 *시뮬레이션 엔진 동치*).

## 영향 파일 (blast radius)
- `frontend/src/features/record-memory/ui/MemoryForm.tsx`, `model/compose.machine.ts`, `api/record-memory.ts` (데모 read-only·프리셋·서버 미호출 모드)
- `frontend/src/shared/lib/demo/{data.ts,simulate.ts,personas.ts}` (`demoAddRecord`·프리셋 코퍼스·`demoAddRandomStars` 제거·데모 노브)
- `frontend/src/pages/home/model/use-demo-flow.ts`, `frontend/src/pages/home/ui/*` (새 별 버튼 분기)
- `frontend/src/shared/lib/demo/free-mode.test.ts` (랜덤 별 테스트 갱신)
- `spec/values.yaml` + 생성물(`values.gen.ts`·`values_gen.go`)

## 검증 / DoD
- [x] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [x] (type=change면) 기존 plan 수용 기준 회귀 없음
- [x] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter ./frontend build`·`lint` / BE `go vet ./... && go build ./...`(Docker) 통과(해당 시)
- [x] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음

## 리뷰
- [x] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [x] `plan/`을 새 현실로 갱신 · 영향받은 `policy/**`·`tech/**` · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [x] (type=change면) `changes/` 출처 문서를 `changes/archive/`로 이동
- [x] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
