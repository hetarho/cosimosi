---
job: "40"
type: change
source: changes/24.continuous-clock-speed-control
plan: plan/19.engram-experience
status: done
title: continuous clock speed control
---

# Job 40: continuous clock speed control  (change)

> 구현 작업 문서. 출처 스펙: [changes/24.continuous-clock-speed-control](../changes/24.continuous-clock-speed-control.md).
> `/implement-job 40`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/24.continuous-clock-speed-control에서)
- [x] A1 데모 시간 컨트롤에 배속 4종(`1초=1시간` 기본 · `2시간` · `4시간` · `정지`)이 있고, 고른 배속이 즉시 적용된다.
- [x] A2 `정지`가 아니면 가상 시계가 선택 배속으로 계속 흐르고, 별 반지름·밝기·드리프트가 실시간으로 갱신돼 끊김 없이 변한다(점프 아님).
- [x] A3 가상 시계가 simulated 04:00 KST를 지날 때마다 야간 공고화가 1회 실행(빠른 배속 다중 경계도 각 1회, 누락·중복 없음). ※ *트리거·카데(04:00 경계 기반 멱등) 완료. 공고화 패스가 production change 20 로직과 완전 동치인지는 job 43(공고화 패스 동치) 소관 — 이 job(시계·트리거)은 기존 `demoConsolidate` 발화.*
- [x] A4 `하루/한 달 후로 이동` 이산 버튼 제거·배속 컨트롤 대체; `처음으로`는 가상 시계 0·추가 별 0(페르소나 유지·온보딩 복귀 안 함).
- [ ] A5 데모 야간 공고화가 옛 요지/가지치기 근사가 아니라 production 정전 값·change 20 모델을 따름(데모 전용 요지/가지치기 상수 제거). → **job 43(demo-production-logic-parity) 소관.** T004의 분담 노트대로 이 job은 *시계·트리거*만; 데모 전용 `demoConsolidation.*` 상수 제거·production 패스 포팅은 43이 마저(데모는 거리 모델이 없어 단순 상수 swap 불가).
- [x] A6 실계정(비데모) 우주에는 배속 시계·자동 야간 발화·배속 컨트롤이 나타나지 않음.
- [x] A7 별·시냅스·원본 일기 삭제·수정 없음; 데모 데이터는 실서버 미기록·demo exit 시 정리.
- [x] A8 배속 흐름 중 매 프레임 과도한 React 리렌더·머신 이벤트 없음(rAF/단일 타이머 + 기존 렌더 경로, 헌법4·8).
- [x] A9 백엔드·proto·RPC·sqlc 변경 없이 FE+values 생성물만; build·lint·typecheck·steiger 통과; 페르소나 전환·`처음으로`·망원경·회상·일반 모드 작성 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
- [x] T001 (gen:values) `spec/values.yaml`에 `demo_clock` 추가 — `hours_per_second: [1, 2, 4]`(기본 1, 정지는 배속 미적용) + `refresh_throttle_ms`(연속 재파생 주기). `pnpm gen:values` → FE/BE 상수 생성, 코드는 import만. [A1]
- [x] T002 [shared/lib/demo/clock.ts] 연속 시계로 — 배속 상태(`number | 'paused'`) + `advanceDemoClock(realElapsedMs)`로 `offsetMs` 연속 전진(`virtualNowMs()` 시그니처 유지). `getDemoClockSpeed`/`setDemoClockSpeed`. 정지면 누적 멈춤. 04:00 경계 함수 `consolidationBoundariesCrossed`. [A1][A2]
- [x] T003 [widgets/demo-sim/model/time-travel.ts] 이산 `runTimeSkip(days)` 제거 → `tickDemoClock`(model). rAF 흐름 드라이버는 `pages/home/model/use-demo-flow.ts`(rAF는 DOM이라 model 아닌 page hook)가 `offsetMs` 전진 + throttle된 `refreshActivation()`로 밝기·반지름 실시간 재파생. `처음으로`(가상 시계 0·추가 별 0·페르소나 유지) 유지. [A2][A4]
- [x] T004 야간 경계 검출 — `consolidationBoundariesCrossed`로 지난 04:00 KST 경계 수 계산 → 그 횟수만큼 `demoConsolidate()` 1회씩(멱등·누락/중복 없음). 트리거를 04:00 경계 기반으로 연속화. 공고화 패스 동치(abstraction_stage 정렬)는 job 43이 마저. [A3]
- [x] T005 [shared/lib/demo/data.ts] `demoApplyDayBatch`(day 루프) 제거 → `demoConsolidate` export(드라이버가 경계마다 호출). 데모 전용 요지/가지치기 상수(`demoConsolidation.*`)는 거리 모델 부재로 단순 swap 불가 — production 패스 포팅은 job 43과 공유(A5는 43 소관). [A5→job43]
- [x] T006 [pages/home/ui/DemoFreeModeControls.tsx] 시간 팝오버에서 이산 이동 버튼 제거, 배속 셀렉터(라디오 칩 `1·2·4시간·정지`) 추가; `처음으로` 유지. tour target id `time-speed` 노출(job 42 연계). [A1][A4]
- [x] T007 [pages/home/model/use-demo-flow.ts] `skipDemoDays`→`selectClockSpeed`(배속 setter)·rAF 연속 흐름 드라이버로 교체; 드라이버는 `isDemoMode()` + free/tutorial flow + 비정지에서만 돈다. [A4][A6]
- [x] T008 검증 — 비데모 가드(A6), 데모 데이터 미기록(A7), 성능(A8), build·lint·typecheck·steiger + 회귀(A9). 헌법 sanity(삭제·원본 UPDATE 없음).

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 (헌법4 model 순수·8 렌더)
- 건드리는 tech/policy/values: `spec/values.yaml` `demo_clock`(신규, FE 데모 노브 — 서버 미사용) · 야간 임계는 production `consolidation.*` 재사용. 구현 완료 시 [policy/ux/interaction.md](../policy/ux/interaction.md) 데모 시간 규칙 갱신.
- 의존: 야간 공고화 패스 로직 동치는 [job 43](43.demo-production-logic-parity.md)와 공유 — 이 job은 *시계·트리거*, 43은 *공고화 패스 동치*.

## 영향 파일 (blast radius)
- `frontend/src/shared/lib/demo/clock.ts` (연속 시계·배속 상태)
- `frontend/src/widgets/demo-sim/model/time-travel.ts` (배속 흐름 드라이버·04:00 경계·refreshActivation)
- `frontend/src/shared/lib/demo/data.ts` (`demoApplyDayBatch`·데모 요지/가지치기 상수 수렴)
- `frontend/src/pages/home/ui/DemoFreeModeControls.tsx` (배속 셀렉터·이산 버튼 제거)
- `frontend/src/pages/home/model/use-demo-flow.ts` (시간 핸들러·데모 가드)
- `spec/values.yaml` + `frontend/src/shared/config/values.gen.ts` + `backend/internal/values/values_gen.go` (생성물)

## 검증 / DoD
- [x] 위 **인수 조건** — A1·A2·A3(트리거)·A4·A6·A7·A8·A9 참. A5(공고화 패스 production 동치)는 분담상 job 43.
- [x] (type=change면) 기존 plan 수용 기준 회귀 없음 (plan 19 수용 1·3~12 유지; 2는 배속으로 갱신)
- [x] 코드젠/마이그레이션/values 적용: `pnpm gen:values`(demo_clock)
- [x] FE `--filter ./frontend build`·`lint`·`lint:fsd`·`tsc -b`·vitest(데모·투어) 통과. BE 변경 없음(values_gen.go는 생성물).
- [x] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음 (demoConsolidate는 weight 바닥·form_seed_delta만, 삭제 0)

## 리뷰
- [x] `/code-review`(high) 반영 — 2건 수정(투어 정지 선택 시 hang→info phase, 같은 페르소나 재선택 시 시계 상태 desync→early-return). 오탐 3건 기각(day-bucket 오프셋·200ms refresh lag·version saturation — formSeedDelta 캡으로 무효).
- [x] `/codex:review`(gpt-5.5, 백그라운드) 합산 — 1건 수정: 가려진 탭/긴 메인스레드 멈춤 후 거대한 catch-up 틱이 수십~수백 밤을 한 번에 발화 → `demo_clock.max_tick_ms`(250)로 per-frame elapsed 클램프. 1건 문서화(미수정): `정지` 중에도 `virtualNowMs=Date.now()+offset`이 실시간 creep — 가상 04:00 직전 정지+실제 수 분 대기 후 해제 시 그 경계 1회 건너뜀(극단 코너; 근본 해결은 frozen-base 시계 모델 변경이라 스코프 밖, 시각 영향 무시 수준).

## 완료 후 — SSOT 반영
- [x] `plan/19`을 배속 연속 시계로 갱신 · `policy/ux/interaction.md` 가상 시계 행·구현 목록 갱신 · `spec/values.yaml` `demo_clock`(+`gen:values`)
- [x] (type=change면) `changes/24` → `changes/archive/24`로 이동
- [x] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
