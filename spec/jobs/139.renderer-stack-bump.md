---
job: '139'
type: 'refactor'
source: 'code-review/12.frontend-rendering-performance-audit'
plan: 'none'
status: todo
title: 'Renderer stack bump'
---

# Job 139: Renderer stack bump (refactor)

> Implementation work doc. Source spec: [code-review/12.frontend-rendering-performance-audit](../code-review/12.frontend-rendering-performance-audit.md).
> /cosimosi:implement-job 139 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

Three renderer-critical dependencies are behind on fixes that land exactly on this repo's hottest paths (R005):
`react-native-webgpu` 0.5.16 → **0.8.1** (0.6.0 fixed iOS-simulator instanced-draw instabilities — the whole scene
is instanced draws; 0.6.2 thread-safe native caches; 0.8.0 ships a Jest mock; 0.8.1 texture-copy fix), R3F 9.6.1 →
**9.7.0** (batched instance reconstruction — the `key={instanceCount}` remount path in `InstancedNodeLayer`), three
0.185.0 → **0.185.1** (patch fixes). Independent of the other review-12 jobs; land it early so their verification
runs on the current stack.

## Acceptance Criteria (from code-review/12 — R005)

- [ ] A1 **`react-native-webgpu` is 0.8.1** across `apps/mobile` and the `packages/3d-renderer` peer range; iOS pods
      and Android gradle rebuilt; the universe scene renders correctly on the iOS simulator **and** at least one
      physical device (§3.5's pre-1.0 caveat: pin exact, test on hardware), with instanced draws (star / cell-star /
      gist layers) spot-checked.
- [ ] A2 **R3F is 9.7.0 with the patch rebased.** `patches/@react-three__fiber@9.6.1.patch` (a one-line
      `package.json` `react-native`-field redirect) is regenerated against 9.7.0 and the `pnpm.patchedDependencies`
      key renamed; both canvas hosts init and render as before.
- [ ] A3 **three is 0.185.1** everywhere it is declared (apps + packages, one version).
- [ ] A4 **Mobile Jest uses the upstream mock where it simplifies ours.** If 0.8.0's shipped Jest mock covers what
      hand-rolled test shims do today, replace them; otherwise record why not in this doc.
- [ ] A5 **No behavior or visual change.** Headless CDP screenshot parity on web (landing hero + universe); mobile
      device spot-check; full `pnpm check` green including mobile jest with its guards
      (`apps/mobile/jest.guards.js`) quiet.

## Implementation Checklist

- [ ] T001 Bump `react-native-webgpu` to 0.8.1 in `apps/mobile/package.json` (exact pin) and the
      `packages/3d-renderer` optional peer range; `pnpm install`; `cd apps/mobile/ios && pod install`; confirm the
      Android gradle sync. Read the 0.6.x–0.8.x release notes for migration notes beyond the fixes named above.
- [ ] T002 Bump `@react-three/fiber` to 9.7.0: update manifests, rename the `pnpm.patchedDependencies` key in the
      root `package.json`, regenerate the patch (`pnpm patch @react-three/fiber@9.7.0`, re-apply the one-line
      `react-native`-field change, `pnpm patch-commit`). Verify the native entry still resolves to the web build
      (the patch's whole purpose).
- [ ] T003 [P] Bump `three` (and `@types/three`) to 0.185.1 in every manifest that declares it (apps/web,
      apps/mobile, packages/3d-renderer, any other consumer) — one workspace-wide version.
- [ ] T004 Mobile Jest: try the upstream `react-native-webgpu` mock in place of local shims; keep whichever setup is
      smaller, note the choice here.
- [ ] T005 Verification sweep: web headless CDP screenshots (landing + universe) byte-compared against pre-bump
      captures; iOS simulator + physical-device run of the universe scene (instanced layers, bloom, sky); full
      `pnpm check`.

## Grounding

- Source finding: [code-review/12](../code-review/12.frontend-rendering-performance-audit.md) R005 (P2);
  Cross-Cutting Theme 5 (the modernization story is additive).
- Architecture: [ARCHITECTURE.md](../ARCHITECTURE.md) §3.3 (pin three's version — the post pipeline rides it),
  §3.5 (react-native-webgpu is pre-1.0/early-adopter: pin versions, no Expo Go, test on physical devices).
- Upstream release notes: react-native-webgpu 0.6.0/0.6.2/0.6.4/0.8.0/0.8.1; R3F 9.7.0 (batched instance
  reconstruction, keyed-reorder children sync); three 0.185.1 changelog.
- tech / policy / values touched: none (dependency-only); update any `spec/tech/*` doc that pins these version
  numbers in prose.

## Affected files (blast radius)

- `package.json` (root — `pnpm.patchedDependencies` key) · `patches/@react-three__fiber@*.patch`
- `apps/mobile/package.json` · `apps/mobile/ios/Podfile.lock` · Android gradle lock outputs
- `apps/web/package.json` · `packages/3d-renderer/package.json` (+ any other three/R3F declarer) · `pnpm-lock.yaml`
- `apps/mobile/jest.setup*` / test shims — only if T004 adopts the upstream mock

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
