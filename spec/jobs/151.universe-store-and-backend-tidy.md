---
job: '151'
type: 'refactor'
source: 'code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-'
plan: 'none'
status: todo
title: 'Universe store and backend tidy'
---

# Job 151: Universe store and backend tidy (refactor)

> Implementation work doc. Source spec: [code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-](../code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-.md).
> /cosimosi:implement-job 151 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: R006 (P3, decided: device preference — document, don't reset), R013 (P3), R014 (P3
confirmed items). Decision: Questions #6 in the source report.

- [ ] A1 `universe-view-store.ts` states the decided intent — pinned/free is a DEVICE
      preference that survives account switches — and `user-state-reset.ts`'s contract wording
      no longer claims every user-visible store (it names what it covers and why the view
      store is excluded).
- [ ] A2 `diary-store.ts`'s `add` no-op condition is either simplified to its real meaning or
      the refresh-on-duplicate intent is stated; the dead first clause is gone.
- [ ] A3 The `use-diary-archive` mirror semantics are safe: `enabled: false` no longer wipes
      the shared mirror, and the owner-`setAll`-erases-`add`-contribution ordering issue is
      fixed or its bound documented at the seam.
- [ ] A4 `isDateRangeUsable` (dead export) is deleted from `packages/memory`; the
      `entity-collection.ts` walk-order comment matches the loop.
- [ ] A5 The diaries min/max memory-count SQL computes the per-diary count once (single
      LATERAL/scalar alias) and a Postgres integration test covers `MinMemories`/`MaxMemories`
      (match on later page, soft-deleted excluded).
- [ ] A6 `TestExactlyTwoAchievementRowsOnePerKind` is renamed to what it asserts; the
      mood-claim protocol is a shared registry constant, not a comment.
- [ ] A7 The `view_semantic` fingerprint deploy note (pre-deploy receipts → conflict on
      retry) is recorded in `DEPLOY.md`.
- [ ] A8 No behavior change beyond the fixes; universe/memory/api suites green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [ ] T001 `packages/universe/src/universe-view-store.ts` — add the decided-intent doc
      (device preference, survives account switch); `user-state-reset.ts` — narrow the
      contract comment and name the exclusion.
- [ ] T002 [P] `packages/universe/src/diary-store.ts:31` — simplify the condition to
      `diaries.length === 0` (or implement content-identical no-op like `entity-collection`)
      and state the intent.
- [ ] T003 `packages/universe/src/react/use-diary-archive.ts` — guard `enabled: false` from
      touching the mirror; fix or bound the owner-`setAll` vs day-modal-`add` ordering
      (re-add on mirror settle, or document the transient at the hook seam); test both.
- [ ] T004 [P] Delete `isDateRangeUsable` from `packages/memory/src/diary.ts` +
      `index.ts`; correct `entity-collection.ts:10-11`'s comment.
- [ ] T005 (gen) `apps/api/db/queries/memory/diaries.sql` — deduplicate the four copies of
      the correlated count subquery into one LATERAL/scalar alias per statement; `pnpm gen:sql`.
- [ ] T006 Add a Postgres integration test for `MinMemories`/`MaxMemories` (bounds honored,
      soft-deleted memories excluded, agreement with `ListDiarySplitRefs`).
- [ ] T007 [P] `apps/api/internal/store/catalog_test.go:116` — rename to match "at most one
      per kind"; `apps/api/internal/account/pg/store_integration_test.go` — hoist the claimed
      moods into a shared registry constant both packages import.
- [ ] T008 `DEPLOY.md` — record the view_semantic fingerprint change note (old receipts
      conflict on retry; sweep only if prod data exists).
- [ ] T009 Reflect any store-contract wording into `spec/tech/state-machine.md` /
      `spec/policy/ux/universe-view.md` (view-mode persistence intent).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/policy/ux/universe-view.md` (view-mode persistence),
  `DEPLOY.md` (fingerprint deploy note).

## Affected files (blast radius)

- `packages/universe/src/universe-view-store.ts`, `packages/universe/src/user-state-reset.ts`
- `packages/universe/src/diary-store.ts` (+ test), `packages/universe/src/react/use-diary-archive.ts` (+ test)
- `packages/universe/src/entity-collection.ts` (comment only)
- `packages/memory/src/diary.ts`, `packages/memory/src/index.ts` (dead export removal)
- `apps/api/db/queries/memory/diaries.sql` (+ regenerated `apps/api/db/gen/diaries.sql.go`) — (gen)
- `apps/api/internal/memory/pg/` integration test (new min/max coverage) — memory context
- `apps/api/internal/store/catalog_test.go`, `apps/api/internal/account/pg/store_integration_test.go`
- `DEPLOY.md`, `spec/policy/ux/universe-view.md` (doc reflection)

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
