---
job: "43"
type: change
source: changes/27.demo-production-logic-parity
plan: plan/19.engram-experience
status: todo
title: demo production logic parity
---

# Job 43: demo production logic parity  (change)

> 구현 작업 문서. 출처 스펙: [changes/27.demo-production-logic-parity](../changes/27.demo-production-logic-parity.md).
> `/implement-job 43`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/27.demo-production-logic-parity에서)
- [ ] A1 데모 야간 공고화가 서버 change 20 패스(반경 스코프 ≤60·시간링크 약화/의미링크 강화·spread·`abstraction_stage` 반경 승급·마지막 연결 보호·re-KNN)를 충실히 수행, 시간이 지나면 데모 별 `abstraction_stage`가 0→4로 올라 형태가 단계적으로 단순(요지화)해짐(현 버그 해소).
- [ ] A2 데모 별 생성·링크가 서버 식과 동일 결과 — 의미 KNN 게이트(`cos≥0.75`)·시간 보너스·감정 유사도(job 37)·캡 0.79·흥분성 할당·소프트 억제 — 데모 전용 링크 휴리스틱 제거.
- [ ] A3 거리=강함·밝기=반경·감쇠·재공고화 PE 재성형이 실렌더와 동일 순수 함수/식으로 계산, 데모 전용 바닥·근사 제거.
- [ ] A4 데모 전용 `demo_*` 튜닝 노브가 사라지고 데모·실로직이 `spec/values.yaml` 단일 출처 공유(프리셋/콘텐츠만 데모 고유).
- [ ] A5 parity 보장 수단 존재 — 공유 가능 공식은 순수 함수 추출(데모·실렌더 동일 함수), Go↔TS로 갈리는 부분은 골든 대조 테스트로 동치 고정.
- [ ] A6 임베딩/LLM 의존부(의미 임베딩·KNN·조각화/감정)는 명시적 근사·프리셋으로만 남고 경계가 문서/코드에 표기.
- [ ] A7 백엔드·proto·RPC·sqlc·DB·실렌더(인증 우주)에 회귀·변경 없음.
- [ ] A8 build·lint·typecheck·steiger 통과; 기존 데모 체험(회상·잠든 별·일기 목록·페르소나·시간 흐름)·실계정 우주 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
- [ ] T001 [shared/lib/demo/data.ts] **추상화 버그 해소** — `toStar()`가 `abstractionStage`를 별 반지름(거리)에서 파생해 항상 탑재(현재 미설정 → proto 기본 0). 별 생성·갱신 경로 모두. [A1]
- [ ] T002 [shared/lib/demo/data.ts] `demoConsolidate()`를 서버 change 20 패스의 충실한 포트로 교체 — 반경 스코프 ≤60·시간링크 약화/의미링크 강화·spread(결정론 해시)·`abstraction_stage = GREATEST(현재, target)` 반경 승급·마지막 연결 보호(degree≥1)·re-KNN. 옛 `formSeedDelta` 나이 트리거 요지 경로 제거. (`backend/internal/job/consolidate.go`·`radius.go` 미러) [A1]
- [ ] T003 [shared/lib/demo/data.ts · simulate.ts] 링크 생성을 서버 식으로 — 의미 KNN(클라 근사) 게이트 `cos≥0.75` + 시간 보너스 + 감정 유사도(job 37) + 캡 0.79 + 흥분성 편향 할당·소프트 억제(×0.5). 데모 전용 `demoLinking` 휴리스틱·캡 제거. (`backend/internal/job/worker.go`·`excitability.go` 미러) [A2]
- [ ] T004 [P] [entities/memory/model/{activation.ts,reshape.ts,seed.ts}] 거리=강함·밝기=반경·감쇠·재공고화 PE 재성형 식을 플랫폼 무관 순수 함수로 정리/추출(이미 일부 공유) → 데모·실렌더가 같은 함수 import. 데모 전용 바닥(`FLOOR`)·근사 제거. (행동 보존 리팩터 — 실렌더 회귀 금지) [A3]
- [ ] T005 (gen:values) `spec/values.yaml`의 데모 전용 노브(`demoLinking`·`demoConsolidation` 데모 고유 항 등) 제거 → 서버 공유 값 수렴, `pnpm gen:values`. (job 40·41이 손댄 노브와 충돌 없이 합의) [A4]
- [ ] T006 [parity 테스트] 공유 순수 함수는 데모·실 경로가 같은 함수를 쓰는지 확인하고, Go↔TS로 갈리는 핵심 로직(공고화 단계 승급·링크 가중치)은 **골든 픽스처 대조 테스트**(동일 입력→동일 출력)로 동치를 고정. [A5]
- [ ] T007 임베딩/LLM 환원 불가 경계(의미 임베딩·KNN·조각화/감정 = 근사·프리셋)를 코드 주석 + tech 문서에 표기. [A6]
- [ ] T008 검증 — 백엔드/proto/DB/실렌더 무변경(A7), 기존 데모·실계정 회귀(A8), build·lint·typecheck·steiger·`go vet`. 헌법 sanity(삭제·원본 UPDATE 없음).

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 (헌법3·4 model 순수·클라 파생, 8 렌더)
- 건드리는 tech/policy/values: 포팅 원본 `backend/internal/job/{consolidate.go,worker.go,cluster.go,excitability.go,radius.go}`·`backend/internal/memory/service.go`; 거울이 따라야 할 정전 [policy/domain/star.md](../policy/domain/star.md)·[synapse.md](../policy/domain/synapse.md)·[memory.md](../policy/domain/memory.md)·[universe.md](../policy/domain/universe.md). `demo_*` 노브 제거. 구현 완료 시 [policy/ux/interaction.md](../policy/ux/interaction.md) + tech 문서에 **데모 parity 계약**(순수 함수 공유 + 골든 대조) 기록.
- 선행 의존: [job 40](40.continuous-clock-speed-control.md)(연속 시계·04:00 트리거)·[job 41](41.demo-preset-diary-flow.md)(작성 흐름) 위에 시뮬레이션 엔진 동치를 얹는다 — 40·41 → 43.

## 영향 파일 (blast radius)
- `frontend/src/shared/lib/demo/data.ts` (`toStar` abstraction_stage·`demoConsolidate` 포트·`demoReshape`·링크)
- `frontend/src/shared/lib/demo/simulate.ts` (그래프 생성·KNN 근사·흥분성)
- `frontend/src/entities/memory/model/{activation.ts,reshape.ts,seed.ts}` (공유 순수 함수 추출 — 행동 보존)
- `spec/values.yaml` + 생성물 (`demo_*` 노브 제거)
- 테스트: 데모 parity 골든 대조(신규) + 기존 `shared/lib/demo/*.test.ts` 갱신
- 참조(미변경, 포팅 원본): `backend/internal/job/*`, `backend/internal/memory/service.go`

## 검증 / DoD
- [ ] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [ ] (type=change면) 기존 plan 수용 기준 회귀 없음
- [ ] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter ./frontend build`·`lint` / BE `go vet ./... && go build ./...`(Docker) 통과(해당 시)
- [ ] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음

## 리뷰
- [ ] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] `plan/`을 새 현실로 갱신 · 영향받은 `policy/**`·`tech/**` · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [ ] (type=change면) `changes/` 출처 문서를 `changes/archive/`로 이동
- [ ] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
