---
job: '74'
type: 'refactor'
source: 'code-review/05.plans-35-53-runtime-and-architecture-aud'
plan: 'none'
status: todo
title: 'Harden persistence and mobile lifecycle gates'
---

# Job 74: Harden persistence and mobile lifecycle gates (refactor)

> Implementation work doc. Source spec: [code-review/05.plans-35-53-runtime-and-architecture-aud](../code-review/05.plans-35-53-runtime-and-architecture-aud.md).
> /cosimosi:implement-job 74 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/05 — R015, R016)

- [ ] A1 The persistence lint rejects a product table where `user_id` appears only in a foreign-key reference,
      expression, comment, or another column name; it requires an actual owned `user_id` column.
- [ ] A2 Product SELECT/INSERT/UPDATE/DELETE statements are accepted only when the owned table is genuinely scoped
      by `user_id`; projection-only, unrelated-alias, join-only, and CTE mentions fail with an actionable location.
- [ ] A3 All current migrations and queries pass the strengthened checker without weakening the repository's
      per-user isolation contract, and positive/negative fixtures cover aliases, CTEs, multi-statements, and SQL noise.
- [ ] A4 `CI=true pnpm test:mobile` passes with every suite/test and emits no unexpected React `act(...)` warning,
      worker force-exit, open-handle warning, or flaky Diary timeout.
- [ ] A5 `jest --runInBand --detectOpenHandles` exits normally with zero leaked handles after the mobile shell suite.
- [ ] A6 Web and mobile primary lint commands fail on warnings; the seven current changed-code warnings are fixed or
      narrowly documented as intentional before zero-warning enforcement lands.
- [ ] A7 Existing FSD, observability, raw-string, comment, generated-freshness, and standard CI aggregation probes
      continue to fire and pass as appropriate.
- [ ] A8 No runtime product semantics, user data, palette behavior, or auth routing change as part of this gate job.

## Implementation Checklist

- [ ] T001 Reproduce and capture the R015 semantic probe and R016 mobile diagnostic before editing; invoke
      `/be-architecture`, `/fe-architecture`, and `/mobile-architecture` for the SQL/tooling and test surfaces.
- [ ] T002 Define the supported SQL grammar and ownership rule for product tables/statements; select a parser or a
      bounded statement-kind/alias-aware implementation that fails closed on unsupported product SQL.
- [ ] T003 Implement actual `user_id` column and predicate validation with file/query-name diagnostics, preserving
      the explicit platform-table/query allowlists.
- [ ] T004 Expand persistence fixtures with false-negative controls (FK reference, projection, unrelated join/alias,
      CTE, INSERT source, UPDATE/DELETE without owned predicate) and positive controls for every supported form.
- [ ] T005 [P] Fix mobile shell test ownership: retain render handles, unmount/cleanup before global facade/store
      resets, and wrap sign-out, palette, and other external-store mutations in `act`.
- [ ] T006 [P] Locate and cancel/unsubscribe the palette-triggered Timeout/worker work at its true owner; remove the
      flaky Diary wait by awaiting the authoritative navigation/read condition rather than raising timeouts.
- [ ] T007 [P] Resolve the six web `react(only-export-components)` warnings and mobile `no-void` warning without
      changing route/account behavior, then configure primary oxlint/ESLint commands to reject warnings.
- [ ] T008 Make unexpected console errors/warnings and leaked handles fail the relevant mobile smoke test without
      adding a blanket force-exit or warning suppression.
- [ ] T009 Run all deliberate enforcement probes, the diagnostic Jest mode, `CI=true pnpm test:mobile`, `pnpm lint`,
      and the complete `pnpm check`; document parser limitations explicitly rather than silently accepting them.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/ARCHITECTURE.md` §4, plan 05 persistence contract, plan 53 auth gate,
  test/lint tooling policy. No product values, schema migration, or generated contract change.

## Affected files (blast radius)

- `scripts/check-persistence-isolation.mjs` — root persistence enforcement implementation.
- `scripts/check-persistence-isolation.test.mjs` — semantic positive/negative controls.
- `apps/api/db/migrations/*.sql`, `apps/api/db/queries/**/*.sql` — read-only validation corpus; edit only if the
  stronger parser exposes a real malformed/unscoped statement.
- `apps/mobile/src/app/App.test.tsx` — mobile shell lifecycle, auth mutation, palette cleanup, and Diary wait.
- `apps/mobile/jest.config.js` — open-handle/console policy only if configuration is the correct ownership seam.
- `packages/emotion/src/palette.ts` — subscriber/timer investigation input; production edit only if it owns leaked work.
- `apps/web/src/app/routes/route-tree.tsx` — isolate route components/exports to clear React lint warnings.
- `apps/mobile/src/features/account-settings/ui/AccountSection.tsx` — clear the `no-void` warning without behavior change.
- `apps/web/package.json`, `apps/mobile/package.json`, root `package.json` — zero-warning and aggregate command wiring.

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] No runtime behavior change and no regression of persistence, palette, settings, or auth-gate acceptance criteria
- [ ] No codegen, migration, or values change is expected
- [ ] R015 semantic negative controls fail before the fix and pass as expected after the fix
- [ ] `CI=true pnpm test:mobile` and mobile Jest `--runInBand --detectOpenHandles` pass cleanly
- [ ] `pnpm lint` has zero primary-linter warnings and every deliberate enforcement probe fires
- [ ] `pnpm typecheck && pnpm test && pnpm build:web && pnpm check` passes
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Reflect the strengthened persistence and lifecycle-gate semantics in architecture/quality SSOT; no product
      tuning number is introduced
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
