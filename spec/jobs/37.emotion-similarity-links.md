---
job: "37"
type: change
source: changes/21.emotion-similarity-links
plan: plan/05.embedding-worker
status: todo
title: 링크 가중치에 감정 유사도 추가
---

# Job 37: 링크 가중치에 감정 유사도 추가  (change)

> 구현 작업 문서. 출처 스펙: [changes/21.emotion-similarity-links](../changes/21.emotion-similarity-links.md).
> `/implement-job 37`이 구현. 독립적(다른 job 비의존)이나, job 32에서 `initialWeight` 위치가 옮겨졌으면 그 위치 확인.

## 인수 조건 (acceptance — changes/21에서)
- [ ] A1 의미 유사도·시간이 같고 감정만 다른 두 후보 중 **감정이 더 닮은 쪽 weight가 더 크다**.
- [ ] A2 감정 항을 더해도 weight가 `semanticWeightCap(0.79)`를 못 넘는다(일내 결속 0.8 보장).
- [ ] A3 감정만 닮고 의미 유사도 < 0.75면 **여전히 안 이어진다**(후보 게이트 불변).
- [ ] A4 `emoAlpha=0`이면 기존 동작과 동일(회귀 경계).

## 구현 체크리스트 (implementation)
- [ ] T001 (gen:values) `spec/values.yaml`: `emoAlpha`(감정 유사도 가중) 신규.
- [ ] T002 감정 유사도 헬퍼(서버) 신규: mood→circumplex 좌표(`MOOD_AFFECT`) 거리 또는 (valence, intensity) 벡터 코사인 → `emoSim ∈ [0,1]`.
- [ ] T003 [excitability.go](../../backend/internal/job/excitability.go) `initialWeight`에 항 추가: `clamp(weightAlpha·cos + temporal + emoAlpha·emoSim, 0, 1)`, `semanticWeightCap` 상한 유지. (job 32에서 함수 이전됐으면 그 위치.)
- [ ] T004 후보 게이트(`cos ≥ 0.75`) 불변 확인 — 감정은 weight만 조정.
- [ ] T005 테스트: `excitability_test.go` — 감정 닮은 쪽 weight↑·상한·게이트 불변·emoAlpha=0 회귀.

## 참고 (grounding)
- 헌법: [00.overview](../plan/00.overview.md) §불변 원칙 — 헌법3(weight 의미 그래프 파생)
- 건드리는: [policy/domain/synapse.md](../policy/domain/synapse.md) §생성, `frontend/src/shared/config/mood.ts`(MOOD_AFFECT), `spec/values.yaml`

## 영향 파일 (blast radius)
- `backend/internal/job/excitability.go`(initialWeight) + 감정유사도 헬퍼 신규
- `spec/values.yaml`, `spec/policy/domain/synapse.md`, `spec/plan/05.embedding-worker.md`

## 검증 / DoD
- [ ] 위 **인수 조건** 전 항목 참
- [ ] 기존 링크 생성 회귀 없음(게이트·상한·일내결속)
- [ ] `pnpm gen:values` 적용
- [ ] BE `go vet && go build`(Docker)·test 통과
- [ ] 헌법 sanity: 행 삭제·records UPDATE 없음

## 리뷰
- [ ] `/code-review` 반영 · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] `plan/05` 갱신 · `policy/domain/synapse.md` · `spec/values.yaml`
- [ ] `changes/21.emotion-similarity-links` → `changes/archive/`
- [ ] 00.overview ✅ · `status: done`
