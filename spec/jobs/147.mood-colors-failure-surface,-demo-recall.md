---
job: '147'
type: 'refactor'
source: 'code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-'
plan: 'none'
status: todo
title: 'Mood colors failure surface, demo recall gating and web polish'
---

# Job 147: Mood colors failure surface, demo recall gating and web polish (refactor)

> Implementation work doc. Source spec: [code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-](../code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-.md).
> /cosimosi:implement-job 147 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: R004 (P2), R005 (P2), R015 (P3, minus adjudication-struck items).

- [ ] A1 A failed mood-color save is visible to a sighted user WITHOUT closing the dialog: the
      failure notice renders inside the open dialog (not on the card behind the scrim), and the
      stale card-comment ("the failure notice is on this card") is corrected.
- [ ] A2 During the demo recall beat, dismissing the star panel cannot strand the visitor: the
      panel is either beat-inert (parity with the writing/decoration sheets' `canClose` model)
      or auto re-opens while the beat's work is undone.
- [ ] A3 The hue control never renders a value outside its own `min`/`max` (a stored 359.5–360
      hue no longer produces a controlled-input mismatch).
- [ ] A4 `RANDOM_SWATCH` (13-stop conic gradient) has exactly one definition shared by
      `MoodColorDialog` and `ChooseMoodColors`.
- [ ] A5 `robots.txt`'s stated rationale is consistent with what the root actually renders.
- [ ] A6 `EMPTY_SKY_WEIGHTS` is typed over the mood union so a typo'd key fails typecheck.
- [ ] A7 `TwinkleBalanceHud` claims `aria-haspopup="dialog"` only when an open handler is
      actually wired.
- [ ] A8 No behavior change beyond the fixes; web suites green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [ ] T001 `MoodColorDialog.tsx` — add a danger `Alert` (visible failure branch) inside the
      dialog, fed by the editor's error state; decide whether the card-level Alert in
      `MoodColorsTab.tsx:70` stays as a secondary notice or goes.
- [ ] T002 `MoodColorsTab.tsx:35-36` — rewrite the comment describing where the failure notice
      lives to match the new reality.
- [ ] T003 Test: a refused `writeMoodColor` shows the in-dialog failure notice and keeps the
      draft editable (extend `MoodColorsTab.test.tsx`).
- [ ] T004 `DemoPage.tsx:452-455` + `DemoStarPanel.tsx` — recall-beat gating: re-arm the
      auto-open when the panel closes with the beat undone, or thread a `canClose`-style prop
      (match `DemoDecorationSheet`'s documented model); test the dismiss-mid-beat path.
- [ ] T005 [P] `MoodColorPicker.tsx:65-67` — clamp/round the hue before it reaches the input
      (or widen `max`) so value ≤ max always holds.
- [ ] T006 [P] Extract `RANDOM_SWATCH` to one shared home (per `/fe-architecture`: a shared
      segment both features may import) and point both call sites at it.
- [ ] T007 [P] `apps/web/public/robots.txt` — fix the `/login` rationale to describe the root
      as-is (the root renders the sign-in door and stays crawl-allowed via canonical → `/about`).
- [ ] T008 [P] `apps/web/src/widgets/empty-sky/config/illustration.ts:39` — type the weights
      as `Record<(typeof EMPTY_SKY_MOODS)[number], number>`.
- [ ] T009 [P] `TwinkleBalanceHud` — render `aria-haspopup`/button semantics conditionally on
      `onOpenDetail` presence.
- [ ] T010 Reflect into docs: `spec/policy/ux/emotion-palette.md` (failure surface lives in
      the dialog), `spec/policy/ux/demo.md` (recall beat dismiss gating).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/policy/ux/emotion-palette.md`, `spec/policy/ux/demo.md`.

## Affected files (blast radius)

- `apps/web/src/features/change-mood-colors/ui/MoodColorDialog.tsx` — features/change-mood-colors/ui
- `apps/web/src/features/change-mood-colors/ui/MoodColorsTab.tsx` (+ `MoodColorsTab.test.tsx`)
- `apps/web/src/features/change-mood-colors/ui/MoodColorPicker.tsx`
- `apps/web/src/features/choose-mood-colors/ui/ChooseMoodColors.tsx` (swatch extraction call site)
- `apps/web/src/pages/demo/ui/DemoPage.tsx`, `apps/web/src/pages/demo/ui/DemoStarPanel.tsx`
- `apps/web/src/widgets/empty-sky/config/illustration.ts`
- `apps/web/src/features/twinkle-balance-hud/ui/TwinkleBalanceHud.tsx`
- `apps/web/public/robots.txt`
- `spec/policy/ux/emotion-palette.md`, `spec/policy/ux/demo.md` (doc reflection)

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
