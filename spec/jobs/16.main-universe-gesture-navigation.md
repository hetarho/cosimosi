---
job: "16"
type: change
source: changes/08.main-universe-gesture-navigation
plan: plan/06.universe-canvas
status: todo
title: 메인 우주 제스처 항행 UX 전면 재생성
---

# Job 16: 메인 우주 제스처 항행 UX 전면 재생성  (change)

> 구현 작업 문서. 출처 스펙: [changes/08.main-universe-gesture-navigation](../changes/08.main-universe-gesture-navigation.md).
> `/implement-job 16`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/08.main-universe-gesture-navigation에서)
<!-- 출처 스펙의 수용 기준. 구현 후 각 항목이 코드에서 참인지 검증한다. -->
- [ ] A1 **용어 교체.** 메인 우주 화면에서 카메라 모드 이름, 버튼 title, aria-label, 안내 copy에 `성운`/`회상`이 카메라 모드명으로 노출되지 않는다. 토글은 **멀리서 내 우주 보기** 와 **별들 가까이서 탐험하기** 로 표시된다.
- [ ] A2 **회상 도메인 보존.** 별 선택 후 2초 dwell로 `RecallMemory`가 발생하는 도메인 행위 이름과 동작은 그대로다. 카메라 모드 이름 변경이 회상 패널, 공동 회상, 잠든 별 재점화에 회귀를 만들지 않는다.
- [ ] A3 **근접 광원 이동.** 별들 가까이서 탐험하기 모드에서 카메라/탐험자가 이동하면 `StarField.selfLightPos`가 같은 기준 위치로 갱신되어 별 표면 반사가 함께 이동한다. 원거리 모드에서는 중심 자아 광원 규칙이 유지된다.
- [ ] A4 **광원 채널 경계.** 이동 광원은 반사 채널만 바꾼다. `selfGlow`, `activation`, `λ_eff`, 별 색, 별 좌표, `A_MIN` 밝기 바닥은 변하지 않는다.
- [ ] A5 **근접 한 손가락 look.** 별들 가까이서 탐험하기에서 한 손가락/마우스 드래그가 고개 회전으로 동작한다. 좌우는 yaw, 상하는 pitch이며, 기존 화살표 버튼과 같은 방향 감각이다.
- [ ] A6 **근접 두 손가락 전후진.** 별들 가까이서 탐험하기에서 두 손가락을 올린 뒤 위로 움직이면 전진, 아래로 움직이면 후진한다. 추력은 deadzone 이후 이동량에 비례해 ramp하고, 손을 떼면 기존 관성/제동으로 멈춘다.
- [ ] A7 **근접 버튼 비주도화.** 터치 지원 모바일에서 전진/후진/화살표 `NavPad`가 기본 조작 표면으로 고정 노출되지 않는다. 데스크톱 키보드와 접근성 fallback은 유지된다.
- [ ] A8 **원거리 한 손가락 회전 유지.** 멀리서 내 우주 보기에서 기존 한 손가락/마우스 arcball 회전, wheel zoom, pinch zoom은 회귀 없이 동작한다.
- [ ] A9 **원거리 두 손가락 pan.** 멀리서 내 우주 보기에서 두 손가락을 같은 방향으로 움직이면 화면 평면 기준으로 camera와 target이 함께 이동해 조망 기준점이 중앙에 고정되지 않는다.
- [ ] A10 **원거리 double-tap-hold zoom.** 멀리서 내 우주 보기에서 double-tap-hold 후 위로 드래그하면 zoom in, 아래로 드래그하면 zoom out이 된다. 이 gesture가 lock된 동안 pan은 발생하지 않는다.
- [ ] A11 **제스처 충돌 방지.** 탭/드래그/별 선택/두 손가락/pinch/double-tap-hold가 lock 규칙으로 분리된다. 별을 탭하면 선택되고, 드래그 중 별이 잘못 선택되지 않으며, 두 손가락 gesture가 `onPointerMissed` dismiss를 유발하지 않는다.
- [ ] A12 **브라우저 gesture 제어.** 우주 캔버스는 Pointer Events 기반으로 동작하고, 캔버스 표면에 `touch-action: none` 또는 동등한 처리를 적용해 브라우저 스크롤/핀치와 커스텀 gesture가 충돌하지 않는다. 차단 범위는 우주 캔버스에 한정된다.
- [ ] A13 **전환/포커스 중 stand down.** `flyingToStar`, `framingDiary`, `modeTransition`, 별 focus 중에는 사용자 gesture가 유도 비행/포커스 컨트롤러와 싸우지 않는다. 전환이 끝나면 해당 모드의 gesture가 다시 활성화된다.
- [ ] A14 **값 단일 출처.** gesture deadzone, sensitivity, double-tap 시간/거리, pan/zoom 속도는 `spec/values.yaml`에서 생성된 `VALUES`를 사용한다. 새 하드코딩 튜닝 숫자가 없다.
- [ ] A15 **성능/아키텍처.** pointermove가 React state 리렌더를 유발하지 않는다. 연속 입력은 ref/useFrame 경로로 처리되고, `shared/lib`/model 순수 모듈은 three/React/DOM을 import하지 않는다.
- [ ] A16 **모바일 검증.** Playwright 또는 수동 모바일 시뮬레이션으로 최소 390x844와 데스크톱에서 다음을 확인한다: 근접 look, 근접 두 손가락 전/후진, 원거리 pan, 원거리 zoom scrub, 별 탭 선택, 빈 우주 탭 dismiss.
- [ ] A17 build·lint·steiger·관련 vitest가 통과하고, 우주 셸 첫 로드/빈 우주/렌더러 fallback/일기 조망/fly-to에 회귀가 없다.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- 어떻게 만드나. 위→아래 순서. [P]=서로 다른 파일·의존 없는 병렬. (gen)/(migrate)/(gen:values) 표시.
     config·튜닝 수치는 코드 하드코딩 금지 — spec/values.yaml에 추가 후 (gen:values)로 생성 상수 import. -->
- [ ] T001 …

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙
- 건드리는 tech/policy/values: <!-- -->

## 영향 파일 (blast radius)
<!-- 출처 스펙·코드 grep으로 찾은 정확한 경로 — 이 범위 밖은 안 건드린다. -->

## 검증 / DoD
- [ ] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [ ] (type=change면) 기존 plan 수용 기준 회귀 없음
- [ ] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter ./frontend build`·`lint` / BE `go vet ./... && go build ./...`(Docker) 통과(해당 시)
- [ ] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음

## 리뷰
- [ ] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] `plan/`을 새 현실로 갱신 · 영향받은 `policy/**`·`tech/**` · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [ ] (type=change면) `changes/` 출처 문서를 `changes/archive/`로 이동
- [ ] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
