---
job: "34"
type: change
source: changes/20.nightly-rework
plan: plan/27.nightly-consolidation
status: todo
title: 야간 배치 재작성 (반지름 스코프·링크 재가중·spread·요지화 단계·재-KNN)
---

# Job 34: 야간 배치 재작성 (반지름 스코프·링크 재가중·spread·요지화 단계·재-KNN)  (change)

> 구현 작업 문서. 출처 스펙: [changes/20.nightly-rework](../changes/20.nightly-rework.md).
> `/implement-job 34`이 구현. **선행: job 32·33(망각 토대).** 가장 큰 변경 — DB 마이그레이션 포함.

## 인수 조건 (acceptance — changes/20.nightly-rework에서)
- [ ] A1 잡이 04:00 KST(19:00 UTC)에 깨어난다.
- [ ] A2 재안정화·재분배가 **반지름 임계 안 별에만** 적용(멀리 표류한 별 좌표 안 끌려옴).
- [ ] A3 야간 후 `temporal` 링크 weight↓, `semantic` 링크 weight↑(상한 내).
- [ ] A4 재분배 후 성단이 한 점에 안 뭉치고 3D 분산(같은 입력→같은 결과, 결정론).
- [ ] A5 요지화: 반지름 임계 초과 시 `abstraction_stage` +1(0→…→4 단조, 최대 4). 나이/회상 트리거 폐기.
- [ ] A6 가지치기 후에도 **모든 별 degree ≥ 1**(완전 고립 없음). 링크 row DELETE 없음.
- [ ] A7 재-KNN 패스가 고립/끊긴 옛 별에 닮은 기억 생기면 새 링크 생성.
- [ ] A8 schemaBonus가 코드·values에서 사라짐.
- [ ] A9 원본 record·기존 별 row 무변경·무삭제(잡 재실행 안전).

## 구현 체크리스트 (implementation)
- [ ] T001 (migrate) DB: 별 `abstraction_stage smallint default 0`(0~4) 컬럼. `memory_links`에 `severed boolean`/약화 표현(끊김 처리 방식). `pnpm db:migrate` + sqlc 재생성.
- [ ] T002 (gen:values) `spec/values.yaml`: 시각(19 UTC), 반지름 스코프 임계, 링크 재가중 계수(temporal↓·semantic↑), spread 강도, 요지화 단계별 반지름 임계 4개, 재-KNN 주기/임계. **schemaBonus 제거.**
- [ ] T003 [consolidate.go](../../backend/internal/job/consolidate.go) 시각 상수 `consolidateHourUTC` 18→19.
- [ ] T004 재안정화/재분배에 **반지름 스코프** — 반지름 임계 안 별만 force-sim·centroid 당김 대상. (별 반지름은 32 공식; 서버는 stable 좌표/원자료로 근사.)
- [ ] T005 재분배에서 **schemaBonus 분기·상수 제거**.
- [ ] T006 **링크 재가중 패스**(신규): `temporal` 계열 weight 하향, `semantic` 상향(상한 유지). link_type 기준 SQL/로직.
- [ ] T007 **spread 패스**(신규): 재분배 후 성단별 약한 결정론 변위(seed 기반, Math.random 금지)로 3D 분산.
- [ ] T008 **요지화 재작성**: 트리거 age/recall → **반지름 임계 초과**, 연속 form_seed_delta → `abstraction_stage` 이산 +1(GREATEST 단조, ≤4). 변천사 append 유지.
- [ ] T009 **가지치기 + 마지막 1링크 보호**: 약한 링크 weight 하향/severed, 단 별마다 max-weight 1개 보호(degree≥1 보장). DELETE 금지.
- [ ] T010 **재-KNN 패스**(신규): 주기적으로 옛 별 의미 KNN 재실행 → 신규 닮은 기억과 링크. (severed 링크 되살림 포함.)
- [ ] T011 `RunConsolidation` 트랜잭션에 신규 패스 통합(원자성·멱등 유지).
- [ ] T012 테스트: `consolidate_test.go`/`worker_*_test.go` — 반지름 스코프·재가중·단계 승급·degree≥1·재-KNN·spread 결정론.

## 참고 (grounding)
- 헌법: [00.overview](../plan/00.overview.md) §불변 원칙 — 헌법1·2(삭제 없음, 끊김도 weight/플래그)·3
- 선행: job 32·33(망각 토대). 소비처: job 35(단계→형태)·36(단계→AI).
- 건드리는: [policy/domain/universe.md](../policy/domain/universe.md) §야간 공고화, `spec/values.yaml`

## 영향 파일 (blast radius)
- `backend/internal/job/consolidate.go`·`repository_pg.go`·`excitability.go`(재-KNN 재사용)
- DB 마이그레이션(`abstraction_stage`, link severed), `backend/internal/db/queries/**`·sqlc gen
- `spec/values.yaml`, `spec/policy/domain/universe.md`, `spec/plan/27.nightly-consolidation.md`

## 검증 / DoD
- [ ] 위 **인수 조건** 전 항목 참
- [ ] 기존 plan 27 회귀 없음(4패스 멱등·재시도 안전)
- [ ] `pnpm db:migrate`·`pnpm gen`·`pnpm gen:values` 적용
- [ ] BE `go vet ./... && go build ./...`(Docker)·`go test ./internal/job/...` 통과
- [ ] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·records 본문 UPDATE 없음

## 리뷰
- [ ] `/code-review` 반영 · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] `plan/27` 갱신(신규 패스·단계·재가중) · `policy/domain/universe.md` · `spec/values.yaml`
- [ ] `changes/20.nightly-rework` → `changes/archive/`
- [ ] 00.overview ✅ · `status: done`
