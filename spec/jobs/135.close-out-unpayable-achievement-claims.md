---
job: '135'
type: 'refactor'
source: 'code-review/11.jobs-116-128-demo,-sequence,-landing-and'
plan: 'none'
status: todo
title: 'Close out unpayable achievement claims'
---

# Job 135: Close out unpayable achievement claims (refactor)

> Implementation work doc. Source spec: [code-review/11.jobs-116-128-demo,-sequence,-landing-and](../code-review/11.jobs-116-128-demo,-sequence,-landing-and.md).
> /cosimosi:implement-job 135 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

Covers **R008 (P3, PLAUSIBLE)** — the most speculative finding in review 11, and the only one that
**must not be built before its owner decision is settled**.

**The gap.** `SettleClaims` (`apps/api/internal/achievement/claim.go:180-186`) skips a row whose
achievement has left the catalog: `row, published := LookupAchievement(...)`, and when `!published` it
`continue`s without settling or otherwise closing the row. The loop's filter at `:177` is
`if !record.Claimed() || record.Settled() { continue }`, so the row stays in scope on every future
call. It sits at `claimed_at NOT NULL, paid_at NULL` forever.

**Why that is not merely untidy.** Per the design's own statement of what that state means
(`claim.go:28-31`): "the row can SAY it: `paid_at` is stamped only once a leg returns, so the read
answers claimed-and-unsettled and the client keeps a retry affordance instead of hiding the row." So
the user is left with a permanent retry affordance for a reward that can never land. The existing
comment is candid that no retry will help — but _skipping_ is not the same as _closing_, and the
current representation has no way to say "abandoned", so it says "still owed".

**Owner decision to settle FIRST** (review 11, _Questions / Tradeoffs_). The code comments are unusually
clear that this state is understood, which raises a real possibility that it is intentional. Choose one
before writing code:

- **(a) Product rule** — "a claimable achievement never leaves the catalog." Then the correct fix is a
  **boot-time assertion**, not a new column. Cheaper, more honest, and it matches the existing
  composition-root refusals (`claim.go:161-164` notes the root already refuses to boot on a reward with
  no payable leg, both legs, or an unpublished ornament — this is the same family of check).
- **(b) It can happen** — then the row needs a way to say "abandoned", plus a read that presents it as
  closed rather than pending.

**Recommendation: (a).** It is a smaller change, it fits an existing pattern, and it converts a runtime
mystery into a startup failure. Build (b) only if the product genuinely intends to retire achievements
after they are claimable. **The checklist below branches — do only the chosen arm.**

## Acceptance Criteria (from code-review/11)

Common to both arms:

- [ ] A1 A claimed row whose achievement is no longer published can no longer sit indefinitely in a
      state the read reports as "claimed and still owed".
- [ ] A2 `apps/api/internal/achievement/claim_test.go` covers the de-published case. The current suite
      covers the payable legs but not this one, which is why the gap is invisible today.
- [ ] A3 **No behavior change for the payable path.** Claiming, paying, the settle stamp, the drain and
      the idempotency-on-`claim_id` guarantees are all untouched — including the deliberate choices
      review 11 verified as sound: `stampSettled` swallowing its error (`claim.go:145-152`),
      `SettleClaims` attempting every row before reporting the first failure (`:176-188`), and the
      no-retry-cap treatment via `retriedIndefinitely` (`memory/types.go:48-49`).
- [ ] A4 Per-user isolation is preserved: every statement stays conjunctively user-scoped, and
      `pnpm lint:persistence` is green. `SettleClaims` is user-scoped by construction and must remain so.

Arm (a) — boot assertion:

- [ ] A5a The composition root refuses to boot when a **claimable** achievement id is absent from the
      published catalog, alongside the existing reward-shape refusals. The failure names the offending
      id and is fatal at startup, not deferred to a runtime path.
- [ ] A6a `SettleClaims`' `!published` branch becomes genuinely unreachable, and its comment says so by
      pointing at the boot check rather than describing a state that can occur.

Arm (b) — representable abandonment:

- [ ] A5b A claimed-but-unpayable row is durably marked (an `abandoned_at` column, migration
      `000NN_*.sql` — **take the next free number from the migration tree, not from any table in
      00.overview**, per that doc's standing rule) with a `CHECK` that keeps it consistent with
      `claimed_at` / `paid_at`, mirroring `00026_achievement_paid_at.sql:15-16`'s constraint style.
- [ ] A6b The read surface presents such a row as closed, not pending, so the retry affordance
      disappears. Trace the FE achievement-list surface to confirm — review 11 marked this finding
      PLAUSIBLE precisely because that trace was not done.

## Implementation Checklist

- [ ] T001 **Settle the owner decision (a) or (b) and record it in this doc before any code.** Job 122
      set the precedent of recording such decisions in the job's preamble; do the same here.
- [ ] T002 Trace the read path first, either way: from `record.Settled()` through
      `internal/achievement/rpc/server.go` to the FE `features/achievement-list`, and confirm what a
      claimed-and-unsettled row actually renders. This is the evidence R008 was missing, and it may
      itself decide the arm.

Arm (a) — boot assertion:

- [ ] T003a Add the catalog-consistency check beside the existing composition-root reward validations
      (`apps/api/cmd/api/achievement.go`, and the worker's equivalent in `apps/api/cmd/worker/main.go`
      — both roots build an `achievement.Service`, so both must refuse identically or the worker will
      happily run against a catalog the API rejected).
- [ ] T004a Reduce `SettleClaims`' `!published` branch to a defensive no-op whose comment points at the
      boot check. Do not delete the branch — `LookupAchievement` still returns two values, and a silent
      fallthrough would be worse than an explicit unreachable one.
- [ ] T005a Test the refusal: a root wired with a claimable-but-unpublished id fails to construct, with
      the id in the error.

Arm (b) — representable abandonment:

- [ ] T003b (migrate) Write the migration adding `abandoned_at TIMESTAMPTZ` with its `CHECK`, plus a
      `COMMENT ON COLUMN` in the house style of `00026_achievement_paid_at.sql:18-20`. Reserve the
      number from the tree.
- [ ] T004b (gen) Extend the query in `apps/api/db/queries/achievement/achievement.sql` and run
      `pnpm gen:sql` (or `pnpm gen`). Never hand-edit `db/gen`.
- [ ] T005b Add the domain predicate beside `Claimed()` / `Settled()` in
      `apps/api/internal/achievement/types.go`, and stamp it from `SettleClaims`' `!published` branch.
- [ ] T006b Extend the RPC projection and the FE read so the row presents as closed (A6b). Keep the
      handler thin — the decision belongs in context behavior, per §2.9 #7.
- [ ] T007b (migrate) Verify the down migration cleanly reverses, and run `pnpm db:migrate`.

Both arms:

- [ ] T008 Add the `claim_test.go` case (A2) and confirm it fails before the fix.
- [ ] T009 Re-verify the untouched guarantees in A3 with the existing suites —
      `claim_test.go`, `service_test.go`, `store_fake_test.go`, `rpc/server_test.go` and
      `pg/store_integration_test.go` all cover the settle pairing and must stay green unmodified except
      for the new case.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 — no deletes of domain rows; this
  job stamps or asserts, and never removes an `achievement_progress` row. `[I1]`'s "the system does not
  delete" applies to the audit trail here as much as to memories.
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) **§2** — `cmd/` is the composition
  root (arm (a) lives there, not in the context package); §2.4's dependency rule; §2.9 #7 keeps the RPC
  handler thin (arm (b)); each context owns its tables, so only `achievement` touches
  `achievement_progress`. §4's per-user isolation governs every new statement. Invoke
  `/be-architecture` before placing any new file.
- tech / policy / values touched: [tech/achievements.md](../tech/achievements.md) — record the chosen
  semantics either way. No `values.yaml` change (no tuning scalar involved).

## Affected files (blast radius)

Arm (a):

| Path                                          | Placement             | Change                         |
| --------------------------------------------- | --------------------- | ------------------------------ |
| `apps/api/cmd/api/achievement.go`             | composition root      | catalog-consistency boot check |
| `apps/api/cmd/worker/main.go`                 | composition root      | the same refusal, identically  |
| `apps/api/internal/achievement/claim.go`      | `achievement` context | `!published` branch + comment  |
| `apps/api/internal/achievement/claim_test.go` | `achievement` test    | the de-published case          |

Arm (b) additionally:

| Path                                                   | Placement               | Change                        |
| ------------------------------------------------------ | ----------------------- | ----------------------------- |
| `apps/api/db/migrations/000NN_*.sql`                   | `achievement`-owned DDL | `abandoned_at` + CHECK        |
| `apps/api/db/queries/achievement/achievement.sql`      | sqlc input              | stamp + read the new column   |
| `apps/api/db/gen/**`, `apps/api/internal/gen/**`       | generated — do not edit | `pnpm gen` output             |
| `apps/api/internal/achievement/types.go`               | `achievement` domain    | the new predicate             |
| `apps/api/internal/achievement/{pg/store.go,ports.go}` | `achievement` adapter   | the stamp path                |
| `apps/api/internal/achievement/rpc/server.go`          | `achievement` transport | projection only               |
| `apps/web` + `apps/mobile` `features/achievement-list` | FE features             | present as closed (both apps) |

Out of scope either way: the `store.GrantOwnership` transaction, the twinkle ledger, the job queue's
`retriedIndefinitely` set, and the `paid_at` semantics from migration `00026` — all verified sound.

## Verification / DoD

- [ ] Every **Acceptance Criteria** item for the **chosen arm** is true in the current code
- [ ] The owner decision is recorded in this doc's preamble (T001)
- [ ] Codegen / migration applied (arm b): `pnpm gen` · `pnpm db:migrate` · `pnpm check:gen` green
- [ ] `pnpm vet:api` and `pnpm build:api` pass (Docker) · `pnpm test:api` green
- [ ] `pnpm lint:persistence` green — every new statement conjunctively user-scoped (A4)
- [ ] (arm b) web↔mobile parity: both apps' achievement lists present the closed row the same way
- [ ] `pnpm check` green end to end
- [ ] Constitution sanity: no row deletes on `records` / `memories` / `memory_links`, no body UPDATE on
      `records`, and no `achievement_progress` row deleted
- [ ] Architecture self-audit passed (`/be-architecture`, plus `/fe-architecture` for arm b): boot
      checks in `cmd/`, policy in context behavior, handler thin, no sqlc row leaking inward

## Review

- [ ] `/code-review` applied (rejections noted with reason) · `/codex:review --background` — this
      touches a payout path, where a wrong change can credit twice or strand a reward

## After completion — reflect into the SSOT

- [ ] Update [tech/achievements.md](../tech/achievements.md) with the chosen semantics — either "a
      claimable achievement never leaves the catalog, enforced at boot" or the abandonment state and
      how the read presents it
- [ ] 00.overview progress board ✅ (review 11 → job 135) · this doc's frontmatter `status: done`
