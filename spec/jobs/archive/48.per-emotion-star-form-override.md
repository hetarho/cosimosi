---
job: "48"
type: change
source: changes/30.per-emotion-star-form
plan: plan/52.form-skins
status: done
title: 감정별 별 형태 오버라이드 — 전역 기본 + 감정별 선택(서버 동기)
---

# Job 48: 감정별 별 형태 오버라이드 — 전역 기본 + 감정별 선택(서버 동기)  (change)

> 구현 작업 문서. 출처 스펙: [changes/30.per-emotion-star-form](../changes/30.per-emotion-star-form.md).
> `/implement-job 48`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/30.per-emotion-star-form에서)
<!-- 출처 스펙의 수용 기준. 구현 후 각 항목이 코드에서 참인지 검증한다. -->
- [x] A1 별 형태에 **전역 기본 + 감정(13종)별 오버라이드**가 있다. 오버라이드가 있는 감정의 별은 그 룩으로, 없는 감정은 전역 기본 룩으로 렌더된다.
- [x] A2 스위처에 **감정 선택 드롭다운**(전역 기본 + 13감정)이 있고, 선택된 대상에 따라 기본/특정 감정 룩이 갱신된다.
- [x] A3 감정별 형태 선택이 **서버 동기**된다 — `GetSettings`로 시드, `UpdateSettings`로 부분 업서트(바꾼 mood만), 다른 기기에서 같은 설정이 보인다. 빈 맵(오버라이드 0개)은 기존 전역 단일 동작과 동치다.
- [x] A4 미소유 룩을 감정에 배정하려 하면 BE가 거부한다(plan 44 잠금 규약). 룩은 한 번 소유하면 여러 감정에 자유 배정된다.
- [x] A5 미지 mood·미지 룩 값은 안전 폴백(전역 기본)하고 크래시하지 않는다.
- [x] A6 색 오버라이드(`emotion_colors`)·나·시냅스·배경 선택은 이 변경으로 안 바뀐다(회귀 경계). proto 필드 번호 append-only.
- [x] A7 FE `build`·`lint`·`steiger`·test + BE `build`·test 통과. 공유 우주(35/37)·근접 탐험(49) 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- 어떻게 만드나. 위→아래 순서. [P]=서로 다른 파일·의존 없는 병렬. (gen)/(migrate)/(gen:values) 표시.
     config·튜닝 수치는 코드 하드코딩 금지 — spec/values.yaml에 추가 후 (gen:values)로 생성 상수 import. -->

**proto (계약 — append-only, 헌법5)**
- [x] T001 `proto/cosimosi/v1/memory.proto`: `EmotionColor`(필드 1·2) 옆에 `message EmotionForm { Mood mood = 1; string look = 2; }` 추가. `Settings`에 `repeated EmotionForm emotion_forms = 6;`(기존 1~5 불변, 신규 6), `UpdateSettingsRequest`에 `repeated EmotionForm emotion_forms = 6;` 추가. 주석은 `emotion_colors`의 "사용자가 바꾼 mood만 업서트(부분 패치)" 규약을 그대로 복사.
- [x] T002 `pnpm gen` (gen) — proto → TS(`frontend/src/shared/api/gen/...`)·Go(`backend/internal/...gen`) 재생성. 손수정 금지.

**DB (마이그레이션 — 색 패턴 미러)**
- [x] T003 `backend/internal/db/migrations/00015_user_emotion_forms.sql` (migrate) — `user_emotion_forms(user_id, mood, look, PRIMARY KEY(user_id, mood))` 신규 테이블. `00002`의 `user_emotion_colors` 정의를 본떠 up/down 작성. `schema.sql`에도 동일 테이블 반영.
- [x] T004 `backend/internal/db/queries/settings.sql`: `ListUserEmotionForms :many`(`SELECT mood, look ... WHERE user_id=$1 ORDER BY mood`)·`UpsertUserEmotionForm :exec`(ON CONFLICT 업서트) 추가 — `EmotionColor` 쿼리 쌍을 미러. sqlc 코드젠은 T002 `pnpm gen`에 포함(아니면 별도 sqlc gen).

**BE (settings 서비스 — 부분 업서트 + 소유권 검증)**
- [x] T005 `backend/internal/settings/settings.go`: `EmotionForm{ Mood, Look string }` 구조체 + `Settings.EmotionForms []EmotionForm` + `Patch.EmotionForms []EmotionForm`. `EmotionColor` 옆에 평행 정의.
- [x] T006 `backend/internal/settings/handler.go`: 요청 `GetEmotionForms()` → `Patch.EmotionForms` 매핑(mood 유효성=handler, 색과 동일), 응답 `Settings.EmotionForms` → proto `EmotionForm` 매핑(L54·L116 색 루프 미러).
- [x] T007 `backend/internal/settings/service.go` `Update`: 순수검사 단계에 emotion_forms 각 `look`이 `isKnownItem` 아니면 `ErrUnknownItem`; 소유권 단계에 각 look이 `isOwned` 아니면 `ErrNotOwned`(plan 44 잠금 규약 재사용 — A4). 미소유/미지면 **아무 행도 안 씀**(A2/A4 규율 유지).
- [x] T008 `backend/internal/settings/repository_pg.go`: `Get`에서 `ListUserEmotionForms` 읽어 `Settings.EmotionForms` 채우고, `Update`에서 `Patch.EmotionForms` 각 항목 `UpsertUserEmotionForm`(색 L45·L77 미러).
- [x] T009 [P] `backend/internal/settings/service_test.go`: 빈 맵=기존 동치, 부분 업서트(바꾼 mood만), 미소유 look 거부(`ErrNotOwned`), 미지 look 거부(`ErrUnknownItem`) 케이스 추가.

**FE store (전역 기본 + 감정별 맵)**
- [x] T010 `frontend/src/entities/appearance/model/store.ts`: `starFormByEmotion: Record<string, StarLook>`(빈 맵=전부 전역 기본) 상태 + `setStarFormByEmotion(mood, look)`·전역기본 setter 유지(`setObject`). `SelectionSnapshot`/`savedSelection`·`commitSelection`·`resetAssets`·`applyServerSettings`(서버 시드)에 통합. `normalizeStarLook`으로 시드/하이드레이트 경계 정규화(미지 mood·look 폴백 — A5). emotionColors와 동일하게 **메모리 전용**(per-user 자산 미영속, 공용 PC)인지 색 정책과 맞춰 결정 — 색은 영속 안 함(L190).
- [x] T011 `frontend/src/entities/appearance/api/settings-query.ts`: `toServerAppearance`가 `settings.emotionForms`→`Record<mood,look>` 맵으로 디코드(색 L82~ 미러), `applyServerSettings`로 store 시드. 저장 경로 — 별 룩 저장 시 바꾼 mood만 `UpdateSettings.emotion_forms`로 부분 전송(`saveEmotionColors` 패턴). 전역 기본은 기존 `starObject`로.

**FE 렌더 (mood별 룩 = 오버라이드 ?? 전역 기본 → look×stage 버킷)**
- [x] T012 `frontend/src/entities/star/ui/StarField.tsx`: 별 prop을 단일 `object`(전역 기본) + `starFormByEmotion` 맵으로 확장. 별 instance의 look = `parseStarLook(starFormByEmotion[mood] ?? object)`. **InstancedMesh 버킷을 `look×stage`로 일반화**(현재 `${look}-${stage}` 단일 look × STAGE_LEVELS → 사용 중인 look 집합 × STAGE_LEVELS, 헌법8: 별 수와 무관한 소수 고정 메시 유지). 각 별을 그 (look,stage) 버킷 슬롯에 배치, raycast→별 매핑도 버킷별로.
- [x] T013 `frontend/src/widgets/universe-canvas/`(StarField 소비처: `OverlayUniverse.tsx`·`UniverseCanvas.tsx` 등): `object`와 함께 `starFormByEmotion`을 store에서 읽어 StarField에 전달.

**FE 스위처 UI (감정 선택 드롭다운)**
- [x] T014 `frontend/src/features/switch-appearance/ui/AppearanceSwitcher.tsx`: 별 룩 피커(`star-look`, slot:`look`)에 "어느 감정에 적용?" 드롭다운(전역 기본 + `MOODS` 13감정, 라벨 `MOOD_LABEL`) 추가. 선택 대상이 전역기본이면 `setObject`, 특정 mood면 `setStarFormByEmotion(mood, look)`. 피커 `value`도 선택 대상에 따라 표시(오버라이드 ?? 전역 기본).
- [x] T015 [P] (gen:values) `spec/values.yaml` — 신규 정전 수치는 원칙적으로 없음(룩 카탈로그·가격은 change 29 소관). 드롭다운 기본 대상('전역 기본') 등 UX 상수가 필요하면 values에 추가 후 import, **하드코딩 금지**. 불필요하면 이 태스크 스킵 사유 기록.

**검증/테스트**
- [x] T016 [P] FE: 빈 맵 동치·미지 mood/look 폴백 단위 테스트(store normalize) + 가능 시 렌더 버킷 회귀.

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 — §5(proto=DTO append-only, 도메인 순수)·§3(형태는 클라 파생)·§8 개정(룩×단계 소수 고정 메시).
- 의존: **change 29 / job 45**(단일 축 3룩·단계별 지오메트리 — 이미 구현, commit `d985672`). 이 job은 그 위에 *선택 차원*만 얹는다.
- 선례 패턴: per-mood **색** 오버라이드(`emotion_colors`·`user_emotion_colors`·`saveEmotionColors`) — 이 job의 형태 오버라이드는 그 패턴의 평행 복제다.
- 건드리는 policy/tech/values:
  - [policy/domain/customization.md](../policy/domain/customization.md) — 별 형태 선택 전역 단일 → 전역 기본 + 감정별 오버라이드(소유 룩 내 자유 배정).
  - [policy/domain/star.md](../policy/domain/star.md) §형태 — 별 렌더 룩 = (mood 오버라이드 ?? 전역 기본).
  - [tech/architecture.md](../tech/architecture.md) §데이터모델 — 마이그레이션 목록에 `00015_user_emotion_forms` 추가, proto `Settings` append-only 필드 6.
  - [values.yaml](../values.yaml) — 신규 수치 원칙 없음(필요 시 UX 상수만).

## 영향 파일 (blast radius)
<!-- 출처 스펙·코드 grep으로 찾은 정확한 경로 — 이 범위 밖은 안 건드린다. -->

**proto / gen**
- [proto/cosimosi/v1/memory.proto](../../proto/cosimosi/v1/memory.proto) — `EmotionForm` 메시지 + `Settings`/`UpdateSettingsRequest` 필드 6 (append-only)
- `frontend/src/shared/api/gen/cosimosi/v1/memory_pb.ts`·`backend/internal/.../gen` — `pnpm gen` 산출(손수정 금지)

**backend (settings)**
- [backend/internal/db/migrations/00015_user_emotion_forms.sql](../../backend/internal/db/migrations/) (신규) · [backend/internal/db/schema.sql](../../backend/internal/db/schema.sql)
- [backend/internal/db/queries/settings.sql](../../backend/internal/db/queries/settings.sql) — List/Upsert UserEmotionForm
- [backend/internal/settings/settings.go](../../backend/internal/settings/settings.go) · [handler.go](../../backend/internal/settings/handler.go) · [service.go](../../backend/internal/settings/service.go) · [repository_pg.go](../../backend/internal/settings/repository_pg.go) · [service_test.go](../../backend/internal/settings/service_test.go)

**frontend**
- [entities/appearance/model/store.ts](../../frontend/src/entities/appearance/model/store.ts) · [api/settings-query.ts](../../frontend/src/entities/appearance/api/settings-query.ts)
- [entities/star/model/forms.ts](../../frontend/src/entities/star/model/forms.ts)(이미 `parseStarLook`/`STAR_LOOKS` 제공 — 재사용만) · [entities/star/ui/StarField.tsx](../../frontend/src/entities/star/ui/StarField.tsx)
- [widgets/universe-canvas/ui/](../../frontend/src/widgets/universe-canvas/ui/) StarField 소비처(OverlayUniverse·UniverseCanvas 등 — prop 전달)
- [features/switch-appearance/ui/AppearanceSwitcher.tsx](../../frontend/src/features/switch-appearance/ui/AppearanceSwitcher.tsx)
- [shared/config/mood.ts](../../frontend/src/shared/config/mood.ts)(`MOODS`·`MOOD_LABEL` 재사용 — 드롭다운)

**범위 밖(안 건드림)**: 색 오버라이드 로직(`emotion_colors`/`user_emotion_colors`/`EmotionColorEditor`)·나/시냅스/배경 축·룩 카탈로그/렌더 변형(change 29 소관).

## 검증 / DoD
- [x] 위 **인수 조건** 전 항목이 현재 코드에서 참 (A1–A7 — 렌더 룩×단계 버킷·드롭다운·서버 부분 업서트·소유권 거부·폴백·proto append-only)
- [x] (type=change면) 기존 plan 수용 기준 회귀 없음 (plan 52 form×surface·경제·헌법8 불변; FE 283 + BE 전체 테스트 통과)
- [x] 코드젠/마이그레이션 적용: `pnpm gen`(proto+sqlc) · `pnpm db:migrate`(00015 적용·`\d user_emotion_forms` 확인). values 신규 수치 없음(T015)
- [x] FE `build`·`lint`·`lint:fsd`(steiger)·`test`(283) / BE `go vet && go build && go test ./...`(Docker) 통과
- [x] 헌법 sanity: 신규는 settings 독립 테이블(`user_emotion_forms`)만 — `records`/`memories`/`memory_links` 무관, 삭제·본문 UPDATE 없음

## 리뷰
- [x] `/code-review`(high) 반영 — 정확성 버그 0건(렌더 버킷·store 드래프트·BE 검증 모두 confirm). 정리 3건 반영: ① `moodToProto` 헬퍼 추출(emotion_colors·forms 변환 중복 제거) ② `bucketFor` 도달불가 중간 폴백 제거(`?? lookIndexOf[look]`) ③ 하이드레이트 merge `savedSelection.starFormByEmotion: {}` 리터럴. **기각**: shallow-map-equality 헬퍼를 shared/lib로 추출(3곳, FSD 2계층 가로질러 — 저가치 churn, formDirty 입력은 이미 정규화돼 무해).

## 완료 후 — SSOT 반영
- [x] `plan/`·`policy/**` 갱신 — [plan/52](../../plan/52.form-skins.md)(change 30 헤더 노트)·[policy/domain/star.md](../../policy/domain/star.md)(§형태 감정별 오버라이드 행)·[policy/domain/customization.md](../../policy/domain/customization.md)(별 축·선택·RPC·데이터모델 00015). tech/architecture.md = 무변경(마이그레이션 번호 목록 없음·settings 테이블은 customization.md 소관). values.yaml 무변경(신규 수치 없음).
- [x] `changes/30.per-emotion-star-form.md` → `changes/archive/`로 이동
- [x] 00.overview 진행 현황 ✅(아래) · 이 문서 frontmatter `status: done` · 이 문서 → `jobs/archive/`
