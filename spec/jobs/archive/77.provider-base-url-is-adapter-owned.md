---
job: '77'
type: 'change'
source: 'changes/03.provider-base-url-is-adapter-owned'
plan: 'plan/28.ai-provider-abstraction'
status: done
title: 'provider base url is adapter owned'
---

# Job 77: provider base url is adapter owned (change)

> Implementation work doc. Source spec: [changes/03.provider-base-url-is-adapter-owned](../changes/03.provider-base-url-is-adapter-owned.md).
> /cosimosi:implement-job 77 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from changes/03.provider-base-url-is-adapter-owned)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 `base_url` / `BaseURL` / `BASE_URL` no longer appears in the AI provider-config seam: `ProviderConfig` and `CapabilityConfig` carry no base URL, and `COSIMOSI_LLM_BASE_URL` / `COSIMOSI_EMBEDDING_BASE_URL` are gone from the factory and `.env.example`.
- [x] A2 Each implemented adapter owns its endpoint — a grep shows no base-URL value flowing from config/env into `internal/ai/anthropic` or `internal/ai/voyage`; both still build and call their vendor correctly.
- [x] A3 `ai_provider_keys` has **no** `base_url` column; the admin proto (`ProviderKey`, `SetProviderKeyRequest`), admin `types`/`ports`/`service`/`pg`/`rpc`, and the provider-keys UI carry **no** base URL. `pnpm db:migrate` / `pnpm db:down` still apply/reverse cleanly.
- [x] A4 The admin console manages exactly: per-provider API keys (encrypted at rest, write-only, masked hint) and per-capability provider + model selection — nothing else.
- [x] A5 SSOT is consistent: plan 28 (no "base URL is env"), plan 58, `tech/ai-worker.md`, `tech/admin-console.md`, and `policy/ops/admin.md` all state the endpoint is **adapter-owned**.
- [x] A6 **No regression:** anthropic LLM extraction/semanticize/prediction-error and voyage embedding still work; the keyless mock fallback, per-provider key management, and capability selection are unaffected; `pnpm test:api`, `pnpm build:api`, `pnpm typecheck`, and `pnpm build:web` pass.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

> Pure deletion delta — remove `base_url` from the AI provider-config seam + admin surface; each adapter owns its
> endpoint. No new config (no values.yaml). Run `/be-architecture` for `apps/api`, `/fe-architecture` for the web slice.

- [x] T001 (gen) Remove base URL from the admin proto — `proto/cosimosi/admin/v1/admin.proto`: drop
      `SetProviderKeyRequest.base_url` and `ProviderKey.base_url`. Run `pnpm gen:proto` (regenerates Go + TS; chown
      docker output on WSL). This cascades to the generated admin client — the `ProviderKey.baseUrl` field disappears.
- [x] T002 (migrate) Drop the `base_url` column from `ai_provider_keys`. Migration `00015_admin_console.sql` is
      unreleased (this branch), so edit it in place: remove `base_url TEXT NOT NULL DEFAULT ''` from the
      `ai_provider_keys` CREATE TABLE. `pnpm db:down` then `pnpm db:migrate` to re-apply; confirm down/up clean (A3).
- [x] T003 (gen) sqlc — `apps/api/db/queries/admin/admin.sql`: remove `base_url` from `GetProviderKey`,
      `ListProviderKeys`, and `UpsertProviderKey` (params + selected columns). Run `pnpm gen:sql`.
- [x] T004 Admin BE — drop every base-URL field/param:
      `internal/admin/types.go` (`StoredProviderKey.BaseURL`, `ProviderKeyInfo.BaseURL`),
      `internal/admin/ports.go` (`Store.UpsertProviderKey`/`GetProviderKey`/`ListProviderKeys` shapes; the
      `ai.ConfigReader` `ReadProviderKey` baseURL return, below), `internal/admin/service.go`
      (`SetProviderKey(…, baseURL)` param + audit + `ProviderKeyInfo` mapping), `internal/admin/pg/store.go` (upsert/
      list/get + `ReadProviderKey`), `internal/admin/rpc/server.go` (`providerKeyToProto` base_url,
      `SetProviderKey` handler baseURL). A4: admin keeps only key + provider/model selection.
- [x] T005 AI seam — remove base URL from `internal/ai`:
      `factory.go` (`CapabilityConfig.BaseURL`, `ProviderConfig.BaseURL` via `providerConfig`, the
      `EnvLLMBaseURL`/`EnvEmbeddingBaseURL` consts, `NewAdaptersFromEnv` env reads, `EnvCapabilityConfigs`),
      `runtime.go` (`ConfigReader.ReadProviderKey` baseURL return, `effective()` baseURL wiring, and the `base-URL`
      term in `fingerprint(...)`), `clients.go` (`ProviderConfig.BaseURL` field + any consumer). A1.
- [x] T006 Adapters own their endpoint (A2): `internal/ai/voyage/client.go` — replace the config base-URL with an
      adapter-level endpoint constant (its current default); `internal/ai/anthropic/client.go` — drop the base-URL
      option so the SDK default is used. Update `voyage/client_test.go` + `anthropic/client_test.go` +
      `adapters_test.go` accordingly.
- [x] T007 [P] FE — `apps/web/src/features/admin-ai-config/ui/AiConfigSection.tsx`: remove the base-URL `TextField`,
      the `baseUrl` state, and the `baseUrl` arg to `setProviderKey`/the reset comparison. Remove the now-unused
      `admin_ai_base_url` + `admin_base_url_placeholder` message keys from `packages/i18n/messages/{ko,en}.json` and run
      `pnpm gen:messages`. (`packages/api-client/src/admin.ts` needs no manual edit — the `ProviderKey` type loses
      `baseUrl` via T001 regen.)
- [x] T008 Env/compose — `.env.example`: remove the `COSIMOSI_LLM_BASE_URL` / `COSIMOSI_EMBEDDING_BASE_URL` lines.
      `docker-compose.yml` added no base-URL var (SUPABASE_URL stays), so no change there.
- [x] T009 SSOT reflect (also the After-completion step): plan 28 (drop "base URLs are runtime env/secrets" → endpoint
      is adapter-owned; remove the `COSIMOSI_*_BASE_URL` from §Selection & wiring), plan 58 (remove base URL from the
      `ai_provider_config`/`ai_provider_keys` description, `SetAIConfig`/`GetAIConfig`/`SetProviderKey` surfaces),
      `tech/ai-worker.md` (drop the `COSIMOSI_*_BASE_URL` env lines + "base URLs are env/secrets"),
      `tech/admin-console.md` (§4/§5 drop base URL from the fingerprint + key rows), `policy/ops/admin.md` (rule 4).
- [x] T010 Gates + regression (A6): `pnpm check:gen` (proto/sql byte-stable), `pnpm typecheck`, `pnpm lint`
      (incl. `lint:persistence`, `lint:raw-strings`), `pnpm test:api` (Docker — anthropic/voyage adapter tests + admin
      still green), `pnpm build:api` / `pnpm vet:api`, `pnpm build:web`, and a `pnpm db:down`/`db:migrate` cycle.
- _(No task — non-goals stay unchanged: the adapter registry, provider slots, capability support, API-key encryption,
  provider+model selection, DB→env→mock fallback, metering, error taxonomy, keyless mock, admin authorization. No new
  provider adapters. If a self-hosted endpoint override is later needed, it is that adapter's own env seam — out of
  scope here.)_

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 — no invariant is touched (provider transport
  config only; no diary/memory/economy/emotion behavior changes).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) BE **§2.2** (`internal/ai` supporting
  external-service wrapper; each vendor adapter owns its transport + endpoint at the outermost ring), **§2.4**
  (consumer-owned ports; the `ai.ConfigReader` port shape narrows), **§2.6** (`admin/pg` the only sqlc seam),
  **§2.7** (proto = DTO; the handler stays thin) · FE **§3.1** (the provider-keys `ui` field is removed;
  `model`/`api` unaffected). Invoke `/be-architecture` for `apps/api`, `/fe-architecture` for the web slice. This is
  the read-through of [changes/03.provider-base-url-is-adapter-owned](../changes/03.provider-base-url-is-adapter-owned.md).
- tech / policy / values touched: **values** — none (this change deletes config, adds none; base URL is neither env
  nor values now — adapter-owned). **tech** — `tech/ai-worker.md`, `tech/admin-console.md`. **policy** —
  `policy/ops/admin.md`. **Modifies** plan 28 (provider config contract) and plan 58 (admin AI-config surface). The
  regression boundary is the existing plan-28/58 acceptance criteria minus the base-URL dimension (see DoD).

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

- `proto/cosimosi/admin/v1/admin.proto` — **EDIT**: drop `base_url` from `ProviderKey` + `SetProviderKeyRequest`.
  _(gen)_ · `apps/api/internal/gen/**` · `packages/api-client/src/gen/**` — regenerated.
- `apps/api/db/migrations/00015_admin_console.sql` — **EDIT**: drop `base_url` column from `ai_provider_keys`.
  _(migrate)_
- `apps/api/db/queries/admin/admin.sql` — **EDIT**: base_url out of provider-key get/list/upsert. _(gen sql)_ ·
  `apps/api/db/gen/**` — regenerated.
- `apps/api/internal/admin/{types.go,ports.go,service.go,rpc/server.go}` · `apps/api/internal/admin/pg/store.go` —
  **EDIT**: remove base-URL fields/params (BE admin context).
- `apps/api/internal/ai/{factory.go,runtime.go,clients.go}` — **EDIT**: remove base URL from `ProviderConfig`/
  `CapabilityConfig`, env reads, the `ConfigReader.ReadProviderKey` return, and the fingerprint (BE supporting wrapper).
- `apps/api/internal/ai/anthropic/{client.go,client_test.go}` · `apps/api/internal/ai/voyage/{client.go,client_test.go}`
  · `apps/api/internal/ai/adapters_test.go` — **EDIT**: adapter owns its endpoint (voyage constant, anthropic SDK
  default).
- `apps/web/src/features/admin-ai-config/ui/AiConfigSection.tsx` — **EDIT**: remove the base-URL field + state (FE
  `features/admin-ai-config`).
- `packages/i18n/messages/{ko,en}.json` — **EDIT**: remove `admin_ai_base_url` + `admin_base_url_placeholder`. _(gen
  messages)_
- `.env.example` — **EDIT**: remove `COSIMOSI_LLM_BASE_URL` / `COSIMOSI_EMBEDDING_BASE_URL`.
- `spec/plan/28.ai-provider-abstraction.md` · `spec/plan/58.admin-console.md` · `spec/tech/ai-worker.md` ·
  `spec/tech/admin-console.md` · `spec/policy/ops/admin.md` — **EDIT** at reflect step (SSOT).
- _(Untouched: the adapter registry + `RegisterLLMProvider`/`RegisterEmbeddingProvider`, provider slots, capability
  support, key encryption (`platform/secretbox`), provider+model selection, `AIConfigReader` capability-config read,
  metering, admin authorization; all of `apps/mobile/**`.)_

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria (`pnpm test:api` incl. admin +
      anthropic + voyage suites, `pnpm typecheck`, `pnpm build:web`, `pnpm lint` all green)
- [x] Codegen / migration / values applied: `pnpm gen:proto` · `pnpm gen:sql` · `pnpm gen:messages` ·
      `pnpm db:down`/`db:migrate` cycle clean (`\d ai_provider_keys` shows no `base_url`); `pnpm check:gen` byte-stable
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (`/be-architecture` + `/fe-architecture`): pure deletion, no file moved or
      created; ports stay consumer-owned and domain-shaped; `pnpm lint:fsd:layout` green

## Review

- [x] `/code-review` applied (no findings requiring change) · `codex exec -m gpt-5.5` on the staged diff: **no code
      findings**; two notes — (1) proto `ProviderKey` field 8 reuse (`updated_by` 9→8): accepted, consistent with the
      change's own unreleased-branch premise (migration 00015 edited in place on the same premise; FE+BE regenerate
      and deploy together); (2) anthropic success-path tests no longer exercised `New`: **fixed** by adding
      `TestNewSelectsModel` (default + override), ai suites re-run green.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml`
      (none — this change deletes config, adds none)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
