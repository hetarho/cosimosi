---
job: '35'
type: refactor
source: code-review/04.epic-a-memory-genesis-runtime-and-gate-a
plan: none
status: done
title: Promote Epic A write vertical to packages
---

# Job 35: Promote Epic A write vertical to packages (refactor)

> Implementation work doc. Source spec: [code-review/04.epic-a-memory-genesis-runtime-and-gate-a](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md).
> /cosimosi:implement-job 35 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Result (complete — 2026-07-06)

Landed in two passes. Pass 1 (2026-07-05) fixed R011/A4; the finalizing pass (2026-07-06) performed the R002 bulk
promotion pass 1 had deferred, so every Acceptance Criterion below is now met.

**R011 (A4) — optimistic launch no longer flickers (pass 1).** `use-universe.ts` (both apps) merges server truth with
any optimistically-launched memory not yet visible server-side (server wins on id; optimistic-only rows survive an
eager refetch / stale GET cache), and still clears all stores on a signed-out empty read (per-user isolation
preserved). Fixed identically in web + mobile.

**R002 (A1–A3) — write vertical promoted to packages (finalizing pass).** The pure `model`/`api`/`lib`, the
`writing-flow` machine, the read-model Zustand stores (episodic-memory/neuron/synapse + latent-consumed +
awaken-registry), and the domain→visual channel projections now live once in **`@cosimosi/universe`**; the six
non-forking R3F bindings live once in **`@cosimosi/universe-render`**. Both apps import them verbatim; the app-local
copies were deleted (`git grep` confirms 0 stale imports) and the nine Epic A logic tests moved with the code
(universe: 70 tests). Every promoted file is a byte-identical rename (only import lines changed). `typecheck`/`lint`/
`test` green on both apps.

## Acceptance Criteria (from code-review/04 — R002, R011)

<!-- Behavior preserved (the write flow works identically on both apps), structure fixed to §3.5 promote-on-reuse. -->

- [x] A1 **Pure write-vertical code lives once, in `packages/`.** The writing-flow XState machine + the pure
      `model`/`api`/`lib` modules (proposal, pick-awaken-seed, launch-stars, latent-field, contributors, star/cell-star/
      filament channels) and the read-model stores (episodic-memory/neuron/synapse + latent-consumed + awaken-registry)
      are defined once in **`@cosimosi/universe`** and imported by both apps. No pure module is duplicated. (R002.)
- [x] A2 **Non-forking R3F UI is not duplicated.** The six rendering bindings (`StarLayer`/`CellStarLayer`/
      `FilamentLayer`/`LatentStarField`/`NebulaField`/`AwakenNeuron`) — three only via `@cosimosi/3d-renderer`, no
      DOM/RN primitive — live once in **`@cosimosi/universe-render`** and both apps consume it verbatim. No `*.native`
      sibling exists (none forks a primitive); the genuinely-forked DOM/RN UI (WritingFlowSheet, ReviseControls,
      LaunchButton, NebulaNotice, WriteDiaryFields, ProposedMemoryList) stays app-local. (R002.)
- [x] A3 **The write-vertical tests move with the code and run for both apps.** The nine Epic A logic tests
      (proposal/writing-flow.machine/pick-awaken-seed/launch-stars/latent-field/contributors/star·cell-star·filament
      channels) live next to the promoted code in `@cosimosi/universe` and run under `pnpm test` (70 tests). Because both
      apps import the package, mobile's core write loop is now covered, not shipped as untested copies. (R002.)
- [x] A4 **The optimistic launch no longer clobbers the just-inserted star.** After `LaunchStars`, the optimistic
      memories survive the immediate `GetUniverse` refetch (merge, don't overwrite) OR the eager invalidation is
      deferred so the next natural read reconciles — no flicker. Fixed once (shared), not per-app. (R011.)
- [x] A5 **No behavior change / no regression.** A content-preserving move: `git diff -M` shows the promoted files
      at R097–R100 similarity (only import lines changed; logic byte-identical). The write flow behaves identically on
      web and mobile — full `pnpm typecheck` · `lint` · `test` (both apps + Go) · `build:web` green; steiger + boundaries
      green. The optimistic void/`.catch` drift is unified to `.catch(() => undefined)` (the form both linters accept —
      mobile's eslint forbids `void`).

## Implementation Checklist

<!-- top to bottom. [P] = parallel. Prefer promoting into an existing package over a new one. -->

- [x] T001 Re-verified 0 drift on all byte-identical pairs (9 model/api/lib + 6 R3F UI) before moving.
- [x] T002 Package homes (via `/fe-architecture` + ARCHITECTURE §3.4/§3.5): pure logic + machine + read-model stores +
      channel projections → **`@cosimosi/universe`** (extends the `universe-navigation.machine` precedent; gained
      api-client/config/emotion/memory-logic/zustand deps); the non-forking R3F bindings → a **new
      `@cosimosi/universe-render`** package (deps `@cosimosi/3d-renderer` + `@cosimosi/universe`; react/three peers,
      mirroring `3d-renderer`). Both packages classified as visual/scene paths in the UL lint.
- [x] T003 Moved the 14 pure modules + machine into `@cosimosi/universe` (`git mv`, imports repointed `@x`→
      `@cosimosi/memory`, store→sibling); wrote the package `index.ts` public API; deleted the mobile copies; repointed
      both apps' consumers (use-universe, WritingFlowSheet, proposal-store, nebula-demo-panel).
- [x] T004 Hosted the six R3F bindings once in `@cosimosi/universe-render`; both apps' `UniverseCanvasWidget` import
      them directly. No `*.native` sibling (none forks a primitive). (A2.)
- [x] T005 Moved the nine `*.test.ts` next to the promoted code in `@cosimosi/universe`; added
      `typecheck:universe-render`/`test:universe-render` and wired both into the root umbrellas. `pnpm test` runs them
      (universe 70 tests). (A3.)
- [x] T006 R011/A4 optimistic merge was fixed in the prior pass and is preserved in the (unchanged) app-local
      `use-universe`. The web-`void` / mobile-`.catch()` drift is unified to `.catch(() => undefined)`. (R011, A4.)
- [x] T007 Kept `draft-store` (write-diary), `launched-neurons-store` (launch-stars), and `proposal-store` (writing-flow
      widget) app-local — session/write state, not read-model. `pnpm lint:fsd` (steiger) + boundaries green; the steiger
      config's insignificant-slice scope was updated to the surviving single-reference slices (nebula, universe-canvas).
- [x] T008 Full both-app gate green (typecheck/lint/test/build:web) + Go; web↔mobile parity confirmed (both consume the
      same packages; only DOM/RN forks + app wiring differ).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3.1 (layers/segments; promote **down**, never
  copy sideways), §3.4 (domain→visual seam — the shared R3F UI is a rendering entity), §3.5 (web/mobile parity; shared
  `model`/`api`/machine from `packages/`; `*.native` only where a primitive differs). Invoke `/fe-architecture` and
  `/mobile-architecture`.
- tech / policy / values touched: none expected (pure code relocation); update `spec/tech/*` only if a package boundary
  is documented there.

## Affected files (blast radius)

<!-- FE only. Moves pure code from apps/{web,mobile}/src into packages/. -->

- `packages/` (target package(s), e.g. `@cosimosi/universe` / `@cosimosi/memory-logic` / a new `@cosimosi/writing-flow`)
  — receive the machine, pure modules, shared R3F UI, and their tests
- `apps/web/src/{widgets/writing-flow,features/{write-diary,split-diary,revise-split,launch-stars,awaken-neuron},
entities/{star,cell-star,filament,latent-star,nebula}}/**` — imports repointed to the package
- `apps/mobile/src/**` (same slices) — duplicated copies deleted, imports repointed
- root `package.json` (`test:*`/`typecheck:*` umbrellas) if a new package is added
- `apps/{web,mobile}/src/**/use-universe.ts` + the writing-flow launch call site — optimistic merge / deferred invalidate

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] `pnpm --filter @cosimosi/web build && lint` and `pnpm --filter @cosimosi/mobile lint` pass; `pnpm typecheck`,
      `pnpm test`, `pnpm lint` (incl. `lint:fsd:layout`, steiger, language) green
- [x] No pure module remains duplicated across `apps/web/src` and `apps/mobile/src` (the T001 inventory files are gone
      from both apps — `git ls-files` shows 0 tracked files under the promoted slices)
- [x] Architecture self-audit passed (`/fe-architecture`): promotion down to `packages/` (never sideways), one-way
      domain→visual (channels import `@cosimosi/memory` types, never the reverse), app layers segmented, web↔mobile
      parity (both import the same packages; `*.native` only where a primitive forks)
- [x] Constitution sanity: positions still emergent (no coordinate stored in the moved code — force-sim output only,
      [I5]); no domain word leaks back through the visual path; the UL lint's memory↔memory-edge + synonym checks still
      run on the scene packages

## Review

- [x] `/code-review` (self-review — subagents returned no tool calls this session): for a content-preserving move the
      highest-signal check is `git diff -M`, which shows every promoted file at **R097–R100 similarity** (only import
      lines changed; logic byte-identical) — so behavior cannot have drifted. Cross-checked: no consumer missed (full
      `pnpm typecheck` EXIT 0 resolves every repointed import; it caught the one straggler, `nebula-demo-panel`, which
      was repointed); store singletons are per-bundle (web/mobile are separate builds, no shared instance); the
      domain→visual seam stays one-way. No findings.
- [x] `/codex:review` **skipped** — a behavior-preserving structural move (no new logic; content verified byte-identical
      via git rename detection; full typecheck/lint/test/build green on both apps). Codex's race/lifecycle strength has
      no changed logic to bite on.

## After completion — reflect into the SSOT

- [x] `spec/tech/rendering.md` updated (job-35 as-built note: rendering entities promoted to `@cosimosi/universe`
      [logic/stores/channels] + `@cosimosi/universe-render` [R3F bindings]; corrected the read-model location, the
      one-way §3.4 seam, and the stale "copied verbatim" mobile notes). ARCHITECTURE §3.5's package list is illustrative
      (placeholder names) so needs no edit; the authoritative registry is the packages + the root `typecheck`/`test`
      umbrellas (both wired to `universe-render`). `apps/web/steiger.config.js` + `scripts/lint-ubiquitous-language.mjs`
      updated for the new structure.
- [x] 00.overview progress board ✅ (review 04 → job 35) · this doc's frontmatter `status: done`
