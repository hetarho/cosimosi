---
job: '156'
type: 'refactor'
source: 'code-review/14.full-codebase-runtime,-architecture,-and'
plan: 'none'
status: done
title: 'Mobile i18n seam convergence and optimistic seam removal'
---

# Job 156: Mobile i18n seam convergence and optimistic seam removal (refactor)

> Implementation work doc. Source spec: [code-review/14.full-codebase-runtime,-architecture,-and](../../code-review/14.full-codebase-runtime,-architecture,-and.md).
> /cosimosi:implement-job 156 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/14.full-codebase-runtime,-architecture,-and)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: review 14 R007 (P3) + R009 (P3). Decisions: Questions #3 (test files may import the
pure package directly; the exception is encoded in the lint rule) and #4 (delete the unused
optimistic seam) in the source report.

- [x] A1 Zero production files on mobile import `@cosimosi/i18n` directly outside the app
      barrel (`apps/mobile/src/shared/i18n/index.ts`) and the platform locale adapter
      (`apps/mobile/src/shared/native/locale-storage.ts`) — currently 19 offenders.
- [x] A2 Mobile enforces the seam the way web does: a `no-restricted-imports` rule mirroring
      `apps/web/eslint.config.js`, with the adapter and test-file exceptions encoded in the
      rule itself; a probe/violation case proves an offender fails and each allowed path
      passes.
- [x] A3 `beginOptimisticMutation` and its surface are deleted: the export, the clone/rollback
      machinery, its test, the diagnostics row, and the `client_cache.optimistic_rollback_ms`
      value — with zero dangling references.
- [x] A4 No behavior or copy change on either platform; mobile + web suites green,
      `check:gen` fresh after the value removal.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 Extend `apps/mobile/src/shared/i18n/index.ts` with the pure exports the 19
      production callers genuinely need (messages, locale helpers, domain projections —
      mirror the web barrel's surface).
- [x] T002 Migrate the 19 files (pages/login, pages/universe, pages/me, app providers,
      features account-profile/buy-ornament/export-diaries/invite-link/preview-ornament/
      replay-onboarding/sign-up/withdraw-account, widgets decoration-panel, app diagnostics/
      navigation) to the barrel; no copy change.
- [x] T003 Add the mobile `no-restricted-imports` rule for `@cosimosi/i18n` (allow: the
      barrel itself, `shared/native/locale-storage.ts`, `**/*.test.*`); add the probe case
      proving a production offender fails.
- [x] T004 (gen:values) Delete the optimistic seam: `packages/client-cache/src/optimistic.ts`,
      its exports in `index.ts`, its cases in `client-cache.test.ts`, the
      `optimistic_rollback_ms` default in `defaults.ts`, the `spec/values.yaml`
      `client_cache.optimistic_rollback_ms` key, and the diagnostics row in
      `apps/web/src/pages/test/lib/platform-panel-components.tsx`; regenerate values.
- [x] T005 Sweep for dangling references (`beginOptimisticMutation`, `optimistic_rollback`,
      `optimisticRollbackMs`) — must be zero.
- [x] T006 Reflect into docs: `spec/tech/i18n.md` (mobile enforcement now executable, the
      test-file exception), and remove the optimistic-rollback mention from any client-cache
      doc/values comment.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/tech/i18n.md`; `spec/values.yaml` (key removal,
  gen:values).

## Affected files (blast radius)

- `apps/mobile/src/shared/i18n/index.ts` (barrel extension) — shared/i18n
- 19 mobile production files (pages/app/features/widgets listed in T002) — import path swap only
- `apps/mobile/.eslintrc.js` (restricted-imports rule + exceptions)
- `packages/client-cache/src/{optimistic.ts,index.ts,defaults.ts,client-cache.test.ts}` — deletion
- `apps/web/src/pages/test/lib/platform-panel-components.tsx` (diagnostics row removal)
- `spec/values.yaml` (+ regenerated values) — (gen:values)
- `spec/tech/i18n.md` (doc reflection)

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — n/a (refactor);
      web, mobile, i18n, and client-cache suites cover the retained behavior
- [x] Codegen / migration / values applied (if any): `pnpm gen`, `pnpm gen:values`, and
      `pnpm gen:messages` applied; no migration was needed
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` pass via full
      `pnpm check`
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

Review notes:

- The first read-only Codex review found that copying every web barrel export made the two app-local
  pure modules byte-identical and failed the FSD parity gate. The mobile barrel now exposes only the
  additional helpers its production callers use.
- The final read-only Codex review returned no findings. The i18n restriction probe proves the
  production refusal and all three owned exceptions.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
      — plans 06/11 and tech i18n/config docs now record the enforced mobile seam and the absence of
      an unused generic optimistic transaction; the orphan value and diagnostics copy are removed
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — n/a (refactor)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
