---
job: '88'
type: 'change'
source: 'changes/04.provider-model-dropdown'
plan: 'plan/58.admin-console'
status: done
title: 'provider model dropdown from live model listing'
---

# Job 88: provider model dropdown from live model listing (change)

> Implementation work doc. Source spec: [changes/04.provider-model-dropdown](../changes/04.provider-model-dropdown.md).
> /cosimosi:implement-job 88 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from changes/04.provider-model-dropdown)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 With a valid DeepSeek key stored, the LLM capability row offers DeepSeek's currently served model ids in a dropdown, fetched live from the vendor via `ListProviderModels` — no model catalog hardcoded in FE or i18n.
- [x] A2 The same holds for Anthropic (LLM) and voyage (embedding; adapter-owned curated set).
- [x] A3 The saved model is preselected; a configured id absent from the listing renders as an unlisted-but-selectable entry.
- [x] A4 If the listing fails (no key, vendor error), the row degrades to manual text entry with an inline notice, and saving still works.
- [x] A5 Manual entry stays available even when the listing succeeds, and saving an unlisted id is accepted exactly as today (`SetAIConfig` unchanged).
- [x] A6 `ListProviderModels` is admin-gated and neither requires nor returns a raw API key on the wire.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 (gen) `proto/cosimosi/admin/v1/admin.proto`: add `ListProviderModels(provider, capability)` RPC
      (`NO_SIDE_EFFECTS`) + `ProviderModel {id, display_name}` request/response messages; run `pnpm gen`.
- [x] T002 `internal/ai`: `ModelInfo` DTO + per-capability lister registries
      (`RegisterLLMModelLister` / `RegisterEmbeddingModelLister`, mirroring the provider registries) +
      `ListLLMModels` / `ListEmbeddingModels(ctx, cfg)` with the same unknown / not-implemented error semantics.
- [x] T003 [P] `internal/ai/deepseek`: `ListModels` over the vendor's live model-listing endpoint (Bearer auth,
      OpenAI-compatible list shape), registered from `init()`; error-mapped like `CompleteJSON`; httptest coverage.
- [x] T004 [P] `internal/ai/anthropic`: `ListModels` over the SDK's model-listing API, registered from `init()`;
      vendor errors normalized; test via SDK base-URL override.
- [x] T005 [P] `internal/ai/voyage`: `ListModels` derived from the adapter-owned `modelDimensions` catalog filtered
      to models that honor `values.AiEmbeddingDim` (no public vendor listing endpoint); registered from `init()`.
- [x] T006 `internal/admin`: consumer-owned `ModelCatalog` port (`ListModels(ctx, capability, provider)`), a
      `ProviderModel` type, `ErrModelCatalogRequired` + `ErrModelListingUnavailable` sentinels; service use-case
      validates capability → provider slot → capability support → implemented → stored key present, then delegates
      to the port. Raw keys never enter the admin context.
- [x] T007 `internal/admin/rpc`: `ListProviderModels` handler + `domainError` mapping —
      `ErrModelListingUnavailable` → `CodeUnavailable` (reason `ADMIN_MODEL_LISTING_UNAVAILABLE`), existing
      provider/key sentinels keep their codes; service tests for the validation chain.
- [x] T008 `cmd/api`: composition-root `aiModelCatalog` adapter binding the port over the admin store's
      `ReadProviderKey` + the secretbox decrypter + the `internal/ai` listers (key resolved outside admin, AC6);
      wire into `adminDeps`.
- [x] T009 [P] `packages/client-cache/src/http-policy.ts`: `listProviderModels` → `userScopedUnaryReadPolicy`.
- [x] T010 [P] `packages/api-client/src/admin.ts`: export the new types + `createListProviderModelsQueryOptions`.
- [x] T011 [P] `packages/i18n/messages/en.json` + `ko.json`: dropdown strings (default-model option, custom-id
      option, unlisted marker, listing-failed notice, loading).
- [x] T012 `apps/web/src/features/admin-ai-config/ui/AiConfigSection.tsx`: model text field → dropdown fed by the
      listing query (enabled per selected provider); preselect the saved model (unlisted entry when absent);
      "custom id" option reveals the text field; listing failure degrades to the text field + inline notice;
      empty selection still means adapter default.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: [plan/28.ai-provider-abstraction](../plan/28.ai-provider-abstraction.md)
  (adapter-owned vendor knowledge); no `spec/values.yaml` keys (no new tuning numbers).

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

- `proto/cosimosi/admin/v1/admin.proto` — contract (+ generated `apps/api/internal/gen/**`,
  `packages/api-client/src/gen/**`)
- `apps/api/internal/ai/{factory,models}.go` — capability seam: `ModelInfo` + lister registries
- `apps/api/internal/ai/deepseek/client.go`(+test) · `anthropic/client.go`(+test) · `voyage/client.go`(+test) —
  provider adapters (vendor knowledge)
- `apps/api/internal/admin/{ports,types,errors,service}.go`(+`service_test.go`) — admin context use-case + port
- `apps/api/internal/admin/rpc/{server,reasons}.go` — thin proto↔domain handler
- `apps/api/cmd/api/{admin,memory}.go` — composition root binding
- `packages/client-cache/src/http-policy.ts` — cache classification (shared FE package)
- `packages/api-client/src/admin.ts` — typed client bindings (shared FE package)
- `packages/i18n/messages/{en,ko}.json` — message catalog
- `apps/web/src/features/admin-ai-config/ui/AiConfigSection.tsx` — features layer, `admin-ai-config` slice, ui segment

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
