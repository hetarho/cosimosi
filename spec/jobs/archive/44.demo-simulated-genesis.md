---
job: "44"
type: change
source: changes/28.demo-simulated-genesis
plan: plan/47.demo-free-mode-onboarding
status: done
title: 데모 우주 = 30일 시뮬레이션 genesis (정적 프리셋 별 폐기·페르소나별 일기 확률 생성)
---

# Job 44: 데모 우주 = 30일 시뮬레이션 genesis (정적 프리셋 별 폐기·페르소나별 일기 확률 생성)  (change)

> 구현 작업 문서. 출처 스펙: [changes/28.demo-simulated-genesis](../changes/28.demo-simulated-genesis.md).
> `/implement-job 44`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/28.demo-simulated-genesis에서)
<!-- 출처 스펙의 수용 기준. 구현 후 각 항목이 코드에서 참인지 검증한다. -->
- [x] A1 페르소나를 고르고 자유모드로 들어가면 정적 프리셋 코퍼스가 한꺼번에 뜨지 않고, 우주가 **빈(또는 거의 빈) 상태에서 시작**해 30일 genesis가 재생된다(`simulate(CORPORA[persona])` 일괄 시드 폐기).
- [x] A2 genesis는 simulated day마다 페르소나별 `demo_genesis.daily_write_prob`로 난수를 굴려 그 날 일기 작성 여부를 정하고(하루 최대 1편), 페르소나마다 작성 빈도가 눈에 띄게 다르다.
- [x] A3 일기를 쓰는 날엔 **새 per-persona 사전분절 템플릿 풀**에서 그 페르소나 분포로 한 편을 뽑아, change 25 조각화 fan-out + change 27 엔진(일내 결속·의미 KNN·감정 유사도 링크·거리=강함·밝기=반경)으로 별·연결이 태어난다. 데모마다 뽑히는 일기가 달라 서로 다른 우주가 자란다.
- [x] A4 genesis는 change 24 연속 시계를 `demo_genesis.hours_per_second` 배속으로 자동 재생하고, 30일 안의 매 밤(04:00 KST)에 야간 공고화가 발화해 별이 태어나고·정리되고·드리프트하는 과정이 보인다.
- [x] A5 genesis 동안 `새 별 띄우기`는 잠겨 사용자가 별을 추가할 수 없다(카메라 이동/줌/별 클릭=읽기·회상은 허용).
- [x] A6 30 simulated days를 마치면 시계가 멈추고 **명시적 환영 안내**가 뜬 뒤 `새 별 띄우기`(change 25 작성 흐름)와 배속 컨트롤이 열려 자유모드가 평소대로 동작한다.
- [x] A7 genesis 입력(작성 여부·템플릿 선택)에만 난수가 있고 엔진 함수는 결정론을 유지해 change 27 골든 parity 대조가 깨지지 않는다.
- [x] A8 페르소나 전환·`처음으로`는 genesis를 처음부터 다시 돌린다(가상 시계 0, 추가 별 0, 새 난수). demo exit 시 genesis 상태가 정리되고 실계정 우주에 섞이지 않는다.
- [x] A9 별·시냅스·원본 삭제·수정 없음, 서버 미호출, `A_MIN=0.05` 단일 바닥 유지. 실계정 우주엔 genesis가 나타나지 않는다.
- [x] A10 백엔드·proto·RPC·sqlc 변경 없이 FE + values 생성물만으로 구현되고 build·lint·typecheck·steiger 통과. 기존 데모 체험(회상·잠든 별·일기 목록·페르소나 전환·시간 흐름·자유모드 작성)·실계정 우주 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- 어떻게 만드나. 위→아래 순서. [P]=서로 다른 파일·의존 없는 병렬. (gen)/(migrate)/(gen:values) 표시.
     config·튜닝 수치는 코드 하드코딩 금지 — spec/values.yaml에 추가 후 (gen:values)로 생성 상수 import. -->
- [x] T001 (gen:values) `spec/values.yaml`에 `demo_genesis` 그룹 추가 — `days: 30`, `daily_write_prob: [<student>, <worker>, <homemaker>]`(페르소나 순서 배열, 예 [0.65, 0.45, 0.8]), `hours_per_second: <genesis 자동 재생 배속>`(예 24 — 하루≈1초 관전), 선택 `daily_recall_prob: [<student>, <worker>, <homemaker>]`(0=비활성). `pnpm gen:values` 후 `VALUES.demoGenesis.*` import(하드코딩 금지). 페르소나 배열 순서는 `PERSONA_ORDER`와 일치시킨다.
- [x] T002 [P] per-persona 사전분절 **템플릿 풀** 작성 — [shared/lib/demo/diary-presets.ts](../../frontend/src/shared/lib/demo/diary-presets.ts)(change 25)를 단편 1개가 아닌 *풀*로 확장: 페르소나별 `DiaryPreset[]`에 topic + 분포 weight. `pickGenesisDiary(persona, rng)`로 페르소나 토픽 분포대로 한 편 샘플. genesis와 자유모드 `새 별 띄우기` 프리셋(`pickDiaryPreset`)이 **같은 풀**을 본다(단일 콘텐츠 출처). 텍스트·조각 mood/intensity/valence는 코드 콘텐츠(values 제외).
- [x] T003 [P] genesis 입력용 순수 난수 헬퍼(shared/lib/demo, model 순수성 유지) — 작성-여부 roll·템플릿 pick·(선택)회상 대상 pick. 세션마다 다른 난수(고정 시드 아님)지만, 엔진 다운스트림은 입력이 같으면 결정론(A7) — 난수는 *입력*에만.
- [x] T004 정적 일괄 시드 폐기 — [shared/lib/demo/data.ts](../../frontend/src/shared/lib/demo/data.ts) `ensureSeeded()`의 `simulate(CORPORA[persona])` 통째 시드(`baseStars = uni.stars.map(...)`, `entryDaysAgo`/recall 일괄 주입; data.ts:57·123~158)를 제거하고 genesis day 0엔 `baseStars=[]`(빈 우주)로 출발. `seededAt`/리셋(`resetDemo` data.ts:794~797) 경계는 genesis 상태 초기화로 재정의.
- [x] T005 genesis 오케스트레이터 — 30 simulated days 루프: 각 day마다 `daily_write_prob` roll → 작성이면 `pickGenesisDiary`로 그 날 날짜에 change 25 fan-out + change 27 엔진(`demoAddRecord`/생성·링크 경로, data.ts:345~436)으로 별·연결 생성 → 시계 진행 → 04:00 경계마다 야간 공고화 발화(`tickDemoClock` 경로 재사용). (선택) `daily_recall_prob` roll로 과거 별 회상.
- [x] T006 가속 시계로 genesis 재생 — 자유모드 진입 시 `demo_genesis.hours_per_second` 배속으로 자동 재생(사용자 배속 컨트롤은 genesis 중 비활성). [pages/home/model/use-demo-flow.ts](../../frontend/src/pages/home/model/use-demo-flow.ts) rAF 드라이버(tickDemoClock, :88~106)가 genesis를 구동. genesis 진행 상태 도입 — [shared/lib/demo/flag.ts](../../frontend/src/shared/lib/demo/flag.ts) `DemoFlow`에 `genesis` 단계 추가하거나 `free` + `genesisActive` 플래그(둘 중 하나, implement 시 결정·문서화).
- [x] T007 genesis 동안 `새 별 띄우기` 잠금 — [pages/home/model/use-demo-flow.ts](../../frontend/src/pages/home/model/use-demo-flow.ts) `addDemoRandomStars`(:167; change 25 후엔 프리셋 작성 폼 진입) 비활성 + 버튼 disabled. 카메라/줌/별 클릭(읽기·회상)은 허용(HUD 표시 유지).
- [x] T008 30일 완료 전환 — genesis 끝(30 simulated days 도달) 시 시계 정지 → genesis 상태 해제 → **명시적 환영 안내**(카드/모달, 예 "30일을 함께 보냈어요 — 이제 당신의 별을 띄워보세요") → `새 별 띄우기`(change 25 흐름)·사용자 배속 컨트롤 개방. [pages/home/ui/HomePage.tsx](../../frontend/src/pages/home/ui/HomePage.tsx) HUD 게이트 연결.
- [x] T009 리셋 경계 — 페르소나 전환·`처음으로`·demo exit 시 genesis 처음부터 재실행(가상 시계 0, 추가 별 0, 새 난수). [widgets/demo-sim/model/time-travel.ts](../../frontend/src/widgets/demo-sim/model/time-travel.ts) `switchDemoPersona`·`resetDemoExperience`, [shared/lib/demo/session.ts](../../frontend/src/shared/lib/demo/session.ts) `startDemoSession`, `resetDemo`(data.ts)에서 genesis 상태 비움.
- [x] T010 오버레이/공유 경로 결정 — `simulate(CORPORA[...])`를 쓰는 overlay/공명 경로(data.ts:183·210·218, spec 37)는 genesis 폐기 영향권. CORPORA를 overlay 전용으로 남길지 genesis 출력으로 전환할지 implement 시 결정·문서화(범위 밖 회귀 금지가 우선 — overlay 동작 보존).
- [x] T011 테스트·검증 — [shared/lib/demo/free-mode.test.ts](../../frontend/src/shared/lib/demo/free-mode.test.ts)·[guided-tour.test.ts](../../frontend/src/shared/lib/demo/guided-tour.test.ts) 갱신(빈 우주 출발·genesis 락·30일 후 개방), change 27 골든 parity 통과 확인, 실계정 우주 회귀 없음. build·lint·typecheck·steiger.

## 참고 (grounding)
- 출처 change: [changes/28.demo-simulated-genesis](../changes/28.demo-simulated-genesis.md)
- 의존 change/job(선행, 이 위에 얹음): [change 24 / job 40](../jobs/archive/40.continuous-clock-speed-control.md)(배속 시계·야간 발화, ✅) · [change 25 / job 41](41.demo-preset-diary-flow.md)(프리셋 작성·fan-out·diary-presets.ts) · [change 27 / job 43](43.demo-production-logic-parity.md)(서버 동치 엔진·골든 parity) — genesis는 이 셋의 *목표 상태* 위에서 동작
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙
- 건드리는 tech/policy/values: `spec/values.yaml`(`demo_genesis.*` 신규) · 완료 시 [policy/ux/interaction.md](../policy/ux/interaction.md) 데모 규칙 · [plan 47](../plan/47.demo-free-mode-onboarding.md) 진입 상태/`새 별 띄우기` 갱신

## 영향 파일 (blast radius)
<!-- 출처 스펙·코드 grep으로 찾은 정확한 경로 — 이 범위 밖은 안 건드린다. -->
- `frontend/src/shared/lib/demo/data.ts` — `ensureSeeded`/`demoStars`(정적 시드 폐기·genesis 출발), `resetDemo` genesis 상태 초기화, overlay 경로(:183·210·218) 결정
- `frontend/src/shared/lib/demo/personas.ts` — 정적 `CORPORA` 일괄 시드 의존 제거(overlay 잔류 여부 T010)
- `frontend/src/shared/lib/demo/diary-presets.ts` — 단편 프리셋 → per-persona 샘플 풀(`pickGenesisDiary`), 자유모드 프리셋과 공유
- `frontend/src/shared/lib/demo/flag.ts` — `DemoFlow`에 genesis 단계/플래그
- `frontend/src/shared/lib/demo/session.ts` — `startDemoSession` 진입 시 genesis 초기화
- `frontend/src/shared/lib/demo/{clock.ts,index.ts}` — genesis 배속 재생·export
- `frontend/src/widgets/demo-sim/model/time-travel.ts` — `switchDemoPersona`·`resetDemoExperience` genesis 재실행
- `frontend/src/pages/home/model/use-demo-flow.ts` — rAF 드라이버 genesis 구동, `addDemoRandomStars` genesis 락, 30일 완료 게이트
- `frontend/src/pages/home/ui/HomePage.tsx` — genesis 중 HUD/`새 별 띄우기` 게이트, 환영 안내 표면
- `frontend/src/shared/config/values.gen.ts`, `backend/internal/values/values_gen.go` — `pnpm gen:values` 산출물(FE 데모 전용 노브)
- `frontend/src/shared/lib/demo/{free-mode.test.ts,guided-tour.test.ts}` — genesis 동작 테스트

## 검증 / DoD
- [x] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [x] (type=change면) 기존 plan 수용 기준 회귀 없음 — plan 47 온보딩(페르소나 선택→모드 선택)·페르소나/시간 컨트롤·UI 숨김·실계정 우주, change 27 골든 parity
- [x] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter ./frontend build`·`lint`·typecheck·steiger 통과 (BE 변경 없음)
- [x] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음 (데모는 서버 미호출 — 클라 상태만)

## 리뷰
- [x] `/code-review`(high, 7각도) + `/codex:review`(gpt-5.5, 백그라운드) 합산 — 반영 4·기각 4
  - **반영**: ① (HIGH) 자유모드→"둘러보기 다시 보기"로 튜토리얼 진입 시 genesis가 계속 별을 빚고 튜토리얼이 genesis 우주를 보임 → `advanceDemoGenesis`에 `getDemoFlow()==='free'` 가드 + `replayTour`가 `resetDemoExperience`로 정적 코퍼스 재시드·genesis 정지. ② (MED) `chooseFree` 진입 첫 프레임 잠금 레이스 → `chooseFree`에서 `armGenesisPlayback()` 동기 호출. ③ (LOW) 환영 카드 "내 별 띄우기"가 닫기만 함 → 작성 폼 열도록 연결. ④ (cleanup) `demoComposeSegments`·`advanceDemoGenesis` 조각 매핑 중복 → `presetSegments` 헬퍼로 통합. 회귀 테스트 1개 추가(튜토리얼 가드).
  - **기각**: ① `crypto.randomUUID` "결정론 위반" — 골든 parity는 순수 물리 함수만 대조(id 무관, 통과), 세션마다 다른 id는 A8 의도. ② 새로고침 시 genesis 재생 — 데모 휘발성(모듈 상태 소멸) 정책과 일관, 추가 별은 원래도 소멸. ③ 다중 경계 틱 entryDate 붕괴 — genesis 배속에서 `max_tick_ms` 캡으로 틱당 ≤1 경계라 도달 불가. ④ genesisActive React 미러 분리 — 이 파일의 clockSpeed/clockDay와 동일한 표준 패턴.

## 완료 후 — SSOT 반영
- [x] `plan/47`을 새 현실로 갱신(진입=30일 genesis 관전→환영→자유 작성) · 영향받은 [policy/ux/interaction.md](../policy/ux/interaction.md) · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [x] (type=change면) `changes/28.demo-simulated-genesis`를 `changes/archive/`로 이동
- [x] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
