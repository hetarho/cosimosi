---
job: '72'
type: 'refactor'
source: 'code-review/05.plans-35-53-runtime-and-architecture-aud'
plan: 'none'
status: done
title: 'Consolidate cross-app frontend core'
---

# Job 72: Consolidate cross-app frontend core (refactor)

> Implementation work doc. Source spec: [code-review/05.plans-35-53-runtime-and-architecture-aud](../../code-review/05.plans-35-53-runtime-and-architecture-aud.md).
> /cosimosi:implement-job 72 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/05.plans-35-53-runtime-and-architecture-aud)

<!-- R012. This is a behavior-preserving placement refactor. -->

- [x] A1 **The audited 30 pure cross-app modules have one implementation.** Their 884-line web
      and mobile copies (rows 1-20 from the review range; rows 21-30 predate it and were confirmed
      byte-identical during doc verification) are promoted to domain-owned packages, both apps
      import the same exports, and no app-local `api`/`model`/`lib`/`config`/`shared` duplicate
      remains for this inventory. (R012.)
- [x] A2 **Package ownership remains cohesive.** Palette preference code belongs to emotion,
      diary DTO mapping belongs to memory, auth-session binding belongs to auth, universe/deletion/
      diary orchestration belongs to universe, and Twinkle read/write/quote/config code has one
      Twinkle-owned package home; clock/time-sync/launch/writing/split orchestration follows the
      same universe/memory split, and `shared/i18n/mood-label.ts` is classified to its owning
      domain package during T001. No umbrella “shared features” package or same-layer app import is
      introduced.
- [x] A3 **Logic tests move with the shared code.** Existing API/model tests are relocated beside
      the promoted definitions, mobile and web consume the tested package code, and app-local
      tests retain only platform UI/composition assertions.
- [x] A4 **Only real platform forks stay in apps.** DOM/RN components, router/navigation adapters,
      native primitives, and genuinely different hosts remain in their FSD slices; pure hooks,
      stores, DTO mappers, request functions, and constants do not acquire `*.native` copies.
- [x] A5 **Mobile product screens use the mirrored `pages` layer.** Diary reader, settings, login,
      and universe composition live at `pages/<slice>/ui` behind one public API per slice;
      `app/navigation` only declares routes/stacks and the neutral boot shell, and imports pages
      through those public APIs.
- [x] A6 **Placement drift fails a gate.** The FSD layout/duplicate checks reject a new product
      screen under `app/navigation/screens` and a new byte-identical cross-app pure module. Plan
      52's “no pages dir” exception and related plan 13/53 prose are reconciled to the canonical
      `ARCHITECTURE.md` rule.
- [x] A7 **No unrelated behavior change / no regression.** This is a content-preserving move after
      Job 71: RPC inputs, cache invalidation, optimistic behavior, store identity, route names,
      deep links, visual UI, and plans 35-53 behavior remain unchanged; the full web/mobile gates
      and production builds pass.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). -->

- [x] T001 Run a byte/hash inventory over the 30 source pairs listed below, record consumers and
      tests with `rg`, and classify every module by domain and dependency. Stop if a pair has
      gained behavior drift; reconcile that behavior under its owning job before moving it.
- [x] T002 Complete Job 71 first. Re-run the inventory so the session-safe palette implementation
      is the version promoted and this job does not preserve or reintroduce the lifecycle bugs fixed there.
- [x] T003 [P] Promote the six palette preference modules and their logic tests into
      `@cosimosi/emotion` (a pure core plus an explicit React/query export seam); repoint both
      `features/change-palette` UI barrels and bootstrap consumers to the package API.
- [x] T004 [P] Promote the diary mapper to `@cosimosi/memory` and the shared universe/deletion/
      diary hooks, draft model, and invalidation helpers to `@cosimosi/universe`; place rows 21-30
      with their T001 owners (universe for the clock/time-sync/launch/writing stores and hooks,
      memory for the split/revise-split API mappers, the owning domain package for
      `shared/i18n/mood-label.ts`). Keep DTO mapping at the domain anti-corruption boundary and
      React/query bindings on an explicit subpath.
- [x] T005 [P] Create a cohesive `@cosimosi/twinkle` FE domain package for balance query/invalidation,
      quote/pending-spend, charge actions, and the generated-config-backed pack projection. Move
      their tests, wire package dependencies/scripts into the root umbrellas, and do not merge
      this IO/state package into the deterministic `@cosimosi/twinkle-logic` formulas.
- [x] T006 [P] Move the shared account-session React binding into `@cosimosi/auth/react`; keep the
      account section UI app-local. Preserve hook identity and sign-out error semantics.
- [x] T007 Repoint every app consumer and public API to the package exports, then delete both
      copies of each audited module in one pass. Verify no deep import, app-to-app import, stale
      barrel, or second Zustand singleton remains; use rename/content comparison to prove the
      move did not alter logic beyond import paths and Job 71's prerequisite fixes.
- [x] T008 Move mobile DiaryReader, Settings, Login, and Universe screens into mirrored
      `pages/<slice>/ui` slices with `index.ts` public APIs. Reduce `NavigationRoot` to typed route
      registration, keep Boot as app-shell infrastructure, and preserve route params/deep links.
- [x] T009 Extend `scripts/lint-fsd-layout.mjs` and its tests (or an equally root-reachable guard)
      with negative controls for product imports/screens under navigation and copied pure modules.
      Update plans 13/52/53 and the FE technical note to remove the obsolete native exception.
- [x] T010 Run package tests/typechecks after each promotion, then web/mobile lint, typecheck,
      tests, web build, duplicate/hash audit, boundary probes, `pnpm lint:fsd:layout`, and full
      `pnpm check`.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: [ARCHITECTURE.md](../../ARCHITECTURE.md) §3.1/§3.5,
  [plan 13](../../plan/13.mobile-app-shell.md), [plan 52](../../plan/52.settings-page.md),
  [plan 53](../../plan/53.auth-universe-gate.md), and the owning frontend/package technical note.
  No policy, RPC, schema, migration, or values change is expected.
- Dependency: implement Job 71 first because this job moves its corrected palette/session modules.

## Affected files (blast radius)

### Implementation inventory (2026-07-22)

- A pre-move SHA-256 pass reported all 30 web/mobile pairs byte-identical (884 lines per app); no
  behavior drift was found. Job 71 was already archived as done, and its session-safe palette
  implementation was the version hashed and promoted.
- Ownership classification: emotion = rows 2–7 and 28; universe = 1, 8–11, 13, 19, 21–24, 27,
  29–30; memory = 17, 25–26; Twinkle = 12, 14, 16, 18, 20; auth = 15. The three
  comment-only near-copies were normalized and promoted too: provenance query/model to memory and
  recall draft state to universe.
- `rg` consumer/test inventory found both app UI hosts plus app reset/bootstrap composition as the
  consumers. Logic tests moved with palette, charge, provenance, release/restore/let-go,
  time-consent, and release-advance definitions; app tests now cover only UI/composition behavior.
- The post-move audit parses this table and reports `inventory rows=30`, `remaining=0`; the root FSD
  guard also reports no byte-identical same-relative pure module and no mobile product screen under
  `app/navigation/screens`.

### Audited source pairs (delete after promotion)

Each row means the two exact paths `apps/web/src/<module>` and `apps/mobile/src/<module>`.

| #   | Module path under each app                                  |
| --- | ----------------------------------------------------------- |
| 1   | `widgets/universe-canvas/api/use-universe.ts`               |
| 2   | `features/change-palette/api/change-palette.ts`             |
| 3   | `features/change-palette/api/apply-palette.ts`              |
| 4   | `features/change-palette/api/read-palette-preference.ts`    |
| 5   | `features/change-palette/api/use-palette-version.ts`        |
| 6   | `features/change-palette/model/palette-preference-store.ts` |
| 7   | `features/change-palette/lib/palette-display-name.ts`       |
| 8   | `features/let-go/api/use-let-go.ts`                         |
| 9   | `features/read-diary-list/api/use-diary-archive.ts`         |
| 10  | `features/delete-memory/api/use-release.ts`                 |
| 11  | `features/restore-memory/api/use-restore.ts`                |
| 12  | `features/charge-twinkle/api/charge.ts`                     |
| 13  | `widgets/deletion-flow/model/deletion-draft-store.ts`       |
| 14  | `entities/twinkle/api/balance.ts`                           |
| 15  | `features/account-settings/api/use-account-session.ts`      |
| 16  | `features/spend-cost-display/model/pending-spend.ts`        |
| 17  | `entities/diary/api/diary-mapper.ts`                        |
| 18  | `features/spend-cost-display/api/quote-spend.ts`            |
| 19  | `widgets/diary-reader/model/invalidate-universe.ts`         |
| 20  | `features/charge-twinkle/config/pack.ts`                    |
| 21  | `entities/universe-clock/api/sync-universe-clock.ts`        |
| 22  | `features/accelerate-time/model/advance-interval-store.ts`  |
| 23  | `features/confirm-time-sync/model/consent.ts`               |
| 24  | `features/launch-stars/model/launched-neurons-store.ts`     |
| 25  | `features/revise-split/api/revise-split.ts`                 |
| 26  | `features/split-diary/api/split-diary.ts`                   |
| 27  | `features/write-diary/model/draft-store.ts`                 |
| 28  | `shared/i18n/mood-label.ts`                                 |
| 29  | `widgets/universe-time/model/release-advance.ts`            |
| 30  | `widgets/writing-flow/model/proposal-store.ts`              |

Rows 1-20 are the review-05 in-range inventory; rows 21-30 predate that range (plans 26-31) and were confirmed
byte-identical during doc verification (2026-07-21). Three further in-range pairs differ only in header comments and
are promoted once their owning jobs land — after job 65: `features/star-provenance/api/get-provenance.ts` and
`features/star-provenance/model/provenance.ts`; after jobs 70/71: `widgets/recall-flow/model/recall-draft-store.ts`.
Normalize their comments and move them, or record explicitly why they remain forks.

### Target package and consumer surfaces

- `packages/emotion/src/preference/**`, `packages/emotion/src/index.ts`, and
  `packages/emotion/package.json` — palette domain core + explicit React/query seam.
- `packages/memory/src/diary.ts`, `packages/memory/src/index.ts` — Diary DTO-to-domain mapper.
- `packages/auth/src/react.ts` and `packages/auth/package.json` — shared account-session React binding.
- `packages/universe/src/react/**`, `packages/universe/src/deletion-draft-store.ts`,
  `packages/universe/src/index.ts`, and `packages/universe/package.json` — shared universe,
  release/restore/let-go/diary orchestration and model.
- `packages/twinkle/package.json`, `packages/twinkle/tsconfig.json`, and `packages/twinkle/src/**` (new) — FE Twinkle
  domain/IO package; root `package.json` and `pnpm-lock.yaml` wire its typecheck/test umbrellas.
- The affected web/mobile slice `index.ts` files under `features/{change-palette,let-go,read-diary-list,delete-memory,
restore-memory,charge-twinkle,account-settings,spend-cost-display,accelerate-time,confirm-time-sync,launch-stars,
revise-split,split-diary,write-diary}`, `entities/{diary,twinkle,universe-clock}`, `shared/i18n`, and
  `widgets/{universe-canvas,deletion-flow,diary-reader,universe-time,writing-flow}` — re-export package definitions
  for existing UI consumers.
- Existing logic tests `apps/web/src/features/{change-palette/api/change-palette.test.ts,
charge-twinkle/api/charge.test.ts,delete-memory/api/release.test.ts,let-go/api/let-go.test.ts,
restore-memory/api/restore.test.ts}` — move beside their package owners; platform UI tests stay in apps.
- `apps/mobile/src/app/navigation/NavigationRoot.tsx` and current
  `apps/mobile/src/app/navigation/screens/{DiaryReaderScreen.tsx,SettingsScreen.tsx,LoginScreen.tsx,
UniverseScreen.tsx}` — navigation consumers and source screens.
- `apps/mobile/src/pages/{diary-reader,settings,login,universe}/index.ts` and each slice's
  `ui/*Page.tsx` (new) — target page-layer composition.
- `scripts/lint-fsd-layout.mjs` and `scripts/lint-fsd-layout.test.mjs` (new if no co-located test exists) — placement and
  duplicate negative controls.

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] No regression of plans 35-53; `git diff -M`/content comparison confirms behavior-preserving moves after Job 71
- [x] No codegen, migration, or values change; workspace lockfile reflects only package wiring for `@cosimosi/twinkle`
- [x] All affected package tests/typechecks, both app tests/typechecks/lints, web build, and full `pnpm check` pass
- [x] Hash/`rg` audit reports zero remaining copies of the 30 audited modules and zero app-to-app/deep imports; the
      three comment-only near-copies are promoted or their fork status is explicitly recorded
- [x] Constitution sanity: no backend file changed, so there are no row deletes on `records`/`memories`/`memory_links`
      and no body UPDATE on `records`
- [x] Architecture self-audit passed (`/fe-architecture` and `/mobile-architecture`; `/be-architecture` is n/a because
      no backend surface changed): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` was covered by acceptance, behavior-preservation, dependency, architecture, and comment-quality
      diff review; the bundled Codex CLI also reviewed the uncommitted change in read-only mode, and its confirmation
      pass found no remaining actionable correctness issue. Findings:
  - **Applied — exclude the new Twinkle workspace package from Vite prebundling (P3):** added
    `@cosimosi/twinkle` to `optimizeDeps.exclude`, so edits under `packages/twinkle/src` remain live during
    `pnpm dev:web` instead of requiring a dev-server restart.
  - **Applied during dependency audit:** each promoted package now directly declares the Connect dependency its
    source imports instead of relying on the workspace's hoisted transitive installation.
  - **Rejected findings:** none.

## After completion — reflect into the SSOT

- [x] Updated `ARCHITECTURE.md`, plans 13/52/53, and the affected auth/emotion/i18n/rendering/state-machine/
      Twinkle/routing technical notes; no policy or tuned value changed, so `spec/values.yaml` is unchanged
- [x] (if type=change) n/a — this is a `refactor`, so there is no `changes/` source to archive
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
