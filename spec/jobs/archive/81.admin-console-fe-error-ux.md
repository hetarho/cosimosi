---
job: '81'
type: 'refactor'
source: 'code-review/06.batch-75-78-admin-console,-error-taxonom'
plan: 'none'
status: done
title: 'admin console FE error UX'
---

# Job 81: admin console FE error UX (refactor)

> Implementation work doc. Source spec: [code-review/06.batch-75-78-admin-console,-error-taxonom](../../code-review/06.batch-75-78-admin-console,-error-taxonom.md).
> /cosimosi:implement-job 81 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/06 · R005, R006, R009, R012)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 (R006) Admin console errors route through the plan-59 shared seam — `useErrorToast` / `presentAppError` from
      `@cosimosi/errors` + the app-layer error-provider — **not** raw `cause.message`. No admin surface renders an
      untranslated ConnectError message. `AiConfigSection` no longer does
      `setError(cause instanceof Error ? cause.message : …)`.
- [x] A2 (R005) Every admin dashboard section handles the **error** state, not only `isPending`: `JobsSection`,
      `UsageSection`, and the model-select surface render `m.admin_load_error()` (matching `ProviderKeysSection`) on a
      failed read — an outage never renders as an all-zero (`0n`) healthy queue.
- [x] A3 (R009) Admin **mutations** (grant / promote / revoke in `UsersSection`) surface failures to the operator via
      the toast seam instead of `.catch(() => undefined)`.
- [x] A4 (R012) The hardcoded operator strings in `AiConfigSection` are internationalized: `'unset'` becomes a message
      key and the server `source` enum (`db`/`env`/`empty`) is rendered through a small label helper mapped to i18n
      keys. No raw user-facing literal remains in the admin surface.
- [x] A5 No behavior change to the happy path: successful reads/mutations render and refetch exactly as today; only the
      failure/degraded and copy paths change.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values). -->

- [x] T001 Add any new i18n keys via `spec` message source → `pnpm gen:messages` (gen): a grant/promote/revoke failure
      message (or reuse a generic one), the `source`-enum labels, and an `unset` label. Confirm `error_*`/`admin_*`
      keys reused where they already exist.
- [x] T002 [P] `apps/web/src/features/admin-ai-config/ui/AiConfigSection.tsx`: replace both
      `setError(cause.message)` catches (~~:67, :243) with the `useErrorToast` seam; drop the local `error` string
      state + raw `<span>` renders (~~:131, :253) unless kept as an inline i18n message. Fix the `'unset'`/`source`
      literals (~:199) (R006/R012).
- [x] T003 [P] `apps/web/src/features/admin-jobs/ui/JobsSection.tsx` + `admin-usage/ui/UsageSection.tsx`: add an
      `isError` branch rendering `m.admin_load_error()` before the `data?.… ?? 0n` render (R005).
- [x] T004 [P] `apps/web/src/features/admin-users/ui/UsersSection.tsx`: replace `runAction`'s `.catch(() => undefined)`
      (~:38) with an error-toast surface (R009).
- [x] T005 Verify against the plan-59 FE seam usage elsewhere (recall/deletion flows) so the admin adoption matches the
      established pattern; run `pnpm --filter @cosimosi/web lint build` + `lint:raw-strings`.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3. [I2] privacy unaffected (metadata-only surface).
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3. Invoke `/fe-architecture`. Web-only feature
  slices under `apps/web/src/features/admin-*` (parity waived for `/admin`, `spec/tech/admin-console.md` §8).
- Source findings: code-review/06 **R005** (silent zeros), **R006** (raw message bypasses seam), **R009** (silent
  mutations), **R012** (hardcoded strings). Related theme: plan-59 seam adoption.
- tech / policy / values touched: `spec/tech/i18n.md` (new keys); no values/migration/BE.

## Affected files (blast radius)

- `apps/web/src/features/admin-ai-config/ui/AiConfigSection.tsx` — FE feature slice: seam adoption + i18n (R006/R012).
- `apps/web/src/features/admin-jobs/ui/JobsSection.tsx` — FE feature slice: isError branch (R005).
- `apps/web/src/features/admin-usage/ui/UsageSection.tsx` — FE feature slice: isError branch (R005).
- `apps/web/src/features/admin-users/ui/UsersSection.tsx` — FE feature slice: mutation error surface (R009).
- i18n message source + `packages/i18n/src/gen/**` (gen) — new admin error/label keys.

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) n/a — refactor; happy path unchanged (only catch handlers, error branches, and copy changed)
- [x] Codegen applied: `pnpm gen:messages` for `admin_ai_source_db/env/unset` (en + ko); no values/migration/BE
- [x] FE `pnpm --filter @cosimosi/web build`·`lint` (oxlint + steiger + boundaries + probe), `lint:raw-strings`,
      `lint:fsd:layout`, `test:i18n`, `test:errors` all green
- [x] Constitution sanity: FE-only, no persistence touched
- [x] Architecture self-audit passed (`/fe-architecture`): edits stay in existing `features/admin-*/ui` segments;
      `shared/model` import is downward; no new files/slices

## Review

- [x] `/code-review` applied (rejections noted with reason) · codex skipped (mechanical seam adoption, per the
      small/mechanical rule)
  - Adversarial review (fresh-context agent): P1/P2 none. Verified: `/admin` mounts under `WebErrorProvider` (hook
    never throws), hook calls are unconditional component-top-level, `sourceLabel` handles undefined/empty/future
    values as unset, TanStack focus/remount refetch recovers `isError` sections (matches the `ProviderKeysSection`
    precedent), i18n keys present in both locales with idempotent gen output.
  - Applied beyond ACs: the same reviewer noted `UsersSection`'s read queries still rendered a failed `ListUsers` as
    "no users" and failed grants as "no grants yet" (the R005 class, missed by the original review's scope) — both got
    `isError` branches.
  - Rejected: restoring per-row inline error markers alongside the toast (the `RestoreSection` pattern) — T002
    explicitly allows dropping the inline span, and the failing row is the one just operated on; not worth the state.

## After completion — reflect into the SSOT

- [x] SSOT: no plan/policy/tech rule changed — this adopts the already-documented plan-59 seam on the plan-58 surface;
      new i18n keys live in the message source (the SSOT for copy); no values change
- [x] (if type=change) n/a — refactor
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
