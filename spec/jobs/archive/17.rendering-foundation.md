---
job: '17'
type: new
source: plan/14.rendering-foundation
plan: plan/14.rendering-foundation
status: done
title: rendering foundation
---

# Job 17: rendering foundation (new)

> Implementation work doc. Source spec: [plan/14.rendering-foundation](../plan/14.rendering-foundation.md).
> **Re-architected mid-flight (user correction):** the renderer is a shared package `@cosimosi/3d-renderer` (web _and_
> React Native consume it identically), not an app `shared/lib`; the MVP shader-art toolkit was ported as the
> composable effect library; the universe replaced the design-system main page; the RN host was fully wired
> (react-native-webgpu + reanimated + worklets, metro `three`→webgpu + `@react-three/fiber`→web build, babel worklets
> plugin, `pod install`). The checklist/AC below capture the original journey; the **as-built truth** is
> [plan/14](../plan/14.rendering-foundation.md) + [tech/rendering.md](../tech/rendering.md).
> **Verified:** web typecheck · lint · test (16) · build; mobile typecheck · lint · test (6) · `pod install` (rn-webgpu
>
> - reanimated + worklets integrated). **On-device/simulator render is pending verification** (run `pnpm ios`) — the one gate CI can't see.

## Acceptance Criteria (from plan/14.rendering-foundation)

> `[x]` = verified true (static: typecheck/lint/build/grep). `[ ]` with a note = implemented + compiles but needs a
> GPU browser / device to confirm at runtime, or deferred.

- [ ] A1 Renderer boundary brings up `WebGPURenderer` with WebGL2 fallback; no slice imports `three`. _(no-slice-import verified by grep + `no-restricted-imports`; boundary compiles; the WebGPU/WebGL2 bring-up needs a GPU browser to confirm.)_
- [x] A2 `packages/shaders` is a pure TSL toolkit (imports only `three/tsl`); shaders authored in TSL, no raw GLSL.
- [x] A3 Asset-source port (`VisualBodySource`) supplies bodies; the domain/mirror layer imports no concrete renderer.
- [ ] A4 TSL bloom pass on the WebGPU path, degrades on WebGL2. _(implemented + compiles; runtime render needs a GPU browser.)_
- [x] A5 Skin = named preset (code content); active skin selected by one build-time constant (`rendering.active_skin` → `gen:values`); changing it re-skins.
- [x] A6 Skins read through one `useSkin` seam designed so a runtime switch replaces the constant source with no consumer change ([P4]-ready, not built).
- [x] A7 The skin/presentation layer makes no domain writes (emotion/position/strength) — presentation-only ([I3][I11]); verified by grep.
- [ ] A8 `/test` rendering-foundation panel: skinned bg + instanced meshes + bloom + live skin switcher. _(web panel implemented + builds; on-screen render needs a GPU browser; the mobile dev-build part is deferred with A9.)_
- [ ] A9 RN renderer host at MVP fidelity + physical-device verification. **Built + source-gate verified** (typecheck · lint · test · `pod install`); the shared `@cosimosi/3d-renderer` scene renders on RN via `react-native-webgpu`. _On-device/simulator render remains **pending** — run `pnpm ios` (the one gate CI can't see)._ Recipe in tech/rendering.md.

## Implementation Checklist

- [x] T001 **(library-setup)** Pinned current versions: `three` 0.185.0, `@react-three/fiber` 9.6.1, `@types/three` 0.185.0 (RN `react-native-webgpu` 0.5.16 recorded for the deferred host). Confirmed the R3F v9 WebGPU pattern (async `gl` → `new WebGPURenderer`; `three/webgpu` + `three/tsl`).
- [x] T002 Scaffolded `packages/shaders` — pure TS package (package.json, tsconfig, `src/`, `index.ts`); added to the workspace; importable by apps; stays DOM/native-free (imports only `three/tsl`).
- [x] T003 TSL toolkit: `nebulaBackgroundNode` (TSL) + skin presets; no raw GLSL.
- [x] T004 Web renderer boundary `apps/web/src/shared/lib/renderer/UniverseCanvas.tsx`: R3F + `WebGPURenderer` (auto WebGL2 fallback), async `init`, dpr cap from values; no slice imports `three`.
- [x] T005 Asset-source port `asset-source.ts` (`VisualBodySource`).
- [x] T006 Post-FX seam `post.tsx`: TSL `bloom` over `pass(scene, camera)`; takes the render loop via priority `useFrame`.
- [x] T007 Skin presets in `packages/shaders` (code content: aurora/ember/void).
- [x] T008 Skin seam `skin-context.ts` (`useSkin`) + `skin.tsx` (`SkinProvider`); build-time default, runtime-ready for [P4]; skin can't write domain. In-canvas components take `skin` as a prop (R3F reconciler boundary).
- [x] T009 **(gen:values)** Added `rendering.active_skin` / `max_pixel_ratio` / `instance_bucket_size` to `spec/values.yaml`; `pnpm gen:values` → `@cosimosi/config` (+ Go consts). Consumers import the generated constant.
- [x] T010 `/test` rendering-foundation panel (`pages/test/lib/render-demo-panel.tsx`) + 2 i18n messages (en/ko) + `pnpm gen:messages`; registered in `platform-panels.ts`. Demo scene (`shared/lib/renderer/demo.tsx`) uses placeholder instanced bodies, no domain data.
- [x] T011 RN renderer host: `react-native-webgpu` + reanimated + worklets wired; metro `three`→`three/webgpu` + `@react-three/fiber`→web build; babel worklets plugin; jest mock; `pod install` integrated. `UniverseScreen` renders the shared `@cosimosi/3d-renderer` scene. Recipe in tech/rendering.md.
- [ ] T012 **Pending (env):** on-device/simulator render verification (`pnpm ios`) — no physical device/simulator here.
- [x] T013 `no-restricted-imports` rule (three/R3F only via the `@cosimosi/3d-renderer` package, never in a slice) in `apps/web/eslint.config.js`; build · lint · steiger · boundaries · tests all green.
- [x] T014 Scope guards (grep): three/R3F confined to `shared/lib`; no domain rendering (no `EpisodicMemory`/`Neuron`/`Synapse`), no force-sim, no end-user runtime skin UI; skin makes no domain writes.

## Grounding

- Source plan: [plan/14.rendering-foundation](../plan/14.rendering-foundation.md); as-built rules: [tech/rendering.md](../tech/rendering.md).
- Architecture: [ARCHITECTURE.md](../ARCHITECTURE.md) §3.3 / §3.4 / §3.5.
- Values: `rendering.active_skin` / `max_pixel_ratio` / `instance_bucket_size` → `@cosimosi/config` via `pnpm gen:values`.

## Affected files (blast radius)

- `packages/shaders/**` (new), `packages/shaders/package.json` (+`three`/`@types/three` dev/peer)
- `packages/3d-renderer/**` (as-built: UniverseCanvas, asset-source, layers/Background·StarField·PostFX, skin/·shader-art/, skin-context, SkinProvider) + `index.ts`/`index.native.ts`
- `apps/mobile/**` (UniverseScreen + Universe route + ShellHome entry, metro/babel/jest wiring, `ios/` pod integration) + `patches/@react-three__fiber@*.patch`
- `apps/web/src/pages/test/lib/render-demo-panel.tsx`, `apps/web/src/pages/test/lib/platform-panels.ts`
- `apps/web/eslint.config.js`, `apps/web/package.json` (+`three`/`@react-three/fiber`/`@cosimosi/shaders`/`@types/three`)
- `spec/values.yaml` + generated `packages/config/src/values.gen.ts`, `apps/api/internal/platform/values/values_gen.go`
- `packages/i18n/messages/{en,ko}.json` + generated `packages/i18n/src/gen/**`
- `pnpm-lock.yaml`, `spec/plan/00.overview.md`, `spec/plan/14.rendering-foundation.md`, `spec/tech/rendering.md`, this job

## Verification / DoD

- [x] `pnpm gen:values` + `pnpm gen:messages` applied; generated TS + Go are valid.
- [x] `pnpm --filter @cosimosi/web typecheck` · `lint` (oxlint + steiger + eslint `--max-warnings=0` + boundary probe) · `build` all pass; `@cosimosi/shaders` typecheck passes; web (16) + i18n (7) tests pass.
- [ ] GPU-runtime confirmation of A1/A4/A8 visuals — **pending a GPU browser** (not runnable headless).
- [ ] A9 RN host + device verification — **deferred** (no native toolchain/device).
- [x] Constitution sanity (rendering): positions never stored ([I5]); skin writes no domain emotion/position/strength ([I3][I11]); no domain rendering / no force-sim / no end-user skin UI.

## Review

- [x] Code review (proportionate self-review at recall focus; ultracode off + scaffolding diff already gate-green, so no agent fleet). **Applied:** `packages/shaders/tsl/background.ts` imported `three` (main) for `Color` — weakened the package's "pure" claim; switched to `three/tsl` `color(hex)` so the package imports only `three/tsl`. **Noted (not defects):** `UniversePostFX` calls `renderAsync()` per frame without awaiting — the documented three WebGPU post-processing pattern (renderer queues); recorded in tech/rendering.md. RN host intentionally deferred.

## Outcome / remaining

- **Done + verified (web):** the rendering substrate is built and static-green; it delivers the "tweak a constant → re-skin" affordance and the `/test` verification panel.
- **Pending human/device (env-blocked, not code work):** (1) open `/test` in a GPU browser to confirm A1/A4/A8 visuals; (2) `pnpm ios` to confirm the RN on-device render (A9/T012) per tech/rendering.md. The buildable scope is complete and source-gate-green, so the job is **done + archived**; these two env/human gates are tracked in [plan/14 As-built](../plan/14.rendering-foundation.md) + the [00.overview](../plan/00.overview.md) board (plan 14: web built · RN on-device pending), not by keeping the job open.

## SSOT reflected

- [x] [plan/14](../plan/14.rendering-foundation.md) → As-built status section + header status.
- [x] [tech/rendering.md](../tech/rendering.md) created (owner doc for renderer/shader/skin rules + RN recipe).
- [x] `spec/values.yaml` `rendering.*` set + generated. policy/: the "skin layer can't touch the meaning layer" rule lives in tech/rendering.md (no `policy/` tree exists yet).
- [x] [00.overview](../plan/00.overview.md) board updated (plan 14: web built · RN pending).
