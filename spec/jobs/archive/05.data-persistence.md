---
job: "05"
type: new
source: plan/05.data-persistence
plan: plan/05.data-persistence
status: done
title: data persistence
---

# Job 05: data persistence  (new)

> Implementation work doc. Source spec: [plan/05.data-persistence](../plan/05.data-persistence.md).
> /cosimosi:implement-job 05 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/05.data-persistence)
- [x] A1 `docker compose up` starts a local Postgres service with pgvector available.
- [x] A2 `pnpm db:migrate`, `pnpm db:status`, `pnpm db:down`, and `pnpm db:reset` target the clean `apps/api` database path.
- [x] A3 The initial migration enables required platform extensions and creates no product domain tables.
- [x] A4 `pnpm gen:sql` runs sqlc when schema/query inputs exist and skips cleanly before they do.
- [x] A5 Generated sqlc code is placed under an infra-only package path and is not imported by domain model/context behavior.
- [x] A6 The API platform DB helper can open a pool, ping the database, and close cleanly in tests.
- [x] A7 Persistence port/adapter examples or tests show consumer-owned interfaces declared where used and implemented by `<context>/pg` packages when an interface is actually needed.
- [x] A8 The persistence convention requires future product tables and queries to be scoped by authenticated user id.
- [x] A9 No ORM, server-authoritative client-derived layout state, or direct cross-context table access is introduced.

## Implementation Checklist
- [x] T001 Before changing dependencies/tools, check current official pgvector, Postgres Docker, Goose, sqlc, and pgx docs.
- [x] T002 Add a local Postgres+pgvector service back to Docker Compose without reintroducing MVP runtime services.
- [x] T003 (migrate) Create `apps/api/db/migrations` and the initial Goose migration for platform extensions only.
- [x] T004 Update `scripts/db.mjs` so `pnpm db:migrate`, `db:status`, `db:down`, and `db:reset` target the clean API database path and Docker service.
- [x] T005 Configure sqlc under `apps/api` with schema/query inputs and an infra-only generated output path.
- [x] T006 (gen) Update `scripts/gen.mjs`/`scripts/lib.mjs` detection so `pnpm gen:sql` runs when schema/query inputs exist and skips cleanly before then.
- [x] T007 Add `apps/api/internal/platform/db` (or final platform path) for config, pgx pool creation, ping, close, and transaction helper.
- [x] T008 Add tests for DB pool open/ping/close against the local service or a documented Docker-backed test path.
- [x] T009 Add a minimal persistence port/adapter example/test showing consumer-owned interfaces and `<context>/pg` implementations without product tables.
- [x] T010 Document the user-id persistence convention for future tables/queries and connect it to auth context from job 04.
- [x] T011 Verify no ORM, product table, direct cross-context query, or server-authoritative client-derived state is added.

## Grounding
- Source plan: [plan/05.data-persistence](../plan/05.data-persistence.md).
- Architecture: [ARCHITECTURE.md](../../spec/ARCHITECTURE.md) §2.3, §2.4, §2.6, §4.
- Vocabulary: [ubiquitous-language.md](../../spec/ubiquitous-language.md) §1 for future table naming.
- Values: no `spec/values.yaml` entries expected.
- Policy/tech reflection: persistence, migration, sqlc, and per-user query conventions should be reflected after implementation if policy/tech docs exist.

## Affected files (blast radius)
- `docker-compose.yml`, possible `.env.example`/setup docs
- `apps/api/go.mod`, `apps/api/go.sum`
- `apps/api/db/migrations/**`, `apps/api/db/queries/**`, `apps/api/db/gen/**`, `apps/api/sqlc.yaml`
- `apps/api/internal/platform/db/**`, possible `apps/api/internal/platform/config/**`
- `scripts/db.mjs`, `scripts/gen.mjs`, `scripts/lib.mjs`
- root `package.json` DB/codegen scripts if needed
- DB/sqlc/repository tests
- `spec/plan/00.overview.md`, `spec/jobs/05.data-persistence.md`

## Verification / DoD
- [x] Every Acceptance Criteria item above is true in the current code.
- [x] `docker compose up` starts Postgres with pgvector.
- [x] `pnpm db:migrate`, `pnpm db:status`, `pnpm db:down`, and `pnpm db:reset` work on the clean API path.
- [x] `pnpm gen:sql` behavior matches the presence/absence of schema/query inputs.
- [x] Go tests for DB pool and persistence port/adapter example pass.
- [x] Grep confirms no ORM dependency and no product domain tables.

## Review
- [x] `/code-review` applied: self-review added a nil-store guard for the persistence probe and removed stale `spec 05`
  AI env comments · Codex review applied 1 finding (default `pnpm test:api` now prepares the DB and injects a test DB
  URL) · attachment review applied plan05 findings for deploy migration sync, sqlc runtime probe execution, DB helper
  guard cleanup, generated-path cleanup, and read-only `db:status` behavior · 0 rejected.

## After completion - reflect into the SSOT
- [x] Update [plan/05.data-persistence](../plan/05.data-persistence.md) to built/as-built details.
- [x] Reflect persistence rules into policy/tech docs if present.
- [x] Update [00.overview](../plan/00.overview.md) status for plan/job 05.
- [x] Set this job frontmatter `status: done`.
