---
job: "11"
type: new
source: plan/11.config-values
plan: plan/11.config-values
status: done
title: config values
---

# Job 11: config values  (new)

> Implementation work doc. Source spec: [plan/11.config-values](../plan/11.config-values.md).
> /cosimosi:implement-job 11 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/11.config-values)
- [x] A1 `spec/values.yaml` exists with documented grouping and at least the Phase 1 client-cache entries.
- [x] A2 `pnpm gen:values` validates the YAML and generates deterministic TypeScript and Go constants.
- [x] A3 Generated TypeScript constants are platform-pure and importable by both web and mobile.
- [x] A4 Generated Go constants live under an API platform package, not a product domain package.
- [x] A5 Non-finite numbers, invalid key casing, and unsupported nested structures fail generation with clear errors.
- [x] A6 `pnpm gen` runs values generation before proto/sql generation.
- [x] A7 The full quality gate fails when generated values output is stale.
- [x] A8 Documentation states what belongs in `values.yaml` and what must remain env/proto/sql/CSS/code.
- [x] A9 No runtime remote config, secrets, feature flags, or product formulas are introduced.

## Implementation Checklist
- [x] T001 Audit the existing `scripts/gen-values.mjs` behavior and decide final output paths for platform-pure TS and API platform Go constants.
- [x] T002 (gen:values) Create `spec/values.yaml` with documented grouping and the Phase 1 `client_cache.*` entries required by plan 06.
- [x] T003 Update `scripts/gen-values.mjs` validation for snake_case groups/keys, finite values, allowed arrays/maps/strings, and clear failures for unsupported structures.
- [x] T004 Update generated TS output to a DOM-free package/path importable by both web and mobile.
- [x] T005 Update generated Go output to an API platform package, not a product domain package.
- [x] T006 Ensure `pnpm gen` runs values generation before proto/sql generation and keeps skip/fail behavior consistent once `spec/values.yaml` exists.
- [x] T007 Add tests or fixtures for non-finite values, invalid key casing, unsupported nested structures, and deterministic formatting.
- [x] T008 Integrate generated-values freshness into the quality gate from job 02, or document the dependency if job 02 lands first.
- [x] T009 Document what belongs in `values.yaml` versus env/proto/sql/CSS/code/flags.
- [x] T010 Run `pnpm gen:values` and confirm a second run is clean.

## Grounding
- Source plan: [plan/11.config-values](../plan/11.config-values.md).
- Architecture: [ARCHITECTURE.md](../../spec/ARCHITECTURE.md) §4.
- Existing generator seam: `scripts/gen-values.mjs`.
- Dependencies: [plan/06.client-data-cache](../plan/06.client-data-cache.md) consumes the initial `client_cache.*` values; [plan/02.quality-gates](../plan/02.quality-gates.md) enforces freshness.
- Values: creates and owns `spec/values.yaml` for Phase 1 values.
- Policy/tech reflection: generated-config ownership should be reflected after implementation if policy/tech docs exist.

## Affected files (blast radius)
- `spec/values.yaml`
- `scripts/gen-values.mjs`, possible generator tests/fixtures
- root `package.json` `gen` scripts if ordering changes
- generated TS values under `packages/config/**` or final platform-pure output path
- generated Go values under `apps/api/internal/platform/**` or final API platform output path
- package/workspace manifests if a new config package is created
- docs for values ownership
- `spec/plan/00.overview.md`, `spec/jobs/11.config-values.md`

## Verification / DoD
- [x] Every Acceptance Criteria item above is true in the current code.
- [x] `pnpm gen:values` succeeds and a second run is clean.
- [x] Invalid values fixtures fail with clear errors.
- [x] Web and mobile can import generated TS constants without DOM/native bundler assumptions.
- [x] Go can import generated constants from a platform package.
- [x] `pnpm gen` ordering is values first.
- [x] No secrets/runtime config/feature flags/formulas are added to `spec/values.yaml`.

## Review
- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion - reflect into the SSOT
- [x] Update [plan/11.config-values](../plan/11.config-values.md) to built/as-built details.
- [x] Reflect generated-values rules into policy/tech docs if present.
- [x] Update [00.overview](../plan/00.overview.md) status for plan/job 11.
- [x] Set this job frontmatter `status: done`.
