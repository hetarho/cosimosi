---
job: '99'
type: 'refactor'
source: 'code-review/09.epic-k-account-runtime-and-gate-audit'
plan: 'none'
status: done
title: 'Gate scope and guard coverage'
---

# Job 99: Gate scope and guard coverage (refactor)

> Implementation work doc. Source spec: [code-review/09.epic-k-account-runtime-and-gate-audit](../../code-review/09.epic-k-account-runtime-and-gate-audit.md).
> /cosimosi:implement-job 99 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

Covers **R008** and **R014** (P2/P3) plus the orphaned-probe hole recorded in review 09's Verification Notes — the
_guards narrower than the rules they advertise_ theme. Every item here is a gate that passes today while covering less
than its own wording claims, which is precisely the cluster per-job verification cannot see.

The R4 guard's own scope gap is job 98's; this job takes the remaining three.

## Acceptance Criteria (from code-review/09.epic-k-account-runtime-and-gate-audit)

- [x] A1 `lint:comments` scans `.sql` and `scripts/`. The plan/job-number narration its own `NARRATION` set already
      forbids (`/\bplan[-\s]?\d/i`, `/\bjob[-\s]?\d/i`, `/\bEpic [A-Z]\b/`) is caught wherever it appears in active
      source, not only in `.go`/`.ts` under four roots.
- [x] A2 The backlog the widened scope surfaces is cleared, not suppressed: each flagged comment is rewritten to state
      the invariant it protects, with the plan/job number dropped. Bracketed requirement anchors (`[U1]`, `[I1]`, `[G6]`)
      and `§` architecture pointers stay — the lint's own note exempts them as design rationale.
- [x] A3 The lint does not flag its own fixtures. `lint-comment-history.mjs` contains deliberate violation strings for its
      probe, and `new-job.mjs` carries a usage example (`pnpm spec:job plan 15`) that is a command, not narration —
      neither may break the gate, and the exemption is narrow and explicit rather than a blanket `scripts/` skip.
- [x] A4 `pnpm lint:comments:probe` still proves the guard rejects a violation after the widening, including one in a
      newly covered file type.
- [x] A5 The `[I1]` hard-delete gate matches `TRUNCATE` as well as `DELETE FROM`, so a `TRUNCATE` of a product user table
      can no longer pass while the gate claims product rows are hard-deleted only by the sanctioned retention mechanisms.
- [x] A6 A probe proves A5, in the same style as the existing "fails a scoped product DELETE that is not in the [I1]
      hard-delete allowlist" test.
- [x] A7 The three orphaned guard probes — `lint:raw-strings:probe`, `lint:comments:probe`, `lint:style-escapes:probe` —
      run as part of `pnpm check`. Today none is wired into `lint`, `test`, or `check`, so all three could silently stop
      detecting anything while CI stays green.
- [x] A8 **No behavior change** to runtime code. This job touches gate scripts, their tests, gate wiring, and comments
      only. No product `.go`/`.ts` logic, no SQL statement text, no schema — a comment edit inside a migration must not
      alter the migration's effect.
- [x] A9 `pnpm check` is green end to end after the widening, with no rule relaxed to get there.

## Implementation Checklist

- [x] T001 Widen [lint-comment-history.mjs](../../../scripts/lint-comment-history.mjs): extend `CODE_EXT`
      (`/\.(go|[jt]sx?)$/`) to cover `.sql`, and add `scripts` to `SRC_GLOBS`
      (`['apps/web/src', 'apps/mobile/src', 'apps/api', 'packages']`). Add the `--` SQL comment marker to `COMMENT` if
      the existing pattern does not already reach it. Keep `SKIP_FILE`'s generated exemptions — `.sql.go` output is
      already excluded, so `db/gen/*.sql.go` stays out.
- [x] T002 Add the narrow self-exemption for A3 before running the widened lint: the guard's own fixture block in
      `lint-comment-history.mjs` and the usage example in `new-job.mjs`. Exempt by explicit path (with the reason at the
      exemption, matching how `check-persistence-isolation.mjs` names its global-query allowlist entries) — not by
      skipping `scripts/` wholesale, which would undo T001.
- [x] T003 Run the widened lint and capture the full backlog. Expected from the audit's grep: migrations `00005`, `00006`,
      `00011`, `00012`, `00014`, `00015`, `00016`; queries `admin/admin.sql`, `memory/{admin_stats,consolidate,deletion,diaries,encode,recall,reconsolidation,release,synapses,view_semantic}.sql`;
      scripts `check-persistence-isolation.mjs`, `check-spec-hygiene.mjs`, `probe-web-boundary.mjs`,
      `probe-mobile-boundary.mjs`. Treat the list produced by the run as authoritative — the grep was approximate.
- [x] T004 Clear the backlog per A2. Migrations are the priority: they are permanent and never rewritten, so
      "users — plan 60's account profile row" must become a statement of what the table guarantees. Preserve every
      requirement anchor and `§` pointer. Do not reword a comment into something the code does not do — if a comment is
      wrong rather than merely historical, fix the comment to match the code and note it.
- [x] T005 Extend `lint:comments:probe` (A4) with a case in a newly covered file type, and confirm it fails against the
      pre-T001 guard.
- [x] T006 [P] Add `TRUNCATE` to the hard-delete scan in
      [check-persistence-isolation.mjs](../../../scripts/check-persistence-isolation.mjs) (R014). The existing pattern is
      `/\bDELETE\s+FROM\s+…/gi` matched against the migration-derived `productUserTables` set; add the `TRUNCATE` form
      (including `TRUNCATE TABLE` and comma-separated table lists) against the same set and the same
      `hardDeleteQueries` allowlist.
- [x] T007 [P] Add the A6 probe to [check-persistence-isolation.test.mjs](../../../scripts/check-persistence-isolation.test.mjs),
      beside the existing `[I1]` DELETE probe. Keep the "real repository corpus passes" test green — it is the regression
      net for both T006 and T001.
- [x] T008 Wire the three orphaned probes into the gate (A7). Add them to `lint` (next to the guards they prove) or to
      `test:guards`; pick one home and keep all three together so a future probe is added to an obvious place. Verify each
      genuinely fails when its guard is broken — a probe wired in but passing unconditionally is the same hole with more
      steps.
- [x] T009 Run the full `pnpm check` and confirm A9. If the widened lint surfaces a case the `NARRATION` set flags
      wrongly, fix the pattern's precision rather than exempting the file — a false positive breaks the whole gate, which
      is why the pattern set is deliberately tight.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_) — [I1] "the only hard delete
  is the user's own sweep" is the invariant T006 widens the guard for.
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — §4 (_Per-user isolation_ — the gate enforces it
  semantically, with deliberately global scans named one-by-one next to the reason they cross users; _Gates fail loudly_ —
  no gate uses force-exit or warning suppression to stay green).
- Principle: [code-comments.md](../../principle/code-comments.md) — "never record process or history … change/ticket
  numbers"; [timeless-documents.md](../../principle/timeless-documents.md) — the artifact must read correctly cold.
- tech / policy / values touched: none expected. If the widened lint's scope is described anywhere in
  `spec/ARCHITECTURE.md` §4 or the principle docs, update the wording to match what the guard now covers.

## Affected files (blast radius)

Gate scripts and their tests:

- `scripts/lint-comment-history.mjs` — `CODE_EXT`, `SRC_GLOBS`, `COMMENT`, self-exemption (R008)
- `scripts/check-persistence-isolation.mjs` — the `TRUNCATE` form in the `[I1]` scan (R014)
- `scripts/check-persistence-isolation.test.mjs` — the A6 probe
- `scripts/lint-fsd-layout.test.mjs` — untouched here (job 98 owns it)
- `package.json` — wire `lint:raw-strings:probe`, `lint:comments:probe`, `lint:style-escapes:probe` into the gate (A7)

Comment backlog — SQL only, statements unchanged (BE context `memory`/`admin`/`account`, `db/` layer):

- `apps/api/db/migrations/00005_forgetting_offset_days.sql` · `00006_memory_provenance.sql` ·
  `00011_deletion_safe_jobs.sql` · `00012_release_seal_ownership_repair.sql` · `00014_memory_paid_action_receipts.sql` ·
  `00015_admin_console.sql` · `00016_account_identity.sql`
- `apps/api/db/queries/admin/admin.sql`
- `apps/api/db/queries/memory/{admin_stats,consolidate,deletion,diaries,encode,recall,reconsolidation,release,synapses,view_semantic}.sql`
- `scripts/{check-persistence-isolation,check-spec-hygiene,probe-web-boundary,probe-mobile-boundary}.mjs` — comment text only

Excluded: `apps/api/db/gen/*.sql.go` (generated; already exempt via `SKIP_FILE`).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — n/a, this is a refactor
- [x] Codegen / migration / values applied: sqlc comments regenerated; no values change; all 17 migrations applied to
      an isolated fresh database and the temporary database was removed
- [x] `pnpm lint:comments`, `pnpm lint:persistence`, `pnpm test:guards` green; all three probes green **and** proven to
      fail when their guard is broken
- [x] `pnpm check` green end to end with no rule relaxed (A9)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`; no new
      `[I1]` allowlist entry added (the point of T006 is to widen detection, not to permit more deletes)
- [x] Architecture self-audit passed: no product source file's behavior changed; every guard edit is accompanied by a
      probe that fails without it

## Review

- [x] Focused review applied. One precision issue was found and fixed: SQL and non-SQL comment markers are dispatched
      separately, so a JavaScript decrement cannot be mistaken for an SQL comment. No actionable findings remain.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality (`plan/62` now names `DELETE` and `TRUNCATE`) · no policy, tech, or values change
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — n/a
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
