---
job: '84'
type: 'refactor'
source: 'code-review/06.batch-75-78-admin-console,-error-taxonom'
plan: 'none'
status: done
title: 'batch-06 cross-cutting hygiene'
---

# Job 84: batch-06 cross-cutting hygiene (refactor)

> Implementation work doc. Source spec: [code-review/06.batch-75-78-admin-console,-error-taxonom](../code-review/06.batch-75-78-admin-console,-error-taxonom.md).
> /cosimosi:implement-job 84 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/06 · R010, R011, R014, R017, R018)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 (R010) The platform-pure `error-toast` context + hook (`ErrorToastContext`, `ShowErrorToast`,
      `useErrorToast`) live in a shared module (`@cosimosi/errors` or a shared react seam) and both apps re-export from
      it (promote-on-reuse); only the provider **component** stays forked per platform. Web and mobile behavior is
      unchanged.
- [x] A2 (R011) The forbidden ticket/task tokens (`T010`, `A3`–`A9`) are removed from the Go comments in
      `internal/ai/{factory.go,metering.go,clients.go,extractor.go,embedder.go}` and `internal/admin/ports.go` (prose
      kept). `scripts/lint-comment-history.mjs` is extended to flag the `T0xx`/`Ax` forms, and `lint:comments:probe`
      proves a violation fails. (`[I1]`/`§x.y` anchors remain allow-listed.)
- [x] A3 (R014) The dead helpers `boolStr` and `deref` in `apps/api/internal/admin/helpers.go` are deleted; `itoa`
      (the only used helper) remains.
- [x] A4 (R017) The CI Go toolchain is pinned: `.github/workflows/ci.yml` uses `actions/setup-go` with a version
      matching `apps/api/go.mod` (or forces the Docker path) so the backend gate no longer depends on the runner's
      ambient Go. (Coordinate with job 80/A6 if it also touches `ci.yml`.)
- [x] A5 (R018) `spec/plan/58.admin-console.md` AC4 no longer references `baseURL` — it states the provider endpoint is
      adapter-owned per change03.
- [x] A6 No behavior change: this job is hygiene/docs/CI + one promotion with identical runtime behavior; the full
      gate stays green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values). -->

- [x] T001 [P] Promote `error-toast`: move the context + hook into `@cosimosi/errors` (react entry) or a shared seam;
      update `apps/web/src/shared/model/error-toast.ts` + `apps/mobile/src/shared/model/error-toast.ts` to re-export;
      keep each provider component forked. Run both apps' typecheck/lint (R010). (Coordinate with job 81, which adopts
      the toast seam in admin — sequence 84→81 or share the import path.)
- [x] T002 [P] Strip `T010`/`A3`–`A9` tokens from the Go comments listed in A2; keep the explanatory prose (R011).
- [x] T003 [P] Extend `scripts/lint-comment-history.mjs` to flag `T0\d+`/`\bA\d+\b` ticket forms; verify with
      `pnpm lint:comments:probe`; ensure existing allow-listed anchors still pass (R011).
- [x] T004 [P] Delete `boolStr` + `deref` from `apps/api/internal/admin/helpers.go` (R014); `pnpm vet:api`.
- [x] T005 [P] Add `actions/setup-go` (pinned to `go.mod`'s version) to `.github/workflows/ci.yml`, before `pnpm check`
      (R017).
- [x] T006 [P] Edit `spec/plan/58.admin-console.md` AC4 (~:347): drop `baseURL`, note adapter-owned endpoint (change03)
      (R018).
- [x] T007 Run the full gate (`pnpm lint`, `pnpm test`, `pnpm vet:api`) — especially `lint:comments` after T003.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (unaffected — hygiene).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (promote-on-reuse to `packages/*`) · BE §2.
  Invoke `/fe-architecture` for the `error-toast` promotion, `/be-architecture` for the Go comment/helper changes.
- Source findings: code-review/06 **R010** (duplicated toast glue), **R011** (ticket-number comments + lint gap),
  **R014** (dead helpers), **R017** (unpinned CI Go), **R018** (plan-58 `baseURL` doc drift).
- Principle: `spec/principle/code-comments.md` (no plan/ticket numbers in comments).
- tech / policy / values touched: `spec/plan/58.admin-console.md` (AC4 doc fix); no values/migration.

## Affected files (blast radius)

- `packages/errors/src/**` (+ react entry) — **new** shared `error-toast` context/hook (R010).
- `apps/web/src/shared/model/error-toast.ts` + `apps/mobile/src/shared/model/error-toast.ts` — re-export (R010).
- `apps/api/internal/ai/{factory.go,metering.go,clients.go,extractor.go,embedder.go}` + `internal/admin/ports.go` —
  comment token removal (R011).
- `scripts/lint-comment-history.mjs` — ticket-form guard (R011).
- `apps/api/internal/admin/helpers.go` — delete dead helpers (R014).
- `.github/workflows/ci.yml` — pin Go (R017).
- `spec/plan/58.admin-console.md` — AC4 `baseURL` doc fix (R018).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) n/a — refactor; A6 identical runtime behavior (promotion is a re-export, comment/CI/doc edits)
- [x] Codegen: none (no proto/sql/values change); `pnpm install` picked up the new `@cosimosi/errors` react peer dep
- [x] FE `pnpm --filter @cosimosi/web build`·`lint`, `--filter @cosimosi/mobile lint`, `lint:fsd:layout`,
      `typecheck`, `typecheck:mobile`, `test:mobile`, `test:errors` green; BE `go vet && go build && go test ./...`
      green in Docker; `lint:comments` + `lint:comments:probe` green
- [x] Constitution sanity: hygiene only, no persistence touched
- [x] Architecture self-audit passed (`/fe-architecture` promote-on-reuse: the shared context/hook now lives in
      `@cosimosi/errors/react`, provider components stay forked; `/be-architecture`: comment/helper edits only)

## Review

- [x] `/code-review` applied (rejections noted with reason) · codex skipped (mechanical hygiene: docs/CI/comment-strip + one re-export promotion, per the small/mechanical rule)
  - Adversarial review (fresh-context agent). Verified: the react promotion is behavior-identical (plain
    `createContext`, no test asserts the old throw text, all 18 `useErrorToast` consumers resolve through the
    unchanged `shared/model` barrel, `react` is peer+dev only so non-react consumers don't pull it in); the 7 Go
    comment edits leave clean prose; `boolStr`/`deref` deletion safe (`itoa` still used); `setup-go@v5 1.26` matches
    `go.mod`; plan-58 `baseURL` removed; the FE `Ax` convention correctly left untouched.
  - **Applied** (reviewer F1/F2): A2/T003 require the `Ax` form flagged too. A bare `\bA\d+\b` would break ~200
    pre-existing acceptance-criteria refs across the memory epic + frontend (earlier batches, out of this job's
    blast radius). Resolution: cleaned **all** `Ax` tokens from the two contexts R011 actually names
    (`internal/ai` + `internal/admin`, 4 residual sites beyond the enumerated files) and added a **path-scoped**
    `Ax` guard for exactly those two contexts (bracketed requirement anchors like `[A11]` stay allow-listed via
    lookbehind); the `T0xx` task-id form is guarded globally. The probe proves both catch and proves the same `Ax`
    form is left alone outside the scoped contexts.
  - **Deferred with rationale**: enforcing the `Ax` acceptance-criteria form tree-wide (memory context + all of the
    frontend) is a dedicated cleanup of ~200 comment lines across dozens of out-of-scope files — a separate hygiene
    job, not this P3. The two contexts under review 06 are now clean and guarded.

## After completion — reflect into the SSOT

- [x] SSOT: `spec/plan/58.admin-console.md` AC4 + `GetAIConfig` bullet drop `baseURL` (adapter-owned per change03);
      no policy/values change (hygiene). The `Ax`-scoping decision is recorded in this job doc + the lint script's
      own comment.
- [x] (if type=change) n/a — refactor
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
