---
job: "37"
type: refactor
source: code-review/04.epic-a-memory-genesis-runtime-and-gate-a
plan: none
status: done
title: Rendering and backend seam hygiene and tests
---

# Job 37: Rendering and backend seam hygiene and tests  (refactor)

> Implementation work doc. Source spec: [code-review/04.epic-a-memory-genesis-runtime-and-gate-a](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md).
> /cosimosi:implement-job 37 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Result (complete — 2026-07-06)

Landed in two passes. Pass 1 (2026-07-05) fixed the GPU leak, the R013 hygiene trio, and the force-sim edge tests; the
finalizing pass (2026-07-06) resolved every item pass 1 had deferred, so every Acceptance Criterion below is now met.
Both passes gate-green (`pnpm typecheck` · `lint` · `test` incl. Go).

**Pass 1 (2026-07-05):**
- **R005 (A1) — GPU buffer leak fixed.** `InstancedNodeLayer` `dispose()`s the previous `InstancedBufferAttribute`
  before replacing it on a node-count change, matching `ColorField`.
- **R013 hygiene:** mobile `NebulaNotice` imports `m` via the app `shared/i18n` barrel; `UniverseCanvas.native`
  disposes its `WebGPURenderer` on cleanup; the anthropic `maxTokens <= 0` branch errors instead of minting a
  1-token request.
- **R012:** `packages/force-sim` gained empty-universe and single-unconnected-neuron+memory tests
  (no NaN / no divide-by-zero) — 19 tests pass.

**Finalizing pass (2026-07-06):**
- **R010 (A2) — `past_dated` on the wire.** `LaunchStarsResponse.past_dated` added (proto field 3 + `gen:proto` +
  `rpc/server.go` maps `result.PastDated`); both apps gate the optimistic insert on `!response.pastDated` instead of
  inferring it from an empty id list. `isPastDated` is kept and documented as the pre-launch UX *prediction* — a
  warning before the call, a different role, not a re-derivation of the server outcome.
- **R010 — plasticity seams annotated as forward reservations** (not dropped): `SignalKindSharedNeuron`/
  `SignalKindTemporal` + their `synapse.initial_*` values, `Depress`, and `EffectiveStrength`/`EffectiveBrightness`/
  `EffectiveSynapseStrength` carry timeless "why" reservation comments (PRD §5.3.2 [L10] tiered initials).
- **R013 — remaining hygiene:** the inert web HUD Settings/Explore buttons removed (web↔mobile parity — mobile has
  none), the now-dead `universe_home_settings` message dropped; `AWAKEN_CAPACITY` sourced from
  `VALUES.rendering.awakenCapacity` (`awaken_capacity: 32`).
- **R012 — invariant tests added:** web + a new mobile `ReviseControls.test.tsx` pin the [I3] editable-surface
  invariant on both platforms; `internal/memory/rpc/mapping_test.go` pins the proto↔domain mappers.

## Acceptance Criteria (from code-review/04 — R005, R010, R013, R012)
<!-- GPU leak closed, forward seams resolved, drift/dead-code removed, invariants pinned by tests. -->
- [x] A1 **No orphaned GPU buffers on node-count change.** `InstancedNodeLayer` disposes the previous
      `InstancedBufferAttribute` before replacing it on count change, matching the sibling `ColorField`. Launching
      diaries repeatedly does not grow GPU buffer count unboundedly. (R005.)
- [x] A2 **Forward seams are resolved.** The monotonic-guard outcome is returned on the wire (`past_dated` on
      `LaunchStarsResponse`) and the FE gates its optimistic insert on it (the pre-launch `isPastDated` stays as an
      explicitly-documented client *prediction*, a warning-before-the-call — a different role, not a re-derivation of
      the server outcome). `SignalKindSharedNeuron`/`SignalKindTemporal` + their `values.yaml` initials are annotated as
      explicit forward reservations ([L10] tiered initials — today's Link seeds no synapse from them: a shared neuron is
      the link via membership, temporal is a bonus); `EffectiveStrength`/`EffectiveBrightness`/`Depress`/
      `EffectiveSynapseStrength` documented as forward reservations. (R010.)
- [x] A3 **Minor divergences / dead affordances removed.** `NebulaNotice` imports `m` through the same `shared/i18n`
      barrel on both apps (done job 37 prior pass); the inert web HUD Settings/Explore buttons are removed (both apps now
      agree — mobile has none; the dead `universe_home_settings` message dropped, `universe_home_explore` still used by
      mobile's shell home); `AWAKEN_CAPACITY` is sourced from `VALUES.rendering.awakenCapacity`; the native
      `UniverseCanvas` disposes its `WebGPURenderer` and the anthropic `maxTokens <= 0` branch errors (both done prior). (R013.)
- [x] A4 **Core invariants are pinned by platform-agnostic tests.** The revise-split editable-surface invariant ([I3]:
      no strength/position/time control) is asserted against the rendered control set on **both** platforms — the web
      vitest test (control set + no continuous-scalar affordance) and a new mobile jest/testing-library-rn test (mood is
      a bounded selection, nothing `adjustable`, no numeric TextInput); `packages/force-sim` has empty-graph and
      single-node fixtures (prior pass); `internal/memory/rpc` has a thin proto↔domain mapping test. (R012.)
- [x] A5 **No behavior change / no regression.** Rendered output, launch behavior, and force-sim golden output are
      unchanged; all gates (`typecheck`/`lint`/`test` incl. Go) stay green.

## Implementation Checklist
- [x] T001 `packages/3d-renderer/src/layers/InstancedNodeLayer.tsx`: `existing?.dispose()` before `setAttribute` in the
      count-change branch, mirroring `ColorField`. (R005, A1.) — done in job 37's prior pass.
- [x] T002 Returned the monotonic-guard outcome on the wire: `past_dated` added to `LaunchStarsResponse`
      (`proto/cosimosi/memory/v1/memory.proto`, `gen:proto`); `result.PastDated` mapped in
      `internal/memory/rpc/server.go`; both apps' `WritingFlowSheet.runLaunch` gate the optimistic insert/announce on
      `response.pastDated` (the authoritative outcome). `isPastDated` kept + documented as the pre-launch UX prediction. (R010, A2.)
- [x] T003 Resolved the plasticity seams via `/be-architecture`: **annotated as forward reservations** (not dropped) —
      PRD §5.3.2 [L10] spec's the tiered initials (same > shared > temporal), so dropping would contradict an invariant.
      `SignalKindSharedNeuron`/`SignalKindTemporal` + their `values.yaml` initials, `Depress`, and
      `EffectiveStrength`/`EffectiveBrightness`/`EffectiveSynapseStrength` carry timeless "why" reservation comments. (R010, A2.)
- [x] T004 Minor FE/BE hygiene (R013, A3):
      - `NebulaNotice` `m`-via-barrel — done prior pass.
      - `apps/web/src/pages/universe/ui/UniverseHomePage.tsx`: removed the inert Settings/Explore buttons (web↔mobile
        parity — mobile has none); dropped the now-dead `universe_home_settings` message + updated `App.test.tsx`.
      - `AwakenNeuron.tsx` (both apps): `AWAKEN_CAPACITY = VALUES.rendering.awakenCapacity` (`awaken_capacity: 32` added
        to `values.yaml`); overflow was already handled (batched retry).
      - native `UniverseCanvas` dispose + anthropic `maxTokens <= 0` error — done prior pass.
- [x] T005 Tests (R012, A4):
      - `apps/web/src/features/revise-split/ui/ReviseControls.test.tsx` rewritten to assert the control set + absence of
        any continuous-scalar affordance; **new** `apps/mobile/src/features/revise-split/ui/ReviseControls.test.tsx`
        (jest + testing-library-rn) asserts the same contract on the RN Pressable fork.
      - `packages/force-sim` empty-graph + single-node tests — done prior pass.
      - **new** `apps/api/internal/memory/rpc/mapping_test.go` — pure proto↔domain mapper tests.
- [x] T006 Full gate green: `pnpm typecheck` · `pnpm lint` · web/mobile/force-sim vitest+jest · Go `vet`/`build`/`test`
      (real Postgres). Force-sim golden byte-identical (19 tests). Generated code fresh (`pnpm gen`).

## Grounding
- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 — esp. [I3] (emotion never in layout; the
  editable-surface test guards it), [I5] (positions emergent), [I10] (monotonic time — the `past_dated` seam).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3.3/§3.4 (rendering lifecycle & disposal;
  domain→visual), §3.5 (web/mobile parity for the hygiene fixes); BE §2.7 (proto = DTO; handler maps proto↔domain),
  §2.9 (no computed value dropped then re-derived). Invoke `/be-architecture`, `/fe-architecture`, `/mobile-architecture`.
- tech / policy / values touched: `spec/values.yaml` (`synapse.*` / `awaken` if changed), `proto/` (`LaunchStarsResponse`),
  `spec/tech/rendering.md` (disposal note), `spec/tech/synapse-plasticity.md` (seam status).

## Affected files (blast radius)
- `packages/3d-renderer/src/layers/InstancedNodeLayer.tsx` — buffer dispose
- `packages/3d-renderer/src/canvas/UniverseCanvas.native.tsx` — renderer dispose
- `proto/cosimosi/memory/v1/*.proto` (+ generated Go/TS) — `past_dated`
- `apps/api/internal/memory/rpc/server.go` (+ `rpc/*_test.go`) — map `past_dated`, mapping test
- `apps/api/internal/memory/plasticity.go`, `link.go`, `effective.go` — seam resolution
- `apps/api/internal/ai/anthropic/client.go` — maxTokens branch
- `apps/{web,mobile}/src/features/launch-stars/**` — consume `past_dated`
- `apps/{web,mobile}/src/entities/nebula/ui/NebulaNotice.tsx`, `apps/web/src/pages/universe/ui/UniverseHomePage.tsx`,
  `apps/{web,mobile}/src/features/awaken-neuron/ui/AwakenNeuron.tsx` — hygiene
- `apps/web/src/features/revise-split/ui/ReviseControls.test.tsx` — invariant test
- `packages/force-sim/src/**` (+ fixtures) — empty/single-node tests
- `spec/values.yaml` (+ regen) — only if `synapse.*`/awaken values change

## Verification / DoD
- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] `pnpm gen` (proto) · `pnpm gen:values` (awaken_capacity) · `pnpm gen:messages` (dropped message) applied
- [x] `pnpm --filter @cosimosi/web build && lint`, `pnpm typecheck`, `pnpm test`, BE `go vet && go build && go test`
      (Docker, real Postgres) pass; force-sim golden byte-identical
- [x] Architecture self-audit passed (`/be-architecture`): proto stays DTO (`past_dated` added, handler maps
      `result.PastDated` thinly), domain plasticity stays pure, no proto/sql leak inward; UL: fixed a "filament"
      rendering word that had slipped into domain plasticity.go
- [x] Constitution sanity: no memory row deletes; `past_dated` is now server-authoritative on the wire ([I10]); positions
      emergent ([I5]); the reserved plasticity seams only insert/strengthen ([I1])

## Review
- [x] `/code-review` (xhigh) over the job-37 working-tree diff (73 lines, 13 hand-written files) — **0 correctness
      findings**. Behavior-preserving: past-dated gating matches the prior empty-ids no-op path; `AWAKEN_CAPACITY` keeps
      value 32; HUD removal leaves no dangling import (`Button`/`m` still used by the fallback); the new test assertions
      are meaningful (TextField.native renders a real `TextInput`, mood chips filtered by `selected` state, option count
      = MOODS×memories). Note (not a defect): the `LaunchStars→past_dated` copy is an inline handler mapping, so it is
      exercised via the handler, not the pure `mapping_test.go`.
- [x] `/codex:review` **skipped** — the remaining work is hygiene + a straightforward field-wire + tests/annotations
      (small/mechanical per the skill); no race/lifecycle logic. (Prior pass's completed items were self-reviewed then.)

## After completion — reflect into the SSOT
- [x] `spec/tech/synapse-plasticity.md` — added the "Reserved tiers" note (shared/temporal initials, Depress, effective
      functions are forward reservations). `spec/tech/rendering.md` — added `awaken_capacity` to the config list.
      `spec/plan/20.encode-usecase.md` — `LaunchStarsResponse.past_dated` recorded as-built. `spec/values.yaml` —
      `awaken_capacity` added; initials annotated; stale temporal comment fixed.
- [x] 00.overview progress board ✅ (review 04 → job 37) · this doc's frontmatter `status: done`
