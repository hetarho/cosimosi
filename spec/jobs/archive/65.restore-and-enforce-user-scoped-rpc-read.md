---
job: '65'
type: 'refactor'
source: 'code-review/05.plans-35-53-runtime-and-architecture-aud'
plan: 'none'
status: done
title: 'Restore and enforce user-scoped RPC read policies'
---

# Job 65: Restore and enforce user-scoped RPC read policies (refactor)

> Implementation work doc. Source spec: [code-review/05.plans-35-53-runtime-and-architecture-aud](../code-review/05.plans-35-53-runtime-and-architecture-aud.md).
> /cosimosi:implement-job 65 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/05 — R001, R011)

- [x] A1 `TwinkleService/GetBalance`, `TwinkleService/QuoteSpend`, `MemoryService/GetProvenance`,
      `MemoryService/Export`, and `MemoryService/GetDiaries` each have an explicit
      `userScopedUnaryReadPolicy`; production transport allows them to proceed instead of throwing before I/O.
- [x] A2 Every authenticated read policy remains `GET`, idempotent, user-scoped, and never shared-CDN cacheable;
      no write RPC is made GET-eligible.
- [x] A3 A completeness test enumerates every generated unary `NO_SIDE_EFFECTS` descriptor and fails if it has
      no policy, has more than one policy, or is registered with an incompatible policy.
- [x] A4 Tests exercise the same interceptor composition used by `resolveClientCacheTransport`, not only a router
      transport, and prove all five newly-covered reads reach the downstream handler.
- [x] A5 Provenance loading, error, retry, success, and the theoretically-impossible empty response are distinct
      states on web and mobile; a transport failure is never rendered as “no history.”
- [x] A6 Existing Ping, GetUniverse, GetPalettePreference, mutation transport, query keys, auth headers, and
      telemetry behavior do not regress.
- [x] A7 No unrelated product behavior changes; all existing plan 44, 46, and 47 read semantics stay intact.

## Implementation Checklist

- [x] T001 Invoke `/fe-architecture` and `/mobile-architecture`; re-read R001/R011 plus
      `ARCHITECTURE.md` §2.7 and inventory every unary generated method whose idempotency is
      `NO_SIDE_EFFECTS`.
- [x] T002 Add the Twinkle read-policy registry and the three missing Memory entries, compose them once in the
      production interceptor, and keep all five on `userScopedUnaryReadPolicy`.
- [x] T003 Add a descriptor-completeness test that derives the expected set from generated service schemas and
      checks missing, duplicate, wrong-method, and shared-CDN policy failures; retain focused tests for the
      fail-loud unknown-read behavior.
- [x] T004 Add a production-composition transport test for each of the five reads, including auth and telemetry
      interceptor coexistence, while preserving tests for writes passing as POST.
- [x] T005 [P] Extend the provenance query seam on web and mobile to expose error/refetch explicitly; render a
      localized retry state in the feature UI and reserve empty copy for an actual successful empty payload.
- [x] T006 [P] Add parity tests: rejected provenance query -> error/retry (never empty), retry -> synthesized
      baseline success, and normal loading/success behavior unchanged.
- [x] T007 Ran the targeted API-client/client-cache/i18n/web/mobile suites, all affected typechecks, and the complete
      `pnpm check` gate successfully. The known pre-existing web fast-refresh/mobile `no-void` warnings and mobile
      React `act`/open-handle diagnostics remain assigned to later review findings and did not fail the gate.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/ARCHITECTURE.md` §2.7/§4; plans 44, 46, 47;
  `spec/tech/memory-persistence.md`. No values, migration, or proto change is expected.

## Affected files (blast radius)

- `packages/client-cache/src/http-policy.ts` — shared transport policy registry and aggregate.
- `packages/client-cache/src/client-cache.test.ts` — exhaustive descriptor and interceptor-composition coverage.
- `packages/client-cache/src/provider.ts` — production composition seam; change only if a testable exported
  composition helper is required.
- `packages/api-client/src/{memory,twinkle,account,platform}.ts` and generated service descriptors — read-only
  inventory inputs; facade edits only if an exhaustive descriptor export is needed.
- `apps/web/src/features/star-provenance/{api,ui}/**` — web feature read/error/retry state (`api/` already holds
  `get-provenance.ts` and `get-provenance.test.ts`).
- `apps/mobile/src/features/star-provenance/{api,ui}/**` — mobile parity.
- `apps/web/src/widgets/star-detail/ui/DetailPanel.tsx` — web widget composition of provenance states.
- `apps/mobile/src/widgets/star-detail/ui/DetailPanel.tsx` — mobile widget composition parity.
- `packages/i18n/messages/{en,ko}.json` plus generated message outputs — only if existing generic error/retry copy
  cannot express the state (`pnpm gen:messages`).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] No unrelated behavior change and no regression of plans 44, 46, or 47
- [x] Codegen applied for i18n and the generated API service inventory; no migration, values, or proto-contract change
- [x] `pnpm test:client-cache && pnpm test:api-client && pnpm test:web && pnpm test:mobile` passes
- [x] `pnpm typecheck:client-cache && pnpm typecheck:web && pnpm typecheck:mobile` passes
- [x] `pnpm lint && pnpm build:web && pnpm check` passes
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] Code review applied. Two P2 findings were fixed: (1) the provenance state classifier now recognizes both
      no-data and cached-data retries without collapsing transport failures into empty history; (2) the generated
      proto service inventory replaces a manually maintained service tuple, so a new service participates in the
      exhaustive policy gate automatically. Rejected alternative: a proto-filesystem scan inside the browser-targeted
      `client-cache` test package required Node types outside that package's runtime contract; generation-time
      inventory is deterministic, checked by `check:gen`, and keeps the test package browser-safe. The requested
      Codex CLI review was attempted directly with `gpt-5.5` but could not start because this checkout's installed
      CLI is missing its optional `@openai/codex-linux-x64` binary; the manual and independent read-only review found
      no remaining P0/P1 issue.

## After completion — reflect into the SSOT

- [x] Reflect the exhaustive read-policy registry and provenance error state in affected plan/tech SSOT; no tuning
      number is introduced
- [x] (not applicable — `type: refactor`) no `changes/` source doc to archive
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
