---
job: '128'
type: 'change'
source: 'changes/10.free-play-demo-with-a-step-gated-tutoria'
plan: 'plan/79.demo-page'
status: done
title: 'Free-play demo with a step-gated tutorial'
---

# Job 128: Free-play demo with a step-gated tutorial (change)

> Implementation work doc. Source spec: [changes/10.free-play-demo-with-a-step-gated-tutoria](../../changes/archive/10.free-play-demo-with-a-step-gated-tutoria.md).
> /cosimosi:implement-job 128 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from changes/10.free-play-demo-with-a-step-gated-tutoria)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 While the tutorial runs, only the current beat's anchored control is interactive; every other control is non-interactive **and** rendered dimmed + blurred; the skip affordance stays fully visible and interactive throughout ([O4]).
- [x] A2 Completing the last beat or pressing skip moves the run to free play permanently: all controls become interactive and undimmed, and no tutorial chrome lingers.
- [x] A3 The gating derives from one explicit finite-state machine that owns the run's phase (tutorial step ↔ free play); a control's availability cannot be set anywhere else, and pressing a gated control does nothing — a state-machine test walks the script and asserts per-state availability.
- [x] A4 In free play, a control with the product's writing verb draws a random prepared diary from the fixture pool and presents it through the same UI shapes as the product's writing flow: the entry appears, 별 쪼개기 reveals its precomputed split, 별 띄우기 launches it — repeatable at least as many times as the pool is deep, with no real write and no LLM call ([Z1][Z4][Z5]).
- [x] A5 The fixture pool is extended (authored, both locales, precomputed splits) and its integrity tests cover every added diary; the same diary always splits identically ([Z5]).
- [x] A6 Time travel is three controls — +1일 · +1주 · +1달 — advancing the demo clock by one day, seven days and one month respectively (month length per `spec/values.yaml`), and the advance is presented like the product's universe-time passing, not as a bare date-string swap; `UniverseTimeOverlay` itself is still never mounted.
- [x] A7 Forgetting is experienceable: as the clock advances, unrecalled stars dim by `effectiveBrightness` and their words erode by decay stage, and opening a launched star's entry shows the eroded current text while the diary body stays verbatim ([I2]).
- [x] A8 Recall is per-star and repeatable: each 회고 re-brightens that star, increments its recall count, reshapes its seed and restores its words — with no cost, balance or spend surface anywhere ([Z2][Z8]).
- [x] A9 Semanticization is experienceable per memory: memories left unrecalled while time advances climb the semantic stages and their gist bodies rise into the neocortex band; the sky keeps mirroring the strength-weighted emotions as the universe grows ([M5]).
- [x] A10 The isolation boundary is unchanged: a full session issues zero RPCs, DB writes and LLM calls; the demo-scoped lint block still bans every transport path; no `isDemo` appears in shared code; leaving `/demo` clears everything and nothing persists ([Z1][Z7][I13]).
- [x] A11 All `demo_*` copy is re-voiced in 해요체 with the product's verbs — "시간 밀기" and "일기 두 편 더" no longer exist in either catalogue — key sets match across `en.json`/`ko.json`, `pnpm gen:messages` output is committed, `lint:raw-strings` passes, and every sentence stays [I12]-honest with beat 8 carrying the [M5] definition.
- [x] A12 `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:guards`, `pnpm lint:fsd:layout` and the web build pass; the demo adds no `steiger` exemption and no new shared-code seam.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 (gen:values) `spec/values.yaml` — retire `demo.time_travel_step_days`, add
      `demo.time_travel_month_days` (universe-days one "+1달" press advances; day = 1 and week = 7 are fixed
      calendar units, not keys); `pnpm gen:values`, import the generated constant, no hardcoding.
- [x] T002 [P] `packages/demo` — the free-play pool: extend the corpus with authored diaries carrying
      precomputed splits (ko/en; names, moods, neurons, synapse rows canonically ordered, decay/reconsolidation
      texts where the scenario needs them), a per-diary draw (`pickDemoDiary`-shaped, deterministic given the
      draw number, cycling without immediate repetition) beside the set-wise pick, and `integrity.test.ts`
      coverage for every added diary ([Z4][Z5]; plan 77 is the consequent owner).
- [x] T003 `pages/demo/model/run-machine.ts` + `run-machine.test.ts` — the run FSM on the shipped XState 5
      foundation: states `tutorial(step ids from DEMO_SCRIPT)` → `freePlay` (final; entered on last-beat
      completion or skip). It exposes one derivation — which `DemoAnchor` controls are interactive in the
      current state (skip always; only the current beat's anchor during tutorial; all in free play) — and the
      test walks the script asserting per-state availability, including that skip from any step lands in
      `freePlay`.
- [x] T004 `pages/demo/model/use-demo-run.ts` + `use-demo-run.test.ts` — generalize the one-shot flags to
      per-memory stored facts: launched diaries as a growing list fed by pool draws, per-memory
      `recallCount`/`lastRecalledUniverseTime`/seed/currentText moves on each recall (repeatable, any star),
      per-memory gist-timer anchors on the demo clock (replacing the single scripted `gistStage`),
      `advanceClock(days)` for the three jumps, and `drawDiary()` for the write flow. Every derived quantity
      stays a shipped production function; the diary body stays verbatim ([I2][I8]).
- [x] T005 `pages/demo/model/anchors.ts` + `model/script.ts` — re-anchor the ten beats onto the new controls:
      the write-diary control replaces `add-diaries-action` (the constellation beat draws via the same flow),
      the time beats anchor the appropriate jump control, recall anchors the per-star control; captions stay
      an exhaustive record; the unions stay closed with no member for a control the demo does not own.
- [x] T006 `pages/demo/ui/` — the chrome: rework `DemoControlRail` (write-diary flow presented in the product
      writing-flow's UI shapes — entry appears → 별 쪼개기 → 별 띄우기 — three time-jump controls, per-star
      회고, signup CTA), a demo-local universe-time presentation mirroring the product's passing-time look
      (`UniverseTimeOverlay` stays never-mounted), and gating wired ONLY from the machine's derivation:
      gated controls are unpressable and rendered dim + blur; the current control keeps the highlight; skip
      untouched ([O4]).
- [x] T007 `pages/demo/ui/` — the entry reader: opening a launched star's entry shows its eroded
      `currentDecayText` beside/over the verbatim diary body (demo-local look-alike; `DetailPanel`,
      `RecallFlowSheet`, `WritingFlowSheet` stay never-mounted).
- [x] T008 Copy — re-voice every `demo_*` key in `packages/i18n/messages/{en,ko}.json` to 해요체 with the
      product's verbs (별 쪼개기 · 별 띄우기 · 회고하기; +1일/+1주/+1달; "시간 밀기" and "일기 두 편 더"
      removed), captions re-anchored where controls changed, beat 8 keeps the [M5] sentence, [I12] intact;
      then (gen) `pnpm gen:messages`.
- [x] T009 [P] Docs — `spec/policy/ux/demo.md` (tutorial-then-playroom: the gating rule, the free-play loop,
      the three time grains, per-star recall, retired batch controls), `spec/tech/web-routing.md`'s demo
      isolation section (the FSM note; closure unchanged), PRD §5.12 amendment notes ([Z3] beats re-anchored,
      [Z4] extended to per-diary draws); reflect plans 79 + 77 as-built at completion.
- [x] T010 Gates + isolation proof — `pnpm lint` · `pnpm typecheck` · `pnpm test` · `pnpm test:guards` ·
      `pnpm lint:raw-strings` · `pnpm lint:fsd:layout` · web build; the demo-scoped `no-restricted-imports`
      block is untouched and still bites; repo grep shows no `isDemo` in `packages/*`/`features/*`/`entities/*`;
      unmount still clears read models, clock, awaken registry and palette ([Z7]).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/policy/ux/demo.md` · `spec/tech/web-routing.md` (demo isolation
  section) · `spec/PRD.md` §5.12 ([Z3]/[Z4] amendment notes) · `spec/values.yaml` `demo` group
  (`time_travel_step_days` → `time_travel_month_days`, (gen:values)).

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

- `spec/values.yaml` + `packages/config/src/values.gen.ts` (via `pnpm gen:values`) — the `demo` group key swap
- `packages/demo/src/diary-sets/*` (new authored diaries) · `diary-set.ts` · `pick.ts` · `resolve.ts` ·
  `scenario.ts` · `index.ts` · `integrity.test.ts` — pure fixture package (plan 77 consequent)
- `apps/web/src/pages/demo/model/run-machine.ts` · `run-machine.test.ts` — NEW (pages/demo, `model`; XState 5)
- `apps/web/src/pages/demo/model/use-demo-run.ts` · `use-demo-run.test.ts` — generalized run state (`model`)
- `apps/web/src/pages/demo/model/anchors.ts` · `script.ts` — re-anchored unions/script (`model`)
- `apps/web/src/pages/demo/ui/DemoPage.tsx` · `DemoControlRail.tsx` (reworked; write flow + time jumps +
  gating treatment) · new demo-local time-presentation and entry-reader components · `DemoTasterRail.tsx`
  (gating props only) · `DemoUniverseScene.tsx` (only if the scene needs per-memory gist anchors) — (`ui`)
- `packages/i18n/messages/en.json` · `ko.json` (+ generated messages via `pnpm gen:messages`) — `demo_*` re-voice
- `spec/policy/ux/demo.md` · `spec/tech/web-routing.md` · `spec/PRD.md` §5.12 ·
  `spec/plan/79.demo-page.md` · `spec/plan/77.demo-fixtures-and-mirror.md` — SSOT reflection
- NOT touched: `apps/web/eslint.config.js` demo block (closure unchanged), `packages/sequence` (engine
  consumed as-is), `features/highlight-next-control` / `widgets/sequence-guide` (consumed as-is), any
  `packages/*` outside `demo` — no `isDemo`, no new shared seam

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — notably plan 79 AC1–AC5
      (route, zero-RPC, transport starvation, no `isDemo`, own canvas host + never-mount list), AC7 (tour on
      every entry, clean replay), AC8 ([Z4]/[Z5] determinism), AC10 ([Z8] no money), AC11 (taster ids-only),
      AC12 ([Z7] nothing survives), AC13–AC14 ([I2]/[I4]/[I6]), AC15 ([I12] copy)
- [x] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

Review notes: the `/code-review` command is not model-invocable in this environment, so the diff was
hand-reviewed against the same bar (machine sync/races, stale closures, signal double-fire — the
engine's current-step match plus the `ADVANCE` echo guard make the double `time_advanced`/`gist_risen`
report safe by design). Codex (gpt-5.5, read-only) reviewed the logic diff and returned three findings,
all accepted and applied: (1) HIGH — a repeated write press during the neuron-reuse beat could walk the
pool cursor past the authored second diary, and a no-op launch of an already-launched diary still
reported `launched` (fixed: `drawDiary` is a no-op while a draft exists; `onLaunch` reports only a
launch that actually added a diary; regression test added); (2) MED — the first sweep frame flashed the
committed final clock before the rAF loop started (fixed: `playSweep` seeds the displayed clock to
`previous` in the same commit); (3) LOW — the entry reader read the committed clock while the canvas
swept a sampled one (fixed: the reader reads `displayTime`). No rejections.

Post-ship user-feedback pass (same session): (a) the spotlight measured the gating wrapper instead
of the control (`SequenceAnchor` measures its first child) — the gate now wraps the anchor, and the
page re-measures on phase/draft layout shifts, so the ring hugs the actual button through every
beat; (b) the gate's blur softened (`blur-sm` → `blur-xs`) so set-aside controls stay legible; (c)
the clock pill moved top-centre out of the skip affordance's corner; (d) the closing CTA beat now
opens the whole room — gating everything but the highlighted CTA made leaving the only possible end
of the tour, the opposite of the change's intent. Verified end-to-end in headless Chrome (CDP
walkthrough: all ten beats, per-star narrowing at the recall beat, open room at the CTA beat,
chrome retiring into free play, draw/launch/time/reader all live).

Second feedback pass: (e) the signup CTA left the tour entirely — top-right under the skip's
corner, never highlighted, never gated, no `DemoAnchor` member; the closing beat anchors nothing
and its caption names the corner; (f) the covering became ONE page-level mask (`DemoTutorialMask`) — four dimmed, lightly blurred
strips with a hole cut at the union box of the beat's open controls, measured through the engine's
anchor registry; the rail keeps only `disabled`, and anchor-less beats (the sky beat) and free play
play unmasked;
(g) gist timers ride launch-induced clock jumps (existing anchors shift by the jump, launched
memories anchor at the arrived clock), so semanticization is earned with the time controls only —
writing a diary no longer pops a gist body; (h) memory stars and gist bodies picked on the canvas
open the entry reader (the layers' index→store-id mapping, the product widget's own); cell stars
stay non-interactive because the demo has free navigation instead of the rig's focus/fly
choreography. Verified again end-to-end in headless Chrome.

Third feedback pass: (i) the always-on honesty line dropped its "inspired by engram theory" opener —
the denial alone stays; the inspired-by framing is the landing's; (j) the tour's captions grew to
`text-base` and float mid-screen (`SequenceGuide` gained `captionStyle: 'edge' | 'center' | 'top'`,
`resolveCenteredCaptionPlacement` yields to the edges when the anchored control crosses the middle,
and the page switches to `edge`/`top` while the writing dialog / decoration sheet own the middle);
(k) the mask dim strengthened (`bg-background/60` → `/80`) and the whole layer now LIFTS (opacity,
not unmount) while a launch's awaken or a time sweep plays, returning after
`demo.mask_lift_linger_ms` (new values key); (l) the write flow moved into the product's own dialog
shapes (`DemoWritingSheet`, a read-only twin of `widgets/writing-flow` — the shipped widget is
transport-fused and an `isDemo` prop is the flag [I13] forbids): same `Dialog`, `writing_flow_*`
labels, proposal rows with mood/neuron chips; dismissable only when the write control could reopen
it; (m) the taster rail became the product's decoration panel as a read-only twin
(`DemoDecorationSheet`): 꾸미기 opens the same `Sheet` with the full catalog names from
`@cosimosi/store/i18n` (the names subpath — opened in the isolation block via a `paths` entry for
the barrel, since gitignore-style patterns cannot re-include a subpath; the probe now pins 10/10),
apply-on-select, `기본` rows, no price/save; the fixtures' per-set `ornamentTastes` were retired;
(n) a start-over control (처음부터 다시) sits under the CTA and re-keys the run; (o) mobile
arrangement: rail as bottom band, clock pill top-left of the skip, decorate button above the band —
verified in headless Chrome at 1440×900 and emulated 390×844, full ten beats + free play + reset.

Fourth feedback pass: (p) the standing theory note left the page entirely (`demo_theory_note`
retired from both catalogues) — honesty rides the [I12] review of the `demo_*` copy, and the
framing belongs to the landing; (q) the free-floating caption moved off dead center to the LOWER
third — the "정 중앙이라 우주가 안 보인다" note was about the caption, not the dialog (a Sheet
container was tried and reverted on feedback; the writing surface stays the product's centered
`Dialog`, and the launch closes it before the memory goes up); (r) the launch/sweep presentations now hold the next beat's
caption and ring back — the page hands the guide `null` while the mask is lifted — so the star
rises in the clear and "일기를 한 편 더 써볼까요?" arrives with the covering's return; (s)
`captionStyle` gained `'attached'`: a step staged inside the writing sheet glues its line just
below (or above) the highlighted control instead of the screen bottom; (t) the neuron-reuse beat's
ring follows the flow — the page overrides the guide's rect to 쪼개기 then 띄우기 once the drawn
diary is on screen; (u) `CameraControls` stays unmounted until free play, so drag/zoom cannot
carry the scene away from the tour. Verified again at both viewports end-to-end.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
