---
job: "03"
type: new
source: plan/03.rpc-transport
plan: plan/03.rpc-transport
status: done
title: rpc transport
---

# Job 03: rpc transport  (new)

> Implementation work doc. Source spec: [plan/03.rpc-transport](../plan/03.rpc-transport.md).
> /cosimosi:implement-job 03 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/03.rpc-transport)
- [x] A1 `pnpm gen:proto` generates Go and TypeScript outputs from `proto/` deterministically and skips cleanly only when no `.proto` files exist.
- [x] A2 The API serves Connect unary RPCs and `/health` from the same `net/http` composition root.
- [x] A3 `PlatformService.Ping` succeeds from a Go handler test and from a TypeScript client test or probe.
- [x] A4 Generated TypeScript transport code is importable by both `apps/web` and `apps/mobile` without DOM, Vite, or Metro specific dependencies.
- [x] A5 Request-id, logging, panic recovery, CORS, and auth-placeholder interceptors are wired without embedding business policy in handlers.
- [x] A6 The transport has no server-streaming API and no websocket/subscription path.
- [x] A7 A read-method convention for idempotent GET and cacheable responses is documented in code/tests.
- [x] A8 No product domain message, database table, or feature use-case is introduced by this unit.

## Implementation Checklist
- [x] T001 Before changing dependencies, check current official Buf, Connect, protobuf-es, connect-go, and connect-query docs and choose latest compatible versions.
- [x] T002 (gen) Add/refresh `proto/buf.yaml`, `proto/buf.gen.yaml`, and `proto/cosimosi/platform/v1/platform.proto` with `PlatformService.Ping` only.
- [x] T003 (gen) Update `scripts/gen.mjs` and package/workspace wiring so `pnpm gen:proto` emits Go code under `apps/api/internal/gen` and TypeScript under a platform-pure shared package/facade.
- [x] T004 Add the shared API-client package/facade that exports generated descriptors, transport creation, and test transport hooks without DOM/Vite/Metro dependencies.
- [x] T005 Update `apps/api` dependencies and composition root to mount Connect handlers, h2c, CORS, request-id, logging, panic recovery, and auth-placeholder interceptors.
- [x] T006 Add the thin `PlatformService.Ping` handler and keep it platform-only with no domain/use-case/database dependency.
- [x] T007 Add TypeScript client tests or probes for `Ping`, including import checks from both web and mobile contexts.
- [x] T008 Add Go handler/interceptor tests for `Ping`, request id, panic recovery, and public-method behavior.
- [x] T009 Document the unary-only and idempotent GET convention for future read RPCs in transport code/docs.
- [x] T010 Run `pnpm gen:proto`, format generated code, and ensure generated outputs are committed and deterministic.

## Grounding
- Source plan: [plan/03.rpc-transport](../plan/03.rpc-transport.md).
- Architecture: [ARCHITECTURE.md](../../spec/ARCHITECTURE.md) §2.7 and §3.5.
- Vocabulary: [ubiquitous-language.md](../../spec/ubiquitous-language.md) §0-§1.
- Values: no `spec/values.yaml` entries expected.
- Policy/tech reflection: unary-only transport, idempotent reads, and RPC interceptor rules should be reflected after implementation if policy/tech docs exist.

## Affected files (blast radius)
- `proto/buf.yaml`, `proto/buf.gen.yaml`, `proto/cosimosi/platform/v1/platform.proto`
- generated Go under `apps/api/internal/gen/**`
- generated/shared TS under `packages/api-client/**` or the final pure API-client package path
- `apps/api/go.mod`, `apps/api/go.sum`, `apps/api/cmd/api/main.go`, `apps/api/internal/platform/**`, `apps/api/internal/**/rpc/**` for platform transport only
- `apps/web/**` and `apps/mobile/**` provider/test import points for the shared client
- `scripts/gen.mjs`, `scripts/lib.mjs` if generator detection/output paths change
- `package.json`, `pnpm-workspace.yaml`, relevant app/package `package.json` files
- transport docs/tests
- `spec/plan/00.overview.md`, `spec/jobs/03.rpc-transport.md`

## Verification / DoD
- [x] Every Acceptance Criteria item above is true in the current code.
- [x] `pnpm gen:proto` succeeds and a second run is clean.
- [x] Go tests for the platform RPC handler/interceptors pass.
- [x] TS client tests/probes pass from web and mobile import contexts.
- [x] API still serves `/health`.
- [x] No product domain proto, table, use-case, streaming, websocket, or subscription path is introduced.

## Review
- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

Review notes: local `/code-review` found no additional blockers. Codex review findings were applied: production Docker
build copies `go.sum`, Buf CLI image is pinned, and root `pnpm check` now exercises `@cosimosi/api-client`
typecheck/tests. No findings were rejected.

## After completion - reflect into the SSOT
- [x] Update [plan/03.rpc-transport](../plan/03.rpc-transport.md) to built/as-built details.
- [x] Reflect transport rules into policy/tech docs if present.
- [x] Update [00.overview](../plan/00.overview.md) status for plan/job 03.
- [x] Set this job frontmatter `status: done`.
