---
job: '155'
type: 'refactor'
source: 'code-review/14.full-codebase-runtime,-architecture,-and'
plan: 'none'
status: done
title: 'Worker maintenance cadence and backend tuning ownership'
---

# Job 155: Worker maintenance cadence and backend tuning ownership (refactor)

> Implementation work doc. Source spec: [code-review/14.full-codebase-runtime,-architecture,-and](../code-review/14.full-codebase-runtime,-architecture,-and.md).
> /cosimosi:implement-job 155 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/14.full-codebase-runtime,-architecture,-and)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: review 14 R004 (P2) + R008 (P3). Decision: Questions #2 in the source report —
cleanup cadence and batch size are product build values in `spec/values.yaml`; poll interval
stays a composition-root argument with one named home.

- [x] A1 Terminal-job cleanup runs on its own maintenance cadence (`nextCleanupAt`), not
      before every claim: an idle worker no longer issues a purge per poll tick, and a busy
      worker no longer issues one per completed job.
- [x] A2 Cleanup cadence and batch size are generated from `spec/values.yaml`; a full batch
      permits a bounded drain; cleanup stays fail-open for product claims.
- [x] A3 Tests cover: repeated idle claims within one window (one purge), a backlog (bounded
      drain), cleanup errors (claims proceed), multiple clock advances.
- [x] A4 The duplicated one-second poll literal has a single named home shared by
      `cmd/worker` and `cmd/api`'s dev worker.
- [x] A5 The backend tuning inventory is CLASSIFIED, each value with an owner: product/cache
      behavior that must be identical everywhere → `spec/values.yaml` (withdrawal status
      cache TTL + max entries, AI adapter cache entries); deployment-shaped values → named
      composition-root arguments (HTTP client timeouts); protocol/structural limits → stay in
      code with a comment stating the invariant. Zero unexplained literals remain in the
      inventoried set.
- [x] A6 No behavior change beyond call frequency (same purge semantics); API suites green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 (gen:values) Add `worker.cleanup_interval_s` and `worker.cleanup_batch_size` to
      `spec/values.yaml`; regenerate Go values.
- [x] T002 `apps/api/internal/memory/worker.go` — replace the purge-before-every-claim with a
      `nextCleanupAt` window on `maintenanceQueue` (bounded drain when a batch fills; back
      off until the next window otherwise; fail-open preserved).
- [x] T003 Extend `worker_test.go`: repeated idle/busy claims, backlog drain, cleanup error,
      clock advances.
- [x] T004 [P] Single named home for the poll interval (one exported const or values key,
      consumed by `cmd/worker/main.go:47` and `cmd/api/worker.go:34`).
- [x] T005 Classify + move the inventory: `withdrawal.go:14-17` (TTL 5s, 4096 entries) and
      `ai/hash.go:9` (1024 entries) → values.yaml (gen:values); vendor/admission HTTP
      timeouts (`deepseek/client.go`, `voyage/client.go`, `cmd/api/main.go`) → named
      composition-root arguments; anything left in code gets an invariant comment.
- [x] T006 Reflect into `spec/tech/config-values.md` (the classification, so the next knob
      lands on the right side) and the touched values.yaml key comments.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/values.yaml` (worker/account/ai keys),
  `spec/tech/config-values.md` (ownership classification).

## Affected files (blast radius)

- `apps/api/internal/memory/worker.go` (+ `worker_test.go`) — memory context
- `apps/api/cmd/worker/main.go`, `apps/api/cmd/api/worker.go` — composition roots
- `apps/api/internal/account/withdrawal.go`, `apps/api/internal/ai/hash.go` — cache knobs → values
- `apps/api/internal/ai/anthropic/client.go`, `apps/api/internal/ai/deepseek/client.go`, `apps/api/internal/ai/voyage/client.go`,
  `apps/api/cmd/api/main.go` — timeout arguments
- `spec/values.yaml` (+ regenerated `values_gen.go` / `values.gen.ts`) — (gen:values)
- `spec/tech/config-values.md` (doc reflection)

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — n/a (refactor);
      existing API behavior remains covered by the backend suites
- [x] Codegen / migration / values applied (if any): `pnpm gen` and `pnpm gen:values` applied;
      no migration was needed
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` pass via full
      `pnpm check`
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

Review notes:

- Read-only Codex reviews caught and closed two HTTP ownership gaps: Anthropic's SDK policy is
  preserved while DeepSeek and Voyage receive provider-specific clients from each composition root.
- The final read-only Codex review returned no findings. Focused API tests, `go vet ./...`, and
  `go build ./...` passed before the final root gate.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
      — `spec/tech/config-values.md` now records product, deployment, and structural ownership;
      cache and worker tuning values are generated from `spec/values.yaml`
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — n/a (refactor)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
