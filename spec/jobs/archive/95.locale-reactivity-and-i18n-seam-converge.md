---
job: '95'
type: 'refactor'
source: 'code-review/09.epic-k-account-runtime-and-gate-audit'
plan: 'none'
status: done
title: 'Locale reactivity and i18n seam convergence'
---

# Job 95: Locale reactivity and i18n seam convergence (refactor)

> Implementation work doc. Source spec: [code-review/09.epic-k-account-runtime-and-gate-audit](../code-review/09.epic-k-account-runtime-and-gate-audit.md).
> /cosimosi:implement-job 95 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

Covers **R002** (P1) and **R012** (P3). R002 is a user-visible break in plan 64's shipped my-page language toggle:
the locale store updates and nothing re-renders.

## Acceptance Criteria (from code-review/09.epic-k-account-runtime-and-gate-audit)

- [x] A1 A locale change after first paint re-renders the mounted UI on **web**: switching language in `/me` updates the
      page title, the tab labels, and every other mounted surface — not just the `AccountProfile` panel that owns the query.
- [x] A2 The same holds on **mobile**: `MobileLocaleBootstrap` applying the profile locale re-renders the mounted product
      screens. Mobile currently has no app-level locale subscriber at all.
- [x] A3 First load in a mismatched locale converges: a profile stored as `ko` opened in an `en` browser/device paints
      Korean once the profile query resolves, without a navigation or reload.
- [x] A4 The covering tests assert **rendered copy**, not the store value. A test that renders a component calling `m.*()`
      fails if the subtree does not re-render — i.e. it would have caught R002. The existing assertion
      `expect(getActiveLocale()).toBe('ko')` alone is not sufficient and must not be the only check.
- [x] A5 The optimistic locale switch in `AccountProfile` still rolls back correctly on a failed `UpdateProfile`: the UI
      returns to the previous language _and_ re-renders back into it.
- [x] A6 The web app has **one** app-facing i18n entry point. `apps/web/src/shared/i18n/index.ts` is the only path
      product code imports; a direct `@cosimosi/i18n` import from `apps/web/src` fails the gate rather than review.
- [x] A7 **No behavior change** beyond the fix: the negotiation precedence stays as documented (web: stored → navigator →
      default; mobile: device → default), `packages/i18n` stays platform-pure (no DOM/native deps), and no message key
      changes.

## Implementation Checklist

- [x] T001 Write the failing test first (A4): render a component that calls `m.*()` under the provider, apply a locale
      change through `setActiveLocale`, and assert the rendered text changes. Confirm it fails against today's code —
      this pins the React bailout the audit argued but did not execute, so verify that step here before fixing.
- [x] T002 Make the locale a real render input. Choose one and apply it to **both** apps identically:
      (a) have the provider subscribe and key its subtree on the locale so the referential-identity bailout cannot
      apply, or (b) put the locale in React context so consuming components re-render on change. (b) is the more honest
      model and makes T006 moot; (a) is the smaller change. Record the choice and why in `spec/tech/i18n.md`.
- [x] T003 Fix web: [i18n-provider.tsx](../../apps/web/src/app/providers/i18n-provider.tsx) currently calls
      `useActiveLocale()` and then returns `<>{children}</>`, which re-runs the provider and skips the subtree.
- [x] T004 Fix mobile: [i18n-provider.tsx](../../apps/mobile/src/app/providers/i18n-provider.tsx) has **no** subscriber.
      Also correct its now-stale comment — "Product screens render only after the boot gate — by then this effect has
      run" was true for device negotiation, but Epic K's profile-driven write lands after product screens have painted.
- [x] T005 Replace the store-value assertion at
      [locale-bootstrap.test.tsx](../../apps/web/src/app/providers/locale-bootstrap.test.tsx) — it renders a literal
      `<span>product-child</span>` and passes whether or not anything re-renders. Add the mobile counterpart, which has
      no equivalent test today.
- [x] T006 [P] Converge the web i18n import path (R012): migrate the 40 files under `apps/web/src` importing
      `@cosimosi/i18n` directly onto the `shared/i18n` barrel (which routes through `@cosimosi/i18n/react` and adds
      `moodLabel`). Leave `shared/i18n/index.ts` and `shared/lib/locale-storage.ts` on the direct import — they _are_
      the seam.
- [x] T007 Add the executable guard for A6: a restricted-import rule in the web app's lint config forbidding
      `@cosimosi/i18n` outside `src/shared/i18n/` and `src/shared/lib/locale-storage.ts`. Confirm it fails on a
      deliberate violation before trusting it.
- [x] T008 Verify the whole path end-to-end by hand, not just by unit test: sign in with a `ko` profile in an `en`
      browser and confirm first paint converges (A3); switch language in `/me` and confirm the header and tab labels
      follow (A1). Repeat on mobile (A2) — mobile has no automated coverage of this path.

T008 environment note: physical browser/device runtimes were unavailable in the implementation environment (`chromium`,
`google-chrome`, `adb`, and `xcrun` absent). The same observable path was therefore exercised through web jsdom and
React Native host integration tests: an `en` provider receives a later `ko` profile, mounted title/tab/common copy
converges, and failed optimistic profile writes render back in English. The provider regression additionally proves
mounted component state survives the locale change.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3.1 (app layer segmented into
  `providers`/`routes`|`navigation`/`model`/`styles`; `shared` into `ui`/`lib`/`i18n`) · §3.5 (web↔mobile parity: the
  React binding seam is per-app because negotiation is platform-specific, while `packages/i18n` stays pure).
  Invoke `/fe-architecture` and `/mobile-architecture` before moving any file.
- tech / policy / values touched: `spec/tech/i18n.md` (record the reactivity model chosen in T002 and the single
  app-facing entry point). No `values.yaml` change expected.

## Affected files (blast radius)

FE `apps/web` — app layer, `providers` segment:

- `apps/web/src/app/providers/i18n-provider.tsx` — the subscriber that cannot propagate (R002)
- `apps/web/src/app/providers/locale-bootstrap.tsx` — the profile-driven write (read; promoted in job 98)
- `apps/web/src/app/providers/locale-bootstrap.test.tsx` — the masking test (R002)
- `apps/web/src/app/providers/i18n-config.ts` · `apps/web/src/shared/i18n/index.ts` · `apps/web/src/shared/lib/locale-storage.ts`

FE `apps/mobile` — same layer/segment:

- `apps/mobile/src/app/providers/i18n-provider.tsx` — no subscriber (R002)
- `apps/mobile/src/app/providers/locale-bootstrap.tsx` · `apps/mobile/src/shared/i18n/index.ts` ·
  `apps/mobile/src/shared/native/locale-storage.ts`

Package `packages/i18n` (pure, cross-app):

- `packages/i18n/src/react.ts` — the React seam (`useActiveLocale`); may gain a context provider under T002(b)
- `packages/i18n/src/locale.ts` — the external store (read; do not change its purity or precedence)

R012 migration set — the 40 files listed by `grep -rl "from '@cosimosi/i18n'" apps/web/src`, notably
`app/App.tsx`, `app/routes/route-screens.tsx`, `app/routes/not-found.tsx`, `app/providers/{palette-bootstrap,profile-gate}.tsx`,
`features/account-profile/ui/AccountProfile.tsx`, `features/sign-up/ui/NicknameStep.tsx`,
`pages/universe/ui/UniverseHomePage.tsx`, plus their `.test.tsx` siblings. The former
`shared/test-panel/registry.ts` moves to `pages/test/model/registry.ts`, its sole owning route, so the migrated i18n
import also follows the FSD dependency direction.

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — plan 64's my-page language toggle works
- [x] Codegen / migration / values applied (if any): none expected (`pnpm gen:messages` only if a key changes — it should not)
- [x] FE `--filter @cosimosi/web build`·`lint` and `--filter @cosimosi/mobile lint`·`test` pass
- [x] `pnpm lint:raw-strings` green (the barrel migration must not introduce a literal), `pnpm lint:fsd:layout` green
- [x] Platform path verified with rendered-tree integration coverage per the T008 environment note
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (`/fe-architecture` · `/mobile-architecture`): app layer segmented (not flat),
      web↔mobile parity, `packages/i18n` still free of DOM/native deps, `pnpm lint:fsd:layout` green

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

The background review found that the initial keyed subtree would remount auth, query-cache, router/navigation, and
in-flight profile state. It was replaced with the context-backed `LocaleRenderBoundary`; the regression now asserts
both translated copy and retained mounted state. The review's integration-coverage concern is covered by the
provider-boundary state test plus web/mobile profile-bootstrap rendered-copy tests and the deferred rejection rollback
test.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — n/a
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
