---
job: "02"
type: new
source: plan/02.quality-gates
plan: plan/02.quality-gates
status: done
title: quality gates
---

# Job 02: quality gates  (new)

> Implementation work doc. Source spec: [plan/02.quality-gates](../plan/02.quality-gates.md).
> /cosimosi:implement-job 02 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/02.quality-gates)
- [x] A1 The root package exposes documented `lint`, `typecheck`, `test`, and `check` scripts; `pnpm check` runs every active non-deployment gate.
- [x] A2 CI runs the same logical gates on pull requests and direct pushes to the active branches, with no references to `apps/*-mvp`.
- [x] A3 Web gates include TypeScript, oxlint, Vitest, and FSD boundary checks; a deliberate forbidden layer import fails.
- [x] A4 Mobile gates include TypeScript and ESLint and can run on a host machine without launching an emulator.
- [x] A5 Backend gates include `gofmt` cleanliness, `go vet ./...`, `go test ./...`, and `go build ./...`; the documented Docker fallback runs the same checks.
- [x] A6 Generated-code freshness is enforced: after `pnpm gen`, changed generated outputs fail the full gate.
- [x] A7 Ubiquitous-language lint fails for domain/visual vocabulary drift and for a modeled memory-to-memory edge.
- [x] A8 The checks do not import, install, lint, typecheck, test, or build `apps/api-mvp` or `apps/web-mvp`.

## Implementation Checklist
- [x] T001 Audit current scripts/configs (`package.json`, app package scripts, CI, `scripts/gen*.mjs`) and record which active roots already have lint/build/test/typecheck commands.
- [x] T002 Add root scripts for `lint`, `typecheck`, `test`, `check`, and any lower-level helpers needed to keep app commands composable.
- [x] T003 [P] Add web unit-test and FSD boundary gates: Vitest config, Steiger/ESLint boundary config, package scripts, and a small negative fixture/test or documented boundary probe.
- [x] T004 [P] Add mobile host-only gates: TypeScript check, existing RN ESLint script integration, and a no-emulator test/smoke command placeholder if the runner is introduced here.
- [x] T005 [P] Add backend quality helpers for `gofmt` cleanliness, `go vet ./...`, `go test ./...`, and `go build ./...`, including the documented Docker fallback path.
- [x] T006 [P] Keep the blog gate explicit and limited to the active Astro package, not archived/MVP paths.
- [x] T007 (gen) Add a generated-code freshness check that runs `pnpm gen` and fails on changed generated outputs.
- [x] T008 Add the ubiquitous-language lint script/config that reads `spec/ubiquitous-language.md`, scans active source roots only, and catches visual/domain vocabulary drift.
- [x] T009 Update `.github/workflows/ci.yml` to run the same logical gates in split jobs and keep `apps/*-mvp` excluded.
- [x] T010 Document local gate commands and any expected Docker fallback in root/app docs.
- [x] T011 Run the full new gate once, fix only in-scope breakage, and record any existing out-of-scope failures in the job outcome notes during implementation.

## Grounding
- Source plan: [plan/02.quality-gates](../plan/02.quality-gates.md).
- Architecture: [ARCHITECTURE.md](../../spec/ARCHITECTURE.md) §1, §2.9, §3.1, §5.
- Vocabulary source: [ubiquitous-language.md](../../spec/ubiquitous-language.md) §0 and §4.
- Values: no `spec/values.yaml` entries expected.
- Policy/tech reflection: platform quality rules should be reflected after implementation if the policy/tech tree exists.

## Affected files (blast radius)
- `package.json`
- `apps/web/package.json`, `apps/web/tsconfig*.json`, `apps/web/.oxlintrc.json`, possible `apps/web/vitest.config.*`, `apps/web/steiger.config.*`, `apps/web/eslint.config.*`
- `apps/mobile/package.json`, `apps/mobile/tsconfig.json`, `apps/mobile/.eslintrc.js`
- `apps/blog/package.json`, `apps/blog/tsconfig.json`
- `apps/api/**` quality helper touchpoints only; no product/domain code
- `scripts/**` for check, generated-freshness, and vocabulary-lint helpers
- `.github/workflows/ci.yml`
- root/app docs as needed
- `spec/plan/00.overview.md`, `spec/jobs/02.quality-gates.md`

## Verification / DoD
- [x] Every Acceptance Criteria item above is true in the current code.
- [x] `pnpm check` runs the full active non-deployment gate.
- [x] CI workflow contains no active `apps/*-mvp` target.
- [x] `pnpm gen` freshness behavior fails on a deliberately stale generated output or is covered by a testable script.
- [x] Ubiquitous-language lint catches at least one deliberate visual/domain drift fixture or documented probe.
- [x] FE web lint/build/typecheck/test gates, mobile host gates, blog gate, and API vet/build/test/gofmt gates pass or have documented pre-existing blockers.

## Review
- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

Review notes:
- Applied: made `scripts/go-check.mjs` avoid host `sh`, narrowed generated freshness pathspecs, and made the web
  boundary probe use unique temporary slice names so it cannot remove real source directories.
- Rejected: Codex review reported a Steiger `EMFILE` failure inside its read-only sandbox; normal shell verification
  passed `pnpm --filter @cosimosi/web lint:fsd`, `pnpm --filter @cosimosi/web lint`, and `pnpm check`.
- Scope note: this worktree already contains unrelated Phase 1 plan/job docs and `.codex` prompt edits; they were not
  reverted or included as implementation claims for this job.

## After completion - reflect into the SSOT
- [x] Update [plan/02.quality-gates](../plan/02.quality-gates.md) to built/as-built details.
- [x] Update affected policy/tech docs for quality and vocabulary lint rules if those docs exist.
- [x] Update [00.overview](../plan/00.overview.md) status for plan/job 02.
- [x] Set this job frontmatter `status: done`.
