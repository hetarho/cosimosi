---
job: "39"
type: change
source: changes/23.landing-neuroscience-verification
plan: plan/15.landing-revamp
status: done
title: landing neuroscience verification
---

# Job 39: landing neuroscience verification  (change)

> 구현 작업 문서. 출처 스펙: [changes/23.landing-neuroscience-verification](../changes/23.landing-neuroscience-verification.md).
> `/implement-job 39`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/23.landing-neuroscience-verification에서)
- [x] A1 VI 망각 카드가 거리 드리프트 + 반경 밝기로 설명·시연(연결·회상·감정이 거리를 늦춰 가깝고 밝게, 고립 별은 멀어져 어두워짐); 바닥 `A_MIN=0.05`이고 `FLOOR=0.12`·`λ_eff`·relevance 표현 없음.
- [x] A2 VIII 야간 공고화 카드가 change 20 재설계(반경 스코프 ≤60·시간링크 약화/의미링크 강화·spread·`abstraction_stage` 요지화·마지막 연결 보호·re-KNN) 반영, "약한 선은 빛만 낮춤·원본 불변" 유지.
- [x] A3 V 재공고화 카드가 고유 형태(다축 시드) + 추상화 단계 형태 단순화 + 높은 단계 AI 내용 변형(원본 불변·변천사 append)을 보여줌.
- [x] A4 IV 연합 카드에 감정 유사도가 링크 가중치를 미세조정(후보 게이트 `cos≥0.75` 불변 명시)한다는 사실이 있고, 간격 효과 주장 문구 없음.
- [x] A5 랜딩 어딘가에서 "연결 많은·자주 함께 떠올린 별은 가까이 머문다(거리=강함)"가 명시됨.
- [x] A6 concept/engram/fragmentation/present/resonance 막의 수치·문구가 현재 policy와 일치(틀린 것만 정정, 맞는 것 불변).
- [x] A7 랜딩 9막 IA·라우팅·마케팅 구조·CTA 그대로(검증·정정·소폭 보강 외 재설계 없음).
- [x] A8 백엔드·proto·RPC·sqlc 변경 없이 FE만으로 구현, build·lint·typecheck·steiger 통과; 랜딩 스크롤·테마·기존 인터랙션 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
- [x] T001 [SilentEngramCard.tsx] 망각(VI)을 거리 드리프트 + 반경 밝기로 재작성 — 카피 "어두워질 뿐"→"멀어지며 어두워진다", 미니 viz의 밝기 바닥을 `FLOOR=0.12`→정전 `A_MIN`(`VALUES.decay.aMin` 인용)로 통일, `λ_eff`·relevance 그림 제거, "연결·회상·감정이 거리를 늦춰 가깝고 밝게" 표현 추가. "삭제 없음·재점화·불변 원본" 유지. [A1]
- [x] T002 [P] [NightlyConsolidationCard.tsx] 야간(VIII)을 change 20 재설계로 — 반경 스코프 ≤60·시간링크 약화/의미링크 강화·spread·`abstraction_stage` 반경 요지화·마지막 연결 보호(degree≥1)·re-KNN. "약한 선은 빛만 낮춤·원본 불변" 유지. [A2]
- [x] T003 [P] [ReconsolidationCard.tsx] 재공고화(V)에 고유 형태(다축 시드)·추상화 단계 단순화·높은 단계 AI 내용 변형(원본 불변·변천사 append) 반영(시연 형태 변형을 "단순 시드 흔들림"→"고유 형태+단계 추상화"로). [A3]
- [x] T004 [P] [HebbianCard.tsx · TimeWindowCard.tsx] 연합(IV)에 감정 유사도 링크 미세조정(게이트 `cos≥0.75` 불변 명시) 보강, 간격 효과 문구 있으면 제거. [A4]
- [x] T005 연결→거리 보강 — 적절한 막(IV 또는 VI, 혹은 1개 소형 카드)에 "연결 많은·자주 함께 떠올린 별은 가까이 머문다(거리=강함)" 명시. 밝기·위치가 하나의 변수(자기-거리)로 통합됨을 한 문장으로. [A5]
- [x] T006 [P] 나머지 막 검증 — concept/engram/fragmentation/present/resonance 카피·수치를 policy와 대조, 틀린 것만 정정(ambient τ·흥분성 게인 `g=1+0.3·각성`·끌림=의미+0.25·흥분성·소프트 억제 ×0.5). 맞으면 불변. [A6]
- [x] T007 [LandingPage.tsx] 막 헤더/요약 카피(망각·야간·재공고화·연합 act)를 위 카드와 정합되게 갱신. 9막 순서·라우팅·CTA 불변. [A7]
- [x] T008 검증 — build·lint·typecheck·steiger, 랜딩 스크롤·테마·인터랙션 회귀 없음. 백엔드/proto 무변경 확인. [A8]

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙
- 건드리는 tech/policy/values: 검증 기준 [policy/domain/star.md](../policy/domain/star.md)(밝기=자기-거리·요지화) · [synapse.md](../policy/domain/synapse.md)(감정 유사도 링크) · [memory.md](../policy/domain/memory.md)(재공고화 AI 변형). 카드 밝기 바닥은 `spec/values.yaml` `decay.a_min` 인용(새 값 신설 없음). [tech/neuroscience.md](../tech/neuroscience.md) §4.2 λ_eff 서술은 구 모델 잔존 — 별도 정리 대상(이 job 범위 밖이나 검증 시 참고).

## 영향 파일 (blast radius)
- `frontend/src/pages/landing/ui/section/SilentEngramCard.tsx` (망각 VI — 거리 드리프트·반경 밝기·바닥)
- `frontend/src/pages/landing/ui/section/NightlyConsolidationCard.tsx` (야간 VIII — change 20)
- `frontend/src/pages/landing/ui/section/ReconsolidationCard.tsx` (재공고화 V — 고유 형태·추상화·내용 변형)
- `frontend/src/pages/landing/ui/section/HebbianCard.tsx`, `TimeWindowCard.tsx` (연합 IV — 감정 유사도·간격효과 제거)
- `frontend/src/pages/landing/ui/section/{AmbientMoodCard,PresentSelfCard,ConceptSection,EngramCard,FragmentationCard,ResonanceSection}.tsx` (검증·소폭 정정)
- `frontend/src/pages/landing/ui/LandingPage.tsx` (막 카피 정합)

## 검증 / DoD
- [x] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [x] (type=change면) 기존 plan 수용 기준 회귀 없음
- [x] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter ./frontend build`·`lint` / BE `go vet ./... && go build ./...`(Docker) 통과(해당 시)
- [x] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음

## 리뷰
- [x] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [x] `plan/`을 새 현실로 갱신 · 영향받은 `policy/**`·`tech/**` · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [x] (type=change면) `changes/` 출처 문서를 `changes/archive/`로 이동
- [x] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
