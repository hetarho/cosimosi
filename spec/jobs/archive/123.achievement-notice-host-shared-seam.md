---
job: '123'
type: 'refactor'
source: 'code-review/10.jobs-94-115-achievement,-decorate-and-sh'
plan: 'none'
status: done
title: 'Achievement notice host shared seam'
---

# Job 123: Achievement notice host shared seam (refactor)

> Implementation work doc. Source spec: [code-review/10.jobs-94-115-achievement,-decorate-and-sh](../../code-review/10.jobs-94-115-achievement,-decorate-and-sh.md).
> /cosimosi:implement-job 123 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

Covers **R002** and **R003** (both P2) — one seam, seen from two sides. Both are cases where a comment asserts a
property the code does not have, which is review 10's leading cross-cutting theme.

`AchievementNoticeHost.tsx` is **byte-identical** in both apps (56 lines, `cmp` confirms it), and platform-pure: it
returns `null`, touches no DOM and no RN element, and its only app-relative import is `shared/i18n`. What is duplicated
is 40 lines of real logic — the `pushNotice` shape, the `formatNotice` resolver, and the `lastUserId` ref effect whose
comment explains a development-mode double-effect hazard that now has to be maintained twice.
`packages/achievement/src/react/unlock-notice.ts:8-11` already names this exact risk and claims both hosts are "a short
shell over this". The extraction stopped one layer short.

Meanwhile `SESSION_SCOPED_TOAST_OWNERS` (`packages/ui/src/toast-queue.ts:60-65`) declares itself the SSOT for the
session-scoped owner tag — "so adding a session-scoped toast is one edit in one place" — and has **zero consumers**.
The tag it should own is a private `NOTICE_OWNER = 'achievement-notice'` literal, duplicated in both hosts. Three
copies of one magic string with a comment asserting there is one.

**Direction (review 10 offers two; this job takes (b)).** Give the constant the consumer its comment describes, rather
than deleting it. The reason is that the real session boundary already exists and is already the right place: both apps
mount `SessionScopeBoundary onScopeChange={resetScope}` inside `WebClientCacheProvider` / the mobile equivalent, which
sits **inside** `WebToastProvider` — so `useToastQueue()` is reachable there. Moving the drop into that boundary makes
the constant load-bearing, removes the `lastUserId` ref effect and its double-effect hazard from both hosts, and puts
the scope-change drop beside every other per-user reset instead of in a feature.

## Acceptance Criteria (from code-review/10.jobs-94-115-achievement,-decorate-and-sh)

- [x] A1 The notice host's body lives once, in `packages/achievement`. Each app's `AchievementNoticeHost` is a genuine
      shell that resolves only what genuinely differs — the copy (`m.achievement_notice`) and the toast queue — and
      contains no duplicated logic, no diff, no cap, and no scope-change effect.
- [x] A2 A `cmp` of the two apps' `AchievementNoticeHost.tsx` files no longer reports two identical files with a real
      body. If the two shells legitimately end up identical because the shell is a barrel-thin wrapper, that is
      acceptable only if the file holds no logic — the R4 widening in **job 124** is what will enforce the line, so the
      two jobs must agree on where it falls.
- [x] A3 `SESSION_SCOPED_TOAST_OWNERS` has the consumer its comment describes: the session boundary iterates it and
      drops each owner's pending entries on a scope change. Its comment is true — adding a second session-scoped owner
      is one edit in one place, and that edit is enough.
- [x] A4 The owner tag is declared once. Neither app declares a `NOTICE_OWNER` literal; both take the tag from
      `packages/ui`, and the pushing surface and the dropping surface cannot disagree about it.
- [x] A5 **The scope-change guarantee is preserved, not weakened.** A notice queued for one signed-in user still cannot
      surface for whoever signs in next, and the queue still never learns a user id. The development-mode
      double-effect hazard the old `lastUserId` ref effect defended against must still be defended against — the new
      home is a scope-change callback rather than an effect keyed on identity, so the reasoning changes and the comment
      must change with it.
- [x] A6 **No behavior change.** The first read resolution of a session still enqueues nothing, the per-resolution cap
      (`VALUES.achievement.unlockNoticeMax`) still drops the remainder rather than queueing it, the dwell is still
      `VALUES.achievement.claimToastMs`, and the notice is still one line with no reward figure, no button and no
      navigation. Both platforms still mount the host where they mount it today.
- [x] A7 The comments that were ahead of the code are now accurate: `unlock-notice.ts:8-11`'s "both apps' hosts are a
      short shell over this" is true, and `toast-queue.ts:60-65`'s SSOT claim is true.
- [x] A8 `pnpm check` green end to end, including `pnpm lint:fsd:layout` and both apps' lint/typecheck.

## Implementation Checklist

- [x] T001 Decide the promoted hook's exact seam before moving code, and record it in a comment. Review 10's
      _Questions / Tradeoffs_ notes the tension: the host's only genuinely app-local dependency is `shared/i18n`, so
      the shared hook takes `formatNotice` plus the queue's `push`/`dropByOwner` and (if the drop stays in the hook)
      `userId` — four injected params for a ~15-line shell. The alternative it names is a shared host in
      `packages/achievement/react` taking a copy resolver, matching `packages/errors/src/react-toast.tsx`. Read that
      file first and follow whichever shape it establishes, so the two toast-owning packages do not diverge.
- [x] T002 Export the owner tag from [packages/ui/src/toast-queue.ts](../../../packages/ui/src/toast-queue.ts) as a named
      constant (`ACHIEVEMENT_NOTICE_TOAST_OWNER`) and define `SESSION_SCOPED_TOAST_OWNERS` in terms of it, so the
      list and the tag cannot drift. Re-export from both barrels — `packages/ui/src/index.ts:10` and
      `index.native.ts:19` already export the list; the tag joins it in both.
- [x] T003 Move the drop to the session boundary. In `apps/web/src/app/providers/query-provider.tsx`'s `resetScope`
      callback (line ~42) and the mobile sibling (`apps/mobile/src/app/providers/query-provider.tsx`, line ~63), take
      `dropByOwner` from `useToastQueue()` and iterate `SESSION_SCOPED_TOAST_OWNERS`. Verify the provider nesting first:
      web mounts `WebToastProvider` outside `WebClientCacheProvider` (`App.tsx:49-52`), so the hook is available —
      confirm the mobile tree has the same order before writing the code, and if it does not, fix the order or keep the
      drop in the hook and say why in a comment.
- [x] T004 Consider whether the drop belongs in `packages/auth`'s `USER_STATE_RESET_REGISTRY`
      (`packages/auth/src/user-state-reset.ts`) instead of the provider. It is the existing inventory of per-user state
      cleared on every scope transition, which is conceptually exactly this — but its entries are plain
      `(nextScopeKey) => void` functions over module-level stores, and the toast queue is React context. Do not force a
      React-context drop through that registry; if it does not fit, note in the registry's neighborhood that the
      session-scoped toast drop lives at the provider and why, so the next reader looking for "everything cleared on
      scope change" finds both halves.
- [x] T005 Promote the host body into
      [packages/achievement/src/react/unlock-notice.ts](../../../packages/achievement/src/react/unlock-notice.ts) per
      T001's decision. `useAchievementUnlockNotice` already owns the diff, the cap and the first-resolution silence;
      what moves in is the push shape (variant, dwell, owner) — the toast's _composition_, which is platform-pure —
      leaving the queue itself and the copy injected. Update the file's header comment: it currently describes an
      intended end state, and after this job it describes the code.
- [x] T006 Rewrite both `AchievementNoticeHost.tsx` files as shells (`apps/web/src/features/achievement-notice/ui/` and
      the mobile sibling). Delete the `NOTICE_OWNER` literal from both, delete the `lastUserId` ref effect from both
      (T003 owns it now), and keep each file's only app-relative import as `shared/i18n`.
- [x] T007 Keep the mounts untouched: `apps/web/src/app/routes/route-screens.tsx:69` and
      `apps/mobile/src/app/navigation/NavigationRoot.tsx:214`. Review 10 confirmed full parity there; this job must not
      change where the host mounts.
- [x] T008 Tests. `packages/achievement` gets the promoted hook's coverage — first-resolution silence, the cap, the
      push shape — since it is now the only place that logic exists. Add or extend a test proving the scope-change drop
      still happens after T003's move (`apps/web/src/app/App.test.tsx` already exercises `resetUserState`, so the
      scope-change path has a test home). A promotion whose tests stay behind in the apps is how the next reader
      concludes the package is untested.
- [x] T009 Run `pnpm check`. `pnpm lint:fsd:layout` cannot currently see this file pair (that is job 124's finding), so
      **do not** treat a green R4 as evidence A2 holds — verify it with `cmp` directly, and coordinate with job 124: if
      124 lands first, its widened R4 must go green on this job's output.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 — [A5] (the notice is a diff of a read, never a
  push: the recorder returns `error` alone, so nothing can be pushed back through a write) and [I11] (customization and
  presentation cannot touch the meaning layer — a toast carries copy, never a memory field).
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) §3.1 — "if two slices need the same thing it
  promotes **down** a layer — never copied sideways"; §3.5 — a `*.native` sibling exists only where a primitive
  genuinely differs, and this file does not differ at all. §3 app-layer segment rule for T003's provider edit. Invoke
  `/fe-architecture` and `/mobile-architecture`.
- Precedent to follow: `packages/errors/src/react-toast.tsx` — the other package that owns a toast surface and already
  resolved the copy-resolver-vs-shared-host tension (T001).
- Source findings: review 10 **R002** (the constant with no consumer + two duplicated literals) and **R003** (the
  byte-identical host and why R4 cannot see it).
- tech / policy / values touched: none expected. `VALUES.achievement.claimToastMs` and `unlockNoticeMax` are consumed,
  not changed. If `spec/tech/**` documents the toast queue's ownership model, update it to name the session boundary as
  the drop site.

## Affected files (blast radius)

FE shared packages (`packages/` — the promotion target):

- `packages/ui/src/toast-queue.ts` — the owner tag constant + `SESSION_SCOPED_TOAST_OWNERS` defined over it
- `packages/ui/src/index.ts` · `packages/ui/src/index.native.ts` — both barrels export the tag
- `packages/achievement/src/react/unlock-notice.ts` — the promoted body + its corrected header comment
- `packages/achievement/src/react.ts` — barrel, if the exported surface changes

FE app layer (§3 — `app/providers`, the session boundary):

- `apps/web/src/app/providers/query-provider.tsx` — the scope-change drop
- `apps/mobile/src/app/providers/query-provider.tsx` — the same, and confirm provider nesting order
- `packages/auth/src/user-state-reset.ts` — T004: only if the registry is the right home; otherwise a locating comment
- `apps/web/src/app/App.test.tsx` — the scope-change test home

FE `features/achievement-notice` (§3.1 — reduced to shells, both apps):

- `apps/web/src/features/achievement-notice/ui/AchievementNoticeHost.tsx`
- `apps/mobile/src/features/achievement-notice/ui/AchievementNoticeHost.tsx`
- both `features/achievement-notice/index.ts` — identical barrels, structurally required per-app public APIs; review 10
  confirmed R4's exclusion of barrels is correct, so leave them

Untouched on purpose: the two mount sites (`route-screens.tsx:69`, `NavigationRoot.tsx:214`);
`features/achievement-list` (job 122's); `scripts/lint-fsd-layout.mjs` (job 124's).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — n/a, refactor; A6 carries the
      no-behavior-change obligation
- [x] Codegen / migration / values applied: none expected — no proto, no schema, no new tuning scalar
- [x] FE `--filter @cosimosi/web build`·`lint` + `--filter @cosimosi/mobile lint` + `typecheck:ui` ·
      `typecheck:achievement` · `typecheck:auth` pass
- [x] `cmp` on the two `AchievementNoticeHost.tsx` files re-run and its result recorded — the green R4 gate is not
      evidence here (job 124's finding)
- [x] The scope-change drop proved by a test, not by reading: queue a notice, change scope, assert it is gone and that
      the queue never saw a user id
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records` — n/a,
      FE only
- [x] Architecture self-audit passed (`/fe-architecture` + `/mobile-architecture`): promotion went **down** a layer,
      app layer stays segmented, web↔mobile parity, `pnpm lint:fsd:layout` green

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

**Codex was not run**: the local Codex CLI is broken on this WSL box (`codex --version` throws inside the npm shim).

Decisions taken while building:

- **T001 — the `packages/errors` shape, with one addition.** The hook takes the queue structurally (so this domain
  package still does not import the UI kit) plus the owner tag and the copy resolver. The three are held in a ref, so a
  host does not have to memoize them to be correct — which is what lets each shell be two statements instead of two
  `useCallback`s. `ERROR_TOAST_OWNER` lives in `packages/errors`, but the achievement tag cannot: `packages/ui` defines
  `SESSION_SCOPED_TOAST_OWNERS` over it and must not import a domain package, so the tag is declared beside the queue
  and passed in.
- **The pure part was split out rather than tested through a renderer.** `unlockNoticeToasts(previous, next, …)` in
  `unlock-diff.ts` decides WHAT to announce and HOW it reads (diff + cap + composition); the hook only pushes and
  advances the snapshot. `packages/achievement` has no `react-dom` and adding one to render a hook would have been more
  new infra than the seam is worth, and the split is the better factoring anyway.
- **A2 — the two shells are byte-identical, deliberately.** `cmp` confirms it, and the criterion allows it because the
  file holds no logic: `useToastQueue()`, then the hook call. Job 124's widened R4 must treat this pair as compliant.
- **`WebClientCacheProvider` now requires a toast queue above it.** `useToastQueue` fails loud by design, so the three
  web provider tests that mounted it bare are wrapped in `WebToastProvider` — the same nesting `App.tsx` already has.
- **T004 — the drop is NOT in `USER_STATE_RESET_REGISTRY`.** Its entries are `(nextScopeKey) => void` over module-level
  stores; the queue is React context and only a component can read it. A locating comment beside `resetUserState` says
  where the other half lives.
- Two stale comments fixed while passing: both `toast-provider.tsx` files still said the achievement host drops on
  unmount.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality (the notice host is one shared seam; the session-scoped toast drop lives at the
      session boundary) · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — n/a
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
