---
job: '149'
type: 'refactor'
source: 'code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-'
plan: 'none'
status: done
title: 'Config-seam consolidation and breakpoint single-sourcing'
---

# Job 149: Config-seam consolidation and breakpoint single-sourcing (refactor)

> Implementation work doc. Source spec: [code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-](../../code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-.md).
> /cosimosi:implement-job 149 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: R010 (P3, decided: values.yaml), R018 (P3). Decision: Questions #1 in the source
report — awaken/spotlight clusters AND the sheet-gesture thresholds route through
`spec/values.yaml`; the SSOT lines this supersedes are updated in the same job.

- [x] A1 The awaken cluster (`AWAKEN_DURATION_S`, `AWAKEN_PEAK_SIZE`, `AWAKEN_MAX_STEP_S`) and
      the spotlight cluster (`SPOTLIGHT_HOLD_SECONDS`, `SPOTLIGHT_FADE_LAMBDA`,
      `SPOTLIGHT_SCENE_DIM`, `SPOTLIGHT_STAR_LIFT`) are generated from `spec/values.yaml` —
      same numeric values, so no visual change.
- [x] A2 The sheet-gesture thresholds (`DISMISS_PX`, `FLICK_PX`, `FLICK_VELOCITY`, `SETTLE_MS`,
      `TALLEST`, `SHORTEST`, `TAP_PX`) are generated from `spec/values.yaml`; the
      `packages/ui` no-config-dependency stance is updated in the SSOT (generated values are
      platform-pure and allowed).
- [x] A3 Tailwind's `md` breakpoint has ONE source feeding `SHEET_VIEWPORT` and
      `WIDE_MIN_WIDTH_PX`; a chain test (pattern: `backdrop-scale.test.ts`) fails when any
      copy — including `TALLEST` ↔ `max-h-[70dvh]` and `SETTLE_MS` ↔ the base.css leave
      animation — drifts.
- [x] A4 `spec/tech/design-system.md`'s interaction-geometry note and any `packages/ui` config
      stance line reflect the new ruling; `spec/values.yaml` documents the new keys.
- [x] A5 No behavior or visual change (identical numbers end to end); full gate green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 (gen:values) Add keys to `spec/values.yaml`: `rendering.awaken_duration_s`,
      `awaken_peak_size`, `awaken_max_step_s`; `rendering.spotlight_hold_s`,
      `spotlight_fade_lambda`, `spotlight_scene_dim`, `spotlight_star_lift`; a `ui.sheet.*`
      block for the gesture thresholds — values copied verbatim from code.
- [x] T002 `packages/universe-render/src/awaken-flare-pool.ts` — consume the generated
      constants; delete the local consts.
- [x] T003 `packages/universe/src/spotlight-store.ts` — same; keep the exported names if other
      modules import them (re-export the generated values).
- [x] T004 `packages/ui` — add the `@cosimosi/config` dependency (platform-pure, generated);
      `lib/use-sheet-drag.ts`, `lib/use-sheet-resize.ts` consume the generated thresholds.
- [x] T005 Single-source the breakpoint: one exported constant (rem + derived px) feeding
      `lib/sheet-shape.ts`'s `SHEET_VIEWPORT` and `SequenceGuide.tsx`'s `WIDE_MIN_WIDTH_PX`;
      the `md:` utility classes stay but are named in the chain test.
- [x] T006 Add the drift chain test: breakpoint copies, `TALLEST` ↔ `sheet.tsx`'s
      `max-h-[70dvh]`, `SETTLE_MS` ↔ `base.css` leave-animation duration.
- [x] T007 Update SSOT: `spec/tech/design-system.md:80` (interaction geometry now flows
      through values.yaml), `spec/tech/rendering.md` if it names the moved constants,
      key comments in `spec/values.yaml`.
- [x] T008 `pnpm gen:values` + full gate; verify zero visual diff (numbers identical).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/values.yaml` (new rendering + ui.sheet keys),
  `spec/tech/design-system.md`, `spec/tech/rendering.md`, `spec/ARCHITECTURE.md` §4 if it
  names the packages/ui exception.

## Affected files (blast radius)

- `spec/values.yaml` (+ generated `packages/config/src/values.gen.ts`) — (gen:values)
- `packages/universe-render/src/awaken-flare-pool.ts` (+ test expectations)
- `packages/universe/src/spotlight-store.ts` (+ dependents reading the exported constants)
- `packages/ui/package.json` (config dep), `packages/ui/src/lib/{sheet-shape,use-sheet-drag,use-sheet-resize}.ts`
- `packages/ui/src/primitives/sheet.tsx`, `packages/ui/src/base.css` (chain-test anchors)
- `apps/web/src/widgets/sequence-guide/ui/SequenceGuide.tsx`
- New chain test in `packages/ui` (breakpoint/height/timing drift)
- `spec/tech/design-system.md`, `spec/tech/rendering.md` (doc reflection)

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` applied (4 P3 findings fixed, 0 rejected, 0 unresolved) · `/codex:review --background` completed

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` (not applicable: refactor)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
