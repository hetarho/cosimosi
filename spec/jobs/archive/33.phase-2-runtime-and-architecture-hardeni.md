---
job: "33"
type: refactor
source: code-review/03.phase-2-runtime-and-architecture-audit
plan: none
status: done
title: Phase 2 runtime and architecture hardening
---

# Job 33: Phase 2 runtime and architecture hardening  (refactor)

> Implementation work doc. Source spec: [code-review/03.phase-2-runtime-and-architecture-audit](../code-review/03.phase-2-runtime-and-architecture-audit.md).
> /cosimosi:implement-job 33 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.
>
> Covers all eight findings of review 03 (R001–R008, all verified VALID against current code), grouped into four
> workstreams. Refactors must not change behavior beyond each finding's stated fix — "no regression" is an acceptance
> item. Decisions taken up front: **R002** is resolved by the R001 fix (feeding id-keyed `previousPosition` into the
> graph gives the inline/mobile branch the same continuity as the worker branch — no mobile-only snap, no separate
> parity exception). **R003** uses a generated mobile dev-user config sourced from the root `.env`. **R005** pins Air
> only (no CI dev-image build — the pin removes the drift risk).

## Acceptance Criteria (from code-review/03.phase-2-runtime-and-architecture-audit)
<!-- Testable end-states derived from each finding's Recommendation / Why-it-matters. Verify each in running code. -->
- [x] A1 (R001) The universe sim bridge carries previous coordinates by stable `forceSimNodeKey(kind, id)`, not by array
      slot. A `GetUniverse` refetch that inserts or reorders a neuron (a new random id that sorts before an existing id
      under `ORDER BY n.id`) keeps every surviving node's coordinate bound to its own id — no star / cell-star /
      filament / click-target is shown at another node's coordinate during the bridge-swap window. A regression test
      exercises insertion/reorder.
- [x] A2 (R002) The web (worker) and mobile (inline) bridges use the same id-keyed carry-over seam; on refetch both
      preserve visual continuity for surviving nodes (mobile no longer snaps/reseeds to a fresh sim). A test pins both
      branch behaviors.
- [x] A3 (R003) The mobile dev-user id is single-sourced from the root `.env` `COSIMOSI_DEV_USER_ID` (default
      `dev-user`) via a generated config — no hand-synced literal in `runtime-config.ts`. Changing the id in `.env`
      propagates to web + api + mobile without editing source. The production value stays `undefined`.
- [x] A4 (R004) A **CI** build with `VITE_DEV_USER_ID` set exits non-zero at build time (`command === 'build'` &&
      `process.env.CI`); a local build/dev server is unaffected so the dev keeps the value in `.env` for `pnpm dev` and
      the pre-commit build still passes. The runtime assert in `auth-provider.tsx` stays as universal defense in depth.
      `.env.example` / README match the enforced behavior.
- [x] A5 (R005) `apps/api/Dockerfile.dev` pins Air to an explicit version (no `@latest`); `pnpm dev:api` still builds
      and hot-reloads.
- [x] A6 (R006) The web and mobile boundary probes each prove, in addition to the existing `entities → pages` case:
      (a) an allowed `entities → entities/@x` import passes, (b) a forbidden direct `entities → entities/<other-slice>/model`
      import fails through `boundaries/dependencies`, and (c) an `@x` file may reach only its own owner slice.
- [x] A7 (R007) Plan 24's normative scope / design / AC and the archived job-29 wording describe the filament as a
      "batched camera-billboarded ribbon fat-line"; `Line2` survives only in the rejected / as-built decision note.
- [x] A8 (R008) The overview / plan-24 status and job-29 verification notes distinguish "mobile source-gate wired"
      from "on-device render verified"; no unqualified "renders on mobile" claim remains. Aligns with
      `tech/rendering.md`'s status.
- [x] A9 No behavior change beyond the stated fixes; the full merge gate (`pnpm check`) is green.

## Implementation Checklist
<!-- Top to bottom. [P] = parallel (different files, no dependency). Flags: (gen) touches generated output. -->

### WS-A — Graph identity & refetch stability (R001 / R002)
- [x] T001 Add id-keyed carry helpers to `@cosimosi/force-sim` (new `packages/force-sim/src/carry.ts`, exported from
      `index.ts`): `carryPreviousPositions(graph, previousBuffer, previousIndex)` → returns the graph with each
      surviving neuron's `previousPosition` and each surviving episodic memory's `seedHint` set from the previous
      buffer looked up by `forceSimNodeKey(kind, id)` (removed ids dropped, genuinely-new ids left unseeded);
      `remapCoordinateBuffer(nextIndex, previousBuffer, previousIndex)` → a `Float32Array` sized to `nextIndex` that
      copies each surviving id's 3 floats from its previous offset to its new offset (new slots left at 0). Both key by
      id, never by array position.
- [x] T002 Rewrite `createWorkerSimBridge` (`packages/universe/src/sim-bridge.ts:45-135`): track
      `previousIndex: ForceSimNodeIndex | null` (via `createForceSimNodeIndex`) beside the buffer instead of the
      `displayedNeurons` / `displayedMemories` slot counts. Replace the slot-based `subarray` carry-over
      (`sim-bridge.ts:86-97`) with `remapCoordinateBuffer` for the on-screen swap-window buffer, and post the
      `carryPreviousPositions`-enriched graph to the worker `init`. Preserve the existing no-flicker semantics (first
      load stays `null`; a broken sim reads as empty).
- [x] T003 Update `createInlineSimBridge` (`packages/universe/src/sim-bridge.ts:139-158`) to track `previousIndex` and
      build the same `carryPreviousPositions`-enriched graph before `createForceSimulation`, so the inline/mobile
      branch seeds survivors near their prior positions — the R002 parity fix (no mobile-only snap).
- [x] T004 [P] Tests: `packages/force-sim/src/carry.test.ts` (survivor keeps coord by id; removed id dropped; new id
      absent from `previousPosition`; a new id sorting *before* an existing id does not steal the existing node's
      coordinate). Extend `packages/universe` sim-bridge coverage: a worker refetch with inserted/reordered ids keeps
      each id↔coordinate mapping; an inline refetch seeds survivors from prior coordinates (parity with worker).

### WS-B — Dev auth & production-env guard (R003 / R004)
- [x] T005 Add `scripts/gen-mobile-env.mjs`: read the root `.env` `COSIMOSI_DEV_USER_ID` (fallback: `.env.example`,
      then literal `dev-user`) and write `apps/mobile/src/shared/config/dev-user.gen.ts` exporting
      `MOBILE_DEV_USER_ID`. Commit the file with the `dev-user` default so CI typecheck/build has it. Wire a refresh at
      mobile dev start (a `predev:mobile` / mobile `prestart` hook). Do **not** add it to `pnpm gen` /
      `check-generated.mjs` — its source is local `.env`, not a tracked spec, so freshness must not be gate-enforced.
      (gen)
- [x] T006 Rewrite `apps/mobile/src/shared/config/runtime-config.ts:44-45` so `mobileDevUserId` derives from
      `MOBILE_DEV_USER_ID` (still `undefined` when `NODE_ENV === 'production'`); replace the "keep in sync BY HAND"
      comment with the single-source note. Update `.env.example:40-46` and `README.md:44-48` to state mobile now reads
      the same `COSIMOSI_DEV_USER_ID` (no separate hand-synced literal).
- [x] T007 Add a build-time guard to `apps/web/vite.config.ts`: `loadEnv(mode, envDir, 'VITE_')` and throw when
      `VITE_DEV_USER_ID` is set (`.env` or shell) and `command === 'build'` && `process.env.CI` (keyed on the build
      command + CI, not the `--mode` name — see As-built notes), so a CI/deploy build fails fast while a local build /
      dev server is never blocked. Keep the runtime assert in `apps/web/src/app/providers/auth-provider.tsx:29-35` as
      defense in depth. Confirmed: `CI=1 VITE_DEV_USER_ID=probe … vite build` exits non-zero; a local build passes.

### WS-C — Dev infrastructure reproducibility (R005)
- [x] T008 Pin Air in `apps/api/Dockerfile.dev:7` to an explicit released version
      (`go install github.com/air-verse/air@vX.Y.Z` — resolve the current tag at build time). Confirm the dev image
      builds and `pnpm dev:api` still hot-reloads via `.air.toml`.

### WS-D — Plan 24 boundary probes & SSOT (R006 / R007 / R008)
- [x] T009 [P] Extend `scripts/probe-web-boundary.mjs` and `scripts/probe-mobile-boundary.mjs` with three plan-24 cases
      (keep the existing `entities → pages` case): an allowed `entities → entities/<slice>/@x/*` import that must pass;
      a forbidden direct `entities → entities/<other-slice>/model/*` import that must fail through
      `boundaries/dependencies`; and an `@x` file importing another slice's internals that must fail (proving `@x`
      reaches only its owner slice).
- [x] T010 [P] Rewrite the `Line2` normative wording in `spec/plan/24.star-neuron-rendering.md` (scope ~L50, design
      table ~L124, `VisualBodySource` narrative ~L135, `§entities/filament` ~L169-176, AC 9 ~L249-250) to "batched
      camera-billboarded ribbon fat-line"; keep `Line2` only in the as-built / rejected decision note (~L209-214).
      Mirror the same wording fix in `spec/jobs/archive/29.star-neuron-rendering.md`.
- [x] T011 [P] Qualify the mobile render status: `spec/plan/00.overview.md:305` and job-29's render acceptance /
      verification notes must distinguish "mobile source-gate wired" from "on-device render verified" (with the
      `pnpm ios` verification still pending), matching `spec/tech/rendering.md:6-9`.

## As-built notes
- **Review (Codex `gpt-5.5` + an adversarial review workflow, both on the staged job-33 diff only — the working tree
  also held unrelated in-flight jobs 31/32).** Findings applied:
  - The Vite guard first used `mode !== 'development'`; both reviewers flagged that a `--mode` build could slip a
    dev-auth bundle through (false negative) and a non-`development` dev server would be wrongly blocked (false
    positive). **Fix:** key the guard on `command === 'build'`. That surfaced a second issue during commit: the
    pre-commit hook runs `build:web` locally with the dev `.env` (which sets `VITE_DEV_USER_ID` for `pnpm dev`), so an
    unconditional build throw blocked every local commit. **Resolved** by also gating on `process.env.CI` — the guard
    fires on the CI/deploy build (R004's actual threat: a shipped bundle carrying the bypass) but never on a local
    build/dev server; the runtime assert remains the universal backstop. Verified: `CI=1 … vite build` throws, local
    build passes.
  - The boundary probes matched injected fixtures by bare basename, which a same-named real file with an unrelated lint
    error could spoof. **Fix:** match by a path fragment that includes the unique `mkdtemp` slice dir (collision-proof).
  - Plan 24's intro (scope + Purpose) still called plan 23's placeholder edges "`Line2` edges"; plan 23's as-built
    actually uses plain 1px `LineSegments` and defers `Line2` to plan 24. **Fix:** reworded to "line-segment edges".
    Plan 24 AC 12 (a mobile-render overclaim, same family as R008) was qualified to "source-gate verified · on-device
    pending". `Line2` now survives only in plan 24's as-built rejected note and the ARCHITECTURE §3.3 citations.
  - Codex cleared the WS-A carry logic (id-keyed remap, survivor-only seeding, worker transferable ping-pong), the
    generator's injection-safety, and the probe cases as correct.
- **R002 resolved by the R001 fix:** feeding id-keyed `previousPosition` into the graph makes the inline (mobile) branch
  seed survivors from prior positions, matching the worker branch — no mobile-only snap, no parity exception needed.
- **Verification caveat (A9 / DoD build+lint items).** Job-33's own slices are green — `test:force-sim`, `test:universe`,
  `typecheck:force-sim/universe/mobile` (job-33 files), `test:mobile`, both boundary probes, `lint:spec`,
  `lint:raw-strings`, `lint:fsd:layout`, the dev-image build, and the isolated Vite guard. The repo-wide `pnpm check` /
  `pnpm build:web` / `pnpm lint` are currently red **only** from concurrent jobs 31/32's uncommitted in-flight work
  (`UniverseCanvasWidget` `NebulaField` import, `values.gen.ts` "nebula" term, `nebula/lib/contributors.ts` "Epic A"
  comment) — none are job-33 files. Re-run the full gate once 31/32 land.
- **Out of scope, observed:** ARCHITECTURE §3.3 still prescribes `Line2` fat-line edges as the generic renderer
  primitive, but the as-built renderer uses `FatLineLayer` (ribbon) everywhere. A future doc-hygiene pass on
  ARCHITECTURE §3.3 / plan 14 should reconcile this — outside review 03's plan-24 scope.

## Grounding
- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (*The constitution*). Relevant: [I5] positions are
  derived / never persisted (WS-A must not persist or store coordinates); [I4][I6] filaments connect neurons only.
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2. Invoke `/fe-architecture` (WS-A universe/force-sim packages, WS-B web auth) ·
  `/mobile-architecture` (WS-B mobile config) for those surfaces. WS-C/WS-D touch infra + docs, not app placement.
- tech / policy / values touched: `spec/tech/rendering.md` (WS-D status wording only — the `Line2`/as-built rules are
  already correct here); `spec/plan/24.star-neuron-rendering.md`, `spec/plan/00.overview.md` (WS-D). No `spec/values.yaml`
  change, no proto/sql change, no migration.

## Affected files (blast radius)
**WS-A (R001/R002)** — FE shared packages:
- `packages/force-sim/src/carry.ts` (new), `packages/force-sim/src/index.ts` (export), `packages/force-sim/src/carry.test.ts` (new)
- `packages/universe/src/sim-bridge.ts` (worker + inline branches), `packages/universe/src/*` sim-bridge test
- Readers that depend on id↔slot alignment (no change expected, verify): `apps/web/src/widgets/universe-canvas/ui/UniverseCanvasWidget.tsx:53,165-177`,
  `apps/web/src/entities/filament`, `apps/web/src/entities/star`, `apps/web/src/entities/cell-star`, mobile universe widget mirror

**WS-B (R003/R004)** — mobile config + web build:
- `scripts/gen-mobile-env.mjs` (new), `apps/mobile/src/shared/config/dev-user.gen.ts` (new, committed default)
- `apps/mobile/src/shared/config/runtime-config.ts`, `apps/mobile/src/shared/config/index.ts` (if re-export needed)
- `apps/mobile/package.json` and/or root `package.json` (dev-start refresh hook)
- `apps/web/vite.config.ts`, `apps/web/src/app/providers/auth-provider.tsx` (keep runtime assert)
- `.env.example`, `README.md`

**WS-C (R005)** — dev infra:
- `apps/api/Dockerfile.dev`

**WS-D (R006/R007/R008)** — probes + docs:
- `scripts/probe-web-boundary.mjs`, `scripts/probe-mobile-boundary.mjs`
- `spec/plan/24.star-neuron-rendering.md`, `spec/jobs/archive/29.star-neuron-rendering.md`, `spec/plan/00.overview.md`
- (reference, already correct) `spec/tech/rendering.md`

## Verification / DoD
- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] No behavior change beyond the stated fixes (refactor): existing universe render, navigation, and dev-auth flows
      still work; new regression tests (WS-A) pass
- [x] Codegen applied where relevant: `pnpm gen` unaffected (the mobile dev-user file is intentionally out of
      `check:gen`); `node scripts/gen-mobile-env.mjs` produces the committed default
- [x] FE `pnpm --filter @cosimosi/web build` · `lint` · `pnpm typecheck` · `pnpm test` pass; BE `pnpm vet:api` ·
      `pnpm build:api` pass; the dev image builds (WS-C)
- [x] Boundary probes green (`pnpm lint` runs them) and now fail correctly on the injected plan-24 violations (WS-D)
- [x] Constitution sanity: WS-A introduces no coordinate persistence and no memory↔memory edge; filament endpoints stay
      neuron-only
- [x] Architecture self-audit passed (`/fe-` `/mobile-architecture`): placement correct, web↔mobile parity,
      `pnpm lint:fsd:layout` green
- [x] Full merge gate `pnpm check` green

## Review
- [x] `/code-review` applied (rejections noted with reason) · for the WS-A sim-bridge logic, `/codex:review --background`

## After completion — reflect into the SSOT
- [x] Update `plan/` to the new reality: plan 24 wording (WS-D), overview status (WS-D). Update `tech/rendering.md`
      status wording if WS-D changes it. No `spec/values.yaml` change.
- [x] 00.overview refactor-jobs board: review 03 → job 33 row ✅ · this doc's frontmatter `status: done`
