---
job: '87'
type: 'refactor'
source: 'code-review/07.deepseek-llm-provider-implementation-rev'
plan: 'none'
status: done
title: 'DeepSeek registration and documentation hardening'
---

# Job 87: DeepSeek registration and documentation hardening (refactor)

> Implementation work doc. Source spec: [code-review/07.deepseek-llm-provider-implementation-rev](../code-review/07.deepseek-llm-provider-implementation-rev.md).
> /cosimosi:implement-job 87 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/07 · composition half of R005, R006)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 (R005) A test compiled as part of the API composition-root package proves its production blank imports
      register DeepSeek and that `aiProviderCatalog` reports the LLM adapter as implemented. The test imports only the
      provider-neutral `internal/ai` package, never `internal/ai/deepseek`.
- [x] A2 (R005) A separate test compiled as part of the worker command proves that binary also registers DeepSeek,
      likewise without importing the adapter from test code. Removing either production blank import makes the
      corresponding test fail.
- [x] A3 (R006) DeepSeek's default-model source comment explains only the current default and override seam; it
      contains no dated alias-retirement or provider-history narration.
- [x] A4 (R006) The AI-worker as-built SSOT lists the exact runtime LLM registry identifiers
      `anthropic · openai · deepseek · glm · gemini · kimi`, identifies `glm` as the Z.ai/Zhipu slot, and retains the
      exact embedding registry identifiers.
- [x] A5 (R006) The comment-history guard and its probe reject the high-confidence scheduled
      retirement/removal/deprecation narration form that escaped the existing rule, without treating legitimate
      product concepts such as a past-dated diary as source-history narration.
- [x] A6 No runtime behavior changes: provider constructors, registry slots, composition imports, catalog results,
      default model, endpoint, and provider selection remain unchanged; only tests, comments, docs, and the executable
      comment guard are hardened.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 Create `cmd/api/provider_registration_test.go` in package `main`. Through `aiProviderCatalog`, assert the
      production API package reports DeepSeek as an implemented LLM provider; import `internal/ai` only as needed for
      neutral registry assertions and never import `internal/ai/deepseek`.
- [x] T002 [P] Create `cmd/worker/provider_registration_test.go` in package `main`. Assert
      `ai.ImplementedLLM("deepseek")` is true solely because `main.go` carries the production blank import; do not add
      any provider import to the test.
- [x] T003 [P] Rewrite the default-model comment in `internal/ai/deepseek/client.go` as timeless current-state
      rationale: what the default is for and how deployment overrides it. Do not change the model constant.
- [x] T004 [P] Reconcile `spec/tech/ai-worker.md` with `internal/ai/factory.go`: use exact LLM and embedding registry
      identifiers and explain the operator-facing `glm` label without renaming the runtime slot.
- [x] T005 [P] Extend `scripts/lint-comment-history.mjs` with a narrow rule for scheduled
      retirement/removal/deprecation narration and add must-catch/must-allow probe fixtures. Keep tests, stories,
      fixtures, generated code, requirement anchors, architecture-section references, and legitimate domain
      "past-dated" wording under the existing exemptions.
- [x] T006 Run the API and worker command-package tests, `pnpm lint:comments`, and
      `pnpm lint:comments:probe`; then run `go test ./...`, `go vet ./...`, and `go build ./...` from `apps/api` and
      finish with the repository spec/format checks.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) §2.1–§2.4 — `cmd/api` and `cmd/worker` are the
  composition roots and the only production packages that select concrete provider adapters. Invoke
  `/be-architecture` before implementation.
- Source findings: code-review/07 **R005** (composition registration is not tested) and **R006** (dated provider
  history and inaccurate slot identifiers).
- Registry SSOT: `internal/ai/factory.go` defines the exact runtime identifiers; `cmd/api/worker.go` and
  `cmd/worker/main.go` perform the independent production registrations; `spec/tech/ai-worker.md` documents the
  as-built result.
- Comment principle: [principle/code-comments.md](../principle/code-comments.md) — source comments explain current
  code, never chronology or planned lifecycle events. The executable guard stays deliberately high-confidence.
- Architecture decision: test each composition root directly instead of adding a shared provider-import package.
  Direct tests preserve the two explicit production wiring sites and fail at the binary whose import regresses.
- tech / policy / values touched: `spec/tech/ai-worker.md`; no plan, policy, values, migration, RPC, frontend, or
  mobile changes.

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

- `apps/api/cmd/api/provider_registration_test.go` _(new)_ — API composition-root/catalog registration regression
  (R005).
- `apps/api/cmd/worker/provider_registration_test.go` _(new)_ — worker composition-root registration regression
  (R005).
- `apps/api/internal/ai/deepseek/client.go` — outer-ring adapter's default-model comment only; no runtime edit (R006).
- `spec/tech/ai-worker.md` — exact provider registry identifiers and `glm` operator label (R006).
- `scripts/lint-comment-history.mjs` — executable guard and probe for the newly observed lifecycle-history form (R006).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) n/a — refactor; A6 explicitly pins runtime behavior
- [x] Codegen / migration / values: none
- [x] `go test ./cmd/api ./cmd/worker`, full `go test ./...`, `go vet ./...`, and `go build ./...` pass from
      `apps/api`; comment probe, scoped Prettier, spec, and diff checks pass
- [x] Constitution sanity: tests/comments/docs/guard only; no persistence or domain behavior touched
- [x] Architecture self-audit (`/be-architecture`): registration assertions live with their composition roots;
      provider behavior stays in the existing outer-ring subpackage and no shared wiring package is introduced

## Review

- [x] `/code-review` applied (rejections noted with reason) · `/codex:review` run (gpt-5.5)
  - Manual review and Codex found no actionable issue in the Job 87 scope. Static search confirms only production
    composition files import `internal/ai/deepseek`; both tests depend on those blank imports.
  - Repository-wide gates remain blocked only by concurrent out-of-scope design-system work:
    `pnpm format:check` reports four design/lint files, `pnpm lint:comments` reports two design comments, and
    `pnpm lint:language` reports three design files. Job 87's exact files and new comment probe pass.

## After completion — reflect into the SSOT

- [x] Update `tech/ai-worker.md` to the exact as-built registry identifiers; no plan/policy/values change
- [x] (if type=change) n/a — refactor
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
