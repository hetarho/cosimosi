---
job: "15"
type: refactor
source: code-review/01.phase-1-critical-readiness-audit
plan: none
status: done
title: Phase 1 architecture gates and hygiene
---

# Job 15: Phase 1 architecture gates and hygiene  (refactor)

> Implementation work doc. Source spec: [code-review/01.phase-1-critical-readiness-audit](../../code-review/01.phase-1-critical-readiness-audit.md).
> /cosimosi:implement-job 15 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.
>
> **Behavior-preserving job.** Every item either adds a *gate*, *relocates* code, or *removes unused* code — runtime
> behavior must not change, except the dev-only StrictMode provider lifecycle (R013), which is fixed as part of the
> promote-to-packages move (R017). "No regression" is the hard bar.
>
> **Ordering:** the provider promote/lifecycle block (R013, R017) should run **after [job 14](14.phase-1-runtime-correctness.md)**
> so it relocates already-corrected code instead of re-fixing it. The gates (R001–R005), cleanup (R018–R022), and
> conventions (R023) are independent of job 14 and can be done in any order.
>
> Covers findings: **R001, R002, R003, R004, R005, R013, R017, R018, R019, R020, R021, R022, R023.**

## Acceptance Criteria (from code-review/01.phase-1-critical-readiness-audit)
<!-- Testable end-state for each chosen finding. After building, verify each is true in the running code. -->
- [x] A1 (R001) Root `pnpm lint` (incl. `lint:observability`) is green: the observability-boundary allowlist and
      `spec/tech/observability-and-flags.md` match the as-built mobile provider path, and a probe test fails loudly if
      the allowed path moves again.
- [x] A2 (R002) A clean CI run means the same thing as local `pnpm check`: CI covers shared-package typechecks+tests,
      the mobile smoke test (`test:mobile`), `lint:raw-strings`, `lint:observability`, language lint, generated
      freshness, and API vet/build/test.
- [x] A3 (R003) Mobile has an executable FSD/boundary gate (Steiger and/or `eslint-plugin-boundaries`) wired into root
      lint + CI, plus a probe proving a forbidden import fails.
- [x] A4 (R004) An executable per-user persistence-isolation guard exists before the first product table: product
      migrations must declare `user_id` (unless platform-owned), sqlc query fixtures prove user-scoped predicates, and
      repository adapter tests require `platform.UserScope`. Resolved consistently with A11 (R018).
- [x] A5 (R005) An executable toolchain pin (`.node-version`/`.tool-versions`/`mise.toml`) selects Node ≥22.13 and root
      scripts fail fast with a clear message on an unsupported runtime.
- [x] A6 (R013) Web auth + observability providers survive a React StrictMode dev remount (auth stays authenticated;
      telemetry still captures/tracks) — fixed by one shared lifecycle model, with production behavior unchanged.
- [x] A7 (R017) The platform-pure provider glue lives once in `packages/*` and both apps consume it: the observability
      runtime/session-bridge/capture mappers, the client-cache provider helper, a `@cosimosi/auth` React layer, a
      `@cosimosi/state-machine/react` seam, and a `@cosimosi/i18n` `useActiveLocale` — with the previous web↔mobile
      drift reconciled (single `getServerSnapshot` semantics; flag-override seam present on both; one `xstate-react`
      export set). Each app retains only its genuinely platform-specific vendor wiring. No runtime behavior change.
- [x] A8 (R018) Genuinely dead code is gone: the unused `createAuthInterceptor` pass-through, the unused
      `MobileAuthProvider` `adapter` prop, and the unreachable `resolvedQueryClient` third operand (web + mobile). The
      `persistenceprobe`/`UserScope` decision is made explicitly (see T-notes) and left consistent with A4.
- [x] A9 (R019) The telemetry/cache denylists keep only generic secret/ArrayBuffer guards; deleted-MVP domain field
      names are removed from `safe-properties.ts` and `render-state.ts`.
- [x] A10 (R020) `getFeatureFlag` resolves a definition in O(1) via the existing `definitionMap`; `use-reduced-motion`
      creates one module-scope `MediaQueryList` (SSR-guarded) instead of one per `getSnapshot`.
- [x] A11 (R021) JWKS cache TTL + miss-refresh interval come from `spec/values.yaml` via `gen:values` (or the exemption
      is documented in `spec/tech/config-values.md`).
- [x] A12 (R022) `apps/web/src/app/query-provider.test.ts` disposes its facade/observability/queryClient in
      `afterEach`/`try-finally`, so cleanup runs even on assertion failure.
- [x] A13 (R023) No source comment records plan/ticket numbers or phase labels; each is rephrased to timeless rationale
      (8 files listed in Affected files).
- [x] A14 (no-regression) Full `pnpm check` passes; no runtime behavior changes except A6.

## Implementation Checklist
<!-- Top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values). -->

### Quality gates & infra (R001, R002, R005)

- [x] T001 (R001) Reconcile the observability boundary: update `scripts/check-observability-boundaries.mjs` allowlist
      and `spec/tech/observability-and-flags.md` to the as-built `apps/mobile/src/app/providers/observability-provider.tsx`
      path; add a probe test for the allowed path. Prove `pnpm lint:observability` and root `pnpm lint` pass.
- [x] T002 (R002) Make CI mirror the root logical gate (`.github/workflows/ci.yml`): either run `pnpm check` as the
      authoritative job, or add explicit jobs covering shared-package typechecks/tests, `test:mobile`,
      `lint:raw-strings`, `lint:observability`, language lint, generated freshness, and API vet/build/test.
- [x] T003 [P] (R005) Add a Node toolchain pin (`.node-version` or `mise.toml`, ≥22.13) and a fail-fast runtime check in
      the root `check`/setup scripts when `process.versions.node` is below the declared engine.

### Architecture enforcement gates (R003, R004)

- [x] T004 (R003) Add a mobile FSD/boundary gate (`eslint-plugin-boundaries`, and Steiger if it models the RN skeleton)
      matching the web rule set; wire into `apps/mobile` lint + root lint + CI; add a forbidden-import probe.
- [x] T005 (R004) Add the per-user persistence-isolation guard: a check that product migrations declare `user_id`
      (unless platform-owned), an sqlc query fixture proving a user-scoped predicate, and a repository-adapter test
      template requiring `platform.UserScope`. **(migrate)** if a fixture migration is added. Keep consistent with T009.

### Provider lifecycle + promote-to-packages (R013, R017) — run after job 14

- [x] T006 (R017) Extract the platform-pure observability glue (delegated adapter, runtime factory, session bridge,
      `captureContext`/`stringProperty`/`toSentryLevel`) into `@cosimosi/observability`; both providers import it and
      pass only their vendor adapter. Carry the flag-override seam to both (reconcile the web/mobile drift).
- [x] T007 [P] (R017) Extract the client-cache provider helper (transport assembly + owned-QueryClient lifecycle +
      cache-scope reset) into `@cosimosi/client-cache`; both `query-provider`s consume it.
- [x] T008 [P] (R017) Add a `@cosimosi/auth` React layer (`AuthContext`, `useAuthFacade`, `useSessionSnapshot`,
      owned-facade binding) with **one** `getServerSnapshot` semantics; both apps consume it. Add a
      `@cosimosi/state-machine/react` seam (incl. `createActorContext`) and a `@cosimosi/i18n` `useActiveLocale`; remove
      the per-app copies. Consolidate the duplicated test fakes into the shared test seam.
- [x] T009 (R013) During the extraction, adopt one correct provider-lifetime model for app-lifetime singletons (e.g.
      create-once via `useState`, drop dispose-on-unmount, or make `dispose()` idempotent+revivable) so a StrictMode dev
      remount no longer kills auth/telemetry. Add a remount regression test. Production behavior unchanged.

### Cleanup (R018–R022)

- [x] T010 [P] (R018) Remove clearly-dead code: `createAuthInterceptor` pass-through + its barrel export
      (`packages/auth/src/auth-interceptor.ts`), the unused `adapter` prop in mobile `auth-provider.tsx`, and the
      unreachable `?? createClientCacheQueryClient()` operand in both `query-provider`s.
- [x] T011 (R018) Decide `persistenceprobe` + `platform.UserScope`: **recommended** — keep as the worked example the
      R004 guard (T005) builds on and add a smoke test that exercises `UserScope`; **alternative** — remove the probe
      package and reintroduce with the first persisted feature. Leave the tree consistent with A4/A8 either way.
- [x] T012 [P] (R019) Trim `packages/observability/src/safe-properties.ts` and `packages/client-cache/src/render-state.ts`
      to generic secret/ArrayBuffer guards; drop the deleted-MVP domain field names.
- [x] T013 [P] (R020) Expose `getDefinition(key)` on the flag registry backed by the existing `definitionMap` and use it
      in `getFeatureFlag`; hoist a single SSR-guarded `MediaQueryList` in `packages/ui/src/a11y/use-reduced-motion.ts`.
- [x] T014 [P] (R021) Add `supabase_auth` JWKS timings to `spec/values.yaml` and read the generated constants in
      `apps/api/internal/platform/supabase_auth.go` **(gen:values)** — or document the exemption in
      `spec/tech/config-values.md`.
- [x] T015 [P] (R022) Move teardown in `apps/web/src/app/query-provider.test.ts` into `afterEach`/`try-finally`.

### Conventions (R023)

- [x] T016 [P] (R023) Rephrase plan/ticket/phase-number comments to timeless rationale in:
      `apps/mobile/src/app/providers/machine-actors-provider.tsx`, `apps/mobile/src/shared/native/secure-token-storage.ts`,
      `apps/mobile/src/app/navigation/routes.ts`, `packages/state-machine/src/index.ts`,
      `packages/ui/src/theme/theme-store.ts`, `apps/web/src/app/i18n-config.ts`, `packages/ui/src/base.css`,
      `packages/ui/src/primitives/types.ts`.

### Close-out

- [x] T017 Run `pnpm check` (or the new CI-equivalent gate) end to end; confirm A14 (no regression) and green lint/CI.

## Grounding
- Constitution (the 8 invariants): [00.overview](../../plan/00.overview.md) §불변 원칙
- tech / policy / values touched:
  - [spec/tech/observability-and-flags.md](../../tech/observability-and-flags.md) — boundary path (R001), shared runtime (R017).
  - [spec/ARCHITECTURE.md](../../ARCHITECTURE.md) — FSD enforcement (R003), promote-on-reuse §3.5 (R017), persistence isolation §4 (R004).
  - [spec/plan/05.data-persistence.md](../../plan/05.data-persistence.md) — `user_id` + `UserScope` contract (R004/R018).
  - [spec/tech/config-values.md](../../tech/config-values.md) — JWKS timings via values seam (R021).
  - [spec/principle/code-comments.md](../../principle/code-comments.md) — no ticket/phase numbers in comments (R023).
  - `spec/values.yaml` — JWKS timing constants **(gen:values)** if T014 takes the values path.

## Affected files (blast radius)

- Gates/infra: `scripts/check-observability-boundaries.mjs` · `spec/tech/observability-and-flags.md` ·
  `.github/workflows/ci.yml` · `.node-version`/`mise.toml` · root `package.json` scripts · `apps/mobile/.eslintrc.js` ·
  `apps/mobile/package.json`
- Persistence guard: `apps/api/db/migrations/*` · `apps/api/db/queries/**` · `apps/api/internal/**` (repository test
  template) · a lint/check script
- Promote-to-packages: `packages/observability/src/*` · `packages/client-cache/src/*` · `packages/auth/src/*` (+ new
  `react` entry) · `packages/state-machine/src/*` (+ `react`) · `packages/i18n/src/*` (+ `react`) ·
  `apps/web/src/app/{observability-provider,query-provider,auth-provider,i18n-provider}.tsx` ·
  `apps/web/src/shared/{auth,i18n,model}/*` · `apps/mobile/src/app/providers/*` · `apps/mobile/src/shared/{i18n,model,testing}/*`
- Cleanup: `packages/auth/src/auth-interceptor.ts` (+ `index.ts`) · `apps/{web,mobile}/src/app/.../query-provider.tsx` ·
  `apps/api/internal/persistenceprobe/**` · `apps/api/internal/platform/user_scope.go` ·
  `packages/observability/src/safe-properties.ts` · `packages/client-cache/src/render-state.ts` ·
  `packages/observability/src/{facade,flags}.ts` · `packages/ui/src/a11y/use-reduced-motion.ts` ·
  `apps/api/internal/platform/supabase_auth.go` · `apps/web/src/app/query-provider.test.ts`
- Conventions: the 8 files listed in T016

## Verification / DoD
- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] No regression: full `pnpm check` passes; runtime behavior unchanged except the StrictMode lifecycle fix (A6)
- [x] Codegen / migration / values applied (if any): `pnpm gen:values` (T014) · `pnpm db:migrate` (T005 fixture)
- [x] FE `--filter @cosimosi/web build`·`lint` + `test:mobile` / BE `go vet ./... && go build ./...` (Docker) + `test:api` pass
- [x] Root `pnpm lint` and CI are green (A1, A2)
- [x] Constitution sanity: N/A for Phase 1 (no `records`/`memories`/`memory_links` tables exist yet)

### Verification Notes

- `pnpm gen:values`
- `pnpm install`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm test`
- `git diff --check`
- `pnpm check`

## Review
- [x] `/code-review` applied (rejections noted with reason) · for the promote-to-packages refactor, `/codex:review --background`

### Review Notes

- Direct diff review applied after the extraction/cleanup pass.
- Codex CLI review was unavailable: the installed global CLI fails at startup because optional dependency
  `@openai/codex-linux-x64` is missing.

## After completion — reflect into the SSOT
- [x] Update affected `tech/**` (observability-and-flags, config-values) + `ARCHITECTURE.md`/`plan/05` notes to the new reality · JWKS timings → `spec/values.yaml` (+`pnpm gen:values`)
- [x] Mark R001–R005, R013, R017–R023 resolved in [code-review/01](../../code-review/01.phase-1-critical-readiness-audit.md)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
