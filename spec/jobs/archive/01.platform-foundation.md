---
job: '01'
type: new
source: plan/01.platform-foundation
plan: plan/01.platform-foundation
status: done
title: platform foundation
---

# Job 01: platform foundation (new)

> Implementation work doc. Source spec: [plan/01.platform-foundation](../plan/01.platform-foundation.md).
> /cosimosi:implement-job 01 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/01.platform-foundation)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 `apps/api`, `apps/web`, `apps/mobile` exist as fresh roots; `apps/api-mvp` and `apps/web-mvp` are the renamed MVP apps with git history preserved; `apps/blog` is unchanged.
- [x] A2 `apps/*-mvp` are excluded from the pnpm workspace and CI — a clean `pnpm install` and full build/lint/typecheck never touch them, and their contents cannot break the clean build.
- [x] A3 Backend: from `apps/api`, `go run ./cmd/api` serves `GET /` → `hello world` and `GET /health` → 200.
- [x] A4 Web: `pnpm --filter @cosimosi/web dev` serves a page showing "hello world"; `pnpm --filter @cosimosi/web build` produces a bundle.
- [x] A5 Mobile: the React Native app builds and runs (emulator/simulator or Metro) showing a "hello world" screen.
- [x] A6 No file under `apps/{api,web,mobile}` or `packages/` imports from `apps/*-mvp` or `spec/.mvp/`.
- [x] A7 No proto/RPC, database, domain logic, three.js/WebGPU, auth, or deployment is introduced.
- [x] A8 Root tooling (docker-compose, `wrangler.jsonc`, CI, root scripts) references the clean roots, not the renamed MVP paths.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

**Set the MVP aside** (do first — frees the clean names)

- [x] T001 `git mv apps/api apps/api-mvp` and `git mv apps/web apps/web-mvp` (git history preserved). Leave `apps/blog` untouched.
- [x] T002 Rename the moved-aside web package so it can never collide with the fresh one: in `apps/web-mvp/package.json` set `name: @cosimosi/web-mvp` (and any self-references). `apps/api-mvp` keeps its own `go.mod` — it is outside any Go build for the clean roots, so its module path is irrelevant going forward.

**Workspace exclusion** (A2)

- [x] T003 `pnpm-workspace.yaml`: members = `apps/web`, `apps/mobile`, `apps/blog`, `packages/*`. Enumerate them (current file is missing `apps/mobile`) and **do not** match `apps/*-mvp`. Confirm a clean `pnpm install` never installs/builds/lints/typechecks the `*-mvp` dirs.

**`apps/api` — fresh Go hello world** (A3)

- [x] T004 [P] Fresh `apps/api`: new `go.mod`, module path `github.com/cosimosi/api`, Go 1.26 (do not reuse the `*-mvp` module). `cmd/api/main.go` = a plain `net/http` server, port from env (default `8080`); `GET /` → `hello world`, `GET /health` → 200. No Connect, no router framework, no `internal/` contexts.
- [x] T005 [P] Verify `go run ./cmd/api` from `apps/api` serves both routes (run in the Go-toolchain Docker image — see [local-toolchain](../../CLAUDE.md), `go` is not on host).

**`apps/web` — fresh React + Vite hello world** (A4)

- [x] T006 [P] Fresh `apps/web`: Vite + React 19 + TypeScript, package `name: @cosimosi/web`. `index.html` → `src/main.tsx` → an `app/` entry that renders "hello world". Only the FSD `app/` layer exists (entry/providers shell) — no `pages/widgets/features/entities`. **Pin React/Vite/TS versions from current official docs**, not memory.
- [x] T007 [P] Verify `pnpm --filter @cosimosi/web dev` (Vite server) and `pnpm --filter @cosimosi/web build` (static bundle to `apps/web/dist`) both work.

**`apps/mobile` — fresh React Native hello world** (A5)

- [x] T008 [P] Replace the `apps/mobile` placeholder (currently README-only): scaffold from the official React Native **TypeScript bare** template — Metro bundler, committed `ios/` and `android/` projects, `App.tsx` rendering a "hello world" `<Text>`. Same `app/`-shell FSD intent. **Do not** wire `react-native-webgpu`/three. Pin versions from current official docs.

**Repoint root tooling at the clean roots** (A8)

- [x] T009 Root `package.json` scripts: add mobile run/start (e.g. `dev:mobile`); keep `build:api` / `build:web` / `dev` / `dev:web` pointed at the clean roots. Ensure no script targets `*-mvp`.
- [x] T010 `docker-compose.yml` / `docker-compose.prod.yml`: the backend service build context `./apps/api` now resolves to the fresh Go root — ensure the fresh `apps/api` has the Dockerfile(s) the compose expects, or simplify the service to the hello-world build. Drop any `*-mvp` service.
- [x] T011 `wrangler.jsonc`: confirm `directory: ./apps/web/dist` points at the new Vite build output (path is reused by the fresh web root — verify, no `*-mvp`).
- [x] T012 `.github/workflows/ci.yml`: build/test paths (`working-directory: apps/api`, `go.mod`/`go.sum`, JS app filters) target the clean roots; `*-mvp` excluded. `deploy-backend.yml`: confirm it references the clean `apps/api` path only (deployment itself is a non-goal — don't add deploy steps). **Defer** the codegen/db script path fixes (`scripts/gen.mjs`, `scripts/db.mjs`, `scripts/gen-values.mjs` referencing `apps/api/internal/**`) to the transport/data units per the plan — do not change them here.

**Local run path** (the documented "see hello world")

- [x] T013 Document how to start each app and see "hello world" (root README or run doc): web via `pnpm --filter @cosimosi/web dev`, api via `go run ./cmd/api`, mobile via Metro/emulator.

**Guards** (A6, A7)

- [x] T014 Grep-verify no file under `apps/{api,web,mobile}` or `packages/` imports from `apps/*-mvp` or `spec/.mvp/`.
- [x] T015 Confirm nothing introduced beyond the empty shell: no proto/RPC, DB/persistence, domain code, three.js/WebGPU, auth, or real deployment.

> **No `spec/values.yaml` entries / no `gen:values` task.** The only configurable here is the API port, which is
> environment/runtime config (`.env`), not a science/tuning constant (plan §Policy / Values Impact).

## Grounding

- Constitution (the invariants): [00.overview](../plan/00.overview.md) §3 — the constitution; details in [concept.md](../../concept.md) §9–§10 and [ARCHITECTURE.md](../../ARCHITECTURE.md).
- Architecture (the empty frame this realizes): [ARCHITECTURE.md](../../ARCHITECTURE.md) — System shape (apps are peers sharing `packages/`), §2.3 (`apps/api` starts empty and grows into domain-first context packages), §3.1 (web FSD layers), §3.5 (web/mobile split + `packages/`).
- Toolchain: Node ≥ 20, pnpm via corepack, Go 1.26. `go` is not on host → run Go via Docker ([local-toolchain](../../CLAUDE.md)).
- tech / policy / values touched: **none** (no domain/UX rules; `spec/values.yaml` untouched).

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched. -->

- **Renamed:** `apps/api/**` → `apps/api-mvp/**`, `apps/web/**` → `apps/web-mvp/**` (T001); `apps/web-mvp/package.json` name (T002).
- **New roots:** `apps/api/{go.mod,cmd/api/main.go,...}` (T004), `apps/web/{package.json,index.html,vite config,tsconfig,src/main.tsx,src/app/**}` (T006), `apps/mobile/{package.json,App.tsx,ios/**,android/**,metro/tsconfig,...}` replacing `apps/mobile/README.md` (T008).
- **Workspace / scripts:** `pnpm-workspace.yaml` (T003), root `package.json` scripts (T009).
- **Root tooling:** `docker-compose.yml`, `docker-compose.prod.yml` (T010), `wrangler.jsonc` (T011), `.github/workflows/ci.yml`, `.github/workflows/deploy-backend.yml` (T012).
- **Docs:** root README / run doc (T013).
- **Untouched (deferred to later units):** `scripts/gen.mjs`, `scripts/db.mjs`, `scripts/gen-values.mjs`, `scripts/lib.mjs` (codegen/db paths → transport/data units); `apps/blog/**`; `proto/`; `packages/*`; `spec/values.yaml`.

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] Clean `pnpm install` succeeds and never touches `apps/*-mvp`
- [x] Web `pnpm --filter @cosimosi/web build`·`dev` work; Go `go run ./cmd/api` (Docker) serves `/` and `/health`; mobile builds & shows "hello world"
- [x] No codegen / migration / values needed (none in this unit)
- [x] Constitution sanity: no domain code, no persistence, no transport, no rendering, no deployment introduced

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [x] Update `plan/01.platform-foundation` status to built · 00.overview status board ⬜ → ✅
- [x] No `policy/**` / `tech/**` / `spec/values.yaml` changes expected (this unit lands no rules/numbers)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`

## Outcome notes

**As-built specifics** (recorded in plan/01): pnpm workspace uses `node-linker=hoisted` (`.npmrc`) for React Native /
Metro; `apps/mobile/metro.config.js` watches the repo root. Versions pinned via official scaffolders — React 19.2,
Vite 8, TS 6, RN 0.86, Go 1.26; web lint = oxlint. `docker-compose.yml` reduced to the `api` dev service (+ `apps/api`
`Dockerfile`/`Dockerfile.dev`); Postgres/MVP env return with their units. `deploy-backend.yml` is manual-only
(`workflow_dispatch`) so the rebuild never auto-deploys over live infra. `scripts/setup.mjs` reduced to `.env` + deps.

**Verification:** web `build`+`lint`+`dev`(:5173) ✅ · go `vet`+`build`(Docker) ✅ + runtime `/`→"hello world",
`/health`→200, unknown→404 ✅ · mobile `tsc`+`eslint` ✅ + Metro bundles iOS & Android (bundle contains "hello world")
✅ · `pnpm install --frozen-lockfile` ✅. **Mobile native-run caveat:** an actual emulator/simulator can't run in this
headless env; Metro bundling + tsc are the strongest available proof the RN app builds and renders the screen.

**Code review:** correctness pass (Go + Dockerfiles) and tooling-consistency pass run — 0 correctness bugs in
hand-written code. One in-scope regression found and **fixed**: `scripts/setup.mjs` referenced the removed `postgres`
compose service → reduced to `.env` + deps. Codex skipped (logic is trivial/mechanical scaffolding).

**Deferred (not this unit) / pre-existing — flagged for follow-up:**

- **`pnpm gen` / `gen:proto` landmine:** `proto/` still holds the MVP contract under `cosimosi/v1-mvp` with
  `buf.gen.yaml` present, so `pnpm gen` would generate MVP stubs into the fresh apps. Codegen is the **transport unit**
  (plan 03) — it rebuilds `proto/` + `buf.gen.yaml`. Do not run `pnpm gen` until then.
- **`db:*` scripts:** self-guarded by `hasDbSchema()` (no `apps/api/internal/db/schema.sql`) → no-op now; the
  **data-persistence unit** (plan 05) re-adds Postgres to compose and the migrate path.
- **`deploy-backend.yml` migration step** references `apps/api/internal/db/migrations` (gone) — workflow_dispatch-only
  and parked until the deployment unit.
- **`apps/blog` build is broken pre-existingly** (out of scope; blog is "untouched"): `rss.xml.ts` / `index.astro`
  import `../../../../spec/blog.md`, which the spec rebuild (commit afabbde) moved to `spec/.mvp/blog.md`. Absent at
  HEAD before this job; CI's `blog` job is red until a blog-content fix re-points or restores the source.
