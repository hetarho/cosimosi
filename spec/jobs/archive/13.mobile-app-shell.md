---
job: '13'
type: new
source: plan/13.mobile-app-shell
plan: plan/13.mobile-app-shell
status: done
title: mobile app shell
---

# Job 13: mobile app shell (new)

> Implementation work doc. Source spec: [plan/13.mobile-app-shell](../plan/13.mobile-app-shell.md).
> /cosimosi:implement-job 13 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/13.mobile-app-shell)

- [x] A1 `apps/mobile/src/app/App.tsx` is a provider composition root rather than a hello-world component.
- [x] A2 The mobile app has a typed navigation skeleton with `Boot`, `ShellHome`, and `Diagnostics` or equivalent non-product routes.
- [x] A3 Session, transport, QueryClient, state-machine, i18n, theme/design-system, observability, error-boundary, and native shell providers are composed in a documented order.
- [x] A4 The shell can render in host tests with fake auth, transport, storage, locale, and observability adapters.
- [x] A5 Mobile consumes the same generated/shared API, values, i18n, state-machine, and token contracts as web; no parallel mobile-only architecture is introduced.
- [x] A6 Native-only concerns such as secure token storage, device locale, safe area/status bar, and link handling stay at app/shared boundaries and do not leak into feature/domain slices.
- [x] A7 The diagnostics surface exposes provider health without secrets or product/private memory data.
- [x] A8 Quality gates include mobile TypeScript/lint and a shell smoke test that does not require an emulator.
- [x] A9 No product feature screen, universe renderer, RN WebGPU integration, new RPC, DB schema, or domain model is added.

## Implementation Checklist

- [x] T001 Before changing dependencies, check current official React Native navigation, safe-area, status-bar, link handling, and secure-storage docs.
- [x] T002 Replace hello-world `apps/mobile/src/app/App.tsx` with a thin provider composition root.
- [x] T003 Add `MobileAppProviders` with documented provider order: error boundary, observability, i18n, theme, session, transport, QueryClient, machine actors, and navigation.
- [x] T004 Add typed RN navigation skeleton with `Boot`, `ShellHome`, and `Diagnostics` or equivalent non-product routes.
- [x] T005 Add native adapter boundaries for secure token storage, device locale, mobile API base URL, safe area/status bar, and link handling.
- [x] T006 Add the diagnostics surface showing provider health only: locale, theme, session state, API base URL/ping, QueryClient summary, and flag defaults when available.
- [x] T007 Add host-render smoke tests using fake auth, transport, storage, locale, and observability adapters; no emulator required.
- [x] T008 Ensure mobile imports the same generated/shared API, values, i18n, state-machine, and token contracts as web.
- [x] T009 Update mobile quality gates from job 02 if needed so TypeScript/lint and shell smoke test run in the full gate.
- [x] T010 Verify native concerns stay at app/shared boundaries and no feature/domain slice imports native adapter internals.
- [x] T011 Verify no product feature screen, universe renderer, RN WebGPU integration, new RPC, DB schema, or domain model is added.

## Grounding

- Source plan: [plan/13.mobile-app-shell](../plan/13.mobile-app-shell.md).
- Architecture: [ARCHITECTURE.md](../../spec/ARCHITECTURE.md) §3.5.
- Dependencies: [plan/04.auth-session](../plan/04.auth-session.md), [plan/06.client-data-cache](../plan/06.client-data-cache.md), [plan/07.state-machine-foundation](../plan/07.state-machine-foundation.md), [plan/08.i18n-seam](../plan/08.i18n-seam.md), [plan/09.design-system](../plan/09.design-system.md), and [plan/10.observability-and-flags](../plan/10.observability-and-flags.md).
- Values: no new `spec/values.yaml` entries expected.
- Policy/tech reflection: mobile shell, provider, navigation, and diagnostics rules should be reflected after implementation if policy/tech docs exist.

## Affected files (blast radius)

- `apps/mobile/package.json`, `apps/mobile/metro.config.js` only if navigation/native dependencies require changes
- `apps/mobile/src/app/App.tsx`
- `apps/mobile/src/app/providers/**`, `apps/mobile/src/app/navigation/**`, `apps/mobile/src/app/diagnostics/**`
- `apps/mobile/src/shared/**` native adapters and UI touchpoints
- shared provider/facade packages from jobs 04, 06, 07, 08, 09, 10, 11
- mobile tests/smoke test setup
- root/app quality gate scripts if mobile smoke tests are added to job 02 outputs
- `spec/plan/00.overview.md`, `spec/jobs/13.mobile-app-shell.md`

## Verification / DoD

- [x] Every Acceptance Criteria item above is true in the current code.
- [x] Mobile TypeScript/lint gates pass.
- [x] Mobile app-shell host smoke test passes with fake adapters and no emulator.
- [x] Diagnostics surface contains no secrets or product/private memory data.
- [x] Grep/review confirms no parallel mobile-only state/i18n/data architecture.
- [x] Grep/review confirms no product feature UI, universe renderer, RN WebGPU, new RPC, DB schema, or domain model was added.

## Review

- [x] `/code-review` applied — fixed: i18n locale negotiated synchronously (no wrong-locale first paint), Diagnostics ping guarded against setState-after-unmount, `AppProps` omits `children` (footgun), version-drift note. Rejected: query-provider `?? createClientCacheQueryClient()` (type-narrowing fallback, not dead); cache-scope warm-start clear (facade always mounts `bootstrapping`/`userId=null`, so the anonymous→auth transition always clears); observability `identify` on consent change (gated inside the facade; re-identify-on-grant is intended). Codex skipped: the working tree concurrently holds another in-flight job's changes, so a whole-tree Codex pass would be unreliable.

## After completion - reflect into the SSOT

- [x] Update [plan/13.mobile-app-shell](../plan/13.mobile-app-shell.md) to built/as-built details (status: built + As-built section).
- [x] Reflect mobile-shell rules into policy/tech docs if present — no policy tree exists; the `platform.diagnosticsSurface` flag review note (packages/observability) updated to "wired by plan/13". As-built rules live in the plan.
- [x] Update [00.overview](../plan/00.overview.md) status for plan/job 13 (board ✅, plan index ✅ + archived).
- [x] Set this job frontmatter `status: done`.
