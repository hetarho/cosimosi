---
job: '158'
type: 'change'
source: 'changes/13.origin-centered-lens-universe-with-z-off'
plan: 'plan/19.force-sim-layout'
status: done
title: 'Origin-centered lens universe with z-offset gist copies'
---

# Job 158: Origin-centered lens universe with z-offset gist copies (change)

> Implementation work doc. Source spec: [changes/13.origin-centered-lens-universe-with-z-off](../../changes/archive/13.origin-centered-lens-universe-with-z-off.md).
> /cosimosi:implement-job 158 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from changes/13.origin-centered-lens-universe-with-z-off)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 The hippocampus band is origin-centered and values-owned: `force_sim.hippocampus_z_min = −force_sim.hippocampus_z_max` (default ±9), and the center force targets (0, 0, 0) on all three axes.
- [x] A2 z is a genuinely spread force output: on the golden fixture graph the settled z distribution fills the band interior as an oblate lens (fixtures assert spread; no pile-up at the clamp faces), while every coordinate stays in-band.
- [x] A3 Every risen memory's gist body renders at live episodic (x, y, z) + `gistZOffset(stage)`; `gistZOffset` is pure, stage-monotonic, Go/TS golden-parity-pinned; no absolute gist coordinate is stored or reverse-projected ([C6][I5]).
- [x] A4 Layer separation holds by construction: `hippocampus_z_min + gistZOffset(1) > hippocampus_z_max` (guard test), so no episodic star can ever sit above any gist star, and BandFog sits inside that gap.
- [x] A5 The latent field samples the lens volume (matching silhouette) and remains rendering-only with no DB rows ([E7a][V7]).
- [x] A6 The default camera pose and the pinned opening pose have non-zero elevation so the lens depth reads; the pinned orbit axis remains world +z.
- [x] A7 Sim inputs remain structure-only (forbidden-field guard passes); positions are never persisted or sent ([I3][I5][V1]).
- [x] A8 Web and mobile produce byte-identical coordinates for the fixture graph after the one fixture re-bake (golden-parity preserved).
- [x] A9 PRD [C5][C6][V9], plan 19 and plan 42 as-built notes, policy/ux/layout.md, tech/force-sim.md, and tech/rendering.md state the lens + offset-copy contract.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 (gen:values) `spec/values.yaml` `force_sim`: re-center the band (`hippocampus_z_min: -9`,
      `hippocampus_z_max: 9`), **rename** `neocortex_z_min/max` → `gist_z_offset_min: 27` / `gist_z_offset_max: 45`
      (comment: absolute band → per-stage offset ladder), **add** `z_center_gain` (anisotropic z multiplier on the
      center pull; initial default, tuned at T015). Run `pnpm gen:values`; sweep the renamed constants through every
      consumer below (compile-driven).
- [x] T002 `packages/force-sim/src/forces.ts` — `applyCenterForces` z term: target = band mid (now 0) with the
      `z_center_gain` multiplier, so the settled cloud flattens into an oblate lens instead of piling on clamp faces.
- [x] T003 `packages/force-sim/src/seed.ts` — drop the ×0.25 z flattening in `seededClusterCenter`; seeds sample the
      band interior (lens-scaled), keeping the per-node seeded-PRNG stream discipline.
- [x] T004 `packages/force-sim/src/simulation.ts` — keep the per-tick hippocampus clamp and the non-degenerate band
      guard; drop the hippocampus<neocortex absolute-band ordering guard (offsets replace the upper band — the
      separation guard moves to T012).
- [x] T005 force-sim golden fixtures — one re-bake; add lens assertions: settled z fills the band interior (spread
      floor, no clamp-face pile-up), every coordinate in-band [A1][A2].
- [x] T006 `packages/memory-logic/src/semanticization.ts` — replace absolute `gistCoordinate(x, y, stage)` with pure,
      stage-monotonic `gistZOffset(stage)` (stage clamped 0..`SEMANTIC_MAX_STAGE`); update the TS goldens [A3].
- [x] T007 `apps/api/internal/memory/semanticization.go` — mirror `GistZOffset`; regenerate
      `testdata/semanticization-golden.json` so Go/TS parity pins the offset ladder [A3].
- [x] T008 `packages/universe/src/gist-star-channels.ts` + `packages/universe-render/src/GistStarLayer.tsx` — the
      channel emits the stage **offset**, the layer composes live episodic (x, y, z) + offset per frame and eases only
      the offset delta on a rise; no absolute gist coordinate stored or reverse-projected [A3].
- [x] T009 [P] `packages/universe/src/latent-field.ts` — lens-volume sampling (oblate taper matching the band
      silhouette) instead of disc × uniform z; still rendering-only [A5].
- [x] T010 [P] `packages/3d-renderer/src/layers/BandFog.tsx` + `packages/universe-render/src/UniverseSceneLayers.tsx` —
      fog occupies the gap `hippocampus_z_max`..`hippocampus_z_min + gistZOffset(1)` [A4].
- [x] T011 [P] `packages/3d-renderer/src/canvas/UniverseCanvas.tsx`/`.native.tsx` + `packages/universe/src/camera-rig.ts` + `packages/3d-renderer/src/layers/NavigationRig.tsx` — default pose and pinned opening gain non-zero elevation;
      orbit axis stays world +z; scalar retune only [A6].
- [x] T012 Guard test (values-level): `hippocampus_z_min + gistZOffset(1) > hippocampus_z_max` [A4]; confirm the
      forbidden-input-fields guard still passes untouched [A7].
- [x] T013 [P] Staged/demo surfaces re-fit to the re-centered bands (read renamed constants / shift hand-authored z;
      visual re-check, no behavior change): `apps/web/src/pages/demo/ui/DemoUniverseScene.tsx`,
      `apps/web/src/widgets/empty-sky/ui/EmptySky.tsx`, `packages/universe-render/src/MoodStarLayer.tsx`,
      `packages/universe/src/showcase-scene.ts`, `apps/web/src/pages/landing/ui/LandingWalkthroughScene.tsx`,
      `apps/web/src/pages/test/lib/engram-demo-scene.ts`.
- [x] T014 Golden-parity: run web + mobile fixture suites; byte-identical coordinates post re-bake [A8].
- [x] T015 Visual pass (headless CDP screenshot loop): lens depth reads, gap visible, each gist copy sits directly
      above its episodic star; tune `z_center_gain` + camera scalars here, values updated via (gen:values) [A2][A6].
- [x] T016 SSOT amendments: PRD [C5][C6][V9] (meaning-preserving wording), plan 19 + plan 42 as-built notes,
      `policy/ux/layout.md`, `tech/force-sim.md`, `tech/rendering.md` [A9].

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: [tech/force-sim.md](../../tech/force-sim.md) (coordinate contract + values list) ·
  [tech/rendering.md](../../tech/rendering.md) §"Gist-star / z-layer rendering" ·
  [policy/ux/layout.md](../../policy/ux/layout.md) (neocortex copy line) · PRD [C5][C6][V9] ·
  `spec/values.yaml` `force_sim.*` (re-centered band, offset-ladder rename, new `z_center_gain`)

## Affected files (blast radius)

- `spec/values.yaml` + `packages/config/src/values.gen.ts` (gen:values) — config values
- `packages/force-sim/src/{graph,forces,seed,simulation}.ts`, `simulation.test.ts`, golden fixtures — FE shared pure
  domain package (§3.5)
- `packages/memory-logic/src/semanticization.ts` + `semanticization.test.ts` — FE shared pure domain package (§3.5)
- `apps/api/internal/memory/semanticization.go`, `semanticization_test.go`, `testdata/semanticization-golden.json` —
  BE memory context, pure derivation mirror only (§2; no proto/schema/RPC surface)
- `packages/universe/src/{gist-star-channels,latent-field,camera-rig,showcase-scene}.ts` +
  `gist-star-channels.test.ts` — FE shared visual-entity logic (§3.4/§3.5)
- `packages/universe-render/src/{GistStarLayer,MoodStarLayer,UniverseSceneLayers}.tsx`, `use-universe-scene.ts` — FE
  shared render layer (§3.4)
- `packages/3d-renderer/src/layers/{BandFog,NavigationRig}.tsx`, `BandFog.test.ts`,
  `canvas/UniverseCanvas.tsx` + `.native.tsx`, `canvas-defaults.ts` — FE shared renderer (§3.4/§3.5)
- `apps/web/src/pages/demo/ui/DemoUniverseScene.tsx`, `apps/web/src/widgets/empty-sky/ui/EmptySky.tsx`,
  `apps/web/src/pages/landing/ui/LandingWalkthroughScene.tsx`, `apps/web/src/pages/test/lib/engram-demo-scene.ts` —
  web app staged surfaces (pages/widgets layers)
- Docs: `spec/PRD.md`, `spec/plan/19.force-sim-layout.md`, `spec/plan/42.gist-star-rendering.md`,
  `spec/policy/ux/layout.md`, `spec/tech/force-sim.md`, `spec/tech/rendering.md`

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] Reviewed: /code-review is not model-invocable in this environment, so the diff was hand-reviewed against the
      same rubric (NaN/clamp paths, SETTLED sentinel + byInstance cache, renamed-constant consumers — no findings);
      Codex (`codex exec -m gpt-5.5`, read-only) returned 4 findings, all applied: stage-monotonic `stageSeen` clamp
      (stale-read rise replay), origin-symmetry + positive-linkDistance validators, and two missed
      semanticization SSOT docs. No findings rejected.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
