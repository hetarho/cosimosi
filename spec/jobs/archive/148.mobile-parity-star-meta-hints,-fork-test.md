---
job: '148'
type: 'refactor'
source: 'code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-'
plan: 'none'
status: done
title: 'Mobile parity: star-meta hints, fork tests and a11y'
---

# Job 148: Mobile parity: star-meta hints, fork tests and a11y (refactor)

> Implementation work doc. Source spec: [code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-](../../code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-.md).
> /cosimosi:implement-job 148 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: R009 (P3, decided: port), R011 (P3), R016 (P3). Decisions: Questions #4 and #7 in the
source report.

- [x] A1 The star-meta hints (`star_meta_hint_*`, already translated) are shown on mobile —
      inline in the layout per the native tooltip's own guidance — and a mobile `MetaBlock`
      test pins them.
- [x] A2 The mobile mood-colors fork owns its tests (§3.5): the risky-colour confirm gate, the
      save-failure path, and the picker are covered on mobile.
- [x] A3 Mobile `MoodColorPicker` can reach chroma 0 (achromatic), matching web's floor. (The
      8-vs-12 step counts and border variance stay — adjudicated mobile visual scope.)
- [x] A4 Every mood-color swatch on mobile has a distinguishing accessibility name (no more 32
      buttons sharing one label); the preset buttons' name no longer shadows their visible
      title on either platform.
- [x] A5 Mobile mood-colors surfaces use the native `Alert` primitive (warning/danger variants,
      web-matching politeness) instead of bare `accessibilityRole="alert"` views.
- [x] A6 Mobile `UniverseViewToggle`'s accessible name contains the visible state word (WCAG
      2.5.3, matching web's documented strategy), with a test.
- [x] A7 `menu.native.tsx` and `obscured-text.native.tsx` own their tests.
- [x] A8 No web behavior change; web + mobile suites green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 `apps/mobile/src/features/star-meta/ui/MetaBlock.tsx` — render the four hints
      inline beneath their readings (native guidance: "a screen with one keeps its explanation
      in the layout"); add `MetaBlock.test.tsx` pinning label+hint pairs.
- [x] T002 `apps/mobile/src/features/change-mood-colors/ui/MoodColorPicker.tsx` — make chroma 0
      reachable (index from 0, or an explicit achromatic swatch); give each swatch a
      per-value accessibility name (hue/chroma in the label).
- [x] T003 Both platforms — stop shadowing the preset button's visible title with the generic
      `palette_preset_label` (compose the name from the visible title, keep the instruction as
      description/hint).
- [x] T004 Mobile mood-colors surfaces (`MoodColorsTab`, `MoodColorDialog` RiskNotice, save
      failure) — replace bare alert views with the native `Alert` primitive; match web's
      polite/assertive choices.
- [x] T005 `apps/mobile/src/features/pin-universe-view/ui/UniverseViewToggle.tsx` — put the
      visible state word into the accessible name (state+action, mirroring web's
      `aria-pressed` + hidden-action strategy in native form); add the fork test.
- [x] T006 Add mobile fork tests: `MoodColorsTab.test.tsx` (confirm gate one-press safe save,
      risky confirm, failure), `MoodColorDialog`/picker coverage.
- [x] T007 [P] `packages/ui/src/primitives/` — add tests importing `menu.native.tsx` and
      `obscured-text.native.tsx` (own-test rule §3.5).
- [x] T008 Reflect into docs: `spec/policy/ux/star-detail.md` (hints exist on both platforms,
      inline on native), `spec/policy/ux/emotion-palette.md` (picker floor, a11y naming),
      `spec/policy/ux/universe-view.md` (toggle naming strategy).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/policy/ux/star-detail.md`,
  `spec/policy/ux/emotion-palette.md`, `spec/policy/ux/universe-view.md`.

## Affected files (blast radius)

- `apps/mobile/src/features/star-meta/ui/MetaBlock.tsx` (+ new test) — features/star-meta/ui
- `apps/mobile/src/features/change-mood-colors/ui/{MoodColorPicker,MoodColorsTab,MoodColorDialog}.tsx` (+ new tests)
- `apps/web/src/features/change-mood-colors/ui/MoodColorDialog.tsx` (preset name shadowing, shared defect)
- `apps/web/src/features/choose-mood-colors/ui/ChooseMoodColors.tsx` (same)
- `apps/mobile/src/features/pin-universe-view/ui/UniverseViewToggle.tsx` (+ new test)
- `packages/ui/src/primitives/{menu.native,obscured-text.native,native-primitives.test}.tsx`
- `spec/policy/ux/{star-detail,emotion-palette,universe-view}.md` (doc reflection)

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] `/code-review` applied (0 findings, 0 rejections) · `/codex:review --background` returned 0 findings

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
