---
job: "07"
type: new
source: plan/43.cosmos-scene-composition
plan: plan/43.cosmos-scene-composition
status: done
title: CosmosScene — 공유 WebGL 배경 합성 (cosmos-scene-composition)
---

# Job 07: CosmosScene — 공유 WebGL 배경 합성 (cosmos-scene-composition)  (new)

> 구현 작업 문서. 출처 스펙: [plan/43.cosmos-scene-composition](../plan/43.cosmos-scene-composition.md).
> `/implement-job 07`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, plan/43.cosmos-scene-composition에서)
<!-- 이 job은 plan 43의 **J3 단계**(랜딩 전체 배경 이주 + 남은 구 배경 정리)만 다룬다. 선행: job 05(J1)·06(J2). -->
- [x] A5 랜딩 배경이 테마(vast/lively/calm)별 **팔레트**로 `CosmosScene`(fullscreen)으로 교체되고, 배경이 **페이지 전체**에 깔려(스크롤 동안 영속) 별 둘레 동그란 패치가 없다. 히어로 별이 bloom으로 글로우.
- [x] A6 히어로 섹션에서 배경 레이어가 **별 앞·뒤** 깊이로 정렬된다("일부는 별 앞으로·일부는 뒤로 흐른다"). 비-히어로 섹션은 별 0개 배경(같은 엔진).
- [x] A7 `prefers-reduced-motion`이면 정적. 랜딩 배경 부하는 `quality`/기기등급으로 조절되고 노브는 `VALUES.cosmos.*`에서 온다(하드코딩 없음). 페이지 전체 WebGL 배경이 저사양에서 과부하 안 되게 품질 단계가 실효.
- [x] A8 셰이더 TSL·별먼지 Points/InstancedMesh 유지(헌법8). build·lint·steiger 통과. **랜딩 무회귀**(히어로 CTA·스크롤·테마 전환·타 섹션 콘텐츠 불변, 배경만 교체).
- [x] C2 J3로 더는 안 쓰이는 구 배경 제거 — `pages/landing/ui/background/{LandingBackground,Vast,Lively,Calm}Background.tsx` · `widgets/star3d`(`ThemedStar`·`StarCanvas`·`Star3D`·`halo` — 랜딩 히어로가 마지막 소비처였으면) · `shared/ui/StarFieldCanvas.tsx`·`shared/ui/NebulaBackground.tsx`(미사용) · `GrainOverlay`(잔여 소비처 없으면). grep으로 잔여 import 0 확인 후 삭제.

> **선행/범위:** job 05(J1)·06(J2) 완료 후. CosmosScene API는 확정본 소비(새 컴포넌트 안 만듦). 이 job으로 plan 43의 전 화면 이주가 끝나고 구 배경 코드가 사라진다.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- J3 = 랜딩 전체 배경 이주 + 남은 구 배경 정리. 선행: job 05·06. -->
- [x] T001 테마→팔레트 매핑 — vast/lively/calm 각 `CosmosPalette`(현 배경색·성운 톤에서 도출). 페이지 어댑터(랜딩)에 둔다(CosmosScene은 팔레트만 받음).
- [x] T002 랜딩 페이지 배경 교체 — `pages/landing/ui/LandingPage.tsx`의 `<LandingBackground theme>` → `<CosmosScene camera="fullscreen" palette={themePalette} twinkle …>` (fixed inset-0 영속 배경). 스크롤 중 유지.
- [x] T003 히어로 별 — 히어로 섹션이 CosmosScene에 별(StarVisual: `appearance.object`+accent)을 실어 배경 위에 글로우(별 둘레 패치 없음, 앞/뒤 레이어). `HeroSection.tsx`의 `ThemedStar` 제거(배경 합성으로 흡수). cosimosi 워드마크·CTA·스크롤 힌트 등 DOM은 그대로 위에.
- [x] T004 비-히어로 섹션 — 별 0개 배경은 페이지 전역 CosmosScene이 담당(섹션마다 캔버스 두지 않음). 섹션 콘텐츠(카드 등)는 불변.
- [x] T005 성능 — 페이지 전체 WebGL 배경이므로 `quality`(기기등급/저사양 다운그레이드)·reduced-motion 정적·dpr/fps(`VALUES.cosmos.*`)를 확실히 적용(A7).
- [x] T006 잔여 소비처 확인 — `grep -rn "LandingBackground\|VastBackground\|LivelyBackground\|CalmBackground\|ThemedStar\|StarCanvas\|StarFieldCanvas\|NebulaBackground\|GrainOverlay" src`로 소비처 0 확인.
- [x] T007 구 배경 제거 — 위 확인 후 `pages/landing/ui/background/*` · 미소비 `widgets/star3d`(ThemedStar·StarCanvas·Star3D·halo) · `shared/ui/StarFieldCanvas.tsx`·`NebulaBackground.tsx`·`GrainOverlay`(잔여 0이면) 삭제 + index export 정리.
- [x] T008 [P] 게이트·검증 — `tsc -b`·`eslint .`·`steiger`·`vite build`. 랜딩: 테마 3종 배경 교체·동그라미 없음·히어로 글로우·스크롤/CTA 무회귀(시각). 사인인·초대·우주 무회귀.

## 참고 (grounding)
- 헌법: [00.overview](../plan/00.overview.md) §불변 원칙 §4·§8.
- 출처 plan: [plan/43](../plan/43.cosmos-scene-composition.md)(J3). 선행 job: 05(J1)·06(J2). 랜딩 테마·배경: [plan/15](../plan/15.landing-revamp.md). 별 프리미티브: [plan/42](../plan/42.star-visual-primitive.md).
- 렌더링: [tech/architecture](../tech/architecture.md) §3.1·§3.3. values: `cosmos.*`(J1에서 생성, 여기선 소비).

## 영향 파일 (blast radius)
- **수정:** `frontend/src/pages/landing/ui/LandingPage.tsx`(배경 → CosmosScene) · `frontend/src/pages/landing/ui/section/HeroSection.tsx`(ThemedStar 제거, 별을 배경 씬에)
- **삭제(J3로 무소비):** `frontend/src/pages/landing/ui/background/{LandingBackground,Vast,Lively,Calm}Background.tsx` · `frontend/src/widgets/star3d/ui/{ThemedStar,StarCanvas,Star3D,halo,star-canvas-context,use-in-view}.tsx`(잔여 소비처 0이면 widget 자체 정리) · `frontend/src/shared/ui/StarFieldCanvas.tsx` · `frontend/src/shared/ui/NebulaBackground.tsx` · `frontend/src/shared/ui/GrainOverlay.tsx`(미소비 시) (+ index export 정리)
- **무변경:** `widgets/cosmos-scene`(소비만) · `widgets/universe-canvas`(레퍼런스) · `entities/star`(소비만) · 랜딩 섹션 콘텐츠 컴포넌트

## 검증 / DoD
- [ ] 위 **인수 조건** 전 항목이 현재 코드에서 참
- [ ] (type=change면) 기존 plan 수용 기준 회귀 없음
- [ ] 코드젠/마이그레이션/values 적용(해당 시): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter ./frontend build`·`lint` / BE `go vet ./... && go build ./...`(Docker) 통과(해당 시)
- [ ] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음

## 리뷰
- [ ] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] `plan/`을 새 현실로 갱신 · 영향받은 `policy/**`·`tech/**` · 튜닝 수치는 `spec/values.yaml`(+`pnpm gen:values`)
- [ ] (type=change면) `changes/` 출처 문서를 `changes/archive/`로 이동
- [ ] 00.overview 진행 현황 ✅ · 이 문서 frontmatter `status: done`
