---
job: '150'
type: 'refactor'
source: 'code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-'
plan: 'none'
status: done
title: 'Comment hygiene and guard widening'
---

# Job 150: Comment hygiene and guard widening (refactor)

> Implementation work doc. Source spec: [code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-](../code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-.md).
> /cosimosi:implement-job 150 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: R007 (P3) UNIONED with review 14's R006 (cross-referenced both ways) + the Questions
#2 decision (plan-document anchors like `[64]` are banned and removed; comment↔spec tracing
returns later by a different mechanism).

- [x] A1 No job/plan/change/review numbers remain in comments across source AND executable
      config: the flagged hits are reworded as timeless constraints (the rationale stays, the
      history goes).
- [x] A2 `[64]`-style plan-document anchors are removed (requirement-ID anchors `[I2]`/`[V3]`
      stay allowed).
- [x] A3 `scripts/lint-comment-history.mjs` scans app-root and repo-root executable configs
      (vite/eslint/steiger etc.), and its patterns cover `change NN`, `code-review/NN`,
      `RNNN`, and bare `[NN]` plan anchors; the `--probe` negative fixtures prove each new
      form fails.
- [x] A4 `spec/principle/code-comments.md` names the plan-anchor ban explicitly.
- [x] A5 Full gate green — including the widened guard against the cleaned tree.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [x] T001 Reword the config-file hits: `apps/web/vite.config.ts:37` ("Job 144"),
      `eslint.config.packages.mjs:57,67` ("job 133"/"job 136"), `apps/web/eslint.config.js:71-72`
      ("not this job's work"), `apps/web/steiger.config.js` (nine plan-NN comments).
- [x] T002 Reword the in-tree hits: `packages/3d-renderer/src/layers/adaptive-dpr.ts:6`
      ("code-review/12"), `apps/api/internal/memory/view_semantic.go:20,121` ("no longer"),
      `apps/api/internal/store/catalog.go:77` ("went from two to five"),
      `apps/api/internal/ai/voyage/client.go`, `apps/api/internal/ai/models.go`,
      `apps/api/internal/admin/ports.go`, `packages/demo/src/pick.ts` ("change 03/04/10").
- [x] T003 Remove the `[64]` plan anchors: `apps/mobile/src/pages/me/ui/MePage.tsx:18`,
      `apps/web/src/app/App.test.tsx:387` (also drop its "no longer a tab" narration),
      `spec/plan/67.…md`-style anchors in code if grep finds more (`\[\d{2}\]` sweep,
      excluding `[I\d]`/`[V\d]`/`[P\d]`-class requirement IDs).
- [x] T004 Widen `scripts/lint-comment-history.mjs`: add app-root/repo-root executable configs
      to the scan roots (exclude generated/vendor), add patterns for `change[-\s]?\d`,
      `code-review/\d`, `\bR\d{3}\b` (comment-only), bare plan anchors; keep the CLI-example
      exemptions (`new-job.mjs` scaffold lines).
- [x] T005 Extend the `--probe` fixtures with one offender per new form/root; wire stays in
      `test:guards`.
- [x] T006 Update `spec/principle/code-comments.md`: plan-document anchors are banned; note
      that comment↔spec tracing will be reintroduced by a different mechanism later.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/principle/code-comments.md`.

## Affected files (blast radius)

- `scripts/lint-comment-history.mjs` (roots, patterns, probe fixtures)
- `apps/web/vite.config.ts`, `apps/web/eslint.config.js`, `apps/web/steiger.config.js`,
  `eslint.config.packages.mjs` (comment rewrites only — zero behavior)
- `packages/3d-renderer/src/layers/adaptive-dpr.ts`, `packages/demo/src/pick.ts`
- `apps/api/internal/memory/view_semantic.go`, `apps/api/internal/store/catalog.go`,
  `apps/api/internal/ai/{voyage/client.go,models.go}`, `apps/api/internal/admin/ports.go`
- `apps/mobile/src/pages/me/ui/MePage.tsx`, `apps/web/src/app/App.test.tsx`
- `spec/principle/code-comments.md` (doc reflection)

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) N/A — this is a refactor job
- [x] Codegen / migration / values applied (if any): `pnpm check` confirmed generated outputs are fresh
- [x] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` pass
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [x] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [x] External code review applied: three P3 probe-coverage findings fixed; final focused follow-up reported no findings.
      No findings were rejected or left unresolved.

## After completion — reflect into the SSOT

- [x] Updated the affected principle and progress board; no plan, policy, tech, or value changes were needed.
- [x] (if type=change) N/A — this is a refactor job
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
