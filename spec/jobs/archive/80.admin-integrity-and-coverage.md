---
job: '80'
type: 'refactor'
source: 'code-review/06.batch-75-78-admin-console,-error-taxonom'
plan: 'none'
status: done
title: 'admin integrity and coverage'
---

# Job 80: admin integrity and coverage (refactor)

> Implementation work doc. Source spec: [code-review/06.batch-75-78-admin-console,-error-taxonom](../../code-review/06.batch-75-78-admin-console,-error-taxonom.md).
> /cosimosi:implement-job 80 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/06 · R002, R003, R004, R015)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 (R003) `GrantStardust` **rejects an empty grant id** with the already-wired `ErrGrantIDRequired`
      (→ `ADMIN_GRANT_ID_REQUIRED`, InvalidArgument) instead of auto-minting one — the money-path idempotency is
      enforced server-side, not by client convention. The web client already sends `crypto.randomUUID()`, so no FE
      regression; a direct-RPC retry that omits the id is refused rather than double-crediting.
- [x] A2 (R004) `RevokeAdmin` **refuses a seed admin identified by email**, not only by id: a revoke targeting a user
      whose resolved email ∈ `seedEmails` returns `ErrSeedAdminUndemotable`, mirroring `isSeedIdentity`
      (`service.go:252`). The RPC no longer returns a misleading `isAdmin:false` for a still-admin seed.
- [x] A3 (R002) `internal/admin/pg` has integration test coverage (DB-gated like the other `*_integration_test.go`)
      proving: the grant + audit rows are written in one transaction (atomic — a failed audit rolls back the grant
      record), the append-only logs are never UPDATE/DELETE, and an idempotent replay (same grant id) records exactly
      one row.
- [x] A4 (R002) `internal/ai/runtime.go` has unit coverage: fingerprint change → rebuild, stable fingerprint → cached
      reuse, key rotation → rebuild, cleared-key resolution (shared with job 79's A2 — do not duplicate).
- [x] A5 (R015) At least one service/rpc test asserts the provider-key hint **shape** (masked/short) using the real
      `secretbox` cipher, not the constant `fakeCipher.Hint` — so the masking assertion can actually fail if masking
      breaks.
- [x] A6 (R002) The CI Postgres decision is made explicit: **either** `ci.yml` provisions a Postgres service so the
      DB integration tests run in CI, **or** `spec/tech/admin-console.md` states that DB integration coverage is
      local-only (so a green CI is not read as full coverage). (Coordinate with job 84 / R017 if touching CI.)
- [x] A7 No behavior change beyond the stated fixes: capped/append-only/audited grant semantics, DB→env→mock
      resolution, and the write-only key boundary are all preserved.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values). -->

- [x] T001 In `apps/api/internal/admin/service.go` `GrantStardust` (lines ~276-279): replace the empty-id auto-mint with
      `if grantID == "" { return 0, ErrGrantIDRequired }`. Confirm `internal/admin/rpc/server.go:314-315` already maps
      it (it does) — this revives the dead path, no new wiring.
- [x] T002 [P] In `service.go` `RevokeAdmin` (line ~194): resolve the target's email (via the `AccountDirectory` port)
      and refuse with `ErrSeedAdminUndemotable` when it matches a seed email, mirroring `isSeedIdentity`.
- [x] T003 [P] Add `apps/api/internal/admin/pg/store_integration_test.go` (**new**, DB-gated via
      `COSIMOSI_TEST_DATABASE_URL` like `twinkle/pg/store_integration_test.go`): grant-tx atomicity, audit append-only,
      idempotent-replay-records-once.
- [x] T004 [P] Add `apps/api/internal/ai/runtime_test.go` cases (A4) — coordinate with job 79 T004 so the cleared-key
      case is written once.
- [x] T005 [P] Add the real-`secretbox` hint-shape assertion (A5) in `admin/service_test.go` or `rpc/server_test.go`.
- [x] T006 Update `internal/admin/service_test.go:230` `TestGrantStardustCapAndIdempotency` to assert the empty-id
      **rejection** (it currently exercises the auto-mint) and keep the stable-id idempotency assertion.
- [x] T007 Resolve A6: add a Postgres service to `ci.yml` **or** document local-only coverage in
      `spec/tech/admin-console.md`. State the choice in the SSOT reflection.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../../plan/00.overview.md) §3. Grants are append-only [I1], capped [I3],
  metadata-only [I2].
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — BE §2. Invoke `/be-architecture`. Backend-only
  (`internal/admin` core + `internal/admin/pg` + `internal/ai` tests + CI).
- Source findings: code-review/06 **R002** (zero coverage + no CI DB), **R003** (grant idempotency dead error),
  **R004** (seed-email revoke), **R015** (weak masking test).
- tech / policy / values touched: `spec/tech/admin-console.md` (§2 authz, §5 CI/coverage note); `spec/policy/ops/admin.md`
  rule 1 (undemotable) + rule 3 (idempotent grant) reaffirmed; no values/migration.

## Affected files (blast radius)

- `apps/api/internal/admin/service.go` — `GrantStardust` (R003), `RevokeAdmin` (R004).
- `apps/api/internal/admin/service_test.go` — grant-id rejection + masking-shape (R003/R015).
- `apps/api/internal/admin/pg/store_integration_test.go` — **new** (R002).
- `apps/api/internal/ai/runtime_test.go` — **new/shared with job 79** (R002/A4).
- `apps/api/internal/admin/rpc/server_test.go` — optionally the masking-shape assertion (R015).
- `.github/workflows/ci.yml` and/or `spec/tech/admin-console.md` — CI Postgres decision (R002/A6).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression (n/a — refactor; grant/authz semantics pinned by the extended tests)
- [x] Codegen / migration / values applied: `pnpm gen:sql` for the new `GetTwinkleGrant` query (no migration/values)
- [x] BE `go vet && go build && go test ./...` green in Docker, **including** the DB-gated `admin/pg` integration
      tests run against the live local Postgres; FE `pnpm test:errors` + `pnpm typecheck` green (core.ts registry row)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
      (integration-test cleanup deletes only its own synthetic admin rows, per the twinkle test precedent)
- [x] Architecture self-audit passed (`/be-architecture`): new port method is consumer-owned (`admin.Store.GetGrant`),
      sqlc stays behind `admin/pg`, no proto/sql type leaks inward

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`
  - Adversarial review (fresh-context agent) + Codex (`gpt-5.5`) merged. Applied: (1) **P1 (Codex)** — cross-user
    grant-id reuse could credit twinkle (per-user dedup) while the global grant/audit insert silently no-opped;
    `GrantStardust` now records first (the grant row is the idempotency lock) and refuses a reused id whose stored
    target/amount differ with new `ErrGrantIDConflict` (→ `ADMIN_GRANT_ID_CONFLICT`, FailedPrecondition; registered
    in reasons.go, server mapping, server_test, policy/platform/errors.md, packages/errors core.ts); (2) **P2** —
    `RevokeAdmin` now fails CLOSED on a directory error (proceeding could delete the DB promotion that keeps an
    email-seed admin effective during the same outage, and wrote a misleading audit row); (3) fakeGranter now dedups
    per (user, grant id) so the idempotency test can fail on a credit-side regression; (4) append-only guard broadened
    to all `db/queries/*/*.sql` + TRUNCATE/schema-qualified forms; (5) job 81's `admin-console.md` §7→§8 reference
    fixed after the section renumber.
  - Rejected/deferred: web `UsersSection` mints a fresh UUID per click (a lost-response re-click legitimately
    double-credits; true retry safety needs one id per logical operation reused on retry) — FE surface, deferred to
    job 81's admin FE pass. Both reviewers cleared the atomicity test as non-vacuous (grant insert precedes audit)
    and the `testllm` registry entry as harmless.

## After completion — reflect into the SSOT

- [x] SSOT updated: `tech/admin-console.md` §2 (seed-email undemotable, fail-closed lookup), §6 (record-then-credit
      ordering + `ADMIN_GRANT_ID_CONFLICT` + server-enforced grant id), new §7 (DB integration coverage is
      **local-only**; CI provisions no Postgres — the A6 decision); `policy/platform/errors.md` reason row;
      plan 58 already states grant semantics at the AC level (unchanged)
- [x] (if type=change) n/a — refactor
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
