---
job: "30"
type: new
source: plan/25.latent-star-field
plan: plan/25.latent-star-field
status: done
title: latent star field
---

# Job 30: latent star field  (new)

> Implementation work doc. Source spec: [plan/25.latent-star-field](../plan/25.latent-star-field.md).
> /cosimosi:implement-job 30 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/25.latent-star-field)
<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->
- [x] A1 **Rendering-only, no DB.** The latent-star field is generated and drawn entirely on the client; it produces **no**
  database rows, no RPC calls, and no server state. A `neurons` row exists only for an *activated* neuron, and it is
  written by Encode ([20.encode-usecase](../plan/20.encode-usecase.md)), not by this unit ([E7a]).
  *Verified: the two slices carry no `api` segment, no transport/RPC import (grep); no `apps/api`/`proto`/`db` diff.*
- [x] A2 **Field density & placement.** On scene init the universe shows roughly `rendering.latent_star_count` gray points,
  each with **no brightness, no color, and no identity**, positioned within the hippocampus z-band
  `[force_sim.hippocampus_z_min, force_sim.hippocampus_z_max]` (from [19.force-sim-layout](../plan/19.force-sim-layout.md))
  and filling the visible hippocampus volume in x,y.
  *Verified: `generateLatentField` + `latent-field.test.ts` (count, z ∈ band, x,y within radius, positions-only — no channels).*
- [x] A3 **Background layer.** The field renders as a background layer inside [23.universe-canvas](../plan/23.universe-canvas.md),
  *behind* the real stars/neurons/synapses of [24.star-neuron-rendering](../plan/24.star-neuron-rendering.md); real bodies
  always render on top.
  *Verified: `LatentField` sets `depthTest=false`/`depthWrite=false`/`renderOrder=-1`; mounted before the real body layers.*
- [x] A4 **Awaken on new-neuron creation.** When a launch creates a new neuron
  ([20.encode-usecase](../plan/20.encode-usecase.md)/[27.writing-flow-ui](../plan/27.writing-flow-ui.md)), exactly one gray
  latent star plays an **awaken** animation and hands off to the real `cell-star`. Multiple new neurons in one launch each
  awaken a distinct latent star.
  *Verified: `AwakenNeuron` reacts to `newNeuronIds`; `pickAwakenSeeds` distinctness tested (N births → N distinct).*
- [x] A5 **Awaken anchor = near a recent neuron, else random.** The gray star chosen to awaken is the one nearest a
  *recently-active* neuron (the [L4] temporal window used conceptually); when there is no recent cue the pick is
  **random**. This selection is a client presentation decision — nothing about it is persisted or sent to the server.
  *Verified: `recentlyActiveNeuronIds` (window) + `pickAwakenSeeds` (nearest-to-anchor / random fallback) tested.*
- [x] A6 **Final position emergent, not stored.** The awaken start point is only the animation's anchor; the real neuron's
  **final position is emergent** from the force-sim ([19.force-sim-layout](../plan/19.force-sim-layout.md)) and is **never
  stored** ([I5]). No "awaken position" is written anywhere.
  *Verified: the flare plays at the seed only; no coordinate is written anywhere (no DB, no store beyond consumed marks).*
- [x] A7 **Instanced + TSL.** The field is drawn with `InstancedMesh` bucket(s) and a material authored in **TSL** (compiles
  to WGSL + GLSL; no raw-GLSL `onBeforeCompile`), mounted through the [14.rendering-foundation](../plan/14.rendering-foundation.md)
  renderer boundary — no slice imports `three` directly ([ARCHITECTURE §3.3](../ARCHITECTURE.md)).
  *Verified: `LatentField` = `InstancedMesh` + `MeshBasicNodeMaterial` (TSL `positionNode` drift); no `three` import in any slice (grep + eslint).*
- [x] A8 **Count tunable; mobile lowers it.** `rendering.latent_star_count` is a single build-time generated scalar
  (`values.yaml` → `pnpm gen:values`); changing it changes the field density with no code edits, and the mobile MVP
  renders a reduced count ([ARCHITECTURE §3.5](../ARCHITECTURE.md)).
  *Verified: web uses `latentStarCount` (800), mobile uses `latentStarCountMobile` (300); both generated, no literals in code.*
- [x] A9 **Emergent stays rendered-not-modeled.** The unit adds **no** `latent-star` type to the Go domain, DB, proto, or the
  FE domain-mirror; visual words do not travel upward into the domain slices ([ARCHITECTURE §3.4](../ARCHITECTURE.md)).
  *Verified: no `latent` in `packages/*` domain / `proto` / `apps/api` beyond the generated config scalars (grep).*

## Implementation Checklist
<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->
- [x] T001 Add `rendering.latent_star_count` to `spec/values.yaml` (under the existing `rendering:` group — the
  approximate number of gray latent-neuron points, [E7a], [V7]). The plan enumerates the key but leaves the number
  "roughly" — **choose + record the value now**. Run `pnpm gen:values` so `@cosimosi/config` exposes the camelCase
  constant; all code imports the generated constant, never a literal. The mobile MVP's **reduced count** must also come
  through generated values — if the reduction proves to need its own scalar (not a code-level composition choice), add
  it under `rendering.*` here too and record it; never a hardcoded fraction. **(gen:values)**
- [x] T002 Create `entities/latent-star` `model` (a *visual* entity, §3.1/§3.4 — imports **no** domain-mirror entity):
  a **deterministic, seeded field generator** — a pure function producing instance transforms for
  `rendering.latent_star_count` points, PRNG seeded by the generated `force_sim.seed` (plan 19) so web↔mobile produce
  the identical, reproducible field; each point's z ∈ `[force_sim.hippocampus_z_min, force_sim.hippocampus_z_max]`,
  x/y filling the visible hippocampus volume; points carry **no brightness, no color, no name, no identity**. Also the
  slice field state the awaken feature reads (positions + "consumed" marks — a consumed point stops being drawn as
  latent). Build-order note: the `force_sim.*` generated constants land with job 23 (plan 19) — build after them.
- [x] T003 Create `entities/latent-star` `ui`: the R3F background-layer component — one (or a few) `InstancedMesh`
  bucket(s), transforms written **once** into `InstancedMesh.matrix` at scene init; material authored in **TSL**
  through `@cosimosi/3d-renderer`'s seams (never `three` directly): a single uniform gray tint, small size, optional
  subtle **shader-time** ambient drift (meaning-free); **no per-frame React state and no force-sim participation** —
  latent stars are not sim nodes and never attract/repel real nodes.
- [x] T004 Add `entities/latent-star` `config` (slice-local constants that are **not** tuning numbers — tuning stays in
  generated values) and the slice public `index.ts` (single public API, no wildcard barrel).
- [x] T005 Mount the field inside `widgets/universe-canvas` (job 28's scene composition) as a **background layer
  behind** the plan-24 real bodies — arrange render order/depth so real stars/neurons/synapses always draw on top
  (AC A3); the widget imports the slice only via its `index.ts`.
- [x] T006 [P] Create `features/awaken-neuron` `model`: the **seed-pick logic** ([E7a]) — (1) anchor = a
  *recently-active* neuron over the visible graph (read from plan 23's `entities/neuron` mirror; the [L4] window used
  **conceptually** — a client-side presentation heuristic, not a server fact); (2) seed = the latent star **nearest**
  the anchor; (3) a **random** latent star when there is no recent cue (e.g. the first-ever neuron); N simultaneous
  births consume N **distinct** latent stars (picks must not collide). The precise window + nearest-star metric are
  code-level presentation constants **chosen + recorded at implement**; if one proves genuinely tunable, promote it
  under `rendering.*` then via `pnpm gen:values`. Nothing here is persisted or sent to the server.
- [x] T007 Model the awaken lifecycle (idle → awakening → done) as slice `model` control-state — an XState machine
  (`model/*.machine.ts`, §3.2) **only if** it needs discrete phases; per-frame position reads via `getSnapshot()`/refs,
  never 60 fps React state; several concurrent awakens (one per new neuron) must coexist.
  *Built without XState (the §3.2 "only if discrete phases" escape): the awaken is a single continuous 0→1 progress
  envelope with no branching phases, so it lives as a fixed-capacity flare pool in refs advanced by `FrameTick` — no
  60 fps React state, and several concurrent awakens coexist by design.*
- [x] T008 Create `features/awaken-neuron` `ui`: the awaken animation — the chosen gray point brightens/scales/hands
  off while the real `cell-star` (plan 24) fades in at its **emergent** target position (arriving from the sim on the
  next `GetUniverse` read); the latent point is "consumed" (stops being drawn as latent; the remaining field count is
  unaffected). **Choose + record the animation vocabulary** (brighten vs. scale vs. dissolve, duration, easing) at
  implement — the motion curve / shader graph is **code**, never a `values.yaml` entry. No "awaken position" is
  written anywhere ([I5]).
- [x] T009 Expose the feature's public `index.ts` reacting to **`new_neuron_ids`** (the `LaunchStarsResponse` field
  owned by plan 20 — only genuinely-*created* ids awaken; dedup onto an existing neuron does not) and mount the
  feature in the universe scene. The launch-flow invocation itself is plan 27 (job 32) — this job provides the seam +
  scene mount; until job 32 lands, drive the trigger from tests / the dev harness.
- [x] T010 Mobile parity (§3.5): mirror `entities/latent-star` + `features/awaken-neuron` in `apps/mobile/src` and
  mount them in the mobile universe-canvas mirror — the field generator + TSL material are platform-pure and reused
  **verbatim**; the MVP renders the **reduced count** through the generated constant (T001) and may simplify the
  ambient drift; a `*.native.tsx` sibling exists **only if** the R3F host wiring genuinely differs — the
  field/animation logic does not fork.
  *Slices copied verbatim (matching the plan-24 star/cell-star/filament mirror convention); no `*.native` sibling
  needed — the R3F host is already forked at the canvas level (plan 14). Only the widget differs: the mobile mount
  passes `latentStarCountMobile`.*
- [x] T011 Unit tests: field-generator **determinism** (same seed → identical field; count = the generated constant;
  every z inside the hippocampus band; no per-point identity/brightness/color) — AC A2; seed-pick logic (nearest to a
  recent anchor; random fallback when no cue; N births → N distinct picks) — AC A4/A5.
- [x] T012 Boundary greps: no slice imports `three` directly (only `@cosimosi/3d-renderer`) — AC A7; the two new slices
  carry **no** `api` segment, no RPC call, no transport import (rendering-only, AC A1); no `latent` vocabulary appears
  in the domain-mirror entities, `packages/*` domain code, `proto/`, or `apps/api` — AC A9 (there is no BE diff at all).
- [x] T013 Run the FE gates: `pnpm typecheck`, `pnpm lint` (incl. `pnpm lint:fsd:layout` — the new slices are
  segmented; `lint:raw-strings`; boundary probes), `pnpm test:web` + `pnpm test:mobile`, `pnpm build:web`. No
  `apps/api`, `proto/`, or `db/` diffs.
- *(No task — reserved / non-goals stay unbuilt: no DB schema / server / RPC surface ([E7a]); no real-neuron logic —
  extraction/dedup/activation/synapses are plans 20/21; no `cell-star`/`star`/`filament` bodies (plan 24); no renderer
  bootstrap (plan 14); no stored awaken position. A representational-drift *meaning* for the ambient wobble is a later
  direction — the drift here stays meaning-free.)*

## Grounding
- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (*The constitution*). Pure presentation — it
  honors **[I5]** (positions emergent, never stored: the awaken anchor is a transient animation start point, the real
  neuron settles wherever the force-sim pulls it) and **[I1]/[F2]** (silent engram — the gray field is a metaphor, not
  deletable domain state).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — §3.1/§3.4 (`entities/latent-star` is a *visual*
  entity beside — never leaking into — the domain mirror; `features/awaken-neuron` = one verb, one user-facing
  outcome), §3.2 (lifecycle control-state in XState; per-frame reads via `getSnapshot()`), §3.3 (instancing first,
  TSL-only authoring, no direct `three`), §3.5 (web=open beta / mobile=MVP lowers instance counts). Invoke
  `/fe-architecture` + `/mobile-architecture` for the surfaces this job touches (no BE surface).
- tech basis: [tech/rendering.md](../tech/rendering.md) (plan 14 as-built — the `@cosimosi/3d-renderer` boundary,
  TSL-only shader authoring, instancing, skin seam). The host scene + `entities/neuron` mirror land with
  [jobs/28.universe-canvas](28.universe-canvas.md); the `cell-star` handoff target with
  [jobs/29.star-neuron-rendering](29.star-neuron-rendering.md).
- tech / policy / values touched: **values** — introduces `rendering.latent_star_count` (**gen:values**; number chosen
  and recorded at implement; the mobile reduced count also flows through generated values). Reused, not re-declared:
  `force_sim.hippocampus_z_min`/`_max` + `force_sim.seed` (owned by [19.force-sim-layout](../plan/19.force-sim-layout.md),
  landing with job 23) and `rendering.instance_bucket_size`/`rendering.active_skin`/`rendering.max_pixel_ratio`
  (owned by [14.rendering-foundation](../plan/14.rendering-foundation.md)). **tech/** — on completion extend
  [tech/rendering.md](../tech/rendering.md) with *"latent stars are rendering-only (no DB); the awaken is entry
  choreography; the seed anchor is a client presentation choice; the real neuron's final position is emergent and
  never stored"* (**not pre-written**). **policy/ux** — reinforces [V7]/[F2] (the empty universe is never truly empty;
  neuron birth is legible); lands on implement, no new invariant. **Depends on**
  [14.rendering-foundation](../plan/14.rendering-foundation.md) (renderer boundary · TSL toolkit · skin seam),
  [19.force-sim-layout](../plan/19.force-sim-layout.md) (hippocampus z-band · seed · emergent positions),
  [23.universe-canvas](../plan/23.universe-canvas.md) (host widget · `entities/neuron` mirror); **relates to**
  [20.encode-usecase](../plan/20.encode-usecase.md) (`new_neuron_ids` — the only writer of `neurons`),
  [21.link-usecase](../plan/21.link-usecase.md) ([L4] window, used conceptually),
  [24.star-neuron-rendering](../plan/24.star-neuron-rendering.md) (the real `cell-star` the awaken hands off to),
  [27.writing-flow-ui](../plan/27.writing-flow-ui.md) (surfaces the launch response).

## Affected files (blast radius)
<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->
- `spec/values.yaml` — add `rendering.latent_star_count` under the existing `rendering:` group. *(gen:values)*
- `packages/config/src/values.gen.ts` — regenerated by `pnpm gen:values`. *(gen:values artifact)*
- `apps/web/src/entities/latent-star/model/latent-field.ts` — **NEW**, FE `entities/latent-star` (visual entity)
  `model` segment: deterministic seeded field generator + the field state (positions, consumed marks) the feature reads.
- `apps/web/src/entities/latent-star/model/latent-field.test.ts` — **NEW**, `model` segment test: determinism / band /
  count / no-identity.
- `apps/web/src/entities/latent-star/ui/LatentStarField.tsx` — **NEW**, `ui` segment: R3F `InstancedMesh` background
  layer + TSL gray material (imports `@cosimosi/3d-renderer` only, never `three`).
- `apps/web/src/entities/latent-star/config/constants.ts` — **NEW**, `config` segment: slice-local non-tuning constants.
- `apps/web/src/entities/latent-star/index.ts` — **NEW**, slice public API.
- `apps/web/src/features/awaken-neuron/model/pick-awaken-seed.ts` — **NEW**, FE `features/awaken-neuron` `model`
  segment: anchor → nearest → random pick; distinct picks for simultaneous births.
- `apps/web/src/features/awaken-neuron/model/pick-awaken-seed.test.ts` — **NEW**, `model` segment test: pick logic.
- `apps/web/src/features/awaken-neuron/model/awaken.machine.ts` — **NEW** (only if discrete phases are needed, §3.2):
  idle → awakening → done lifecycle.
- `apps/web/src/features/awaken-neuron/ui/AwakenNeuron.tsx` — **NEW**, `ui` segment: the awaken animation +
  `cell-star` handoff.
- `apps/web/src/features/awaken-neuron/index.ts` — **NEW**, feature public API (the `new_neuron_ids` entry seam).
- `apps/web/src/widgets/universe-canvas/ui/` scene composition — **EDIT** (file created by job 28; exact name per its
  as-built): mount the field behind the real bodies + mount the awaken feature.
- `apps/mobile/src/entities/latent-star/**` + `apps/mobile/src/features/awaken-neuron/**` — **NEW** mirror slices
  (§3.5: same slices/segments as web, logic shared verbatim; reduced count via the generated constant; `*.native.tsx`
  only if the R3F host wiring genuinely differs — none expected).
- `apps/mobile/src/widgets/universe-canvas/` scene-composition mirror — **EDIT**: the same mounts on mobile.
- *(Untouched: `apps/api/**` · `proto/**` · `db/**` — rendering-only, no server/DB/RPC [E7a]; `packages/3d-renderer/**`
  — the boundary is consumed, not modified; all other `packages/*` except the regenerated `packages/config` output;
  sibling slices `entities/{episodic-memory,neuron,synapse}` and `entities/{star,cell-star,filament}` — read via their
  public APIs only; `spec/tech/**` · `spec/policy/**` — extended only at completion per After-completion, not
  pre-written.)*

## Verification / DoD
- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — n/a (type=new)
- [x] Codegen / migration / values applied (if any): `pnpm gen:values` ✅ (no proto/db)
- [x] FE `--filter @cosimosi/web build`·`lint` pass; mobile `typecheck`·`lint`·`test` pass; no BE diff (rendering-only)
- [x] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records` — n/a (no DB diff)
- [x] Architecture self-audit passed (`/fe-architecture` + `/mobile-architecture`): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review
- [x] `/code-review` (3 parallel finder agents + verify) **and** Codex (`gpt-5.5`, background) applied. All findings
  fixed (none rejected):
  - **Burst overflow** (codex + review): a birth-burst larger than the flare pool consumed latent stars without
    flaring them and marked ids processed before a slot was assigned → now takes only `min(fresh, freeSlots)` per pass,
    consumes/claims only what actually flares; the rest retry.
  - **Awaken idempotency across remounts** (codex): `processed` lived in a component ref, so a remount holding the same
    `new_neuron_ids` could re-consume → moved to a module-level `awaken-registry` store (survives remounts).
  - **Unclamped frame delta** (review): a large delta after a tab resume could skip a whole flare → per-frame step
    clamped (`AWAKEN_MAX_STEP_S`).
  - **LatentField remount flash** (review): a fresh `InstancedMesh` starts full-count with zero matrices → stable ref
    now hides it (`count=0`/`visible=false`) until the matrix effect writes, mirroring `InstancedNodeLayer`.
  - **FrameTick ordering fragility** (review): correct by mount order — documented with a comment (no code change).
  - Verified-fine (no change): in-place `scales` mutation read by the layer, the depthTest/renderOrder background
    trick, TSL drift validity, disposal deps, Park-Miller determinism, web↔mobile parity.
  - Comment pass: swept new comments to the "why-only" bar; fixed one imprecise `drift` contract note.

## After completion — reflect into the SSOT
- [x] Updated `plan/25` to as-built (LatentField layer · flare-pool/registry · motion choices · values) · extended
  `tech/rendering.md` (plan-25 section) · added `policy/ux/latent-field.md` · values in `spec/values.yaml` (+`pnpm gen:values`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — n/a (type=new)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
