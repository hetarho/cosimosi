---
job: "26"
type: change
source: changes/15.wire-inert-core-logic
plan: plan/23.reconsolidation-reshaping
status: done
title: inert 핵심 로직 실배선 + SSOT 하드코딩 제거
---

# Job 26: inert 핵심 로직 실배선 + SSOT 하드코딩 제거  (change)

> 구현 작업 문서. 출처 스펙: [changes/15.wire-inert-core-logic](../changes/15.wire-inert-core-logic.md).
> `/implement-job 26`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.
> **MVP 임시 no-op 제거 — 랜딩이 단언하는 핵심 이론을 인증 우주에서 실제로 돌린다. T001은 사용자 설계 사인 필요.**

## 인수 조건 (acceptance — 기획 관점, changes/15.wire-inert-core-logic에서)
<!-- 출처 스펙의 수용 기준. 구현 후 각 항목이 코드에서 참인지 검증한다. -->
- [x] A1 인증 우주에서 새 회상이 `pe ≥ peThreshold`이면 별이 재성형되고(brightness/hue/form_seed_delta 변동·version++) `evolution_history` 행이 기록된다 — `pe`는 더 이상 구조적 0이 아니다(통합테스트로 recall/consolidated 임베딩이 다름을 확인).
- [x] A2 `arousal>0` 사용자의 새 기억 경쟁 점수에서 흥분성 항이 `g=ExcitabilityGain(arousal)`배로 스케일된다(arousal=0 대비, worker 테스트).
- [x] A3 랜딩 카드 표시 수치가 전부 `values.gen.ts`/SSOT에서 온다 — grep으로 `AmbientMoodCard`의 `0.3`·`ReconsolidationCard`의 `0.15` 인라인 리터럴 0.
- [x] A4 `ResonanceSection` 배지 = `done`·plan 36·37. VIII footnote가 미존재 write를 암시하지 않는다.
- [x] A5 프로덕션에서 별 회상 직후 캔버스 밝기가 즉시 갱신된다(staleTime echo 대기 없음).
- [x] A6 9개 live 이론에 회귀 없음(기존 FE·BE 테스트 green), 원본 `record` 불변 유지.
- [x] A7 `go build/vet/test ./...`·FE `build`·`vitest run`·`eslint .` green.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- 위→아래. [P]=다른 파일·무의존 병렬. (gen)/(migrate)/(gen:values) 표시. 튜닝 수치는 spec/values.yaml→gen, 하드코딩 금지. -->
- [x] **T001 설계 결정 — 재공고화 novelty 출처 ⚠️ 사용자 사인.** 현재 `repository_pg.go:335-336`이 `RecallEmbedding`·`ConsolidatedEmbedding`에 동일 `e.embedding`을 넣어 cos=1 → pe=0(`service.go:367`). seam 주석(`service.go:350-353`·`memory.sql:152` "PE=1-cos(recall_ctx, last_consolidated)")은 둘을 **다르게** 공급할 것을 명시. **단순 consolidation 스냅샷만으론 live `e.embedding`이 정적이라 pe>0 불가** → recall-context 임베딩이 필요하다. A(서버 recall-context centroid)로 구현.
  - **권장(A)**: 서버 파생 recall-context centroid — `GetReshapeContext`가 `RecallEmbedding`을 "최근 회상 세션(예: 직전 N분/co-recall 이웃) 임베딩 centroid"로 계산, `ConsolidatedEmbedding`=별 저장 임베딩. proto·FE 무변경, 컬럼 불필요.
  - **대안(B)**: 야간 consolidation 임베딩 스냅샷 컬럼 + recall 시 현재 맥락 비교(컬럼 신설 필요·T002).
  - 결정이 T002·T003의 데이터 흐름을 정한다. 권장 A로 진행하되 사용자 확인 후 확정.
- [x] T002 (migrate) (B 선택 시에만) `00013_consolidation_embedding.sql` — `memories`/`embeddings`에 `last_consolidated_emb vector(N)` + `consolidate.go`의 `RunConsolidation`/`ConsolidationWrite`(consolidate.go:103)가 야간에 채움. `pnpm db:migrate`. A 선택으로 스킵.
- [x] T003 (gen) BE 재공고화 배선: `GetReshapeContext`(`memory.sql:150`·`repository_pg.go:311-337`)가 `RecallEmbedding`≠`ConsolidatedEmbedding`을 반환하도록 쿼리·매핑 수정(현 `emb`/`emb` 중복 제거). 게이트(`service.go:367-368`)·`reshapeStep`·`applyAndLog`·이웃 전파는 **무수정**. `pnpm gen:sql` 완료.
- [x] T004 (gen:values) 흥분성 게인 배선: `worker.go:543 score = n.CosSim + wExc*(e/maxE)`의 `wExc`를 `wExc * memory.ExcitabilityGain(arousal)`로 스케일, `LoadExcitabilityInputs`(`worker.go:311`·`repository_pg.go`)가 사용자 단위 arousal을 동반 로드(또는 ambient에서 파생). `worker.go:41-49` seam 주석을 "배선됨"으로 갱신. 신규 튜닝값 없이 기존 `ExcitabilityWExc`·`arousalGainCoef` 재사용.
- [x] T005 worker 단위테스트: 동일 입력에서 `arousal>0`이 흥분성 항을 `g`배 스케일(arousal=0 대비) 단언. 경쟁 할당의 선택·순위 회귀 없음.
- [x] T006 통합테스트(`memory`/`job`): 새 회상에서 recall-ctx≠consolidated → `pe ≥ peThreshold` → `ApplyReshape`(version++)·`evolution_history` append. `pe` 비-0 확인 + 원본 `records` UPDATE 0 단언(헌법§1).
- [x] T007 [P] FE SSOT 하드코딩 제거: `AmbientMoodCard.tsx:96 const gain = 1 + 0.3 * cur.arousal` 및 카피 리터럴(`:27`·`:89`·`:206`) → `VALUES.ambient.*`/`excitabilityGain()` 파생 · `ReconsolidationCard.tsx:17 PE_THRESHOLD = 0.15` → `values.gen.ts` import; magnitude/hue 공식(`:40 0.22·log2`·`:49 0.1+0.12·pe`)이 BE와 다르면 "예시" 라벨 또는 BE 값 정렬.
- [x] T008 [P] FE 메타·카피: `ResonanceSection.tsx:123 <TheoryBadge status="planned" plan="30" />` → `status="done" plan="36·37"`(단일 표기 시 `36`) · `NightlyConsolidationCard` footnote "흥분성을 0으로 되돌려요" → 자연감쇠(tau=6h) 문구로.
- [x] T009 FE 캔버스 즉시 갱신: `MemoryPanel.tsx:104`의 `if (isDemoMode())` 게이트 제거 → 프로덕션도 회상 후 `universeInvalidateKey()` 무효화. (`:59`의 resonant 쿼리 `!isDemoMode()`는 별개 — 미수정.)
- [x] T010 검증: `pnpm gen`(proto 변경 시)·`db:migrate`(B 시)·`gen:values` 적용 · BE `go vet/build/test ./...`(Docker) · FE `build`·`lint`·`vitest run` · grep `0\.3`/`0\.15` 카드 인라인 0 · 9개 live 이론(엔그램·분할·시냅스·헵·능동인출·망각·침묵엔그램·야간공고화·공명) 수동 회귀.

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 **§1 원본 불변** — 재공고화는 `memories`(별 layer)만 빚고 `records`는 절대 안 건드린다(`ApplyReshape`가 보장, grep: UPDATE records 0).
- 재공고화 seam: `service.go:345-401`(`reconsolidate`·`peGate`)·`memory.sql:150 GetReshapeContext`·`repository_pg.go:311-337`. 흥분성 seam: `worker.go:38-49·311·539-543`·`memory.go:227-232 ExcitabilityGain`.
- 머신/튜닝 SSOT: `spec/values.yaml`→`frontend/src/shared/config/values.gen.ts`·`backend/internal/values/values_gen.go`(`ReshapePeThreshold`·`ExcitabilityWExc`·`arousal*`).
- 출처: [changes/15](../changes/15.wire-inert-core-logic.md), 대상 plan [23.reconsolidation-reshaping](../plan/23.reconsolidation-reshaping.md)·영향 [25.ambient-mood](../plan/25.ambient-mood.md)·[22.competitive-allocation](../plan/22.competitive-allocation.md)·[15.landing-revamp](../plan/15.landing-revamp.md)·[26.relevance-emotion-decay](../plan/26.relevance-emotion-decay.md). `SSOT-AUDIT.md` B1·B2 해소.
- 건드리는 tech/policy/values: `policy/domain/star.md`·`universe.md`(재공고화 활성·각성 게인 배선)·`tech/` 워커/임베딩 문서. `spec/values.yaml`(B 선택 시 스냅샷 관련, 신규 튜닝값).

## 영향 파일 (blast radius)
<!-- 이 범위 밖은 안 건드린다. -->
- **BE 재공고화** — `backend/internal/db/queries/memory.sql`(`GetReshapeContext`) · `internal/memory/repository_pg.go`(311-337) · `internal/memory/service.go`(`Recall`→`reconsolidate` 호출부, 게이트 무수정) · (B 시) `internal/db/migrations/00013_*.sql`·`internal/job/consolidate.go`(스냅샷 write).
- **BE 흥분성** — `backend/internal/job/worker.go`(38-49·311·539-543) · `internal/memory/repository_pg.go`(`LoadExcitabilityInputs`) · `internal/memory/memory.go`(`ExcitabilityGain` 재사용).
- **BE 테스트** — `internal/job/worker_test.go`(게인 스케일) · `internal/memory/service_test.go`/`job/worker_integration_test.go`(pe>0·재성형·history).
- **FE** — `frontend/src/pages/landing/ui/section/AmbientMoodCard.tsx`(96·27·89·206) · `ReconsolidationCard.tsx`(17·40·49) · `ResonanceSection.tsx`(123) · `NightlyConsolidationCard.tsx`(footnote) · `features/recall/ui/MemoryPanel.tsx`(104).
- **values** — `spec/values.yaml`(B 시 스냅샷·신규 튜닝값) → `pnpm gen:values`.
- **미수정(확인만)** — `peThreshold`/`baseStep`/`arousalGainCoef` 값 자체 · 9개 live 이론 경로 · `MemoryPanel.tsx:59` resonant 게이트.

## 검증 / DoD
- [x] 위 **인수 조건**(A1–A7) 전 항목이 현재 코드에서 참
- [x] (type=change) 기존 plan 23·25·22·26 + 9개 live 이론 수용 기준 회귀 없음
- [x] codegen/migrate/values 적용: `pnpm gen`(proto 시)·`pnpm db:migrate`(B 시)·`pnpm gen:values` — 이번 변경은 `pnpm gen:sql`만 필요, B migrate/values/proto 생성은 없음.
- [x] FE `pnpm --filter ./frontend build`·`lint`·`vitest run` / BE `go vet ./... && go build ./... && go test ./...`(Docker) 통과
- [x] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음 — **재공고화는 `memories`만 version++**
- [x] grep: 랜딩 카드 인라인 튜닝 리터럴(`0.3`·`0.15`) 0

## 리뷰
- [x] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background` — Codex CLI review는 timeout, 수동 diff review + integration-tag compile 검증 완료.

## 완료 후 — SSOT 반영
- [x] `plan/23`(재공고화 활성)·`plan/25`(각성 게인 배선) as-built 갱신 · `policy/domain/star.md`·`universe.md` 반영 · `SSOT-AUDIT.md` B1·B2 해소 표기 · 신규 튜닝값 `spec/values.yaml`(+`pnpm gen:values`)
- [x] `changes/15.wire-inert-core-logic.md`를 `changes/archive/`로 이동
- [x] 00.overview 진행 현황 ✅(행 제거) · 이 문서 frontmatter `status: done`
