---
job: '127'
type: 'change'
source: 'changes/09.one-guided-walkthrough-on-the-landing'
plan: 'plan/81.landing-page'
status: todo
title: 'One guided walkthrough on the landing'
---

# Job 127: One guided walkthrough on the landing (change)

> Implementation work doc. Source spec: [changes/09.one-guided-walkthrough-on-the-landing](../changes/09.one-guided-walkthrough-on-the-landing.md).
> /cosimosi:implement-job 127 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from changes/09.one-guided-walkthrough-on-the-landing)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [ ] A1 The landing renders one walkthrough section where `playground`, `feature-tour` and `mirror` used to be; the retired sections' components and message keys are gone, and the section tuple is `hero · walkthrough · demo-cta-top · theory · blog · closing-cta`, still a compile-time literal tuple.
- [ ] A2 The walkthrough has exactly six steps in the fixed order — split, launch, sky-takes-your-color, fade, recall, mirror — as a fixed-length literal tuple ending in the mirror step; removing, reordering or adding a step fails `pnpm typecheck`.
- [ ] A3 The walkthrough is hardcoded and advance-only: an authored diary and precomputed split (both locales), no free-text input, no mood picker, no time slider; the visitor's only inputs are each step's single action and step navigation, and every run is deterministic.
- [ ] A4 Step actions use the product's real UI verbs where one exists (별 쪼개기 · 별 띄우기 · 회고 register), and each step's visual change happens on the walkthrough canvas through the shipped production functions — split reveal, star launch, sky tint, `effectiveBrightness` dimming with decay-stage word loss, recall recovery with a changed form, and the strength-weighted mirror tilt.
- [ ] A5 The mirror step states the [M5] definition — the sky mirrors the emotions you return to, not their average — in both locales; no standalone mirror section remains, and the definition cannot be dropped without a `tsc` failure.
- [ ] A6 No meta-disclaimer remains on the page: `landing_play_note`, `landing_mirror_illustration_note` and `landing_cta_demo_note` are removed from both catalogues and nothing equivalent replaces them.
- [ ] A7 Both demo CTAs are a single refined label in the "우주 미리보기" register with no explanatory note, top and bottom; the closing block still hardcodes demo-before-signup; targets stay `/demo` and `/signup`.
- [ ] A8 All copy is `landing_*` in both catalogues with identical key sets, in the 해요체 voice with no 문어체 or 번역투; `pnpm gen:messages` output is committed, `pnpm lint:raw-strings` passes, and `copy-honesty.test.ts` passes with its `REVIEWED_DENIALS` allowlist updated for any re-voiced denial.
- [ ] A9 The public-page import closure holds: no transport is reachable from `pages/landing`, the page issues zero `apps/api` calls, and the hero's two-layer scene and poster fallback are unchanged.
- [ ] A10 PRD §5.11 [K1] carries the recorded amendment (tour + mirror delivered as one interactive section), and `spec/policy/ux/public-copy.md` + `spec/tech/landing-page.md` describe the as-built six-section IA.
- [ ] A11 `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:guards`, `pnpm lint:fsd:layout` and the web build pass; no new `steiger` exemption block is added.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [ ] T001 `pages/landing/model/walkthrough.ts` — the six-step contract: `WALKTHROUGH_STEPS` as a
      fixed-length literal tuple (`'split' · 'launch' · 'color' · 'fade' · 'recall' · 'mirror'`,
      `as const satisfies` the restated tuple, mirror last), `WalkthroughStepId`, and the pure step-progression model
      (current step, advance-only, restart) the section renders from. Derived scene facts per step come from
      the shipped production functions only (`effectiveBrightness`, decay-stage text, recall
      recovery/reshape, strength-weighted emotion slices) — no local formula.
- [ ] T002 [P] `pages/landing/config/walkthrough-content.ts` — the authored fixture: one well-written diary
      (ko/en), its precomputed split scenes (names, moods, neurons where the visuals need them), the
      accumulation entries step 3 tints the sky with, and the recall target — deterministic, locale-resolved,
      no coordinate authored ([I5]). Captions/labels stay `landing_*` message keys, not content.
- [ ] T003 `pages/landing/model/walkthrough.test.ts` — the tuple's length/order/mirror-last, advance-only
      progression, determinism of the authored content (same input → same scene facts), and the [M5] step's
      presence.
- [ ] T004 `pages/landing/ui/sections/LandingWalkthrough.tsx` + `ui/LandingWalkthroughScene.tsx` — the
      section: caption + one action per step (별 쪼개기 → 별 띄우기 → … → 회고 register) + next/restart,
      over a walkthrough canvas evolved from `LandingPlaygroundScene` (real render layers, authored data,
      no transport, lazy like today). Step visuals: split reveal, star launch, sky tint, dimming + word
      loss, recall recovery, mirror tilt.
- [ ] T005 `model/sections.ts` + `sections.test.ts` + `ui/LandingPage.tsx` — the section tuple becomes
      `hero · walkthrough · demo-cta-top · theory · blog · closing-cta`; the component record follows; the
      test asserts the new order and that no mirror/tour/playground section id survives.
- [ ] T006 Retire the three sections: delete `ui/sections/LandingPlayground.tsx`,
      `ui/sections/LandingFeatureTour.tsx`, `ui/sections/LandingMirror.tsx`, `ui/LandingPlaygroundScene.tsx`,
      `model/playground.ts`, `model/playground.test.ts`; trim `config/illustration.ts` to what the hero ramp
      still needs (the mirror swatch half goes) and `config/theory-cards.ts` to theory-only (the five tour
      items go).
- [ ] T007 Copy — `packages/i18n/messages/{en,ko}.json`: add the walkthrough's `landing_*` keys (captions,
      step actions in the product's verbs, next/restart), re-label `landing_cta_demo` to the "우주 미리보기"
      register, remove `landing_play_*`, `landing_tour_*`, `landing_mirror_*` (the [M5] sentence moves to
      the walkthrough's mirror-step keys) and `landing_cta_demo_note`; 해요체 voice throughout; then (gen)
      `pnpm gen:messages`.
- [ ] T008 `model/copy-honesty.test.ts` — update `REVIEWED_DENIALS` for re-voiced denial sentences, keep the
      five claim classes and the one-deliberate-offender-per-class probes green over the new catalogue; if
      the `apps/web/index.html` `noscript` [M5] sentence is re-voiced, keep it present and honest.
- [ ] T009 [P] Docs — amend `spec/PRD.md` §5.11 [K1] (tour + mirror delivered as one interactive section;
      order and required content survive), update `spec/policy/ux/public-copy.md` (six-section IA, [M5]'s
      home, the no-meta-disclaimer stance) and `spec/tech/landing-page.md` (walkthrough model/scene, the
      step-tuple guard, retired sections); reflect plan 81 as-built at completion.
- [ ] T010 Gates — `pnpm lint` · `pnpm typecheck` · `pnpm test` · `pnpm test:guards` ·
      `pnpm lint:raw-strings` · `pnpm lint:fsd:layout` · web build; verify the landing import closure still
      bans transports and that no new `steiger` exemption appeared.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/policy/ux/public-copy.md` · `spec/tech/landing-page.md` ·
  `spec/PRD.md` §5.11 [K1] · **no `spec/values.yaml` key** (walkthrough content is content; timings are
  design tokens).

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

- `apps/web/src/pages/landing/model/walkthrough.ts` · `walkthrough.test.ts` — NEW (pages/landing, `model`)
- `apps/web/src/pages/landing/config/walkthrough-content.ts` — NEW (pages/landing, `config`; authored content)
- `apps/web/src/pages/landing/ui/sections/LandingWalkthrough.tsx` · `ui/LandingWalkthroughScene.tsx` — NEW (`ui`)
- `apps/web/src/pages/landing/model/sections.ts` · `sections.test.ts` — tuple change (`model`)
- `apps/web/src/pages/landing/ui/LandingPage.tsx` — section record (`ui`)
- `apps/web/src/pages/landing/ui/sections/LandingPlayground.tsx` · `LandingFeatureTour.tsx` ·
  `LandingMirror.tsx` · `apps/web/src/pages/landing/ui/LandingPlaygroundScene.tsx` ·
  `apps/web/src/pages/landing/model/playground.ts` · `playground.test.ts` — DELETED
- `apps/web/src/pages/landing/config/illustration.ts` · `config/theory-cards.ts` — trimmed (`config`)
- `apps/web/src/pages/landing/ui/sections/LandingCtas.tsx` — CTA label, note removed (`ui`)
- `apps/web/src/pages/landing/model/copy-honesty.test.ts` — `REVIEWED_DENIALS` + probes (`model`)
- `packages/i18n/messages/en.json` · `ko.json` (+ generated messages via `pnpm gen:messages`) — key churn
- `apps/web/index.html` — only if the `noscript` [M5] sentence is re-voiced
- `spec/PRD.md` · `spec/policy/ux/public-copy.md` · `spec/tech/landing-page.md` ·
  `spec/plan/81.landing-page.md` — SSOT reflection

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria — notably plan 81 AC1–AC4
      (route/gate untouched), AC6 ([K3] CTA order/targets), AC9–AC10 (theory cards + copy honesty), AC11
      (locale), AC12 (hero), AC13 (import closure), AC14 (SEO root)
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
