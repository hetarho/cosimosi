---
job: '146'
type: 'refactor'
source: 'code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-'
plan: 'none'
status: todo
title: 'Gist rise origin and renderer lifecycle fixes'
---

# Job 146: Gist rise origin and renderer lifecycle fixes (refactor)

> Implementation work doc. Source spec: [code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-](../code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-.md).
> /cosimosi:implement-job 146 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from code-review/13.jobs-136-145,-backdrop-rebuild-and-mood-)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

Findings: R001 (P2), R017 (P3).

- [ ] A1 A stage deepening (1→2, 2→3) eases the gist body UPWARD from the previous stage's
      band z — never from the hippocampal sim z, never through a downward snap ([I10]: the
      rise never reverses).
- [ ] A2 A memory's FIRST gist appearance still rises out of the hippocampus (sim-z origin) —
      unchanged behavior, pinned by a test.
- [ ] A3 `GistStarLayer.test.tsx`'s rise-origin assertion pins the band-z origin (the current
      assertion canonicalizes the bug and must flip).
- [ ] A4 The diary-armed spotlight survives React StrictMode's dev mount→cleanup→remount:
      `SpotlightDim`'s unmount cleanup no longer erases state it did not arm.
- [ ] A5 `AwakenNeuron`'s claim semantics and its comment agree: either unflared ids stay
      unclaimed and retry (comment's current promise), or the whole-batch claim is kept and the
      comment states it as a deliberate stop against retrying an exhausted field.
- [ ] A6 No behavior change beyond the three fixes; all renderer suites green.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->

- [ ] T001 `GistStarLayer.tsx` — on a `rose` re-arm (stage deepening), seed `entry.startZ`
      from the PREVIOUS stage's band z (derive via the same `gistCoordinate` projection from
      the prior `stageSeen` value) instead of `buffer[offset + 2]`; keep the sim-z origin for
      `isNew` re-arms.
- [ ] T002 `GistStarLayer.test.tsx:138-140` — flip the conforming assertion to the band-z
      origin; add a deepening case asserting the drawn z is monotonically non-decreasing
      across the ease.
- [ ] T003 [P] `SpotlightDim.tsx:43` — make the unmount clear StrictMode-safe (clear only ids
      this mount observed, or an arm-generation token the cleanup compares before clearing).
- [ ] T004 [P] Add a test: arm the spotlight, mount→cleanup→remount `SpotlightDim`, assert the
      armed ids survive and the dim still plays.
- [ ] T005 [P] `AwakenNeuron.tsx:83-93` — reconcile behavior and comment (recommended: claim
      only `picks`, leave the shortfall unclaimed with a bounded-retry guard; otherwise
      rewrite the comment to state the whole-batch stop); cover the short-field case in
      `awaken-flare-pool.test.ts` or a component test.
- [ ] T006 Reflect any contract wording change into `spec/tech/rendering.md` (gist rise
      one-way contract; spotlight lifecycle) — docs move with the code.

## Grounding

- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments, §3.4 domain→visual,
  §3.5 mobile) · BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/mobile-architecture` ·
  `/be-architecture` for the surfaces this job touches.
- tech / policy / values touched: `spec/tech/rendering.md` (rise contract wording, spotlight
  lifecycle); `spec/plan/42.gist-star-rendering.md` if its as-built text names the origin.

## Affected files (blast radius)

- `packages/universe-render/src/GistStarLayer.tsx` — rendering package (§3.4 seam)
- `packages/universe-render/src/GistStarLayer.test.tsx`
- `packages/universe-render/src/SpotlightDim.tsx` (+ new/extended test)
- `packages/universe-render/src/AwakenNeuron.tsx` (+ short-field test)
- `spec/tech/rendering.md`, `spec/plan/42.gist-star-rendering.md` (doc reflection)

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
