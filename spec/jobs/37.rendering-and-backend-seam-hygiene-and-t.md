---
job: "37"
type: refactor
source: code-review/04.epic-a-memory-genesis-runtime-and-gate-a
plan: none
status: doing
title: Rendering and backend seam hygiene and tests
---

# Job 37: Rendering and backend seam hygiene and tests  (refactor)

> Implementation work doc. Source spec: [code-review/04.epic-a-memory-genesis-runtime-and-gate-a](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md).
> /cosimosi:implement-job 37 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Result (partial — 2026-07-05)

**Done & gate-green** (all of `pnpm typecheck` · `lint` · `test` incl. Go pass):
- **R005 (P2) — GPU buffer leak fixed.** `InstancedNodeLayer` now `dispose()`s the previous
  `InstancedBufferAttribute` before replacing it on a node-count change, matching `ColorField`. (A1 ✓)
- **R013 hygiene:** mobile `NebulaNotice` imports `m` via the app `shared/i18n` barrel like every sibling;
  `UniverseCanvas.native` captures the `WebGPURenderer` in a ref and disposes it on cleanup; the anthropic
  `maxTokens <= 0` branch now errors instead of minting a 1-token request. (A3 ✓ for these three.)
- **R010 (partial):** `effective.go` `EffectiveStrength`/`EffectiveBrightness` documented as identity forward
  reservations for the forgetting/recall dynamics.
- **R012 (partial):** `packages/force-sim` gained empty-universe and single-unconnected-neuron+memory tests
  (no NaN / no divide-by-zero) — 19 tests pass.

**Deferred (documented follow-up — kept honest, not silently dropped):**
- **R010 — `past_dated` on the wire (A2).** Returning `LaunchResult.PastDated` on `LaunchStarsResponse` (proto +
  `gen:proto` + `rpc/server.go` + both apps' `launch-stars` consuming it instead of re-deriving `isPastDated`) is a
  proto-contract change; deferred to keep this pass low-risk. The FE `isPastDated` re-derivation is non-fatal today
  (the optimistic insert keys off returned ids, so an empty result inserts nothing).
- **R010 — `SignalKindSharedNeuron`/`SignalKindTemporal` + their `synapse.initial_*` values** left in place
  (consume-or-drop decision deferred; they are spec'd for the wider cross-memory link job).
- **R012 — the revise-split editable-surface invariant test (platform-agnostic rewrite) and the `internal/memory/rpc`
  mapping test** not added.
- **R013 — `AWAKEN_CAPACITY` const and the inert web HUD placeholder buttons** left as-is (acknowledged intentional
  placeholders; cosmetic).

## Acceptance Criteria (from code-review/04 — R005, R010, R013, R012)
<!-- GPU leak closed, forward seams resolved, drift/dead-code removed, invariants pinned by tests. -->
- [x] A1 **No orphaned GPU buffers on node-count change.** `InstancedNodeLayer` disposes the previous
      `InstancedBufferAttribute` before replacing it on count change, matching the sibling `ColorField`. Launching
      diaries repeatedly does not grow GPU buffer count unboundedly. (R005.)
- [ ] A2 **Forward seams are resolved.** Each consumer-less seam is either wired or explicitly marked a reservation:
      the monotonic-guard outcome is returned on the wire (`past_dated` on `LaunchStarsResponse`) and the FE reads it
      instead of re-deriving; the unused `SignalKindSharedNeuron`/`SignalKindTemporal` initials + their `values.yaml`
      entries are either consumed by Link or dropped; `EffectiveStrength`/`EffectiveBrightness`/`Depress` are
      documented as forward reservations. (R010.)
- [ ] A3 **Minor divergences / dead affordances removed.** `NebulaNotice` imports `m` through the same `shared/i18n`
      barrel on both apps; the inert web HUD Settings/Explore buttons are removed (or wired, both apps agreeing);
      `AWAKEN_CAPACITY` is sourced from config or its ceiling is documented; the native `UniverseCanvas` disposes its
      `WebGPURenderer` on cleanup; the anthropic `maxTokens <= 0` branch errors instead of minting a 1-token request. (R013.)
- [ ] A4 **Core invariants are pinned by platform-agnostic tests.** The revise-split editable-surface invariant ([I3]:
      no strength/position/time control) is asserted against the rendered control set (not DOM tag strings) so it
      covers both web and mobile; `packages/force-sim` has empty-graph and single-node fixtures; `internal/memory/rpc`
      has a thin proto↔domain mapping test. (R012.)
- [x] A5 **No behavior change / no regression.** Rendered output, launch behavior, and force-sim golden output are
      unchanged; all gates (`typecheck`/`lint`/`test` incl. Go) stay green.

## Implementation Checklist
- [ ] T001 `packages/3d-renderer/src/layers/InstancedNodeLayer.tsx` (~line 122): call `existing?.dispose()` before
      `mesh.geometry.setAttribute(...)` in the count-change branch, mirroring `ColorField.tsx:89-93`. (R005, A1.) [P]
- [ ] T002 Return the monotonic-guard outcome on the wire: add `past_dated` to `LaunchStarsResponse` in
      `proto/cosimosi/memory/v1/*.proto`; map `LaunchResult.PastDated` in `internal/memory/rpc/server.go`; the FE
      `launch-stars` reads it instead of `isPastDated` re-derivation. **(gen proto)** (R010, A2.)
- [ ] T003 Resolve the plasticity seams: either make `LinkService` consume `SignalKindSharedNeuron`/`SignalKindTemporal`
      initials, or remove those cases + drop `synapse.initial_shared_neuron`/`initial_temporal` from `spec/values.yaml`.
      Annotate `EffectiveStrength`/`EffectiveBrightness`/`Depress`/`EffectiveSynapseStrength` as forward reservations
      (a timeless "why", not a plan number). Decide via `/be-architecture`. **(gen:values if values dropped)** (R010, A2.)
- [ ] T004 Minor FE/BE hygiene (R013, A3) [P]:
      - `apps/{web,mobile}/src/entities/nebula/ui/NebulaNotice.tsx`: mobile imports `m` from the app `shared/i18n`
        barrel like every sibling.
      - `apps/web/src/pages/universe/ui/UniverseHomePage.tsx`: remove the inert Settings/Explore placeholder buttons
        (keep web↔mobile parity — mobile has none).
      - `apps/{web,mobile}/src/features/awaken-neuron/ui/AwakenNeuron.tsx`: source `AWAKEN_CAPACITY` from config or
        document the ceiling + handle overflow explicitly.
      - `packages/3d-renderer/src/canvas/UniverseCanvas.native.tsx`: dispose the `WebGPURenderer` in the effect cleanup.
      - `apps/api/internal/ai/anthropic/client.go:62-64`: error on `maxTokens <= 0` instead of clamping to 1.
- [ ] T005 Tests (R012, A4) [P]:
      - Rewrite `apps/web/src/features/revise-split/ui/ReviseControls.test.tsx` to assert the rendered control set
        (no strength/position/time control) in a way that also covers the mobile `Pressable` fork — or add a shared
        assertion consumed by both.
      - Add `packages/force-sim` empty-graph (0 neurons/0 memories) and single-node fixtures + tests (no NaN, no throw).
      - Add a `internal/memory/rpc` proto↔domain mapping test.
- [ ] T006 Run the full gate and confirm green; re-verify the force-sim golden fixture is byte-identical.

## Grounding
- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 — esp. [I3] (emotion never in layout; the
  editable-surface test guards it), [I5] (positions emergent), [I10] (monotonic time — the `past_dated` seam).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3.3/§3.4 (rendering lifecycle & disposal;
  domain→visual), §3.5 (web/mobile parity for the hygiene fixes); BE §2.7 (proto = DTO; handler maps proto↔domain),
  §2.9 (no computed value dropped then re-derived). Invoke `/be-architecture`, `/fe-architecture`, `/mobile-architecture`.
- tech / policy / values touched: `spec/values.yaml` (`synapse.*` / `awaken` if changed), `proto/` (`LaunchStarsResponse`),
  `spec/tech/rendering.md` (disposal note), `spec/tech/synapse-plasticity.md` (seam status).

## Affected files (blast radius)
- `packages/3d-renderer/src/layers/InstancedNodeLayer.tsx` — buffer dispose
- `packages/3d-renderer/src/canvas/UniverseCanvas.native.tsx` — renderer dispose
- `proto/cosimosi/memory/v1/*.proto` (+ generated Go/TS) — `past_dated`
- `apps/api/internal/memory/rpc/server.go` (+ `rpc/*_test.go`) — map `past_dated`, mapping test
- `apps/api/internal/memory/plasticity.go`, `link.go`, `effective.go` — seam resolution
- `apps/api/internal/ai/anthropic/client.go` — maxTokens branch
- `apps/{web,mobile}/src/features/launch-stars/**` — consume `past_dated`
- `apps/{web,mobile}/src/entities/nebula/ui/NebulaNotice.tsx`, `apps/web/src/pages/universe/ui/UniverseHomePage.tsx`,
  `apps/{web,mobile}/src/features/awaken-neuron/ui/AwakenNeuron.tsx` — hygiene
- `apps/web/src/features/revise-split/ui/ReviseControls.test.tsx` — invariant test
- `packages/force-sim/src/**` (+ fixtures) — empty/single-node tests
- `spec/values.yaml` (+ regen) — only if `synapse.*`/awaken values change

## Verification / DoD
- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] `pnpm gen` (proto) · `pnpm gen:values` (if values changed) applied
- [ ] `pnpm --filter @cosimosi/web build && lint`, `pnpm typecheck`, `pnpm test`, BE `go vet && go build && go test`
      (Docker) pass; force-sim golden byte-identical
- [ ] Architecture self-audit passed (`/be-`, `/fe-`, `/mobile-architecture`): proto stays DTO, handler thin, disposal
      in the layer, web↔mobile parity, no domain word in the visual path
- [ ] Constitution sanity: no memory row deletes; `past_dated` derivation stays server-authoritative ([I10]); positions
      emergent ([I5])

## Review
- [x] Focused self-review of the completed items (R005 dispose, native dispose, anthropic guard, NebulaNotice
      parity, force-sim edge tests). All gates green. Deferred items listed under **Result** above. Consolidated
      `/code-review` over the full working tree runs before hand-off.

## After completion — reflect into the SSOT
- [ ] Update `spec/tech/rendering.md` (disposal), `spec/tech/synapse-plasticity.md` (seam status); values → `spec/values.yaml`
- [ ] 00.overview progress board ✅ (review 04 → job 37) · this doc's frontmatter `status: done`
