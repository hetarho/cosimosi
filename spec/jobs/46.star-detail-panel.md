---
job: "46"
type: new
source: plan/35.star-detail-panel
plan: plan/35.star-detail-panel
status: todo
title: star detail panel
---

# Job 46: star detail panel  (new)

> Implementation work doc. Source spec: [plan/35.star-detail-panel](../plan/35.star-detail-panel.md).
> /cosimosi:implement-job 46 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.
>
> **Ordering note (cross-epic seam):** the 변천사 view consumes the `GetProvenance` read **owned by plan 46
> provenance-export (Epic G)** — that RPC does not exist yet and is *not* built here (plan 35 non-goal). Build jobs
> 43–44 first (the table + the writes); the provenance feature's `api` segment is wired against plan 46's contract, so
> either pull plan 46's `GetProvenance` slice forward or accept the 변천사 view rendering the (always-present)
> synthesized-baseline path only after that read ships. Flag the choice at implement.

## Acceptance Criteria (from plan/35.star-detail-panel)
<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->
- [ ] A1 Clicking a node on the universe canvas (selection non-nil in the canvas machine, [23]) opens
  `widgets/star-detail` **over** the running canvas without remounting the renderer; deselecting / a close affordance
  dismisses it ([D1][U3]).
- [ ] A2 For an **episodic** (big) star the meta block shows **shape · emotion color · brightness · 작성일 · 강도 ·
  current forgetting state**; for a **neuron** (small) star it shows **info only, with no emotion** ([D1][I3]); all
  derived values come from `packages/memory-logic` read-time functions, not re-derived in the panel.
- [ ] A3 An episodic star's **forgotten current memory text** is shown (rendered faded per decay stage where Epic D
  provides it; full text until then), and viewing it is **free** — it **advances no clock**, **spends no 별가루**, and
  **restores no content** ([R1][G1][F1][T3][I8][I10]).
- [ ] A4 A **변천사 보기** view renders the star's shape + a **time-ordered stage-text list** with **kind**
  (생성/요지화/재공고화) and **source** (원본/시스템/사용자) labels, populated by the `GetProvenance` read (created
  baseline synthesized at read + `memory_provenance` entries, CC5); distortion is **not** separately flagged
  ([R8a][D1]).
- [ ] A5 **회고하기** (shown only for an episodic star) **opens the recall flow** (plan 36 / job 47) — this panel emits
  the request and does **not** itself recall, reconsolidate, price, or spend 별가루 ([R1][R6]).
- [ ] A6 **원본 일기 보기** emits the navigation intent to the diary reader at the star's origin diary (plan 47, [D5]);
  this panel does not build the reader.
- [ ] A7 A **gist (요지) star** selection routes to the paid gist-view surface (plan 34 / job 45, [R8]), not this
  panel's free-text block; a clicked **neuron** shows the info-only meta with no episodic buttons.
- [ ] A8 The panel exposes **no** override of a star's emotion / strength / position and **no** placement or
  meaning-layer control — it only reads ([I3][I11]); the provenance view shows representation history only and never a
  mutable original ([I2]).
- [ ] A9 The panel's **control-state** is one XState machine (`closed → meta → provenance`); the selected id stays
  owned by the **canvas** machine (single source) and the fetched provenance lives in Zustand/Query, not in machine
  context (§3.2).
- [ ] A10 **All** user-visible copy resolves through the i18n seam (no raw strings; `lint:raw-strings` passes), in a
  literary / restrained Korean voice.
- [ ] A11 **web + mobile parity:** the same widget + three features run on both apps, sharing `model` / `api`
  verbatim; `*.native.tsx` siblings exist **only** for the panel / sheet primitives (§3.5).

## Implementation Checklist
<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values).
     Config/tuning numbers are never hardcoded — add them to spec/values.yaml, then import the (gen:values) constant. -->
- [ ] T001 Add the panel machine + pure selection resolver to `@cosimosi/universe` (the as-built home of shared FE
  `model` code — `packages/universe/src/`, following the `writing-flow.machine.ts` precedent so web + mobile share it
  verbatim §3.5): `star-detail.machine.ts` — states `closed → meta → provenance` **exactly per plan 35 §Panel
  control-state** (open/closed **derived from the canvas machine's `selectedNodeId`** — the navigation machine stays
  the single selection owner, per plan 35's Decided note; `SHOW_PROVENANCE`/`BACK`/`CLOSE`/deselect transitions;
  `RECALL`/`OPEN_DIARY` emitted intents that leave panel state intact; ids/phase only in context, A9) — plus a pure
  `resolveSelection(selectedNodeId)` selector over the existing `episodic-memory-store`/`neuron-store` mirrors
  yielding `episodic | neuron | gist` (gist routes to job 45's paid-view surface, A7). Machine tests
  (`star-detail.machine.test.ts`) for the phase table + derivation from selection.
- [ ] T002 [P] Build `features/star-meta` (web `apps/web/src/features/star-meta/`, mobile mirror
  `apps/mobile/src/features/star-meta/` — `ui` segment; any pure helpers in a shared segment per parity §3.5): the
  meta block ([D1]) — episodic: **shape** (seed-driven body preview drawn through the plan-24 star-body seam in
  `@cosimosi/universe-render`, not re-implemented), **emotion color** (`moodColor(mood)` single palette entry, plan
  17, display-only [I3]), **brightness** (`effectiveBrightness` from `@cosimosi/memory-logic` — the Epic-A stub now,
  Epic D later, CC1), **작성일** (`created_universe_time`), **강도** (`effectiveStrength(base, recall_count)` —
  live recall term once job 43 lands), **current forgetting state** (decay stage/word-loss extent — stub until Epic
  D); neuron: info only (name/type/connectivity), **no** emotion, no episodic buttons (A2). No formula re-derived in
  the panel (golden-parity read-only stance).
- [ ] T003 [P] Build `features/current-memory-text` (web + mobile): the episodic star's `current_text` rendered with
  the decay-stage word erasure from the stored `decay_stages` forgetting-texts (per plan 35's Decided note) — until
  Epic D ships the stub shows the full text unerased (CC1). A **pure read**: no clock call, no spend, no
  reset — free by requirement (A3).
- [ ] T004 [P] Build `features/star-provenance` (web + mobile): `ui` — the 변천사 list (star shape + time-ordered
  stage texts, each labelled kind 생성/요지화/재공고화 + source 원본/시스템/사용자; distortion **not** flagged, A4);
  `api` — the `GetProvenance` call + proto↔domain map against **plan 46's contract**
  (`GetProvenance{episodic_memory_id} → {entries[{kind, source, text, universe_time}]}` — the read is owned by plan
  46, see the ordering note; the feature renders whatever ordered list the read returns and does **not** know the
  baseline-synthesis rule). Fetched via Query on entering the `provenance` view (cached), not on every panel open.
- [ ] T005 Build `widgets/star-detail` (web `apps/web/src/widgets/star-detail/{ui,model,index.ts}` + mobile mirror):
  the panel/side-sheet host mounting **over** the running canvas (no renderer remount, A1), subscribing to the canvas
  selection via the shared machine's snapshot/selector (no per-frame React state §3.2), composing the three features +
  the three buttons ([D1], A5/A6): 회고하기 → emit "recall requested" (consumed by job 47's flow; episodic-only),
  변천사 보기 → toggle the `provenance` view, 원본 일기 보기 → emit the origin-diary navigation intent (plan 47
  consumes; until the reader exists the intent is emitted to the router seam with the route landing in plan 47).
  `*.native.tsx` sibling **only** for the panel/sheet host primitive (web side-panel ↔ RN bottom-sheet, A11). Panel
  sizing/animation constants live in the widget's `config/` segment (code-level, plan-23 precedent — no values group).
- [ ] T006 Wire the app layer (web + mobile per the as-built composition): mount the panel over the universe page
  (web `/` route composition; mobile `UniverseScreen`), route a **gist**-star selection to job 45's paid-view surface
  instead of the free panel (A7), and hook the emitted `RECALL`/`OPEN_DIARY` intents to their consumers (job 47's
  open-recall affordance; the router navigation seam). Invoke `/fe-architecture` (+ `/mobile-architecture`) for every
  placement.
- [ ] T007 i18n: add every string (button labels 회고하기/변천사 보기/원본 일기 보기, kind/source labels, meta
  captions, close affordance) as message keys through the plan-08 seam, literary restrained Korean voice, web +
  mobile drawing the **same** keys; `pnpm lint:raw-strings` green (A10).
- [ ] T008 Tests: machine tests (T001); component/feature tests per the as-built FE test harness (plan 12) — meta
  renders episodic vs neuron correctly (no emotion on neuron, A2); free-text feature performs no mutation/RPC other
  than reads (A3); provenance list renders ordered kind/source entries from a fixture response (A4); button emissions
  (A5/A6); gist routing (A7). `pnpm --filter @cosimosi/web test` (+ the universe package tests) green.
- [ ] T009 Boundary audit (grep): the panel imports no `three`/visual entity — the shape preview goes through the
  `universe-render` seam (§3.4); no write RPC call anywhere in the panel (read-only surface, A3/A8); no price/cost
  literal (CC2/CC3); no palette/emotion/strength/position override affordance (A8 [I11]); selection owned by the
  navigation machine only (A9); no hardcoded copy (A10).
- [ ] T010 Gates: `pnpm --filter @cosimosi/web build`·`lint`, the mobile typecheck/lint per the as-built mobile gates,
  `pnpm lint:fsd:layout`, `pnpm lint:raw-strings`, package tests. No BE / proto / `spec/values.yaml` diffs.
- *(No task — reserved / non-goals stay unbuilt: the recall flow/modal/spend is [job 47](47.recall-flow-ui.md) over
  [job 44](44.recall-usecase.md); the `GetProvenance` RPC + baseline synthesis + export are plan 46 (Epic G — see the
  ordering note); the diary reader + jump are plan 47; the decay/brightness/word-erasure math is Epic D (plans 37/39 —
  the panel reads stubs until then, CC1); gist viewing is job 45 + plan 42's surface; the economy is Epic F; the
  selection/picking engine stays plan 23's; **no new `spec/values.yaml` keys** — a pure read surface.)*

## Grounding
- Constitution ([I1]–[I11]): [00.overview](../plan/00.overview.md) §3 (*The constitution*). This job is bound by
  **[I2]** (the provenance view shows representation history, never a mutable original; the objective record is
  reached only via 원본 일기 보기), **[I3]** (emotion displayed, never a control; neuron meta shows no emotion),
  **[I8]** (viewing resets/restores nothing — recovery is recall's, job 44), **[I10]** (opening the panel moves no
  clock [T3]), **[I11]** (no override of emotion/strength/position — read-only).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) FE **§3.1** (widgets compose features; the
  domain-mirror read model — as-built: the `@cosimosi/universe` stores promoted by archive job 35 — is reused, no new
  entity slice), **§3.2** (one XState machine for panel phases; selection via `getSnapshot()`/selector; data in
  Zustand/Query), **§3.4** (domain→visual one-way; no `three` import in the panel), **§3.5** (web + mobile peers;
  `*.native.tsx` only for the sheet host), **§2.7** (`GetProvenance` is a `NO_SIDE_EFFECTS` read — plan 46's).
  Invoke `/fe-architecture` and `/mobile-architecture` for every file placed.
- tech / policy / values touched: **values** — **none new** (panel sizing/animation are widget `config/` code
  constants; `forgetting.*` is Epic D's, referenced through `memory-logic` when it lands; prices are Epic F's).
  **policy/** — the as-built rules land in a new `policy/ux/star-detail.md` at completion (**not pre-created**):
  viewing free + clock-still, faded-text-not-original, neuron-no-emotion, 변천사-unflagged-distortion,
  buttons-hand-off. **tech/** — the machine/composition record extends
  [tech/state-machine.md](../tech/state-machine.md) at completion. **Depends on**
  [23.universe-canvas](../plan/23.universe-canvas.md) (selection state + domain-mirror stores + running scene),
  [24.star-neuron-rendering](../plan/24.star-neuron-rendering.md) (the star-body preview seam),
  [17.emotion-model](../plan/17.emotion-model.md) (`moodColor`), [18.synapse-plasticity](../plan/18.synapse-plasticity.md)
  + [job 43](43.reconsolidation-rules.md) (the `memory-logic` effective values it displays — **43 first** for the live
  strength term), [job 44](44.recall-usecase.md) (the recall contract job 47 fronts; provenance rows to show),
  [08.i18n-seam](../plan/08.i18n-seam.md), [13.mobile-app-shell](../plan/13.mobile-app-shell.md),
  [15.web-router-foundation](../plan/15.web-router-foundation.md) (the navigation-intent seam). **Consumes** plan 46's
  `GetProvenance` (Epic G — ordering note above). **Opens** [job 47](47.recall-flow-ui.md); **routes gist to**
  [job 45](45.view-semantic-usecase.md)'s surface; **navigates to** plan 47's reader.

## Affected files (blast radius)
<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->
- `packages/universe/src/star-detail.machine.ts` — **NEW**, shared FE model (panel phase machine; open/closed derived
  from `universe-navigation.machine.ts` selection; the `writing-flow.machine.ts` precedent).
- `packages/universe/src/star-detail.machine.test.ts` — **NEW**, machine tests.
- `packages/universe/src/index.ts` — **EDIT**, export the machine + resolver.
- `apps/web/src/widgets/star-detail/{ui,model,config,index.ts}` — **NEW**, FE `widgets/star-detail` (panel host over
  the canvas; composes the three features; `*.native` handled in the mobile mirror).
- `apps/web/src/features/star-meta/**` — **NEW**, FE `features/star-meta` (meta block, episodic vs neuron).
- `apps/web/src/features/current-memory-text/**` — **NEW**, FE `features/current-memory-text` (free faded text).
- `apps/web/src/features/star-provenance/**` — **NEW**, FE `features/star-provenance` (`ui` list + `api`
  GetProvenance call/map against plan 46's contract).
- `apps/mobile/src/widgets/star-detail/**` · `apps/mobile/src/features/{star-meta,current-memory-text,star-provenance}/**`
  — **NEW**, mobile mirrors (`model`/`api` shared verbatim; `*.native.tsx` sheet-host sibling only, §3.5).
- Web app-layer composition (the `/` universe route composition under `apps/web/src/app/**` per its as-built shape) —
  **EDIT**, mount the panel + intent wiring.
- Mobile app-layer composition (`UniverseScreen` composition under `apps/mobile/src/**` per its as-built shape) —
  **EDIT**, same.
- i18n message catalogs (the plan-08 seam's catalog files) — **EDIT**, the panel/provenance/button keys.
- *(Untouched: `apps/api/**` · `proto/**` · `db/**` — no backend surface (the `GetProvenance` RPC is plan 46's);
  `packages/universe-render/**` — the body preview consumes its existing public seam; `packages/memory-logic/**` —
  read-only consumer; `widgets/universe-canvas/**` — selection machinery unchanged; `spec/values.yaml` — no keys;
  `spec/tech/**` · `spec/policy/**` — written at completion.)*

## Verification / DoD
- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review
- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT
- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
