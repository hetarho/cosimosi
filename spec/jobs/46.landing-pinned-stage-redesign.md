---
job: "46"
type: change
source: changes/31.landing-pinned-stage-redesign
plan: plan/15.landing-revamp
status: todo
title: 랜딩 전면 재설계 — 상단 고정 무대(stage) + 스크롤 콘텐츠 인터랙션, 전체 뇌과학 아크
---

# Job 46: 랜딩 전면 재설계 — 상단 고정 무대(stage) + 스크롤 콘텐츠 인터랙션, 전체 뇌과학 아크  (change)

> 구현 작업 문서. 출처 스펙: [changes/31.landing-pinned-stage-redesign](../changes/31.landing-pinned-stage-redesign.md).
> `/implement-job 46`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy) 반영, status: done.

## 인수 조건 (acceptance — 기획 관점, changes/31.landing-pinned-stage-redesign에서)
<!-- 출처 스펙의 수용 기준. 구현 후 각 항목이 코드에서 참인지 검증한다. -->
- [ ] A1 `/landing` 진입 시 히어로 별이 크게 보이고, 스크롤하면 **위로 밀려 올라가 화면 상단에 고정**되며 약 30vh의 **투명 인터랙션 무대**로 축소된다. 그 아래 콘텐츠는 계속 스크롤되고 무대는 화면 밖으로 사라지지 않는다.
- [ ] A2 기존 카드별 자족 인터랙션(슬라이더·버튼·hover·토글이 카드 안에서만 시연)은 **하나도 남지 않는다**. 모든 인터랙션 트리거는 콘텐츠 영역 버튼/일기 UI이고, 그 **결과는 상단 고정 무대**에서 펼쳐진다.
- [ ] A3 장이 §3 표의 **전체 뇌과학 아크**(엔그램 → 일기→별 분할/띄우기[엔그램+사건분할 통합] → 헵·시간 창 → 재공고화 → 망각 → 요즘의 나 → 야간 공고화)로 재구성되고, 카피가 그에 맞게 정정된다.
- [ ] A4 **뇌=우주** 장: 무대에 별 하나가 추가되며 뉴런(곡선 시냅스)으로 자연스럽게 연결되는 애니메이션이 보인다.
- [ ] A5 **일기→별** 장: 실제 작성 UI와 동일한 일기가 있고, "별 나누기→별 띄우기"로 일기가 N개 조각 별로 분할되어 무대로 떠오른다(일내 결속으로 가까이 모임).
- [ ] A6 **헵·시간 창** 장: 콘텐츠 버튼으로 무대 두 별 시냅스가 실제 규칙대로 강화(+0.05·상한 1.0)되고 같은 날 보너스가 반영된다(단조 강화, 정전 상수 import).
- [ ] A7 **재공고화** 장: "다시 떠올리기"로 무대 별이 밝기·색이 변하고(형태 변화는 job 45 완료 시 자동 반영), 원본 문장은 불변으로 병치된다.
- [ ] A8 **망각** 장: "시간 흐르기"로 무대 별이 `A_MIN=0.05` 바닥까지 어두워지되 사라지지 않고, 어두운 별을 클릭하면 다시 밝아진다.
- [ ] A9 **요즘의 나** 장: 마음(mood) 선택 시 **랜딩 페이지 전체 배경색**이 그 감정으로 물들고, 장을 벗어나면 복귀한다.
- [ ] A10 **야간 공고화** 장: "밤 보내기"로 무대에서 재활성화→재분배→요지화(형태 단순화·내용 흐려짐)→가지치기(약한 링크 밝기↓·삭제 없음) 단계가 펼쳐진다.
- [ ] A11 의미 색(mood hue) 보존·시냅스 곡선 전용·밝기만/삭제 없음·원본 불변 헌법이 무대 시각화에서도 지켜진다.
- [ ] A12 `entities/theory` 데모는 그대로 남아 체험 우주 이론 모달이 정상 동작한다(회귀 없음). 데모 진입 CTA(체험 우주/로그인) 정상.
- [ ] A13 `prefers-reduced-motion: reduce`이면 무대 전환·연결·물들임이 정적 프레임으로 안착하고 콘솔 에러가 없다.
- [ ] A14 FE `build`·`lint`·`steiger` 통과, 테마 시스템(현행)·체험 우주·이론 모달 회귀 없음.

## 구현 체크리스트 (implementation — 구현 관점)
<!-- 어떻게 만드나. 위→아래 순서. [P]=서로 다른 파일·의존 없는 병렬. (gen)/(migrate)/(gen:values) 표시.
     config·튜닝 수치는 코드 하드코딩 금지 — spec/values.yaml에 추가 후 (gen:values)로 생성 상수 import. -->

### 무대(stage) 셸 — 인터랙션 모델 교체
- [ ] T001 스크롤 진행 훅 `lib/use-stage-progress.ts` — `useLenis()`(또는 스크롤 위치)로 (a) 히어로→무대 전환 진행도 0→1, (b) 현재 활성 장 id를 산출. reduced-motion·Lenis 부재 시 폴백.
- [ ] T002 무대 레이어 `ui/StageLayer.tsx` — 화면 상단 ≈30vh **투명** 고정 영역(레이아웃 높이만 차지, `pointer-events`는 클릭 가능한 별에만). 히어로 별이 진행도에 따라 중앙(큰 별)→상단 무대(축소·고정)로 이동·축소. 기존 fixed `CosmosScene`의 `stars` 배열을 무대 상태가 구동(별 `anchor`/`size`/`color`/밝기 + 시냅스).
- [ ] T003 무대 상태 모델 `model/stage.ts`(zustand 또는 context, 랜딩 로컬) — 활성 장 + 장별 인터랙션 상태(추가된 별·시냅스·밝기·hue·배경 물듦)를 한곳에서 관리해 `CosmosScene`/배경에 투영. 콘텐츠 버튼이 이 상태를 갱신.
- [ ] T004 `LandingPage.tsx` 셸 재구성 — `<StageLayer/>`(상단 고정) + 그 아래 스크롤 콘텐츠(`<main>`). `AppearanceSwitcher`·테마(`data-landing-theme`)·`CosmosScene` 배경은 유지(테마 제거는 비범위). viz를 카드 안에서 무대로 이전.
- [ ] T005 `JourneyAct.tsx` — viz 슬롯 제거(또는 트리거 슬롯으로 축소). 장은 이제 eyebrow·heading·lead·**콘텐츠 트리거(버튼/일기 UI)** 만 담는다. 진입 시 무대 활성 장 전환.
- [ ] T006 `HeroSection.tsx` — 별 엠블럼을 무대 진행도에 위임(중앙 큰 별→상단 축소). 워드마크·CTA·스크롤 힌트 유지.

### 장별 무대 인터랙션 (전체 뇌과학 아크 — §3 표 순서)
- [ ] T007 **뇌=우주**(concept): 무대에 별 1개 추가 + 곡선 시냅스로 자연스럽게 연결되는 애니메이션. `ConceptSection` 재작성.
- [ ] T008 **일기→별**(엔그램+사건분할 통합): 실제 `record-memory` 작성 UI 시각 톤을 차용한 일기 표시 + "별 나누기→별 띄우기" 버튼 → 일기가 N조각 별로 분할되어 무대로 떠오름(일내 결속으로 근접 배치). `EngramCard`+`FragmentationCard` 통합 → 단일 장.
- [ ] T009 **헵·시간 창**(연합): "함께 떠올리기"/"시간 간격" 버튼 → 무대 두 별 시냅스 강화(`CO_RECALL_DELTA` import·상한 1.0·단조) + 같은 날 시간 보너스. `HebbianCard`+`TimeWindowCard` → 무대 구동으로 재작성.
- [ ] T010 **재공고화**: "다시 떠올리기" 버튼 → 무대 별 밝기±·hue 드리프트. **형태 변화는 `abstraction_stage` 형태 매핑 소비(job 45/change 29 의존 — 켜짐은 나중)**. 원본 문장 불변 병치. `ReconsolidationCard` 재작성.
- [ ] T011 **망각·침묵 엔그램**: "시간 흐르기" → 무대 별 밝기 `A_MIN` 바닥까지 감쇠(`HALF_LIFE_DAYS` import, 삭제 없음) / "어두운 별 클릭" → 재점화(밝아짐). `SilentEngramCard` 재작성.
- [ ] T012 **요즘의 나**(ambient+경쟁적 할당): 마음(mood) 선택 → **랜딩 전역 배경색**을 그 감정으로 일시 물듦(장 벗어나면 복귀) + 새 별이 그 성단으로 끌림. `AmbientMoodCard`+`PresentSelfCard` → 통합·재작성. (전역 물듦은 무대 밖 배경/루트까지 영향.)
- [ ] T013 **야간 공고화**: "밤 보내기" 버튼 → 무대에서 재활성화→재분배→요지화(형태 단순화·내용 흐려짐)→가지치기(약한 링크 밝기↓·마지막 링크 보호·삭제 없음) 단계 애니메이션. `NightlyConsolidationCard` 재작성.
- [ ] T014 **(선택) 공명**(소셜 🚧): `ResonanceSection`을 닫는 비전 비트로 유지(무대 토글 또는 축소). `CtaFooterSection` 유지.

### 정리·정합
- [ ] T015 카피 정정 — 장 heading/lead 갱신(특히 "쓰는 순간 별 하나가 태어나요" → **"일기를 별로 나눠 우주에 띄워요"**), `TheoryBadge`/`TryInUniverse`는 새 장 구성에 맞게 배치 유지(체험 가능한 장만).
- [ ] T016 폐기된 카드 인터랙션 코드 제거 — 자족 슬라이더/hover/토글 데모 삭제. **`entities/theory` 데모(EngramDemo 등)·테마 시스템·`AppearanceSwitcher`는 건드리지 않는다.** `pnpm steiger`로 FSD 위반·고아 export 정리.
- [ ] T017 reduced-motion 폴백 — 무대 전환·뉴런 연결·물듦·야간 단계 애니메이션이 정적 프레임으로 안착(콘솔 에러 0). Lenis 부재 시 네이티브 스크롤.

## 참고 (grounding)
- 출처 변경: [changes/31.landing-pinned-stage-redesign](../changes/31.landing-pinned-stage-redesign.md) — §3 장별 무대 인터랙션 표가 구현 기준.
- 대상 plan(as-built): [plan/15.landing-revamp](../plan/15.landing-revamp.md) — 셸·장·시각 시스템 소유 경계.
- 뇌과학 근거: [tech/neuroscience.md](../tech/neuroscience.md) §0·§1.1–1.2·§2.1–2.3·§3.1·§4.1–4.2·§5.1.
- 렌더 기반: [plan/43.cosmos-scene-composition](../plan/43.cosmos-scene-composition.md) — 랜딩은 fixed `CosmosScene`(R3F)에 `stars`/`palette`/`texture` 주입. 무대는 이 별 배열·시냅스를 스크롤·트리거로 구동.
- **의존: [changes/29.star-form-looks-redesign](../changes/29.star-form-looks-redesign.md)([job 45](45.star-form-looks-redesign.md))** — 재공고화·요지화의 **형태 변화**(`abstraction_stage`별 지오메트리)를 소비. 형태 켜짐은 job 45 완료 후 자동(이 job은 seam만 둠, 블로킹 아님).
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 — §1 원본 불변·§2 밝기만/삭제 없음·§8 시냅스 곡선·의미 색 보존.
- 정전 상수 import(하드코딩 금지): `A_MIN`·`HALF_LIFE_DAYS`(entities/memory), `CO_RECALL_DELTA`(features/recall). **신규 values.yaml 노브 없음** — 무대 높이(≈30vh)·애니메이션 타이밍은 랜딩 로컬 UI 상수(테마 CSS류와 함께 values.yaml 비대상).

## 영향 파일 (blast radius)
<!-- 출처 스펙·코드 grep으로 찾은 정확한 경로 — 이 범위 밖은 안 건드린다. -->
- 셸·스크롤: `frontend/src/pages/landing/ui/LandingPage.tsx`, `pages/landing/lib/scroll.ts`(+신규 `lib/use-stage-progress.ts`), 신규 `ui/StageLayer.tsx`, 신규 `model/stage.ts`, `pages/landing/index.ts`.
- 장·히어로: `pages/landing/ui/JourneyAct.tsx`, `ui/section/HeroSection.tsx`.
- 장별 카드(재작성/통합/삭제): `ui/section/{ConceptSection,EngramCard,FragmentationCard,HebbianCard,TimeWindowCard,AmbientMoodCard,PresentSelfCard,ReconsolidationCard,SilentEngramCard,NightlyConsolidationCard,ResonanceSection,CtaFooterSection,TheoryBadge,TryInUniverse}.tsx`.
- 소비(수정 가능): `widgets/cosmos-scene/ui/CosmosScene.tsx`(무대 별/시냅스 주입 — `StarVisual`/`SynapseVisual` 이미 지원), `entities/star/ui/VizStar.tsx`·`entities/synapse/ui/VizSynapse.tsx`(재사용).
- **안 건드림(회귀 경계)**: `entities/theory/**`(체험 우주 모달), `entities/appearance/**`·`features/switch-appearance/**`·테마 CSS(`app/styles/index.css` `--ld-*`)(테마 제거는 별도 태스크), 백엔드·proto·DB 전부.
- SSOT 반영 대상(완료 후): `spec/policy/ux/landing.md`, `spec/plan/15.landing-revamp.md`, `spec/policy/ux/motion-accessibility.md`.

## 검증 / DoD
- [ ] 위 **인수 조건**(A1–A14) 전 항목이 현재 코드에서 참
- [ ] (type=change) 기존 plan 15 수용 기준 회귀 없음 — 단, 인터랙션 모델 교체로 **의도적으로 바뀌는** 항목(카드별 자족 인터랙션)은 새 무대 모델로 대체됨을 plan 반영에 명시. 테마 전환·데모 진입·곡선 시냅스·reduced-motion·이론 모달은 회귀 없음.
- [ ] FE `pnpm --filter ./frontend build`·`lint`·`steiger` 통과
- [ ] 헌법 sanity: 무대 시각화가 별·링크를 삭제하지 않고 밝기만 낮춤, 원본 문장 불변 병치

## 리뷰
- [ ] `/code-review` 반영(기각은 이유) · 비자명 시 `/codex:review --background`

## 완료 후 — SSOT 반영
- [ ] [plan/15.landing-revamp](../plan/15.landing-revamp.md)을 새 현실로 갱신(셸=상단 고정 무대+스크롤 콘텐츠, 장 아크, 인터랙션 모델) · [policy/ux/landing.md](../policy/ux/landing.md) §이론 카드 표→무대 인터랙션 모델·카피 정정 · [policy/ux/motion-accessibility.md](../policy/ux/motion-accessibility.md) 무대 애니메이션 reduced-motion
- [ ] `changes/31.landing-pinned-stage-redesign.md`를 `changes/archive/`로 이동
- [ ] 00.overview 진행 현황 ✅(행 제거) · 이 문서 frontmatter `status: done`
