---
job: '143'
type: 'refactor'
source: 'code-review/12.frontend-rendering-performance-audit'
plan: 'none'
status: todo
title: 'Diary archive list virtualization'
---

# Job 143: Diary archive list virtualization (refactor)

> Implementation work doc. Source spec: [code-review/12.frontend-rendering-performance-audit](../code-review/12.frontend-rendering-performance-audit.md).
> /cosimosi:implement-job 143 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

> **Deliberately deferred.** Review 12 records R015 as a cliff-with-a-number, not a today-problem: the web lists are
> fine at MVP scale, and mobile's diary list already windows via `FlatList`. Implement when archive entry counts
> warrant it (dogfooding shows a slow archive, or entries reach the high hundreds) — until then this job sits in the
> backlog on purpose. Do not pick it up ahead of jobs 136–142.

## Acceptance Criteria (from code-review/12 — R015)

- [ ] A1 **The web diary archive windows its rows.** `DiaryList` renders only the visible window (+overscan) via
      `@tanstack/react-virtual`; keyset paging (the IntersectionObserver sentinel) keeps working; DOM node count
      stays bounded as the user pages through a year of entries.
- [ ] A2 **UX is unchanged.** Scroll position, keyboard navigation/focus order, and the empty/loading states behave
      exactly as before; no layout shift from row-height estimation (measure or fix row heights).
- [ ] A3 **The other unwindowed lists are decided, not forgotten.** `TwinkleLedgerList` (its "No virtualization
      library" note — and mobile's deliberate non-FlatList nesting — reviewed) and `AchievementList` are either
      virtualized in the same pattern or their deferral re-recorded here with the reason.
- [ ] A4 **No behavior change / no regression.** Full gate green; list feature tests updated alongside.

## Implementation Checklist

- [x] T001 Precondition check (this is a deferred job): confirm with current data that the archive actually needs
      it now (entry counts / measured scroll jank); if not, stop and leave the job in backlog — record the
      measurement here either way. **→ Not met (2026-08-07); see _T001 measurement_ below. T002–T005 not started.**
- [ ] T002 Add `@tanstack/react-virtual` to `apps/web` (per `/library-setup`: current docs, latest version).
- [ ] T003 `apps/web/src/features/read-diary-list/ui/DiaryList.tsx:112`: window the `diaries.map` rows; keep the
      sentinel-based keyset paging (`:77`) by placing the sentinel after the virtualized range; handle
      variable-height rows via the library's measurement API. The list is **window-scrolled**, not
      element-scrolled (`window.scrollTo` reset at `:67`, viewport-rooted IntersectionObserver at `:77`; the
      `<main>` expands with its content rather than defining a fixed scroll viewport): use `useWindowVirtualizer`,
      NOT the scroll-element `useVirtualizer`, do not wire `getScrollElement` to `<main>`, and account for the
      list's document offset/scroll margin per the current library docs.
- [ ] T004 Extend the feature's tests: bounded DOM count for a large page, paging still fires at the sentinel,
      focus/scroll behavior pinned.
- [ ] T005 A3's decision pass over `TwinkleLedgerList` / `AchievementList` (virtualize or re-defer with reason);
      then `pnpm check` green.

## T001 measurement (2026-08-07) — precondition NOT met

Server-rendered `DiaryList` at increasing row counts (throwaway probe, not committed):

| rows | DOM elements | per row | HTML   |
| ---- | ------------ | ------- | ------ |
| 20   | 211          | 10.6    | 18 KB  |
| 100  | 1,043        | 10.4    | 87 KB  |
| 500  | 5,203        | 10.4    | 437 KB |
| 1000 | 10,403       | 10.4    | 873 KB |

A closed row costs ~10.4 elements — an `li`, its button, the date, the preview and the mood dots, none of them
animated. Two things keep this off the critical path today. The DOM holds **what has been paged in, not what the
archive contains**: `diary_reader.page_size` is 20 and rows accumulate only as the sentinel fires, so reaching even
5,000 elements takes a user deliberately scrolling 25 consecutive pages in one session — an archive of a thousand
entries opens at 211 elements like every other one does. And the product is still invite-gated pre-launch, so no real
archive is anywhere near the "high hundreds" the review named as the trigger.

Re-run this probe when dogfooding reports a slow archive, or when one account's entry count passes ~500 — the
arithmetic above turns straight into a decision.

## Grounding

- Source finding: [code-review/12](../code-review/12.frontend-rendering-performance-audit.md) R015 (P3, explicitly
  deferred).
- Architecture: [ARCHITECTURE.md](../ARCHITECTURE.md) §3.1 (feature slice owns its list UI; the change stays inside
  `features/read-diary-list`). Invoke `/fe-architecture`; `/library-setup` before adding the dependency.
- Mobile parity note: mobile's `DiaryList` already uses `FlatList`; the mobile ledger's non-FlatList nesting is a
  documented exception — web-only work here.
- tech / policy / values touched: none.

## Affected files (blast radius)

- `apps/web/package.json` · `pnpm-lock.yaml` — `@tanstack/react-virtual`
- `apps/web/src/features/read-diary-list/ui/DiaryList.tsx` (+ tests) — windowed rows
- `apps/web/src/features/twinkle-ledger/ui/TwinkleLedgerList.tsx` · `apps/web/src/features/achievement-list/ui/AchievementList.tsx`
  — only if A3 decides to virtualize them too

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
