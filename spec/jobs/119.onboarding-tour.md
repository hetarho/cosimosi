---
job: '119'
type: 'new'
source: 'plan/80.onboarding-tour'
plan: 'plan/80.onboarding-tour'
status: todo
title: 'onboarding tour'
---

# Job 119: onboarding tour (new)

> Implementation work doc. Source spec: [plan/80.onboarding-tour](../plan/80.onboarding-tour.md).
> /cosimosi:implement-job 119 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

## Acceptance Criteria (from plan/80.onboarding-tour)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [ ] A1 The tour runs on [78.sequence-engine](78.sequence-engine.md)'s engine, unmodified, over the **real** universe and the user's own first diary; the only difference from [79.demo-page](79.demo-page.md) is the injected script, and no branch, flag or host-kind field is added to `packages/sequence` ([O1]; [79.demo-page](79.demo-page.md)'s isolation rule).
- [ ] A2 The tour performs **no** product action: it issues no RPC, writes no read model, seeds no star, advances no clock and spends no Twinkle. Every state change during a run is the user's own press through the shipped [27.writing-flow-ui](27.writing-flow-ui.md) slices ([O2][I10][I2][G1][U2]).
- [ ] A3 `OnboardingAnchor` and `OnboardingSignal` are closed string-literal unions of exactly five and three members; no recall, deletion, clock, palette, ornament, purchase or star member exists, so such a step is a compile error rather than a review finding ([G1][I1][I10][I11][I5]).
- [ ] A4 `packages/onboarding` depends only on `@cosimosi/sequence` and `@cosimosi/i18n`; it imports no `@cosimosi/demo`, no `@cosimosi/api-client`, no domain-logic package, and none of the universe read-model store symbols, enforced by an import rule on the package ([I13][U2]).
- [ ] A5 Anchors are registered only by wrapping existing children at `pages/universe` and `widgets/writing-flow` in both apps; **no `features/*` or `entities/*` slice imports `@cosimosi/onboarding` or `@cosimosi/sequence`**, and no prop, flag or callback is threaded into any product slice ([I13], [78](78.sequence-engine.md) AC 11).
- [ ] A6 `reportSequenceSignal(signal): void` returns nothing and accepts nothing but a signal id, so no reporting site can read whether a tour is running or branch on it; with no run active the report is inert, and a duplicate or stale report cannot advance the run ([O2], [78](78.sequence-engine.md) AC 6).
- [ ] A7 The script is the nine steps above in that order, each with an i18n caption accessor that takes **no parameters**, so no domain value can appear in a caption and one script is true for both a first-run account and a replay over a populated universe ([O1][O2]).
- [ ] A8 Every step that represents user progress advances on a signal, never on a dwell timer; a failed split or launch leaves the tour on its current step with no error surface, no timeout and no retry of its own ([O2]).
- [ ] A9 The tour starts on exactly two triggers, each consumed once — [63.signup-flow-ui](63.signup-flow-ui.md)'s signup-completion signal, and an explicit replay request — and on no other; entering the universe on any later session starts nothing ([O3]).
- [ ] A10 **No "already seen" record exists** on the server or the client: no `users` column, no `AccountService` field, no `localStorage` key. "최초 1회만" holds because `SignUp` succeeds at most once per account ([O3]; [60.account-profile-model](60.account-profile-model.md)).
- [ ] A11 After signup completes, the tour's first caption is the first thing rendered over the universe, without a blocking interstitial route and without remounting the canvas; the shipped first-run welcome and single 일기 쓰기 affordance are untouched beneath it ([O3][U2][V7]).
- [ ] A12 A skip is visible on every step and ends the run immediately with no confirmation and no re-offer; `completed`, `skipped` and `abandoned` all leave nothing behind ([O4]).
- [ ] A13 `/me`'s profile tab carries the onboarding replay row as its last row; activating it returns the user to the universe and starts a fresh run from step 1 with no teardown and no residue from the previous pass ([O5]).
- [ ] A14 `resetOnboardingUserState()` clears the start request and the signal channel and is registered in both apps' shared user-scope reset inventory, so a run and a pending replay cannot cross an account boundary ([U1]).
- [ ] A15 The unit ships on **web and mobile** with no waiver: the script, the unions and the start policy shared verbatim from `packages/onboarding`, each app hand-writing its own `ui`, `lint:fsd:layout` R4 clean ([00.overview](00.overview.md) §4, ARCHITECTURE §3.5).
- [ ] A16 All tour copy resolves through the i18n seam under `sequence_*` in both catalogues in the restrained literary voice, with `pnpm lint:raw-strings`, `pnpm check:gen`, `pnpm lint`, `pnpm typecheck`, `pnpm typecheck:mobile`, `pnpm test:web` and `pnpm build:web` green and **no** BE change.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate)/(gen:values). -->

> **Lands after [job 117](117.sequence-engine.md)** (the engine, its chrome and the `sequence` values group). **No
> parity waiver** — unlike the demo, the tour ships on both apps, which is why the script is a package. It adds no
> values key, no proto, no migration. Invoke `/fe-architecture` · `/mobile-architecture`.

**The package (`@cosimosi/onboarding`) — the fifth of v2's five new FE packages**

- [ ] T001 `packages/onboarding/package.json` — depends on `@cosimosi/sequence` + `@cosimosi/i18n` and **nothing else**.
      It cannot live inside `packages/sequence` (whose dependency list is pinned to `xstate` + `zustand` precisely so
      plan 79's import closure can admit it, and a caption accessor needs i18n) and it cannot be app-local `model`
      (byte-identical on both apps → lint R4).
- [ ] T002 `src/anchors.ts` — the **two closed unions**, and they are the whole safety argument:
      `OnboardingAnchor = 'universe-write-entry' | 'writing-draft' | 'writing-proposal' | 'writing-confirm' |
'universe-clock'` and `OnboardingSignal = 'writing-flow-opened' | 'split-succeeded' | 'launch-succeeded'`. A step
      naming anything outside those eight strings is a compile error. Record what they omit and why: **no recall anchor
      or signal** (a step waiting on a paid reconsolidation), **no 놓아주기/delete anchor** (a tutorial that destroys a
      memory), **no clock or sync signal** (a step moving universe time to make dimming visible), **no
      palette/ornament anchor** (changing a render parameter for effect), **no star anchor** (a star's position comes out
      of the force-sim and changes every frame — there is no composition site to register and no rect to measure), and
      **no anchor inside a `features/*` slice**.
- [ ] T003 `src/script.ts` — `ONBOARDING_SCRIPT = defineScript<OnboardingAnchor, OnboardingSignal>(…)`, the nine steps:
      `welcome` (dwell) · `entry` (signal `writing-flow-opened`) · `draft` (signal `split-succeeded`) · `proposal`
      (dwell) · `confirm` (signal `launch-succeeded`) · `arrival` (dwell) · `clock` (dwell) · `revisit` (dwell) ·
      `closing` (dwell). Every step representing progress the user must make is a **signal** step; `dwell` is reading
      time only. **Step 8 is the deliberate stopping point**: recall costs Twinkle and requires consent, so the tour
      _names_ recall and stops rather than walking a first-minute user into a spend.
- [ ] T004 [P] `src/signal-channel.ts` — `useOnboardingSignalStore` + `reportSequenceSignal(signal): void`, pushing
      `{signal, nonce}` into a Zustand channel (the shipped `useRecallTargetStore` / `useDeletionTargetStore` shape).
      **The signature is the guard**: it takes one id and returns nothing, so a reporting site cannot learn whether a
      tour is running, cannot read a step index, and cannot branch on either.
- [ ] T005 [P] `src/start.ts` — `takeOnboardingStart(): 'signup' | 'replay' | null`, a pure take-once read, plus
      `requestOnboardingReplay()`. That is the **entire** exposure policy: the signup completion signal plan 63 already
      sets, a replay request, or nothing. **No "already seen" record exists, on the server or the client, and adding one
      is out of scope by decision** — plan 60 closed the `users` shape and the RPC inventory, and a `localStorage` flag
      would turn "가입 직후 최초 1회" into "once per browser". The guarantee is a property of the trigger: plan 63's
      profile gate makes `SignUp` reachable only while no `users` row exists, so the signal has no second occasion.
- [ ] T006 [P] `src/user-state-reset.ts` — `resetOnboardingUserState()`, plus `'onboarding'` in
      `packages/auth/src/user-state-reset.ts`'s shared inventory (the tour runs inside a real session, so an
      unregistered store would leak one account's run into another's subtree).

**The hosts — the tour observes and reports; it never intercepts**

- [ ] T007 `pages/universe` owns the run actor with `useActorRef` (exactly as it already owns
      `universeNavigationMachine`), calls `takeOnboardingStart()` on mount, subscribes to the signal channel and
      translates a matching signal into the machine's **echo-guarded** `ADVANCE`. It mounts `widgets/sequence-guide`
      above the HUD so the caption band, the spotlight and the always-visible skip render over the live scene **without
      remounting the renderer**.
- [ ] T008 Register the five anchors by **wrapping an existing child at a composition site**: `pages/universe` wraps the
      일기 쓰기 cluster and the `UniverseTimeOverlay`; `widgets/writing-flow`'s sheet wraps its three inner panels around
      the shipped `features/write-diary`, `features/split-diary` / `features/revise-split` and `features/launch-stars`
      children. A widget is a composition site, not a product slice, so **no `features/*` or `entities/*` slice imports
      `@cosimosi/sequence` or `@cosimosi/onboarding`** and nothing is passed down through a wrapper — no prop, no flag,
      no callback.
- [ ] T009 Report the three signals from the writing-flow sheet's existing `OPEN`, `SPLIT_OK` and `LAUNCH_OK`
      transitions. The sheet's own behaviour is unchanged with or without a tour: with no run active the channel write is
      inert, and the engine's index echo makes a stale or duplicated report a no-op. **The tour cannot get ahead of the
      product** — a failed `SplitDiary` returns the shipped machine to `writing` and emits nothing, so step 3 simply
      holds its caption; there is no retry logic, no timeout and no tour-owned error surface.
- [ ] T010 `features/replay-onboarding/ui` — the `/me` profile-tab row: it calls `requestOnboardingReplay()` and then the
      page's existing exit callback (the page imports no router), the universe page mounts, `takeOnboardingStart()`
      returns `'replay'`, and the run begins at step 1. **No teardown is needed** — a replay is `START` with a fresh run
      id. A replay over a populated universe behaves identically; steps 3–5 wait for a genuine second diary, which is a
      welcome act rather than a cost. Scoped `'fsd/insignificant-slice': 'off'` block with a stated reason.
- [ ] T011 All three outcomes end the same way: `completed`, `skipped` and `abandoned` (the route unmounted) leave the
      chrome gone and **nothing recorded**. The tour draws no distinction between finishing and skipping — no "are you
      sure", no re-offer, no follow-up prompt, because replay makes a mis-skip cheap and a nagging tutorial is worse than
      a missed one.

**Copy, tests, SSOT**

- [ ] T012 Captions under the existing `sequence_*` prefix in a `sequence_tour_*` sub-namespace (v2 adds no
      `onboarding_*` prefix): the nine `_caption` keys plus `sequence_tour_replay_action` and
      `sequence_tour_replay_description`. **Every caption is a zero-parameter accessor** — no star name, memory count,
      date or Twinkle amount can appear — which is exactly what lets one script be true for an empty first universe and
      for a replay over a full one. Both catalogues, `pnpm gen:messages`, `check:gen`, `lint:raw-strings`. Voice:
      unhurried, second-person, no exclamation, no decorative emoji; [I12] honesty applies even behind auth.
- [ ] T013 Tests: a step naming an unknown anchor fails to typecheck (a type-level assertion); `takeOnboardingStart`
      returns each trigger exactly once and `null` thereafter; a signal reported with no active run is inert; a stale or
      duplicated report does not double-advance; a failed split holds the step; skip and completion are indistinguishable
      in their after-state; the writing-flow sheet's behaviour is unchanged with the tour absent; sign-out clears the
      run.
- [ ] T014 SSOT at implement: append an `## Onboarding` section to `spec/policy/ux/sequence.md` (job 117 creates the
      doc) — the tour runs once at signup and thereafter only on an explicit replay, is never re-offered, nagged or
      confirmed before a skip, points and narrates and never acts (no star seeded, no clock moved, no Twinkle spent,
      nothing deleted), highlights no paid or destructive control (which is why recall is named at step 8 and not
      performed), and every caption must be true for both an empty and a full universe. Record the two closed unions and
      their omissions, the anchor-wrapper rule, `reportSequenceSignal`'s void-in-void-out signature with its three call
      sites, and the page-owned run actor in `spec/tech/state-machine.md`.

## Grounding

- Constitution: [00.overview](../plan/00.overview.md) §3. Load-bearing **because the tour runs over a real session**:
  **[I10]** (no step moves universe time), **[I2]** (the diary is the user's own write), **[I11]** (the meaning layer is
  untouched), **[G1]** (recall stays paid and consented — hence step 8 stops), **[I1]** (nothing is deleted), **[I5]**
  (no star anchor, because a position is emergent), **[I13]** (no product slice learns a tour exists).
- Architecture: [ARCHITECTURE.md](../ARCHITECTURE.md) **§3.1** (a package because it is byte-identical on both apps —
  R4; anchors register at composition sites, never in a product slice), **§3.2** (the run actor is control state owned by
  the page, the signal channel is a one-slot mirror), **§3.5** (each app hand-writes its `ui`; no `*.native.tsx`
  sibling). Invoke `/fe-architecture` · `/mobile-architecture`.
- tech / policy / values touched: appends to `policy/ux/sequence.md` and `tech/state-machine.md`. **No new values key.**
- Builds on plan 78 (job 117 — the engine, the chrome, `defineScript`), plan 63 (the one-shot signup completion signal),
  plan 27 (the writing-flow sheet whose transitions report), plan 64 (the `/me` row's host).

## Affected files (blast radius)

<!-- placement: FE package + layer/slice/segment (§3.1) -->

- `packages/onboarding/**` — **NEW**: `src/{anchors,script,start,signal-channel,user-state-reset,index}.ts`.
- `packages/auth/src/user-state-reset.ts` — `'onboarding'` added to the shared reset inventory.
- `apps/{web,mobile}/src/pages/universe/ui/**` — the run actor, `takeOnboardingStart()`, the signal subscription, the
  `sequence-guide` mount, and two anchor wrappers.
- `apps/{web,mobile}/src/widgets/writing-flow/ui/**` — three anchor wrappers + three `reportSequenceSignal` calls on the
  existing transitions.
- `apps/{web,mobile}/src/features/replay-onboarding/ui/**` — **NEW** `features` slice (the `/me` row).
- `apps/{web,mobile}/src/pages/me/**` — hosting that row.
- `packages/i18n/messages/{en,ko}.json` + generated output · `apps/web/steiger.config.js` (one scoped exemption).
- **Unchanged on purpose:** every `features/*` product slice (`write-diary`, `split-diary`, `launch-stars`, …) — they
  never learn a tour exists.

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
