---
job: "25"
type: change
source: changes/14.dead-code-and-refactor-cleanup
plan: plan/06.universe-canvas
status: doing
title: 죽은 코드·미사용 정리 + 구조 리팩터
---

# Job 25: 죽은 코드·미사용 정리 + 구조 리팩터  (change)

> 구현 작업 문서. 출처 스펙: [changes/14.dead-code-and-refactor-cleanup](../changes/14.dead-code-and-refactor-cleanup.md).
> `/implement-job 25`이 아래 두 체크리스트로 구현한다. 끝나면 SSOT(plan/policy/values) 반영, status: done.
> **순수 정리·이관·분해 — 동작·시각·카피 무변경. codegen/migrate/values 없음.**

## 인수 조건 (acceptance — 기획 관점, changes/14.dead-code-and-refactor-cleanup에서)
<!-- 출처 스펙의 수용 기준. 구현 후 각 항목이 코드에서 참인지 검증한다. -->
- [x] A1 `knip`이 미사용 파일·export·의존성 0 보고(예외: `*/gen/`·`*.gen.ts`·`*_pb.ts`·`steiger.config.ts`·`vite.config`).
- [x] A2 `eslint .` green, `pnpm --filter ./frontend build`·`vitest run` green.
- [x] A3 BE `go build ./...`·`go vet ./...`·`go test ./...` green(`buildLinks` 제거에 따른 테스트 재작성 포함).
- [ ] A4 UniverseCanvas·HomePage·worker.go가 책임 단위로 분해되고 분해 전후 **시각·동작 회귀 없음**(데모·실유니버스 수동 확인). UniverseCanvas·worker.go 완료, HomePage hook 추출(T008)은 job 24 충돌로 미수행.
- [x] A5 중복 헬퍼(`newID` 등)가 단일 출처로 수렴하고 호출부가 전부 이관된다.
- [x] A6 삭제 심볼을 인용하던 `spec/**` `.md` 라인이 갱신돼 dangling 참조 0.
- [x] A7 inert seam 코드(`ExcitabilityGain`·재공고화 reshape)가 보존된다([job 26] 입력).

## 구현 체크리스트 (implementation — 구현 관점)
<!-- 위→아래(안전한 삭제 → deps → 중복 수렴 → god-file 분해). [P]=다른 파일·무의존 병렬. codegen/migrate/values 없음. -->
- [x] T001 [P] 미사용 의존성 제거: `pnpm --filter ./frontend remove @tanstack/react-virtual class-variance-authority date-fns @tanstack/router-plugin` → build/typecheck. (`react-query-devtools`·`react-router-devtools`는 `DevTools.tsx` 재활성화 레시피라 **보존**.)
- [x] T002 죽은 FE 파일·피처 삭제: `features/dormant-search/`(3) + `entities/memory/api/dormant-query.ts`의 `dormantQueryOptions`(동시 — **`dormantInvalidateKey`는 보존**) · `features/universe/`(셸 스토어 3) · `shared/ui/OverlayHost.tsx`(+`ui/index.ts` export) · `entities/synapse/ui/SynapseLines.tsx`(+`synapse/index.ts:4`·`mapping.ts:27 alpha`) · `entities/appearance/model/themes.ts` shim · `shared/lib/demo/observe.ts`(+`demo/index.ts:45`) · `shared/lib/r3f` 추상 렌더러 타입(`GraphRenderer`/`StarNode`/`SynapseEdge` — **`CameraMode` 보존**).
- [x] T003 죽은 BE 코드 삭제: `internal/ai/seed.go`(`SegmentSeed`/`normalizeText` — `collapseSpace`가 타처 사용이면 이동) + 테스트 · `internal/job/worker.go:buildLinks` 제거 + `worker_test.go`를 prod 경로 `biasedLinks` 기준으로 재작성 · `admin.Repository.UserExists`(인터페이스+`repository_pg`+fakeRepo) 제거, prod unexported `r.userExists` 유지.
- [x] T004 barrel/export 정리(knip 잔여 0까지): `navigation.machine.ts:231-232 selectIsOverlay`/`selectIsFramingPair`(정의+export) · `focus.machine.ts selectIsPairFocus` · `@x/theory` decay 상수 barrel · `appearance/index.ts` 죽은 9개 · recall·evolution·record-memory·star-explorer·wayfinding barrel 잉여 · `toSnapshotVM`/`membershipStatusKey`는 `export`만 제거(함수 유지). **R_MIN/R_MAX·A_MIN·MOOD/MoodKey·selectFramingPair·UserEmailFromContext 보존(오탐).**
- [x] T005 [P] BE 중복 수렴: `newID()` 6패키지 → `internal/platform/id`(`New()`/`NewN()`; `invite.newCode`는 별개) · `moodToProto/From` 3핸들러 → 단일 변환(`settings` strict 별도) · "missing auth user" 가드 ~21곳 → `rpcserver` 헬퍼 · nullable pgtype 매퍼 5패키지 → `internal/db`.
- [x] T006 [P] FE 중복 수렴: FNV-1a 해시 `SynapseFilaments`/`SynapseDust` → `synapse/lib/hash.ts`(byte-identical 계약 유지) · 13-mood quadrant 배열 `GiftPage`/`MemoryForm` → 파생 상수.
- [x] T007 god-file — `widgets/universe-canvas/ui/UniverseCanvas.tsx`(2062L): 상수벽(208-272)→`model/nav-tuning`, R3F 컨트롤러→`ui/controllers/`, NavController/LiveLayout 분리. **per-frame pump·positionsRef·시각 회귀 없음.**
- [ ] T008 god-file — `pages/home/ui/HomePage.tsx`(1055L): `useDemoFlow`/`useTutorialTour`/`useUniverseSurfaces` 훅 추출. **Esc 순서·rAF setState 보존.** ⚠️ **job 24(demo-tour 머신화)가 HomePage 471-563을 재배선하므로 충돌 — job 24 완료/머지 후 진행.**
- [x] T009 god-file — `internal/job/worker.go`(613L): excitability/link 순수 헬퍼→`excitability.go`(consolidate.go 선례). **inert `ExcitabilityGain` seam은 보존**(job 26 배선 대상) · union-find 클러스터(worker/consolidate 2벌) `clusterByUnionFind` 추출.
- [x] T010 dangling 참조 동기화: 삭제 심볼을 인용하는 `spec/policy/**`·`spec/tech/**`(overlay-shell·navigation·universe·architecture)·`SSOT-AUDIT.md` grep → 갱신(문서=규칙).
- [x] T011 검증: `knip` 미사용 0(예외 목록) · `eslint .`·FE `build`·`vitest run` · BE `go vet/build/test ./...`(Docker) · UniverseCanvas/HomePage 데모·실유니버스 수동 회귀. **codegen/migrate/values 해당 없음.**

## 참고 (grounding)
- 헌법(불변 원칙): [00.overview](../plan/00.overview.md) §불변 원칙 — 정리도 헌법을 안 깬다(특히 데이터 행 삭제·`records` UPDATE 금지는 이 변경과 무관하나 sanity 유지).
- 출처: [changes/14](../changes/14.dead-code-and-refactor-cleanup.md). 정적 신호: `knip`(미사용 파일 10·export 168·dep 3), `eslint`(clean).
- 짝 변경: [job 26](26.wire-inert-core-logic.md) — 26이 살릴 inert seam(`ExcitabilityGain`·reshape/peGate)은 이 job에서 삭제 금지. demo-tour 영역은 [job 24](24.demo-tour-state-machine.md)와 충돌(T008).
- 건드리는 tech/policy/values: 완료 후 — 삭제 심볼 인용 `policy/**`·`tech/**` 동기화. `spec/values.yaml` **변경 없음**.

## 영향 파일 (blast radius)
<!-- 이 범위 밖은 안 건드린다. -->
- **삭제** — `frontend/src/features/dormant-search/**` · `features/universe/**` · `shared/ui/OverlayHost.tsx` · `entities/synapse/ui/SynapseLines.tsx` · `entities/appearance/model/themes.ts` · `shared/lib/demo/observe.ts` · `shared/lib/r3f/types.ts`(추상 타입만) · `backend/internal/ai/seed.go`(+테스트) · `backend/internal/job/worker.go`(`buildLinks`).
- **barrel/export 수정** — `entities/{memory,synapse,appearance,theory}/index.ts` · `features/{recall,evolution,record-memory,star-explorer,wayfinding}/**/index.ts` · `shared/{ui,config,lib}/index.ts` · `widgets/universe-canvas/model/navigation.machine.ts` · `entities/memory/model/focus.machine.ts`.
- **중복 수렴(신규/수정)** — `backend/internal/platform/id/`(신규) + `db/fragment·memory·admin·gift·job·invite` 호출부 · `internal/platform/rpcserver`(auth 헬퍼) · `internal/db`(pgtype 매퍼) · mood 변환 핸들러(gift·share·memory) · `frontend/src/entities/synapse/lib/hash.ts`(신규) + `SynapseFilaments`/`SynapseDust` · `pages/gift`·`features/record-memory`(mood quadrant).
- **god-file 분해** — `frontend/src/widgets/universe-canvas/ui/UniverseCanvas.tsx`(+`model/`·`ui/controllers/` 신규) · `frontend/src/pages/home/ui/HomePage.tsx`(+훅 신규) · `backend/internal/job/worker.go`(+`excitability.go` 신규).
- **테스트** — `backend/internal/job/worker_test.go`(buildLinks→biasedLinks 재작성) · `internal/admin/*_test.go`(UserExists 인라인화).
- **문서** — 삭제 심볼 인용 `spec/policy/**`·`spec/tech/**`·`SSOT-AUDIT.md`.
- **보존(미수정 확인만)** — `CameraMode`·`R_MIN`/`R_MAX`·`A_MIN`·`MOOD`/`MoodKey`·`dormantInvalidateKey`·`UserEmailFromContext`·`ExcitabilityGain`·재공고화 reshape·`values_gen.go` FE 전용 값.

## 검증 / DoD
- [ ] 위 **인수 조건**(A1–A7) 전 항목이 현재 코드에서 참 — A4의 HomePage hook extraction만 job 24 충돌로 남음
- [x] (type=change) 기존 plan 수용 기준 회귀 없음 — 동작·시각·카피 무변경(데모·실유니버스 수동)
- [x] codegen/migrate/values **해당 없음**
- [x] FE `pnpm --filter ./frontend build`·`lint`·`vitest run` / BE `go vet ./... && go build ./... && go test ./...`(Docker) 통과
- [x] `knip` 미사용 파일·export·dep 0(예외 목록 외)
- [x] 헌법 sanity: `records`/`memories`/`memory_links` 행 삭제·`records` 본문 UPDATE 없음(이 변경은 코드 정리만)
- [x] inert seam(`ExcitabilityGain`·reshape) 보존 확인(job 26 입력)

## 리뷰
- [x] 로컬 diff review 완료(추가 수정 없음). Codex CLI review는 usage limit으로 실행 불가(Jun 22, 2026 04:12까지 제한).

## 완료 후 — SSOT 반영
- [x] `plan/06`(+분해로 바뀐 구조) as-built 갱신 · 삭제 심볼 인용 `policy/**`·`tech/**`(overlay-shell·navigation·universe·architecture) 동기화 · `SSOT-AUDIT.md` 해당 항목 정리 · `spec/values.yaml` 변경 없음
- [ ] `changes/14.dead-code-and-refactor-cleanup.md`를 `changes/archive/`로 이동
- [ ] 00.overview 진행 현황 ✅(행 제거) · 이 문서 frontmatter `status: done`
