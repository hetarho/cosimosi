---
job: '141'
type: 'refactor'
source: 'code-review/12.frontend-rendering-performance-audit'
plan: 'none'
status: todo
title: 'Per-frame efficiency and adaptive quality'
---

# Job 141: Per-frame efficiency and adaptive quality (refactor)

> Implementation work doc. Source spec: [code-review/12.frontend-rendering-performance-audit](../code-review/12.frontend-rendering-performance-audit.md).
> /cosimosi:implement-job 141 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/values) and set status: done. Write it in English.

Cuts the steady-state per-frame cost and adds the missing quality levers. Today five instanced layers recompose and
re-upload every matrix every frame even when the coordinate buffer did not change (R009), and the only quality
control anywhere is a static DPR cap — no fps-adaptive DPR, no background gate, and reduced motion freezes time but
keeps full frame cost (R010). Measurement lands first so every change gets a number.

**Build after jobs 136 and 138.** T007/T008 extend `UniverseCanvas.native.tsx`, whose effect structure 136 rewrites;
until 136 lands, a `dpr` change disposes the WebGPU device instead of using the live backing-size seam. The shared
adaptive layer mounts in job 138's scene core, while the web-only diagnostics HUD stays in the web thin shell (a
shared package must not import an app-only dependency). T002's regen serializes with jobs 140/142.

## Acceptance Criteria (from code-review/12 — R009, R010)

- [ ] A1 **Clean frames are (near-)free for contiguous-slot layers.** When the coordinate buffer version, the
      channels, slot offset, scale, and explicit animation revision are unchanged, `InstancedNodeLayer` skips
      matrix recomposition and does not set `instanceMatrix.needsUpdate`. T003 keeps the published version stable
      across visually unchanged ticks on both bridge arms, including inline mobile. Genuine animation still moves:
      projection mappers such as gist rise opt out of the skip, and `AwakenNeuron` supplies an active-flare matrix
      revision while its in-place scale array changes. (R009.)
- [ ] A2 **The awaken layer is idle-free.** With no flare playing, `AwakenNeuron` writes no matrices (today it
      rewrites its full 32-slot capacity every frame); an active flare still updates every frame until completion.
- [ ] A3 **Per-frame name lookups are gone from the hot loop** (the `geometry.getAttribute(name)` per frame — held
      as a ref; the gist rise-state `seen.get(nodeId)` per instance per frame — indexed against the committed
      instance order whenever that snapshot/reconciliation changes).
- [ ] A4 **DPR adapts to sustained fps.** A small monitor steps the canvas DPR between 1 and
      `rendering.max_pixel_ratio` on sustained-fps windows with hysteresis (no oscillation); thresholds/windows are
      values-seamed (gen:values). Works on both platforms (web `setDpr`; native's manual sizing path).
- [ ] A5 **A backgrounded native app stops rendering.** `AppState !== 'active'` pauses the native frameloop (and
      the inline sim pump); returning foregrounds cleanly. (Web already inherits rAF's hidden-tab pause.)
- [ ] A6 **The change is measured.** A dev-only perf HUD (`r3f-webgpu-perf`, behind the existing diagnostics gate)
      is available on web, and this doc's Result section records before/after frame cost for: converged scene,
      acceleration sweep, and launch — desktop + one phone.
- [ ] A7 **No visual/behavior change** at steady fps: same output, same interactions; full gate green.

## Implementation Checklist

- [ ] T001 Measurement first: per `/library-setup`, add the current `r3f-webgpu-perf` as a dev-only dependency and
      mount it behind the diagnostics flag in the post-job-138 web thin shell (`/test` gating precedent); capture
      baseline numbers for A6's three scenarios.
- [ ] T002 (gen:values) `spec/values.yaml`: add `rendering.adaptive_dpr_*` (target fps window, step-down/step-up
      thresholds, hysteresis) and a coordinate-publication epsilon small enough to be visually sub-pixel at the
      normal camera envelope; run `pnpm gen:values`.
- [ ] T003 `packages/universe/src/sim-bridge.ts`: expose a monotonic published-coordinate **version** through the
      `CoordinateBufferRef` seam (`InstancedNodeLayer.tsx:13`). Increment on start/remap and whenever a candidate
      buffer differs from the last **presented** buffer beyond T002's epsilon — not merely because worker
      `onmessage` fired or inline `pump` ticked. The inline arm must not expose the force-sim's mutating internal
      `sim.coordinates` as its presented buffer; use the simulation's output-buffer seam/ping-pong candidate so a
      stable version also means stable visible contents. Compare cumulatively against the last presented snapshot,
      so sub-epsilon drift eventually publishes once it becomes visible. Test both arms: start/remap bumps; stable
      or sub-epsilon tick does not; above-threshold/cumulative motion bumps and swaps.
- [ ] T004 `packages/3d-renderer/src/layers/InstancedNodeLayer.tsx:182-247`: skip the per-instance loop and
      `needsUpdate` flags when (published buffer version, channels identity, `count`, `firstNodeIndex`, `scale`, and
      optional animation-revision ref) are unchanged **and** no `getInstancePosition` mapper is present (mappers
      animate per frame and opt out). Keep hide-on-missing-buffer behavior. Tests: same object/version → one
      recompose; same object/new version → recompose; slot-offset or animation-revision change → recompose.
- [ ] T005 [P] `packages/universe-render/src/AwakenNeuron.tsx:91-125`: track whether any flare is active and
      early-out before touching its 32-slot scale array when none is. While active, increment and pass T004's
      animation revision only when the frame updates scales; on completion it becomes stable again. Test idle
      frames write nothing, active frames continue recomposing, and completion returns to the skip path (never use
      a zero-instance draw — preserve the documented WebGPU caveat).
- [ ] T006 [P] Hot-loop lookups: hold the vertex-scale attribute in a ref beside the mesh ref
      (`InstancedNodeLayer.tsx:193-198`); in `packages/universe-render/src/GistStarLayer.tsx:140`, pre-index
      `riseState.seen` entries against the committed `snapshot.instances` order whenever reconciliation changes,
      and update the indexed entry when the frame mapper replaces a rising record with `SETTLED` (`:158-160`).
      `positionSlots` is already indexed at `:69-71`; do not rebuild the wrong lookup or leave the cache stale at
      rise completion.
- [ ] T007 Adaptive DPR: a shared layer in `packages/3d-renderer` sampling frame times in `useFrame` and stepping
      DPR (web: R3F `setDpr`; native: the host's manual backing-store scale) between 1 and
      `VALUES.rendering.maxPixelRatio` per T002's thresholds; mounted from the job-138 shared core (see ordering
      above). Make the child→host bridge explicit: the mobile thin shell owns an adaptive-DPR state value, passes it
      to the outer `UniverseCanvas dpr` prop, and injects a stable step callback into the shared scene/monitor. A
      sustained-window step may cross into React state; per-frame samples may not. The prop change then goes through
      136 T001's live-config effect (in-place backing-store resize), never its device effect. Web continues to use
      R3F `setDpr`. Test the hysteresis logic as a pure function and add a native integration test: sustained slow
      frames lower the backing size while renderer identity stays constant and `dispose`/`init` counts do not move.
- [ ] T008 `packages/3d-renderer/src/canvas/UniverseCanvas.native.tsx`: gate the frameloop on `AppState` — pause
      rendering (and thereby the `FrameTick` sim pump) when backgrounded, resume on foreground; the fork owns its
      test (§3.5): background → no frames; foreground → frames resume, no leaked listeners
      (`apps/mobile/jest.guards.js` quiet).
- [ ] T009 Re-measure A6's scenarios; record before/after in this doc's Result section; `pnpm check` green.

## Grounding

- Source findings: [code-review/12](../code-review/12.frontend-rendering-performance-audit.md) R009 (P3), R010 (P3);
  Questions/Tradeoffs (why `frameloop="demand"` is rejected globally — the ambient sky/twinkle animation is the
  product; DPR is the safe lever).
- Architecture: [ARCHITECTURE.md](../ARCHITECTURE.md) §3.2 (the skip logic must stay ref/uniform-side — no React
  state per frame), §3.3 (renderer reads coords; the buffer-version seam extends that contract), §4 (thresholds are
  values, never hardcoded).
- Research grounding (review's web pass): drei's `PerformanceMonitor`/`AdaptiveDpr` are not adoptable (drei's WebGPU
  line unfinished) — the pattern is hand-rolled here; `r3f-webgpu-perf` is the one maintained WebGPU-aware R3F HUD.
- tech / policy / values touched: `spec/values.yaml` (`rendering.adaptive_dpr_*` + coordinate publication epsilon)
  (gen:values); `spec/tech/rendering.md` (frame-loop/quality contract) if present.

## Affected files (blast radius)

- `packages/universe/src/sim-bridge.ts` (+ test) — published-buffer version + content-aware publication
- `packages/3d-renderer/src/layers/InstancedNodeLayer.tsx` (+ test) — dirty-skip + attribute ref
- `packages/universe-render/src/AwakenNeuron.tsx` · `GistStarLayer.tsx` (+ tests) — idle skip · indexed lookup
- `packages/3d-renderer/src/layers/` — new adaptive-DPR layer (+ pure hysteresis test)
- `packages/3d-renderer/src/index.ts` — export the adaptive layer/contract for the shared scene consumer
- `packages/3d-renderer/src/canvas/UniverseCanvas.native.tsx` (+ test) — AppState gate
- `spec/values.yaml` · `packages/config/src/values.gen.ts` ·
  `apps/api/internal/platform/values/values_gen.go` — adaptive/publication thresholds (gen:values)
- `packages/universe-render/src/**` (job-138 shared scene) — mount the adaptive layer
- `apps/web/src/widgets/universe-canvas/ui/UniverseCanvasWidget.tsx` (post-job-138 thin shell) — mount the app-only
  dev HUD behind diagnostics
- `apps/mobile/src/widgets/universe-canvas/ui/UniverseCanvasWidget.tsx` (post-job-138 thin shell) — own the native
  adaptive-DPR state/callback bridge
- `apps/web/package.json` · `pnpm-lock.yaml` — `r3f-webgpu-perf` (dev-only)
- `spec/tech/rendering.md` — published-coordinate, adaptive-quality, and native background-frame contracts

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [ ] Codegen / migration / values applied (if any): `pnpm gen` / `pnpm db:migrate` / `pnpm gen:values`
- [ ] FE `--filter @cosimosi/web build`·`lint` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [ ] Constitution sanity: no row deletes on `records`/`memories`/`memory_links`, no body UPDATE on `records`
- [ ] Architecture self-audit passed (the relevant `/fe-` `/mobile-` `/be-architecture` checklist): placement correct,
      app-layer segmented (not flat), web↔mobile parity, `pnpm lint:fsd:layout` green (FE)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `policy/**`·`tech/**` · tuned numbers → `spec/values.yaml` (+`pnpm gen:values`)
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
