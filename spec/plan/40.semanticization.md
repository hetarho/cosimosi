# 40. semanticization

> Semanticization — the pure domain model that _rises_ an episodic memory's abstraction over universe-time: `Semanticize`
> (a total, deterministic abstraction-stage progression), the **gist-timer** ([C6a]: units-per-stage in universe-days,
> `values.yaml`), and the **z-axis 2-store gist coordinate** ([C5][C6]) — copy the hippocampal x,y, **raise only z** into
> the reserved neocortex band. The 4 gist-stage _texts_ already exist (pregenerated at launch, plan 20/22); this unit owns
> the STAGE-RISING math, not text generation. Golden-parity for the timer + z-coord, mirrored TS↔Go.
> Scope: BE | Status: planning
>
> Layer D (domain) — the first unit of Epic E (우주의 잠·공고화). Home: the core-domain `internal/memory` context — pure
> types + functions alongside plan 18's plasticity math, sharing the one `packages/memory-logic` (TS) ↔ `internal/memory`
> (Go) golden-parity harness. It fills the semanticization seam that
> [16.memory-aggregate-schema](16.memory-aggregate-schema.md) reserved (`semantic_stage`, `semanticize_timer_reset_at`,
> `semantic_stages`) and the neocortex-band coordinate contract [19.force-sim-layout](19.force-sim-layout.md) reserved
> (z 15–25, "x,y copied, z raised"). The _when_ of advancing — replay-reorg + `Downscale` + firing this stage rise on the
> advance interval — is the use-case [41.consolidation-usecase](41.consolidation-usecase.md) via plan 30's
> `AdvanceProgression` hook; the neocortical-layer render + ascent choreography are [42.gist-star-rendering](42.gist-star-rendering.md).

## Purpose

Over universe-time an episodic memory does not only _fade_ ([F], forgetting) — it also **abstracts**: the concrete "on
Tuesday I ate carbonara with Jinsu and it was so good" compresses toward "a warm dinner with a friend," and a **gist
version rises into the neocortex** as a separate, higher body ([C5]–[C7], complementary learning systems; McClelland
1995). This is the _systems consolidation_ axis, deliberately **independent of forgetting** ([F]): forgetting randomly
_erases words_, semanticization _compresses meaning_. A memory can be both dim and highly gistified, or bright and still
concrete.

The 4 gist-stage **texts** are already written — the semanticize job pregenerates them into `semantic_stages` at launch
(plan 20/22, [C7]), so the _content_ exists from day one. What is missing is the **model of rising through the stages
over time**: a per-memory countdown (the gist-timer, [C6a]) measured in universe-days that, when it fills, advances the
abstraction stage by one; a **z-axis coordinate** for the risen gist body that copies the hippocampal star's emergent
x,y and lifts only z into the reserved neocortex band ([C6][V9][I5]); and an append to the memory's provenance history as
each stage rises ([R8a], CC5). Because the client renders the gist stars every frame and the server later needs the same
stage/coordinate for cost-gating ([R8][G4]) and reads, the timer and z-coord math live once in Go and once in TS, pinned
by golden fixtures — the same discipline as plan 18. This unit owns that model as **pure, IO-free domain functions**; the
orchestration that _fires_ the rise on a clock advance is Epic E's use-case ([41.consolidation-usecase](41.consolidation-usecase.md)),
and the render is [42.gist-star-rendering](42.gist-star-rendering.md).

## Scope / Non-goals

**In scope**

- **`Semanticize` — the pure abstraction-stage progression.** Given the current `semantic_stage` and the number of
  gist-timer units that have elapsed, return the new stage, monotone non-decreasing and clamped to the 5-value ladder
  `0..4` (stage 0 = concrete, no gist risen; stages 1..4 = the four pregenerated gist texts). Deterministic, side-effect-free.
- **The gist-timer model ([C6a]).** A per-memory countdown to the next gist stage: `elapsedUniverseDays / gist_units_per_stage`
  = whole units crossed since `semanticize_timer_reset_at`. One filled unit → one stage. The unit length
  (`semantic.gist_units_per_stage`, e.g. 10 universe-days) is a `values.yaml` scalar. Includes the **arousal / connection
  modulation** ([F6][F7]): higher-arousal and more-connected memories advance _slower_ — the same read-time modulation
  factor forgetting uses, applied to the effective elapsed time.
- **The timer-reset contract.** `semanticize_timer_reset_at` is set to universe-time-now on **recall / reconsolidation**
  (plan 33, [C6a][F5]) so the next gist stage is _delayed_; already-risen stages are **kept** (z-axis one-way, [C7]). This
  unit defines the reset semantics + the read that consumes the anchor; the _write_ on recall is plan 33's.
- **The z-axis 2-store gist coordinate ([C5][C6][V9]).** A pure `GistCoordinate(hippocampalX, hippocampalY, stage) →
(x, y, z)` that **copies the hippocampal star's emergent x,y** and returns a z inside the **reserved neocortex band**
  (`force_sim.neocortex_z_min`..`neocortex_z_max`, 15..25). The neocortex layer has **no force-sim** ([C6][I5]); the gist
  body is placed by copy, not simulated. Higher stages may sit deeper into the band (stage → z within bounds) so the
  ascent reads as progressive.
- **Provenance append on stage rise (CC5).** Each time a stage rises, an append-only `memory_provenance` row
  (`kind = semanticized`, `source = system`, the risen stage's text, `universe_time`) is written — the semanticization
  half of the variant history ([R8a]). This unit specifies _what_ is appended and _when_; the table + append port are
  owned by [32.reconsolidation-rules](32.reconsolidation-rules.md) (CC5); the actual write executes inside Epic E's
  consolidation transaction ([41.consolidation-usecase](41.consolidation-usecase.md)).
- **The `semantic.*` scalars** these functions read (`gist_units_per_stage`, the max-stage ladder length as a derived
  count, the modulation reuse) enumerated for `values.yaml`. The **formulas** (the unit-crossing division, the
  modulation shape, the stage→z placement) stay in code.
- **Golden-parity (TS↔Go)** for the timer (`Semanticize` + elapsed-unit math) and the z-coordinate — identical inputs
  produce identical outputs within a fixed tolerance, pinned by shared fixtures, so the client render and the server
  read agree.

**Non-goals**

- **No gist-text generation.** The 4 gist-stage texts are LLM-pregenerated at launch into `semantic_stages` (plan 20/22,
  [C7]); this unit **reads** the text for the risen stage and never writes or regenerates it. Reconsolidation's
  "regenerate only the remaining stages" ([C7]) is [33.recall-usecase](33.recall-usecase.md).
- **No advance orchestration / no cron ([T4]).** _When_ stages rise is driven by the `AdvanceProgression` hook on the
  universe clock ([29.universe-clock](29.universe-clock.md)/[30.time-advance-usecase](30.time-advance-usecase.md), Epic B).
  Firing the rise over an advance interval `[from,to]`, running replay-reorg ([C2]), and `Downscale` (SHY [C4]) are
  [41.consolidation-usecase](41.consolidation-usecase.md). This unit is a pure model with no clock and no hook.
- **No rendering.** The neocortical-layer gist stars, the z-axis 2-layer depth, and the "빨려 올라가듯" ascent
  choreography are [42.gist-star-rendering](42.gist-star-rendering.md) ([V8][V9][C8]).
- **No force-sim.** The neocortex band has no simulation ([C6][I5]); x,y are _copied_ from the hippocampal layout owned
  by [19.force-sim-layout](19.force-sim-layout.md). This unit does not place hippocampal stars and does not re-run layout.
- **No `Downscale` (SHY).** Homeostatic sleep-time synaptic renormalization ([C4][I9]) is a _different mechanism_ from
  this stage-rising model; it is the `Downscale` fn introduced by [41.consolidation-usecase](41.consolidation-usecase.md).
  Seam only.
- **No forgetting math.** Brightness decay, decay-stage word-loss, and the arousal/connection _forgetting_ modulation are
  [37.forgetting-decay](37.forgetting-decay.md) (Epic D). This unit reuses the **same modulation signal** ([F6][F7]) for
  the gist-timer but does not own it; the shared coefficient home is noted below.
- **No cost pricing.** `semantic_stage` is the **gist-depth signal** the view-cost curve consumes ([R8][G4], CC3), but
  the Twinkle price of _viewing_ a gist star lives in Epic F's [43.stardust-ledger](43.stardust-ledger.md). This unit
  exposes the depth, not a price.
- **No schema reshape.** `semantic_stage`, `semanticize_timer_reset_at`, and `semantic_stages` already exist on
  `episodic_memories` (plan 16); this unit adds **no** column. `memory_provenance` is added by plan 32 (CC5).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](00.overview.md) §3 (_The constitution_), owned by [PRD.md](../PRD.md) §7.
  Directly honored here: **[I5]** — the gist body is **not** independently laid out; its x,y are _copied_ from the
  hippocampal star (whose x,y remain emergent from force-sim), and only z is raised, so meaning still comes from the
  hippocampal emergence and the neocortex is not a second coordinate authority; **[I1]** — semanticization never deletes
  a memory, neuron, or synapse (it only _rises_ a derived stage; the concrete star stays); **[I3]** — the timer's
  arousal modulation uses **arousal only** (a rate modulator, [F6]), never valence, and semanticization touches neither
  position nor connection; **[I10]** — the gist-timer flows in _universe-days_ (monotonic, forward-only clock), so stages
  never rise on wall-clock or on mere viewing; the **z-axis one-way** rule (already-risen stages are never lowered on
  recall, [C7]) is the semanticization-specific expression of forward-only progression.
- tech / policy / concept basis:
  - [ARCHITECTURE.md](../ARCHITECTURE.md) §2.2 (semanticization is core-domain behavior of the memory context — it lives
    in `internal/memory`, not a new context), §2.4 (the dependency rule — pure domain free of proto/sqlc; the provenance
    repository port is consumer-owned by the use-case), §2.5 (domain services are **pure functions over the aggregates**,
    which is exactly what lets a client-side render and the server run the _same_ stage/coordinate math, pinned by
    golden-parity), §2.6 (`memory/pg` is the only sqlc/pgx seam — but this unit adds no query; it is pure math).
  - [PRD.md](../PRD.md) §5.3.6 **[C5]** (z-axis 2-store CLS: 해마 z 0–10, 신피질 z 15–25; episodic memories born in the
    hippocampus), **[C6]** (요지화 = 신피질로 상승; **x,y copied from the hippocampal emergent coords, z raised only**;
    the neocortex has **no force-sim**), **[C6a]** (the gist-timer: units-per-stage in universe-time, e.g. 10 days, value
    in `values.yaml`; recall/reconsolidation **reset it to 0** to _delay_ the next stage; already-risen stages kept —
    z-axis one-way; independent of the forgetting axis; frequently-recalled memories gistify _and_ forget slower),
    **[C7]** (4 gist-stage texts pregenerated at launch; stages _stack_ as they rise, prior gist stars kept; on
    reconsolidation only the _remaining_ stages regenerate — that regen is plan 33, not here). §6 **[V5]** (abstraction
    is expressed by **semanticization — z-axis ascent + gist text**, not by star-shape change), **[V9]** (the z-axis
    2-layer universe: 해마 below, 신피질 above; the neocortex layer is stable, no force-sim), the visual model table
    (형태 = 추상화 단계 + seed, slow). §5.3.5 **[F6][F7]** (arousal & connection-strength modulation slows the gist-timer,
    reused from forgetting). §5.3.6 **[C4][C8]** noted as Epic-E seams (Downscale / ascent choreography).
- Sibling plans:
  - [16.memory-aggregate-schema](16.memory-aggregate-schema.md) — owns `semantic_stage`, `semanticize_timer_reset_at`,
    `semantic_stages` (the columns this model reads/advances) and the reserved "semantic stages exist; fill/advance is
    Epics C/E" seam this unit fills.
  - [18.synapse-plasticity](18.synapse-plasticity.md) — the precedent for a pure, golden-parity domain-math unit in the
    same `internal/memory` ↔ `packages/memory-logic` home; this unit sits beside it and shares its harness.
  - [19.force-sim-layout](19.force-sim-layout.md) — reserved the **neocortex z-band** (15–25) as bounds + the "copy x,y,
    raise z" contract with _no_ force-sim; this unit realizes the copy-and-raise for the gist coordinate.
  - [29.universe-clock](29.universe-clock.md) / [30.time-advance-usecase](30.time-advance-usecase.md) — universe time is
    the read-time "now" the gist-timer measures elapsed universe-days against; the `AdvanceProgression` hook is the seam
    Epic E's consolidation binds to fire the rise (this unit provides the math the hook drives).
  - [32.reconsolidation-rules](32.reconsolidation-rules.md) — owns the `memory_provenance` table + append port (CC5);
    this unit appends `semanticized/system` rows through it. [33.recall-usecase](33.recall-usecase.md) — writes the
    gist-timer reset on recall ([C6a][F5]) and regenerates the _remaining_ gist texts on reconsolidation ([C7]).
  - [37.forgetting-decay](37.forgetting-decay.md) — owns the arousal/connection modulation coefficients ([F6][F7]) this
    unit reuses; the _independent_ forgetting axis running in parallel.
  - [41.consolidation-usecase](41.consolidation-usecase.md) — the use-case that _fires_ `Semanticize` over an advance
    interval, runs replay-reorg + `Downscale`, and executes the provenance append. [42.gist-star-rendering](42.gist-star-rendering.md)
    — reads `semantic_stage` + the gist coordinate to render the neocortical gist stars.
- Grounding for the neuroscience: complementary learning systems / hippocampal→neocortical consolidation (McClelland,
  McNaughton & O'Reilly 1995); systems consolidation & gist extraction (Winocur & Moscovitch 2011); arousal-modulated
  consolidation (McGaugh 2004). See [PRD.md](../PRD.md) §9.

## Design

### Placement (domain-first Go)

Semanticization is **core-domain behavior of the memory context** — it advances a stage on the aggregate root, reads its
timer anchor, and computes a coordinate for its gist body — so it lives in `internal/memory` beside plan 18's plasticity
math, **not** a new context (ARCHITECTURE §2.2; be-architecture: keep tightly-coupled memory behavior in one context).
The functions are pure (types + `Semanticize` + the elapsed-unit math + `GistCoordinate`) and mirror into
`packages/memory-logic` (TS). This unit adds **no** table, **no** query, and **no** RPC: the columns it reads already
exist (plan 16) and are already returned by `GetUniverse` (`semantic_stage`, `semanticize_timer_reset_at`,
`semantic_stages` — recon confirms all three are selected). The provenance **append port** is consumer-owned by Epic E's
use-case (ARCHITECTURE §2.4), not pre-declared here.

### The aggregate surface (given, not defined here)

From `episodic_memories` (owned by [16.memory-aggregate-schema](16.memory-aggregate-schema.md)):

- `semantic_stage SMALLINT` in `0..4` — the current abstraction stage (stored fact; advanced by `Semanticize` on
  consolidation). `0` = concrete only, no gist risen; `1..4` = the four pregenerated gist stars have risen.
- `semanticize_timer_reset_at DATE` — the universe-time anchor the gist-timer counts elapsed universe-days from ([C6a]).
  Set to `created_universe_time` at launch, and re-set to `now` on recall/reconsolidation (plan 33).
- `semantic_stages JSONB` — the 4 pregenerated gist texts ([C7]); read (never written) here to pick the risen stage's
  text for provenance/render.
- `arousal REAL`, plus the memory's aggregate connection strength — the modulation inputs ([F6][F7]).

The pure functions never mutate the aggregate; they take scalars and return scalars, so the same functions serve a Go
domain object and a TS mirror without either owning the other's struct shape (matching plan 18).

### `Semanticize` — pure abstraction-stage progression ([C6a][C7])

Signature (canonical; representation casing may differ per language):

```
Semanticize(currentStage int, unitsElapsed int) → int
```

`unitsElapsed` is the number of _whole_ gist-timer units that have crossed since the timer anchor (computed by the timer
model below). The new stage is:

```
newStage = min(currentStage + unitsElapsed, SEMANTIC_MAX_STAGE)      // SEMANTIC_MAX_STAGE = 4 (derived ladder length)
```

Consequences the acceptance criteria pin:

- **Monotone non-decreasing.** `Semanticize(s, u) ≥ s` for `u ≥ 0` — a stage never _lowers_. This is the z-axis one-way
  rule ([C7]): once a gist star has risen it is kept, even across a recall that resets the timer.
- **Clamped to the ladder.** `Semanticize(s, u) ≤ SEMANTIC_MAX_STAGE = 4`; there is no stage beyond the four
  pregenerated texts. Stage 0 with no elapsed units stays at 0 (concrete only).
- **Deterministic & total.** Depends only on its arguments and `SEMANTIC_MAX_STAGE` (a derived array length, not a tuned
  value — the count of pregenerated gist texts). No clock, no store, no randomness.

**Decided (2026-07):** a single advance can cross **multiple** stages when the clock jumps far (Epic B's launch/recall
can advance the universe clock by many days at once, [T2]). `Semanticize` therefore adds _all_ whole units elapsed and
clamps — the consolidation use-case appends one provenance row **per stage crossed** (CC5), preserving a continuous
변천사 even across a large jump. It does not silently skip intermediate gist stars.

### The gist-timer ([C6a]) — elapsed units in universe-days, arousal/connection-modulated

The timer converts elapsed universe-time into whole stage-units. Given the universe-time "now" (from the clock, plan 29),
the memory's `semanticize_timer_reset_at`, and the modulation inputs:

```
rawElapsedDays  = max(0, now − semanticize_timer_reset_at)                       // universe-days [I10]
effectiveDays   = rawElapsedDays × timerModulation(arousal, connectionStrength)  // [F6][F7]; ≤ 1, slows the timer
unitsElapsed    = floor(effectiveDays / semantic.gist_units_per_stage)
```

- **`semantic.gist_units_per_stage`** (universe-days per gist stage, e.g. 10 — confirmed at implement) is a `values.yaml`
  scalar; the _division_ is a formula (code).
- **Modulation ([F6][F7]).** `timerModulation` returns a factor in `(0, 1]` that **slows** the timer for high-arousal
  and strongly-connected memories — the same signal forgetting uses to slow brightness decay. **Decided (2026-07):** the
  modulation **coefficients** live once with the forgetting axis ([37.forgetting-decay](37.forgetting-decay.md),
  `forgetting.arousal_slow_coefficient` / `forgetting.connection_slow_coefficient` — enumerated there), and semanticization _reuses_
  them so a highly-arousing memory forgets _and_ gistifies slower with one consistent knob ([C6a] "frequently-recalled
  memories gistify and forget slower"). This unit references those keys; it does not introduce a second modulation
  coefficient. The modulation _shape_ is a formula (code).
- **Reset delays the next stage ([C6a][F5]).** Because `unitsElapsed` counts from `semanticize_timer_reset_at`, setting
  that anchor to `now` on recall/reconsolidation (plan 33) makes `rawElapsedDays = 0` → `unitsElapsed = 0` → no new stage
  _for now_ — the next gist is _delayed_, not undone. Already-risen stages (`semantic_stage`) are untouched by the reset
  (z-axis one-way, [C7]).
- **Independence from forgetting ([F]).** The gist-timer reads `semanticize_timer_reset_at`; forgetting decay reads
  `last_recalled_universe_time` (+ the neighbor `forgetting_offset_days`, CC4). They are separate anchors on separate
  axes — a memory can be deeply forgotten yet barely gistified, or vice versa. This unit computes only the gist axis.

### `GistCoordinate` — copy x,y, raise z ([C5][C6][V9][I5])

Signature:

```
GistCoordinate(hippocampalX float, hippocampalY float, stage int) → (x, y, z)
```

The risen gist body's position is **not simulated** — the neocortex band has no force-sim ([C6][I5]). Instead:

- **x,y are copied verbatim** from the hippocampal star's _emergent_ settled coordinates (force-sim output, plan 19). The
  gist star sits directly _above_ its concrete star. Meaning still comes from the hippocampal emergence ([I5] upheld: x,y
  are not re-derived and the neocortex is not a second coordinate authority).
- **z is raised into the reserved neocortex band** `force_sim.neocortex_z_min`..`force_sim.neocortex_z_max` (15..25),
  disjoint from the hippocampus band (0..10) by the reserved gap ([C5][V9]). **Decided (2026-07):** `z` is a function of
  `stage` within the band (deeper stages sit higher in the band, e.g. a linear map of `stage/SEMANTIC_MAX_STAGE` across
  `[neocortex_z_min, neocortex_z_max]`) so the ascent reads as progressive depth; the exact map is a formula (code). All
  four gist stages fall strictly inside `[neocortex_z_min, neocortex_z_max]`.

The neocortex layer is therefore **stable** ([V9]): its bodies move only when their hippocampal x,y move (the hippocampal
layout re-settles) or when a stage rises. There is no independent neocortical simulation and no neocortex→hippocampus
coordinate feedback.

### Provenance append on stage rise (CC5, [R8a])

Each rising stage is an event in the memory's variant history. When `Semanticize` advances the stage from `s` to `s'`,
**one `memory_provenance` row per crossed stage** is appended: `kind = semanticized`, `source = system`, `text` = that
stage's pregenerated gist text (from `semantic_stages`), `universe_time` = the advance's universe-time. This is the
semanticization half of 변천사 ([R8a]); reconsolidation appends `reconsolidated/user` rows (plan 33), and the
`created/original` baseline is synthesized at read (CC5). The `memory_provenance` table + append port are owned by
[32.reconsolidation-rules](32.reconsolidation-rules.md); the actual append executes inside Epic E's consolidation
transaction ([41.consolidation-usecase](41.consolidation-usecase.md)). This unit specifies the row _contents_ and the
_one-per-crossed-stage_ rule; it performs no IO itself.

### Golden-parity (TS↔Go)

Per ARCHITECTURE §2.5, purity exists so the two runtimes agree. The Go implementation in `internal/memory` and the TS
mirror in `packages/memory-logic` are pinned equal by **shared golden fixtures**
(`apps/api/internal/memory/testdata/semanticization-golden.json`), `(function, inputs) → expected output` rows with a
fixed tolerance. Coverage: `Semanticize` at each stage `0..4` including the clamp at `4`, a multi-unit single-advance
crossing several stages, `unitsElapsed = 0` (stage unchanged); the timer's elapsed-unit math at the reset anchor
(`elapsed = 0` → 0 units), across a range of universe-days, and under low/high modulation; and `GistCoordinate` asserting
x,y are copied unchanged and z lands strictly inside `[neocortex_z_min, neocortex_z_max]` for every stage. Both consumers
read the same generated `semantic.*` / `force_sim.*` constants (`pnpm gen:values` emits Go + TS), so a tuning change
cannot desync the two sides.

**Decided (2026-07):** golden-parity covers the **timer + z-coordinate** math (both the client render and the server read
must agree on stage and placement), matching plan 18's precedent. The LLM-gated pieces — gist-text _content_ (plan 20/22)
and reconsolidation's remaining-stage regen (plan 33) — are **not** golden-parity (they are non-deterministic LLM output),
consistent with the brief's parity stance.

### Re-implementation note (e.g. Flutter)

Semanticization is fully specified by three rules — _stage rises by whole gist-units elapsed, clamped to 4 and never
lowered_; _a unit is `gist_units_per_stage` modulated universe-days from the reset anchor_; and _the gist body copies the
hippocampal x,y and raises z into the neocortex band_ — plus the reused forgetting modulation. Any client reproduces it
from those rules + the `semantic.*` / `force_sim.*` constants; there is no hidden state beyond the per-memory stage +
timer anchor.

## Acceptance Criteria

1. `Semanticize(currentStage, unitsElapsed)` is **monotone non-decreasing** (`≥ currentStage` for `unitsElapsed ≥ 0`) and
   **clamped** to `SEMANTIC_MAX_STAGE = 4` (a derived ladder length = the count of pregenerated gist texts, not a tuned
   value); it is deterministic and IO-free ([C7], z-axis one-way).
2. A single advance may cross **multiple** stages (`unitsElapsed > 1`): `Semanticize` adds all whole units and clamps, and
   the consolidation use-case appends **one provenance row per crossed stage** so 변천사 stays continuous across a large
   clock jump (CC5, [R8a]).
3. The gist-timer computes `unitsElapsed = floor(effectiveDays / semantic.gist_units_per_stage)` where `effectiveDays =
max(0, now − semanticize_timer_reset_at) × timerModulation(arousal, connectionStrength)`, measured in **universe-days**
   ([C6a][I10]); at the reset anchor (`now = semanticize_timer_reset_at`) `unitsElapsed = 0` (no stage rises) ([F5]).
4. Higher **arousal** and stronger **connection** _slow_ the gist-timer (`timerModulation ∈ (0,1]`), **reusing the
   forgetting modulation coefficients** ([37.forgetting-decay](37.forgetting-decay.md), [F6][F7]) — no second modulation
   coefficient is introduced — and the modulation uses **arousal only, never valence** ([I3]).
5. Recall/reconsolidation resetting `semanticize_timer_reset_at` to `now` **delays** the next stage (`unitsElapsed`
   recomputes to 0) but **never lowers** `semantic_stage` — already-risen gist stars are kept ([C6a][C7], z-axis one-way).
   (The reset _write_ is [33.recall-usecase](33.recall-usecase.md); this unit defines the read semantics.)
6. The gist axis is **independent** of forgetting: the gist-timer reads `semanticize_timer_reset_at`, while forgetting
   decay reads `last_recalled_universe_time`; a memory may be deeply decayed yet minimally gistified and vice versa ([F], [C]).
7. `GistCoordinate(hippocampalX, hippocampalY, stage)` **copies x,y verbatim** from the hippocampal star and returns a
   `z` strictly inside the reserved neocortex band `[force_sim.neocortex_z_min, force_sim.neocortex_z_max]` (15..25),
   disjoint from the hippocampus band (0..10); the neocortex has **no force-sim** and no independent coordinate authority
   ([C5][C6][V9][I5]).
8. Semanticization **never deletes** any memory, neuron, or synapse and never mutates the `Diary` — it only rises a
   derived stage and computes a coordinate; the concrete hippocampal star and its `current_text` remain ([I1][I2]).
9. As stages rise, the model specifies a `memory_provenance` append with `kind = semanticized`, `source = system`, the
   risen stage's pregenerated text, and the advance `universe_time` (CC5, [R8a]); the table/port are owned by
   [32.reconsolidation-rules](32.reconsolidation-rules.md) and the write executes in
   [41.consolidation-usecase](41.consolidation-usecase.md) — this unit performs no IO.
10. `semantic_stage` is exposed as the **gist-depth signal** for the view-cost curve ([R8][G4], CC3); this unit exposes
    the depth only and defines **no** Twinkle price (pricing is [43.stardust-ledger](43.stardust-ledger.md)).
11. The Go functions (`internal/memory`) and the TS mirror (`packages/memory-logic`) produce **identical outputs** for a
    shared golden-fixture table within a fixed tolerance (TS↔Go golden-parity for the timer + z-coordinate), and both read
    the same generated `semantic.*` / `force_sim.*` constants; no formula, curve shape, or clamp is stored in `values.yaml`.
12. This unit adds **no** DB column, query, RPC, or proto field — it reads columns that already exist on
    `episodic_memories` (plan 16) and are already returned by `GetUniverse`; it is pure domain math ([ARCHITECTURE.md](../ARCHITECTURE.md) §2.4/§2.5).

## Policy / Values Impact

- **tech/** — the as-built semanticization rules will land in `tech/semanticization.md` (a
  new memory-context tech doc): semanticization is a **pure, golden-parity** stage-rising model in `internal/memory` ↔
  `packages/memory-logic`; the gist-timer flows in universe-days and is **reset (not reversed) by recall** ([C6a]); stages
  are **z-axis one-way** (kept across recall, [C7]); the gist body **copies the hippocampal x,y and raises z** into a
  neocortex band with **no force-sim** ([C6][I5]); the gist axis is **independent of forgetting** ([F]); and `Semanticize`
  is distinct from `Downscale` (SHY, Epic E [C4][I9]). Written at `/implement-job` — **do not** pre-create the doc.
- **policy/** — the domain rule "**semanticization rises, never regresses** — a gist stage, once risen, is kept even when
  recall resets the timer; recall _delays_ the next stage but does not lower the current one; semanticization compresses
  meaning and is independent of forgetting's word-loss" lands in a new `policy/domain/semanticization.md` once implemented
  (written at `/implement-job`; **do not** pre-create policy files). It reinforces [I5][I10] and is consistent with
  [C5]–[C7][C6a].
- **spec/values.yaml** — group `semantic` (generated to TS + Go constants read identically by both runtimes; enumerated
  now, values confirmed at implement; formulas stay code):
  - `semantic.gist_units_per_stage` (universe-days per gist stage, e.g. `10` — **confirmed at implement**) — the
    gist-timer unit length: how many universe-days fill one stage-unit before the next gist rises ([C6a]).
  - _(No new modulation key._ The arousal/connection modulation the gist-timer applies **reuses**
    `forgetting.arousal_slow_coefficient` / `forgetting.connection_slow_coefficient` owned by
    [37.forgetting-decay](37.forgetting-decay.md) ([F6][F7]) — one consistent knob for both axes; this unit introduces no
    second coefficient.)*
  - _(Reused, not owned here:_ `force_sim.neocortex_z_min` / `force_sim.neocortex_z_max` (15 / 25) — the neocortex z-band
    bounds `GistCoordinate` places into, owned by [19.force-sim-layout](19.force-sim-layout.md) [C5][C6][V9]. Listed for
    coherence; not introduced by this unit.)*
  - _(Excluded from values — code, not values:_ the unit-crossing division, the `timerModulation` shape, the
    `stage → z` placement map, the stage clamp, and `SEMANTIC_MAX_STAGE = 4` (a derived array length = count of
    pregenerated gist texts, per the values exclusion rule). Formulas and derived counts stay code.)*
- **Dependencies:** depends on [16.memory-aggregate-schema](16.memory-aggregate-schema.md) (the `semantic_stage` /
  `semanticize_timer_reset_at` / `semantic_stages` columns it reads), [29.universe-clock](29.universe-clock.md) (universe
  time as the read-time "now" the gist-timer measures), and [19.force-sim-layout](19.force-sim-layout.md) (the reserved
  neocortex z-band + the "copy x,y, raise z" contract). **Reuses** [37.forgetting-decay](37.forgetting-decay.md)'s
  arousal/connection modulation coefficients ([F6][F7]). **Pairs with** [32.reconsolidation-rules](32.reconsolidation-rules.md)
  (the `memory_provenance` table + append port, CC5) and [33.recall-usecase](33.recall-usecase.md) (writes the gist-timer
  reset + regenerates remaining gist texts [C6a][C7]). **Consumed by** [41.consolidation-usecase](41.consolidation-usecase.md)
  (fires `Semanticize` over the advance interval via plan 30's `AdvanceProgression` hook, runs replay-reorg + `Downscale`,
  executes the provenance append) and [42.gist-star-rendering](42.gist-star-rendering.md) (reads `semantic_stage` + the
  gist coordinate to render neocortical gist stars). **Enables** the gist-depth signal that [43.stardust-ledger](43.stardust-ledger.md)
  prices for gist-view cost ([R8][G4], CC3). **Seam only** with `Downscale` (SHY, [41.consolidation-usecase](41.consolidation-usecase.md) [C4][I9]).
