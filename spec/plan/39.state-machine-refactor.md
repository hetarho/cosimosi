# 39. 상태 머신 리팩토링 (XState 도입)

> 흩어진 제어 상태(카메라 모드·포커스·작성·회상·세션)를 **XState v5 상태 머신**으로 단일화한다. | 범위: FE | 선행: 28, 31, 38 | 차단: 없음

## 목적 (Why)

cosimosi는 일기 앱이 아니라 **우주를 항해하는 게임**이다 — 카메라가 성운/회상/비행/조망 사이를 오가고, "지금 무엇에 집중하나"(별/일기/리스트/변천사)가 서로 배타다. 이 "모드·단계·수명주기"가 지금은 **여러 스토어의 nullable 필드 + 동기화 이펙트**로 흩어져 있어, 같은 논리 상태를 두 진입점이 다른 변수로 표현해 **불일치**가 난다(실증: "일기로 별 보기"를 `frameDiary`와 `onSeeDiaryStars`가 다르게 다뤄 한쪽만 카드 표시·`NearFarHighlightGuard`가 다른 쪽을 지움). 제어 상태를 명시적 FSM으로 모델링해 **불가능한 상태를 표현 불가능하게** 만들고, 동기화 이펙트를 전환 함수로 흡수한다.

설계 전반은 [state-machines.md](../state-machines.md), 작성 규약은 [xstate-guide.md](../xstate-guide.md)가 단일 출처다. 이 스펙은 **그 설계를 코드로 옮기는 단계적 작업 순서**다.

## 범위

**포함** — 아래 7개 영역을 XState로 승격(2 Core + 3 Lifecycle + evolution 흡수 + 선택적 time-skip):

- 세션(P0·레퍼런스), 포커스(P1), 항행(P2), 작성(P3), 회상 flush(P4), (선택) leaf 흡수/평가(P5).
- XState v5 + @xstate/react 도입, `createActorContext` 앱 패턴, R3F per-frame `getSnapshot` 패턴, 머신 간 통신 배선.

**비목표 (Out of scope)**

- **데이터를 머신으로 옮기지 않는다** — `stars[]`·`edges[]`·`ambient`·`emotionColors`·쿼리 캐시·force-sim 좌표 버퍼는 zustand/Query/ref 유지. 머신은 `id` 참조만.
- **동작·시각 변경 금지(behavior-preserving)** — 카메라 감각·UX는 그대로. 단 P1은 의도적으로 "일기로 별 보기" 두 진입점을 **동일**하게 만든다(이게 목표).
- **백엔드·proto·DB 무변경.** 순수 FE 리팩토링.
- **leaf UI 일괄 전환 금지** — MemoryPanel phase·SignIn step·BottomSheet snap·DemoSimPanel·랜딩 카드 데모는 가치가 증명될 때만 승격(기본 유지).
- 새 카메라/포커스 기능 추가 금지(리팩토링이지 기능 확장이 아니다).

## 참고

- [state-machines.md](../state-machines.md) — 제어상태 vs 데이터, FSD 배치, 전체 카탈로그, 두 Core 머신 상태도, 나브↔포커스 계약.
- [xstate-guide.md](../xstate-guide.md) — v5 템플릿, 액터, React 바인딩, R3F per-frame, 테스트, v4→v5 함정, 안티패턴.
- [Architecture.md](../Architecture.md) §2.7(Zustand 배치)·§3.2(60fps에 React state 금지)·§11(상태 머신).
- 헌법 4(FSD 단방향·`model` 순수)·8(셰이더 TSL·씬 내 DOM 금지)는 그대로 지킨다.

## 설계 요점

- **승격 기준:** "N개 배타 상황의 명시적 수명주기"만 머신. 값/컬렉션은 데이터로 둔다([state-machines §0](../state-machines.md)).
- **배치:** 머신은 소유 레이어의 `model/<name>.machine.ts`(순수 TS). React 바인딩·R3F 연동은 `ui`. entities는 머신 스냅샷 파생 props로 렌더만(헌법4).
- **두 Core의 관계:** `navigation` 액터 먼저 생성(ref 불필요, 도착을 `emit`) → `focus` 액터가 nav ref를 `input`으로 받아 `sendTo` → 페이지가 `navActor.on('arrived.star', …) → focusActor.send(…)` 한 줄 브리지(순환 ref 회피, 둘 다 top-level standalone이라 per-frame getSnapshot 빠름).
- **R3F:** 컨트롤러는 `useActorRef`로 ref만 얻어 `useFrame`에서 `getSnapshot()`; 임계 도달 시 `send(ARRIVED)`. 연속 lerp·`move`·drag는 머신 밖. `useMachine` 금지.
- **단계 독립성:** 각 Phase는 빌드/리뷰/검증을 독립으로 통과하고 따로 커밋 가능. P0가 패턴·도구를 깔고, P1→P2 순서(포커스가 나브 계약을 정의)로 의존. P3·P4는 Core와 독립이라 순서 자유.

## 영향 파일

**P0 — 도입·세션**
- `frontend/package.json` (+xstate, +@xstate/react)
- `frontend/src/app/model/session.machine.ts` (신설), `session.context.tsx` (신설)
- `frontend/src/app/model/auth-store.ts` (제거/대체), `app/ui/SessionGate.tsx`·`SignInScreen.tsx`·`App.tsx` (바인딩 교체)
- `frontend/src/app/model/session.machine.test.ts` (신설)

**P1 — 포커스**
- `frontend/src/features/universe/model/focus.machine.ts` (신설), `focus.context.tsx` (신설), `focus.machine.test.ts` (신설)
- 제거/흡수: `features/universe/model/shell-store.ts`, `features/wayfinding/model/store.ts`(highlightedRecordId·frameRequest), `entities/memory/model/store.ts`의 `selectedId`/`select`, `features/evolution/model/store.ts`
- 소비처 교체: `pages/home/ui/HomePage.tsx`, `features/recall/ui/{MemoryPanel,NeighborNav}.tsx`, `features/diary-list/ui/{DiarySheet,DiaryCard}.tsx`, `features/dormant-search/ui/DormantSheet.tsx`, `features/evolution/ui/*`, `widgets/universe-canvas/ui/UniverseCanvas.tsx`(`onPointerMissed`·`NearFarHighlightGuard` 삭제·StarField highlight prop 소스)
- `app/router.tsx`(`?panel=` ↔ focus 머신 동기화)

**P2 — 항행**
- `frontend/src/widgets/universe-canvas/model/navigation.machine.ts` (신설), `navigation.context.tsx` (신설), `navigation.machine.test.ts` (신설)
- 제거/대체: `widgets/universe-canvas/model/use-camera-mode.ts`
- `widgets/universe-canvas/ui/UniverseCanvas.tsx` — FlyTo·FrameAll·ModeTransition·CameraRig·NebulaOrbit·Nav·ViewOffset 컨트롤러를 머신 `getSnapshot` 구독으로 재배선(연속 lerp 유지, `transitioning` → `hasTag`)
- nav 입력 트리거: `pages/dormant`→`focusStar`, 카메라 토글 버튼

**P3 — 작성**
- `frontend/src/features/record-memory/model/compose.machine.ts` (신설, +test), `compose.context.tsx` (신설)
- 제거: `features/record-memory/model/draft-store.ts`; 교체: `features/record-memory/ui/*`, `use-record-memory.ts`

**P4 — 회상 flush**
- `frontend/src/features/recall/model/recall-flush.machine.ts` (신설, +test), `recall-flush.context.tsx` (신설)
- 흡수: `features/recall/model/store.ts`(session·deltas·batchId·inFlight)·`co-recall.ts`(디바운스 로직); 교체: `features/recall/ui/*`

**P5 (선택)**
- `widgets/demo-sim/model/time-travel.ts` → `time-skip.machine.ts`(fromCallback), 그 외 leaf 영역 평가 메모.

## 수용 기준 (EARS/GWT)

1. **도입**
   - 1.1 WHEN `pnpm --filter ./frontend build`·`lint`·`steiger`를 돌리면 THE SYSTEM SHALL xstate/@xstate/react가 설치된 상태로 0 에러로 통과한다.
   - 1.2 THE SYSTEM SHALL 모든 `*.machine.ts`를 `model` 세그먼트에 두고, `model`에서 React/three import가 없다(헌법4·steiger 통과).
2. **세션(P0)**
   - 2.1 WHEN 앱이 부팅하면 THE SYSTEM SHALL `loading`→(세션 있음)`authed`/(없음·실패)`anon`으로 전이한다(현 auth-store와 동일 동작).
   - 2.2 WHEN 사용자 경계가 바뀌면(다른 uid) THE SYSTEM SHALL 우주 데이터 리셋을 1회 트리거한다(기존 `syncIdentity` 동작 보존).
3. **포커스(P1)**
   - 3.1 WHEN 일기 목록에서 일기를 고르거나(`SELECT_DIARY`) 회상 패널의 "이 일기의 다른 별들 보기"를 누르면(`SEE_DIARY_STARS`) THE SYSTEM SHALL **동일하게** `diaryFocus` 상태로 들어가 일기 카드 표시 + 별 조망을 보인다(두 진입점 동작 일치 — 본 스펙의 핵심 수용).
   - 3.2 THE SYSTEM SHALL 별 포커스와 일기 조망이 동시에 활성일 수 없다(머신 단일 상태 — `NearFarHighlightGuard` 삭제 후에도 배타 유지).
   - 3.3 WHEN 빈 캔버스를 탭하거나 Esc를 누르면 THE SYSTEM SHALL `idle`로 복귀한다(별·일기·리스트·변천사 어느 상태에서든).
   - 3.4 WHEN `?panel=dormant|diary`로 딥링크하면 THE SYSTEM SHALL 해당 리스트 상태로 시작하고, 뒤로가기로 복귀한다.
4. **항행(P2)**
   - 4.1 WHEN `FLY_TO_STAR`/`FRAME_DIARY`/`TOGGLE_MODE` 이벤트를 받으면 THE SYSTEM SHALL 각각 `flyingToStar`/`framingDiary`/`modeTransition`(tag `transitioning`)으로 전이하고, 도착 시 `recall`/`nebula`/`to`로 정착한다.
   - 4.2 WHILE `transitioning` tag가 켜져 있으면 THE SYSTEM SHALL 카메라 거리·함선 경계 클램프를 완화한다(기존 동작 보존).
   - 4.3 THE SYSTEM SHALL 매 프레임 카메라 보간을 `useFrame` 안에서 수행하고 머신은 `getSnapshot`으로만 읽는다(React 리렌더로 60fps 구동 금지 — Architecture §3.2).
5. **작성(P3)·회상(P4)**
   - 5.1 WHEN 작성이 분절/제출되면 THE SYSTEM SHALL `compose`↔`review` × `idle/segmenting/submitting/error`를 한 머신으로 표현하고 불가능 조합(예: review 아닌데 submitting)을 만들 수 없다.
   - 5.2 WHEN 공동 회상이 누적되면 THE SYSTEM SHALL `after` 디바운스(유휴 5s)로 flush하고, 실패 시 같은 `batch_id`로 재시도(멱등)한다(기존 동작 보존).
6. **회귀**
   - 6.1 THE SYSTEM SHALL 기존 vitest를 모두 통과시킨다(91+개), 머신별 신규 단위테스트를 추가한다.

## 작업 (Tasks)

### Phase 0 — 도입 & 패턴 확립 (레퍼런스 = 세션 머신) ✅
- [x] T001 `pnpm --filter ./frontend add xstate @xstate/react`. _참조: 1.1_
- [x] T002 [P] `app/model/session.machine.ts` — `setup().createMachine`, states `loading/authed/anon`, `invoke fromPromise(getSession)` + 상시 `fromCallback(onAuthStateChange)`, 경계 변경 시 `resetUniverseData` + 분석 식별을 `syncSession`(enqueueActions) action으로, `lastUserId`는 context. _참조: 2.1, 2.2_
- [x] T003 [P] `app/ui/session-context.tsx` — `createActorContext(sessionMachine)` + `useAuthActions`. (createActorContext는 React라 `ui`에 둔다 — 헌법4; 가이드도 정정.) _참조: 2.1_
- [x] T004 `SessionGate`·`SignInScreen`·`App.tsx`를 `SessionContext.useSelector`/`useAuthActions`로 교체, `auth-store.ts` 제거, `App`이 `SessionContext.Provider`로 라우터 감쌈. _참조: 2.1_
- [x] T005 `session.machine.test.ts` — `createActor`+`send`+`getSnapshot` 6 케이스(부팅 없음/있음/reject·AUTH_CHANGED·경계 리셋·FORCE_ANON). _참조: 6.1_
- [x] T006 검증: build·lint·steiger·vitest(6/6) ✅. (`/code-review`·`/codex`는 스펙 완료 시 전체 diff에 일괄 — 단계별 커밋은 사용자.)

### Phase 1 — 포커스 머신 (불일치 해소 + 배타 구조화) ✅
> **as-built:** focus 머신은 `entities/memory/model`(FSD — widgets·features 공통 하위; selectedId가 원래 거기). `idle|star|diary`만 — `shell.panel/peek`·`evolution`은 포커스와 **직교**라 깨끗한 zustand로 유지(흡수 안 함, §3.1). 모듈 싱글턴 `focusActor`(구 store와 동형). DiaryCard는 `focus=diary`에 묶어(패널 아님) 두 진입점 모두에서 뜬다 — 버그 해소.
- [x] T101 `entities/memory/model/focus.machine.ts` — states `idle/star/diary`, 이벤트 `SELECT_STAR/SELECT_DIARY/SEE_DIARY_STARS/DISMISS`. `SELECT_DIARY`·`SEE_DIARY_STARS`는 동일 `toDiary`(같은 `diary` 타깃·frameNonce++). 모듈 싱글턴 `focusActor` + selectors. _참조: 3.1, 3.2, 3.3_
- [x] T102 [P] selectors(`selectFocusedStarId`/`selectHighlightedRecordId`/`selectIsStarFocus`/`selectIsDiaryFocus`/`selectIsFocused`/`selectFrameNonce`) — `entities/memory` 공개 API로. _참조: 3.1_
- [x] T103 소비처 교체 — `HomePage`·`MemoryPanel`·`NeighborNav`를 `useSelector(focusActor,…)`/`focusActor.send`로. `StarField`는 `selectedId`/`onSelect` prop 구동(엔티티 순수). `frameDiary`/`onSeeDiaryStars` 둘 다 머신 이벤트로(분기 없음). _참조: 3.1_
- [x] T104 `UniverseCanvas` — 9개 컨트롤러의 focus 읽기를 `focusActor` selector/`getSnapshot`로, `onPointerMissed`→`focusActor.send(DISMISS)`, `NearFarHighlightGuard`→얇은 `RecallDismissGuard`(근접 진입 시 diary만 해제), FlyTo 도착→`SELECT_STAR`. 카메라 모드(`use-camera-mode`)는 P2까지 유지. _참조: 3.2, 3.3_
- [x] T105 `memory/store.ts`의 `selectedId`/`select` 제거(focus 머신으로), `wayfinding/model/store.ts` 삭제(frameTarget 기하만 잔존), `resetUniverseData`가 `focusActor.send(DISMISS)`. (`stars`·`ambient`·`edges`·shell·evolution 유지.) _참조: 비목표_
- [x] T106 `?panel=` ↔ `shell-store`는 그대로(포커스와 직교 — 거울 이펙트 유지). diary-close 이펙트는 focus selector 기반으로 동작. _참조: 3.4_
- [x] T107 [P] `entities/memory/model/focus.machine.test.ts` — 두 진입점 동일 도달, 배타(star↔diary), frameNonce 재발화, DISMISS 복귀. 6/6 통과. _참조: 3.1, 3.2, 6.1_
- [x] T108 검증: build·lint·steiger·vitest(103/103) ✅. (`/code-review`·`/codex`는 스펙 완료 시 전체 diff에 일괄.)

### Phase 2 — 항행 머신 (카메라 게임 FSM)
- [ ] T201 `widgets/universe-canvas/model/navigation.machine.ts` — states `nebula/recall/flyingToStar/framingDiary/modeTransition`, tag `transitioning`, 이벤트 `TOGGLE_MODE/FLY_TO_STAR/FRAME_DIARY/ARRIVED`, 도착 시 `emit('arrived.star')`. context = 이산 타깃만. _참조: 4.1_
- [ ] T202 [P] `navigation.context.tsx`. _참조: 4.1_
- [ ] T203 `UniverseCanvas` 컨트롤러 재배선 — FlyTo/FrameAll/ModeTransition을 머신 상태 + 얇은 lerp 컨트롤러로, CameraRig 클램프를 `hasTag('transitioning')`로, NebulaOrbit/Nav 게이트를 focus+nav selector로. 연속 lerp·`move`·drag는 머신 밖 유지. _참조: 4.2, 4.3_
- [ ] T204 나브↔포커스 브리지 — `navActor.on('arrived.star', …) → focusActor.send(CONFIRM_STAR)`, focus `entry`의 `sendTo(nav, FLY_TO_STAR/FRAME_DIARY)` 연결. `use-camera-mode.ts` 제거. _참조: 4.1, state-machines §5_
- [ ] T205 nav 트리거 교체 — dormant `focusStar`·카메라 토글 버튼을 `navActor.send`로. _참조: 4.1_
- [ ] T206 [P] `navigation.machine.test.ts` — 전이·tag·도착 emit. 브라우저 카메라 감각은 사용자 시각 검증(아래 DoD). _참조: 4.1, 6.1_
- [ ] T207 검증 + `/code-review` + `/codex:review` 반영, 커밋(P2).

### Phase 3 — 작성 머신
- [ ] T301 `features/record-memory/model/compose.machine.ts` — compound `compose{idle,segmenting}` / `review{idle,submitting}` + `error`, `invoke fromPromise`(segment·submit), 재시도. `draft-store.ts` 제거. _참조: 5.1_
- [ ] T302 [P] `compose.context.tsx` + `compose.machine.test.ts`. _참조: 5.1, 6.1_
- [ ] T303 `record-memory/ui/*`·`use-record-memory.ts` 교체. _참조: 5.1_
- [ ] T304 검증 + `/code-review`(+ 비자명하면 `/codex:review`) 반영, 커밋(P3).

### Phase 4 — 회상 flush 머신
- [ ] T401 `features/recall/model/recall-flush.machine.ts` — `idle/accumulating/flushing/error`, `after` 디바운스, `invoke fromPromise(flush)` + 멱등 재시도, `beforeunload` flush. `recall/store.ts`·`co-recall.ts` 흡수. _참조: 5.2_
- [ ] T402 [P] `recall-flush.context.tsx` + `recall-flush.machine.test.ts`(누적·디바운스·실패 재시도 멱등). _참조: 5.2, 6.1_
- [ ] T403 `recall/ui/*` 교체. _참조: 5.2_
- [ ] T404 검증 + `/code-review`(+ 비자명하면 `/codex:review`) 반영, 커밋(P4).

### Phase 5 — leaf 평가 (선택)
- [ ] T501 [P] (선택) `time-travel.ts` → `time-skip.machine.ts`(fromCallback 틱). demo 전용. _참조: 비목표_
- [ ] T502 [P] MemoryPanel phase·SignIn step·BottomSheet snap·DemoSimPanel·랜딩 카드 데모를 개별 평가 — 머신 가치 없으면 "유지" 사유를 이 스펙에 1줄 기록. _참조: 비목표_

## 정책 영향 (policy)

이 작업은 **구현 방식(상태 관리)** 변경이지 도메인 규칙·수치 변경이 아니므로 `policy/domain/*` 값은 불변이다. 단:

- [`policy/ux/motion-accessibility.md`](../policy/ux/motion-accessibility.md) R7(오버레이·딤·배경탭 복귀) — 구현 근거 경로를 focus 머신 기준으로 갱신(동작 규칙은 동일).
- [`policy/domain/navigation.md`](../policy/domain/navigation.md) — 카메라 상태 근거를 `use-camera-mode`→`navigation.machine`으로 갱신(파라미터·동작 동일).
- 상태 아키텍처 자체의 규칙은 plan이 아니라 [state-machines.md](../state-machines.md)·[xstate-guide.md](../xstate-guide.md)가 단일 출처다(policy 아님 — 구현 패턴 문서).

## 완료 정의 (DoD)

각 Phase 종료 시(누적):

1. `pnpm --filter ./frontend build` ✅ · `lint` ✅(0 에러).
2. `pnpm --filter ./frontend exec steiger src` ✅ — `model`에 React/three import 0건(헌법4), `*.machine.ts`가 `model`에 위치.
3. `pnpm --filter ./frontend test` ✅ — 기존 테스트 전부 + 신규 머신 단위테스트(Phase별).
4. **그 Phase의 EARS 수용 기준** 충족 — 특히 P1의 3.1(두 진입점 동일)은 머신 단위테스트로 증명.
5. `/code-review` 반영(기각 사유 기록) + 비자명 Phase는 `/codex:review`(`-m gpt-5.5`) 합산.
6. **제거 확인** — 해당 Phase가 없애기로 한 스토어/이펙트(P1: `NearFarHighlightGuard`·`shell-store`·divergent 진입점 / P2: `use-camera-mode`·3 useRef 비행 플래그)가 grep으로 0건.
7. 헌법 4(FSD 단방향·model 순수)·8(씬 내 DOM 0) 위반 없음.

> **브라우저 시각 검증은 사용자 몫** — 특히 P2(카메라 감각: fly-to·framing·mode 토글·클램프)와 P1(딤·배경탭·일기 카드)은 정적 게이트가 "깨지지 않음"만 보장한다. 자동 검증 후 사용자가 브라우저에서 한 바퀴 돈다.
