# 21. link usecase

> Link usecase — the core `internal/memory` use-case that grows the synapse graph after a launch: same-memory Hebbian
> wiring, shared-neuron connection, and a narrow temporal-proximity bonus, all as neuron↔neuron edges only.
> Scope: BE | Status: planning

## Purpose

A launched diary produces an `EpisodicMemory` and its co-activated `Neuron`s (plan 20's `Encode`/`PersistEncoded`).
On its own that is a set of isolated stars. cosimosi's constellations — the *relational* structure a user later
navigates by meaning and context rather than by date — grow from **synapses between neurons**, not from any
memory↔memory relationship. `Link` is the use-case that lays and strengthens those synapses.

It exists so that the moment a user launches stars, the neurons that fired together in one memory are wired together
(Hebbian [L1]); two memories that reuse a neuron are thereby already connected through it (shared neuron [L2]); and
neurons that co-fire in memories written close in time are strengthened a little more (temporal-proximity bonus [L4]).
Every edge is neuron↔neuron ([L6][I6]) — there is never a memory↔memory edge ([I4][L7]). The "nearby-in-time memories
cluster together" behavior is not implemented as a force between memories; it **emerges** from shared neurons + near
seed + the temporal bonus, and is realised as layout by the force-sim (plan 19), never stored.

`Link` reuses the pure plasticity functions from [18.synapse-plasticity](18.synapse-plasticity.md) for all strength
math and persists through the atomic weighted upsert defined by [16.memory-aggregate-schema](16.memory-aggregate-schema.md).
It computes *no* similarity/embedding linking ([L3]) and owns no rendering.

## Scope / Non-goals

**In scope**
- The `Link` use-case in `internal/memory`, invoked by `PersistEncoded` (plan 20) at launch, that grows the neuron
  synapse graph for the launched memory (and its relation to the user's existing neurons).
- **Same-memory Hebbian wiring [L1]:** within one launched `EpisodicMemory`, every pair of co-activated neurons gets a
  synapse (all pairwise edges).
- **Shared-neuron connection [L2]:** when a launched memory reuses a neuron that already belongs to earlier memories,
  the shared neuron **is** the link — no extra edge is created; the connection is the neuron's existing
  `neuron_activations` membership, which `GetUniverse` (plan 23) already exposes.
- **Temporal-proximity bonus [L4]:** neurons co-activated across memories whose diary dates fall inside a narrow window
  (`synapse.temporal_window_days`) receive extra strengthening; time attaches to the **memory's date**, never to a
  neuron ([E6]).
- Strength math delegated to plan 18 pure functions: tiered `InitialStrength` (same-memory > shared-neuron > temporal,
  [L10]) for a first-time pair; saturating `Potentiate` toward the `1.0` cap ([L8][L9]) for a repeat co-firing.
- Persistence via the atomic weighted upsert into `synapses`
  (`INSERT … ON CONFLICT (user_id, neuron_a_id, neuron_b_id) DO UPDATE`, ARCHITECTURE §2.6): incrementing
  `co_activation_count`, advancing `last_activated_universe_time`, and writing the new stored base `strength`. Canonical
  undirected ordering `neuron_a_id < neuron_b_id`; per-user scoped.
- The consumer-owned `SynapseStore` port surface `Link` needs (declared where consumed), backed by the queries plan 16
  provisions.

**Non-goals**
- **No similarity / embedding linking [L3].** `Link` never creates an edge from vector nearness. Embeddings assist
  neuron *identity* (dedup [E10], plan 20) and the latent-field, never connection.
- **No memory↔memory edge or memory↔memory attraction [I4][L7][E9].** There is no memory-to-memory table, force, or
  synapse. Inter-memory proximity is emergent, not modelled.
- **No stored positions, constellations, or coordinates [I5].** Constellations are an emergent read-side relationship
  computed by the force-sim (plan 19) from shared neurons + synapse strength — not a domain source-of-truth and not a
  stored table (ARCHITECTURE §2.9 #3).
- **No plasticity math redefinition.** `Link` calls plan 18's pure functions; it does not re-derive initial values,
  potentiation curves, the cap, or read-time effective strength.
- **No read-time effective strength / decay.** `Link` writes the stored *base* only; time-decayed **effective** strength
  is a read-time pure-function derivation ([L5]), owned by plan 18 / `packages/memory-logic` and consumed by plan 23.
- **No LLM extraction, embedding, semanticization, or persistence of the aggregate root** — those are plan 20 (`Encode`/
  `PersistEncoded`) and plan 22 (worker). `Link` receives already-persisted neurons/activations and only writes synapses.
- **No async queue mechanics.** Whether cross-memory refinement runs in the `link` job is plan 22's concern; this plan
  fixes the *rules* and picks launch-time execution for correctness (see Design).
- **No universe-clock authority.** Universe time is derived from the launched memory's `created_universe_time` (= its
  `diary_date`); the per-user clock is Epic B (seam only).

## Grounding
- Constitution ([I1]–[I11]): [00.overview](00.overview.md) §3 (*The constitution*), owned by [PRD.md](../PRD.md) §7.
  This unit is bound by: **[I4]/[I6]** synapses are neuron↔neuron only — there is no memory↔memory edge or attraction,
  inter-memory proximity is emergent from shared neurons + synapse strength; **[I5]** positions and constellations are
  emergent (force-sim output) and never stored — the server owns only neurons, synapses, activations, and centrality
  facts; **[I1]** no system delete (`Link` only inserts/strengthens, never removes an edge).
- tech / policy / concept basis:
  - [ARCHITECTURE.md](../ARCHITECTURE.md) §2.2 (core-domain context: `Link` is behavior published by `internal/memory`),
    §2.3 (context layout — start-as-one-package; use-case behavior lives in the context package until a split earns it),
    §2.4 (the dependency rule — `Link` is context behavior consuming a consumer-owned `SynapseStore`/`NeuronStore` port;
    no proto/sqlc leaks inward), §2.5 (aggregate boundaries — `Synapse` is a neuron↔neuron edge, an emergent relationship
    is not domain source-of-truth), §2.6 (atomic weighted upsert `INSERT … ON CONFLICT … DO UPDATE`, weighted-graph),
    §2.8 (async work & optimistic write — refinement may be a `link` job), §2.9 (#3 computed relationship not
    source-of-truth), §4 (per-user isolation).
  - [PRD.md](../PRD.md) §5.3.2 linking — **[L1]** same-memory Hebbian, **[L2]** shared neuron = the link, **[L3]** no
    similarity linking, **[L4]** temporal-proximity bonus (narrow window; time on the memory's date), **[L5]** stored
    base + read-time effective strength, **[L6]** synapse neuron↔neuron only, **[L7]** no memory-attraction (emergent),
    **[L8]** saturating potentiation, **[L9]** single `1.0` cap, **[L10]** low tiered initials (same > shared > temporal).
  - [PRD.md](../PRD.md) §5.3.1 encoding — **[E7]** new neurons seed near reused neurons / near recently-active neurons
    (why nearby-in-time memories cluster, the cause of [L4]); **[E7a]** latent (gray) neurons are rendering-only and not
    persisted; **[E9]** memories derive from neurons, no direct inter-memory force.
  - Sibling plans: [16.memory-aggregate-schema](16.memory-aggregate-schema.md) (the `synapses`/`neuron_activations`
    tables, the undirected uniqueness constraint, the upsert query surface), [18.synapse-plasticity](18.synapse-plasticity.md)
    (the pure `InitialStrength`/`Potentiate` functions and the `1.0` cap this unit consumes),
    [19.force-sim-layout](19.force-sim-layout.md) (where constellation clustering emerges as layout, never stored),
    [20.encode-usecase](20.encode-usecase.md) (`PersistEncoded`, which persists the aggregate and then calls `Link`).
  - Shared design brief (Epic A SSOT): §2 (`synapses` table), §4 (ports), §6 (values keys), §7 (cross-cutting
    decisions).

## Design

### Where `Link` lives and when it runs

`Link` is application behavior published by the core `internal/memory` context (ARCHITECTURE §2.2). Per §2.3 it starts
in the context package alongside `Encode`/`PersistEncoded` (plan 20) and moves to `memory/app` only if the context
later earns the `{domain,app,pg,rpc}` split. It is a pure orchestration over the domain plasticity functions plus one
persistence port — it holds no IO of its own.

**`Link` runs synchronously as the last step of `PersistEncoded` at launch, in the same transaction that persisted the
`Diary`, `EpisodicMemory`, `Neuron`s, and `NeuronActivation`s.** The launch path is:

```
LaunchStars (rpc) → PersistEncoded:
    persist Diary + EpisodicMemory + Neuron (dedup applied) + NeuronActivation
    → Link(user, launchedMemory, itsNeurons)        // synapses, same tx
    → enqueue `embed` + `semanticize` jobs          // async (plan 22)
    → return memory ids                             // optimistic (§2.8)
```

Rationale for launch-time (not deferred to the `link` job): the synapse graph is a **correctness** fact of the write,
not a slow external call. Running it inside the launch transaction keeps the atomic upserts idempotent under the write's
own retry, guarantees `GetUniverse` (plan 23) reads a consistent graph on the very next read, and needs no LLM/embedding
call. The async `link` job (plan 22) is reserved for *heavier cross-memory refinement* if it is ever needed (for
example, re-scanning a large existing graph for temporal bonuses beyond the launched memory's own neurons). The brief
(§7) allows same-memory Hebbian at launch and cross-memory shared/temporal linking in the `link` job; **decided
(2026-07): for Epic A this plan runs all three rules at launch, synchronously inside the `PersistEncoded`
transaction** — the async `link` job stays a reserved seam, promoted only if a launch is observed to touch too much of
the graph to stay in the request path.

### Inputs

`Link(ctx, scope platform.UserScope, launched EpisodicMemory, neurons []Neuron)` receives, from `PersistEncoded`:

- the authenticated user (every read and write is scoped to `user_id`, ARCHITECTURE §4);
- the just-persisted `EpisodicMemory` — needed for its `created_universe_time` (= `diary_date`), which drives the
  temporal-window comparison and the `last_activated_universe_time` written on each edge;
- the launched memory's co-activated neurons (after dedup [E10]) — the set the Hebbian rule wires and the set whose
  prior co-activations the temporal bonus examines.

`Link` does not receive or read embeddings, proto types, or sqlc rows — only domain values and the `SynapseStore`/
`NeuronStore` ports (ARCHITECTURE §2.4).

### Rule 1 — same-memory Hebbian wiring [L1]

For the launched `EpisodicMemory`, every **unordered pair** of its co-activated neurons `(a, b)`, `a ≠ b`, gets a
synapse. With *n* co-activated neurons this is the full `n·(n−1)/2` set of pairwise edges. For each pair, `Link`:

1. normalises the pair to canonical undirected order `neuron_a_id < neuron_b_id`;
2. classifies the signal as **same-memory** (this pair co-fired within one memory) and asks plan 18 for the tier;
3. upserts the edge (see *Strength & persistence*).

Same-memory is the **strongest** initial tier ([L10]) and always applies when both neurons are members of the launched
memory, regardless of whether either neuron is new or reused.

### Rule 2 — shared-neuron connection [L2] (the shared neuron *is* the link)

When the launched memory reuses a `Neuron` that already belongs to earlier memories (dedup [E10] made it the *same*
neuron, not a new one), those memories are **thereby connected** — through the shared neuron. `Link` creates **no extra
edge** for this: the connection is the neuron's `neuron_activations` membership across memories, which the read side
(`GetUniverse`, plan 23) already returns and the force-sim (plan 19) already uses to pull those memories toward the
shared neuron's centroid.

The **shared-neuron tier** applies to a *different* case: a synapse between two neurons that are connected because they
co-occur across memories via sharing (not within one memory). Concretely, when the launched memory introduces a
co-activation between neuron `a` (new to this memory) and neuron `b` where the pair's relationship is established by
cross-memory sharing rather than same-memory firing, the edge is initialised at the shared-neuron tier
(same-memory > **shared-neuron** > temporal, [L10]). The rule that *no synapse is ever drawn memory↔memory* holds
absolutely: sharing produces neuron↔neuron edges (or is expressed purely through activation membership), never a
memory-to-memory relationship ([I4][I6][L7]).

### Rule 3 — temporal-proximity bonus [L4] (narrow window, on the memory's date)

Time is **not a neuron** ([E6]): it attaches to a memory's date. When neurons co-activate across memories whose
`created_universe_time` (= `diary_date`) fall within `synapse.temporal_window_days` of each other, their synapse gets
extra strengthening. In practice, for a pair `(a, b)` whose edge is being created or reinforced by the launch, `Link`
checks whether the pair's co-activation spans memories whose dates are within the window; if so it adds the
`synapse.temporal_bonus` on top of the tier's base — either bumping a first-time edge above its bare temporal initial or
adding to the potentiation of a repeat.

The window is **narrow** by design ([L4]): the bonus models competitive/excitability-based allocation (recently active
neurons are likelier to be reused by a near-in-time next memory — [E7], Han 2007), not a broad "same era" effect. The
temporal-only tier (a pair connected *only* by temporal proximity, with neither same-memory co-firing nor sharing) is
the **weakest** initial tier ([L10]). Outside the window, no temporal bonus is applied — plain Hebbian/shared rules
still govern the edge.

The bonus never introduces a memory↔memory edge and never stores time on a neuron. It only adjusts a neuron↔neuron
synapse's stored base strength, keyed on the memories' dates.

### Rule 4 — no similarity / embedding linking [L3]

`Link` never creates or strengthens an edge from embedding nearness. Semantic similarity guides neuron **identity**
(conservative dedup [E10], done in `Encode`, plan 20) and later the latent-field assist — never *connection*. The
constellation graph grows only from neuron reuse ([L2]) + Hebbian co-firing ([L1]) + the temporal bonus ([L4]). This is
an explicit guard against constellation collapse (over-merging by similarity), and it means `Link` reads no
`embeddings` rows.

### Rule 5 — emergent constellations, never stored [I5][L7]

"Nearby-in-time memories cluster" is **not** implemented as a memory-attraction force ([L7][E9]). It emerges from three
stored/seed facts, combined only at read/layout time:

- **shared neurons** ([L2]) — memories that reuse a neuron are pulled toward its centroid by the force-sim;
- **near seed** ([E7]) — a new neuron's seed starts near reused / recently-active neurons (a presentation seed, final
  position emergent);
- **temporal bonus** ([L4]) — near-in-time co-firing makes the neuron↔neuron synapses stronger, so the force-sim's
  link force draws them tighter.

`Link` owns only the third (and contributes the neuron↔neuron edges the first two also rely on). Constellations
themselves are an **emergent relationship**: not a domain source-of-truth, not a memory↔memory table, computed by the
force-sim (plan 19) from shared neurons + synapse strength (ARCHITECTURE §2.5, §2.9 #3, [I4]). `Link` writes no
coordinate and no cluster.

### Strength & persistence (plan 18 pure fns + atomic upsert)

`Link` decides *which* pairs and *which tier/bonus* apply; **all numeric strength math is plan 18's pure functions**
([18.synapse-plasticity](18.synapse-plasticity.md)), so the same math is reproducible client-side (golden-parity):

- **First-time pair** → `InitialStrength(signal)` returns the tier's low starting base
  (`synapse.initial_same_memory` > `synapse.initial_shared_neuron` > `synapse.initial_temporal`, [L10]), plus
  `synapse.temporal_bonus` when Rule 3's window applies.
- **Repeat co-firing (pair already has a row)** → `Potentiate(current, …)` increases the stored base **in proportion to
  the remaining headroom `(1 − current)`** ([L8]), saturating smoothly toward the single cap `synapse.strength_cap = 1.0`
  ([L9]); the temporal bonus, when in window, feeds the potentiation. Frequent pairs converge to the cap without
  runaway.

Persistence is one **atomic weighted upsert** per edge into `synapses`, using the query plan 16 provisions
(ARCHITECTURE §2.6):

```sql
INSERT INTO synapses (id, user_id, neuron_a_id, neuron_b_id,
                      strength, co_activation_count, last_activated_universe_time, created_at)
VALUES (@id, @user_id, @a, @b, @strength, 1, @universe_time, now())
ON CONFLICT (user_id, neuron_a_id, neuron_b_id) DO UPDATE
SET strength                     = @strength,          -- Potentiate(existing.strength, …), computed by Link (plan 18)
    co_activation_count          = synapses.co_activation_count + 1,
    last_activated_universe_time = GREATEST(synapses.last_activated_universe_time, @universe_time);
```

- The `ON CONFLICT` target is the `UNIQUE (user_id, neuron_a_id, neuron_b_id)` constraint (plan 16). Because `Link`
  canonicalises `a_id < b_id` before writing, an unordered neuron pair has exactly one row per user — the invariant that
  makes the upsert **idempotent** ([L8]): re-running `Link` for the same launch (write retry) advances the same row
  rather than creating duplicates.
- On the **repeat** path, `Link` first reads the existing stored base (through `SynapseStore`) to compute
  `Potentiate(existing, …)` in the domain, then writes the result — the stored `strength` is always the **base**, never
  the read-time effective value ([L5]).
- `last_activated_universe_time` and `co_activation_count` are the *accumulated history* facts ([L5]) the read-time
  `EffectiveSynapseStrength` decay consumes; `Link` writes them, plan 23 reads them, plan 18 derives the decay.
- Every statement is scoped by the authenticated `user_id` (ARCHITECTURE §4); `Link` never touches another user's edges.

### The `SynapseStore` port

`Link` consumes a small consumer-owned port declared in `internal/memory` (ARCHITECTURE §2.4, brief §4), implemented by
`memory/pg` (the only package that touches sqlc/pgx). The surface `Link` needs:

- read a user's existing synapse base strength for a canonical neuron pair (for the `Potentiate` repeat path);
- the atomic upsert above (`UpsertSynapse`), taking the canonical pair, the computed base strength, and the universe
  time;
- read, for the launched memory's neurons, their prior co-activations / memberships needed to classify the shared and
  temporal signals (backed by `neuron_activations` — `NeuronStore`/activation queries from plan 16).

No sqlc row escapes `memory/pg`; the port speaks domain values only.

## Acceptance Criteria
1. After `PersistEncoded` launches a memory with *n* co-activated neurons, **all** `n·(n−1)/2` unordered neuron pairs of
   that memory have a synapse row (same-memory Hebbian wiring) [L1].
2. When a launched memory reuses an existing neuron, **no extra edge** is created to represent the sharing; the two
   memories are connected purely through the shared neuron's `neuron_activations` membership, and `GetUniverse` returns
   that shared membership — the shared neuron *is* the link [L2].
3. Two neurons co-activated across memories whose diary dates are within `synapse.temporal_window_days` receive the
   `synapse.temporal_bonus`; outside that window no temporal bonus is applied. The window is compared on the memories'
   dates, and no time value is written onto any neuron [L4][E6].
4. `Link` creates or strengthens **no** synapse from embedding/semantic similarity, and reads no `embeddings` rows;
   the graph grows only from neuron reuse + Hebbian co-firing + the temporal bonus [L3].
5. Every synapse `Link` writes is between two neurons; there is no memory↔memory edge, table, force, or attraction
   anywhere in the use-case, and inter-memory proximity remains emergent [I4][I6][L6][L7][E9].
6. A first-time pair is initialised via plan 18's `InitialStrength` at the correct tier, with
   `synapse.initial_same_memory` > `synapse.initial_shared_neuron` > `synapse.initial_temporal` [L10]; the tier of a
   pair is determined by its strongest applicable signal.
7. A repeat co-firing strengthens the stored base via plan 18's `Potentiate`, increasing in proportion to
   `(1 − current)` and never exceeding `synapse.strength_cap = 1.0`, converging smoothly toward the cap [L8][L9].
8. Persistence uses `INSERT … ON CONFLICT (user_id, neuron_a_id, neuron_b_id) DO UPDATE`, storing the canonical
   `neuron_a_id < neuron_b_id`, incrementing `co_activation_count`, advancing `last_activated_universe_time`, and writing
   the stored **base** strength (not the read-time effective value); re-running `Link` for the same launch is idempotent
   (advances the same row, creates no duplicate) [L5][L8].
9. Constellations are never stored: `Link` writes no coordinate, cluster, or memory↔memory relationship; clustering is
   computed by the force-sim (plan 19) from shared neurons + synapse strength at layout time [I5][L7] (ARCHITECTURE §2.9 #3).
10. Every read and write is scoped to the authenticated `user_id`; `Link` never reads or writes another user's neurons,
    activations, or synapses [U1] (ARCHITECTURE §4).
11. All strength values come from plan 18's pure functions (no strength arithmetic is re-implemented in `Link`), so the
    same inputs produce the same stored base under the client-side golden-parity mirror.
12. `Link` performs no LLM/embedding call and no hard delete of any neuron or synapse; it only inserts or strengthens
    edges [I1].

## Policy / Values Impact
- **policy / tech:** no `spec/policy/**` tree exists yet, and this unit's own `tech/` doc is not yet written, so no
  policy/tech file is created now (`spec/tech/` already holds the Foundation as-built docs). Once implemented, the
  linking rules stated here — **linking is neuron-only ([L6][I6]); the shared neuron *is* the
  link, with no extra edge ([L2]); constellations are emergent, computed by the force-sim, never stored ([I5][L7]); no
  similarity/embedding edges ([L3]); tiered low initials with saturating potentiation ([L8][L9][L10])** — become the
  content of a future `tech/memory` / `policy/linking` doc and remain consistent with
  [ARCHITECTURE.md](../ARCHITECTURE.md) §2.5, §2.6, §2.8, and §2.9.
- **spec/values.yaml:** this unit **introduces** two keys and **consumes** the plan 18 plasticity keys. Introduced by
  this unit, added at implement via `pnpm gen:values` (not now):
  - `synapse.temporal_window_days` — the **narrow** window (in days, on the memories' diary dates) inside which a
    co-activation earns the temporal bonus ([L4]).
  - `synapse.temporal_bonus` — the extra strengthening added to a synapse whose co-activation falls in the temporal
    window ([L4]).
  Consumed here but **owned by [18.synapse-plasticity](18.synapse-plasticity.md)** (declared there, not by this unit):
  `synapse.initial_same_memory`, `synapse.initial_shared_neuron`, `synapse.initial_temporal` (tiered initials, [L10]);
  `synapse.potentiation_rate` (saturating LTP coefficient, [L8]); `synapse.strength_cap` (= `1.0`, [L9]). All are
  scalars with their single source in `spec/values.yaml`, never hardcoded in Go/TS. The exact numeric values (window
  size, bonus magnitude) are tuning details set at implement.
- **Dependencies:** depends on [16.memory-aggregate-schema](16.memory-aggregate-schema.md) (the `synapses` /
  `neuron_activations` tables, the undirected uniqueness constraint, and the atomic upsert query surface),
  [18.synapse-plasticity](18.synapse-plasticity.md) (the pure `InitialStrength` / `Potentiate` functions and the cap it
  consumes), and [20.encode-usecase](20.encode-usecase.md) (`PersistEncoded`, which persists the aggregate and then
  invokes `Link`). It pairs with [19.force-sim-layout](19.force-sim-layout.md) (where constellation clustering *emerges*
  as layout from the synapses `Link` writes) and [22.ai-worker-pipeline](22.ai-worker-pipeline.md) (which owns the
  `link` job seam — reserved, unbuilt in Epic A — should cross-memory refinement ever be promoted out of the launch
  path).
