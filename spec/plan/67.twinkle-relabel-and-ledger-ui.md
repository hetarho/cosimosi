# 67. twinkle relabel and ledger ui

> Twinkle relabel and ledger UI — the visible half of the currency promotion. Every balance surface is relabeled to
> **작은 별가루 / 별가루** by **renaming the i18n keys** (so a stale call site is a compile error, not a silently wrong
> string), the **payment charge path is removed from the UI** (deferred to v3), and the append-only ledger becomes
> readable as a **chronological history in `/me`'s stardust tab** ([G7][U9]). It edits the shipped stardust vertical
> rather than adding a parallel one; the ledger read, the reason set, and the `/me` page shell belong to siblings.
> Scope: FE | Status: built (job 102)
>
> Layer P. Home: the shipped `widgets/stardust` + `features/{twinkle-balance-hud,earn-twinkle}` verticals plus a NEW
> `features/twinkle-ledger` slice, with the pure substance in `packages/twinkle`; web + mobile. It fills the last seam
> of Epic L's presentation and leaves the server ledger read to [66.earn-and-purchase-spend](66.earn-and-purchase-spend.md)
> and the `/me` shell to [64.my-page](64.my-page.md).

## Purpose

Epic L redefines Twinkle by **purpose** rather than provenance: `SMALL` (작은 별가루) is the daily-refilled tier that may
pay only for the recall family, `GENERAL` (별가루) is the permanent, universal tier ([G2][G2a]). The domain,
the contract and the pure mirrors are renamed by [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md). But
the words the diarist actually reads still say **기본 별가루 / 추가 별가루** — "the default one" and "the extra one" —
which is exactly the provenance semantics v2 discards. Until the labels move, the product teaches the old model.

The second gap is honesty. The shipped charge sheet ([45.stardust-ui](45.stardust-ui.md)) offers a **buy** button that
cannot succeed: `startStorePurchase` always throws `PaymentUnavailableError` and the server binds
`UnavailablePaymentVerifier{}`. Payment is deferred to v3 (PRD §8.3), and v2 funds itself from the daily refill, diary
writing, achievements, invites and the signup bonus ([G3]). Shipping v2 with a dead purchase affordance — and an invite
**code** field, when [U8]'s invite is link-bound with no code to type — is shipping a lie. Both come out, and the
insufficient-balance recovery re-points from "채우기" (top up) to "모으기" (gather): the earn paths, and the `/me`
achievements tab where they are claimed.

The third gap is [G7]. Every purchase, claim, earn and spend already lands in one append-only ledger, and [G7] promises
it is **readable chronologically in the my-page stardust tab** ([U9]). Today no surface reads it at all — the diarist
sees a balance that moves with no account of why. This unit builds that history: newest-first, keyed by reason, with the
tier split shown on every spend so the two-kind model is taught by the record itself rather than by explanation.

What it deliberately does not do is invent a fourth surface. `widgets/stardust` already composes the balance HUD over
the running canvas; `/me` already exists as a page to be renamed. This unit **edits the shipped vertical** and adds one
feature slice.

## Scope / Non-goals

**In scope**

- **The relabel, as key renames** ([G2][G2a]): `twinkle_balance_basic_label` → `twinkle_balance_small_label`
  (`작은 별가루` / `Small stardust`), `twinkle_balance_additional_label` → `twinkle_balance_general_label`
  (`별가루` / `Stardust`), across both catalogues, both apps, all call sites.
- **The charge → earn relabel** of the surrounding key family and slice, so no surface still says "채우기" (top up) for
  an economy with no purchase path ([G3]).
- **Removal of the payment UI:** the pay `<section>` and the `onPay`/`paying` props, `packages/twinkle/src/charge.ts`,
  `packages/twinkle/src/pack.ts` (`CHARGE_PACK`), the `PAY` event + `paying` state, the four payment/charge error
  reasons in `packages/errors/src/core.ts`, the three `twinkle_charge_pay_*` keys, and `values.yaml → twinkle.charge_pack`.
- **Removal of the invite-code field:** `ClaimInvite` leaves the wire
  ([66.earn-and-purchase-spend](66.earn-and-purchase-spend.md)), so the `TextField`, the two
  `twinkle_charge_invite_code_*` keys and `claimInvite` go with it. The invite path becomes explanatory.
- **`features/earn-twinkle`** — the renamed earn guide: a restrained explainer of the repeatable earn paths
  (daily refill · 일기 작성 · 업적 · 친구 초대, [G3]) with exactly one affordance, opening `/me`'s achievements tab. It
  performs **no mutation**, keeping `ui/WriteEarnFeedback.tsx` unchanged.
- **The recovery re-target:** `ErrorRecovery`'s `'charge'` becomes `'earn'`, `useChargeRequestStore` becomes
  `useEarnRequestStore`, and the six shortfall call sites (recall flow · gist view · diary reader, web + mobile) open
  the earn guide instead of a charge sheet. `spec/policy/platform/errors.md`'s recovery column follows.
- **`features/twinkle-ledger`** — the NEW `/me` stardust-tab content ([G7][U9]): the two tier balances, and the
  chronological history over [66](66.earn-and-purchase-spend.md)'s `GetLedger`† — newest-first, keyset-cursor infinite
  scroll, one row per ledger entry with its reason label, signed amount and (for spends) tier split.
- **The daily-refill marker:** one synthetic, visually distinct marker for **today**, labeled a refill rather than a
  transaction, its figure read from generated config — so the history has no unexplained gap and [G7] still reads
  "every _event_ is ledgered".
- **web + mobile parity** ([00.overview](00.overview.md) §4): both apps ship every surface above; `model`/`api` shared
  verbatim through `packages/twinkle`, `ui` authored per platform (§3.5).

**Non-goals**

- **The `GetLedger` RPC, its query, its `EntryReason` enum and its `http-policy.ts` classification** —
  [66.earn-and-purchase-spend](66.earn-and-purchase-spend.md) owns them as one two-sided change, under the
  single-owner rule in [00.overview](00.overview.md) §5.6. This unit consumes the contract and adds no proto field.
- **The `SMALL`/`GENERAL` domain, proto and mirror rename** —
  [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md), including
  `TwinkleBalance{small,general}` in `packages/twinkle/src/twinkle-balance-store.ts` and the
  `forbiddenSynonyms` gate entries. This unit renames **copy and i18n keys**, never a type field.
- **The `/me` route rename, page shell, tab registry and tab labels** — [64.my-page](64.my-page.md). This unit
  delivers the stardust tab's **content** as a feature slice the page composes.
- **The achievements tab content and the claim flow** — [76.achievement-ui](76.achievement-ui.md); the earn guide only
  routes there.
- **The shareable invite link** (`account.v1 GetInviteLink`†) — it needs the account read seam
  [60](60.account-profile-model.md)/[61](61.signup-and-invite-usecase.md) establish and belongs to `/me`
  ([64.my-page](64.my-page.md)). The client-half removal list's "all but `claimInvite`'s replacement" resolves
  here: the replacement is not in
  `packages/twinkle` at all — the invite binds from a link at signup ([61][63]) and its link is read in `/me`.
- **Any purchase surface** — ornament prices, the decoration panel and `error_store_insufficient` are
  [73.decoration-panel](73.decoration-panel.md)/[72.decorate-usecase](72.decorate-usecase.md). No price
  string is introduced here, and payment is **not** re-introduced in any form (v3, PRD §8.3).
- **The `SMALL`-ineligible error sibling key** — it accompanies the reason that emits it, so
  [66](66.earn-and-purchase-spend.md) owns it. This unit adds no error reason and no error key.
- **No cost-curve, balance or remaining-`SMALL` math in the FE.** `smallRemaining` stays a golden-parity mirror in
  `packages/twinkle-logic`, not a display path; the tab renders `GetBalance` verbatim.
- **No admin grant surface** — [58.admin-console](58.admin-console.md) owns the operator side; `admin_grant` appears
  here only as a rendered reason label.

## Grounding

- Constitution ([I1]–[I13]): [00.overview](00.overview.md) §3 (_The constitution_), owned by [PRD.md](../PRD.md) §7.
  The invariants this unit is most at risk of breaking, each with the **structural** guard the v2 risk register
  assigns it:
  - **[G7] "one ledger, no unledgered event"** — risk: the synthetic refill marker reads as a transaction the ledger
    lost. Guard: the marker is rendered by a **different component** that takes no `EntryReason` and no entry id, so it
    cannot be produced from, or mistaken for, a row; there is exactly one and it is scoped to today.
  - **[I11] "customization/economy never touches the meaning layer"** — risk: a history row becomes a link to the star
    it priced, turning the ledger into a free navigation index into memories. Guard: the consumed `LedgerEntry`
    carries **no episodic-memory id and no diary id** (see Design), so a jump is unrepresentable, not merely unwired.
  - **[I2] "the `Diary` is never mutated"** — guard: this unit is read-only over the twinkle context; it issues no
    `memory.v1` call at all.
  - **[P9] "`SMALL` may not buy ornaments"** — risk: a summed headline overstates purchasing power. Guard: the stardust
    tab shows the two tiers with **no summed figure** (Design § _Two tiers, one currency_).
- [PRD.md](../PRD.md) **§5.9 [G2][G2a][G3][G7]** — [G2] fixes the two kinds and the spend order `SMALL`→`GENERAL`;
  [G2a] makes 별가루 "제품의 유일한 화폐 단위" and 작은 별가루 "별개 화폐가 아니라 용도 제한된 하위 종류", which is what the
  labels must convey; [G3] enumerates the earn set and states "결제는 v2 범위가 아니다 — v3에서 붙인다"; [G7] requires that
  "구매·수령·적립·소모가 모두 같은 원장에 남아 마이페이지 별가루 탭에서 시간순으로 열람 가능하다".
- [PRD.md](../PRD.md) **§4.1 [U9]** — `/me` carries a **별가루** tab holding "잔액 2종·적립·소모 내역 [G7]"; this unit
  supplies that tab's content. **[U7]** makes `User.Timezone` the day boundary, which is why the history's day grouping
  is server-computed rather than device-local.
- [PRD.md](../PRD.md) **§5.9 [G1][G4][G5]** — the recall family is what `SMALL` pays for, so the HUD's total is honest
  for a recall and the tab's tier split is what a purchase must read.
- [ARCHITECTURE.md](../ARCHITECTURE.md) **§3.1** — six one-way layers, one `index.ts` per slice, segments by technical
  role; **a feature may not import a feature**, which is why the tab renders its own figures (below). **§3.2** —
  control-state in XState only where there are exclusive phases; collections/caches in Zustand/Query. **§3.4** — the
  domain→visual projection is one-way; this unit imports no `three` and no visual entity. **§3.5** — `model`/`api` and
  all of `packages/` shared verbatim, `*.native.tsx` only where a primitive genuinely differs. **§2.7** — unary only,
  no polling; reads refresh by invalidation.
- **Cross-plan constraints:** every layer of the `SMALL`/`GENERAL` redefinition below the copy — the domain types,
  the `twinkle.v1`/`admin.v1` contracts, `TwinkleBalance{small,general}` in
  `packages/twinkle/src/twinkle-balance-store.ts`, the `twinkle.small_daily_amount` key and the
  `lint-ubiquitous-language.mjs` refresh — belongs to
  [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md), which also fixes what the tier labels must imply:
  `SMALL` pays for the recall family only, so a purchase prices against `GENERAL` alone. This unit renames **copy and
  i18n keys**, never a type field. `GetLedger`†, the closed `EntryReason` enum it renders (including `payment`, kept
  so historical rows stay readable), the keyset-cursor page shape and the single
  `packages/client-cache/src/http-policy.ts` classification that every `NO_SIDE_EFFECTS` method carries belong to
  [66.earn-and-purchase-spend](66.earn-and-purchase-spend.md), sole owner of the reason set and of every
  `twinkle_ledger_entries` DDL change; this unit consumes that contract and adds no field.
  `twinkle.ledger_page_size` is 66's key inside the **existing** `twinkle` group — groups are closed, keys inside
  them are not — per the values-group closure rule in [00.overview](00.overview.md) §4, which also carries the
  parity matrix that gives this unit web ✅ mobile ✅ with no waiver. Payment is deferred to v3 (PRD §8.3): 66
  removes the server half, this unit removes the client half in full and owns the `MEMORY_INSUFFICIENT_TWINKLE` /
  `TWINKLE_INSUFFICIENT` recovery re-target; landing last, it is also the unit that deletes `twinkle.charge_pack`
  once 66's Go reader is gone. There is no `daily_grant` row and no cron — the daily `SMALL` refill is a
  derivation, and rendering it as a labelled synthetic marker rather than a transaction is this unit's rule to
  implement. The relabel is performed by **renaming** keys in both `packages/i18n/messages/{ko,en}.json` and
  re-running `pnpm gen:messages` under `pnpm check:gen`, never by editing a value
  ([08.i18n-seam](08.i18n-seam.md)). `/me`, its page shell and its tab registry are
  [64.my-page](64.my-page.md)'s rename; the stardust tab arrives as this unit's `features/twinkle-ledger` slice —
  with its own scoped `steiger.config.js` block — for that page to compose. The invariant risk register naming the
  guards above is [00.overview](00.overview.md) §3; the sprint order 65 → 66 → **67**, which is why both siblings
  have landed before this one, is [00.overview](00.overview.md) §5.6.
- Sibling plans — **builds on** [45.stardust-ui](45.stardust-ui.md) (the shipped HUD, charge sheet, cost display,
  stardust machine and charge-request store this unit edits), [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md)
  (the renamed `TwinkleBalance{small,general}` and `GetBalanceResponse{small,general,total}` it reads),
  [66.earn-and-purchase-spend](66.earn-and-purchase-spend.md) (`GetLedger`†, the `EntryReason` enum, the removal of
  `Charge`/`ClaimInvite` from the wire, `twinkle.ledger_page_size`); **consumed by** [64.my-page](64.my-page.md)
  (composes `features/twinkle-ledger` as the stardust tab and supplies the `/me` navigation),
  [76.achievement-ui](76.achievement-ui.md) (the tab the earn guide routes to); **paired with**
  [59.error-taxonomy-and-handling](59.error-taxonomy-and-handling.md) (the `ErrorRecovery` seam re-targeted here) and
  [08.i18n-seam](08.i18n-seam.md) (the catalogue mechanics).

## Design

### Placement (FSD §3.1 / §3.5 — edit the shipped vertical, add one slice)

```
packages/twinkle/src/
├── earn-request-store.ts     RENAMED from charge-request-store.ts — the decoupled shortfall → open-earn-guide seam
├── react/ledger.ts           NEW — useTwinkleLedgerInfiniteQuery over [66]'s GetLedger options helper
├── ledger-day.ts             NEW — pure: group entries by their server-supplied local date; the today-refill marker model
├── charge.ts · pack.ts       DELETED — no purchase, no pack, no ClaimInvite on the wire
└── twinkle-balance-store.ts  UNCHANGED here — [65] renamed its fields; this unit only reads them

packages/universe/src/stardust.machine.ts   DELETED (+ its test) — see Control-state, below

apps/{web,mobile}/src/features/
├── twinkle-balance-hud/   ui — relabeled to 작은 별가루 / 별가루
├── earn-twinkle/          RENAMED from charge-twinkle — ui/EarnGuideSheet.tsx (replaces ChargeSheet.tsx) + ui/WriteEarnFeedback.tsx
│                          (web, as built later: ui/EarnGuideSections.tsx — the same paths without the Dialog)
├── twinkle-ledger/        NEW — ui/TwinkleBalanceSummary.tsx · ui/TwinkleLedgerList.tsx · ui/LedgerEntryRow.tsx
└── spend-cost-display/    ui — the shortfall affordance relabeled to the earn guide

apps/{web,mobile}/src/widgets/stardust/ui/StardustOverlay.tsx   hosts HUD + earn guide + write-earn feedback; no mutation left
                          (web, as built later: + ui/TwinkleDetailSheet.tsx — the 별가루 panel the HUD opens)
```

`features/twinkle-ledger` carries **no `api` segment**: its read hook is `packages/twinkle/src/react/ledger.ts`, because
a byte-identical `api` module in both apps fails `scripts/lint-fsd-layout.mjs` R4 until it is promoted to its package.
`apps/web/steiger.config.js`'s plan-45 block is edited in place: `./src/features/charge-twinkle/**` becomes
`./src/features/earn-twinkle/**`, `./src/features/twinkle-ledger/**` is added, and the stale
`./src/entities/twinkle/**` path (the mirror was promoted to `packages/twinkle` long ago) is dropped.

**The tab does not mount `features/twinkle-balance-hud`** — §3.1 forbids a feature importing a feature, and the
HUD's chrome is over-canvas (`pointer-events-auto`, `backdrop-blur`) which has no business inside a page tab. The reuse
is of the **shared store and query in `packages/twinkle`**:
`features/twinkle-ledger/ui/TwinkleBalanceSummary.tsx` reads the same
`useTwinkleBalanceStore` + `useTwinkleBalanceQuery` and lays the same two figures out in tab chrome. Nothing is
duplicated except layout, which is per-platform anyway.

### The relabel is a key rename, not a value edit

Renaming the key makes every stale call site a **compile error** — `packages/i18n` generates one module per key and
`m.twinkle_balance_basic_label` simply stops existing — whereas editing the value would leave a silently wrong string
behind on whichever of the two apps was missed. Both catalogues (`packages/i18n/messages/{ko,en}.json`, every key in
both), then `pnpm gen:messages`, with `pnpm check:gen` gating freshness and `pnpm lint:raw-strings` gating any literal
that slips into JSX or an `aria-*`/`accessibilityLabel` prop.

| Key                                                                          | ko               | en                   | Note                                                     |
| ---------------------------------------------------------------------------- | ---------------- | -------------------- | -------------------------------------------------------- |
| `twinkle_balance_title` (kept)                                               | `별가루`         | `Stardust`           | the section's accessible name — the currency, not a tier |
| `twinkle_balance_small_label` (← `_basic_label`)                             | `작은 별가루`    | `Small stardust`     | the recall-only, daily-refilled tier                     |
| `twinkle_balance_general_label` (← `_additional_label`)                      | `별가루`         | `Stardust`           | the universal, permanent tier                            |
| `twinkle_earn_title` (← `twinkle_charge_title`)                              | `별가루 모으기`  | `Gathering stardust` | "채우기" implied a purchase                              |
| `twinkle_cost_earn` (← `twinkle_cost_charge`)                                | `모으는 법 보기` | `How to gather`      | the shortfall affordance                                 |
| `twinkle_earn_invite_{title,body,bonus_label}` (← `twinkle_charge_invite_*`) | unchanged values | unchanged values     | role moved from redeem to explain                        |

The general tier being the **unmarked** name is the point, not an oversight: [G2a] makes 별가루 the one currency and
작은 별가루 a purpose-restricted sub-kind, so the modifier belongs on the restricted one. `twinkle_balance_title`
sharing the string is harmless — it is a section label, never rendered beside the tier label.

**Deleted:** `twinkle_charge_pay_{title,body,action}`, `twinkle_charge_invite_code_{label,placeholder}`,
`twinkle_charge_error` (the guide performs no mutation, so it has no failure to report).
**Added:** `twinkle_earn_body`, `twinkle_earn_{daily,write,achievement,invite}_{title,body}`,
`twinkle_earn_achievement_action`, and the `me_stardust_*` set below. The one-time signup bonus ([U11]) is **not** listed
in the guide — it is not a repeatable path and naming it would read as an offer.

### Two tiers, one currency — what each surface shows

The **HUD** keeps its shipped shape: the derived total as the headline, then `작은 별가루 N · 별가루 N` in spend order.
The total is honest there because the HUD sits over the canvas where the paying actions are recalls, and a recall draws
`SMALL`→`GENERAL` — the total is exactly what a recall can reach ([G2][G5]).

The **stardust tab** shows the two tiers and **no summed figure**. A purchase prices against `GENERAL` alone ([P9]), so
a total there would overstate what the diarist can spend. This is the presentation-layer guard for [P9]: the misleading
number is absent, not annotated.

### The earn guide ([G3]) and the shortfall path

**As-built amendment (after this unit shipped).** On **web**, the guide is no longer a surface of its own: the balance
HUD itself became the press, and what it opens is the **별가루 panel** — held figures, the way to get more, then the earn
paths — composed at the widget layer as `widgets/stardust/ui/TwinkleDetailSheet.tsx`. `EarnGuideSheet.tsx` therefore
became `EarnGuideSections.tsx`: the same four paths and the same single affordance, minus the `Dialog` and minus any
open state, with its headings dropped to `h4` beneath the panel's group heading. `features/charge-twinkle` **returns as
a slice** — deliberately, carrying a disabled control and its reason rather than a purchase — and is re-added to
`apps/web/steiger.config.js`'s stardust block. Three keys arrive with it: `twinkle_charge_action` (별가루 더 얻기),
`twinkle_charge_unavailable` (아직 준비 중이에요.) and `twinkle_detail_held_label` (지금 가진 별가루). The NoticeIcon
affordance beside the figures is **gone**: it put a second, smaller thing in the corner to aim at while the numbers were
already what the reader was looking at. Mobile still ships the dialog form described below.
[policy/ux/stardust](../policy/ux/stardust.md) owns the current rule.

`features/earn-twinkle/ui/EarnGuideSheet.tsx` replaces `ChargeSheet.tsx`. It lists the four repeatable earn paths in
plain, unpressured copy — the daily 작은 별가루 refill, 일기 작성, 업적, 친구 초대 — with the figures read from generated
config (`twinkle.small_daily_amount`, `twinkle.earn_write`, `twinkle.earn_invite_inviter`/`_invitee`; the FE holds no
economy constant). Its single affordance opens `/me`'s **achievements** tab, because that is where earning is actually
claimed ([A4][A5]).

Navigation stays out of the widget: `pages/universe/ui/UniverseHomePage.tsx` already receives its route callbacks as
props from `apps/web/src/app/routes/route-screens.tsx` (`onOpenReader`, `onOpenSettings`) and threads them down — the
one seam the router is allowed through. The recovery re-target points at the achievements tab specifically, so that
route has to be expressible.

**As built, the seam widened as a second INTENT rather than a tab argument.** [64](64.my-page.md) shipped the `onOpenMe`
rename without a parameter, so this unit had to widen it — and `onOpenMe(tab)` turned out to be the wrong shape: a page
cannot name a tab without importing another page's tab type, which is a `pages`→`pages` cross-import FSD forbids. So the
app layer — which owns the router and therefore the tab ids — passes a distinct `onOpenAchievements`, and every layer
below states an intent it can express without knowing a route: `UniverseHomePage`/`UniversePage` thread it down, and
`widgets/stardust` takes `onOpenAchievements: () => void`. On mobile the `Me` route gained a `tab` param and `MePage` an
`initialTab` prop.

The shortfall route is unchanged in shape and renamed in substance: `packages/errors`'s `ErrorRecovery` becomes
`'sync-consent' | 'earn' | 'none'`, `classifyErrorRecovery` keeps mapping `MEMORY_INSUFFICIENT_TWINKLE` and
`TWINKLE_INSUFFICIENT` (now to `'earn'`), and the six call sites — `RecallFlowSheet`, `GistViewSheet`,
`DiaryReaderBlock` on both apps — request the guide through `useEarnRequestStore` instead of `useChargeRequestStore`.
`resetTwinkleUserState()` keeps clearing it, so `@cosimosi/auth/user-state`'s shared registry inventory is untouched.

### Control-state: the stardust machine is retired (§3.2)

The payment retirement removes the `PAY` event and `paying` state; [66](66.earn-and-purchase-spend.md) removes
`ClaimInvite` from the wire, which kills `inviting`
with it. What remains is `idle ↔ charging` — an open/closed boolean, and the earn guide issues no request, so there is
no in-flight phase to be exclusive about. §3.2 puts _exclusive modes_ in XState; a two-state shell is data. So
`packages/universe/src/stardust.machine.ts` and its test are deleted, `@cosimosi/universe`'s export goes with them, and
`StardustOverlay` holds the sheet's open flag as local state while `useEarnRequestStore` stays the cross-widget seam.

### The ledger read — the contract this unit consumes

[66](66.earn-and-purchase-spend.md) owns `rpc GetLedger(GetLedgerRequest) returns (GetLedgerResponse)` with
`idempotency_level = NO_SIDE_EFFECTS`, its single `twinkleRpcCachePolicies` entry in
`packages/client-cache/src/http-policy.ts` (the production transport hard-fails before I/O on a missing or
duplicate classification, so no runtime check is needed), and the keyset-cursor page shape copied from
`GetDiaries` (`page_token`/`next_page_token`, base64url `"<sort key>|<id>"` — never `LIMIT/OFFSET`).
This unit requires exactly these fields on each entry and **adds none**:

| Field                                               | Why this unit needs it                                                                                                                                                                        |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                                                | the list key; stable across pages                                                                                                                                                             |
| `kind` (`earn`/`spend`)                             | the sign — `amount` is always positive in the DDL, `kind` gives its direction                                                                                                                 |
| `reason` — a proto **enum**, not a string           | so the FE's reason→copy map is a **total** `Record` over the generated values and a new reason without copy fails typecheck (the closed set becomes a compile-time obligation)                |
| `amount`                                            | the figure                                                                                                                                                                                    |
| `from_small` / `from_general`                       | the spend's tier split (the domain spelling; the columns keep `from_basic`/`from_additional`)                                                                                                 |
| `occurred_on` — the entry's date in `User.Timezone` | so the FE performs **no** timezone arithmetic. [U7] makes the server the day-boundary authority; a device-local grouping would draw day headers that disagree with the `SMALL` reset boundary |
| `occurred_at`                                       | the within-day ordering and the time shown on the row                                                                                                                                         |

**Deliberately absent:** any `episodic_memory_id`, `diary_id`, or star reference. A ledger row must not become a free
index into memories ([I11], and [G4] prices the read it would bypass) — with no field to carry it, the jump cannot be
built.

`packages/twinkle/src/react/ledger.ts` wraps [66]'s `createGetLedgerInfiniteQueryOptions(transport, pageSize)` helper
(the `createGetDiariesInfiniteQueryOptions` precedent), with `pageSize` from `VALUES.twinkle.ledgerPageSize`. It needs
no invalidation seam of its own: the shipped `useInvalidateTwinkleBalance` invalidates the whole `TwinkleService` key,
which partially matches the infinite query, so a spend or earn refreshes balance, quote and history together — no
polling (§2.7).

### The history list ([G7][U9])

**As built, the refill marker carries no date** — only an amount. Its place is positional (always the head of the
history), so it needs no "is the newest group today?" comparison, which is the one question the client cannot answer
honestly: it has no server-supplied notion of the user's today, and reading the device clock is exactly the arithmetic
[U7] forbids. Keeping the marker date-free is therefore the same guard as keeping it reason-free.

Newest-first (the [D2] archive precedent), grouped under day headers from `occurred_on`, infinite-scrolled: an
`IntersectionObserver` sentinel on web, `FlatList onEndReached` on mobile (the mechanic
[69.diary-list-completion](69.diary-list-completion.md) uses for the diary list; no virtualization library
is added). Each row carries its reason label, the signed amount (`+`/`−`), and — for a **spend** — the tier split
("작은 별가루 3 · 별가루 2"). An **earn** row shows no tier line, because every ledgered earn credits `GENERAL`: the
`SMALL` tier is refilled by derivation and never earned. That asymmetry is a fact of the model, so the row shapes
differ rather than carrying an empty field.

**As-built amendment (after this unit shipped).** The split rides in **parentheses beside the amount** on the same
baseline — `−15 (작은 별가루 5 · 별가루 10)` — one step smaller and one step quieter, instead of on a line of its own
under the reason. It is a breakdown OF that number rather than a second fact about the row, and a full-size line gave it
the weight of one. Its introducing label was dropped with it: the parentheses and the two kind names already say what it
is, so `me_stardust_spend_split_label` ("쓴 몫") was retired for `me_stardust_spend_split`, which carries both figures.

The reason→copy map is a total record over the generated enum. `payment` **must** render: this unit retires the charge
_path_, not the _readability_ of rows written before it ([66](66.earn-and-purchase-spend.md) keeps the constant for
exactly this reason). `daily_grant` is a reserved constant that no row can carry and its entry in the record resolves
to the refill-marker copy, so the record stays total without a fallback arm.

| reason              | ko                           | en                       |
| ------------------- | ---------------------------- | ------------------------ |
| `write_diary`       | `일기를 썼어요`              | `Wrote a diary`          |
| `invite`            | `친구가 함께하기 시작했어요` | `A friend joined`        |
| `invite_signup`     | `초대를 받고 시작했어요`     | `Joined by invitation`   |
| `signup_bonus`      | `처음 온 날의 몫`            | `A welcome share`        |
| `achievement_claim` | `업적을 받았어요`            | `Claimed an achievement` |
| `admin_grant`       | `운영자가 담아준 몫`         | `Granted by an operator` |
| `recall`            | `기억을 떠올렸어요`          | `Recalled a memory`      |
| `gist_view`         | `요지를 열었어요`            | `Opened a gist`          |
| `ornament_purchase` | `우주를 꾸몄어요`            | `Decorated the universe` |
| `payment`           | `별가루를 샀어요`            | `Bought stardust`        |

New `me_stardust_*` keys: `me_stardust_history_title`, `me_stardust_history_empty`
(`아직 오고 간 별가루가 없어요.`), `me_stardust_history_loading`, `me_stardust_spend_split_label` (later replaced by
`me_stardust_spend_split`),
`me_stardust_refill_marker` (`오늘의 작은 별가루가 채워졌어요` + `{amount}`), `me_stardust_refill_note`
(`매일 채워지는 몫이라 내역에 남지 않아요.`). The tab's own id and label are [64](64.my-page.md)'s `me_tab_*`.

### The daily-refill marker

There is no `daily_grant` row and no cron ([T4] — the architecture has no per-user daily scheduler), so the history
would otherwise show a `SMALL` balance that appears from nowhere. The FE inserts **exactly one** marker, pinned above
today's group, reading `VALUES.twinkle.smallDailyAmount` — the refill **granted**, not the amount remaining, which the
balance summary above it already shows. It is rendered by its own component with no `EntryReason` and no entry id, sits
in distinct chrome, and its copy names it a refill; `me_stardust_refill_note` states plainly that it leaves no record.
Past days get no marker: their anchors are unrecoverable client-side, and inventing them would fabricate history. [G7]
is thereby amended by this plan to read "every _event_ is ledgered; the daily `SMALL` refill is a derivation, rendered
as a labeled refill marker".

### Empty and first-run states

A new account has no ledger rows. The tab still renders the two tier figures (`SMALL` is granted from first login,
[G5]), the today refill marker, and `me_stardust_history_empty` — never a bare zero-length list. The balance summary
holds a placeholder only until the first `GetBalance` settles, matching the shipped HUD's `loaded` flag.

### Golden-parity stance

**No golden-parity obligation.** This is presentation: every figure is either a server value (`GetBalance`, `GetLedger`)
or a generated constant. The one pure module added, `packages/twinkle/src/ledger-day.ts`, groups entries by a
**server-supplied** date string and constructs the marker model — it contains no economy math and has no Go twin to
drift from, by construction. The `smallRemaining` derivation that _does_ carry golden parity stays in
`packages/twinkle-logic` ↔ `apps/api/internal/twinkle/testdata/stardust-ledger-golden.json` ([65]) and is **not** on any
display path here. Web and mobile render the same figures from the same contract, sharing every `model`/`api` module
verbatim through `packages/twinkle`; only `ui` is authored per platform (§3.5).

## Acceptance Criteria

1. Every balance surface on both apps reads **작은 별가루** and **별가루**; the keys are **renamed** —
   `twinkle_balance_small_label` / `twinkle_balance_general_label` exist in both `ko.json` and `en.json`,
   `twinkle_balance_basic_label` / `twinkle_balance_additional_label` exist nowhere, and no call site references them
   ([G2][G2a]).
2. `pnpm gen:messages` output is committed and `pnpm check:gen`, `pnpm lint:raw-strings` and `pnpm lint:language` pass;
   every key added or renamed exists in **both** catalogues ([08.i18n-seam](08.i18n-seam.md)).
3. **Amended after this unit shipped** — the web 별가루 panel carries a **disabled** way-to-get-more with the reason
   written beside it ([policy/ux/stardust](../policy/ux/stardust.md)): an absent control read as "this is all there will
   ever be", which is not what is true. The criterion below still holds in the sense it was written — nothing can be
   purchased, nothing charges, no recovery flow points at it, and a shortfall still recovers into the earn paths.
   No **working** purchase affordance exists anywhere in either app: the pay section, `onPay`/`paying`, `charge.ts`, `pack.ts`
   (`CHARGE_PACK`), the `twinkle_charge_pay_*` keys, the four payment/charge error reasons in `packages/errors`, and
   `values.yaml → twinkle.charge_pack` are all gone, and no code path calls `Charge` ([G3], PRD §8.3).
4. No invite-**code** input exists: the `TextField`, `twinkle_charge_invite_code_{label,placeholder}` and `claimInvite`
   are gone; the invite path is explanatory only ([U8]).
5. An insufficient balance opens the **earn guide**, not a charge sheet: `ErrorRecovery` is
   `'sync-consent' | 'earn' | 'none'`, all six shortfall call sites (recall flow · gist view · diary reader, web +
   mobile) request it through `useEarnRequestStore`, and `spec/policy/platform/errors.md`'s recovery column says earn
   ([G3]).
6. The earn guide lists the four repeatable earn paths (daily refill · 일기 작성 · 업적 · 친구 초대) with figures from
   generated config, offers exactly one affordance — opening `/me`'s achievements tab — and issues **no** mutation; the
   one-time signup bonus is not listed ([G3][A4]).
7. `packages/universe/src/stardust.machine.ts` and its test no longer exist, no `@cosimosi/universe` export references
   them, and the guide's open/closed state is local widget state (§3.2).
8. `/me`'s stardust tab renders the **two tier balances with no summed figure**, and the universe HUD keeps its derived
   total — a purchase-facing surface never shows a number `SMALL` cannot pay ([G2][P9]).
9. The tab renders the ledger **newest-first**, grouped under day headers derived from the server-supplied
   `occurred_on` (the user's timezone), paginated by keyset cursor with infinite scroll (web `IntersectionObserver`,
   mobile `FlatList onEndReached`) ([G7][U9][U7]).
10. Every row shows its reason label, its signed amount, and — for a spend — its `작은 별가루 / 별가루` split,
    parenthesised beside that amount and de-emphasised; an earn row shows no tier line ([G7]).
11. The reason→copy map is a **total** `Record` over the generated `EntryReason` enum: a reason added without copy fails
    typecheck, and `payment` rows written before the retirement still render ([G7]).
12. No ledger row is a link to a star, a diary or any memory surface, and the consumed `LedgerEntry` carries no memory
    or diary id ([I11][G4]).
13. Exactly one synthetic **refill marker** appears, above today's group, visually distinct, labeled a refill with a
    note that it leaves no record; its figure is `twinkle.small_daily_amount`; no past day carries one; no
    `daily_grant` row exists in the data ([G7]).
14. An account with no ledger rows still sees both tier figures, the refill marker and a restrained empty line — never
    a bare empty list ([G5][U9]).
15. This unit adds no proto field, no RPC, no cache-policy entry, no migration and no server code; the ledger read is
    consumed from [66](66.earn-and-purchase-spend.md) unchanged.
16. **web + mobile parity:** the relabel, the earn guide, and the stardust tab all ship on both apps, sharing
    `packages/twinkle`'s store, hooks and pure day-grouping verbatim; the only per-platform code is `ui`, and
    `pnpm lint:fsd:layout` passes with no byte-identical app-local pure module (§3.5).

## Policy / Values Impact

- **policy / ux** — the rules below will be true once implemented, written at `/implement-job`; **do not pre-create or
  pre-edit any policy file.**
  - `spec/policy/ux/stardust.md` (existing, owned by [45.stardust-ui](45.stardust-ui.md)) is **rewritten in place**:
    the balance is shown as **작은 별가루** (daily refill, recall only) and **별가루** (universal, permanent); the
    universe HUD shows the derived total, the `/me` stardust tab shows the two tiers with no total; **earn is daily
    refill · 일기 작성 · 업적 · 친구 초대 · 가입 보너스 — there is no purchase path in v2**; a shortfall opens the earn
    guide and points at `/me`'s achievements tab, never a dead end; no Twinkle surface exposes a meaning-layer or
    placement control ([I11]).
  - a new `spec/policy/ux/stardust-history.md`: the history is **newest-first**, day-grouped on the user's timezone,
    one row per ledger entry with its reason and signed amount, tier split on spends only; the daily `SMALL` refill is
    **a derivation rendered as a labeled marker, never a row**; a row is never a link into a memory.
  - `spec/policy/platform/errors.md`: the recovery column for `MEMORY_INSUFFICIENT_TWINKLE` and `TWINKLE_INSUFFICIENT`
    becomes **earn** recovery, and the payment/charge reason rows are removed with their constants.
- **tech** — the as-built FE record (the renamed slice set, the retirement of `stardust.machine.ts`, the ledger infinite
  query riding the service-level invalidation seam, the total reason→copy record, the synthetic marker's separate
  component) is added to [tech/state-machine.md](../tech/state-machine.md) and
  [tech/i18n.md](../tech/i18n.md) (the rename-don't-revalue rule, as a worked example) at `/implement-job`. Same rule:
  do not pre-create.
- **spec/values.yaml** — this unit introduces **no new tuning key**, and **retires one**:
  - **retired:** `twinkle.charge_pack` — the v1 single-pack grant. It has two consumers today:
    `values.TwinkleChargePack` in `apps/api/internal/twinkle/service.go` (removed with `Charge` by
    [66](66.earn-and-purchase-spend.md)) and `VALUES.twinkle.chargePack` in `packages/twinkle/src/pack.ts`, deleted
    here. Because this unit lands last ([00.overview](00.overview.md) §5.6), it is the one that removes the key from
    `spec/values.yaml` at implement and re-runs `pnpm gen:values` so both generated constants disappear.
  - **referenced, not owned** (all in the `twinkle` group): `twinkle.ledger_page_size` (the history page size, owned
    by [66]); `twinkle.small_daily_amount` (the refill marker's figure and the guide's daily line, renamed
    by [65]); `twinkle.earn_write`, `twinkle.earn_invite_inviter`, `twinkle.earn_invite_invitee` (the guide's figures,
    shipped keys owned by [43](43.stardust-ledger.md)/[44](44.earn-spend-usecase.md) and only re-read by [66]).
    `twinkle.earn_signup_bonus` ([61](61.signup-and-invite-usecase.md)) is deliberately **not** read here — the guide
    does not list the one-time signup bonus ([G3][A4]).
  - **Deliberately not values:** the reason→copy table and every label (i18n **content**, and a table is content per
    `twinkle.charge_pack`'s own retired comment); the day-grouping and marker-placement rules (formulas/behavior); the
    `GetLedger` message shape and the `EntryReason` enum (proto contract); the `twinkle_ledger_entries` DDL and its
    column spellings (DB schema); the number of earn paths listed in the guide (a derived list length); slice, key and
    component names (code identifiers); per-platform layout and the infinite-scroll sentinel threshold (slice-local
    `config/` constants, promoted only if they later prove cross-surface tunable — the
    [23.universe-canvas](23.universe-canvas.md) camera-rig precedent).
- **Dependencies:** builds on [45.stardust-ui](45.stardust-ui.md), [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md)
  and [66.earn-and-purchase-spend](66.earn-and-purchase-spend.md); consumed by [64.my-page](64.my-page.md) (composes the
  stardust tab, supplies the tab-carrying `/me` navigation) and [76.achievement-ui](76.achievement-ui.md) (the tab the
  guide routes to); pairs with [59.error-taxonomy-and-handling](59.error-taxonomy-and-handling.md) and
  [08.i18n-seam](08.i18n-seam.md).
