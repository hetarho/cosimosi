# 37. forgetting decay

> Forgetting decay — the read-time domain math that dims a star and erases its words as universe time passes without
> recall: floored effective brightness, a monotone decay-stage progression, and the random word-removal that turns the
> current-memory text into the "…xxxxx…" transient. Pure, IO-free, golden-parity TS↔Go — the client renders the same
> decay the server computes, and nothing is ever deleted (silent engram, floored, never 0). Fills plan 18's
> `EffectiveBrightness` stub with its Epic-D dynamics and adds `decay_stages` to the universe read.
> Scope: BE | Status: built ([job 48](../jobs/archive/48.forgetting-decay.md); as-built rules in
> [tech/forgetting-decay.md](../tech/forgetting-decay.md), policy in [policy/domain/forgetting.md](../policy/domain/forgetting.md))
>
> Layer D (domain) — the first unit of Epic D (Forgetting). Home: the core-domain `internal/memory` context — pure
> functions in `packages/memory-logic` (TS) ↔ `internal/memory` (Go), the same golden-parity home as
> [18.synapse-plasticity](18.synapse-plasticity.md). It realizes the read-time forgetting seam that plan 18 reserved and
> that [29.universe-clock](29.universe-clock.md) named "now" for. Reads `forgetting_offset_days` (the neighbor-forgetting
> column added by [32.reconsolidation-rules](32.reconsolidation-rules.md), CC4) and adds `decay_stages` to the
> `GetUniverse` read + DTO. The **cost** of a deep-decayed star's recall is [38.forgetting-cost](38.forgetting-cost.md);
> the **visuals** (dimming + word-loss text on stars and in the detail panel) are [39.forgetting-visuals](39.forgetting-visuals.md).

## Purpose

Forgetting in cosimosi is **not erasure — it is losing access** (Ryan & Frankland 2022): an episodic-memory star that has
not been recalled in a long while **dims** and its **current-memory text loses words at random** ("나는 오늘 친구랑
xxxxx 파스타랑 xxxx 정말 맛있었다" → progressively more `xxxx`), yet the trace is **never deleted** — brightness and text
stop at a floor and wait to be revived by recall ([F1][F2], the silent engram, Roy 2017). This is the Ebbinghaus
forgetting curve made visible, and it is what makes recall meaningful: _use it or lose it_.

This is **domain math**, not UI: the client renders effective brightness **every frame** while the server needs the
identical value for later cost-gating ([38.forgetting-cost](38.forgetting-cost.md)) — so it must be **the same math
everywhere it runs**, pinned by golden-parity like [18.synapse-plasticity](18.synapse-plasticity.md) — and it must never
violate the model: the system deletes nothing ([I1][F3]), reading never mutates a stored fact ([I5]), and forgetting is a
**read-time function of universe time** ([T4], no cron), floored so a star fades in appearance but its row lives forever.

Plan 18 shipped `EffectiveBrightness(…)` as an Epic-A **stub** resolving to full brightness with the decay input
reserved. This unit **fills that stub**: elapsed universe-days since the last recall (offset by retrieval-induced
neighbor nudges) drive a floored brightness decay and a discrete **decay stage**; each stage removes more words from a
text; **arousal slows it** ([F6]) and **connection strength slows it** ([F7]). It is the load-bearing read-time
derivation of the whole forgetting axis, which [38](38.forgetting-cost.md)/[39](39.forgetting-visuals.md) consume.

## Scope / Non-goals

**In scope**

- **The read-time forgetting math** as pure, IO-free functions in `packages/memory-logic` (TS) ↔ `internal/memory` (Go),
  over scalars drawn from the `EpisodicMemory` aggregate (no VO mutation):
  - **Effective elapsed universe-days** — `effectiveElapsedDays(now, lastRecalled, createdUniverseTime, forgettingOffsetDays) → days`:
    universe-days since the last recall (or since creation, when never recalled), plus the signed neighbor
    `forgetting_offset_days` (CC4), floored at `0`.
  - **`EffectiveBrightness(…)`** — the plan 18 stub, now **filled**: a floored, monotone-decreasing brightness in
    `[floor, 1]` computed from effective elapsed days, **slowed by arousal ([F6]) and by connection strength ([F7])**;
    `elapsed = 0 → 1.0` (full), never below `forgetting.brightness_floor`, never `0`, never mutating a stored fact
    ([F1][F2][I1]).
  - **`DecayStage(…)` — the discrete forgetting stage** `0..N` a memory has reached: a monotone-non-decreasing step
    function of the same effective elapsed days (with the same arousal/connection slowing), so brightness and stage
    **move together** ([F1]) and the stage floors at the deepest stage (never a "gone" stage — [F2]).
  - **The decay-stage word-removal algorithm** — `decayStageText(currentText, stage) → text`: a **deterministic**,
    seeded random removal of a per-stage ratio of words from a text, replacing each removed word with the redaction
    token, **preserving structure** (POS/first-and-last heuristics) so the text degrades naturally ([F1][F9]). This is
    what _produces_ the `decay_stages` strings ([R8a]).
- **Adding `decay_stages` to the universe read + DTO.** The `decay_stages JSONB` column exists (plan 16) but is **not
  currently selected** by `ListUniverseEpisodicMemories` (recon). This unit adds it to the read query and the
  `GetUniverse` DTO so the client has the stored decay texts (plus the fields to compute the current stage) — the read's
  only change here.
- **Golden-parity (TS↔Go)** for every function above, pinned by a shared fixture table like plan 18, so a tuning change
  cannot desync client render from server gating.
- Enumerating the exact `forgetting.*` coefficients/floor/ratios these functions read.

**Non-goals**

- **No recall/reconsolidation writes.** _Resetting_ brightness and _recovering_ decay stage on recall ([F5][R2]) — the
  anchor writes (`last_recalled_universe_time`, `recall_count`, timer reset) — are [33.recall-usecase](33.recall-usecase.md)
  (CC1). This unit only _reads_ those anchors; recovery is the natural consequence of `last_recalled` moving to now.
- **No neighbor-offset write.** The signed `forgetting_offset_days` (retrieval-induced ± on _neighbors_, [R5]) is
  **written** by [32.reconsolidation-rules](32.reconsolidation-rules.md) (CC4) on recall; this unit only **reads** it in
  the elapsed-days computation. The recalled star itself recovers wholly ([F5]); the offset applies to neighbors only.
- **No cost math.** _Deeper decay → costlier recall_ ([F4]) and _recovery on recall_ ([F5], cost side) are
  [38.forgetting-cost](38.forgetting-cost.md); this unit exposes the decay-depth signal (the stage/effective brightness),
  not a cost multiplier and never a Twinkle price ([CC3], no price constant outside Epic F).
- **No rendering.** Mapping effective brightness → star dimming and the word-loss text onto stars / the detail panel is
  [39.forgetting-visuals](39.forgetting-visuals.md) ([V2]); this unit returns numbers and strings, not pixels.
- **No LLM.** Decay texts are **random word removal**, an algorithm (not semantic compression) — no LLM call. Contrast
  the **semantic** stage texts (`semantic_stages`, LLM-pregenerated, [C7], Epic E), a _separate axis_ ([F] is
  independent of [C]).
- **No stage-text generation _scheduling_.** _When_ a new decay-stage string is produced and persisted over an advance
  interval is bound to [41.consolidation-usecase](41.consolidation-usecase.md)'s concrete `AdvanceProgression` handler;
  this unit owns the _algorithm_ that produces a stage text, and the read-time stage/brightness that need no persistence
  at all.
- **No new table, no schema reshape.** `decay_stages` already exists (plan 16); this unit only selects it. No column is
  added here (`forgetting_offset_days` is added by plan 32).
- **No emotion→position/connection coupling.** Arousal slows decay ([F6]) and is read here for _that_ only; valence/mood
  never enter (valence is color-only, [I3][M7]).

## Grounding

- Constitution ([I1]–[I11]): [00.overview](00.overview.md) §3 (_The constitution_), owned by [PRD.md](../PRD.md) §7.
  Directly honored: **[I1]** the system deletes nothing — forgetting is a floored read-time dim, never a row delete or a
  zeroing ([F2][F3]); **[I5]** decay is a **derived** read-time value, not a stored per-tick fact (the stored
  `decay_stages` strings are _content_ the algorithm produced, like `current_text`, not an emergent position); **[I3]**
  emotion touches only color/size/decay-speed, never position/connection — arousal slows decay ([F6]) but never places a
  star; **[I8]** reading resets accessibility but does not restore original content — this unit only _dims/erases_, and
  restoration (recall) is [33](33.recall-usecase.md)/[32](32.reconsolidation-rules.md).
- tech / policy / concept basis:
  - [ARCHITECTURE.md](../ARCHITECTURE.md) §2.5 — **domain services are pure functions (no IO) over the aggregates**;
    purity is what lets the client simulation and the server run the _same_ decay, pinned by golden-parity. §2.4 — the
    dependency rule (adapters → behavior → pure domain); these functions are the innermost layer and import nothing.
    §2.6 — `memory/pg` is the only package touching sqlc, so the `decay_stages` read addition lands there. §2.2 —
    forgetting is core behavior of the memory context, not a new context (CC8).
  - [PRD.md](../PRD.md) §5.3.5 **[F]** (망각):
    - **[F1]** brightness decay + progressive random word-removal move **together** as a function of universe time
      (read-time); per-stage decay texts are stored ([R8a]).
    - **[F2]** the **silent engram** — brightness and text stop at a **floor**, never fully vanish; recall revives.
    - **[F3]** the system deletes **no** neuron/memory/connection ([I1]); user deletion is Epic H ([X]).
    - **[F6]** **arousal modulation** — high-arousal memories dim / decay-stage / (semanticize) **slower** (arousal only,
      valence is color-only, [I3]).
    - **[F7]** **connection strength** — better-connected (well-shared) memories forget slower.
    - **[F9]** the exact word-removal rules (ratio / priority / POS-preservation) are a **v1-tunable "to refine"** —
      simple enough for v1, refined later for naturalness.
  - [PRD.md](../PRD.md) §6 **[V2]** — 밝기 = 회상 최근성; high arousal slows the fade ([F6]) — this unit computes the
    value [39](39.forgetting-visuals.md) renders. The visual model's dynamics column ("밝기 … 빠름") is what this decay
    realizes.
  - [PRD.md](../PRD.md) §5.3.3 **[T4]** — forgetting runs **read-time**, at advance, **no cron**; **[T5]** universe time
    is the single read-time "now".
  - Sibling plans: [16.memory-aggregate-schema](16.memory-aggregate-schema.md) (the `EpisodicMemory` fields + the
    `decay_stages` column this reads), [18.synapse-plasticity](18.synapse-plasticity.md) (the `EffectiveBrightness` stub
    this fills, and the golden-parity home + `EffectiveSynapseStrength`/`EffectiveStrength` siblings), [29.universe-clock](29.universe-clock.md)
    (universe time = the read-time "now" fed as `now`), [32.reconsolidation-rules](32.reconsolidation-rules.md) (writes
    `forgetting_offset_days` this reads, CC4), [33.recall-usecase](33.recall-usecase.md) (the recall anchor writes that
    make forgetting recover, CC1), [38.forgetting-cost](38.forgetting-cost.md) (consumes decay-depth), [39.forgetting-visuals](39.forgetting-visuals.md)
    (renders the dimming + word-loss).
  - Grounding for the neuroscience: Ebbinghaus 1885 (forgetting curve); Schacter (transience); Roy 2017 (silent
    engram); Ryan & Frankland 2022 (forgetting = retrieval failure / access loss); McGaugh 2004 (arousal consolidation).
    See PRD §9.

## Design

### Placement (domain-first Go)

Forgetting decay is **read-time behavior of the memory aggregate** — it dims and erases a star as universe time passes —
so it lives in `internal/memory` (Go) and `packages/memory-logic` (TS), **not** a new context (ARCHITECTURE §2.2,
be-architecture: keep the aggregate's read-time math with the aggregate). It sits in the **pure domain layer**: total,
deterministic, side-effect-free, depending only on its scalar arguments and the generated `forgetting.*` constants — no
clock, repository, logger, or ambient randomness (the word-removal randomness is a _seeded PRNG_, an argument, not IO).
The single non-pure change — selecting `decay_stages` in the universe read — lands in `memory/pg` (the sole sqlc seam)
and the `GetUniverse` DTO/mapper.

### The inputs (given, not defined here)

All read from the `EpisodicMemory` root (plan 16) + the clock (plan 29); none is mutated by reading:

- `last_recalled_universe_time DATE` (NULL until first recall) — the recency anchor. When NULL, decay is measured from
  `created_universe_time` (a never-recalled star still forgets — `elapsed = now − created`).
- `created_universe_time DATE` — the fallback recency anchor + the connection/age baseline.
- `arousal REAL` (Emotion VO, plan 17) — the **only** emotion input; high arousal slows decay ([F6][I3]).
- `forgetting_offset_days REAL NOT NULL DEFAULT 0` — the signed neighbor nudge (CC4), **added by plan 32**, read here.
- a **connection-strength signal** for [F7] — see the note below.
- `now` — the universe clock DATE ([T5], plan 29), passed in as the read-time "now"; the caller computes it, never this
  unit.
- `decay_stages JSONB` — the stored per-stage texts ([R8a]) the read now returns (not an input to the math; produced by
  the algorithm below).

**Decided (2026-07):** the **connection-strength signal for [F7]** is the memory's **`EffectiveStrength`
(size, [V3])** — recall-accumulated strength that already reflects how load-bearing/well-shared the memory is — passed
in as a scalar, not recomputed here. This keeps [F7] a single clean modulation input, avoids this pure function reaching
into the synapse graph (which would break purity), and reuses the memory-level value plan 18 already defines. The exact
mapping strength→slowdown is a coefficient (`forgetting.connection_slow_coefficient`), the shape a formula (code). The
caller (render / gating) already has `EffectiveStrength` in hand, so no extra query.

### `effectiveElapsedDays` — the recency clock with the neighbor offset (CC4)

Signature (canonical; casing may differ per language):

```
effectiveElapsedDays(now DATE, lastRecalled DATE?, createdUniverseTime DATE, forgettingOffsetDays float) → float
```

```
anchor   = lastRecalled ?? createdUniverseTime          // never-recalled → decay from creation
elapsed  = max(0, universeDaysBetween(anchor, now))     // universe-days, forward-only ([I10])
effective = max(0, elapsed + forgettingOffsetDays)      // CC4 neighbor nudge, floored at 0
```

The signed `forgetting_offset_days` (written on a _neighbor's_ recall by plan 32: `−` = slow forgetting for a
1-shared-semantic-neuron neighbor, `+` = speed forgetting for ≥2 shared — [R5]) shifts the neighbor's effective age. It
is floored at `0` so an offset can never make a memory "younger than new". Universe-days are counted between DATEs (day
granularity, consistent with the whole engine — plan 29). This is the single elapsed-days quantity both `EffectiveBrightness`
and `DecayStage` consume, so **brightness and stage move together** ([F1]).

### `EffectiveBrightness` — the plan 18 stub, now filled ([F1][F2][F6][F7])

Signature (fills the reserved decay input on plan 18's stub):

```
EffectiveBrightness(effectiveElapsedDays float, arousal float, effectiveStrength float) → brightness   // in [floor, 1]
```

This **widens** plan 18's Epic-A stub `EffectiveBrightness(elapsedUniverseDays) → 1.0` from one reserved param to three:
the first param's meaning becomes the **offset-inclusive** `effectiveElapsedDays` (`= max(0, (now − last_recalled) +
forgetting_offset_days)`, [CC4]), plus `arousal` and `effectiveStrength`. As part of this unit, plan 18's stub
golden-fixture rows and its two call sites (web `packages/memory-logic`, Go `internal/memory`) are migrated to the new
signature — so "no other read-time function changed" holds while `EffectiveBrightness`'s callers are re-threaded.
Behavior the acceptance criteria pin (the **behavior** is the contract; the **shape** stays in code):

- **`effectiveElapsedDays = 0 → 1.0`** (a just-recalled or brand-new star is full — recall reset [R2] is the elapsed
  going to 0).
- **Monotone non-increasing** in `effectiveElapsedDays` (more time-since-recall → never brighter).
- **Floored, never 0.** The result never drops below `forgetting.brightness_floor` and never reaches `0` — the silent
  engram ([F2][I1]); the base fact is never mutated by reading.
- **Arousal slows it ([F6]).** Higher `arousal` flattens the decay so a high-arousal memory stays bright longer, via
  `forgetting.arousal_slow_coefficient`. Arousal is the _only_ emotion input ([I3]); valence never enters.
- **Connection strength slows it ([F7]).** Higher `effectiveStrength` flattens the decay too, via
  `forgetting.connection_slow_coefficient`.

**Decided (2026-07):** the initial decay **shape is exponential** —
`brightness = floor + (1 − floor) · decayFactor^(effectiveElapsedDays / slow)` where
`decayFactor = 1 − forgetting.brightness_decay_per_day` and `slow = 1 + arousal·arousal_slow_coefficient +
effectiveStrength·connection_slow_coefficient` (the two modulators _stretch_ the time axis, so both slow the fade
without changing the floor) — matching plan 18's `EffectiveSynapseStrength` precedent. The shape is a **formula
(code, excluded from values)**; only the coefficients + floor are values. It is replaceable by any monotone-decreasing,
floored curve as a formula change if [F9]-style tuning demands.

### `DecayStage` — the discrete forgetting stage ([F1][F2])

Signature:

```
DecayStage(effectiveElapsedDays float, arousal float, effectiveStrength float) → int   // 0 .. maxStage
```

A **step function** of the _same_ effective elapsed days (with the _same_ arousal/connection slowing), so the stage a
star is at is always consistent with its brightness ([F1] "함께 움직임"). Behavior:

- **Monotone non-decreasing** in `effectiveElapsedDays`; `effectiveElapsedDays = 0 → 0`.
- **Floors at the deepest stage.** The maximum stage is the **derived length** of the per-stage ratio array (below) — it
  never exceeds it, and there is **no "gone" stage** past the last one ([F2]). The deepest stage is the most-redacted
  text, still readable-as-fragments, never empty.
- **Arousal + connection slow the crossing** ([F6][F7]) — the same `slow` factor stretches the day thresholds, so a
  high-arousal / well-connected star reaches each stage later.

**Decided (2026-07):** stage boundaries are **universe-day thresholds** governed by `forgetting.stage_interval_days`
(days per stage, stretched by `slow`); `stage = min(maxStage, floor(effectiveElapsedDays / (stage_interval_days ·
slow)))`. The _number_ of stages is the **derived length** of `forgetting.stage_word_removal_ratios` (not a separate
declared count — array lengths are derived). The threshold spacing is a formula over one coefficient; the ratios are the
values.

### `decayStageText` — the random word-removal algorithm ([F1][F9])

Signature:

```
decayStageText(currentText string, stage int, seed int64) → string
```

Produces the stage-`stage` decay text from a memory's `current_text` by **removing a per-stage ratio of words at random
and replacing each with the redaction token** ("나는 오늘 친구랑 xxxxx 파스타랑 xxxx 정말 맛있었다"). It is
**deterministic given `(currentText, stage, seed)`** — the randomness is a **seeded PRNG** (an argument), so the same
inputs give the same redaction on client and server (golden-parity) and across re-derivations. Rules:

- **Ratio per stage** — **stage 0 is the vivid, unredacted text**; for stage `s` in `1..maxStage` the redacted fraction
  of removable words is `forgetting.stage_word_removal_ratios[s-1]` (the array indexes the decayed stages), strictly
  increasing with stage (deeper stage → more `xxxx`). `maxStage` is the derived array length.
- **Monotone / nested removal.** Stage _k+1_ removes a **superset** of stage _k_'s words — words already lost stay lost
  as decay deepens, so the progression reads as continuous erosion, and `decayStageText` at successive stages is
  consistent with the same seed.
- **Structure preservation ([F9]).** A **POS/position heuristic** keeps the text legible: prefer removing content words
  (nouns/verbs/adjectives) over function words, and **never remove the first and last word** of a sentence, so the
  sentence's skeleton survives. A sentence boundary is a word ending in a Latin/CJK terminator (`. ! ? 。 ！ ？ …`) after
  trailing closing quotes/brackets are stripped, so `hello."` still anchors the next sentence's opening word. **Decided (2026-07):** v1 ships a **simple, language-agnostic heuristic** (word-index
  seeded selection + a stop-word/first-last guard); the PRD marks the _exact_ ratios / POS rules a **"to refine"**
  ([F9]), so the ratios live in `values.yaml` (tunable without a code change) while the heuristic itself is a
  **formula/content (code)**, refinable later toward per-language POS without reshaping the contract.
- **Floor ([F2]).** Even the deepest stage keeps a **non-empty** skeleton (the first/last-word guard guarantees it) — a
  faded memory is fragments, never a blank.
- **Redaction token** — the visual `xxxx` marker is **UI content**, not a tuning number; it is code/content (excluded
  from values), owned with the algorithm (rendered by [39](39.forgetting-visuals.md)).

**Where the produced text is stored.** These strings are what fill `decay_stages JSONB` ([R8a]). _Producing and
persisting_ a newly-reached stage's text over an advance interval is bound to [41.consolidation-usecase](41.consolidation-usecase.md)'s
`Consolidate` handler, the concrete `AdvanceProgression` hook (CC7) — this unit owns the **algorithm**; [41] owns the
advance-time write that fills missing stage strings. The **read-time** `DecayStage`/`EffectiveBrightness` need no stored text at all (they are pure functions of
elapsed time), so the client can always show the correct dimming even before a stage string is persisted; the stored
strings are the exact word-loss text ([39](39.forgetting-visuals.md) shows the persisted string for the current stage,
falling back to `current_text` when a stage string is not yet filled).

### Adding `decay_stages` to the universe read (the one persistence change)

`ListUniverseEpisodicMemories` (`apps/api/db/queries/memory/universe.sql`) already selects `current_text`, `arousal`,
`recall_count`, `created_universe_time`, `last_recalled_universe_time`, `semantic_stage`, `semantic_stages`, … but
**not** `decay_stages` (recon). This unit adds `decay_stages` to that `SELECT` and threads it through the `memory/pg`
row→domain mapper and the `GetUniverse` DTO, so the client receives the stored decay texts. It also confirms the read
carries `arousal` + the fields to compute `EffectiveStrength` (already selected) so the client can compute
`EffectiveBrightness`/`DecayStage` from stored facts alone ([I5] — derived, not stored). `forgetting_offset_days` is
added to the read by plan 32 (which adds the column); this unit consumes it once present and, until then, treats a
missing offset as `0` (the DEFAULT). No proto **message** change is required beyond the added `decay_stages` field on the
existing `GetUniverse` episodic-memory sub-message.

**Decided (2026-07):** the DTO carries the **stored `decay_stages` strings + the anchor facts**, and the client computes
the _current_ stage/brightness from those facts via `packages/memory-logic` — the server does **not** pre-render a
"current decay text" into the DTO (that would freeze a read-time value into transport, violating [I5] and desyncing from
the frame-by-frame client clock). One exception is not made: the read stays stored-facts-only, exactly like plan 16's
read stance.

### Golden-parity (TS↔Go)

Per ARCHITECTURE §2.5 the two runtimes must agree, because the client renders `EffectiveBrightness`/`DecayStage`/`decayStageText`
every read while the server computes them for cost-gating ([38](38.forgetting-cost.md)). The Go implementation in
`internal/memory` and the TS mirror in `packages/memory-logic` are pinned equal by a **shared golden fixture**
(`apps/api/internal/memory/testdata/forgetting-decay-golden.json`, `(function, inputs) → expected` rows + a fixed
tolerance), extending plan 18's harness. Fixtures cover: `effectiveElapsedDays` with NULL `lastRecalled` (creation
fallback), with signed offsets (both directions, and the `0` floor), `EffectiveBrightness` at `elapsed = 0` (= 1.0),
across a decay range hitting the floor, and at high vs low arousal / strength (slower fade), `DecayStage` at each
threshold boundary and floored at `maxStage`, and `decayStageText` proving determinism under a fixed seed, the nested
superset property, ratio-per-stage, and the first/last-word (non-empty) guard. Both sides read the same generated
`forgetting.*` constants (`pnpm gen:values` emits Go + TS), so a tuning change cannot desync render from gating.

### Purity & re-implementation

- The four functions live in the pure domain of `internal/memory` (Go) and `packages/memory-logic` (TS) — no `pg`,
  `rpc`, SDK, clock, or ambient randomness; the word-removal PRNG is a seeded argument (still pure). They take/return
  domain scalars/strings, never proto DTOs or sqlc rows (§2.4 anti-corruption boundary).
- No formula, curve shape, threshold spacing, redaction token, or POS heuristic is a value: only the coefficients,
  floor, and per-stage ratios go to `spec/values.yaml`; the shapes are code. Array lengths (stage count) are **derived**.
- The whole axis is reproducible on another client from: one elapsed-days clock (offset + floor), one floored monotone
  brightness curve, one step function sharing that clock, and one seeded nested word-removal with a first/last guard —
  plus the `forgetting.*` constants. Nothing is hidden state; everything is a function of stored facts + universe time.

### As-built files (created at `/implement-job`)

- Go pure domain: `apps/api/internal/memory/forgetting.go` (+ mirror of the memory-level effective values in
  `effective.go` where `EffectiveBrightness`'s stub currently lives).
- Go invariant/golden tests: `apps/api/internal/memory/forgetting_test.go`; shared fixture
  `apps/api/internal/memory/testdata/forgetting-decay-golden.json`.
- TS mirror: `packages/memory-logic/src/forgetting.ts` (+ the `effective-values.ts` `EffectiveBrightness` fill); tests
  `packages/memory-logic/src/forgetting.test.ts`.
- Read change: `apps/api/db/queries/memory/universe.sql` (add `decay_stages`), the `memory/pg` mapper, and the
  `GetUniverse` DTO.

## Acceptance Criteria

1. `effectiveElapsedDays(now, lastRecalled, created, offset)` returns universe-days from `lastRecalled` (or from
   `created` when `lastRecalled` is NULL), **plus** the signed `forgetting_offset_days`, **floored at `0`**; it is
   never negative and a never-recalled star still accrues elapsed days from creation ([F1][I10] + CC4).
2. `EffectiveBrightness(effectiveElapsedDays, arousal, effectiveStrength)` satisfies **`= 1.0` at `elapsed = 0`**, is
   **monotone non-increasing** in `effectiveElapsedDays`, and stays within **`[forgetting.brightness_floor, 1]` — never
   below the floor and never `0`** (silent engram; stored fact never mutated) ([F1][F2][I1][I5]).
3. Higher `arousal` yields a **slower** brightness fade at the same elapsed days (`EffectiveBrightness` non-decreasing in
   `arousal`), governed by `forgetting.arousal_slow_coefficient`; **valence/mood are not inputs** ([F6][I3]).
4. Higher `effectiveStrength` (connection/size) yields a **slower** fade at the same elapsed days, governed by
   `forgetting.connection_slow_coefficient` ([F7]).
5. `DecayStage(effectiveElapsedDays, arousal, effectiveStrength)` is a **monotone-non-decreasing** step function of the
   same effective elapsed days (so stage and brightness move together), is `0` at `elapsed = 0`, and **floors at the
   maximum stage** = the derived length of `forgetting.stage_word_removal_ratios` — there is **no stage past the last**
   ([F1][F2]).
6. `decayStageText(currentText, stage, seed)` is **deterministic** given its inputs, removes the
   `forgetting.stage_word_removal_ratios[stage]` fraction of words (strictly increasing with stage) replacing each with
   the redaction token, and its removed-word set at stage _k+1_ is a **superset** of stage _k_'s ([F1]).
7. `decayStageText` **preserves structure**: it never removes the first or last word of a sentence and prefers content
   words over function words, so even the **deepest stage is non-empty** and legible-as-fragments ([F2][F9]). (Exact
   ratios/POS rules are a `values.yaml`-tunable "to refine" per [F9].)
8. Decay texts are produced by an **algorithm, not an LLM**, and are **independent of the semantic (`semantic_stages`)
   axis** — forgetting removes words at random; semanticization compresses meaning ([F1], [C] independence).
9. `ListUniverseEpisodicMemories` (and the `GetUniverse` DTO) **selects and returns `decay_stages`**, plus the anchor
   facts (`arousal`, `created_universe_time`, `last_recalled_universe_time`, and the fields for `EffectiveStrength`) so
   the client computes current stage/brightness from stored facts alone; the server does **not** pre-render a current
   decay text ([I5]). `forgetting_offset_days` (plan 32's column) is read once present, treated as `0` when absent.
10. The Go (`internal/memory`) and TS (`packages/memory-logic`) implementations produce **identical outputs** for the
    shared golden-fixture table within a fixed tolerance, and both read the same generated `forgetting.*` constants
    (TS↔Go golden-parity, matching plan 18).
11. All four functions are **pure**: deterministic, IO-free (no clock, DB, transport, SDK, or ambient randomness — the
    word-removal PRNG is a seeded argument), operating only on their arguments and generated constants; the plan 18
    `EffectiveBrightness` **stub is filled** here with its Epic-D dynamics, with no other read-time function changed.
12. Forgetting is **read-time and cron-free** ([T4]): stage/brightness are pure functions of universe time; persisting a
    newly-reached stage's decay text is bound to [41]'s concrete advance hook, not a scheduler, and the system
    deletes nothing ([F3][I1]).

## Policy / Values Impact

- **tech:** the as-built forgetting rules land in `tech/memory-persistence.md` (or a `tech/forgetting-decay.md` sibling)
  once implemented — forgetting is a **floored, read-time, golden-parity** derivation of universe time (brightness +
  decay stage move together, [F1][F2]); it deletes nothing ([I1]); decay texts are algorithmic random word-removal
  (independent of the semantic axis); arousal + connection strength slow it ([F6][F7]). Written at `/implement-job` —
  **do not** pre-create the doc.
- **policy:** the domain rule "**forgetting is access loss, not erasure — brightness and text decay to a floor and are
  revived only by recall; the system deletes nothing**" is asserted here and lands in `policy/domain/forgetting.md` once
  implemented (written at `/implement-job`; **do not** pre-create policy files). Reinforces [I1][F2][F3].
- **spec/values.yaml** — new group `forgetting` (generated to TS + Go constants read identically by both runtimes;
  numbers as built):
  - `forgetting.brightness_decay_per_day = 0.02` — the read-time brightness decay coefficient (per universe-day); the
    fade rate of `EffectiveBrightness` ([F1][V2]).
  - `forgetting.brightness_floor = 0.15` — the silent-engram floor `EffectiveBrightness` never drops below (never `0`)
    ([F2][I1]). Aligned with `rendering.star_brightness_min` — the render floor is the visual image of this value.
  - `forgetting.stage_interval_days = 30` — universe-days per decay stage (the step spacing `DecayStage` crosses,
    stretched by the slow factor) ([F1]).
  - `forgetting.stage_word_removal_ratios = [0.2, 0.4, 0.6, 0.85]` — the per-stage fraction of words redacted, a
    **strictly-increasing numeric array**; its **length derives the number of decay stages** (`maxStage = 4`) ([F1][F9]
    — the tunable "to refine" ratios). Indexes the **decayed** stages 1..N; stage 0 is the vivid, unredacted text.
  - `forgetting.arousal_slow_coefficient = 1.0` — how strongly `arousal` stretches the decay time-axis (arousal
    modulation) ([F6]).
  - `forgetting.connection_slow_coefficient = 1.0` — how strongly `EffectiveStrength` (connection/size) stretches the
    decay time-axis ([F7]).
  - _(Excluded from values — code/content, not values:_ the exponential brightness curve, the stage step-function shape,
    the seeded word-removal + POS/first-last heuristic, the redaction token string, and the derived stage count.
    Formulas, algorithm content, and derived array lengths are code per the values exclusion rule. The `±` neighbor-offset
    magnitudes + shared-count threshold are `values.yaml` owned by [32.reconsolidation-rules](32.reconsolidation-rules.md),
    not here; no Twinkle price appears here — cost values are Epic F, CC3.)*
- **Dependencies:** depends on [16.memory-aggregate-schema](16.memory-aggregate-schema.md) (the `EpisodicMemory` fields +
  the `decay_stages` column it selects), [18.synapse-plasticity](18.synapse-plasticity.md) (the `EffectiveBrightness`
  stub it fills, `EffectiveStrength` it reads for [F7], and the golden-parity harness), [29.universe-clock](29.universe-clock.md)
  (universe time = the "now" it consumes), and [17.emotion-model](17.emotion-model.md) (the `arousal` VO field, [F6]).
  **Pairs with** [32.reconsolidation-rules](32.reconsolidation-rules.md) (writes `forgetting_offset_days` this reads,
  CC4) and [33.recall-usecase](33.recall-usecase.md) (recall anchor writes make forgetting recover, CC1). **Consumed
  by** [38.forgetting-cost](38.forgetting-cost.md) (decay-depth → recall accessibility/cost, [F4]) and
  [39.forgetting-visuals](39.forgetting-visuals.md) (renders the dimming + word-loss text, [V2]). **Written by**
  [41.consolidation-usecase](41.consolidation-usecase.md)'s concrete `AdvanceProgression` hook, which persists newly
  reached decay-stage texts during clock advances ([T4], no cron).
