# 12. 감쇠·최소 밝기·잠든 별 탐색 (decay-dormant)

> 활성도 감쇠를 렌더 시 계산하고 최소 밝기 바닥을 둬 별·시냅스가 어두워질 뿐 사라지지 않게 하며, 잠든 별을 탐색·재점화한다. | 범위: MVP | 선행: 08, 09, 11 | 차단: —

## 목적 (Why)
원칙 2(별·시냅스는 우주에서 삭제하지 않음, 감쇠는 밝기만)를 실제로 구현한다. 오래 회상하지 않은 별·연결은 `exp(-λΔt)`로 어두워지되 `a_min=0.05` 바닥 아래로는 내려가지 않고 "잠든(dormant) 상태"로 계속 렌더링된다. 어두워 눈에 안 띄는 별을 시간순·검색으로 다시 찾아 회상(재점화)하는 회고 동선을 만든다.

## 범위
- **포함:**
  - FE: `entities/memory/model/activation.ts`를 `exp(-λΔt)` + `a_min` floor로 확장, 별 유효 밝기 `max(a_min, activation)` / 시냅스 유효 밝기 `weight·max(a_min, activation)` 산출.
  - FE: `universe-canvas`에서 잠든 별을 **우주에서 제거하지 않고** 어둡게 잔존 렌더(밝기 0이 아닌 은은한 잔광).
  - FE: `pages/dormant` — 잠든(어두운) 별 목록·검색 → 클릭 시 해당 별로 카메라 이동 → 회상으로 재점화.
  - BE: `ListDormant` RPC — 밝기가 `a_min` 부근(임계 이하)인 별을 탐색 보조용으로 반환(제거 아님). `db/queries/memory.sql`에 쿼리 추가.
- **비목표(Out of scope):**
  - 관련성 가중 감쇠(연결 수·요즘 관련성·감정 강도 가중) — v1 #23(`competitive-allocation + relevance-decay`)이 담당. MVP는 **시간 기반 감쇠만**.
  - 요지 추상화(잠든 별 형태 단순화) — v1 #23/별 형태 고도화.
  - 야간 배치(전체 재안정화·가지치기) — v1 #22(`nightly-consolidation`).
  - 회상 강화·`last_recalled_at` 갱신 자체 — 11(`recall-reinforce`)이 담당. 본 스펙은 그 값을 **읽어 감쇠를 계산**하고 재점화 동선만 연결.
  - `GetUniverse` 신규 구현 — 04(`memory-api`)가 담당(여기선 전체 그래프 반환 유지를 **검증**만).

## 참고
- spec/concept.md §망각 모델 — "기억은 사라지지 않는다, 빛이 꺼지고 형태가 가라앉을 뿐", §우주 탐험(회상) — "잠든 별 탐색".
- spec/Architecture.md §5 망각 모델 구현(λ=ln2/30, a_min=0.05, 유효 밝기 식, 렌더 시 결정론적 계산), §2.9 R3F 컴포넌트 배치, §2.11 model 순수성.
- 코드 기준점: `frontend/src/entities/memory/model/`(11에서 생성), `frontend/src/widgets/universe-canvas/ui/`, `backend/internal/memory/`, `backend/internal/db/queries/memory.sql`, `proto/`(02 계약).

## 설계 요점
**FE 활성도 (순수 model — `three`/React/DOM 미의존, 원칙 4):**
```ts
// entities/memory/model/activation.ts
export const HALF_LIFE_DAYS = 30;
export const DECAY_LAMBDA = Math.LN2 / HALF_LIFE_DAYS; // ≈ 0.0231 /day
export const A_MIN = 0.05;

const MS_PER_DAY = 86_400_000;

/** exp(-λ·Δt_days). lastActivatedAtMs=last_recalled_at(별)|last_activated_at(연결). */
export function activation(lastActivatedAtMs: number, nowMs: number): number {
  const dtDays = Math.max(0, (nowMs - lastActivatedAtMs) / MS_PER_DAY);
  return Math.exp(-DECAY_LAMBDA * dtDays);
}
/** 별 유효 밝기: 절대 0이 되지 않음(원칙 2). */
export function starBrightness(lastRecalledAtMs: number, nowMs: number): number {
  return Math.max(A_MIN, activation(lastRecalledAtMs, nowMs));
}
/** 시냅스 유효 밝기 = weight · max(a_min, activation). */
export function synapseBrightness(weight: number, lastActivatedAtMs: number, nowMs: number): number {
  return weight * Math.max(A_MIN, activation(lastActivatedAtMs, nowMs));
}
/** 잠든 판정: 유효 활성도가 임계 이하(기본 a_min 부근). */
export function isDormant(lastRecalledAtMs: number, nowMs: number, threshold = 2 * A_MIN): boolean {
  return activation(lastRecalledAtMs, nowMs) <= threshold;
}
```
- 렌더는 이 값을 받아 별 밝기 / 시냅스 두께·emissive로 매핑(셰이더 식은 08·09 소관). 잠든 별도 `A_MIN` 잔광으로 그려져 클릭 가능.
- `now`는 외부 주입(테스트 가능). 60fps 매 프레임 재계산은 ref/uniform 갱신으로(React state 금지, §2.8).

**BE `ListDormant`(탐색 보조, 제거 아님):**
```sql
-- db/queries/memory.sql
-- name: ListDormant :many
-- 감쇠 임계 이하(잠든) 별만 탐색 보조용으로 반환. 우주에서 제거하지 않는다.
SELECT id, entry_date, body, mood, intensity, last_recalled_at, created_at
FROM memories
WHERE user_id = $1
  AND exp(-(ln(2)/30.0) * (extract(epoch FROM (now() - last_recalled_at)) / 86400.0)) <= sqlc.arg(threshold)::float8
ORDER BY last_recalled_at ASC
LIMIT sqlc.arg(lim)::int;
```
- 핸들러는 row→도메인→proto 매핑(원칙 5). proto `ListDormantRequest{threshold, limit}` / `ListDormantResponse{repeated DormantStar}`는 02 계약에 필드 추가.
- ⚠️ `GetUniverse`는 변경 없음 — **여전히 전체 그래프 반환**(잠든 별 포함). `ListDormant`는 별도 보조 조회일 뿐 삭제·필터 영속화가 아니다.

**FE `pages/dormant`:** `ListDormant` unary 호출(원칙 6) → 목록 + 텍스트 검색 → 항목 클릭 → 우주로 이동 + 카메라를 해당 별로 → 11의 회상 액션으로 재점화.

## 영향 파일
- `frontend/src/entities/memory/model/activation.ts` — 수정(λ·a_min·유효 밝기·isDormant 추가/확장)
- `frontend/src/entities/memory/index.ts` — 수정(activation 심볼 Public API 노출)
- `frontend/src/widgets/universe-canvas/ui/UniverseCanvas.tsx`(또는 별 렌더 진입) — 수정(잠든 별 잔존 렌더, 밝기 주입)
- `frontend/src/pages/dormant/ui/DormantPage.tsx` — 신규(목록·검색·이동)
- `frontend/src/pages/dormant/index.ts` — 신규(Public API)
- `frontend/src/pages/dormant/api/list-dormant.ts` — 신규(ListDormant unary 호출·매퍼)
- `frontend/src/app/` 라우터 — 수정(`/dormant` 라우트 등록)
- `backend/internal/db/queries/memory.sql` — 수정(`ListDormant` 쿼리 추가)
- `backend/internal/memory/service.go` — 수정(ListDormant 비즈니스 진입)
- `backend/internal/memory/repository.go` — 수정(ListDormant 인터페이스 메서드)
- `backend/internal/memory/repository_pg.go` — 수정(sqlc gen 호출 + row→도메인)
- `backend/internal/memory/handler.go` — 수정(ListDormant RPC 핸들러, proto↔도메인)
- `proto/` 해당 service .proto — 수정(ListDormant rpc + 메시지; 02 계약 진화)

## 수용 기준 (EARS / Given-When-Then)
1.1 WHEN 별의 `last_recalled_at`이 현재보다 며칠~수십 일 과거일 때 THE SYSTEM SHALL 그 별의 표시 밝기를 `exp(-λ·Δt_days)`(λ=ln2/30)로 감소시킨다.
1.2 WHEN 감쇠 계산 결과가 `a_min=0.05`보다 작아질 만큼 오래되었을 때 THE SYSTEM SHALL 별 유효 밝기를 `max(a_min, activation)`로 바닥 처리하여 0(소멸)으로 내려가지 않게 한다. (원칙 2)
1.3 WHEN 시냅스 밝기를 계산할 때 THE SYSTEM SHALL `weight · max(a_min, activation)`을 적용한다.
2.1 GIVEN 활성도가 `a_min` 부근으로 떨어진 잠든 별 WHEN 우주를 렌더링할 때 THE SYSTEM SHALL 그 별을 우주에서 제거하지 않고 어두운 잔광으로 계속 렌더링하며 클릭 가능 상태로 둔다.
2.2 WHEN 사용자가 잠든 별을 회상(11)하여 `last_recalled_at`이 now로 갱신될 때 THE SYSTEM SHALL 다음 렌더에서 그 별을 다시 밝게(activation→1 근처) 표시한다.
3.1 WHEN 클라이언트가 `ListDormant(threshold, limit)`를 호출할 때 THE SYSTEM SHALL 감쇠 활성도가 임계 이하인 별만 `last_recalled_at` 오름차순으로 반환한다.
3.2 WHEN `GetUniverse`를 호출할 때 THE SYSTEM SHALL 잠든 별을 포함한 **전체 그래프**를 변함없이 반환한다(ListDormant는 삭제·영속 필터가 아니다).
4.1 WHEN 사용자가 `/dormant`에서 항목을 클릭할 때 THE SYSTEM SHALL 우주 화면으로 전환하고 카메라를 해당 별로 이동시킨다.
4.2 IF `ListDormant`가 빈 목록을 반환하면 THEN THE SYSTEM SHALL "잠든 별이 아직 없습니다" 안내를 표시하고 빈 화면 오류를 내지 않는다.
5.1 WHEN `activation.ts`를 작성할 때 THE SYSTEM SHALL `three`/React/DOM을 import하지 않는다(순수 model, 원칙 4·모바일 재사용).

## 작업 (Tasks)
### Setup
- [ ] T001 `proto/`의 memory service .proto에 `ListDormant` unary rpc와 `ListDormantRequest{double threshold; int32 limit;}`·`ListDormantResponse{repeated DormantStar stars;}`·`DormantStar` 메시지 추가, Docker/WSL에서 `buf generate` 실행(.exe 직접 실행 금지)  _참조: 3.1_

### 백엔드 (ListDormant)
- [ ] T010 `backend/internal/db/queries/memory.sql`에 `-- name: ListDormant :many` 쿼리 추가(설계 요점 SQL: 감쇠 임계 이하·`last_recalled_at` ASC·LIMIT), Docker/WSL에서 `sqlc generate` 실행  _참조: 3.1_
- [ ] T011 `backend/internal/memory/repository.go`에 `ListDormant(ctx, userID, threshold float64, limit int32) ([]Memory, error)` 인터페이스 메서드 추가  _참조: 3.1_
- [ ] T012 `backend/internal/memory/repository_pg.go`에서 sqlc gen `ListDormant` 호출 + row→도메인 `Memory` 매핑(태그 없는 순수 도메인, 원칙 5)  _참조: 3.1_
- [ ] T013 `backend/internal/memory/service.go`에 `ListDormant` 진입 메서드(기본 threshold 미지정 시 `2·a_min`, limit 상한 가드)  _참조: 3.1_
- [ ] T014 `backend/internal/memory/handler.go`에 `ListDormant` RPC 핸들러 추가(proto→service 인자, 도메인→`DormantStar` proto 매핑; 얇게 유지)  _참조: 3.1_

### 프론트 활성도·렌더
- [ ] T020 [P] `frontend/src/entities/memory/model/activation.ts`에 `DECAY_LAMBDA`·`A_MIN`·`activation`·`starBrightness`·`synapseBrightness`·`isDormant` 구현(설계 요점 시그니처, `now` 주입), `three`/React/DOM import 금지  _참조: 1.1, 1.2, 1.3, 5.1_
- [ ] T021 [P] `frontend/src/entities/memory/index.ts`에서 위 심볼을 명시적 export(와일드카드 금지)  _참조: 1.1_
- [ ] T022 `frontend/src/widgets/universe-canvas/ui/UniverseCanvas.tsx`(별 렌더 경로)에서 별·시냅스 밝기를 `starBrightness`/`synapseBrightness`로 산출해 인스턴스/uniform에 주입, 잠든 별을 `A_MIN` 잔광으로 제거 없이 렌더(매 프레임 ref/uniform 갱신, React state 금지)  _참조: 1.2, 2.1, 2.2_

### 프론트 잠든 별 탐색 페이지
- [ ] T030 [P] `frontend/src/pages/dormant/api/list-dormant.ts`에 `ListDormant` unary 클라이언트 호출 + 응답→뷰모델 매퍼 작성  _참조: 3.1, 6_
- [ ] T031 `frontend/src/pages/dormant/ui/DormantPage.tsx`에 목록 + 본문/날짜 텍스트 검색 필터 + 빈 상태 안내("잠든 별이 아직 없습니다") 구현, 항목 클릭 시 우주로 이동 + 카메라 타깃 별 전달  _참조: 4.1, 4.2_
- [ ] T032 `frontend/src/pages/dormant/index.ts` Public API + `frontend/src/app/`의 라우터에 `/dormant` 등록  _참조: 4.1_

### Polish
- [ ] T040 `frontend/src/entities/memory/model/activation.test.ts`(또는 동등 위치)에 Δt=0→activation≈1, Δt=30일→≈0.5, Δt=수년→`max`로 `a_min` 바닥, 시냅스 밝기 식, `isDormant` 경계 단위 테스트  _참조: 1.1, 1.2, 1.3_

## 완료 정의 (DoD)
- `frontend/src/entities/memory/model/activation.ts`가 `three`/React/DOM import 없이 컴파일되고, 단위 테스트에서 Δt=30일→`activation≈0.5`, 충분히 큰 Δt→유효 밝기가 정확히 `a_min=0.05`에서 멈춤(0 아님)을 검증. (`pnpm --filter frontend test`)
- 어떤 별의 `last_recalled_at`을 과거로 조작하면(예: DB에서 수개월 전으로 갱신) 우주에서 그 별이 **어두워지지만 사라지지 않고** 여전히 클릭 가능. 11의 회상 동작 후 다시 밝아짐.
- `ListDormant` RPC가 임계 이하 별만 반환하고, 동일 상태에서 `GetUniverse`는 잠든 별 포함 전체 그래프를 반환(`curl`/`buf curl` 또는 클라 호출로 양쪽 응답 비교).
- `/dormant` 페이지에서 잠든 별이 목록·검색으로 발견되고, 클릭 시 우주로 이동해 회상으로 재점화됨(수동 E2E). 잠든 별이 없으면 빈 상태 안내 표시.
- `pnpm build`(프론트) 성공, 백엔드 `go build ./...`(Docker/WSL)·`sqlc generate`·`buf generate` 무오류.
