# cosimosi spec — index (00.overview)

> `spec/` is cosimosi's single source of truth. **This file is the index + roadmap**: who acts on the product, what
> each doc owns, how we build (epics delivered as sprints, layered within), the project-wide decisions, and status. It
> **references** the other docs — it does not restate their content.

**The doc set — each owns its content; the others only reference it:**

| Doc | Owns | Language |
|---|---|---|
| [PRD.md](../PRD.md) | **the master** — vision, product scope & features, mechanics, invariants, neuroscience, in one integrated doc | Korean |
| [concept.md](../concept.md) | the vision + the laws & neuroscience guardrails — *why* the product is shaped this way (a PRD-consistent view) | Korean |
| [ubiquitous-language.md](../ubiquitous-language.md) | the normative term list — *the words* (mirrors PRD §3) | bilingual |
| [ARCHITECTURE.md](../ARCHITECTURE.md) | the architecture frame (the *how*): layers, boundaries, placement rules | English |
| [principle/](../principle/) | how we write — docs & comments are timeless and self-contained | English |
| **this file** | the roadmap (the *when / in what order*): actors, epics, decisions, status | English |

> **The PRD is the master.** [PRD.md](../PRD.md) is the integrated source of truth for *what we build* — scope,
> features, mechanics, the invariants ([I1]–[I11]), and the neuroscience grounding. `concept.md` and
> `ubiquitous-language.md` are **PRD-consistent views**: `concept.md` carries the readable *why* (vision, laws,
> guardrails); `ubiquitous-language.md` is the canonical *words* (= PRD §3). Where they overlap the PRD wins.

Read order: **PRD → concept → ubiquitous-language → ARCHITECTURE → this**.

---

## 0. Actors

Who (or what) acts on cosimosi. The product is unusual in that **most change is driven by the system acting on the
user's data** — the memory engine is itself an actor.

| Actor | Kind | Acts | Drives |
|---|---|---|---|
| **Diarist** (일기 작성자) | primary human | writes diaries, names stars, navigates the 3D universe, recalls (회고) / views gist, deletes & "lets go", customizes the palette, charges & spends Twinkle, invites friends | [U][W][R][D][X][P][G] |
| **AI extractor** (`Extractor`·`Embedder`·`Semanticizer`) | system | splits a diary into episodic memories, suggests names, extracts & normalizes neurons, assigns emotion, generates gist/decay stage texts, embeds for nearest-neighbour | [E][M][C7][W2][W4a] |
| **Universe clock** (우주 시계) | system | advances universe time *only* on star-launch / recall; the advance triggers read-time forgetting, consolidation, semanticization | [T][F][C] |
| **Consolidation worker** (공고화 워커) | system | drains the `Job` queue (extract / embed / link / consolidate), replay-reorganizes, downscales (SHY) | [L][C][async] |
| **Payment provider** | external | sells additional Twinkle (store integration; wiring deferred) | [G3] |
| **Friend** | secondary human | receives a shared star, accepts an invite *(sharing deferred; invite-for-growth is v1)* | [S][G3] |

---

## 1. How we build — epics, delivered as sprints, layered within

### 1.1 The roadmap is organized by **product epic**

Forward work is grouped into **epics** — product capability areas drawn from the PRD requirement prefixes
([W] writing, [R] recall, [F] forgetting, …). Epics are sequenced into **sprints** (delivery increments, §2.2). This
is the top-level structure: we ship a capability, not a layer.

### 1.2 Within an epic, units flow through the clean-architecture layers

Each epic's backlog units are ordered by the **product's clean-architecture layers** — **domain → use-case →
presentation** — which is *orthogonal* to the code architecture (FSD on the frontend, domain-first Go on the backend):

- A unit's **layer** decides *when within its epic* it is built; the **architecture** decides *where its code lives*
  (which context / slice / segment). A domain unit is the pure model wherever ARCHITECTURE puts it (the `entities/<noun>/model`
  segment, the Go `<context>` package) — **even while its rendering segment stays empty**. *How a thing is shown* is a
  presentation concern.
- **Domain & use-case units are headless.** Pure domain (types + deterministic functions, FE↔BE golden-parity) and
  use-cases (orchestration behind ports, mock fallback) carry no pixels — verified by golden-parity tests and the
  temporary **`/test` page** (Foundation · plan 12). They parallelize in dependency waves.
- **Presentation units are vertical and sequential.** Each is one user-facing flow taken end-to-end through rendering
  + UI and confirmed on screen, built one slice at a time — **each slice ships web (open beta) + mobile (MVP) together**.
  The rendering layer owns the visual vocabulary and is the one place domain and visual words meet (ARCHITECTURE §3.4).

### 1.3 Shared substrate

Some domain units (the memory aggregate & schema, emotion model, synapse plasticity, the force-sim layout, the
universe clock) are the **engine substrate** that many epics build on. They are first *needed* by — and therefore built
*inside* — the earliest epic, then reused. Reuse promotes **down** a layer (to `packages/`), never copied sideways.
On the presentation side, the **rendering substrate** (plan [14](14.rendering-foundation.md), `rendering-foundation`) —
the renderer boundary, the TSL shader toolkit, and the skinnable background — is the seam every Epic-A render unit
mounts.

### 1.4 Numbering

Plans are a **fresh, stable, contiguous series** — never reused or renumbered once authored. The Foundation is
`spec/plan/01`–`13`, plus the pre-Epic-A substrates `14` (rendering) and `15` (web routing); the next plan
`/create-plan` scaffolds is `16`, and so on. **Backlog units below are listed by
title in dependency order, without a number** — the number is assigned when `/create-plan` scaffolds the unit.

---

## 2. The epics (the roadmap)

> **Foundation (Phase 0 — environment / platform) is built.** Plans `01`–`13` set up the providers, transport,
> persistence, state, i18n, design system, observability, config, test harness, and both app shells. Details in §5.1.
> Everything below is **⬜ planned, pending `/create-plan`** — titles only, sized so each becomes *one* plan → one job
> → one implementation (the grain of a Foundation unit like `auth-session` / `data-persistence`).

Each unit's **L** = clean-architecture layer (**D**omain · **U**se-case · **P**resentation). **PRD** = the requirements
it satisfies.

### Epic A — Memory Genesis (일기 → 별 탄생)
> Write a diary; the LLM splits it into named episodic memories with emotion and neurons; synapses form; a star is born
> in the universe. The engine substrate lives here and is reused everywhere.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `memory-aggregate-schema` | D | `Diary`, `EpisodicMemory` (aggregate root), `Neuron`(+type), `NeuronActivation`, `Synapse`, `Embedding`; Postgres schema (goose) + sqlc + pgvector; per-user isolation | [E][L][U1] |
| `emotion-model` | D | `Emotion` VO; 13-mood (valence×arousal) table; `mood → color` palette function (single entry, swappable); arousal → initial strength/size modulation (valence stays color-only) | [M1]–[M3][M6][E8a] |
| `synapse-plasticity` | D | pure fns `Potentiate`(LTP)/`Depress`(LTD)/`InitialStrength`; saturating strength curve, single 1.0 cap, signal-tiered initials | [L8]–[L10] |
| `force-sim-layout` | D | `packages/force-sim` pure `tick(dt)`: centroid position (neuron-weighted), radius = connectivity, seed placement, z-layer coords; golden-parity | [E7][E8][V1][I5] |
| `encode-usecase` | U | `Encode` — diary → event-split → name suggestion + emotion + neuron extract/normalize(dedup); schema-forced LLM output + NL-edit defense | [W2][W2a][E1]–[E6][E9][E10][E11][W4a] |
| `link-usecase` | U | `Link` — same-memory Hebbian + shared-neuron + temporal-proximity synapses (proximity emergent; no memory↔memory edge) | [L1]–[L7] |
| `ai-worker-pipeline` | U | `Embedder` port + async worker; `Job` queue (extract/embed/link); keyless-mock fallback; cost metering | AI ports, async |
| `ai-provider-abstraction` | U | provider-agnostic `LLMClient`/`EmbeddingClient` seam inside `internal/ai` (Anthropic · OpenAI · DeepSeek · Z.ai · Gemini; Voyage embeddings) — one provider implemented, the rest slots | (infra) |
| **`rendering-foundation`** ✅ | P | **plan [14](14.rendering-foundation.md)** — shared package `@cosimosi/3d-renderer`: TSL shader-art toolkit, composable skins, R3F scene layers, skin seam, asset-source port, canvas host. Web universe = main page; mobile `UniverseScreen`. web + RN consume the same package | (infra) |
| `universe-canvas` | P | mounts the `rendering-foundation` boundary; domain graph → instanced nodes + line-segment edges; force-sim worker wiring; product camera/navigation (zoom/rotate/pan/focus/fly) | [U3][V0] |
| `star-neuron-rendering` | P | episodic-memory star body (size/brightness/color/seed-form) + neuron cell-stars + synapse filaments | [V3][V5][V6] |
| `latent-star-field` | P | gray latent-neuron background + activation "awaken" animation | [V7][E7a] |
| `nebula-color-field` | P | emotion color-blending nebula (strength-weighted, WebGL); honest notice — universe color = emotions you re-read, not your average emotion | [M4][M5][M7][V4] |
| `writing-flow-ui` | P | "일기 쓰기" → input (+ diary date, default today) → "별 쪼개기" (show split + emotion + name, editable) → NL edit → "별 띄우기" → star appears | [W1]–[W5] |

> **`rendering-foundation` (plan 14) is the rendering substrate** — the renderer boundary, the `packages/shaders` TSL
> toolkit, the asset-source port, and the skinnable universe background. `universe-canvas`, `star-neuron-rendering`,
> `nebula-color-field`, and `latent-star-field` all mount it; it pairs with `force-sim-layout` (which feeds coordinates).
> It is **the first unit to build in Epic A** (authored — see §5), web-first with the RN renderer reserved.

> **All thirteen Epic A units above are now authored** as plans **16–28** (the number assigned per §1.4). See the plan
> index (§5.1) for links + status and the Epic A dependency graph (§5.3) for build order. Their cross-cutting decisions
> were resolved with the user (2026-07) and folded into each plan's Design / Policy-Values sections; implement-time
> tuning values are marked "confirmed at implement" in the owning plan.

### Epic B — Universe Time (우주 시계)
> The diary-driven clock. Time advances *only* on star-launch / recall; just looking changes nothing.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `universe-clock` | D | diary-monotonic date constraint; universe-time advance model; time as a link signal, never a position axis | [T1][T5][I10] |
| `time-advance-usecase` | U | star-launch → accelerate to diary date; recall → sync to today; read-time progression hook feeding [F]/[C] | [T2]–[T4] |
| `time-acceleration-ui` | P | acceleration animation (forgetting/consolidation playthrough), sync-confirm modal, "우주의 시간" HUD | [T2][T6][R1a] |

> **All three Epic B units above are now authored** as plans **29–31** (§1.4). See the plan index (§5.1) for links +
> status and the Epic B dependency graph (§5.4) for build order. They realize the universe-clock seam Epic A left
> ([16](16.memory-aggregate-schema.md)/[20](20.encode-usecase.md)): plan 29 introduces the `universe_state` clock,
> plan 30 advances it on launch/recall and reserves the read-time progression hook, plan 31 makes it visible (HUD +
> acceleration + sync-consent modal). The recall side that composes `SyncToToday`/the sync modal lands in Epic C.

### Epic C — Recall & Reconsolidation (회상 = 재공고화)
> Recall a star: a prediction-error gate decides whether you merely reinforce it or rewrite (reconsolidate) it.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `reconsolidation-rules` | D | prediction-error gate (semantic compare), `Reshape`/seed change, `EffectiveStrength` bump; neighbor forgetting ±(spreading/inhibition); `Diary` never mutated | [R3]–[R7][V5] |
| `recall-usecase` | U | `Recall` (brightness reset + decay-stage recovery + gist-timer reset + LTP), `Reinforce` (idempotent batch), `Reconsolidate` (CurrentText + remaining-stage regen; `Diary` untouched) | [R1]–[R7][F5][C6a][C7] |
| `view-semantic-usecase` | U | `ViewSemantic` — gist-star read-only viewing (Twinkle; cheaper the more gistified) | [R8] |
| `star-detail-panel` | P | meta + forgotten current text (free) + buttons (회고하기 / 변천사 / 원본 일기 보기); provenance (변천사) view | [D1][R1][R8a] |
| `recall-flow-ui` | P | 회고하기 → sync modal → rewrite → reconsolidation result on screen | [R1][R1a][R6] |

### Epic D — Forgetting (망각)
> Dimming + word loss, read-time from universe time. A floor, never deletion; recall restores it whole.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `forgetting-decay` | D | `Decay` + `EffectiveBrightness` (read-time, floored — system never deletes), decay-stage random word removal (algorithm), arousal & connection-strength modulation | [F1][F2][F3][F6][F7][F9] |
| `forgetting-cost` | U | accessibility cost (deeper decay → costlier recall); recovery on recall | [F4][F5] |
| `forgetting-visuals` | P | brightness dimming + word-loss text on stars and in the detail panel | [F1][V2] |

### Epic E — Consolidation & Semanticization (우주의 잠)
> During time advance the universe "sleeps": gist rises to the neocortex on the z-axis, weak synapses downscale.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `semanticization` | D | `Semanticize`; gist 4-stage texts; abstraction stage; gist-timer; z-axis 2-store (`Hippocampus`/`Neocortex`) coords (copy x,y, raise z; neocortex no force-sim) | [C5]–[C7][C6a] |
| `consolidation-usecase` | U | `Consolidate` (sleep) on time advance — replay reorg, stage++, `Downscale`(SHY); no cron | [C1][C2][C4][T4] |
| `gist-star-rendering` | P | neocortical-layer gist stars rising; z-axis 2-layer universe; consolidation choreography *(placeholder)* | [V8][V9][C8] |

### Epic F — Stardust Economy (별가루)
> Recall always spends Twinkle. A daily basic pool protects the core loop; the rest is purchased/earned.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `stardust-ledger` | D | `Twinkle` two-tier (basic daily-reset + additional permanent), ledger balance derivation, spend order, cost curves (recall by decay-depth, gist by gist-depth) | [G1][G2][G4] |
| `earn-spend-usecase` | U | `Earn` (write / invite / payment) + `Spend` (recall / view-gist gating); charge paths; core-loop protection | [G3][G5][G6] |
| `stardust-ui` | P | balance HUD, cost display, charge flows (payment / invite / write) | [G2][G3] |

### Epic G — Diary Archive & Provenance (일기 원본 읽기)
> The immutable original, the per-star provenance trail, and the jump back into the universe.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `provenance-export` | U | provenance (변천사) append-only history; Export CSV/MD | [R8a][W6][D4] |
| `diary-reader-page` | P | original diary list (reader); split visualization; "이 일기로 태어난 별 보기" jump (recall-cost sum, whole recall) | [D2][D3][D5] |

### Epic H — Deletion & Letting-go (삭제·놓아주기)
> The only deletes that exist are the user's own. Shared neurons are never removed — only their synapse contribution weakens.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `deletion-rules` | D | full-delete sealing (orphan vs shared neurons; LTD on contribution), exclusion from all dynamics; letting-go semantic-neuron sealing | [X1][X3][X4] |
| `release-usecase` | U | `Release` — full delete (soft-delete, 30-day restore) + letting-go (permanent; AI-suggest semantic neurons → user-approve) | [X2][X5][X6] |
| `deletion-ui` | P | delete flow, letting-go flow, export reassurance copy, professional-resource notice | [X6][X7][W6] |

### Epic I — Customization & Account (커스터마이징·계정)
> Swap the emotion palette and round out the v1 entry path. The meaning layer stays untouchable.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `palette-customization` | U/P | `mood ↔ color` palette swap (good default + change), axis-consistency guardrails; meaning layer immutable | [P1]–[P3][I11] |
| `settings-page` | P | account, palette change, staging-layer customization *(placeholder)* | [P4], §1.6 |
| `auth-universe-gate` | P | login → universe direct (empty universe with latent stars); v1 has no landing | [U1][U2][U4] |

### Epic J — Social (소셜) *(deferred)*
> Async, one-way sharing. The friend-invite growth loop is v1 (Epic F); star *sharing* is deferred.

| Backlog unit | L | Scope | PRD |
|---|---|---|---|
| `star-sharing` | U/P | send a star to a friend (episodic unit) | [S1] |
| `public-universe` | P | browse a public universe | [S2] |

### Deferred placeholders (자리 예약)
Built later, but the v1 structure leaves their slot so nothing collides with it:

- **Persistent interference** — cumulative retroactive interference over time ([F8]; the recall-moment ± in [R5] *is* v1).
- **Consolidation choreography** — the replay / "pulled-upward" visual detail ([V8][C8]).
- **Palette free-edit UI** & **staging-layer customization** ([P4]).
- *(Out of v1 entirely — see PRD §8.3: onboarding/landing, schema [C3], real deployment, multi-user realtime.)*

### 2.2 Sprint sequencing

Epics ship in dependency order; a sprint may carry more than one epic once the substrate exists.

| Sprint | Epics | Outcome |
|---|---|---|
| 1 | **A** | Write a diary → a living star appears. Engine substrate + first vertical. |
| 2 | **B**, **C** | The core loop is alive — write, time advances, recall & reconsolidate. |
| 3 | **D**, **E** | The dynamics — forgetting and the sleeping/semanticizing universe. |
| 4 | **F**, **G** | The economy gates recall; the archive & provenance read back. |
| 5 | **H**, **I** | v1 completion — deletion/letting-go, customization, account gate. |
| 6+ | **J**, placeholders | Social + the reserved future seams. |

---

## 3. The constitution (where the rules live — not restated here)

Every unit honors the project's constitution. It is **owned by the docs below**, not duplicated into this index:

- **The invariants [I1]–[I11] + behavioral laws + neuroscience guardrails** (forgetting = dimming, recall = partial
  restore, sleep consolidation, emotion ≠ position, the must-not-implement list, …) → **[PRD.md](../PRD.md) §7, §9**
  and their readable form in **[concept.md](../concept.md)**.
- **Architecture rules** (one ubiquitous language owned by the domain model; domain-first Go with adapters at the edge
  and consumer-owned ports; proto/domain/sqlc separation; unary-only transport; FSD one-way imports; instancing/TSL
  rendering; config-as-build-time; …) → **[ARCHITECTURE.md](../ARCHITECTURE.md)**.
- **The canonical terms** themselves → **[ubiquitous-language.md](../ubiquitous-language.md)** (= PRD §3, normative).

A plan that would break one of these resolves it against the owning doc *before* it is written. `/create-plan` checks
every new plan against the invariants ([I1]–[I11]).

---

## 4. Cross-cutting decisions

- **App roots:** `apps/api` · `apps/web` · `apps/mobile`, plus `apps/blog`. Shared pure code lives in `packages/`.
- **Mobile = React Native — first-class MVP target, built alongside web (not deferred).** Release targets: **web =
  open beta, mobile = MVP**. RN reuses the one shared stack (no parallel state/i18n lib); the RN WebGPU renderer is in
  scope (pre-1.0 `react-native-webgpu`, early-adopter caveats). Rationale: [ARCHITECTURE.md](../ARCHITECTURE.md) §3.5.
- **No real deployment yet.** The foundation is deployable, but production hosting (Cloudflare / Lightsail / Supabase)
  is out of scope for now.
- **Config is build-time.** Every tuning number a plan introduces goes in `spec/values.yaml` → `pnpm gen:values` →
  FE(TS)/BE(Go) constants; never hardcoded.
- **Numbering.** Fresh contiguous series (01+); stable IDs — never reused or renumbered once authored.
- **Docs.** The PRD, concept stay Korean (the vision source); the glossary is bilingual; plan / job / tech /
  ARCHITECTURE / principle docs are English.
- **tech/ & policy/** docs are added incrementally as units land — the as-built rules each one needs.

---

## 5. Workflow & status

**Author → plan → implement.** Each unit gets a `spec/plan/NN.*.md` (`/create-plan`), then a job doc
(`/create-plan-job`), then implementation (`/implement-job`) into the clean `apps/{api,web,mobile}` roots.

| stage | status |
|---|---|
| PRD.md (master) · concept.md · ubiquitous-language.md · ARCHITECTURE.md · 00.overview | ✅ in place |
| Foundation (plans 01–13) | ✅ built (Phase 1 complete: both app shells shipped) |
| [14 rendering-foundation](14.rendering-foundation.md) (Epic A rendering substrate) · [job 17](../jobs/archive/17.rendering-foundation.md) | ✅ built — web + RN share `@cosimosi/3d-renderer`; on-device render pending (verify via `pnpm ios`) |
| [15 web-router-foundation](15.web-router-foundation.md) (web routing substrate, web↔mobile parity) · [job 19](../jobs/archive/19.web-router-foundation.md) | ✅ built — TanStack Router, `tech/web-routing.md` |
| **Epic A — Memory Genesis** (plans 16–28, Sprint 1) | ✅ built — all thirteen units shipped web + mobile; write a diary → the LLM splits it → a living star appears (§5.1) |
| **Epic B — Universe Time** (plans 29–31, Sprint 2) | ⬜ planned — all three units authored; the diary-driven clock (advance on launch/recall, HUD + acceleration + sync-consent). Next: `/create-plan-job 29` (§5.1/§5.4) |
| Epics C–J (remaining backlog) | ⬜ planned — author each with `/create-plan` |

### 5.1 Plan index

| NN | Plan | Job | Status | Next |
|---|---|---|---|---|
| 01 | [platform-foundation](01.platform-foundation.md) | [job 01](../jobs/archive/01.platform-foundation.md) | ✅ built | archived |
| 02 | [quality-gates](02.quality-gates.md) | [job 02](../jobs/archive/02.quality-gates.md) | ✅ built | archived |
| 03 | [rpc-transport](03.rpc-transport.md) | [job 03](../jobs/archive/03.rpc-transport.md) | ✅ built | archived |
| 04 | [auth-session](04.auth-session.md) | [job 04](../jobs/archive/04.auth-session.md) | ✅ built | archived |
| 05 | [data-persistence](05.data-persistence.md) | [job 05](../jobs/archive/05.data-persistence.md) | ✅ built | archived |
| 06 | [client-data-cache](06.client-data-cache.md) | [job 06](../jobs/archive/06.client-data-cache.md) | ✅ built | archived |
| 07 | [state-machine-foundation](07.state-machine-foundation.md) | [job 07](../jobs/archive/07.state-machine-foundation.md) | ✅ built | archived |
| 08 | [i18n-seam](08.i18n-seam.md) | [job 08](../jobs/archive/08.i18n-seam.md) | ✅ built | archived |
| 09 | [design-system](09.design-system.md) | [job 09](../jobs/archive/09.design-system.md) | ✅ built | archived |
| 10 | [observability-and-flags](10.observability-and-flags.md) | [job 10](../jobs/archive/10.observability-and-flags.md) | ✅ built | archived |
| 11 | [config-values](11.config-values.md) | [job 11](../jobs/archive/11.config-values.md) | ✅ built | archived |
| 12 | [test-harness](12.test-harness.md) | [job 12](../jobs/archive/12.test-harness.md) | ✅ built | archived |
| 13 | [mobile-app-shell](13.mobile-app-shell.md) | [job 13](../jobs/archive/13.mobile-app-shell.md) | ✅ built | archived |
| 14 | [rendering-foundation](14.rendering-foundation.md) | [job 17](../jobs/archive/17.rendering-foundation.md) | ✅ built — web + RN share `@cosimosi/3d-renderer` | on-device render pending (`pnpm ios`) |
| 15 | [web-router-foundation](15.web-router-foundation.md) | [job 19](../jobs/archive/19.web-router-foundation.md) | ✅ built — TanStack Router in `app/routes`, web↔mobile parity | archived |
| 16 | [memory-aggregate-schema](16.memory-aggregate-schema.md) | [job 20](../jobs/archive/20.memory-aggregate-schema.md) | ✅ built | archived |
| 17 | [emotion-model](17.emotion-model.md) | [job 21](../jobs/archive/21.emotion-model.md) | ✅ built | archived |
| 18 | [synapse-plasticity](18.synapse-plasticity.md) | [job 22](../jobs/archive/22.synapse-plasticity.md) | ✅ built | archived |
| 19 | [force-sim-layout](19.force-sim-layout.md) | [job 23](../jobs/archive/23.force-sim-layout.md) | ✅ built | archived |
| 20 | [encode-usecase](20.encode-usecase.md) | [job 26](../jobs/archive/26.encode-usecase.md) | ✅ built — `MemoryService` (SplitDiary/ReviseSplit/LaunchStars + GetUniverse), `Encode`/`PersistEncoded` | archived |
| 21 | [link-usecase](21.link-usecase.md) | [job 27](../jobs/archive/27.link-usecase.md) | ✅ built | archived |
| 22 | [ai-worker-pipeline](22.ai-worker-pipeline.md) | [job 24](../jobs/archive/24.ai-worker-pipeline.md) | ✅ built | archived |
| 23 | [universe-canvas](23.universe-canvas.md) | [job 28](../jobs/archive/28.universe-canvas.md) | ✅ built — web `/` + mobile `UniverseScreen` render the real graph | archived |
| 24 | [star-neuron-rendering](24.star-neuron-rendering.md) | [job 29](../jobs/archive/29.star-neuron-rendering.md) | ✅ built — star/cell-star/filament bodies rendered on web; mobile source-gate wired (on-device render pending, `pnpm ios`) | archived |
| 25 | [latent-star-field](25.latent-star-field.md) | [job 30](../jobs/archive/30.latent-star-field.md) | ✅ built — gray latent field + awaken on web + mobile | archived |
| 26 | [nebula-color-field](26.nebula-color-field.md) | [job 31](../jobs/archive/31.nebula-color-field.md) | ✅ built — emotion color field (additive TSL kernels) + honest notice, web + mobile | archived |
| 27 | [writing-flow-ui](27.writing-flow-ui.md) | [job 32](../jobs/archive/32.writing-flow-ui.md) | ✅ built — write → split → edit (touch + NL) → optimistic launch, web + mobile | archived |
| 28 | [ai-provider-abstraction](28.ai-provider-abstraction.md) | [job 25](../jobs/archive/25.ai-provider-abstraction.md) | ✅ built | archived |
| 29 | [universe-clock](29.universe-clock.md) | — | ⬜ planning | `/create-plan-job 29` |
| 30 | [time-advance-usecase](30.time-advance-usecase.md) | — | ⬜ planning | `/create-plan-job 30` |
| 31 | [time-acceleration-ui](31.time-acceleration-ui.md) | — | ⬜ planning | `/create-plan-job 31` |

> Plans 29–31 are **Epic B — Universe Time** (Sprint 2), authored together as one set (2026-07). Cross-cutting
> decisions (lazy clock birth + one-release read fallback; the advance interval on `LaunchStars`; the no-op progression
> hook reserving the [F]/[C] slot; the sync-consent modal owned in Epic B but triggered by Epic C recall) are folded
> into the plans as "Decided (2026-07)" notes; implement-time tuning values are marked "confirmed at implement". Build
> order: 29 (clock domain) → 30 (advance use-case) → 31 (time UI). See §5.4.

> Plans 16–28 are **Epic A — Memory Genesis** (Sprint 1), authored together as one coherent set. Cross-cutting
> decisions were resolved with the user (2026-07) and folded into each plan; implement-time tuning values are marked
> "confirmed at implement" in the owning plan.
> **Job build order (dependency waves):** job 20 (schema root) → 21 · 22 · 23 (pure domain, parallel) → 24 (worker/ports)
> → 25 (provider seam) → 26 (encode) → 27 (link) → 28 (canvas) → 29 (star bodies) → 30 · 31 (field layers, parallel)
> → 32 (writing flow, the end-to-end vertical).

**Refactor jobs (from code-review):**

| Review | Job | Findings | Status | Next |
|---|---|---|---|---|
| [code-review 01](../code-review/01.phase-1-critical-readiness-audit.md) | [job 14](../jobs/archive/14.phase-1-runtime-correctness.md) | R006–R012 · R014–R016 | ✅ done | archived |
| [code-review 01](../code-review/01.phase-1-critical-readiness-audit.md) | [job 15](../jobs/archive/15.phase-1-architecture-gates-and-hygiene.md) | R001–R005 · R013 · R017–R023 | ✅ done | archived |
| [code-review 02](../code-review/02.phase-1-foundation-runtime-and-gate-audi.md) | [job 16](../jobs/archive/16.phase-1-foundation-hardening-and-hygiene.md) | R001–R005 · R009 · R010 · R012–R014 · R017 · R019 · R020 · R022 (fix set) | ✅ done | archived |
| [code-review 03](../code-review/03.phase-2-runtime-and-architecture-audit.md) | [job 33](../jobs/archive/33.phase-2-runtime-and-architecture-hardeni.md) | R001–R008 (all verified) | ✅ done | archived |
| [code-review 04](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md) | [job 34](../jobs/archive/34.job-queue-lease-fencing-and-ai-retry-cor.md) | R001 · R003 · R004 | ✅ done | archived |
| [code-review 04](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md) | [job 35](../jobs/35.promote-epic-a-write-vertical-to-package.md) | R011 done · R002 deferred | 🟡 partial | bulk promotion (R002) — own session |
| [code-review 04](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md) | [job 36](../jobs/archive/36.gate-parity-and-comment-and-config-drift.md) | R006 · R007 · R008 · R009 · R014 | ✅ done | archived |
| [code-review 04](../code-review/04.epic-a-memory-genesis-runtime-and-gate-a.md) | [job 37](../jobs/37.rendering-and-backend-seam-hygiene-and-t.md) | R005 · R012 · R013 done · R010 partial | 🟡 partial | proto `past_dated` (R010) + 2 tests |

> Foundation jobs are archived under `spec/jobs/archive/`. The job-number series (01–16) is shared by build jobs and
> refactor jobs; the **plan** series (01–13, then 14+) is separate. Forward epic plans continue the plan series at 14.

**Changes (in flight):**

| Change | Targets | Job | Status | Next |
|---|---|---|---|---|
| [change 01 background-type-registry](../changes/archive/01.background-type-registry.md) | [plan 14](14.rendering-foundation.md) | [job 18](../jobs/archive/18.background-type-registry.md) | ✅ done | archived |

> Change proposals modify an already-shipped plan (a WHAT delta); `/create-change-job` turns one into a job. Change
> numbers are their own series (`spec/changes/NN`), monotonic across live + `archive/`.

### 5.2 Foundation dependency graph (built — for reference)

- 01 is the root dependency for every Foundation unit.
- 02 (quality-gates) guards the rest of the phase.
- 03 (rpc-transport) precedes 04, 06, 10, 12 where they need RPC.
- 04 (auth) and 05 (persistence) build in parallel after 03, then converge on per-user isolation.
- 11 (config-values) precedes 06 (cache timing defaults are generated values).
- 08 (i18n) precedes 09, 12 for UI copy boundaries; 09 (design-system) precedes the final shape of 12.
- 13 (mobile shell) composes 04 · 06 · 07 · 08 · 09; built once those exist, in parallel with 12.
- 15 (web router · ✅ built) depends on 01 (web composition root), 08 (i18n copy), 10 (flag gating `/test`; error
  boundary), 12 (`/test`), 14 (universe = `/`); mirrors 13's `app/navigation` discipline. Last Foundation unit before
  Epic A's presentation units, which consume its typed route registry.

### 5.3 Epic A dependency graph (Sprint 1 — plans 16–27)

Layered domain → use-case → presentation (§1.2). Domain & use-case units are headless (verified by golden-parity +
the `/test` page); presentation units are vertical and mount the [14 rendering-foundation](14.rendering-foundation.md)
substrate. The backend core lives in one `internal/memory` context; the AI wrapper in `internal/ai`.

- **Domain (build first, parallel waves):** 16 (memory-aggregate-schema) is the root — the aggregate tables every
  other unit reads/writes; it depends on Foundation 01/04/05. 17 (emotion-model), 18 (synapse-plasticity), and 19
  (force-sim-layout) are pure domain, each depending only on 16 (and 11 config-values for generated tuning). 19 is
  `packages/force-sim` (positions emergent, never stored — [I5]); 17/18 carry TS↔Go golden-parity mirrors.
- **Use-case (after their domain):** 20 (encode-usecase) depends on 16 + 17 (emotion) + 22 (Extractor/Semanticizer
  ports/adapters); 21 (link-usecase) depends on 16 + 18 (plasticity fns) + 20 (runs after persist); 22
  (ai-worker-pipeline) depends on 16 (jobs table) and provides the ports 20/21 consume; 28 (ai-provider-abstraction)
  is the provider-client seam under 22's port adapters (implemented together, or 22-then-28). 20/21/22/28 form the
  launch loop.
- **Presentation (vertical, each ships web + mobile):** 23 (universe-canvas) is the first — it mounts 14, owns the
  `GetUniverse` read contract + the domain-mirror entities, and wires 19's force-sim worker; it depends on 14/15/13/16/
  18/19. 24 (star-neuron-rendering) depends on 23 + 17 + 18 + 19; 25 (latent-star-field) and 26 (nebula-color-field)
  depend on 23 (+ 24 for layering, 17 for color). 27 (writing-flow-ui) is the end-to-end write flow — it depends on
  20/22 (RPCs) + 23 (the universe it launches into) and triggers 24 (new star body) + 25 (awaken).
- **Sprint-1 outcome:** write a diary → the LLM splits it into named episodic memories with emotion + neurons →
  synapses form → a living star appears in the 3D universe (§2.2). The engine substrate (16–19) is reused by Epics B–J.

### 5.4 Epic B dependency graph (Sprint 2 — plans 29–31)

Layered domain → use-case → presentation (§1.2). Epic B realizes the universe-clock seam Epic A deliberately left
(no `universe_state` table; universe time derived as `max(created_universe_time)` — [16](16.memory-aggregate-schema.md)/
[20](20.encode-usecase.md)); it builds strictly on the shipped Epic A substrate, adds **one** table and **no** new
tuning scalars.

- **Domain (build first):** 29 (universe-clock) is the root — the `universe_state` table + the pure clock model
  (`AdvanceClock` = monotonic `max` [I10], the `CanLaunchAt` diary-date constraint [T1], universe time as the read-time
  "now" never a position axis [T5][I7]). It depends on Epic A's [16](16.memory-aggregate-schema.md) (schema it extends) +
  Foundation [04](04.auth-session.md)/[05](05.data-persistence.md).
- **Use-case (after the clock):** 30 (time-advance-usecase) drives the clock — `AdvanceOnLaunch` (→ diary date, no
  warning) folded into [20](20.encode-usecase.md)'s launch transaction (replacing the Epic A `max` guard), `SyncToToday`
  (→ today, the capability Epic C recall composes), the advance interval added to `LaunchStars`, `GetUniverse.universe_time`
  rewired to the stored clock, and the no-op **read-time progression hook** ([T4]) reserving the [F]/[C] slot. Depends on
  29 + [20](20.encode-usecase.md) + [22](22.ai-worker-pipeline.md) (the `jobs` queue the hook enqueues onto in Epics C/E).
- **Presentation (vertical, ships web + mobile):** 31 (time-acceleration-ui) makes the clock visible — the "우주의 시간"
  HUD ([T6]), the acceleration choreography over the advance interval ([T2], reserving the [V8][C8] slot), and the
  sync-consent modal ([T2] case 2 / [R1a], built here, triggered by Epic C recall). Depends on 30 (the clock + interval
  + `SyncToToday`) + [23](23.universe-canvas.md) (the running scene + `GetUniverse` read) + [27](27.writing-flow-ui.md)
  (the launch event it choreographs) + [08](08.i18n-seam.md)/[13](13.mobile-app-shell.md).
- **Sprint-2 outcome (with Epic C):** the core loop is alive — write, the clock advances (visibly), recall &
  reconsolidate (§2.2). Epic B's `SyncToToday` capability + sync-consent modal + the `AdvanceProgression` hook are the
  seams Epic C (recall) and Epics D/E (forgetting/consolidation) compose.
