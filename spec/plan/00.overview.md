# cosimosi spec — index (00.overview)

> `spec/` is cosimosi's single source of truth. **This file is the index + roadmap**: what each doc owns, the build
> sequence, the project decisions, and status. It **references** the other docs — it does not restate their content.

**The doc set — each owns its content; the others only reference it:**

| Doc | Owns | Language |
|---|---|---|
| [concept.md](../concept.md) | the vision + the laws & neuroscience guardrails (the *why*) | Korean |
| [ubiquitous-language.md](../ubiquitous-language.md) | the normative term list (the *words*) | bilingual |
| [ARCHITECTURE.md](../ARCHITECTURE.md) | the architecture frame (the *how*): layers, boundaries, placement rules | English |
| [principle/](../principle/) | how we write — docs & comments are timeless and self-contained | English |
| **this file** | the roadmap (the *what / when*): phases, decisions, status | English |

Read order: **concept → ubiquitous-language → ARCHITECTURE → this**.

---

## 0. Principles

- **Foundation before business logic.** Load-bearing layers come first — monorepo wiring, transport, data, the domain
  core — then the pipeline, rendering, and experience.
- **The domain language is built first, owned by the domain core** (Phase 1), so one vocabulary holds across every
  layer rather than drifting between planning, DB, and code.
- **App roots:** `apps/api` · `apps/web` · `apps/mobile`, plus `apps/blog`. Shared pure code lives in `packages/`.
- **No real deployment yet.** The foundation is deployable, but production hosting (Cloudflare / Lightsail / Supabase)
  is out of scope for now.
- **Docs follow [the writing principles](../principle/)** — each is timeless and self-contained; one doc owns each
  concern and the rest reference it.

---

## 1. Build roadmap (foundation → core domain → pipeline → experience)

Phases are sequenced by **what everything else stands on**. We author and build roughly in order; within a phase,
units parallelize. Numbers are a **fresh, stable series** — never reused or renumbered. Each unit's domain meaning is
defined in concept/ubiquitous-language; the **Purpose** column states only *what the unit delivers*, not the domain.

### Phase 0 — Foundation / platform
> The load-bearing wiring for the clean `apps/{api,web,mobile}` roots. No business logic yet.

| NN | Unit | Purpose | Scope |
|---|---|---|---|
| 01 | [platform-foundation](01.platform-foundation.md) | clean `apps/{api,web,mobile}` roots; pnpm workspace; BE/FE/mobile hello-world | Infra |
| 02 | quality-gates | lint · steiger · vitest · tsc · go vet/test + the ubiquitous-language lint | Infra |
| 03 | rpc-transport | `proto/` contract, Connect unary, interceptors, connect-query | FS |
| 04 | auth-session | Supabase Auth + session machine, per-user isolation | FE |
| 05 | data-persistence | Postgres + pgvector + goose + sqlc; repository ports | BE |
| 06 | client-data-cache | TanStack Query cache / invalidation / optimistic + CDN GET | FE |
| 07 | state-machine-foundation | XState v5 catalog; control-state vs data; R3F per-frame read | FE |
| 08 | i18n-seam | Paraglide string seam from day one | FE |
| 09 | design-system | Tailwind tokens, ui primitives, theme→background, a11y | FE |
| 10 | observability-and-flags | Sentry + PostHog, error/limits/panic, consent, feature flags | FS |
| 11 | config-values | `values.yaml` → `gen:values` (build-time, git-versioned) | FS |

### Phase 1 — Core domain
> The pure domain core — types, physics, schema. (What the domain *is* lives in concept + ubiquitous-language.)

| NN | Unit | Purpose | Scope |
|---|---|---|---|
| 12 | engram-domain-model | the core domain types + the connection rule, as pure code | FS |
| 13 | memory-physics | deterministic pure functions + golden-parity tests (FE↔BE) | FS |
| 14 | emotion-model | the emotion value object + color/intensity mapping | FS |
| 15 | persistence-schema | the core domain's tables (sqlc + pgvector) | BE |
| 16 | spatial-layout-model | the layout rules (center / radius / angle) as client logic | FE |

### Phase 2 — Encoding pipeline & AI ports
> Diary in → universe out, asynchronously. AI lives behind ports.

| NN | Unit | Purpose | Scope |
|---|---|---|---|
| 17 | ai-provider-ports | embedder + extractor ports/adapters, mock fallback, cost metering | BE |
| 18 | encoding-pipeline | the encode use-case (diary → domain objects) | FS |
| 19 | linking-model | the linking rule (overlap + strength signals) | FS |
| 20 | async-jobs | the job queue: idempotency, backoff | BE |
| 21 | consolidation-model | the nightly consolidation pass | FS |

### Phase 3 — Rendering & experience
> The visible universe on top of the settled domain. The rendering layer owns the visual vocabulary (ARCHITECTURE §3.4).

| NN | Unit | Purpose | Scope |
|---|---|---|---|
| 22 | universe-canvas | WebGPU `<Canvas>`, camera, Bloom | FE |
| 23 | force-sim-worker | pure force-sim in a Web Worker → coords to render | FE |
| 24 | cell-star-rendering | the small-star visual tier | FE |
| 25 | star-rendering | the big-star visual tier (TSL bodies, instanced buckets) | FE |
| 26 | synapse-rendering | the edge visual tier (Line2) | FE |
| 27 | record-memory-ui | the writing flow → optimistic render | FE |
| 28 | recall-reinforce | the recall flow + reinforcement | FS |
| 29 | decay-dormant | dimming with floor; dormant browse | FS |

### Phase 4+ — Depth, personalization, social, ops *(after the core loop is alive)*
> Evolution history, ambient mood, customization/skins, embodied navigation, landing, demo, admin, membership,
> sharing/resonance. Numbered 30+ as they are authored.

---

## 2. The constitution (where the rules live — not restated here)

Every unit honors the project's constitution. It is **owned by the docs below**, not duplicated into this index:

- **Behavioral laws + neuroscience guardrails** (forgetting = dimming, recall = partial restore, sleep
  consolidation, emotion ≠ position, the must-not-implement list, …) → **[concept.md](../concept.md) §9–§10**.
- **Architecture rules** (one ubiquitous language owned by the domain; `domain → app → infra`; proto/domain/sqlc
  separation; unary-only transport; FSD one-way imports; instancing/TSL rendering; config-as-build-time; …) →
  **[ARCHITECTURE.md](../ARCHITECTURE.md)**.
- **The canonical terms** themselves → **[ubiquitous-language.md](../ubiquitous-language.md)** (normative).

A plan that would break one of these resolves it against the owning doc *before* it is written.

---

## 3. Cross-cutting decisions

- **Mobile = React Native; built later.** Rationale and structure: [ARCHITECTURE.md](../ARCHITECTURE.md) §3.5.
- **Numbering.** Fresh contiguous series (01+); stable IDs — never reused or renumbered.
- **Docs.** Spec docs are English; the glossary is bilingual; `concept.md` stays Korean as the vision source.
- **tech/ & policy/** docs are added incrementally as units land — the as-built rules each one needs.

---

## 4. Workflow & status

**Author → plan → implement.** Each unit gets a `spec/plan/NN.*.md`, then a job doc, then implementation into the
clean `apps/{api,web,mobile}` roots. Config numbers go in `values.yaml` → `pnpm gen:values` (never hardcoded).

| stage | status |
|---|---|
| concept.md · ubiquitous-language.md · ARCHITECTURE.md · 00.overview | ✅ in place |
| [01 platform-foundation](01.platform-foundation.md) | ⬜ planned (next: `/cosimosi:create-plan-job 01`) |
| 02–29 plans | ⬜ to author |
