# 82. blog site

> Blog site — the long-form theory tier of the public funnel. `apps/blog` (Astro, static) becomes a real content site
> with one file per post, served at the **`/blog` subpath of the product origin** so one domain accumulates authority and
> every landing↔post link stays same-origin ([K5]). Two builds share one Cloudflare Worker asset directory via a staged
> copy — no second Worker, no second zone route, no subdomain. It owns the depth (papers, DOIs); PRD §9 stays the
> requirement↔evidence SSOT, and the lay summary stays on the landing page ([K2]).
> Scope: FE | Status: built (job 121)
>
> Layer P. Home: `apps/blog` (an app root **outside** FSD and outside the i18n gates), plus exactly one FSD slice in
> `apps/web` — `pages/blog-not-found` — which fills the seam the SPA not-found handler opens under `/blog/**`.

## Purpose

The product has a marketing page and a theory story, but the theory has nowhere deep to live. PRD §9 is a
requirement→evidence map written for implementers: it names 엔그램, 재공고화, 침묵 엔그램 and their DOIs in one line
each, which is exactly right for tracing a rule to its paper and exactly wrong for a curious visitor. [K2] resolves this
with two tiers — the landing page carries a lay summary a non-specialist reads ([K1] step 5; §1.5 says neuroscience
knowledge is never required of a user), and **the blog carries the depth**: the papers, the DOIs, the "why this rule and
not another" reasoning. This unit builds that second tier.

It also closes a real hosting gap. `apps/blog` exists today — an Astro 7 app in the pnpm workspace, gated by
`pnpm typecheck:blog` and `pnpm build:blog`, with nine finished Korean essays inside a single `src/blog.md` rendered as
one long anchored page. **Nothing deploys it.** The Cloudflare Workers Builds command is
`pnpm --filter @cosimosi/web build` and `wrangler.jsonc` uploads `./apps/web/dist` alone (`DEPLOY.md` §2, §5 step 9), so
`apps/blog/dist` has never reached the origin. Worse, its output is shaped for the origin _root_: it claims
`/robots.txt`, `/sitemap.xml`, `/rss.xml`, `/favicon.svg` and `/index.html`, three of which collide with the app the
moment both dists share a directory. [K5] fixes the address (`/blog`, a subpath — not a subdomain); this plan fixes the
mechanism, and states both as one arrangement instead of discovering them at deploy time.

Finally, the existing content is not yet publishable under [I12]/[I3]. Post 8 ("비슷한 감정의 기억끼리 끌어당깁니다")
tells the reader that cosimosi binds stars whose **emotion coordinates are close** more tightly, that strong arousal
keeps a memory "가까이", and that memories **attract each other** — three claims the product must never implement ([I3]
emotion never touches position or links; [I4] there is no memory↔memory edge or attraction; [I7] radius is
self-relevance). Public copy that describes forbidden mechanics is the [I12] failure mode in its purest form: not
overselling the brain metaphor, but describing a _different, wronger_ product. The closing note also links
`spec/tech/neuroscience.md`, a file that does not exist, from public copy. Publishing is therefore gated on a content
correction, not just a pipeline.

## Scope / Non-goals

**In scope**

- **The serving arrangement** ([K5]) — `apps/blog` builds to `apps/blog/dist`, which is staged into `apps/web/dist/blog/`
  **after** the web build, so the single Worker (`wrangler.jsonc` `name: "cosimosi"`, `assets.directory:
"./apps/web/dist"`) serves both from one asset directory with no route, zone or DNS change. One root script
  owns the sequence; the Workers Builds command becomes that one script.
- **`base: '/blog'` + `trailingSlash: 'always'` in `apps/blog/astro.config.mjs`** ([K5]) — without `base`, every page
  404s its own `_astro/*` stylesheet; `trailingSlash` makes dev and production agree on the canonical `/blog/` form.
- **Origin-root ownership** ([K5]) — the blog stops shipping `robots.txt` and `favicon.svg`; the origin root owns them.
  The blog's `sitemap.xml` moves under `/blog/` and is referenced from the root one.
- **A content model: one post = one file** — an Astro content collection at `apps/blog/src/content/posts/` with a
  schema-validated frontmatter (`title` · `description` · `pubDate` · `lang` · `pillar` · `sources[]` · `draft`), routed
  to `/blog/<slug>/`, indexed at `/blog/`, and syndicated at `/blog/rss.xml`. The nine essays in `src/blog.md` migrate
  to nine files.
- **The five-pillar link to the landing page** ([K2]) — `pillar` is a closed enum of exactly the five ids
  [81.landing-page](81.landing-page.md)'s theory cards link to: `engram` (엔그램), `spatial-representation` (공간 표상),
  `synapse-time` (시냅스·시간), `reconstructive-recall` (재구성적 회상), `forgetting-accessibility` (망각=접근성).
  The `/blog/` index groups posts under their pillar and **emits each group heading with `id="<pillar>"`**, so the
  landing card's `/blog/#<id>` resolves to a real anchor. Each landing card therefore has a stable blog destination and
  PRD §9 traceability is checkable at build time.
- **The `/blog/**` miss behavior** — a `/blog/$` splat route in `apps/web` rendering blog-shaped not-found chrome, plus
  the honest statement of what the SPA asset handler can and cannot do about the status code.
- **The [I12] content gate** — a repository lint over blog content that rejects "works like a brain"-class claims,
  therapeutic claims, and any sentence presenting the 3D coordinates as the brain's real coordinates; plus the content
  correction of post 8 and the dead `spec/tech/neuroscience.md` link.
- **The three blog build constants stay module constants in the Astro app** — no `spec/values.yaml` key, no group, no
  `@cosimosi/config` dependency (`apps/blog` keeps `astro` as its only dependency). The Policy section carries the
  reasoning: groups are closed and v2 sanctions no blog group, and no other surface reads these numbers.

**Non-goals**

- **The landing page, the origin's `/` route, `gateDecision`, and the origin-root `robots.txt`/`sitemap.xml` files** —
  [81.landing-page](81.landing-page.md) owns the origin, its trailing-slash policy for the routes it introduces, and the
  root SEO files. This plan depends on the root `robots.txt` naming `/blog/sitemap.xml`; it does not
  author it.
- **The stale "no real deployment yet" lines** in [00.overview](00.overview.md) §4 and
  [ARCHITECTURE.md](../ARCHITECTURE.md) §5 — [81.landing-page](81.landing-page.md) owns both corrections.
- **Blog post localization** — [K5] says posts are not i18n'd; per-language posts instead. The `lang` frontmatter field
  is the whole mechanism, and a second-language index is a deferred placeholder ([00.overview](00.overview.md) §2a).
- **A mobile analogue** — waived, see _Parity waiver_.
- **A second Worker, a `…/blog*` zone route, a `blog.` subdomain, or `cosimosi.com`** — rejected in Design; the domain
  is not owned or configured, and PRD [K5] (as amended) deliberately names no domain — the blog's address is "the
  product origin's `/blog` subpath", with the concrete origin recorded only by `DEPLOY.md`.
- **Any product data, RPC, session or SDK in the blog** — the blog is static HTML with no runtime dependency on the app;
  it never reads `@cosimosi/api-client`, Supabase, or the demo.
- **A CMS, comments, blog-side search, blog analytics, or per-post OG image generation** — a committed static OG raster
  serves every post.
- **Rewriting PRD §9** — it stays the requirement↔evidence SSOT ([K5]); the blog cites it, never replaces it.

## Grounding

- Constitution ([I1]–[I13]): [00.overview](00.overview.md) §3 (_The constitution_), owned by [PRD.md](../PRD.md) §7.
  This is a public-copy unit, so [I12] is the invariant it exists to discharge, and [I3]/[I4]/[I5]/[I7] are at risk
  **through prose** rather than through code. Structural guards (the invariant risk register's Epic Q row,
  [00.overview](00.overview.md) §3):
  - **[I12] honest public copy** — a repo lint (`scripts/lint-public-copy.mjs`, wired into `pnpm lint`) over
    `apps/blog/src/content/**` fails the build on the forbidden claim classes; a review habit is not a guard.
  - **[I3]/[I4]/[I7] described mechanics** — the same gate carries the "no emotion→position/link", "no memory↔memory
    attraction" and "radius is self-relevance, not recency" phrase families, because the shipped defect in post 8 proves
    prose drifts where code cannot.
  - **[I5] coordinates are emergent** — every post that discusses position must state that the 3D coordinates are a
    visualization emerging from the relationship graph, never the brain's coordinates.
  - **UL discipline** — content lives under `apps/blog/src/`, which is already a scanned root of
    `scripts/lint-ubiquitous-language.mjs`, so the retired-synonym gate covers blog prose by placement. Poetic
    user-facing vocabulary (별 · 별자리 · 성운 · 별가루) is _canonical_ in this surface
    ([ubiquitous-language.md](../ubiquitous-language.md) "세 층의 언어"); the §4 rendering words are what may not leak
    into domain contexts, and a blog has none. A new domain noun would have to be registered in UL before it appeared
    in code; this unit registers none — `post` and `pillar` are content-authoring vocabulary in a static site, not
    domain concepts.
  - **[I13]** does not apply — this unit is not the demo; it links to `/demo` and nothing more.
- [PRD.md](../PRD.md) **§5.11 [K5]** — Astro static site at the `/blog` subpath for one domain's authority and
  same-origin landing↔post links; posts are not i18n'd; the blog owns the _depth_ of §9 while §9 stays the
  requirement↔evidence SSOT.
- [PRD.md](../PRD.md) **§5.11 [K2]** — two theory tiers: lay summary on the landing, papers and DOIs on the blog; the
  landing never lists academic citations.
- [PRD.md](../PRD.md) **§7 [I12]** — every public surface says "inspired by engram theory", never "works like a brain";
  no therapeutic claim; the 3D coordinates are never the brain's real coordinates.
- [PRD.md](../PRD.md) **§1.6** — the blog is the layer only interested readers reach; **§1.4/§1.5** — the honest [M5]
  definition is the core product message and neuroscience knowledge is never required of a reader, which is exactly why
  the depth is _here_ and the summary is _there_.
- [PRD.md](../PRD.md) **§9.1** — the five pillars and their DOIs the `pillar` enum and `sources[]` schema mirror.
- [ARCHITECTURE.md](../ARCHITECTURE.md) **§3.1** — FSD governs `apps/web`/`apps/mobile`; `apps/blog` is a fourth app
  root with its own framework conventions, so no FSD layer/slice rule applies inside it. The one slice this unit adds to
  `apps/web` (`pages/blog-not-found`) does obey §3.1 (a URL lands on a page; `ui` segment only).
- [ARCHITECTURE.md](../ARCHITECTURE.md) **§3.5** — `packages/` is the pure cross-app core; the blog consumes exactly one
  no workspace package at all (`astro` alone), so no shared code is dragged into a non-FSD app.
- [ARCHITECTURE.md](../ARCHITECTURE.md) **§2.9 #1** — one concept, one name across layers: the blog's `pillar` values
  are §9's five pillars, not new coinages.
- [ARCHITECTURE.md](../ARCHITECTURE.md) **§5** — config is build-time (`spec/values.yaml` → `pnpm gen:values`); English
  for code and identifiers, so slugs are English while the prose is Korean content, not i18n messages.
- **Cross-plan constraints:** **this plan owns** the one-Worker `/blog` subpath arrangement — the staged copy into
  `apps/web/dist/blog/`, `base: '/blog'`, the blog's surrender of the origin-root SEO files, the SPA soft-404 and the
  `/blog/$` route that repairs it, and the rejection of a second Worker — plus the trailing-slash policy of the one
  route it introduces; no other plan edits any of them. [81.landing-page](81.landing-page.md) owns the origin:
  `gateDecision`, the public `/`, the origin-root `robots.txt`/`sitemap.xml` this plan's sitemap is referenced from,
  and the correction of the stale "no real deployment yet" lines this plan's grounding depends on.
  [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md) owns the refresh of
  `scripts/lint-ubiquitous-language.mjs`, which is why the [I12] copy gate here is a **new** script rather than an edit
  to that one. The parity matrix makes `blog-site` an explicit **web-only waiver** that must be written into the plan
  because `scripts/lint-fsd-layout.mjs` does not enforce web↔mobile page peering, and values groups are closed with no
  landing/blog group — this unit adds no tuning key (both [00.overview](00.overview.md) §4). Epic Q's row in the
  invariant risk register makes [I12]-reviewed public copy a plan deliverable ([00.overview](00.overview.md) §3).
  Build order is sprint 9's 77 → 78 → 79/80 → 81 → **82** — this unit is last, so the origin already has an owner when
  it lands ([00.overview](00.overview.md) §5.6).
- Sibling plans: **paired with** [81.landing-page](81.landing-page.md) (the link source, the origin owner, the root SEO
  files); **builds on** [14.rendering-foundation](14.rendering-foundation.md) (the `wrangler.jsonc` Workers-assets
  arrangement it extends); **references** [79.demo-page](79.demo-page.md) (the `/demo` link a post points at) and PRD §9
  (the evidence SSOT it elaborates); **consumed by** nothing — the blog is the funnel's leaf.

## Design

### Placement (a fourth app root, plus one FSD page slice)

```
apps/blog/                             Astro 7, static output — outside FSD, outside the i18n gates
├── astro.config.mjs                   site + base:'/blog' + trailingSlash:'always' + build.format:'directory'
├── package.json                       unchanged — "astro" stays the only dependency
├── public/
│   └── og-blog.png                    the one committed OG raster (1200×630); favicon.svg + robots.txt REMOVED
└── src/
    ├── content.config.ts              the collection + its schema (the build-time gate)
    ├── content/posts/<slug>.md        one post per file — 9 migrated from the retired src/blog.md
    ├── layouts/PostLayout.astro       <head>, canonical, OG/Twitter, JSON-LD, skip link, footer
    └── pages/
        ├── index.astro                /blog/            the index
        ├── [...slug].astro            /blog/<slug>/     one post
        ├── rss.xml.ts                 /blog/rss.xml
        └── sitemap.xml.ts             /blog/sitemap.xml

apps/web/src/pages/blog-not-found/     the SPA's blog-shaped miss surface
├── ui/BlogNotFoundPage.tsx
└── index.ts
```

`apps/blog` is listed in `pnpm-workspace.yaml` and already wired into `pnpm check` (`build:blog`) and `pnpm typecheck`
(`typecheck:blog`); this unit adds no new gate wiring for the app itself. Note that `.astro` files have **no Prettier
parser** in this repo (`prettier .` skips them; naming one explicitly errors), so `format:check` does not cover them —
markdown content _is_ covered, and that asymmetry is stated rather than silently relied on.

### The serving arrangement — two builds, one origin, one asset directory

```
pnpm build:site                                                     ← the single Workers Builds command
  1. pnpm --filter @cosimosi/blog build   → apps/blog/dist          (base '/blog' baked into every asset URL)
  2. pnpm --filter @cosimosi/web  build   → apps/web/dist           (vite empties outDir — must run before 3)
  3. node scripts/stage-blog.mjs          → apps/web/dist/blog/**   (copy, then assert index.html + _astro/ exist)
  4. npx wrangler deploy                  → uploads ./apps/web/dist (deploy command, unchanged)
```

Step 2 must precede step 3 because Vite empties `outDir` on build; a copy staged before the web build is silently
deleted. `scripts/stage-blog.mjs` therefore _fails loudly_ if `apps/web/dist` is missing or if the staged tree lacks
`blog/index.html` — the ordering bug becomes a build error instead of a 404 in production. `wrangler.jsonc` changes in
exactly one way: `assets.html_handling` is declared **explicitly** (`"auto-trailing-slash"` — the value that resolves a
directory index for both `/blog` and `/blog/`; confirmed against the Cloudflare assets reference at implement) so the
behavior is pinned rather than inherited from an undeclared default. `assets.directory` and
`assets.not_found_handling` are untouched. `DEPLOY.md` §2 and §5 step 9 are amended to name `pnpm build:site`.

**Rejected: a second Worker with a `…/blog*` zone route.** It costs a second Workers Builds project on the same
repository, a second wrangler config (the dashboard reads only the root `wrangler.jsonc`; a second needs
`-c apps/blog/wrangler.jsonc`), a second build-variable set, and a second deployment to keep in step — for zero benefit
over one copy, since both apps already build in the same pnpm workspace on the same push. **Also rejected: a
`blog.` subdomain** — it splits domain authority and makes every landing↔post link cross-origin, the exact thing [K5]
forbids.

### What breaks today, and how each is fixed

| Today                                                                                                        | Effect under `/blog`                                                                             | Fix                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `astro.config.mjs` has no `base`                                                                             | every page 404s its own `_astro/*.css`                                                           | `base: '/blog'`                                                                                                                                          |
| `index.astro` / `rss.xml.ts` / `sitemap.xml.ts` each hardcode `const siteUrl = 'https://cosimosi.haeram.me'` | canonical, OG and feed URLs point at the app's root, not the post                                | one absolute-origin helper; every internal href built from `import.meta.env.BASE_URL`, never a literal `/`                                               |
| `<a href="/">`, `<a href="/rss.xml">`, `<link rel="icon" href="/favicon.svg">`                               | leave the blog for the app's universe route                                                      | the same helper — `base` does not rewrite authored href strings, so deriving them is the only guard                                                      |
| `public/favicon.svg` (blog) vs `apps/web/public/favicon.svg`                                                 | two files claim `/favicon.svg`                                                                   | delete the blog's; the blog references the origin-root `/favicon.svg` (same origin, so it resolves)                                                      |
| `public/robots.txt` claims `/robots.txt`                                                                     | a `/blog/robots.txt` is ignored by crawlers, and at the root it would fight the app's            | delete the blog's; the origin-root file is [81.landing-page](81.landing-page.md)'s and must carry `Sitemap: https://cosimosi.haeram.me/blog/sitemap.xml` |
| `src/pages/sitemap.xml.ts` claims `/sitemap.xml` and lists one URL (`/`)                                     | wrong scope, wrong URL                                                                           | becomes `/blog/sitemap.xml`, enumerating every non-draft post; the root sitemap references it                                                            |
| `og.svg` used as the OG image                                                                                | SVG is not rendered by the major social crawlers, so shares show nothing                         | one committed `og-blog.png` (1200×630); every post falls back to it                                                                                      |
| `src/blog.md` — nine essays, one page, `#article-N` anchors                                                  | no per-post URL to share, cite, or link a landing card to                                        | nine collection entries at `/blog/<slug>/`; the single page becomes the index                                                                            |
| the footer links `spec/tech/neuroscience.md`                                                                 | the file does not exist, and the relative href resolves to the app origin's `/tech/…` → SPA miss | prose naming PRD §9 as the evidence SSOT, with no repo-internal path in public copy                                                                      |

### The `/blog/**` miss, stated rather than discovered

`assets.not_found_handling: "single-page-application"` returns the app's `index.html` with **HTTP 200** for any path
matching no asset. Consequences, accepted deliberately:

1. A typo under `/blog/**` renders the _app's_ client not-found screen — wrong chrome, no way back to the blog. Fixed
   by a public splat route `path: '/blog/$'` under `rootRoute` (sibling of `/login`, `/test`, `/design`), **outside**
   `authenticatedRoute`, with **no `beforeLoad`** — it must never require a session or the diagnostics flag. It renders
   blog-shaped chrome and a link back to `/blog/`. This route can never shadow a real post: static assets resolve
   first, so the SPA only executes on a genuine miss — a structural guarantee, not a precedence rule to maintain.
2. The status code stays 200, so a crawler sees a soft-404. Mitigations: the blog's sitemap lists only real, non-draft
   URLs, and the not-found route sets `<meta name="robots" content="noindex">` on mount. A true 404 would require a
   Worker _script_ in front of the assets, which this plan's one-Worker, assets-only arrangement pins out — recorded
   here as the honest limitation, not silently absorbed.
3. Trailing slash: the canonical form is **`/blog/`** (Astro `trailingSlash: 'always'` + `build.format: 'directory'`),
   every internal link is authored in that form, and the declared `assets.html_handling` makes the slashless `/blog`
   resolve too. This plan owns the trailing-slash policy for the route it introduces and touches no other route's —
   the origin's is [81.landing-page](81.landing-page.md)'s.
4. The web slice needs **no** steiger exemption block — every shipped block targets `entities|features|widgets` and
   no `pages/*` slice carries one (plan 81 states the rule this plan follows) — and its three strings go through the
   i18n seam — `lint:raw-strings`
   scans `apps/web/src`, so a raw Korean literal there fails the gate. Keys: `blog_not_found_title` ·
   `blog_not_found_description` · `blog_not_found_back_action`, in **both** `en.json` and `ko.json`, followed by
   `pnpm gen:messages` and a committed generated output (`spec/tech/i18n.md` §2). This adds `blog_*` as one more
   surface prefix in the per-surface key scheme without altering any of its rules. **The blog's own content is not
   i18n'd** ([K5], and `spec/tech/i18n.md` §2: "no marketing/blog localization — the Astro blog keeps its own
   pipeline"); only this `apps/web` route is, because of where it lives.

### The content model — one post is one file

`apps/blog/src/content.config.ts` declares a `posts` collection whose schema is the build-time gate:

| Field         | Type                                                                                                                                      | Why it is schema-enforced                                                                                                                      |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `title`       | string                                                                                                                                    | the `<h1>`, the OG title, the index label, the feed item title                                                                                 |
| `description` | string                                                                                                                                    | the meta description and the index/feed summary — authored, never truncated from the body                                                      |
| `pubDate`     | date                                                                                                                                      | index ordering (newest first), feed `pubDate`, sitemap `lastmod`                                                                               |
| `lang`        | `'ko' \| 'en'`                                                                                                                            | drives `<html lang>` and the index chip; **this is the whole of [K5]'s per-language story**                                                    |
| `pillar`      | enum of the five §9 pillars — `engram` · `spatial-representation` · `synapse-time` · `reconstructive-recall` · `forgetting-accessibility` | a landing theory card ([K1] step 5) resolves to `/blog/#<pillar>`, the id the index's group heading carries; an unknown pillar fails the build |
| `sources`     | array of `{ label, doi }`, min 1                                                                                                          | [K2]'s promise that the depth tier _cites_; a theory post with no source cannot ship                                                           |
| `updated`     | date, optional                                                                                                                            | `lastmod` when a post is corrected                                                                                                             |
| `draft`       | boolean, default false                                                                                                                    | excluded from index, feed and sitemap at once — the only publication switch                                                                    |

Routing: `/blog/` (index — non-draft posts newest first, grouped under their pillar under a heading carrying
`id="<pillar>"`, each with a `lang` chip),
`/blog/<slug>/` (the post, `slug` = filename), `/blog/rss.xml` (the newest N non-draft posts, N a module constant in
the endpoint),
`/blog/sitemap.xml` (every non-draft post). The feed endpoint stays hand-rolled — it already is, and a dependency to
emit thirty lines of XML is not earned. Each post's foot shows up to a fixed number of other posts sharing its
`pillar`, plus one restrained CTA pair — `/demo` and `/` — closing the funnel the landing page opens.

`<head>` per post: `<link rel="canonical">` at the absolute `/blog/<slug>/` URL, OG/Twitter tags with the absolute
`og-blog.png`, `<link rel="alternate" type="application/rss+xml">` at `/blog/rss.xml`, and `BlogPosting` JSON-LD whose
`citation` entries come from `sources[]` — so the DOIs the schema requires are machine-readable too, which is what a
theory-depth tier should look like to a crawler.

### Content migration and the [I3]/[I12] correction

`src/blog.md`'s nine `##` sections become nine files and `src/blog.md` is retired. Slugs are English kebab-case (the URL
is code, [ARCHITECTURE.md](../ARCHITECTURE.md) §5) while the prose stays Korean with `lang: ko`. The existing per-post
"Description(작성자·심화 학습용)" block splits: the 논문·DOI list becomes structured `sources[]` rendered as a citations
section; the "LLM에 물어보기" prompts stay body content, since inviting a reader to interrogate the theory is exactly the
depth tier's job.

Two corrections are conditions of publication, not follow-ups:

- **Post 8 is rewritten.** The claims "감정 좌표가 가까운 별끼리 살짝 더 단단하게 묶습니다", "비슷한 감정의 기억끼리
  끌어당기는 거예요" and "강렬했던 기억일수록 오래, **가까이**, 밝게 남아요" describe emotion driving links and
  distance, and memories attracting one another — forbidden by [I3], [I4] and [I7]. The rewrite keeps the science
  (Russell's circumplex; mood-congruent recall; McGaugh's amygdala modulation) and states what cosimosi actually does:
  valence becomes **color**, arousal modulates **strength and the forgetting rate**, links emerge from **shared neurons
  and synapse strength** with emotion excluded, and the radius from center is **self-relevance** ([I7]) — never
  recency, never emotion. Mood-congruent recall is then described honestly as a phenomenon the product does _not_
  simulate.
- **The footer's `spec/tech/neuroscience.md` link is removed** — the file does not exist, the relative href would
  resolve against the app origin, and repo-internal paths do not belong in public copy. PRD §9 is named as the
  requirement↔evidence SSOT in prose instead.

### The [I12] copy gate

`scripts/lint-public-copy.mjs`, added to the root `lint` chain, scans a declared root list — `apps/blog/src/content/**`
in this unit — and fails on: brain-equivalence claims ("뇌처럼 작동", "works like a brain", "뇌와 동일"), therapeutic or
efficacy claims (치료 · 치유 · 효능 · therapy · cure · clinically), any sentence presenting the 3D coordinates as the
brain's real coordinates, and the mechanics families above (emotion→position/link, memory↔memory attraction,
radius-as-recency). It is deliberately a **new** script: `scripts/lint-ubiquitous-language.mjs` already scans
`apps/blog/src`, but [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md) owns its refresh, and two plans
editing one gate in one sprint is exactly the collision the sprint's single-owner assignments prevent
([00.overview](00.overview.md) §5.6). The root list is data, so the landing's message catalogue can be added later
without re-deciding anything here. The gate is additive to — not a replacement for — the invariant risk register's
requirement that Epic Q copy be reviewed against [I12] as a plan deliverable ([00.overview](00.overview.md) §3).

### Parity waiver

`blog-site` ships **web only**. A native app has no marketing or long-form-article route, and Astro has no React Native
analogue. The waiver is recorded here in full because `scripts/lint-fsd-layout.mjs` does **not** enforce web↔mobile page
peering — an unstated waiver is silently broken rather than caught — following the admin-console precedent
(`spec/policy/ops/admin.md` §6: "the web↔mobile parity rule is deliberately waived for it"). The one `apps/web` slice
this unit adds (`pages/blog-not-found`) is likewise web-only: it repairs a _URL_ miss, and mobile has no URLs.

### Golden-parity stance

**Zero parity obligation, guaranteed by dependency shape rather than by discipline.** This unit re-implements no pure
function, mirrors none, and reads no domain value: it renders authored prose. Its only workspace dependency is
`@cosimosi/config` (generated `spec/values.yaml` constants), itself a generated artifact whose freshness
`pnpm check:gen` already gates. It imports none of the golden-parity-pinned packages (`@cosimosi/memory-logic`,
`@cosimosi/twinkle-logic`, `packages/emotion`, `packages/force-sim`) and adds no fixture, constant or formula — so
`scripts/lint-fsd-layout.mjs` R4 (byte-identical pure modules) has nothing to catch here. Where the prose _describes_ a
shipped formula, no number is restated: the post describes the behavior and the authoritative figures stay in
`spec/values.yaml` and PRD §9. The real risk in a copy unit is not numeric drift but **semantic drift** — prose
describing mechanics the code does not have — and that is what the [I12] gate above guards.
[77.demo-fixtures-and-mirror](77.demo-fixtures-and-mirror.md)'s TypeScript mirrors are irrelevant here; the blog never
runs domain logic.

## Acceptance Criteria

1. `https://cosimosi.haeram.me/blog/` serves the Astro-built blog index from the **same origin** as the app, with no
   subdomain, no second Worker, no zone route and no DNS change; `wrangler.jsonc` still declares one
   `assets.directory` of `./apps/web/dist` ([K5]).
2. One root script (`pnpm build:site`) builds the blog, builds the web app, and stages `apps/blog/dist` into
   `apps/web/dist/blog/` **in that order**; the staging step fails the build if `apps/web/dist` is absent or if
   `blog/index.html` or `blog/_astro/` is missing after the copy. `DEPLOY.md` §2 and §5 name that script.
3. Every blog page loads its own CSS and JS from `/blog/_astro/*` — `base: '/blog'` is set — and **no** authored
   internal href is an origin-root literal: every internal URL derives from `import.meta.env.BASE_URL` or the single
   absolute-origin helper ([K5]).
4. The blog build emits **no** `robots.txt` and **no** `favicon.svg`; the origin root owns both. The blog's sitemap is
   served at `/blog/sitemap.xml` and lists every non-draft post; the origin-root `robots.txt`
   ([81.landing-page](81.landing-page.md)) names it.
5. `/blog` and `/blog/` both resolve to the index (canonical form `/blog/`, pinned by Astro `trailingSlash: 'always'`
   plus `build.format: 'directory'` and by an explicitly declared `assets.html_handling`); no product route's
   trailing-slash behavior changes.
6. A miss under `/blog/**` renders **blog-shaped** not-found chrome with a link back to `/blog/`, from a public
   `/blog/$` route under `rootRoute` with no `beforeLoad` and no session or diagnostics requirement, and it sets
   `robots: noindex`. Its three strings resolve through the i18n seam in both `en.json` and `ko.json`, and
   `lint:raw-strings` passes.
7. Each post is one file at `apps/blog/src/content/posts/<slug>.md` served at `/blog/<slug>/`; the nine essays formerly
   in `src/blog.md` exist as nine posts and `src/blog.md` is gone ([K5]).
8. The collection schema **fails the build** when a post omits `title`, `description`, `pubDate`, `lang`, a `pillar`
   from the closed five-pillar set, or at least one `{ label, doi }` source — so a theory post cannot ship uncited
   ([K2], PRD §9.1).
9. `draft: true` excludes a post from the index, `/blog/rss.xml` and `/blog/sitemap.xml` simultaneously — one switch,
   three surfaces.
10. Every post's `<head>` carries an absolute canonical URL under `/blog/`, OG/Twitter tags whose image is a **raster**
    (`og-blog.png`, 1200×630), an RSS `alternate` link, and `BlogPosting` JSON-LD whose `citation` entries come from
    `sources[]` ([K5]).
11. **No public sentence claims the product works like a brain, claims a therapeutic or clinical effect, or presents
    the 3D coordinates as the brain's real coordinates**; the framing is "inspired by engram theory" throughout, and
    `scripts/lint-public-copy.mjs` runs inside `pnpm lint` and fails on each of those classes ([I12], the Epic Q
    guard row of [00.overview](00.overview.md) §3).
12. **No public sentence describes emotion influencing a star's position or a link's strength, memories attracting one
    another, or the radius meaning recency**; post 8 is rewritten so valence → color, arousal → strength and
    forgetting, links → shared neurons + synapse strength (emotion excluded), radius → self-relevance
    ([I3][I4][I7][I12]).
13. No public copy links a repository-internal path; the `spec/tech/neuroscience.md` reference is replaced by prose
    naming PRD §9 as the requirement↔evidence SSOT ([K5]).
14. The `pillar` enum is exactly `engram` · `spatial-representation` · `synapse-time` · `reconstructive-recall` ·
    `forgetting-accessibility`, the index emits one `id="<pillar>"` heading per group, so the landing page's five
    theory cards each resolve to a live `/blog/#<pillar>` anchor via the `pillar` enum, and the
    landing itself lists **no** academic citation — the DOIs live only on the blog ([K2][K1]).
15. `apps/blog` depends on **no** `@cosimosi/*` package at all (`astro` stays its only dependency), imports no
    transport client, no Supabase client and no domain logic, and defines no pure function; its three build constants
    are named module constants in the Astro app (no values key, no group — the closed-groups rule), each carrying a
    comment stating what it tunes.
16. `pnpm lint`, `pnpm typecheck:blog`, `pnpm build:blog`, `pnpm build:site` and `pnpm check:gen` all pass, and the
    web-only parity waiver for `blog-site` is written into the as-built docs with its admin-console citation.

## Policy / Values Impact

- **policy / ux:** the rules this unit makes true **extend** `spec/policy/ux/public-copy.md`, the doc
  [81.landing-page](81.landing-page.md) creates one unit earlier in Epic Q; this unit adds the blog-facing sections and
  creates no file (written at implement time; do **not** pre-create or pre-edit):
  - **Two theory tiers, never mixed** — the landing carries the lay summary with no citations; the blog carries the
    depth with at least one cited source per post ([K2]).
  - **The forbidden claim classes** are [81.landing-page](81.landing-page.md)'s five, not a second list; this unit only
    sharpens the _described mechanics the code does not have_ class with the blog's concrete cases
    (emotion→position/link, memory↔memory attraction, radius-as-recency) and narrows the citations class to "at least
    one cited source per post" ([I12][I3][I4][I7]).
  - **One origin** — public surfaces live on the product origin; the blog is a subpath, never a subdomain, and
    landing↔post links are same-origin ([K5]).
  - **Posts are not localized; a translation is a separate post** with its own slug and `lang` ([K5]).
  - **No repository-internal path in public copy**; PRD §9 is cited by name as the evidence SSOT.
- **tech:** the as-built serving arrangement (the `build:site` sequence and its ordering constraint, the staged copy and
  its assertions, `base`/`trailingSlash`/`html_handling`, root-SEO ownership, the `/blog/$` splat and the soft-404
  limitation, the collection schema and the `pillar` enum, the copy gate's root list) is recorded at implement time in a
  new `spec/tech/blog-site.md`, with the `/blog/$` route registration also reflected into
  [tech/web-routing.md](../tech/web-routing.md) §7–§9 (whose §9 currently states there is no marketing route).
  `DEPLOY.md` §2 and §5 step 9 are amended to name `pnpm build:site`. Do **not** pre-create any of these.
- **spec/values.yaml** — **this unit declares no key and adds no group**: groups are closed and v2's enumeration has no
  landing/blog group, because neither surface holds a tuning number ([00.overview](00.overview.md) §4). The three
  build constants the pages need — the feed's item ceiling, the related-post count, and the sitemap `<priority>`
  (the shipped endpoint hardcodes `0.8`) — stay module constants in
  the Astro app beside the endpoints that read them (`src/pages/rss.xml.ts`, `src/pages/sitemap.xml.ts`, the post
  layout). They are static-site-generator build constants, not product tuning: nothing outside this app can move
  behind them, and `apps/blog/package.json` depends on `astro` alone — routing the generated `@cosimosi/config`
  values into it would add a workspace dependency to a deliberately dependency-minimal build for three numbers no
  other surface reads.

  **Excluded from values.yaml, deliberately:** the three build constants above; the `/blog` base path, the origin, and
  every URL (addresses and env, not
  tuning); the five `pillar` values and the `lang` set (enum _content_, and their counts derive from it); post
  frontmatter and the collection schema (a contract, like proto or DB schema); the OG raster's 1200×630 (a fixed
  external platform spec, not a knob); `changefreq` and every other string; the blog's CSS custom properties,
  breakpoints and type scale (presentation content authored in the Astro app, the treatment
  [56.2d-ui-design-language](56.2d-ui-design-language.md) gives theme values); and the not-found route's copy (i18n
  content).

- **Dependencies:** builds on [81.landing-page](81.landing-page.md) (the origin owner and link source — it supplies the
  root `robots.txt`/`sitemap.xml` and the five theory cards this plan's `pillar` enum answers to, and it corrects the
  stale "no real deployment yet" lines this plan's grounding depends on) and on
  [14.rendering-foundation](14.rendering-foundation.md) (the `wrangler.jsonc` Workers-assets arrangement this extends);
  references [79.demo-page](79.demo-page.md) (the `/demo` link posts point at) and PRD §9 (the evidence SSOT the depth
  tier elaborates). **Consumed by** nothing — the blog is the funnel's leaf: entered from the landing page, exiting to
  `/demo` or `/`.

## As built — what moved

- **`build.inlineStylesheets: 'never'`** was added, which the plan did not anticipate. Astro inlines a small stylesheet by
  default, and with everything inlined there is no `_astro/*` request left — so `base` is never exercised and the staged
  assertion has nothing to assert. Emitting the file makes the guard real, and one cacheable stylesheet beats the same
  6 KB duplicated into ten pages.
- **A `BaseLayout.astro` sits under `PostLayout.astro`.** The head, the header, the footer and the one stylesheet are
  shared by the index and every post rather than duplicated; `PostLayout` adds the article chrome, the citations and the
  JSON-LD. `src/site.ts` is the single URL helper the plan asked for.
- **The forgetting post needed the same correction as the emotion post.** The plan named post 8 as the condition of
  publication; post 4 also said a star sits near the centre when it is "often recalled, emotionally deep and well
  connected". Emotional depth does not enter the radius — it is connectivity, never emotion and never recency — so that
  clause is gone too. Two rewrites, not one.
- **The copy gate's negation rule was a correction, not a design.** Its first version flagged every denial, including
  "기억끼리 서로를 끌어당기지 않습니다" — the exact sentence the rule exists to produce. Its own `--probe` caught it. A
  match now counts only when the clause it starts is not negated, and the window is deliberately **after** the match:
  Korean negation is post-verbal, so a tail check catches every honest denial without the hole a whole-sentence check has
  (an English-style "Not just a diary — it works like your brain" puts the negation before the claim).
- **Two sources were dropped rather than substituted.** The migrated essays cited a book chapter (Bjork & Bjork 1992) and
  a 1949 monograph (Hebb), neither of which has a DOI the schema will accept. The first instinct was to swap in a
  DOI-bearing paper on the same ground — and that was wrong, because a DOI written from memory is exactly the kind of
  citation a page about source honesty must not carry. Both entries are gone from the lists; the remaining sources on those
  two posts are the DOI-bearing ones the essays already had, and the prose still credits Hebb and the
  storage-versus-retrieval distinction **by name**. The schema governs the citation list, not the argument.
- **One prompt lost its clinical framing.** The migrated post 3 asked about reconsolidation "applied to PTSD treatment" — a
  question rather than a claim, but the therapeutic class cannot tell the difference and should not try to. It now asks
  about clinical research generally, with no condition named. The rule is blunt on purpose.
