# 66. earn and purchase spend

> Earn and purchase spend — the U-layer of the single currency ([G2a]): the earn set becomes daily grant · write ·
> achievement · invite · signup bonus with **payment retired to v3**, and `Spend` grows a third purpose — ornament
> purchase, `GENERAL` only. This unit is the **sole owner of the `EntryReason` closed set and of every
> `twinkle_ledger_entries` DDL change** ([00.overview](00.overview.md) §5.6), so purchase, claim, earn and spend all
> land on the one append-only ledger ([G7]) and the `/me` history has a single source. It leaves the tier _rename_ and
> the pure `PlanSpend` signature to [65](65.twinkle-kind-redefinition.md) and every pixel to
> [67](67.twinkle-relabel-and-ledger-ui.md).
> Scope: BE | Status: built (job 101)
>
> Layer U. Home: the shipped `apps/api/internal/twinkle` context (`ledger.go`, `service.go`, `ports.go`, `pg/store.go`,
> `rpc/`) + `proto/cosimosi/twinkle/v1/twinkle.proto` + migration
> `apps/api/db/migrations/00019_twinkle_purchase_spend.sql`. It fills the purchase seam that
> [72.decorate-usecase](72.decorate-usecase.md) and the reward seam that [75](75.achievement-tracking-usecase.md) call
> through their own ports, and closes the payment seam [44](44.earn-spend-usecase.md) left open.

## Purpose

v1 shipped Twinkle as a _recall_ currency: two tiers, two spend purposes (회고, 요지 열람), three earn paths (write,
invite, verified payment) — [44.earn-spend-usecase](44.earn-spend-usecase.md). v2 promotes it to the product's **only**
currency ([G2a]): the decoration panel pays for backgrounds, star shaders and palettes in the same unit ([P9]), and
achievements pay _into_ it ([A3]). Two things must happen in one place before either sibling can be built. First, the
spend surface must accept a purpose whose price is **not** a cost curve but a catalog total owned by another context.
Second, the earn surface must grow the two paths v1 never had — achievement reward ([A3]) and signup bonus ([U11]) —
while the one path v1 _did_ have and cannot keep is removed.

That removal is the second half of this unit's reason to exist. PRD §8.3 defers payment (스토어/PG) to v3 and [G3] names
the funding set that replaces it: 매일 지급 · 일기 작성 · 업적 · 친구 초대 · 가입 보너스. Today the payment path is a
guaranteed-fail affordance on both sides — `cmd/api/twinkle.go` binds `twinkle.UnavailablePaymentVerifier{}` in
production and the client's store purchase always throws — so shipping v2 with `Charge` reachable ships a broken button
and an error-recovery flow that points nowhere. v2 retires it, and this plan executes the server half exhaustively:
the RPC, the use-case, the port, the five errors, the four transport reasons and `twinkle.charge_pack` all go away,
while the `payment` `EntryReason` constant and migration `00010`'s uniqueness index stay so historical rows remain
readable and foldable.

Third, [G7] demands that 구매·수령·적립·소모 all be readable chronologically in `/me` ([U9]). The ledger already records
them; nothing exposes them. This unit adds the one read — `GetLedger`† — with a keyset cursor, and pins what a ledger
row is allowed to say on the wire. The daily `SMALL` refill deliberately has **no row**: it stays a derivation,
and the wire enum has no member for it, so no client can mistake the refill for a transaction.

## Scope / Non-goals

**In scope**

- **The `EntryReason` closed set, in full** ([G3][G1][G7]) — `daily_grant` (reserved, never written), `write_diary`,
  `invite`, `invite_signup`, `signup_bonus`, `achievement_claim`, `admin_grant`, `recall`, `gist_view`,
  `ornament_purchase`, `payment` (retained, no write path). No other plan may add a reason. One narrow carve-out,
  already taken: [61.signup-and-invite-usecase](61.signup-and-invite-usecase.md) lands `ReasonSignupBonus` +
  `EarnSignupBonus` ahead of this unit because [U11] is its requirement and the sprint order puts it first — this unit
  folds that constant into the single `ledger.go` const block and owns the set from then on
  ([00.overview](00.overview.md) §5.6).
- **`Spend` extended to ornament purchase** ([G4].3, [P9]) — a purchase-shaped `SpendIntent` whose amount is the
  caller's authoritative catalog total, drawn from `GENERAL` only, appended as an `ornament_purchase` spend row inside
  the caller's transaction.
- **Migration `00019_twinkle_purchase_spend.sql`** ([00.overview](00.overview.md) §5.6) — the
  `CHECK (reason NOT IN ('ornament_purchase') OR from_basic = 0)` that makes [P9] true in the schema, plus the
  reason-comment refresh on `twinkle_ledger_entries`.
- **Two new earn entry points** — `EarnAchievementReward(scope, claimID, amount)` keyed `achievement:<claimID>` ([A3])
  and `EarnSignupBonus(scope)` for `twinkle.earn_signup_bonus` keyed `signup_bonus:<userID>` ([U11]).
- **The invitee reason split** — `ClaimInvite` credits the inviter as `invite` and the invitee as `invite_signup`;
  today both sides write `invite`.
- **Payment retirement, server half** — every Go/proto/values symbol enumerated in _Design → Payment
  retirement_.
- **`GetLedger`†** ([G7][U9]) — keyset-paginated chronological ledger read, `twinkle.ledger_page_size`, plus its single
  classification in `packages/client-cache/src/http-policy.ts`.
- **The `SMALL` day-boundary seam** ([U7]) — a consumer-owned `UserZoneReader` port bound at `cmd/api` so the
  pure tier derivation gets the user's IANA zone instead of UTC.
- **Tier-aware insufficiency** — a typed denial carrying `{cost, eligible, shortfall}` so [72] can render [P8]'s "어떤
  아이템이 모자란지" without twinkle knowing what an ornament is.

**Non-goals**

- **The `SMALL`/`GENERAL` rename and the pure ledger math** — `Balance{Small,General}`,
  `SpendPlan{FromSmall,FromGeneral}`, `SmallRemaining`, `PlanSpend`'s purpose parameter, the TS mirror in
  `packages/twinkle-logic`, the golden fixture, the `twinkle.small_daily_amount` key rename and the
  `forbiddenSynonyms` refresh are [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md). This plan
  consumes them and writes no pure function.
- **Every pixel** — the balance labels, the `/me` stardust tab, the ledger list, the synthetic daily-refill marker, the
  removal of the pay `<section>`, the i18n renames and the `TWINKLE_INSUFFICIENT` recovery re-target are
  [67.twinkle-relabel-and-ledger-ui](67.twinkle-relabel-and-ledger-ui.md).
- **Ornament catalog, prices and the purchase transaction** — the `store` context, `ornament_ownerships` /
  `ornament_selections`, the price table, `Decorate`'s transaction and `STORE_INSUFFICIENT_TWINKLE` are
  [71](71.ornament-catalog-model.md) / [72](72.decorate-usecase.md). This unit supplies only
  the debit.
- **Achievement conditions, progress and claim** — [74](74.achievement-catalog-model.md) /
  [75](75.achievement-tracking-usecase.md) own `achievement_progress`, `claim_id`, the reward tiers and the
  `ClaimAchievement` transaction.
- **Invite settlement policy** — the four-part valid-signup conjunction, `invites.rewarded_at`, the per-inviter cap
  `twinkle.invite_reward_max_per_inviter` and the concrete `InviteResolver` are
  [61.signup-and-invite-usecase](61.signup-and-invite-usecase.md). This unit keeps the shipped seam and the
  credit primitive; it decides no eligibility.
- **`users` / `users.timezone`** — [60.account-profile-model](60.account-profile-model.md). This unit reads the
  zone through a port.
- **No payment anything in v2** — no provider adapter, no pack catalog, no receipt verification, no flagged or gated
  code path. Removed, not disabled (PRD §8.3).
- **No cron, no scheduled grant job** — the daily `SMALL` refill stays a derivation ([T4]).

## Grounding

- **Constitution** ([I1]–[I13]): [00.overview](00.overview.md) §3 (_The constitution_), owned by [PRD.md](../PRD.md) §7.
  Most at risk here, each with the structural guard the v2 risk register assigns it:
  - **[I1]** the system never deletes — the ledger is append-only. Guard: this unit adds no `UPDATE`/`DELETE` statement
    to `db/queries/twinkle/ledger.sql`; the only write remains `AppendTwinkleLedgerEntry … ON CONFLICT DO NOTHING`.
  - **[I11]** decoration must not touch meaning — a purchase debit is a scalar. Guard: the purchase `SpendIntent`
    carries an amount and a dedup key and **has no ornament id, kind, color or render field**; twinkle imports no
    `store` type, so an ornament cannot reach the ledger.
  - **[G7] / [I1] spirit** — one ledger, no unledgered event. Guard: the wire enum `LedgerEntryReason` has **no
    `DAILY_GRANT` member**, so the economy's one non-event is unrepresentable both as a row and as a wire value.
  - **[P9]** `SMALL` may not buy ornaments. Guard: `PlanSpend`'s purpose argument (65) returns `FromSmall: 0` for a
    purchase **and** migration `00019`'s `CHECK (reason NOT IN ('ornament_purchase') OR from_basic = 0)` — prose in
    neither place.
  - **[I10]** time flows forward — the `SMALL` window anchor advances with `GREATEST` only, so neither a timezone edit
    nor a fallback zone can mint a second grant.
- [PRD.md](../PRD.md) **§5.9 [G2a][G3][G4][G7]** — [G2a]: 별가루는 유일한 화폐 단위, every future paid feature uses
  `GENERAL` rather than a second currency; [G3]: the five earn paths, "결제는 v2 범위가 아니다 — v3에서 붙인다",
  "접속(로그인) 보너스는 없음"; [G4]: the three spend purposes plus the [D3] diary jump priced as the sum of its
  per-memory recalls; [G7]: "구매·수령·적립·소모가 모두 같은 원장에 남아 마이페이지 별가루 탭에서 시간순으로 열람
  가능".
- [PRD.md](../PRD.md) **§5.11 [P9]**, **§5.10 [A3]**, **§4 [U11][U7]** — [P9]: 오너먼트는 `GENERAL` 별가루로만, 영구
  소유, 환불·기간제·재구매 없음; [A3]: rewards are `GENERAL`, "`SMALL` 별가루를 보상으로 주지 않는다"; [U11]: a one-time
  `GENERAL` signup bonus whose amount is a value, explicitly distinct from an attendance bonus; [U7]: the timezone is
  the `SMALL` reset boundary.
- [ARCHITECTURE.md](../ARCHITECTURE.md) **§2.2** (contexts integrate through published behavior — `store` and
  `achievement` reach the ledger through their own ports, never `twinkle_ledger_entries`), **§2.3** (`internal/twinkle`
  stays the one-package shape; `{pg,rpc}` only), **§2.4** (ports declared by the consumer; the composition root
  injects; proto/sqlc map at the boundary), **§2.6** (static sqlc for the ledger page), **§2.7** (unary only;
  `NO_SIDE_EFFECTS` classified exactly once; the production transport fails before I/O on a missing classification),
  **§2.9** #1 (no cross-layer rename), #4, #6, #7 (thin handlers), #8 (the client never re-derives a debit).
- **Cross-plan constraints:** this plan is the **sole owner** of the closed `twinkle.EntryReason` set and of every
  `twinkle_ledger_entries` DDL change — [72.decorate-usecase](72.decorate-usecase.md) and
  [75.achievement-tracking-usecase](75.achievement-tracking-usecase.md) consume the set and may not add a reason —
  and it claims migration `00019` from the plan-order allocation that starts at `00016`
  ([00.overview](00.overview.md) §5.6, which also fixes the build order 60 → 65 → **66** → 67). It is likewise the
  only unit touching the `cosimosi.twinkle.v1` wire in v2: `GetLedger`† is added, `Charge` is removed, `ClaimInvite`
  leaves the wire, no per-user request carries a `user_id` (scope comes from the authenticated `platform.UserScope`),
  and pagination copies the shipped keyset-cursor shape rather than admin's `LIMIT/OFFSET`. Every `NO_SIDE_EFFECTS`
  method is a two-sided change — the proto option plus exactly **one** `packages/client-cache/src/http-policy.ts`
  classification, which the production transport hard-fails on before I/O. Cross-context integration is
  composition-root adapters over consumer-owned ports: there is no event bus in the repo and v2 adds none. The
  `SMALL`/`GENERAL` rename, the purpose restriction (`SMALL` pays for the recall family only, `PlanSpend` takes the
  purpose) and the persisted column spelling that stays `additional`/`basic_*`/`from_basic`/`from_additional` all
  belong to [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md); the DB half of [P9]'s guard — the
  `ornament_purchase` CHECK — is this plan's. The daily `SMALL` grant stays a derivation with no ledger row and no
  cron, and [67.twinkle-relabel-and-ledger-ui](67.twinkle-relabel-and-ledger-ui.md) renders it as a labelled marker.
  `users.timezone` is the single day-boundary authority and belongs to
  [60.account-profile-model](60.account-profile-model.md), read here through a port; real time may **pace** a grant
  but is never **measured** as a condition, the structural half of that rule being
  [74.achievement-catalog-model](74.achievement-catalog-model.md)'s clock-less service. Payment is deferred to v3
  (PRD §8.3): this unit removes the server half exhaustively and
  [67.twinkle-relabel-and-ledger-ui](67.twinkle-relabel-and-ledger-ui.md) the client half, and 67 — which lands last
  — also owns the i18n renames and deletes `twinkle.charge_pack` once this unit's Go reader is gone. Invite
  settlement policy — the four-part valid-signup conjunction, `invites.rewarded_at`, the per-inviter cap and the
  concrete `InviteResolver` bound in `cmd/api/account.go` — is
  [61.signup-and-invite-usecase](61.signup-and-invite-usecase.md), which also declares
  `twinkle.earn_signup_bonus` and `twinkle.invite_reward_max_per_inviter`; the shipped `InviteResolver` port is
  reused as-is and this unit designs no new seam. The achievement reward is an idempotent pairing keyed by
  `claim_id`, never a cross-context transaction, and its `ClaimAchievement` half is
  [75.achievement-tracking-usecase](75.achievement-tracking-usecase.md); the purchase debit's single cross-context
  leg is `store.SpendGate`, called from inside `Decorate`'s own transaction by
  [72.decorate-usecase](72.decorate-usecase.md). Catalogs are code — the ornament price table and the achievement
  reward table are context content, not tuning numbers — and the only key this unit adds,
  `twinkle.ledger_page_size`, goes into the **existing** `twinkle` group: groups are closed, keys inside them are
  not, per the values-group closure rule in [00.overview](00.overview.md) §4, which also records that a headless
  unit like this one carries no web↔mobile parity question. Every new statement is genuinely user-scoped and the
  migration is `000NN_<snake_case_topic>.sql` with both `-- +goose Up` and `-- +goose Down`. The invariant risk
  register naming the guards above is [00.overview](00.overview.md) §3.
- **Sibling plans** — **builds on** [43.stardust-ledger](43.stardust-ledger.md) (the ledger tables, the derivation, the
  cost curves), [44.earn-spend-usecase](44.earn-spend-usecase.md) (the shipped real `SpendGate` / `EarnPort` /
  `QuoteSpend` this extends), [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md) (the tier vocabulary and
  `PlanSpend`'s purpose argument), [60.account-profile-model](60.account-profile-model.md) (`users.timezone`).
  **Paired with** [61.signup-and-invite-usecase](61.signup-and-invite-usecase.md) (calls the invite and signup-bonus
  credits). **Consumed by** [67.twinkle-relabel-and-ledger-ui](67.twinkle-relabel-and-ledger-ui.md) (`GetLedger`,
  `GetBalance`), [72.decorate-usecase](72.decorate-usecase.md) (the purchase debit),
  [75.achievement-tracking-usecase](75.achievement-tracking-usecase.md) (the reward credit).

## Design

### Placement (domain-first Go, ARCHITECTURE §2.3 — no new context)

Everything lands in the **shipped** `apps/api/internal/twinkle` package: `ledger.go` (the reason set — a domain type),
`service.go` (the use-cases), `ports.go` (consumer-owned ports), `pg/store.go` + `db/queries/twinkle/ledger.sql` (the
page read), `rpc/{server.go,reasons.go}` (thin handlers). No `app/` or `domain/` split is earned (§2.9 #2) and no new
context is created — a purchase debit is twinkle behavior, not store behavior. `twinkle` imports neither `memory`,
`store` nor `achievement`; every cross-context edge stays a composition-root adapter over a consumer-owned port, in
`cmd/api/twinkle.go` (memory) and — bound by their owning plans — `cmd/api/store.go` (72) and
`cmd/api/achievement.go` (75).

### The `EntryReason` closed set (sole ownership)

`twinkle.EntryReason` is exactly the eleven values enumerated in _Scope / Non-goals_, in one const block in
`ledger.go`. Two properties make the set
enforceable rather than merely documented:

1. **Every writable reason has exactly one named use-case entry point, and the unwritable ones have none.** There is no
   exported `Earn(reason, amount)`; the surface is `EarnOnWrite`, `ClaimInvite`, `EarnSignupBonus`,
   `EarnAchievementReward`, `EarnAdminGrant`, `CheckAndSpend`. `daily_grant` and `payment` therefore have **no caller
   at all** — the guard is the absent method, not a validation branch.
2. **The set is asserted whole.** The shipped `TestEarnReasonsAreAClosedSetWithNoLoginBonus`
   (`internal/twinkle/service_test.go`) is extended to enumerate all eleven, assert each reason's earn/spend
   classification, assert no reason smells of login/attendance ([G3]) and assert `daily_grant`/`payment` are reachable
   from no entry point. A plan that adds a twelfth reason fails this test.

**No PG `CHECK` on the `reason` column and no PG enum.** The shipped DDL comment already declares `kind`/`reason` TEXT
closed sets "owned by the domain (like neuron_type / jobs.kind)". A closed-set CHECK on an append-only ledger costs a
migration per reason and risks failing against historical rows; the domain owns the set, the schema owns the
_arithmetic_ invariants. The one reason-shaped CHECK this unit does add is [P9]'s, because that one guards money.

Classification helpers live with the set: `EntryReason.IsSpend()` and `EntryReason.SmallEligible()` — the latter true
for `recall` and `gist_view` only (the recall family; a `DIARY_RECALL` action writes one `recall` row per member, so
it needs no reason of its own). `SmallEligible` is the single place the eligibility question is answered, and it feeds
`PlanSpend`'s purpose argument.

### `Spend` grows a third purpose ([G4].3 [P9])

`SpendIntent` becomes an **opaque value with unexported fields**, built by three constructors, so a caller physically
cannot mix a purchase amount with a curve signal:

```go
// internal/twinkle/service.go — fields unexported; these three are the only way to build one.
func RecallSpendIntent(accessibilityCost float64, dedupKey string) SpendIntent   // reason = recall
func GistViewSpendIntent(semanticStage int, dedupKey string) SpendIntent         // reason = gist_view
func PurchaseSpendIntent(amount int, dedupKey string) SpendIntent                // reason = ornament_purchase
```

A recall intent has no field for an amount (so a caller can never set a recall's own price — [CC3] survives) and a
purchase intent has no field for a decay depth or a gist stage (so the curve can never price a purchase). `spendPrice`
gains one arm: `ornament_purchase` → the intent's amount, refused when non-positive (`ErrPurchaseAmountInvalid`). The
ornament price table stays `store` content; twinkle is told the total and never learns what was bought.

`CheckAndSpend(ctx, scope, ledger LedgerStore, intent SpendIntent)` keeps its shape — the `LedgerStore` argument is
still the caller's transaction-bound store, so [72]'s `Decorate` gets the same "one transaction or nothing" guarantee
recall has: `cmd/api/store.go` type-asserts `interface{ DB() dbgen.DBTX }` and binds `twinklepg.NewStore` onto the same
pgx transaction. The tier draw comes from 65's `PlanSpend(smallRemaining, general, cost, purpose)` with
`purpose` derived from `intent.reason.SmallEligible()` — a purchase yields `FromSmall: 0`, and migration `00019`
refuses the row if it ever did not.

**Insufficiency becomes typed.** `CheckAndSpend` returns `*InsufficientTwinkle{Cost, Eligible, Shortfall}` — an error
wrapping the canonical `ErrInsufficientTwinkle`, so every existing `errors.Is` site keeps working — where `Eligible` is
the balance actually usable for this purpose (`general` for a purchase, `small + general` for the recall family) and
`Shortfall = Cost - Eligible`. This replaces `QuoteSpend`'s shipped `shortfall = cost - balance.Basic -
balance.Additional`, which is wrong for any non-recall purpose. The shortfall reaches the client through
`apperr.Domain(code, reason, err, metadata)`'s shipped `map[string]string` detail channel; [72] maps it onto
`STORE_INSUFFICIENT_TWINKLE` with the item name it alone knows, and [67] re-targets `TWINKLE_INSUFFICIENT`'s recovery.

**`QuoteSpend` gains no purchase kind.** `QuoteKind` stays `recall|gist_view|diary_recall` and `QuoteSpendRequest`
gains no ornament field. A purchase is previewed client-side from [71]'s catalog mirror plus `GetBalance`; routing it
through a server quote would publish the price table on a second contract and hand `/demo` a price string ([Z8]).

### Earn: the five paths of [G3], and the one that leaves

| Reason              | Entry point                | Amount                                      | Dedup key                  | Transaction            |
| ------------------- | -------------------------- | ------------------------------------------- | -------------------------- | ---------------------- |
| `daily_grant`       | **none**                   | `twinkle.small_daily_amount` (65)           | —                          | derived, never written |
| `write_diary`       | `EarnOnWrite` (shipped)    | `twinkle.earn_write`                        | `write_diary:<diaryID>`    | memory's launch tx     |
| `invite`            | `ClaimInvite`, inviter leg | `twinkle.earn_invite_inviter`               | `invite:<signupID>`        | own ledger tx          |
| `invite_signup`     | `ClaimInvite`, invitee leg | `twinkle.earn_invite_invitee`               | `invite_signup:<signupID>` | own ledger tx          |
| `signup_bonus`      | `EarnSignupBonus`          | `twinkle.earn_signup_bonus`                 | `signup_bonus:<userID>`    | own ledger tx          |
| `achievement_claim` | `EarnAchievementReward`    | caller's validated amount (74)              | `achievement:<claimID>`    | own ledger tx          |
| `admin_grant`       | `EarnAdminGrant` (shipped) | operator amount ≤ `twinkle.admin_grant_max` | grant id                   | own ledger tx          |

Every earn credits **`GENERAL`** ([A3][G2] — `SMALL` is the daily reset and is never earned). Every earn is dedup-keyed
and append-first, so a replay skips the balance delta exactly as the shipped `earn` helper already does. `ClaimInvite`
keeps its shipped name, its `InviteResolver` seam and its **own** ledger transaction: running outside the
causing write is precisely what makes [61]'s deferred settlement an idempotent pairing that heals on replay instead of
a cross-context transaction. `EarnAchievementReward` mirrors it for the same reason — `claimed_at` set with the
reward uncredited is recoverable, whereas a shared transaction would make `achievement` the owner of twinkle's tables.

`EarnSignupBonus` and `EarnAchievementReward` are **credit primitives**: they validate scope, amount and key and decide
no eligibility. Whether a signup is valid ([G6] — four-part conjunction, per-inviter cap) is [61]'s; whether an
achievement is achieved is [75]'s.

### The `SMALL` day boundary is the user's zone ([U7])

65 makes the pure derivation take a zone; this unit supplies it. `twinkle.UserZoneReader` is a new consumer-owned port
in `internal/twinkle/ports.go`:

```go
type UserZoneReader interface {
    ZoneFor(ctx context.Context, scope platform.UserScope) (string, error) // IANA name, e.g. "Asia/Seoul"
}
```

**As built this port is [65]'s, not this unit's** — 65's scope and its acceptance criterion A5 claim the port, the
`ErrZoneReaderRequired` guard and the `cmd/api/twinkle.go` adapter, and 65 lands first, so here it is a verification.
It is bound in `cmd/api/twinkle.go` to an adapter over `account`'s published profile read (60) — an IANA **string**, never a
`*User`, so no account type crosses the boundary. An unknown, empty or unparseable zone resolves to `UTC`, and that
fallback is safe in both directions because the stored anchor is the user's local calendar date and
`UpdateTwinkleBalanceDelta` advances it with `GREATEST` only: a wrong zone can delay a refill by at most a day, never
mint a second one ([I10]). The zone is read once per balance derivation; no cache, and no clock port beyond the shipped
`now` seam.

Real time still **paces** the refill and is never **measured**: nothing in this unit counts days, streaks or
elapsed real time.

### `GetLedger`† — the one read [G7] needs

```proto
enum LedgerEntryKind   { LEDGER_ENTRY_KIND_UNSPECIFIED = 0; LEDGER_ENTRY_KIND_EARN = 1; LEDGER_ENTRY_KIND_SPEND = 2; }
enum LedgerEntryReason {
  LEDGER_ENTRY_REASON_UNSPECIFIED = 0; LEDGER_ENTRY_REASON_WRITE_DIARY = 1; LEDGER_ENTRY_REASON_INVITE = 2;
  LEDGER_ENTRY_REASON_INVITE_SIGNUP = 3; LEDGER_ENTRY_REASON_SIGNUP_BONUS = 4;
  LEDGER_ENTRY_REASON_ACHIEVEMENT_CLAIM = 5; LEDGER_ENTRY_REASON_ADMIN_GRANT = 6;
  LEDGER_ENTRY_REASON_RECALL = 7; LEDGER_ENTRY_REASON_GIST_VIEW = 8;
  LEDGER_ENTRY_REASON_ORNAMENT_PURCHASE = 9; LEDGER_ENTRY_REASON_PAYMENT = 10; // historical rows only
}
message GetLedgerRequest  { int32 page_size = 1; string page_token = 2; }   // no user_id — scope is the JWT
message GetLedgerResponse { repeated LedgerEntry entries = 1; string next_page_token = 2; }
message LedgerEntry {
  string id = 1; LedgerEntryKind kind = 2; LedgerEntryReason reason = 3;
  int64 amount = 4; int64 from_small = 5; int64 from_general = 6;
  string occurred_on = 7;  // YYYY-MM-DD, already resolved in User.Timezone
  string occurred_at = 8;  // RFC3339
}
```

**As built, the row carries TWO timestamps, not one `created_at`.** [67]'s design needs a day header, and [U7] makes the
server the day-boundary authority: a device-local grouping would draw headers that disagree with the `SMALL` reset
boundary the same user just watched refill. So `occurred_on` is the entry's date already resolved in the user's zone —
from the same `ResetWindowOf` the refill uses — and `occurred_at` carries the within-day ordering and the time on the
row. 67 explicitly adds no proto field, so a `created_at`-only contract would have forced it to reopen this wire.

Pinned decisions: **no `DAILY_GRANT` member** (the refill is not a row; [67] renders it as a labeled synthetic
day marker from the same derivation); **no `dedup_key` field** (it embeds diary ids, signup ids and claim ids — a leak
with no product use); **no per-reason payload** (an `ornament_id` would put store vocabulary in the twinkle contract;
the reason _is_ the caption and [67] owns the copy); **`from_small`/`from_general` follow 65's domain names** while the
columns stay `from_basic`/`from_additional` (`pg/store.go` is where the two spellings meet); timestamps are
RFC3339 strings, matching the shipped `admin.v1` convention rather than importing `google.protobuf.Timestamp`.

Paging copies the shipped keyset shape, not admin's `LIMIT/OFFSET`: one static sqlc query
`ListTwinkleLedgerPage` in `db/queries/twinkle/ledger.sql` — `WHERE user_id = $1 AND (sqlc.narg(cursor_created_at) IS
NULL OR (created_at, id) < (cursor_created_at, cursor_id)) ORDER BY created_at DESC, id DESC LIMIT $n` — with the
cursor an opaque `base64url("<created_at RFC3339Nano>|<id>")` mirroring `encodeDiaryCursor`
(`internal/memory/diaries.go`). `page_size` clamps to `twinkle.ledger_page_size` as both default and hard cap. Ties on
`created_at` break on the backend-minted id — deterministic, if arbitrary. **As built:** the use-case asks the store for
one row beyond the page, which is how "the history continues" is told apart from "the page ended exactly here" without a
second count query; a fabricated token is refused (`ErrLedgerCursorInvalid`) rather than silently restarting the
history. `GetLedger` is `NO_SIDE_EFFECTS` and is
registered once in `twinkleRpcCachePolicies` (`packages/client-cache/src/http-policy.ts`) with
`userScopedUnaryReadPolicy`; the production transport refuses to start otherwise (§2.7).

### Payment retirement, exhaustive (server half here, client half in [67])

**Removed by this plan.** proto `proto/cosimosi/twinkle/v1/twinkle.proto`: `rpc Charge`, `ChargeRequest{pack_id,
platform, receipt}`, `ChargeResponse{balance_total}` — with their regenerated Go
(`apps/api/internal/gen/cosimosi/twinkle/v1/**`) and TS (`packages/api-client/src/gen/**`).
`internal/twinkle/service.go`: `Service.Charge`, `normalizePaymentProvider`, `paymentTransactionKey`, `const
DefaultChargePackID`, the `Service.verifier` field, `ServiceDeps.Verifier` and its nil guard, and the five errors
`ErrVerifierRequired`, `ErrChargeInputRequired`, `ErrPaymentVerificationUnavailable`,
`ErrPaymentBeneficiaryMismatch`, `ErrPaymentNotVerified`. `internal/twinkle/ports.go`: `PaymentVerificationRequest`,
`VerifiedPayment`, `StorePaymentVerifier`. `internal/twinkle/defaults.go`: `UnavailablePaymentVerifier`.
`cmd/api/twinkle.go`: the `Verifier:` line. `internal/twinkle/rpc/server.go`: the `Charge` handler and its five error
arms; `rpc/reasons.go`: `reasonChargeInputRequired`, `reasonPaymentVerificationUnavailable`,
`reasonPaymentBeneficiaryMismatch`, `reasonPaymentNotVerified`. Tests: the payment cases in
`internal/twinkle/service_test.go`, and `TestProductionTwinkleExternalEarnsFailClosedWithoutAdapters`
(`cmd/api/twinkle_integration_test.go`) narrowed to the invite resolver alone. `spec/values.yaml` is **not** edited
here: removing `twinkle.charge_pack` while `packages/twinkle/src/pack.ts` still reads `VALUES.twinkle.chargePack`
would break the web build, so this unit only drops the Go reader (`values.TwinkleChargePack` in `service.go`) and
[67.twinkle-relabel-and-ledger-ui](67.twinkle-relabel-and-ledger-ui.md) — landing after it — deletes the key and
re-runs `pnpm gen:values`.

**Retained, deliberately.** `ReasonPayment` (historical rows must stay readable and foldable); migration `00010`'s
`twinkle_ledger_payment_transaction_key_unique` partial index and its `DO $$` preflight (the ledger is append-only —
dropping the index buys nothing and re-adding it in v3 costs the same preflight); the `reason = 'payment'` arm of
`TwinkleLedgerDedupExists` and `internal/twinkle/pg/payment_migration_integration_test.go`, both re-commented as
historical guards; `LEDGER_ENTRY_REASON_PAYMENT` on the wire.

**As built, the FE tail had to move WITH the wire.** Deleting `rpc Charge`/`rpc ClaimInvite` breaks both app builds
unless their call sites go too, and 67 lands after this unit — so this unit carried the minimum mechanical amputation
that keeps `pnpm check` green and left every pixel to 67: the `Charge*` re-exports in
`packages/api-client/src/twinkle.ts` (+ its `method.charge` assertion), `chargeTwinkle`/`claimInvite` in
`packages/twinkle/src/charge.ts`, and the two `StardustOverlay.tsx` call sites. `ChargeSheet`'s `onPay`/`onInvite` props
are 67's, so both seams became explicit rejections instead: `startStorePurchase` already threw
`PaymentUnavailableError`, and `claimInvite` was replaced by `redeemInvite`, which throws
`InviteRedemptionUnavailableError` — an invite is link-bound ([U8]), so a typed code has nowhere to go and saying so
beats a silent no-op. No copy, no i18n key, no layout and no state-machine phase changed.

**Owned by [67], listed for completeness:** `packages/twinkle/src/charge.ts` (all but the invite path),
`packages/twinkle/src/pack.ts` (`CHARGE_PACK`) and its `apps/{web,mobile}/src/features/charge-twinkle/index.ts`
re-exports, the `PAY` event + `paying` state in `packages/universe/src/stardust.machine.ts`, the pay `<section>` and
the `onPay`/`paying` props in `apps/{web,mobile}/src/features/charge-twinkle/ui/ChargeSheet.tsx` and
`apps/{web,mobile}/src/widgets/stardust/ui/StardustOverlay.tsx`, the reason codes `twinkleChargeInputRequired` /
`twinklePaymentVerificationUnavailable` / `twinklePaymentBeneficiaryMismatch` / `twinklePaymentNotVerified` in
`packages/errors/src/core.ts`, i18n keys `twinkle_charge_pay_title` / `_body` / `_action` (plus
`twinkle_charge_invite_code_label` / `_placeholder`, deleted because [U8]'s invite is link-bound), and the
`MEMORY_INSUFFICIENT_TWINKLE` / `TWINKLE_INSUFFICIENT` recovery rows in `spec/policy/platform/errors.md`.

**`ClaimInvite` leaves the wire, not the code:** `rpc ClaimInvite`, `ClaimInviteRequest`,
`ClaimInviteResponse` and the `rpc/server.go` handler are removed by this plan; the `Service.ClaimInvite` use-case and
the `InviteResolver` port stay and are called by [61]'s settlement adapter from the launch path. The `TWINKLE_INVITE_*`
transport reasons are retained for that adapter's refusals.

### Migration `00019_twinkle_purchase_spend.sql` (single-owner DDL)

Plan 60 claims `00016` (`users`/`auth_providers`/`invites`) and plan 62 claims `00017` (the withdrawal-sweep
job-target CHECK); plans 61 and 65 introduce no DDL — 65 explicitly forbids a tier-rename migration. **As built:**
`00018` was taken by an out-of-plan memory migration (`00018_episodic_memory_source_text.sql`) after this plan was
written, so this unit took the next free number — exactly what the "take the next free one, only the filename moves"
rule anticipated.

```sql
-- +goose Up
-- [P9] An ornament purchase is GENERAL-only: SMALL (stored as basic) may never fund it. PlanSpend's
-- purpose argument already returns FromSmall: 0 (plan 65) — this is the schema saying the same thing.
ALTER TABLE twinkle_ledger_entries
    ADD CONSTRAINT twinkle_ledger_entries_purchase_general_only
    CHECK (reason NOT IN ('ornament_purchase') OR from_basic = 0);
COMMENT ON COLUMN twinkle_ledger_entries.reason IS
    'closed set (plan 66): daily_grant(reserved, never written) | write_diary | invite | invite_signup | '
    'signup_bonus | achievement_claim | admin_grant | recall | gist_view | ornament_purchase | payment(historical)';

-- +goose Down
ALTER TABLE twinkle_ledger_entries DROP CONSTRAINT twinkle_ledger_entries_purchase_general_only;
```

No column is added, renamed or dropped: `additional`, `basic_spent_this_window`, `basic_reset_window`, `from_basic` and
`from_additional` keep their persistence spelling ([65](65.twinkle-kind-redefinition.md)), and the domain↔column
mapping is documented once in `spec/tech/twinkle-economy.md`. The `basic_reset_window` comment is corrected from "UTC
calendar day" to "the user's local calendar day" by 65; this migration does not restate it.

### Per-user isolation (§4)

`ListTwinkleLedgerPage` is scoped `WHERE user_id = $1` over a single relation, so
`scripts/check-persistence-isolation.mjs` accepts it under the existing `twinkle/ledger.sql#…` key shape; no
`globalQueries` entry is added. `GetLedgerRequest` carries no `user_id` — scope comes from
`platform.UserScope`, which is why a cross-user read is unrepresentable rather than validated.

### Golden-parity stance

**Nothing new here is golden-parity, and this unit defines no pure function.** The mirrored math — `SmallRemaining`,
`PlanSpend`, `RecallCost`, `GistViewCost` — lives in `internal/twinkle/ledger.go` ↔
`packages/twinkle-logic/src/stardust-ledger.ts`, pinned by
`apps/api/internal/twinkle/testdata/stardust-ledger-golden.json`, and 65 owns both the signature change and the
fixture regeneration (including the purchase-branch cases). This unit's logic is BE-only orchestration: earn routing,
dedup keys, the purchase amount pass-through, the ledger page, the zone port. The client reads results (`GetBalance`,
`QuoteSpend`, `GetLedger`) and never recomputes an earn or a debit (§2.9 #8). The purchase affordability _preview_ the
decoration panel renders is 65's `planSpend(..., purchase)` over [71]'s catalog total — a mirror that already exists.

## Acceptance Criteria

1. `twinkle.EntryReason` is exactly eleven values — `daily_grant`, `write_diary`, `invite`,
   `invite_signup`, `signup_bonus`, `achievement_claim`, `admin_grant`, `recall`, `gist_view`, `ornament_purchase`,
   `payment` — defined in one place, each writable reason reachable from exactly one named use-case entry point and
   `daily_grant`/`payment` reachable from none. A test enumerates the whole set, asserts each reason's earn/spend
   classification and fails on a twelfth. ([G3][G7])
2. A `SpendIntent` is constructible only through `RecallSpendIntent` / `GistViewSpendIntent` /
   `PurchaseSpendIntent`; a recall or gist-view intent has **no field** for an amount and a purchase intent has **no
   field** for a decay depth or gist stage. ([G4])
3. An ornament purchase spend draws `FromSmall = 0` for every input, and migration `00019`'s
   `CHECK (reason NOT IN ('ornament_purchase') OR from_basic = 0)` rejects a hand-crafted `INSERT` that says otherwise.
   ([P9][G2a])
4. `CheckAndSpend` with a purchase intent appends one `ornament_purchase` spend row and applies the `GENERAL` delta
   **inside the caller's transaction**; a rolled-back `Decorate` leaves no ledger row and no balance change.
   ([G7][P8])
5. An unaffordable spend writes nothing and returns a typed denial carrying `{cost, eligible, shortfall}` — `eligible`
   is `general` for a purchase and `small + general` for the recall family — which still satisfies
   `errors.Is(err, ErrInsufficientTwinkle)`; `QuoteSpend` reports the same tier-aware shortfall. ([G1][P8])
6. `EarnAchievementReward(scope, claimID, amount)` credits `GENERAL` exactly once per `claimID` under dedup key
   `achievement:<claimID>` in its own ledger transaction; a replay returns the current balance with no second credit.
   ([A3])
7. `EarnSignupBonus(scope)` credits `twinkle.earn_signup_bonus` to `GENERAL` exactly once per user under
   `signup_bonus:<userID>` and validates no eligibility. ([U11])
8. `ClaimInvite` credits the inviter as reason `invite` (`invite:<signupID>`) and the invitee as reason
   `invite_signup` (`invite_signup:<signupID>`), atomically and exactly once per signup; an unavailable or refusing
   resolver credits neither side. ([G3][G6])
9. No earn path writes a `daily_grant` row, no scheduled job or cron is added, and the wire enum has **no**
   `DAILY_GRANT` member; the `SMALL` tier stays a lazy derivation from the stored anchor. ([G7][T4])
10. The `SMALL` reset boundary is the user's IANA zone read through the consumer-owned `UserZoneReader` bound at
    `cmd/api`; an unknown or unparseable zone resolves to `UTC`, and no zone change or fallback ever produces a second
    grant within one local day. ([U7][I10])
11. `GetLedger` returns the user's ledger newest-first with an opaque keyset cursor, clamps `page_size` to
    `twinkle.ledger_page_size` as default **and** hard cap, is `NO_SIDE_EFFECTS`, is classified exactly once in
    `packages/client-cache/src/http-policy.ts`, and exposes no `dedup_key` and no per-reason payload. ([G7][U9])
12. `rpc Charge`, `ChargeRequest`, `ChargeResponse`, `Service.Charge`, `StorePaymentVerifier`,
    `PaymentVerificationRequest`, `VerifiedPayment`, `UnavailablePaymentVerifier`, `DefaultChargePackID`,
    `normalizePaymentProvider`, `paymentTransactionKey`, the five payment errors, the four payment transport reasons
    and `twinkle.charge_pack` exist nowhere in `apps/api`, `proto/`, `spec/values.yaml` or the generated clients.
    (PRD §8.3)
13. `ReasonPayment`, migration `00010`'s partial uniqueness index, the `reason = 'payment'` dedup arm and
    `LEDGER_ENTRY_REASON_PAYMENT` are retained, and a pre-existing `payment` row still folds into the balance and
    renders in `GetLedger`. ([I1])
14. `rpc ClaimInvite` and its messages are gone from the wire while `Service.ClaimInvite` and the `InviteResolver` port
    remain callable from the composition root.
15. `twinkle` imports neither `memory`, `store` nor `achievement`; the purchase amount, the reward amount, the signup
    identity and the zone all arrive as scalars through consumer-owned ports; handlers stay thin (map + call).
    (ARCHITECTURE §2.2/§2.4/§2.9 #4/#6/#7)
16. Every new statement is user-scoped and `pnpm lint:persistence`, `pnpm lint:language`, `pnpm check:gen`,
    `go vet`, `buf lint` and the twinkle suites pass. (§4)

## Policy / Values Impact

- **policy / ux:** the rules that **will be true once implemented** land in the existing
  `spec/policy/domain/twinkle-economy.md` (the doc [43](43.stardust-ledger.md)/[44](44.earn-spend-usecase.md) seeded):
  (1) "the earn set is 매일 지급 · 일기 작성 · 업적 · 친구 초대 · 가입 보너스 — there is no payment path and no
  attendance bonus in v2"; (2) "`GENERAL` funds every purpose, `SMALL` funds the recall family only, and an ornament
  purchase draws zero `SMALL`"; (3) "every economic _event_ is one append-only ledger row; the daily `SMALL` refill is
  a derivation, not a row"; (4) "an earn is idempotent per its dedup key and credits `GENERAL` only". Written at
  `/implement-job`; **do not pre-create or pre-edit**. `spec/policy/platform/errors.md`'s payment-recovery rows are
  [67]'s edit, not this plan's.
- **tech:** the as-built record extends `spec/tech/twinkle-economy.md` — the reason set and its single-owner rule, the
  three spend-intent constructors, the purchase debit and its DB CHECK, the two new earn entry points with their dedup
  keys, the `UserZoneReader` seam, the `GetLedger` keyset contract, and the payment-retirement inventory with the
  domain↔column mapping (`small↔basic*`, `general↔additional`). Written at `/implement-job`; do not pre-create. The
  nouns `LedgerEntry` and `EntryReason` are registered in `spec/ubiquitous-language.md` §1 (the `Twinkle` row's
  supporting vocabulary) **before** the code lands.
- **spec/values.yaml** (group `twinkle` — regenerated with `pnpm gen:values`):
  - `twinkle.ledger_page_size` — rows per `GetLedger` page, both the server default and the hard cap ([G7][U9]);
    confirmed at implement. **The only key this unit declares.**
  - **Retiring `twinkle.charge_pack`:** this unit removes the **Go** consumer —
    `values.TwinkleChargePack` in `apps/api/internal/twinkle/service.go` (the `Charge` pack-amount check) and its
    tests — but does **not** delete the key, because `packages/twinkle/src/pack.ts` still reads
    `VALUES.twinkle.chargePack` until [67](67.twinkle-relabel-and-ledger-ui.md) deletes that file. The single
    `spec/values.yaml` edit and the `pnpm gen:values` re-run are [67]'s, which lands last
    ([00.overview](00.overview.md) §5.6).
  - **Reused, not re-declared here:** `twinkle.small_daily_amount` (renamed by [65]), `twinkle.earn_write`,
    `twinkle.earn_invite_inviter`, `twinkle.earn_invite_invitee`, `twinkle.admin_grant_max`, the recall/gist cost-curve
    keys (all [43](43.stardust-ledger.md)/[44](44.earn-spend-usecase.md)),
    `twinkle.earn_signup_bonus` and `twinkle.invite_reward_max_per_inviter` (both declared by
    [61](61.signup-and-invite-usecase.md), which lands first and whose [U11]/[G6] requirements introduce them;
    `EarnSignupBonus` here only reads the first), the `store` kind prices (`store.background_price` / `store.star_shader_price`, [71](71.ornament-catalog-model.md)),
    `achievement.reward_tier_*` ([74](74.achievement-catalog-model.md)).
  - **Deliberately NOT values:** the `EntryReason` set and its earn/spend classification (a closed domain set — table
    content); the ornament price table and the achievement reward table (context content); the dedup-key formats;
    the cursor encoding; every DDL name and CHECK expression; the proto contract and its enum members; the
    `SmallEligible` reason mapping (a formula over the set); Go/TS identifiers.
- **Dependencies:** builds on [43.stardust-ledger](43.stardust-ledger.md),
  [44.earn-spend-usecase](44.earn-spend-usecase.md), [65.twinkle-kind-redefinition](65.twinkle-kind-redefinition.md),
  [60.account-profile-model](60.account-profile-model.md). Consumed by
  [67.twinkle-relabel-and-ledger-ui](67.twinkle-relabel-and-ledger-ui.md), [72.decorate-usecase](72.decorate-usecase.md),
  [75.achievement-tracking-usecase](75.achievement-tracking-usecase.md); paired with
  [61.signup-and-invite-usecase](61.signup-and-invite-usecase.md).
