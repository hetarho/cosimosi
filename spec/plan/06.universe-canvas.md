# 06. 우주 캔버스 — WebGPU 렌더러·Bloom·카메라 셸

> R3F `<Canvas>` + three `WebGPURenderer`(WebGL2 자동 폴백) + 노드 기반 Bloom + 씬 셸 + 카메라 모드(nebula/recall)를 더미 데이터로 검증한다. | 범위: MVP | 선행: — | 차단: 08, 09

## 목적 (Why)
엔그램 우주를 그릴 **렌더링 토대**를 깐다. 별·시냅스(08/09)·force-sim(07)·데이터 연동(10)을 얹기 전에, WebGPU 렌더러 비동기 생성과 WebGL2 폴백, Bloom 후처리, 카메라 모드 전환이 더미 점만으로도 안정적으로 동작함을 먼저 확정한다. 또한 모바일 이식을 위한 **렌더러 추상화 경계(`GraphRenderer` 포트)** 와 순수/플랫폼 레이어 분리를 처음부터 박는다(원칙 4).

## 범위
- **포함:**
  - `shared/lib/r3f/` — `WebGPURenderer` 비동기 생성·`await renderer.init()`·WebGPU 미지원 시 WebGL2 자동 폴백, `<Canvas gl={...}>` 팩토리.
  - `shared/lib/r3f/` — **도메인 중립 `GraphRenderer` 포트 타입** 및 그래프 모델 타입(`StarNode`/`SynapseEdge`) 정의 — `three`/React 미의존(원칙 4).
  - `widgets/universe-canvas/ui/UniverseCanvas.tsx` — `<Canvas>` 셸, 짙은 남색 배경, 옅은 "별 먼지"(ambient point cloud), 조명, 카메라 컨트롤, Bloom 노드 후처리, 더미 점 렌더.
  - `widgets/universe-canvas/model/use-camera-mode.ts` — Zustand 카메라 모드 스토어(`nebula`=줌 제한 전체 조망 / `recall`=자유 근접 항해).
  - three.js **노드 기반 후처리(`PostProcessing` + `bloom` 노드)** 로 Bloom 적용(`@react-three/postprocessing` 미사용·제거).
  - `pages/home`에 임시 마운트하여 시각 검증.
- **비목표(Out of scope):**
  - 실제 별 인스턴스 렌더·TSL 머티리얼 → **08(star-rendering)**.
  - 시냅스(Line2) 렌더 → **09(synapse-rendering)**.
  - force-directed 레이아웃 `tick`·Worker → **07(force-sim)**.
  - 서버 `GetUniverse` 데이터 바인딩 → **10(record-memory-ui)** 이후.
  - 씬 내부 DOM `Html`/HUD(원칙 4 — 모바일 비이식). 라벨·HUD는 별도 2D widget으로 차후 분리.

## 참고
- spec/concept.md §"우주의 모습"(별 먼지·배경), §"우주 탐험(회상)"(성운 View=전체 조망 / 회상 View=근접 항해, 전환은 카메라 이동), §"우주의 배경"(짙은 색조).
- spec/Architecture.md §2.7(렌더링 스택 — WebGPURenderer·WebGL2 폴백·노드 Bloom·`gl` 비동기 생성·씬 내 DOM 금지), §2.8(시뮬·렌더 분리·렌더러 격리 port), §2.9(R3F 컴포넌트 배치 — `widgets/universe-canvas/ui/UniverseCanvas.tsx`).
- 코드 기준점: `frontend/src/pages/home/ui/HomePage.tsx`(현재 placeholder), `frontend/package.json`(three 0.184, R3F 9.6, `@react-three/postprocessing` 제거 대상), `frontend/vite.config`(@→`frontend/src`, 포트 1214).

## 설계 요점
**렌더러 격리(원칙 4):** 순수 레이어(`shared/lib/r3f/types.ts`)는 `three`/React를 import하지 않는다. 플랫폼 구현(`renderer.ts`, `UniverseCanvas.tsx`)에만 `three`/R3F를 가둔다.

```ts
// shared/lib/r3f/types.ts  — 순수, three/React 미의존 (모바일 재사용)
export interface StarNode { id: string; intensity: number; mood: string }
export interface SynapseEdge { aId: string; bId: string; weight: number }
export type CameraMode = 'nebula' | 'recall'
// 렌더러 추상화 경계: 웹은 R3F/WebGPU, 모바일은 WebGPU/Filament로 교체
export interface GraphRenderer {
  setGraph(nodes: StarNode[], edges: SynapseEdge[]): void
  setCameraMode(mode: CameraMode): void
  dispose(): void
}
```

```ts
// shared/lib/r3f/renderer.ts — three 의존 (플랫폼 레이어)
import * as THREE from 'three/webgpu'
// R3F <Canvas gl={createRenderer}> 에 넘기는 비동기 팩토리.
// WebGPURenderer는 WebGPU 미지원 환경에서 내부적으로 WebGL2 백엔드로 자동 폴백.
export async function createRenderer(props: { canvas: HTMLCanvasElement }) {
  const renderer = new THREE.WebGPURenderer({ canvas: props.canvas, antialias: true })
  await renderer.init()                 // ← 비동기 init 필수 (§2.7)
  return renderer
}
export function rendererBackend(r: THREE.WebGPURenderer): 'webgpu' | 'webgl2' {
  return (r.backend?.isWebGPUBackend) ? 'webgpu' : 'webgl2'  // 콘솔 로그용
}
```

```tsx
// widgets/universe-canvas/ui/UniverseCanvas.tsx (요지)
<Canvas gl={createRenderer as never} flat>      {/* gl=비동기 WebGPU 팩토리 */}
  <color attach="background" args={['#070b1e']} /> {/* 짙은 남색 */}
  <ambientLight intensity={0.4} />
  <StarDust count={1500} />                         {/* 옅은 별 먼지 (Points) */}
  <DummyStars />                                    {/* 검증용 더미 점 — 08에서 대체 */}
  <CameraRig />                                     {/* use-camera-mode 구독 */}
  <BloomPass />                                     {/* 노드 PostProcessing + bloom */}
</Canvas>
```

```ts
// model/use-camera-mode.ts — Zustand. widget 상태(원칙: 스토어는 model/에).
// nebula: 전체 조망. OrbitControls min/maxDistance로 줌 제한.
// recall : 근접 항해. 줌 제한 해제(자유 이동).
type State = { mode: CameraMode; setMode(m: CameraMode): void; toggle(): void }
```

**Bloom(노드 후처리):** `useFrame`에서 `gl.renderAsync()` 대신 `PostProcessing.renderAsync()`를 구동하거나, three `PostProcessing` + `pass(scene, camera)` + `bloom(...)` 노드를 `outputNode`로 구성. WebGPU/WebGL2 양쪽에서 동작해야 한다. emissive 강한 더미 점에 발광이 보여야 한다.

**성능 전제(§2.8):** 더미 점도 `Points`/`InstancedMesh`로. React state로 60fps 구동 금지 — 카메라/포스트만 `useFrame`에서 갱신.

## 영향 파일
- 신규 `frontend/src/shared/lib/r3f/types.ts` — `StarNode`/`SynapseEdge`/`CameraMode`/`GraphRenderer` (순수).
- 신규 `frontend/src/shared/lib/r3f/renderer.ts` — `createRenderer`(비동기·폴백)·`rendererBackend`.
- 신규 `frontend/src/shared/lib/r3f/index.ts` — public API 배럴.
- 신규 `frontend/src/widgets/universe-canvas/ui/UniverseCanvas.tsx` — Canvas 셸·배경·별 먼지·더미 점·Bloom·카메라 리그.
- 신규 `frontend/src/widgets/universe-canvas/ui/BloomPass.tsx` — 노드 기반 Bloom 후처리 컴포넌트.
- 신규 `frontend/src/widgets/universe-canvas/model/use-camera-mode.ts` — Zustand 카메라 모드 스토어.
- 신규 `frontend/src/widgets/universe-canvas/index.ts` — public API 배럴.
- 수정 `frontend/src/pages/home/ui/HomePage.tsx` — `<UniverseCanvas/>` 임시 마운트(전체 화면).
- 수정 `frontend/package.json` — `@react-three/postprocessing` 의존 제거.

## 수용 기준 (EARS / Given-When-Then)
1.1 WHEN 사용자가 홈(`/`)에 진입 THE SYSTEM SHALL `WebGPURenderer`를 비동기 생성·`init()` 후 전체 화면 `<Canvas>`를 렌더한다.
1.2 IF 브라우저가 WebGPU를 미지원 THEN THE SYSTEM SHALL 예외 없이 **WebGL2 백엔드로 자동 폴백**하여 동일 씬을 렌더하고, 콘솔에 사용 중인 백엔드(`webgpu`|`webgl2`)를 1회 로그한다.
1.3 WHEN 씬이 렌더됨 THE SYSTEM SHALL 짙은 남색(`#070b1e`) 배경과 옅은 별 먼지(point cloud)를 표시한다.
1.4 WHEN 더미 점이 그려짐 THE SYSTEM SHALL 노드 기반 Bloom 후처리를 적용해 발광(glow)이 보이게 한다.
1.5 GIVEN 카메라 모드가 `nebula` WHEN 사용자가 줌 THE SYSTEM SHALL `min/maxDistance`로 줌 범위를 제한(전체 조망)한다.
1.6 GIVEN 카메라 모드가 `recall` WHEN 사용자가 줌·이동 THE SYSTEM SHALL 줌 제한을 해제하여 자유 근접 항해를 허용한다.
1.7 WHEN 캔버스가 마운트·언마운트 THE SYSTEM SHALL 콘솔 에러·경고 없이 동작하고 언마운트 시 렌더러를 `dispose`한다.
1.8 THE SYSTEM SHALL 씬 내부에 DOM `Html`을 넣지 않는다(원칙 4 — 모바일 이식성).
1.9 THE SYSTEM SHALL `shared/lib/r3f/types.ts`에서 `three`/React/DOM을 import하지 않는다(원칙 4 — 순수 레이어).

## 작업 (Tasks)

### Phase 1 — Setup
- [ ] T001 `frontend/package.json`에서 `@react-three/postprocessing` 의존 제거 후 `pnpm install`로 락 갱신. _참조: 1.4_
- [ ] T002 [P] `frontend/src/shared/lib/r3f/types.ts` 신규 — `StarNode`/`SynapseEdge`/`CameraMode`/`GraphRenderer` 정의(three/React import 금지). _참조: 1.9_

### Phase 2 — 렌더러·스토어
- [ ] T003 [P] `frontend/src/shared/lib/r3f/renderer.ts` 신규 — `three/webgpu`의 `WebGPURenderer` 비동기 생성·`await init()` 팩토리 `createRenderer`, 백엔드 판별 `rendererBackend`. _참조: 1.1, 1.2_
- [ ] T004 `frontend/src/shared/lib/r3f/index.ts` 신규 — `createRenderer`/`rendererBackend`/타입 재export 배럴. _참조: 1.1_
- [ ] T005 [P] `frontend/src/widgets/universe-canvas/model/use-camera-mode.ts` 신규 — Zustand `mode`/`setMode`/`toggle`(기본 `nebula`). _참조: 1.5, 1.6_

### Phase 3 — 씬 셸·후처리
- [ ] T006 `frontend/src/widgets/universe-canvas/ui/BloomPass.tsx` 신규 — three `PostProcessing` + `bloom` 노드로 후처리 구성(WebGPU/WebGL2 공통). _참조: 1.4_
- [ ] T007 `frontend/src/widgets/universe-canvas/ui/UniverseCanvas.tsx` 신규 — `<Canvas gl={createRenderer}>`, 배경 `#070b1e`, 별 먼지 `Points`(≈1500), 발광 더미 점, `ambientLight`, `OrbitControls` 카메라 리그(`use-camera-mode` 구독), `<BloomPass/>` 합성, 마운트 시 `rendererBackend` 콘솔 1회 로그, 언마운트 `dispose`. 씬 내 DOM `Html` 금지. _참조: 1.1, 1.3, 1.4, 1.5, 1.6, 1.7, 1.8_
- [ ] T008 `frontend/src/widgets/universe-canvas/index.ts` 신규 — `UniverseCanvas`·`useCameraMode` public API 배럴. _참조: 1.1_

### Phase 4 — 마운트·검증
- [ ] T009 `frontend/src/pages/home/ui/HomePage.tsx` 수정 — placeholder를 전체 화면 `<UniverseCanvas/>`로 교체(임시). _참조: 1.1_
- [ ] T010 `pnpm --filter frontend dev`로 `http://localhost:1214` 진입, WebGPU/WebGL2 렌더·Bloom·카메라 모드 전환·콘솔 무에러 육안 검증. _참조: 1.1–1.7_

## 완료 정의 (DoD)
- `pnpm dev:web`(또는 `pnpm --filter frontend dev`) 실행 후 `http://localhost:1214` 진입 시 짙은 남색 우주에 별 먼지 + 발광 더미 점이 표시된다.
- 콘솔에 사용 중인 백엔드가 `webgpu` 또는 `webgl2`로 1회 로그되고, **에러·경고가 없다**(1.7).
- WebGPU 비활성 환경(예: `chrome://flags`에서 WebGPU off 또는 폴백 강제)에서도 동일 씬이 WebGL2로 렌더된다(1.2).
- 카메라 모드를 `recall`↔`nebula`로 토글하면 줌 제한이 해제/적용된다(1.5, 1.6).
- `pnpm --filter frontend build` 통과(타입·번들 무오류), `pnpm --filter frontend lint` 통과.
- `package.json`에 `@react-three/postprocessing`이 더 이상 없다(`grep` 0건).
- `shared/lib/r3f/types.ts`에 `three`/`react`/DOM import가 없다(원칙 4, 1.9 — `grep` 0건).
