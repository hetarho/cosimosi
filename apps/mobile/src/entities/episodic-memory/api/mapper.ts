// The slice's proto→domain mapper seam (§3.4), re-exported from the shared package so
// web and mobile map the GetUniverse DTOs identically (§3.5).
export {episodicMemoryFromDto} from '@cosimosi/memory';
