// Panel presentation constants (widget config/ segment, code-level — none is cross-surface
// tunable, so no values group, per the universe-canvas precedent). The side-sheet's width over
// the running canvas.
export const STAR_DETAIL_PANEL = {
  widthRem: 22,
} as const
