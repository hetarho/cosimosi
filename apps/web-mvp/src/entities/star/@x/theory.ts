// Cross-import public API (FSD `@x`) — entities/theory 전용. 이론 미니 비주얼이 별을
// 랜딩 카드와 같은 시각 언어(VizStar)로 그릴 수 있게 노출한다.
export { VizStar, type VizStarProps } from '../ui/VizStar'
export type { StarObject } from '../model/types'
