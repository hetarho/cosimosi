# Foundation dev image: runs the api with `go run`. docker-compose bind-mounts the
# source into /app and persists the module cache in a volume, so no COPY here.
FROM golang:1.26
WORKDIR /app
EXPOSE 8080
CMD ["go", "run", "./cmd/api"]
