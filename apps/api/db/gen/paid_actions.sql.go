// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: paid_actions.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getPaidActionReceipt = `-- name: GetPaidActionReceipt :one

SELECT action_kind, request_fingerprint, response
FROM memory_paid_action_receipts
WHERE user_id = $1 AND operation_id = $2
`

type GetPaidActionReceiptParams struct {
	UserID      string
	OperationID string
}

type GetPaidActionReceiptRow struct {
	ActionKind         string
	RequestFingerprint string
	Response           []byte
}

// Paid-action idempotency receipts (A2/A3). Every query is user-scoped ([U1]); the recall/gist-view
// transaction takes the per-user graph advisory lock (LockGraphMutation) before the lookup, so
// concurrent duplicates of one operation serialize — the loser reads the winner's committed receipt
// instead of doing the work twice.
// The receipt lookup by client operation id. Absent → pgx.ErrNoRows (the use-case treats it as "no
// prior commit, do the work"). A present row's action_kind + request_fingerprint are compared in Go:
// an exact match replays `response`; a mismatch (same id, different canonical input) is a conflict.
func (q *Queries) GetPaidActionReceipt(ctx context.Context, arg GetPaidActionReceiptParams) (GetPaidActionReceiptRow, error) {
	row := q.db.QueryRow(ctx, getPaidActionReceipt, arg.UserID, arg.OperationID)
	var i GetPaidActionReceiptRow
	err := row.Scan(&i.ActionKind, &i.RequestFingerprint, &i.Response)
	return i, err
}

const insertPaidActionReceipt = `-- name: InsertPaidActionReceipt :exec
INSERT INTO memory_paid_action_receipts (
    user_id,
    operation_id,
    action_kind,
    request_fingerprint,
    episodic_memory_id,
    diary_id,
    response
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7
)
`

type InsertPaidActionReceiptParams struct {
	UserID             string
	OperationID        string
	ActionKind         string
	RequestFingerprint string
	EpisodicMemoryID   pgtype.Text
	DiaryID            pgtype.Text
	Response           []byte
}

// The commit-time receipt write, in the same transaction as the debit + effects (A3). Exactly one
// of episodic_memory_id / diary_id is set (the table CHECK), tying the receipt to its retained
// target for cascade cleanup. A plain INSERT is safe: the graph lock guarantees single-writer per
// user, and the lookup above already proved no receipt exists for this operation.
func (q *Queries) InsertPaidActionReceipt(ctx context.Context, arg InsertPaidActionReceiptParams) error {
	_, err := q.db.Exec(ctx, insertPaidActionReceipt,
		arg.UserID,
		arg.OperationID,
		arg.ActionKind,
		arg.RequestFingerprint,
		arg.EpisodicMemoryID,
		arg.DiaryID,
		arg.Response,
	)
	return err
}
